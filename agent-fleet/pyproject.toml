[project]
name = "agent-fleet"
version = "0.1.0"
description = "Autonomous agent fleet driven by GitHub Issues"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:api"
