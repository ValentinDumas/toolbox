---
phase: 08-gap-fiche-echeance-tracking
plan: 01
type: tdd
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/web/routes/echeances.ts
  - src/main.ts
  - src/web/views/pages/echeances/detail.ejs
  - src/web/views/pages/dashboard/accueil.ejs
  - src/web/views/pages/impayes/liste.ejs
  - tests/integration/web/route-fiche-echeance.test.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [ENC-04, ENC-05, DAS-01]
user_setup: []

must_haves:
  truths:
    - "GET /echeances/:id retourne 200 et rend une fiche échéance (période, total, reste dû, statut, locataire) pour un id existant ; 404 pour un id inconnu."
    - "La fiche liste les encaissements de l'échéance et propose l'action relance disponible (partial relance-action) + un CTA « Saisir un encaissement » vers /encaissements/nouveau?echeance=:id."
    - "Aucun des 3 CTA dashboard/impayés ne pointe plus vers une cible 404 : accueil.ejs S3 (Saisir l'encaissement), accueil.ejs S4 (Lancer la relance), impayes/liste.ejs (Voir l'échéance) mènent à GET /echeances/:id."
    - "REQUIREMENTS.md et la table Progress du ROADMAP reflètent l'état réel sur disque."
  artifacts:
    - path: "src/web/routes/echeances.ts"
      provides: "Route GET /echeances/:id rendant pages/echeances/detail.ejs"
      contains: "/echeances/:id"
    - path: "src/web/views/pages/echeances/detail.ejs"
      provides: "Vue fiche échéance"
    - path: "tests/integration/web/route-fiche-echeance.test.ts"
      provides: "Tests 200 (échéance existante) + 404 (id inconnu)"
      contains: "/echeances/"
  key_links:
    - from: "src/web/views/pages/dashboard/accueil.ejs"
      to: "GET /echeances/:id (echeances.ts)"
      via: "href interpolé /echeances/${echeanceId}"
      pattern: "/echeances/"
    - from: "src/web/routes/echeances.ts (GET /echeances/:id)"
      to: "calculerRelanceDisponible(echeance, relancesActives, today) + encaissementRepo.listerParEcheance"
      via: "deps encaissementRepo + relanceRepo ajoutées au plugin echeances"
      pattern: "calculerRelanceDisponible"
---

<objective>
Fermer le **blocker d'intégration v1.0** remonté par l'audit milestone (`.planning/v1.0-MILESTONE-AUDIT.md`,
finding ROUTE-ECHEANCES-DETAIL) : la route `GET /echeances/:id` (« fiche échéance ») a été spécifiée
dans `02-UI-SPEC.md:117` mais jamais livrée. Le dashboard P7 et la liste impayés P2 y pointent comme
CTA principal → 404 :

- `src/web/views/pages/dashboard/accueil.ejs:60` — « Saisir l'encaissement » (S3 impayés) → 404
- `src/web/views/pages/dashboard/accueil.ejs:89` — « Lancer la relance » (S4 actions du jour) → 404 (aucun chemin de secours)
- `src/web/views/pages/impayes/liste.ejs:94` — « Voir l'échéance » → 404

Le chemin de données est prouvé : `/encaissements/nouveau?echeance=<id>` et le partial `relance-action`
(POST /relances) fonctionnent déjà. On livre la page canonique `GET /echeances/:id` comme cible unique
des 3 CTA, puis on réconcilie le drift de suivi (REQUIREMENTS.md + ROADMAP Progress).

Purpose : faire passer ENC-04, ENC-05, DAS-01 de « partiel » à « satisfait » et lever le seul blocker
fonctionnel de l'audit v1.0, sans replanifier.
Output : route + vue fiche échéance testées (200/404), 3 CTA réorientés, docs de suivi alignées sur le disque.
</objective>

<execution_context>
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/workflows/execute-plan.md
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.0-MILESTONE-AUDIT.md
@CLAUDE.md
@practices/BDD_PRACTICES.md
@practices/DDD.md
@practices/ACCESSIBILITY.md
@practices/UI_DESIGN.md

<interfaces>
<!-- Contrats vérifiés dans le codebase. Implémenter CONTRE eux — peu d'exploration nécessaire. -->

From src/web/routes/echeances.ts (plugin existant — opts à ÉTENDRE) :
  export async function plugin(app, opts: {
    bailRepo; bienRepo; locataireRepo; echeanceLoyerRepo; bailleurRepo; pdfRenderer; clock;
  })
  // Routes existantes : GET /echeances, GET /baux/:id/echeances, GET /echeances/:id/avis-pdf,
  //   GET+POST /baux/:id/activer.
  // IMPORTANT : enregistrer GET /echeances/:id AVANT GET /echeances/:id/avis-pdf n'est pas requis
  //   (chemins distincts), mais NE PAS capturer avis-pdf : la nouvelle route est exactement '/echeances/:id'.

From src/domain/encaissements/echeance-loyer.ts :
  class EcheanceLoyer { readonly id; readonly bailId: BailId; readonly periodeDebut; readonly periodeFin;
    readonly jourEcheanceAttendue; readonly loyerHc: Money; readonly montantCharges: Money;
    readonly modeCharges; readonly total: Money; readonly statut: StatutEcheanceLoyer; readonly annuleLe; }

From src/domain/encaissements/echeance-loyer-repository.ts :
  trouverParId(id): Promise<EcheanceLoyer | null>

From src/domain/encaissements/encaissement-repository.ts :
  listerParEcheance(echeanceId, opts?: { inclureAnnules?: boolean }): Promise<Encaissement[]>

From src/domain/encaissements/relance-repository.ts :
  listerParEcheance(echeanceId): Promise<Relance[]>   // cf. usage impayes.ts:62

From src/application/encaissements/calculer-relance-disponible.ts :
  calculerRelanceDisponible(echeance, relancesActives, today): 1 | 2 | 3 | null
  // pattern de calcul du niveau relance — déjà utilisé dans impayes.ts:63

From src/web/views/partials/relance-action.ejs :
  // attend { echeanceId: string, niveauDisponible: 1|2|3|null } — rend le bon bouton POST /relances

From src/main.ts:275-283 (enregistrement plugin echeances — bloc à ÉTENDRE) :
  await app.register(echeancesPlugin, { bailRepo, bienRepo: repo, locataireRepo,
    echeanceLoyerRepo, bailleurRepo, pdfRenderer, clock });
  // encaissementRepo et relanceRepo existent déjà dans main.ts (passés à impayesPlugin ligne 309+,
  //  racinePlugin ligne 250). Les AJOUTER au bloc echeancesPlugin.

From src/web/routes/impayes.ts (pattern de référence niveau relance) :
  const relancesActives = await opts.relanceRepo.listerParEcheance(impaye.echeanceId);
  niveauxDisponibles[...] = calculerRelanceDisponible(echeance, relancesActives, today);

Reste dû d'une échéance : total − somme(encaissements non annulés). Réutiliser la logique existante
(cf. src/domain/encaissements/impaye.ts listerImpayes qui calcule resteDu) ; si pas de helper réutilisable
simplement, sommer les Encaissement non annulés et soustraire de echeance.total via Money.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (TDD) : livrer GET /echeances/:id (fiche échéance) + vue + câblage deps</name>
  <files>tests/integration/web/route-fiche-echeance.test.ts, src/web/routes/echeances.ts, src/main.ts, src/web/views/pages/echeances/detail.ejs</files>
  <read_first>
    - src/web/routes/echeances.ts (plugin + opts ; routes existantes ; pattern reply.view)
    - src/web/routes/impayes.ts (calcul niveauDisponible + deps encaissementRepo/relanceRepo)
    - src/main.ts:275-283 (bloc echeancesPlugin) + lignes où encaissementRepo/relanceRepo sont déjà construits
    - src/web/views/pages/echeances/liste.ejs (layout/partials EJS, navActive, bannières)
    - src/web/views/partials/relance-action.ejs (variables attendues)
    - tests/integration/web/relances-mailto.test.ts + route-cfe.test.ts (pattern app.inject : build app, inject GET, assert statusCode/payload)
    - src/domain/encaissements/echeance-loyer.ts + encaissement-repository.ts + relance-repository.ts
    - 02-UI-SPEC.md (référence fiche échéance, si présent dans .planning/phases/02-*)
  </read_first>
  <behavior>
    - GET /echeances/:id avec id existant → 200, rend pages/echeances/detail.ejs avec : échéance (période,
      total, reste dû, statut), locataire (nom complet) + repère bail, liste des encaissements de l'échéance,
      partial relance-action (niveauDisponible calculé via calculerRelanceDisponible), CTA « Saisir un
      encaissement » → /encaissements/nouveau?echeance=:id, et lien « Télécharger l'avis (PDF) » → /echeances/:id/avis-pdf.
    - GET /echeances/:id avec id inconnu → 404 (message « Échéance introuvable. », cohérent avec avis-pdf:233).
    - Si le bail ou le locataire de l'échéance est introuvable → 404 (données incomplètes), comme le pattern avis-pdf.
  </behavior>
  <action>
    RED — Créer tests/integration/web/route-fiche-echeance.test.ts (vitest + app.inject, sur le modèle de
    route-cfe.test.ts). Construire une fixture : un bail activé avec au moins une échéance en_attente
    (réutiliser les builders/seeds existants des tests web ; sinon créer via les repos en mémoire/SQLite test).
    Asserter :
      (a) GET /echeances/{idExistant} → statusCode 200 ET le payload contient la période et le statut de l'échéance,
          le nom du locataire, et la chaîne « Saisir un encaissement ».
      (b) GET /echeances/{idInconnu} → statusCode 404.
    Lancer le test → il DOIT échouer en RED (la route n'existe pas encore → 404 sur l'id existant).

    GREEN —
    (1) src/web/routes/echeances.ts : ajouter `encaissementRepo: EncaissementRepository` et
        `relanceRepo: RelanceRepository` au type `opts` du plugin (+ imports type). Ajouter la route
        `app.get('/echeances/:id', ...)` : charger echeance = echeanceLoyerRepo.trouverParId(id) (404 si null),
        bail = bailRepo.trouverParId(echeance.bailId) (404 si null), locataire (404 si null),
        encaissements = encaissementRepo.listerParEcheance(id), relancesActives = relanceRepo.listerParEcheance(id),
        niveauDisponible = calculerRelanceDisponible(echeance, relancesActives, clock.aujourdhui()).
        Calculer resteDu (réutiliser la logique impaye, sinon total − somme encaissements non annulés).
        reply.view('pages/echeances/detail.ejs', { echeance, bail, locataire, locataireNomComplet,
          encaissements, resteDu, niveauDisponible, navActive: 'echeances' }).
    (2) src/main.ts : ajouter `encaissementRepo,` et `relanceRepo,` au bloc `app.register(echeancesPlugin, {...})`.
    (3) src/web/views/pages/echeances/detail.ejs : nouvelle vue (mirror layout de liste.ejs — même include de
        layout/partials, titre, navActive). Afficher la fiche, la liste des encaissements, le CTA encaissement,
        `<%- include('../../partials/relance-action', { echeanceId: echeance.id, niveauDisponible }) %>`, et le
        lien avis-pdf. Échapper toutes les données via <%= %> (jamais <%- %> sauf includes de partials).
        Respecter l'a11y (heading hiérarchique, aria, contraste — practices/ACCESSIBILITY.md, exigence Phase 3/7).
    Relancer → vert.

    REFACTOR — aucun si lisible. Domaine pur préservé (la route est de l'adapter web ; aucune logique métier
    nouvelle introduite — on réutilise calculerRelanceDisponible).
  </action>
  <acceptance_criteria>
    - `grep -c "/echeances/:id'" src/web/routes/echeances.ts` >= 1 (route enregistrée).
    - `grep -c "calculerRelanceDisponible" src/web/routes/echeances.ts` >= 1.
    - `grep -c "encaissementRepo" src/web/routes/echeances.ts` >= 1 ET `grep -c "relanceRepo" src/web/routes/echeances.ts` >= 1.
    - src/web/views/pages/echeances/detail.ejs existe.
    - main.ts : le bloc echeancesPlugin contient `encaissementRepo` et `relanceRepo`.
    - `pnpm vitest run tests/integration/web/route-fiche-echeance.test.ts` exit 0.
    - `pnpm tsc --noEmit` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/integration/web/route-fiche-echeance.test.ts && grep -q "calculerRelanceDisponible" src/web/routes/echeances.ts && grep -q "encaissementRepo" src/web/routes/echeances.ts && grep -q "relanceRepo" src/web/routes/echeances.ts && test -f src/web/views/pages/echeances/detail.ejs && pnpm tsc --noEmit && echo OK</automated>
  </verify>
  <done>GET /echeances/:id rend une fiche échéance complète (200) avec encaissements + action relance + CTA encaissement, et renvoie 404 pour un id inconnu ; deps câblées dans le plugin et main.ts.</done>
</task>

<task type="auto">
  <name>Task 2 : réorienter les 3 CTA morts vers GET /echeances/:id</name>
  <files>src/web/views/pages/dashboard/accueil.ejs, src/web/views/pages/impayes/liste.ejs</files>
  <read_first>
    - src/web/views/pages/dashboard/accueil.ejs (ligne 60 S3 « Saisir l'encaissement » ; lignes 89-90 S4 « Lancer la relance »)
    - src/web/views/pages/impayes/liste.ejs (ligne 94 « Voir l'échéance » ; lignes 92/95-97 chemins relance existants)
  </read_first>
  <action>
    Les 3 liens ciblent déjà `/echeances/<id>` (qui retournait 404). Maintenant que la route existe, ils sont
    valides — vérifier qu'ils pointent bien vers `/echeances/<%= ...echeanceId %>` (forme exacte, sans segment
    supplémentaire) et que `echeanceId` est disponible dans le contexte de chaque vue (impaye.echeanceId,
    action.echeanceId). Aucune nouvelle route à inventer.
    - accueil.ejs:60 (S3) : href="/echeances/<%= impaye.echeanceId %>" — conserver le libellé « Saisir l'encaissement »
      (la fiche échéance porte le CTA encaissement). Conserver tout aria-label éventuel.
    - accueil.ejs:89-90 (S4) : href="/echeances/<%= action.echeanceId %>" — conserver « Lancer la relance »
      (la fiche porte l'action relance). role="button" conservé.
    - impayes/liste.ejs:94 : href="/echeances/<%= impaye.echeanceId %>" « Voir l'échéance » — désormais une cible 200.
    Si une variable echeanceId manquait côté contexte (dashboard actions S4), s'assurer que racine.ts l'expose déjà
    (le href l'utilise déjà → c'est le cas). NE PAS modifier racine.ts si echeanceId est déjà présent.
  </action>
  <verify>
    <automated>grep -q "/echeances/<%= impaye.echeanceId %>" src/web/views/pages/dashboard/accueil.ejs && grep -q "/echeances/<%= action.echeanceId %>" src/web/views/pages/dashboard/accueil.ejs && grep -q "/echeances/<%= impaye.echeanceId %>" src/web/views/pages/impayes/liste.ejs && echo OK</automated>
  </verify>
  <done>Les 3 CTA mènent vers GET /echeances/:id (route 200) ; le lancement de relance depuis le dashboard a un chemin fonctionnel via la fiche échéance.</done>
</task>

<task type="auto">
  <name>Task 3 : réconcilier le suivi (REQUIREMENTS.md + ROADMAP Progress) avec l'état réel</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/v1.0-MILESTONE-AUDIT.md (tableau de couverture des exigences — source de vérité de la réconciliation)
    - .planning/REQUIREMENTS.md (cases v1 lignes 14-57 + table Traceability lignes 127-154)
    - .planning/ROADMAP.md (table Progress — corriger les lignes stale Phases 3/5/6 + ligne Phase 8)
  </read_first>
  <action>
    Aligner les docs de suivi sur l'état vérifié (cf. tableau de couverture de l'audit). NE marquer satisfait
    QUE ce qui est réellement vérifié/livré :
    (A) REQUIREMENTS.md — cases v1 ET table Traceability « Status » :
        - ENC-01..ENC-05 (Phase 2) → Complete / [x]  (02-07-VERIFICATION passed ; ENC-04/05 fermés par cette Phase 8).
        - PAT-03, LOC-03..LOC-06 (Phase 3) → Complete / [x]  (03-VERIFICATION passed).
        - FIS-05, FIS-06 (Phase 6) → Complete / [x]  (06-VERIFICATION passed).
        - DOC-01..DOC-03, INC-01 (Phase 4) → Complete / [x]  (04-VERIFICATION passed).
        - DAS-01 (Phase 7) → reste Complete (CTA réparés par Phase 8).
        - Les REQs déjà [x]/Complete (PAT-01/02, LOC-01/02, FIS-01..04, DAS-02) : inchangés.
    (B) ROADMAP.md table Progress — corriger les lignes contredites par le disque :
        - Phase 3 → `5/5 | Complete` (date depuis 03-VERIFICATION si disponible, sinon laisser la date connue).
        - Phase 5 → `8/8 | Complete` (verdict 05-VERIFICATION passed ; le « Gaps found » était stale).
        - Phase 6 → `7/7 | Complete`.
        - Phase 8 → `1/1 | Complete | 2026-06-16` (cette plan) ; et cocher `- [x] 08-01-PLAN.md` + `- [x] **Phase 8 …**`
          dans la liste des phases en tête de ROADMAP.
        NE PAS toucher aux lignes déjà correctes (Phases 1, 2, 7).
    Commit doc unique. Aucune modification de code dans cette tâche.
  </action>
  <verify>
    <automated>grep -E "^\| ENC-04 .* Complete" .planning/REQUIREMENTS.md && grep -E "Phase 6.*7/7.*Complete|6\. Liasse.*7/7.*Complete" .planning/ROADMAP.md && echo OK</automated>
  </verify>
  <done>REQUIREMENTS.md et la table Progress du ROADMAP reflètent l'état réel : tous les REQs v1 livrés sont marqués satisfaits, les lignes Progress stale (Phases 3/5/6) sont corrigées, et la Phase 8 est marquée complète.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domaine/use case → vue EJS (fiche échéance) | période, montants, statut, nom locataire et encaissements transitent vers le HTML. Rendu via `<%= %>` (échappé). |
| href /echeances/:id | `:id` est un identifiant interne (EcheanceLoyerId), pas une saisie tierce libre. |
| SQLite local → route | Données lues en local (mono-utilisateur). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering (XSS) | detail.ejs — rendu période/montants/nom/encaissements | mitigate | Rendu via `<%= %>` (EJS échappe par défaut). Aucun `<%- %>` sauf `include` de partials de confiance (relance-action). |
| T-08-01-02 | Information disclosure | fiche échéance affiche données locataire | accept | App mono-utilisateur local-first (CLAUDE.md) ; le bailleur voit ses propres données. |
| T-08-01-03 | DoS (id invalide) | GET /echeances/:id | mitigate | trouverParId(id) → null → 404 propre ; pas d'exception non gérée sur id malformé. |
</threat_model>

<verification>
- `pnpm vitest run tests/integration/web/route-fiche-echeance.test.ts` : 200 (échéance existante) + 404 (id inconnu) verts.
- `pnpm vitest run` : suite complète verte (aucune régression — ajout de route + vue, deps optionnelles côté plugin).
- `pnpm exec cucumber-js --tags "@phase7"` : inchangé/vert (le dashboard rend toujours, liens désormais valides).
- `pnpm tsc --noEmit` : 0 erreur.
- `pnpm lint:deps` (dependency-cruiser) : aucun import infra dans le domaine (la route est un adapter web).
- Grep gates des `<acceptance_criteria>` satisfaits.
- Audit v1.0 : le blocker ROUTE-ECHEANCES-DETAIL est levé (3 CTA → 200) ; ENC-04/ENC-05/DAS-01 satisfaits.
</verification>

<success_criteria>
Blocker d'intégration v1.0 fermé :
- GET /echeances/:id existe (200 fiche échéance / 404 id inconnu), testé en intégration.
- Les 3 CTA morts (dashboard S3/S4, impayés) mènent à une cible existante ; le lancement de relance depuis le
  dashboard a un chemin fonctionnel.
- ENC-04, ENC-05, DAS-01 passent de partiel à satisfait.
Suivi réconcilié :
- REQUIREMENTS.md (cases + Traceability) et ROADMAP Progress reflètent l'état réel sur disque.
- Périmètre chirurgical : une route + une vue + 3 hrefs confirmés + 2 docs de suivi ; aucune logique métier nouvelle.
</success_criteria>

<output>
After completion, create `.planning/phases/08-gap-fiche-echeance-tracking/08-01-SUMMARY.md`.
</output>
</content>
