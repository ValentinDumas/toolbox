---
phase: 06-liasse-2031-cfe
plan: 04
type: tdd
wave: 3
depends_on: [06-01, 06-03]
files_modified:
  - src/domain/fiscalite/liasse/case-liasse.ts
  - src/application/fiscalite/generer-brouillon-liasse.ts
  - src/web/routes/fiscalite/liasse.ts
  - src/web/views/pages/fiscalite/brouillon-liasse.ejs
  - src/web/views/partials/partial-bandeau-rectificative.ejs
  - src/web/views/pages/fiscalite/index.ejs
  - tests/bdd/features/liasse-rectificative.feature
  - tests/bdd/step_definitions/brouillon-liasse.steps.ts
  - tests/unit/fiscalite/generer-brouillon-liasse.test.ts
  - tests/integration/web/route-liasse.test.ts
autonomous: true
requirements: [FIS-05]
tags: [fiscalite, liasse, rectificative, declaration-corrigee]

must_haves:
  truths:
    - "L'utilisateur peut générer le brouillon liasse depuis une DeclarationCorrigee (route `/fiscalite/declarations-corrigees/:id/liasse`)."
    - "La liasse rectificative est rendue dans le MÊME format que la liasse originale (D-L6.5 — pas de diff avant/après V1)."
    - "Un bandeau jaune `partial-bandeau-rectificative.ejs` rend le motif de correction et un lien vers la déclaration originale."
    - "La DeclarationAnnuelle originale reste INTACTE et consultable après création de la corrigée (anti-pattern §4 + Phase 5 D-FIS-G4.2 append-only)."
    - "Le bloc 'Brouillons de liasse' sur /fiscalite liste désormais aussi les déclarations corrigées (la plus récente prévaut pour chaque exercice)."
  artifacts:
    - path: "src/web/views/partials/partial-bandeau-rectificative.ejs"
      provides: "Bandeau S6 motif + lien original"
    - path: "src/application/fiscalite/generer-brouillon-liasse.ts"
      provides: "Use case supporte les deux sources : DeclarationAnnuelle et DeclarationCorrigee"
  key_links:
    - from: "src/web/routes/fiscalite/liasse.ts"
      to: "src/domain/fiscalite/declaration-corrigee-repository.ts"
      via: "DI declCorrigeeRepo"
      pattern: "declCorrigeeRepo\\.trouverParId"
---

<objective>
**Liasse rectificative (FIS-05 — slice 4).** Étendre l'orchestration pour générer le brouillon depuis une `DeclarationCorrigee` (déjà modélisée Phase 5) en plus de la `DeclarationAnnuelle` originale.

**Décisions implémentées :**
- D-L6.5 → même format + bandeau motif (pas de différentiel avant/après).
- Anti-pattern §4 (Phase 5 D-FIS-G4.2) → la corrigée NE MUTE PAS l'originale.
- UI-SPEC §S6 → bandeau jaune avec motif + lien "Voir la déclaration originale".

**Périmètre :**
- Route nouvelle : `GET /fiscalite/declarations-corrigees/:id/liasse` (HTML).
- Use case `genererBrouillonLiasse` étendu : accepte `{ declarationCorrigeeId }` au lieu de `{ declarationId }`.
- DTO étendu avec `motifRectification?: string` + `urlOriginale?: string`.
- Bloc `/fiscalite` enrichi : pour chaque exercice afficher la dernière version (corrigée si elle existe, sinon originale).

**Purpose** : fermer le périmètre déclaratif Phase 6 (V1 = couvre toutes les versions clôturées).
**Output** : route HTML rectificative navigable + bandeau motif visible.
</objective>

<execution_context>
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/workflows/execute-plan.md
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-liasse-2031-cfe/06-CONTEXT.md
@.planning/phases/06-liasse-2031-cfe/06-RESEARCH.md
@.planning/phases/06-liasse-2031-cfe/06-UI-SPEC.md
@.planning/phases/06-liasse-2031-cfe/06-01-SUMMARY.md
@.planning/phases/06-liasse-2031-cfe/06-03-SUMMARY.md
@CLAUDE.md

# Snapshots Phase 5 consommés (lecture seule)
@src/domain/fiscalite/declaration-corrigee.ts
@src/domain/fiscalite/declaration-annuelle.ts
@src/infrastructure/repositories/declaration-corrigee-repository-sqlite.ts

# Artefacts Wave 1-2 à étendre
@src/application/fiscalite/generer-brouillon-liasse.ts
@src/web/routes/fiscalite/liasse.ts
@src/web/views/pages/fiscalite/brouillon-liasse.ejs
@src/web/views/pages/fiscalite/index.ejs
@src/web/views/partials/banniere-warning.ejs

<interfaces>
# DeclarationCorrigee (Phase 5)
- id: DeclarationCorrigeeId
- declarationOriginaleId: DeclarationAnnuelleId
- bailleurId: BailleurId
- exercice: number
- motif: string
- regimeApplique: 'micro_bic' | 'reel'
- recettesTotales: Money
- chargesQualifieesParCategorie: Record<QualificationFiscale, Money>
- dotationAmortissement: Money
- ardGenere, ardConsomme: Money
- composantsSnapshot: ComposantSnapshot[]
- clotureLe: Temporal.PlainDateTime

# Le snapshot DeclarationCorrigee EXPOSE LES MÊMES CHAMPS que DeclarationAnnuelle
# (par construction Phase 5 — append-only, motif + valeurs corrigées).
# Le use case Phase 6 peut accepter une union type.

# DeclarationCorrigeeRepository
- trouverParId(id): Promise<DeclarationCorrigee | null>
- listerParBailleurExercice(bailleurId, exercice): Promise<DeclarationCorrigee[]>
  (vérifier nom exact dans le code Phase 5)
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Extension use case pour accepter DeclarationCorrigee + tests BDD rectificative</name>
  <files>
    src/domain/fiscalite/liasse/case-liasse.ts,
    src/application/fiscalite/generer-brouillon-liasse.ts,
    tests/bdd/features/liasse-rectificative.feature,
    tests/bdd/step_definitions/brouillon-liasse.steps.ts,
    tests/unit/fiscalite/generer-brouillon-liasse.test.ts
  </files>
  <read_first>
    src/domain/fiscalite/declaration-corrigee.ts (champs disponibles),
    src/domain/fiscalite/declaration-annuelle.ts (champs comparables — confirmer surface identique),
    src/application/fiscalite/creer-declaration-corrigee.ts (rappel pattern de création),
    src/infrastructure/repositories/declaration-corrigee-repository-sqlite.ts (méthodes du repository),
    src/application/fiscalite/generer-brouillon-liasse.ts (état Plan 03 — pour brancher la nouvelle source),
    .planning/phases/06-liasse-2031-cfe/06-CONTEXT.md (D-L6.5),
    .planning/phases/06-liasse-2031-cfe/06-UI-SPEC.md (§S6 + Copywriting Contract bandeau rectificative).
  </read_first>
  <behavior>
    **TDD RED → GREEN.**

    **RED — `tests/bdd/features/liasse-rectificative.feature` (4 scénarios @phase6 @phase6-liasse-rectificative) :**
    - 1: créer une `DeclarationCorrigee` à partir d'une `DeclarationAnnuelle` clôturée avec un motif "Oubli d'une charge de syndic" puis visiter `/fiscalite/declarations-corrigees/:id/liasse` → page rendue + bandeau S6 motif visible + lien "Voir la déclaration originale" pointant vers `/fiscalite/declarations/:idOriginal/liasse`.
    - 2: la déclaration originale reste consultable post-rectification → visiter `/fiscalite/declarations/:idOriginal/liasse` après création de la corrigée → page originale rendue sans bandeau rectificative.
    - 3: deux `DeclarationCorrigee` successives sur le même exercice → la page `/fiscalite` liste UNIQUEMENT la plus récente comme "brouillon actuel" (la précédente reste consultable via son URL directe).
    - 4: tentative de modifier une `DeclarationCorrigee` après création doit échouer (append-only — anti-pattern §4 Phase 5 doit être déjà testé Phase 5 ; ici on vérifie que Phase 6 ne mute pas).

    **RED — extension `tests/unit/fiscalite/generer-brouillon-liasse.test.ts` :**
    - Test : `genererBrouillonLiasse({ declarationCorrigeeId }, deps)` retourne un DTO avec `motifRectification` non null + `urlOriginale = '/fiscalite/declarations/:idOriginal/liasse'`.
    - Test : si `DeclarationCorrigee` introuvable → throw `DeclarationIntrouvable` (réutilise erreur existante).
    - Test : valeurs de cases viennent des champs de la `DeclarationCorrigee` (recettes/charges/dotation corrigées) — pas de la `DeclarationAnnuelle` originale.

    **GREEN — `src/domain/fiscalite/liasse/case-liasse.ts`** : étendre `BrouillonLiasseDto` avec `motifRectification?: string` + `urlOriginale?: string`.

    **GREEN — `src/application/fiscalite/generer-brouillon-liasse.ts`** :

    Option recommandée : **deux signatures de commande** (overload ou union) :
    - `GenererBrouillonLiasseCommande = { declarationId: DeclarationAnnuelleId } | { declarationCorrigeeId: DeclarationCorrigeeId }`.
    - Ajouter `declCorrigeeRepo: DeclarationCorrigeeRepository` à `GenererBrouillonLiasseDeps`.
    - Au début du use case :
      1. Si `'declarationCorrigeeId' in commande` → `decl = await declCorrigeeRepo.trouverParId(commande.declarationCorrigeeId)` → si null throw `DeclarationIntrouvable`. Conserver `decl.declarationOriginaleId` pour construire `urlOriginale`.
      2. Sinon → comportement Wave 1-2 (charge `DeclarationAnnuelle`).
    3. Construire `BrouillonLiasseDto` enrichi : si `decl` est une `DeclarationCorrigee` → `motifRectification = decl.motif` + `urlOriginale = '/fiscalite/declarations/' + decl.declarationOriginaleId + '/liasse'`.

    **REFACTOR** : extraire une fonction `chargerSnapshotPourLiasse(commande, deps): Promise<SnapshotPourLiasse>` qui retourne une interface commune `{ exercice, regimeApplique, recettesTotales, chargesQualifieesParCategorie, dotationAmortissement, ardGenere, ardConsomme, composantsSnapshot, clotureLe, bailleurId, motifRectification?, urlOriginale? }`. Simplifie le reste du use case (qui consomme l'interface unifiée).

    Commits :
    - `test(06-04): scenarios feature liasse-rectificative (RED)`
    - `feat(06-04): use case genererBrouillonLiasse accepte DeclarationCorrigee + DTO motifRectification`
  </behavior>
  <action>
    **Décisions implémentées :**
    - D-L6.5 → même format. La rectificative diffère UNIQUEMENT par le bandeau S6 + DTO `motifRectification` + `urlOriginale`. Aucune nouvelle section, aucun calcul différentiel.
    - Phase 5 D-FIS-G4.2 → la corrigée et l'originale sont toutes deux append-only ; Phase 6 lit, ne mute pas.

    **À ne PAS faire :**
    - Pas de diff visuel avant/après V1 (D-L6.5 — différé V1.1).
    - Pas de mutation des snapshots Phase 5 (anti-pattern §4 + pitfall §4).
    - Pas de duplication de la logique de mapping — réutiliser la même boucle pour les deux types de snapshot.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test -- --run tests/unit/fiscalite/generer-brouillon-liasse.test.ts && pnpm test:bdd -- --tags "@phase6-liasse-rectificative" && pnpm test:bdd -- --tags "@phase6-liasse-reel"</automated>
  </verify>
  <done>
    - `pnpm test:bdd -- --tags "@phase6-liasse-rectificative"` 4 scénarios verts.
    - Tests réel/micro/traçabilité tous restent verts (régression).
    - `grep -n "motifRectification" src/domain/fiscalite/liasse/case-liasse.ts src/application/fiscalite/generer-brouillon-liasse.ts` retourne ≥ 2 résultats.
    - 2 commits atomiques.
  </done>
</task>

<task type="execute">
  <name>Task 2: Route rectificative + bandeau S6 + intégration vue + extension bloc /fiscalite</name>
  <files>
    src/web/routes/fiscalite/liasse.ts,
    src/web/views/partials/partial-bandeau-rectificative.ejs,
    src/web/views/pages/fiscalite/brouillon-liasse.ejs,
    src/web/views/pages/fiscalite/index.ejs,
    src/main.ts,
    tests/integration/web/route-liasse.test.ts
  </files>
  <read_first>
    src/web/routes/fiscalite/liasse.ts (état Plan 03),
    src/web/views/partials/banniere-warning.ejs (pattern bandeau jaune `--couleur-warning-bg`),
    src/web/views/pages/fiscalite/brouillon-liasse.ejs (état Plan 03 — point d'insertion du bandeau S6),
    src/web/views/pages/fiscalite/index.ejs (bloc "Brouillons de liasse" Plan 02),
    .planning/phases/06-liasse-2031-cfe/06-UI-SPEC.md (§S6 + Copywriting Contract — copie exacte du bandeau),
    src/main.ts (DI déjà câblée Plan 03 — étendre avec declCorrigeeRepo).
  </read_first>
  <action>
    Câbler la route HTML rectificative + bandeau S6 + bloc `/fiscalite` enrichi.

    **`src/web/views/partials/partial-bandeau-rectificative.ejs`** (UI-SPEC §S6) :
    - Props : `motif: string`, `urlOriginale: string`.
    - `<aside role="status" aria-live="polite" style="background: var(--couleur-warning-bg); border-left: 4px solid var(--couleur-warning); padding: 16px; margin-bottom: 24px;">`.
    - Icône `<span aria-hidden="true">⚠</span>` + copie EXACTE UI-SPEC : "**Liasse rectificative — motif : <%= motif %>.** Cette version remplace le brouillon précédent. La déclaration originale reste consultable."
    - Lien accent `<a href="<%= urlOriginale %>">Voir la déclaration originale</a>`.

    **`src/web/views/pages/fiscalite/brouillon-liasse.ejs` — extension** :
    - Juste après `partial-bandeau-brouillon-liasse`, AVANT le bandeau de réconciliation : si `dto.motifRectification` → `<%- include('../../partials/partial-bandeau-rectificative', { motif: dto.motifRectification, urlOriginale: dto.urlOriginale }) %>`.

    **`src/web/routes/fiscalite/liasse.ts` — extension** :
    - Ajouter `app.get<{ Params: { id: string } }>('/fiscalite/declarations-corrigees/:id/liasse', async (req, reply) => { ... })`.
    - Appelle `genererBrouillonLiasse({ declarationCorrigeeId: req.params.id as DeclarationCorrigeeId }, deps)`.
    - Catch `DeclarationIntrouvable` → 404 erreur.ejs.
    - Catch `MappingLiasseAbsent` → 422 erreur.ejs (copy UI-SPEC).
    - Render `pages/fiscalite/brouillon-liasse.ejs`.

    **`src/web/views/pages/fiscalite/index.ejs` — extension** :
    - Bloc "Brouillons de liasse" : pour chaque exercice clôturé du bailleur, si une `DeclarationCorrigee` existe → afficher un lien vers `/fiscalite/declarations-corrigees/:id/liasse` avec label "Consulter le brouillon liasse {exercice} (rectificative)" + petit badge `--couleur-warning`. Sinon → lien vers `/fiscalite/declarations/:id/liasse` (V1 OK Plan 01-02).
    - Implementer cette logique dans une fonction côté route (`/fiscalite` racine — `racine.ts`) qui retourne un tableau de `{ exercice, urlLiasse, estRectificative, motifOptionnel }` consommé par la vue.

    **`src/main.ts` — DI** :
    - Étendre `LiasseRouteDeps` avec `declCorrigeeRepo: DeclarationCorrigeeRepository` (déjà instancié Phase 5).

    **Tests intégration `tests/integration/web/route-liasse.test.ts` — étendre** :
    - Cas 200 rectificative : créer originale clôturée + corrigée avec motif "test motif". Hit GET `/fiscalite/declarations-corrigees/:id/liasse` → 200 + HTML contient "Liasse rectificative — motif : test motif." + URL `<a href="/fiscalite/declarations/:idOriginal/liasse">`.
    - Cas 200 originale post-rectification : hit GET originale → 200 + HTML NE CONTIENT PAS "Liasse rectificative".
    - Cas 404 corrigée inexistante : hit GET `/fiscalite/declarations-corrigees/uuid-fake/liasse` → 404.

    **Décisions implémentées :**
    - D-L6.5 → bandeau S6 motif + lien original.
    - UI-SPEC §S6 → copywriting exact + `role="status"`.
    - UI-SPEC §S11 → bloc `/fiscalite` montre la plus récente version (corrigée prévaut).

    **À ne PAS faire :**
    - Pas de masquage de l'originale (audit-friendly — UI-SPEC §S6).
    - Pas de bouton "supprimer la rectificative" (append-only strict).
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test -- --run tests/integration/web/route-liasse.test.ts && pnpm test:bdd -- --tags "@phase6-liasse-rectificative" && grep -n "Liasse rectificative — motif" src/web/views/partials/partial-bandeau-rectificative.ejs</automated>
  </verify>
  <done>
    - `pnpm test -- --run tests/integration/web/route-liasse.test.ts` exit 0 (≥ 3 nouveaux cas).
    - `pnpm test:bdd -- --tags "@phase6-liasse-rectificative"` 4 scénarios verts.
    - `grep -n "Liasse rectificative — motif" src/web/views/partials/partial-bandeau-rectificative.ejs` retourne 1 résultat (copywriting exact UI-SPEC).
    - `grep -n "declarations-corrigees/:id/liasse\|declarations-corrigees/\\$\\{" src/web/routes/fiscalite/liasse.ts` retourne ≥ 1 résultat.
    - 2 commits : `feat(06-04): route GET liasse rectificative + bandeau S6` + `feat(06-04): bloc /fiscalite priorise la corrigée la plus récente`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `partial-bandeau-rectificative.ejs` rend `motif` saisi par l'utilisateur | EJS `<%= motif %>` auto-échappe XSS. |
| Route `/fiscalite/declarations-corrigees/:id/liasse` | `req.params.id` traité comme opaque ID (cast brand type + lookup repository). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-LIASSE-W3-RECTIF-01 | Tampering (XSS) | `motif` rendu dans bandeau S6 | mitigate | EJS `<%= motif %>` auto-échappe. Test asserte `<script>` rendu littéralement. |
| T-06-LIASSE-W3-RECTIF-02 | Tampering (intégrité) | Modification de l'originale post-rectification | mitigate | Phase 5 D-FIS-G4.2 garantit append-only. Phase 6 ne mute jamais. Test BDD régression scénario 2 + 4. |
| T-06-LIASSE-W3-RECTIF-03 | Information Disclosure | Lien vers déclaration originale exposé | accept | V1 single-user. Pas de problème. Audit-friendly (UI-SPEC §S6 lien explicite). |
</threat_model>

<verification>
- `pnpm typecheck` exit 0.
- `pnpm test && pnpm test:bdd` tous verts.
- Manuel : créer une `DeclarationCorrigee` via wizard Phase 5 → visiter `/fiscalite` → cliquer "Consulter le brouillon liasse (rectificative)" → bandeau jaune visible + lien original cliquable.
</verification>

<success_criteria>
- ROADMAP success criterion 1 (FIS-05) : brouillon disponible aussi sur les rectificatives.
- D-L6.5 entièrement couvert.
- Anti-pattern §4 + pitfall §4 verrouillés par test régression.
</success_criteria>

<output>
After completion, create `.planning/phases/06-liasse-2031-cfe/06-04-SUMMARY.md` : screenshot bandeau S6, confirmation absence de mutation Phase 5, captures /fiscalite avec liste corrigée vs originale.
</output>
</content>
</invoke>