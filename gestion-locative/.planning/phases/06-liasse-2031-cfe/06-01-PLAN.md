---
phase: 06-liasse-2031-cfe
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/domain/fiscalite/erreurs.ts
  - src/domain/fiscalite/liasse/mapping-liasse-2026.ts
  - src/domain/fiscalite/liasse/mapping-liasse-provider.ts
  - src/domain/fiscalite/liasse/case-liasse.ts
  - src/domain/fiscalite/liasse/brouillon-liasse-builder.ts
  - src/application/fiscalite/generer-brouillon-liasse.ts
  - src/web/routes/fiscalite/liasse.ts
  - src/web/views/pages/fiscalite/brouillon-liasse.ejs
  - src/web/views/partials/partial-bandeau-brouillon-liasse.ejs
  - src/web/views/partials/partial-tableau-liasse-section.ejs
  - src/web/helpers/formater-case-liasse.ts
  - src/web/views/pages/fiscalite/index.ejs
  - src/main.ts
  - tests/_builders/fiscalite.ts
  - tests/_fakes/mapping-liasse-provider-fake.ts
  - tests/bdd/features/mapping-liasse-versionne.feature
  - tests/bdd/features/brouillon-liasse-reel.feature
  - tests/bdd/step_definitions/brouillon-liasse.steps.ts
  - tests/unit/fiscalite/mapping-liasse-provider.test.ts
  - tests/unit/fiscalite/generer-brouillon-liasse.test.ts
autonomous: true
requirements: [FIS-05]
tags: [fiscalite, liasse, mapping-versionne, brouillon-reel]

must_haves:
  truths:
    - "L'utilisateur peut consulter une vue HTML brouillon-liasse pour une DeclarationAnnuelle clôturée en régime réel."
    - "Chaque ligne du tableau affiche {numéro de case officiel cerfa} + {libellé officiel} + {valeur du snapshot} (D-L6.1)."
    - "La vue couvre 2031-SD + annexes 2033-A, 2033-B, 2033-C, 2033-D (D-L6.2 + D-A6.1)."
    - "Un bandeau persistant 'Brouillon liasse fiscale {Année} — à reporter manuellement' est rendu en tête de page (UI-SPEC §S1)."
    - "Si la déclaration cible un exercice non couvert par mapping-liasse-2026.ts, la route renvoie une page d'erreur dédiée (MappingLiasseAbsent — D-L6.3)."
    - "Aucun numéro de case n'est hardcodé dans application/ ou web/ (anti-pattern §1 + pitfall §1 RESEARCH.md)."
    - "La valeur affichée vient TOUJOURS du snapshot DeclarationAnnuelle (anti-pattern §3 + §4 — D-T6.4)."
  artifacts:
    - path: "src/domain/fiscalite/liasse/mapping-liasse-2026.ts"
      provides: "Mapping case-par-case versionné millésime 2026 (2031-SD + 2033-A/B/C/D)"
      contains: "MAPPING_LIASSE_2026"
    - path: "src/domain/fiscalite/liasse/mapping-liasse-provider.ts"
      provides: "Port MappingLiasseProvider + impl en mémoire fail-fast année non couverte"
      exports: ["MappingLiasseProvider", "MappingLiasseProviderEnMemoire"]
    - path: "src/domain/fiscalite/liasse/case-liasse.ts"
      provides: "DTO BrouillonLiasseDto / CaseLiasseDto / SectionLiasse / AnnexeLiasse"
      exports: ["BrouillonLiasseDto", "CaseLiasseDto", "SectionLiasse", "AnnexeLiasse"]
    - path: "src/domain/fiscalite/liasse/brouillon-liasse-builder.ts"
      provides: "Port BrouillonLiasseBuilder.construire(dto): unknown (miroir RecapFiscalBuilder)"
      exports: ["BrouillonLiasseBuilder"]
    - path: "src/application/fiscalite/generer-brouillon-liasse.ts"
      provides: "Use case orchestrateur snapshot → mapping → DTO (sans traçabilité ni réconciliation Wave 1)"
      exports: ["genererBrouillonLiasse"]
    - path: "src/web/routes/fiscalite/liasse.ts"
      provides: "GET /fiscalite/declarations/:id/liasse (HTML)"
    - path: "src/web/views/pages/fiscalite/brouillon-liasse.ejs"
      provides: "Vue HTML principale brouillon liasse"
    - path: "migrations/0023_phase6_declaration_cfe.sql"
      provides: "Migration phase 6 (créée par Plan 06 — référencée ici via numérotation)"
  key_links:
    - from: "src/application/fiscalite/generer-brouillon-liasse.ts"
      to: "MappingLiasseProvider.pour(exercice)"
      via: "DI deps.mappingProvider"
      pattern: "mappingProvider.pour\\(.+\\.exercice\\)"
    - from: "src/web/routes/fiscalite/liasse.ts"
      to: "src/application/fiscalite/generer-brouillon-liasse.ts"
      via: "appel use case avec DI Fastify"
      pattern: "genererBrouillonLiasse\\("
    - from: "src/main.ts"
      to: "MappingLiasseProviderEnMemoire"
      via: "instanciation singleton et injection dans la route"
      pattern: "new MappingLiasseProviderEnMemoire\\(\\)"
---

<objective>
**Walking enabler FIS-05.** Livrer la fondation de la vue brouillon liasse en régime réel : port mapping versionné (miroir exact `RegleFiscaleProvider`), DTO traçabilité (types), port builder PDF (signature seule, impl Wave 3), use case orchestrateur, route HTML, vue EJS avec tableau case-par-case sur 2031-SD + 2033-A/B/C/D.

**Périmètre Wave 1 :**
- Régime **réel uniquement** (micro-BIC = Plan 02).
- **Pas de drill-down sources, pas de réconciliation** (Plan 03).
- **Pas de rectificative** (Plan 04), **pas d'export PDF/CSV** (Plan 05).
- Bandeau S1 (UI-SPEC) en tête, ARIA labels par annexe, monospace 14px sur numéros de case.

**Purpose** : ouvrir le sentier critique FIS-05 sans dette de scope ; tout plan suivant ajoute une capacité sans toucher cette fondation.
**Output** : vue HTML fonctionnelle navigable sur `/fiscalite/declarations/:id/liasse` pour une `DeclarationAnnuelle.regimeApplique = 'reel'` clôturée Phase 5.
</objective>

<execution_context>
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/workflows/execute-plan.md
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-liasse-2031-cfe/06-CONTEXT.md
@.planning/phases/06-liasse-2031-cfe/06-RESEARCH.md
@.planning/phases/06-liasse-2031-cfe/06-PATTERNS.md
@.planning/phases/06-liasse-2031-cfe/06-UI-SPEC.md
@.planning/phases/06-liasse-2031-cfe/06-VALIDATION.md
@CLAUDE.md
@LMNP.md
@practices/DDD.md
@practices/BDD_PRACTICES.md
@practices/UI_DESIGN.md
@practices/ACCESSIBILITY.md

# Patterns à cloner (miroir exact)
@src/domain/fiscalite/regles/regle-fiscale-provider.ts
@src/domain/fiscalite/regles/regles-2026.ts
@src/domain/fiscalite/erreurs.ts
@src/domain/fiscalite/declaration-annuelle.ts
@src/domain/fiscalite/declaration-annuelle-repository.ts
@src/domain/fiscalite/recap-fiscal-builder.ts
@src/domain/fiscalite/qualification-fiscale.ts

# Routes et vues de référence
@src/web/routes/fiscalite/exports.ts
@src/web/routes/fiscalite/racine.ts
@src/web/views/pages/fiscalite/recap-annuel.ejs
@src/web/views/pages/fiscalite/index.ejs
@src/web/views/partials/data-table.ejs
@src/web/views/partials/layout-debut.ejs
@src/web/views/partials/banniere-warning.ejs

<interfaces>
# Snapshots Phase 5 consommés (lecture seule)

DeclarationAnnuelle (src/domain/fiscalite/declaration-annuelle.ts) :
- id: DeclarationAnnuelleId
- bailleurId: BailleurId
- exercice: number
- regimeApplique: 'micro_bic' | 'reel'
- recettesTotales: Money
- chargesQualifieesParCategorie: Record<QualificationFiscale, Money>
- dotationAmortissement: Money
- ardGenere: Money
- ardConsomme: Money
- composantsSnapshot: ComposantSnapshot[]
- statutLmnpLmp: VerdictLmp
- clotureLe: Temporal.PlainDateTime

QualificationFiscale (src/domain/fiscalite/qualification-fiscale.ts) :
- type QualificationFiscale = 'entretien_reparation' | 'amelioration' | 'charge_courante_periodique' | 'non_deductible' | 'non_qualifie'
- const QUALIFICATIONS_DEDUCTIBLES = ['entretien_reparation', 'amelioration', 'charge_courante_periodique']

DeclarationAnnuelleRepository (src/domain/fiscalite/declaration-annuelle-repository.ts) :
- trouverParId(id: DeclarationAnnuelleId): Promise<DeclarationAnnuelle | null>

# Pattern à cloner : RegleFiscaleProvider
RegleFiscaleProvider :
- pour(annee: number): RegleFiscale2026
- throws RegleFiscaleAbsente(annee) si année non couverte

# Erreurs typées (étendre erreurs.ts)
class RegleFiscaleAbsente extends Error { constructor(annee: number) { ... } }
=> MappingLiasseAbsent extends Error { constructor(millesime: number) { ... } }
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: TDD port MappingLiasseProvider + data file mapping-liasse-2026.ts + types DTO + erreurs typées</name>
  <files>
    src/domain/fiscalite/erreurs.ts,
    src/domain/fiscalite/liasse/mapping-liasse-2026.ts,
    src/domain/fiscalite/liasse/mapping-liasse-provider.ts,
    src/domain/fiscalite/liasse/case-liasse.ts,
    src/domain/fiscalite/liasse/brouillon-liasse-builder.ts,
    tests/_builders/fiscalite.ts,
    tests/_fakes/mapping-liasse-provider-fake.ts,
    tests/bdd/features/mapping-liasse-versionne.feature,
    tests/bdd/step_definitions/brouillon-liasse.steps.ts,
    tests/unit/fiscalite/mapping-liasse-provider.test.ts
  </files>
  <read_first>
    src/domain/fiscalite/regles/regle-fiscale-provider.ts,
    src/domain/fiscalite/regles/regles-2026.ts,
    src/domain/fiscalite/erreurs.ts,
    src/domain/fiscalite/recap-fiscal-builder.ts,
    .planning/phases/06-liasse-2031-cfe/06-PATTERNS.md (Patterns critiques 1, 2, 8),
    .planning/phases/06-liasse-2031-cfe/06-RESEARCH.md (Cerfa Case Mapping §2031-SD + §2033-A/B/C/D — niveau confiance MEDIUM),
    .planning/phases/06-liasse-2031-cfe/06-CONTEXT.md (D-L6.1, D-L6.2, D-L6.3, D-A6.1, D-A6.2, D-A6.3, D-A6.4),
    tests/_builders/fiscalite.ts (état existant à étendre),
    https://www.impots.gouv.fr/sites/default/files/formulaires/2031-sd/2026/2031-sd_5396.pdf (sources cerfa officiel — vérifier code par code à l'écriture du data file).
  </read_first>
  <behavior>
    **TDD RED → GREEN → REFACTOR.**

    **RED — `tests/bdd/features/mapping-liasse-versionne.feature` (3 scénarios @phase6 @phase6-mapping-versionne) :**
    - Scénario 1: `Etant donné le mapping fiscal pour le millésime 2026` `Quand on récupère le mapping` `Alors le mapping est trouvé` `Et la section 2031-SD contient la case du bénéfice fiscal`
    - Scénario 2: `Etant donné un MappingLiasseProvider en mémoire couvrant 2026 uniquement` `Quand on récupère le mapping pour 2027` `Alors une erreur MappingLiasseAbsent est levée` `Et le message cite "millésime 2027 non couvert"`
    - Scénario 3: `Quand on récupère le mapping pour 2025 (pré-V1)` `Alors une erreur MappingLiasseAbsent est levée`

    **RED — `tests/unit/fiscalite/mapping-liasse-provider.test.ts` :**
    - `MappingLiasseProviderEnMemoire().pour(2026)` retourne `MAPPING_LIASSE_2026`.
    - `pour(2027)` throw `MappingLiasseAbsent` avec `error.name === 'MappingLiasseAbsent'`.
    - `MAPPING_LIASSE_2026.sections` couvre 5 annexes (`'2031-SD'`, `'2033-A'`, `'2033-B'`, `'2033-C'`, `'2033-D'`) — assertions sur la présence des clés.
    - Chaque case du mapping a un `caseId` unique (propriété injectivité — sera renforcée par fast-check Plan 03).
    - `MAPPING_LIASSE_2026.sections['2033-B']` contient une case mappée sur `source: 'decl.recettesTotales'` (clé permettant au use case de relier la case au snapshot).

    **GREEN — Implémentation :**

    1. **`src/domain/fiscalite/erreurs.ts` — étendre** :
       - Ajouter `class MappingLiasseAbsent extends Error` avec constructor `(millesime: number)` + message `"Mapping liasse absent : millésime ${millesime} non couvert. Le cerfa peut changer chaque année (LF) — vérifier le PDF officiel impots.gouv.fr et créer mapping-liasse-${millesime}.ts. (R1.1 RISKS.md)"` + `this.name = 'MappingLiasseAbsent'`. Pattern miroir `RegleFiscaleAbsente`.

    2. **`src/domain/fiscalite/liasse/case-liasse.ts` — types DTO** (D-L6.1 + D-T6.x squelette) :
       - Type union strict `AnnexeLiasse = '2031-SD' | '2033-A' | '2033-B' | '2033-C' | '2033-D' | '2042-C-PRO'` (les 6 valeurs nécessaires V1).
       - Interface `CaseLiasseDef { readonly caseId: string; readonly libelleOfficiel: string; readonly annexe: AnnexeLiasse; readonly source: SourceCleSnapshot; readonly section?: string; }` — `SourceCleSnapshot` est un type union des champs du snapshot (cf. ci-dessous).
       - Type `SourceCleSnapshot = 'recettesTotales' | 'beneficeFiscal' | 'deficitFiscal' | 'dotationAmortissement' | 'ardGenere' | 'ardConsomme' | 'chargesAutresExternes' | 'chargesImpotsTaxes' | 'immobilisationsConstructionsBrut' | 'immobilisationsMobilierBrut' | 'amortissementsCumulesConstructions' | 'amortissementsCumulesMobilier' | 'vncConstructions' | 'manuel'` — permet au use case de dériver la valeur sans nom de variable libre.
       - Interface `CaseLiasseDto { readonly numero: string; readonly libelleOfficiel: string; readonly annexe: AnnexeLiasse; readonly valeur: Money | null; readonly mention?: string; }` — valeur `null` + mention `"à compléter manuellement"` autorisée pour postes 2033-A non modélisés (D-A6.2).
       - Interface `SectionLiasseDto { readonly titre: string; readonly annexe: AnnexeLiasse; readonly cases: ReadonlyArray<CaseLiasseDto>; readonly bandeauPostesManuels?: boolean; }`.
       - Interface `BrouillonLiasseDto { readonly exercice: number; readonly regimeApplique: 'micro_bic' | 'reel'; readonly bailleurNom: string; readonly sections: ReadonlyArray<SectionLiasseDto>; readonly clotureLe: Temporal.PlainDateTime; }` — base extensible Wave 2/3 (`tracabilite`, `reconciliation`, `motifRectification` ajoutés plus tard).

    3. **`src/domain/fiscalite/liasse/mapping-liasse-2026.ts` — data versionnée** :
       - `import { Money } from '../../_shared/money.js'` (utilisé pour JSDoc commentaires, valeurs vivent côté snapshot).
       - JSDoc en tête : citer cerfa officiel 2031-SD millésime 2026, lien `https://www.impots.gouv.fr/sites/default/files/formulaires/2031-sd/2026/2031-sd_5396.pdf`, mentionner R1.1 RISKS.md "Mapping revu chaque janvier post-LF" et D-L6.3.
       - Interface `MappingLiasse2026 { readonly millesime: 2026; readonly sections: Readonly<Record<AnnexeLiasse, ReadonlyArray<CaseLiasseDef>>>; }`.
       - Constante `MAPPING_LIASSE_2026: MappingLiasse2026` exporté `as const`.
       - **Sections à figer (codes vérifiés sur cerfa officiel 2031-SD millésime 2026, RESEARCH.md §Cerfa Case Mapping — confiance MEDIUM, valider PDF au moment de l'écriture)** :
         - `'2031-SD'` : minimum 3 cases — case bénéfice fiscal (`CB` ou `1GF` selon vérification PDF), case déficit fiscal (`CC` ou `1GG`), case régime imposition.
         - `'2033-A'` : 6 cases calculées (immobilisations constructions brut, amortissements cumulés constructions, VNC constructions, immobilisations mobilier brut, amortissements cumulés mobilier, terrain) + bandeau postes manuels (D-A6.2).
         - `'2033-B'` : minimum 6 cases — case recettes (`FC` ou ligne 218), autres charges externes (`FK` ou ligne 242), impôts taxes (`FX` ou ligne 252), dotations amortissement (`FY`/`GA` ou ligne 254), ARD généré, résultat de l'exercice. **Cohérence Phase 5 D-FIS-G2.2** : `chargesQualifieesParCategorie.entretien_reparation + chargesQualifieesParCategorie.charge_courante_periodique` → case autres charges externes. **JAMAIS `amelioration` sur 2033-B** (pitfall §3).
         - `'2033-C'` : minimum 5 cases (constructions début/augmentations/diminutions/fin, amortissements cumulés début/dotation exercice/fin). Cohérence flux : `2033-C dotation exercice = 2033-B dotation = decl.dotationAmortissement` (invariant).
         - `'2033-D'` : 2 cases (ARD cumulé fin exercice + déficit reportable historique).
         - `'2042-C-PRO'` (squelette V1 Plan 02 — laisser vide ici, peuplé Plan 02).
       - **Note** : les codes lettres exacts (`CB`, `FC`, `FK`, `FY`, etc.) sont à confirmer sur le PDF officiel 2031-SD millésime 2026 au moment de l'écriture. RESEARCH.md flagge confiance MEDIUM — privilégier la version code lettres sur les numéros de ligne (cohérent avec impots.gouv.fr).

    4. **`src/domain/fiscalite/liasse/mapping-liasse-provider.ts` — port + impl** (D-L6.3, miroir exact `RegleFiscaleProvider`) :
       - Interface `MappingLiasseProvider { pour(millesime: number): MappingLiasse2026; }` avec JSDoc `@throws MappingLiasseAbsent`.
       - Classe `MappingLiasseProviderEnMemoire implements MappingLiasseProvider` avec `Map<number, MappingLiasse2026>([[2026, MAPPING_LIASSE_2026]])` — **UN SEUL** millésime au démarrage (différence sémantique vs RegleFiscaleProvider triennal — pitfall §6 RESEARCH.md). JSDoc cite explicitement R1.1.

    5. **`src/domain/fiscalite/liasse/brouillon-liasse-builder.ts` — port** (D-L6.4, signature uniquement, impl Plan 05) :
       - Interface `BrouillonLiasseBuilder { construire(dto: BrouillonLiasseDto): unknown; }`. JSDoc cite miroir `RecapFiscalBuilder` + raison `unknown` (préserver pureté domaine).

    6. **`tests/_builders/fiscalite.ts` — étendre** (sans casser builders Phase 5) :
       - Function `unMappingLiasse2026(overrides?: Partial<MappingLiasse2026>): MappingLiasse2026` — retourne `MAPPING_LIASSE_2026` + override sections.
       - Function `unBrouillonLiasseDtoReel(decl, overrides?)` minimaliste (utilisée par Plan 02 aussi).

    7. **`tests/_fakes/mapping-liasse-provider-fake.ts`** :
       - Classe `MappingLiasseProviderFake implements MappingLiasseProvider` avec Map injectable via constructor `(mappings: Map<number, MappingLiasse2026>)` — permet de tester le fail-fast sans patcher l'impl en mémoire.

    **REFACTOR (si nécessaire) :**
    - Extraire constantes locales si lisibilité dégradée (sinon laisser inline).
    - Aucune fonction au-delà de 30 lignes (SOFTWARE_CRAFTSMANSHIP.md).
    - Commits :
      - `test(06-01): scenarios feature mapping-liasse-versionne (RED)`
      - `feat(06-01): port MappingLiasseProvider + data mapping-liasse-2026 + DTO case-liasse + erreur MappingLiasseAbsent`
      - `test(06-01): step definitions brouillon-liasse + builder unMappingLiasse2026`
  </behavior>
  <action>
    Décomposer le travail en cycles RED → GREEN micro-incrémentaux par fichier. Honorer D-L6.1 (case-par-case explicite), D-L6.3 (versioning + fail-fast), D-A6.1 (4 annexes 2033 + 2031-SD + 2042-C-PRO), D-A6.2 (postes manuels marqués), D-A6.3 (charges via `QUALIFICATIONS_DEDUCTIBLES`). Ubiquitous language fr (Mapping, Case, Section, Annexe — pas Code/Line/Field).

    **Décisions implémentées :**
    - D-L6.3 → `MappingLiasseProviderEnMemoire` avec 1 millésime + fail-fast `MappingLiasseAbsent`.
    - D-L6.1 → DTO `CaseLiasseDto { numero, libelleOfficiel, valeur }` (les 3 champs visibles obligatoires).
    - D-A6.2 → champ `mention?: string` + `bandeauPostesManuels?: boolean` au niveau section.

    **Codes cerfa à figer dans `mapping-liasse-2026.ts`** : ouvrir le PDF officiel `2031-SD millésime 2026` au moment de l'écriture du fichier. Les valeurs MEDIUM-confidence du RESEARCH.md §Cerfa Case Mapping (`CB`/`CC`, `FC`/`FK`/`FY`, lignes 210/218/254/280) doivent être validées contre le PDF officiel. Documenter la source URL dans JSDoc en tête de fichier.

    **À ne PAS faire (anti-patterns) :**
    - Ne pas hardcoder de numéro de case dans `application/` ou `web/` (pitfall §1 RESEARCH.md).
    - Ne pas importer pdfmake/kysely/fastify dans `domain/fiscalite/liasse/` (anti-pattern §1).
    - Ne pas couvrir 2027/2028 dans la `Map` (différence sémantique vs `RegleFiscaleProvider` — pitfall §6).
    - Ne pas ajouter Zod dans le domaine (D-15).
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test -- --run tests/unit/fiscalite/mapping-liasse-provider.test.ts && pnpm test:bdd -- --tags "@phase6-mapping-versionne" && grep -rn "from.*infrastructure\|from.*pdfmake\|from.*kysely\|from.*fastify\|from.*zod" src/domain/fiscalite/liasse/ | grep -v '^#' | grep -cv "^Binary" | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>
    - `pnpm test -- --run tests/unit/fiscalite/mapping-liasse-provider.test.ts` exit 0 (≥ 3 tests passent).
    - `pnpm test:bdd -- --tags "@phase6-mapping-versionne"` 3 scénarios verts.
    - `grep -n "from.*infrastructure\|from.*pdfmake\|from.*kysely\|from.*fastify\|from.*zod" src/domain/fiscalite/liasse/` retourne 0 résultats (hexagonal strict).
    - Le data file `mapping-liasse-2026.ts` cite la source `impots.gouv.fr/sites/default/files/formulaires/2031-sd/2026/2031-sd_5396.pdf` en JSDoc.
    - `pnpm lint:deps` exit 0.
    - 3 commits atomiques.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: TDD use case genererBrouillonLiasse (régime réel, snapshot uniquement, sans traçabilité ni réconciliation)</name>
  <files>
    src/application/fiscalite/generer-brouillon-liasse.ts,
    tests/unit/fiscalite/generer-brouillon-liasse.test.ts,
    tests/bdd/features/brouillon-liasse-reel.feature,
    tests/bdd/step_definitions/brouillon-liasse.steps.ts,
    tests/_builders/fiscalite.ts
  </files>
  <read_first>
    src/domain/fiscalite/declaration-annuelle.ts,
    src/domain/fiscalite/declaration-annuelle-repository.ts,
    src/domain/fiscalite/qualification-fiscale.ts (constante QUALIFICATIONS_DEDUCTIBLES),
    src/domain/fiscalite/liasse/mapping-liasse-provider.ts (créé Task 1),
    src/domain/fiscalite/liasse/case-liasse.ts (créé Task 1),
    src/application/fiscalite/exporter-pdf-recap.ts (pattern orchestrateur cross-BC),
    src/application/fiscalite/calculer-amortissement.ts (pour comprendre comment dotation est exposée),
    .planning/phases/06-liasse-2031-cfe/06-PATTERNS.md (Pattern critique 7 — use case orchestrateur),
    .planning/phases/06-liasse-2031-cfe/06-RESEARCH.md (§Cerfa Case Mapping pour chaque source de case + §Common Pitfalls §3 §7),
    .planning/phases/06-liasse-2031-cfe/06-CONTEXT.md (D-A6.2, D-A6.3, D-A6.4, D-T6.4).
  </read_first>
  <behavior>
    **TDD RED → GREEN → REFACTOR.**

    **RED — `tests/bdd/features/brouillon-liasse-reel.feature` (4 scénarios initiaux @phase6 @phase6-liasse-reel — les 4 autres dans Plan 03/05) :**
    - Scénario 1 : `Etant donné une DeclarationAnnuelle clôturée en régime réel avec recettes 12000 €` `Quand on génère le brouillon liasse pour cette déclaration` `Alors le brouillon contient une section "2033-B — Compte de résultat"` `Et une case "{code recettes}" porte la valeur "12000,00 €"`.
    - Scénario 2 : `Etant donné une DeclarationAnnuelle clôturée en régime réel avec dotation amortissement 3500 € et bénéfice` `Quand on génère le brouillon` `Alors la case dotation amortissement vaut "3500,00 €"` `Et la case 2031-SD bénéfice fiscal est non vide` `Et la case 2031-SD déficit fiscal est vide`.
    - Scénario 3 : `Etant donné une DeclarationAnnuelle réel avec déficit fiscal` `Quand on génère le brouillon` `Alors la case 2031-SD bénéfice fiscal est vide` `Et la case 2031-SD déficit fiscal porte la valeur absolue du déficit`.
    - Scénario 4 : `Etant donné une DeclarationAnnuelle réel` `Quand on génère le brouillon` `Alors la section "2033-A — Bilan simplifié" affiche un bandeau "postes à compléter manuellement"` `Et certaines cases 2033-A portent la mention "à compléter manuellement"`.

    **RED — `tests/unit/fiscalite/generer-brouillon-liasse.test.ts` :**
    - `genererBrouillonLiasse({ declarationId }, deps)` retourne un `BrouillonLiasseDto` avec `regimeApplique='reel'`.
    - Si `declarationId` introuvable → throw `DeclarationIntrouvable`.
    - Si `mappingProvider.pour(decl.exercice)` throw `MappingLiasseAbsent` → l'erreur remonte (pas swallow).
    - La valeur `decl.recettesTotales` du snapshot apparaît dans la case 2033-B mappée sur `source='recettesTotales'`.
    - **Invariant cohérence flux** (RESEARCH.md §Cerfa Case Mapping §2033-B / §2033-C) : la case 2033-C `dotation exercice` et la case 2033-B `dotations amortissement` portent toutes deux la même `decl.dotationAmortissement.enEuros()`.
    - **Anti-pattern §3 (pitfall §3)** : test assert que les cases 2033-B `autres charges externes` n'incluent JAMAIS `chargesQualifieesParCategorie.amelioration` (l'amélioration va sur 2033-C `augmentations exercice`, pas 2033-B).
    - Le `bailleurNom` du DTO vient du bailleur récupéré via `bailleurRepo.trouver()`.

    **GREEN — Implémentation `src/application/fiscalite/generer-brouillon-liasse.ts` :**

    Interfaces publiques :
    - `interface GenererBrouillonLiasseCommande { declarationId: DeclarationAnnuelleId; }`
    - `interface GenererBrouillonLiasseDeps { declRepo: DeclarationAnnuelleRepository; bailleurRepo: BailleurRepository; mappingProvider: MappingLiasseProvider; }` — Wave 1 minimal (Plan 03 ajoutera `recettesRepo`/`chargesRepo`/`tableauAmortRepo` pour traçabilité).
    - `export class DeclarationIntrouvable extends Error { constructor(public readonly id: string) }` (réutiliser celle d'`exporter-csv-fiscal.ts` si déjà importée). NB : ne pas dupliquer — vérifier `src/application/fiscalite/exporter-csv-fiscal.ts` ou `exporter-pdf-recap.ts` et **réexporter** depuis ce module si pertinent.

    Algorithme `genererBrouillonLiasse` :
    1. `decl = await declRepo.trouverParId(commande.declarationId)` → throw `DeclarationIntrouvable` si null.
    2. `bailleur = await bailleurRepo.trouver()` → throw `BailleurIntrouvable` si null.
    3. `mapping = mappingProvider.pour(decl.exercice)` — fail-fast `MappingLiasseAbsent` propage.
    4. **Si `decl.regimeApplique === 'micro_bic'` → Plan 02** (pour Wave 1, throw `Error('Régime micro-BIC : brouillon disponible Plan 02')` ou retourner un DTO minimal vide pour les sections autres que 2042-C-PRO. **Décision recommandée Wave 1 : throw provisoire avec message explicite** — fail-fast plutôt que renvoyer un brouillon incomplet ; Plan 02 supprime ce throw).
    5. Pour chaque annexe (`'2031-SD'`, `'2033-A'`, `'2033-B'`, `'2033-C'`, `'2033-D'`), itérer les `CaseLiasseDef` du mapping et résoudre `valeur` via une fonction privée `resoudreValeurCase(def, decl): Money | null`.
    6. `resoudreValeurCase` (switch sur `def.source`) :
       - `'recettesTotales'` → `decl.recettesTotales`.
       - `'beneficeFiscal'` → si `decl.beneficeFiscal()` (ou champ équivalent) > 0 → valeur ; sinon → `null` (case vide).
       - `'deficitFiscal'` → si déficit < 0 → valeur absolue (`Money.fromCentimes(BigInt.abs(...))`) ; sinon → `null`.
       - `'dotationAmortissement'` → `decl.dotationAmortissement`.
       - `'ardGenere'` → `decl.ardGenere`.
       - `'ardConsomme'` → `decl.ardConsomme`.
       - `'chargesAutresExternes'` → `decl.chargesQualifieesParCategorie.entretien_reparation.additionner(decl.chargesQualifieesParCategorie.charge_courante_periodique)` — **filtre `QUALIFICATIONS_DEDUCTIBLES` strict** (jamais `amelioration` ni `non_qualifie` ni `non_deductible` — pitfall §3 + §7).
       - `'chargesImpotsTaxes'` → V1 = `Money.zero()` ou sous-catégorie de `charge_courante_periodique` si ventilation disponible — sinon `null` + mention "à compléter manuellement" (D-A6.2 honnête).
       - `'immobilisationsConstructionsBrut'` → `Σ` `decl.composantsSnapshot.filter(c => c.type in ['gros_oeuvre','toiture_facade','installations_techniques','agencements_interieurs']).map(c => c.montantHt)`. Wave 1 OK car snapshot Phase 5 contient le champ.
       - `'immobilisationsMobilierBrut'` → `Σ` composants `type === 'mobilier'`.
       - `'amortissementsCumulesConstructions'`/`'amortissementsCumulesMobilier'` → V1 si le snapshot expose `amortissementCumule` par composant, sinon `null` + mention. **Vérifier `ComposantSnapshot` interface Phase 5.**
       - `'vncConstructions'` → `immobilisations - amortissementsCumules` (calcul pur, jamais null si les deux champs précédents non null).
       - `'manuel'` → `null` + `mention: "à compléter manuellement"` (D-A6.2).
    7. Construire `BrouillonLiasseDto { exercice: decl.exercice, regimeApplique: 'reel', bailleurNom: bailleur.nomComplet(), sections, clotureLe: decl.clotureLe }`. La section 2033-A porte `bandeauPostesManuels: true`.
    8. Retourner le DTO.

    **REFACTOR :**
    - Extraire `resoudreValeurCase` dans un fichier domaine pur `src/domain/fiscalite/liasse/resoudre-valeur-case.ts` (fonction pure) si test lit mieux séparément. Sinon laisser inline dans le use case.
    - Vérifier qu'aucun import infra/HTTP ne s'est glissé.

    Commits :
    - `test(06-01): scenarios feature brouillon-liasse-reel + tests use case generer-brouillon-liasse (RED)`
    - `feat(06-01): use case genererBrouillonLiasse régime réel (snapshot uniquement)`
    - `refactor(06-01): extraire resoudreValeurCase si pertinent`
  </behavior>
  <action>
    Use case orchestrateur cross-BC pattern miroir `exporter-pdf-recap.ts` (Pattern critique 7 RESEARCH.md). Snapshot = source de vérité pour les VALEURS (D-T6.4). Mapping = source de vérité pour les LIBELLÉS + NUMÉROS (D-L6.3). Wave 1 ne fait PAS la traçabilité (sources vivantes) — Plan 03 l'ajoute via extension du DTO + nouvelles deps.

    Filtrage charges : `QUALIFICATIONS_DEDUCTIBLES = ['entretien_reparation', 'amelioration', 'charge_courante_periodique']` (constante Phase 5 `qualification-fiscale.ts`). **MAIS** sur 2033-B `amelioration` est **exclu** car immobilisable (2033-C augmentations — pitfall §3). Sur 2033-B ne sommer que `entretien_reparation + charge_courante_periodique`.

    **Décisions implémentées :**
    - D-L6.1 → case-par-case orchestré via mapping.
    - D-A6.2 → champ `mention: 'à compléter manuellement'` + `bandeauPostesManuels: true` sur section 2033-A.
    - D-A6.3 → cœur du brouillon = 2033-B (recettes + charges qualifiées + dotation + résultat).
    - D-A6.4 → 2033-C (composants snapshot) + 2033-D (ARD via `ardGenere`/`ardConsomme`).
    - D-T6.4 → valeurs depuis snapshot uniquement (PAS de read-model traçabilité Wave 1).

    **À ne PAS faire (anti-patterns) :**
    - Ne pas appeler `recettesRepo`/`chargesRepo` Wave 1 (Plan 03).
    - Ne pas inclure `amelioration` dans 2033-B `chargesAutresExternes` (pitfall §3).
    - Ne pas hardcoder de numéro de case ni de libellé dans ce fichier — tout vient du mapping.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test -- --run tests/unit/fiscalite/generer-brouillon-liasse.test.ts && pnpm test:bdd -- --tags "@phase6-liasse-reel" && grep -n "amelioration" src/application/fiscalite/generer-brouillon-liasse.ts | grep -v '^#' | grep -c "2033-B\|chargesAutresExternes" | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>
    - `pnpm test -- --run tests/unit/fiscalite/generer-brouillon-liasse.test.ts` exit 0 (≥ 6 tests).
    - `pnpm test:bdd -- --tags "@phase6-liasse-reel"` 4 scénarios verts.
    - `grep -n "case '[A-Z]\{2\}'" src/application/fiscalite/generer-brouillon-liasse.ts` → 0 (zéro hardcode case).
    - `pnpm lint:deps` exit 0.
    - 3 commits atomiques.
  </done>
</task>

<task type="execute">
  <name>Task 3: Route Fastify + vue EJS + helper formaterCaseLiasse + DI dans main.ts + lien depuis /fiscalite</name>
  <files>
    src/web/routes/fiscalite/liasse.ts,
    src/web/views/pages/fiscalite/brouillon-liasse.ejs,
    src/web/views/partials/partial-bandeau-brouillon-liasse.ejs,
    src/web/views/partials/partial-tableau-liasse-section.ejs,
    src/web/helpers/formater-case-liasse.ts,
    src/web/views/pages/fiscalite/index.ejs,
    src/main.ts,
    tests/unit/helpers/formater-case-liasse.test.ts,
    tests/integration/web/route-liasse.test.ts
  </files>
  <read_first>
    src/web/routes/fiscalite/exports.ts (pattern route + RFC 6266 + try/catch),
    src/web/routes/fiscalite/racine.ts (pattern injection deps + render),
    src/web/views/pages/fiscalite/recap-annuel.ejs (pattern page fiscale avec sections),
    src/web/views/pages/fiscalite/index.ejs (page racine à enrichir avec bloc "Brouillons de liasse"),
    src/web/views/partials/data-table.ejs (à composer pour le tableau case-par-case),
    src/web/views/partials/banniere-warning.ejs (réutilisé pour bandeau S3 postes manuels),
    src/web/views/partials/layout-debut.ejs (pattern layout + breadcrumbs),
    src/web/helpers/content-disposition.ts (style des helpers existants),
    src/main.ts (point d'injection DI à étendre),
    .planning/phases/06-liasse-2031-cfe/06-UI-SPEC.md (§S1, §S2, §S3, §S11, §Accessibilité, §Partials),
    .planning/phases/06-liasse-2031-cfe/06-CONTEXT.md (D-L6.1, D-L6.4),
    .planning/phases/06-liasse-2031-cfe/06-RESEARCH.md (§Pattern 5 — route export).
  </read_first>
  <action>
    Câbler la route GET HTML `/fiscalite/declarations/:id/liasse` + vue + bandeau S1 + tableau S2 + bandeau S3 (postes manuels 2033-A) + bloc "Brouillons de liasse" sur la page racine `/fiscalite` (S11).

    **`src/web/helpers/formater-case-liasse.ts`** :
    - `export function formaterCaseLiasse(numero: string): string` — retourne `<span class="case-cerfa" style="font-family:ui-monospace, monospace; font-size:14px">${escapeHtml(numero)}</span>`. **Échapper HTML** (XSS — V5).
    - Tests unitaires : sortie HTML pour `CB`, échappement `<script>`.

    **`src/web/views/partials/partial-bandeau-brouillon-liasse.ejs`** (UI-SPEC §S1) :
    - `<aside role="status" aria-live="polite" style="background: var(--couleur-accent-bg); padding:16px; border-left:4px solid var(--couleur-accent); margin-bottom:24px;">` avec icône `ⓘ` `aria-hidden="true"` + copie exacte UI-SPEC : "**Brouillon liasse fiscale {Année}.** À reporter case-par-case sur votre télédéclaration impots.gouv.fr. Ce document n'est pas une transmission officielle."
    - Props : `exercice: number`.

    **`src/web/views/partials/partial-tableau-liasse-section.ejs`** (UI-SPEC §S2) :
    - Props : `section: SectionLiasseDto`, `exercice: number`.
    - Rend `<section aria-label="${libellé annexe}">` + `<h3>${titre}</h3>` + (si `section.bandeauPostesManuels` → `<%- include('banniere-warning', { message: "Bilan simplifié (2033-A) : seuls les postes calculables sont remplis ci-dessous (immobilisations, amortissements cumulés, VNC). Les autres postes — trésorerie, créances, dettes, emprunts — restent à compléter manuellement sur la télédéclaration." }) %>`) + tableau composé via `data-table.ejs` ou directement avec `<table aria-label="${section.titre} ${exercice}"><thead><tr><th scope="col">Case</th><th scope="col">Libellé officiel</th><th scope="col" class="numeric">Valeur</th></tr></thead><tbody>` 3 colonnes Wave 1 (Sources = colonne 4 ajoutée Plan 03). Lignes : `<tr><td><%- formaterCaseLiasse(c.numero) %></td><td><%= c.libelleOfficiel %></td><td class="numeric"><%= c.valeur ? formatMoney(c.valeur) : '<span style="color:var(--couleur-neutre);font-style:italic">à compléter manuellement</span>' %></td></tr>`.

    **`src/web/views/pages/fiscalite/brouillon-liasse.ejs`** (UI-SPEC §S2 ARIA labels) :
    - Layout `<%- include('../../partials/layout-debut', { titre: 'Brouillon liasse fiscale ' + dto.exercice, breadcrumbs: [{label:'Fiscalité', url:'/fiscalite'}, {label:'Brouillon liasse ' + dto.exercice}], navActive: 'fiscalite' }) %>`.
    - `<%- include('../../partials/partial-bandeau-brouillon-liasse', { exercice: dto.exercice }) %>`
    - `<h2>Brouillon liasse fiscale <%= dto.exercice %> — <%= dto.bailleurNom %></h2>`
    - Itérer `dto.sections.forEach(s => include('partial-tableau-liasse-section', { section:s, exercice: dto.exercice }))` — ordre : 2031-SD, 2033-A, 2033-B, 2033-C, 2033-D.
    - Layout-fin.
    - **PAS de section Exports Wave 1** (Plan 05).

    **`src/web/routes/fiscalite/liasse.ts`** (pattern miroir `exports.ts`) :
    - Interface `LiasseRouteDeps { declRepo, bailleurRepo, mappingProvider }`.
    - `app.get<{ Params: { id: string } }>('/fiscalite/declarations/:id/liasse', async (req, reply) => { ... })` :
      1. `declarationId = req.params.id as DeclarationAnnuelleId`.
      2. try block : `dto = await genererBrouillonLiasse({ declarationId }, deps)`.
      3. `return reply.view('pages/fiscalite/brouillon-liasse.ejs', { dto, navActive: 'fiscalite' });`
      4. catch `DeclarationIntrouvable` → `reply.status(404).view('pages/erreur.ejs', { message: 'Déclaration introuvable.' })`.
      5. catch `MappingLiasseAbsent` → `reply.status(422).view('pages/erreur.ejs', { message: 'Mapping de la liasse non disponible pour l'année ' + err.millesime + '. Mettez à jour l'application — le mapping est revu chaque janvier après publication des nouveautés fiscales (loi de finances).' })`. **Copie EXACTE UI-SPEC Copywriting** (R1.1 + D-L6.3 honnêteté).
    - **Si `decl.regimeApplique === 'micro_bic'`** : Wave 1 throw provisoire dans use case → route catch et affiche erreur "Brouillon micro-BIC arrive avec Plan 02". À simplifier Plan 02.

    **`src/web/views/pages/fiscalite/index.ejs` — ajouter bloc "Brouillons de liasse"** (UI-SPEC §S11) :
    - Sous le bloc existant Phase 5 (déclarations clôturées), ajouter `<section aria-label="Brouillons de liasse fiscale">` avec liste `<ul>` des `DeclarationAnnuelle.clôturée` filtrées sur `regimeApplique === 'reel'` (micro masqué Wave 1, Plan 02 enlève le filtre). Chaque item : `<a href="/fiscalite/declarations/${decl.id}/liasse">Consulter le brouillon liasse ${decl.exercice}</a>`.
    - Empty state (UI-SPEC Copywriting) : "Aucune déclaration clôturée pour {Année}. Clôturez d'abord votre exercice fiscal pour générer le brouillon de la liasse." + CTA "Clôturer un exercice fiscal" → `/fiscalite`.
    - **Réutiliser `lister-vue-consolidee.ts`** ou créer un appel additionnel `declRepo.listerClotureesParBailleur(bailleurId)` si non exposé. Vérifier l'API existante.

    **`src/main.ts` — DI** :
    - Instancier `mappingProvider = new MappingLiasseProviderEnMemoire()`.
    - Wire la route via `await app.register(async (instance) => registerFiscaliteLiasseRoutes(instance, { declRepo, bailleurRepo, mappingProvider }))`.

    **Tests intégration `tests/integration/web/route-liasse.test.ts`** :
    - DB éphémère, créer une `DeclarationAnnuelle` clôturée régime réel via builders, hit GET `/fiscalite/declarations/{id}/liasse`, assert 200 + HTML contient le bandeau S1 + `<aside role="status">` + `<table aria-label="Annexe 2033-B`.
    - Hit GET avec ID inexistant → 404.
    - **Préparer le scénario fail-fast** : DI un `MappingLiasseProviderFake` qui ne couvre QUE 2025, créer `DeclarationAnnuelle` exercice 2026 → 422 + page d'erreur contenant "Mapping de la liasse non disponible".

    **Décisions implémentées :**
    - D-L6.1 → tableau case-par-case `{numero, libellé, valeur}`.
    - D-L6.4 → vue HTML principale (PDF/CSV = Plan 05).
    - UI-SPEC §S1 → bandeau brouillon persistant.
    - UI-SPEC §S2 → tableau ARIA labelé par annexe.
    - UI-SPEC §S3 → bandeau postes manuels sur 2033-A.
    - UI-SPEC §S11 → bloc "Brouillons de liasse" sur `/fiscalite`.
    - UI-SPEC §Accessibilité → `<th scope="col">`, `aria-label` unique par annexe + millésime, `class="numeric"` colonnes monétaires.

    **À ne PAS faire :**
    - Pas de bouton "Télécharger PDF/CSV" Wave 1 (Plan 05).
    - Pas de colonne "Sources" Wave 1 (Plan 03).
    - Pas de bandeau réconciliation (Plan 03).
    - Pas de bandeau rectificative (Plan 04).
    - Pas de nouveau token CSS (UI-SPEC §Design System verrouillé — utiliser strictement les tokens app.css).
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test -- --run tests/integration/web/route-liasse.test.ts tests/unit/helpers/formater-case-liasse.test.ts && curl -s http://localhost:0 2>/dev/null; node -e "console.log('skip curl in CI')" || true</automated>
  </verify>
  <done>
    - `pnpm test -- --run tests/integration/web/route-liasse.test.ts` exit 0 (≥ 3 tests dont 200, 404, 422).
    - `pnpm test -- --run tests/unit/helpers/formater-case-liasse.test.ts` exit 0 (assertions XSS escape + monospace style).
    - `grep -n "aria-label=\"Annexe 2033" src/web/views/pages/fiscalite/brouillon-liasse.ejs src/web/views/partials/partial-tableau-liasse-section.ejs` retourne ≥ 1 résultat (UI-SPEC §Accessibilité).
    - `grep -n "Brouillon liasse fiscale" src/web/views/partials/partial-bandeau-brouillon-liasse.ejs` retourne 1 résultat (copywriting UI-SPEC).
    - `grep -n "MappingLiasseProviderEnMemoire" src/main.ts` retourne 1 résultat (DI câblée).
    - `pnpm lint:deps` exit 0.
    - 2 commits : `feat(06-01): route GET /fiscalite/declarations/:id/liasse + vue brouillon-liasse + partials S1/S2/S3` + `feat(06-01): bloc brouillons-de-liasse sur /fiscalite + DI MappingLiasseProvider`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Navigateur → Fastify route `/fiscalite/declarations/:id/liasse` | `req.params.id` est input untrusted (string libre). Validation : cast brand type + traitement comme opaque ID dans `declRepo.trouverParId`. SQL safe via Kysely paramétré (Phase 5). |
| EJS render → réponse HTML | Sortie HTML doit échapper le `bailleurNom` + `numero` de case + `libelleOfficiel` (XSS — V5). EJS `<%= %>` auto-échappe ; `<%- %>` doit être réservé aux partials internes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-LIASSE-W1-01 | Information Disclosure | `req.params.id` brut dans message d'erreur | mitigate | `DeclarationIntrouvable.constructor(id)` stocke l'ID mais le message client est "Déclaration introuvable." sans l'ID (cohérent `exporter-csv-fiscal.ts`). |
| T-06-LIASSE-W1-02 | Tampering (XSS) | Helper `formaterCaseLiasse(numero)` rend `<span>` avec contenu | mitigate | Échapper `numero` via `escapeHtml` (V5). Test unitaire vérifie qu'`<script>` est échappé. La donnée vient du mapping interne (pas user input) mais V1 défense en profondeur. |
| T-06-LIASSE-W1-03 | DoS génération brouillon | Use case `genererBrouillonLiasse` itère `composantsSnapshot` (pouvant être large) | accept | 1 brouillon = 1 exercice = ≤ 100 biens × 6 composants = 600 itérations, négligeable. RESEARCH §T-06-LIASSE-03. |
| T-06-LIASSE-W1-04 | Information Disclosure | Message d'erreur `MappingLiasseAbsent` cite le millésime à l'utilisateur | accept | Le millésime n'est pas une donnée sensible. La copie UI-SPEC Copywriting fait partie de la pédagogie R1.1 (honnêteté envers l'utilisateur). |
| T-06-LIASSE-W1-05 | Hexagonal violation (Configuration) | Risque d'import infra dans `src/domain/fiscalite/liasse/` | mitigate | `pnpm lint:deps` (`dependency-cruiser`) en pre-commit + verify gate. Phase 5.1 a démontré le pattern, 0 violation tolérée. |
</threat_model>

<verification>
- `pnpm typecheck` exit 0.
- `pnpm test -- --run` tous verts.
- `pnpm test:bdd -- --tags "@phase6-mapping-versionne or @phase6-liasse-reel"` 7 scénarios verts (3 + 4).
- `pnpm lint:deps` exit 0 (hexagonal strict).
- `grep -rn "from.*infrastructure\|from.*pdfmake\|from.*kysely\|from.*fastify\|from.*zod" src/domain/fiscalite/liasse/` retourne 0 résultats.
- `grep -rn "'[A-Z]\{2,3\}'" src/application/fiscalite/generer-brouillon-liasse.ts src/web/routes/fiscalite/liasse.ts src/web/views/pages/fiscalite/brouillon-liasse.ejs` retourne 0 ligne contenant un code lettre cerfa hardcodé (validation pitfall §1).
- Manuel : ouvrir `/fiscalite/declarations/{id}/liasse` sur une déclaration réelle clôturée Phase 5 → vue rendue avec 5 tableaux ARIA-labelés + bandeau S1 visible.
</verification>

<success_criteria>
- ROADMAP success criterion 1 (FIS-05) : brouillon 2031-SD + annexes 2033-A/B/C/D rendu en HTML pour régime réel (PDF/CSV = Plan 05).
- 100 % couverture sur `src/domain/fiscalite/liasse/mapping-liasse-provider.ts` + `src/domain/fiscalite/liasse/case-liasse.ts` (BDD_PRACTICES.md §7).
- Toutes les décisions D-L6.1, D-L6.2, D-L6.3, D-A6.1, D-A6.2, D-A6.3, D-A6.4 partiellement couvertes (totalité avec Plans 02-05).
</success_criteria>

<output>
After completion, create `.planning/phases/06-liasse-2031-cfe/06-01-SUMMARY.md` listant :
- Cases cerfa figées (codes lettres validés sur PDF officiel).
- Décisions de mapping prises pour ambiguïtés (e.g. case bénéfice = `CB` ou `1GF`).
- Cohérence flux 2033-B/2033-C invariant testée.
- Route + vue navigables, screenshot ou description ASCII de la page.
- Backlog pour Plans 02-05 (micro, traçabilité, rectificative, exports).
</output>
</content>
</invoke>