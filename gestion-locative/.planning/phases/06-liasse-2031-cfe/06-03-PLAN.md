---
phase: 06-liasse-2031-cfe
plan: 03
type: tdd
wave: 2
depends_on: [06-01]
files_modified:
  - src/domain/fiscalite/reconciliation.ts
  - src/domain/fiscalite/liasse/case-liasse.ts
  - src/application/fiscalite/generer-brouillon-liasse.ts
  - src/web/views/pages/fiscalite/brouillon-liasse.ejs
  - src/web/views/partials/partial-tableau-liasse-section.ejs
  - src/web/views/partials/partial-drill-down-sources.ejs
  - src/web/views/partials/partial-bandeau-reconciliation.ejs
  - tests/unit/fiscalite/reconciliation.test.ts
  - tests/bdd/features/liasse-tracabilite.feature
  - tests/bdd/step_definitions/brouillon-liasse.steps.ts
  - tests/integration/web/route-liasse.test.ts
autonomous: true
requirements: [FIS-05]
tags: [fiscalite, liasse, tracabilite, reconciliation, audit]

must_haves:
  truths:
    - "Chaque case du tableau case-par-case affiche un lien 'Voir N sources' ou '—' si aucune source mappable (D-T6.1)."
    - "Le drill-down `<details>` affiche recettes (Encaissements), charges (Justificatifs qualifiés déductibles uniquement) et amortissement (lignes AmortissementExercice) avec URLs vers pièces (D-T6.2)."
    - "Les charges non_qualifie et non_deductible sont EXCLUES du drill-down (pitfall §7 — filtre QUALIFICATIONS_DEDUCTIBLES)."
    - "Si Σ sources vivantes ≠ valeur snapshot pour au moins une case → bandeau de réconciliation rouge en tête de page avec le nombre de pièces modifiées (D-T6.4)."
    - "La VALEUR affichée dans le tableau reste TOUJOURS celle du snapshot, jamais recalculée depuis les sources vivantes (anti-pattern §3 + §4 — D-T6.4)."
    - "Aucun bouton 'Re-calculer' n'est rendu (anti-pattern §11)."
    - "La fonction `reconcilier(snapshot, sourcesVivantes)` est pure (zéro dépendance Clock/repo)."
  artifacts:
    - path: "src/domain/fiscalite/reconciliation.ts"
      provides: "Fonction pure reconcilier() + types EcartReconciliationParCase / ResultatReconciliation"
      exports: ["reconcilier", "EcartReconciliationParCase", "ResultatReconciliation"]
    - path: "src/web/views/partials/partial-drill-down-sources.ejs"
      provides: "Partial drill-down `<details>` natif (S4)"
    - path: "src/web/views/partials/partial-bandeau-reconciliation.ejs"
      provides: "Partial bandeau destructif S5 (rendu conditionnel)"
  key_links:
    - from: "src/application/fiscalite/generer-brouillon-liasse.ts"
      to: "src/domain/fiscalite/reconciliation.ts → reconcilier()"
      via: "appel pur après agrégation sources vivantes"
      pattern: "reconcilier\\(snapshotMap, sourcesVivantesMap\\)"
    - from: "src/application/fiscalite/generer-brouillon-liasse.ts"
      to: "RecettesRepository / ChargesRepository / TableauAmortissementRepository"
      via: "DI deps (Phase 5, lecture seule)"
      pattern: "recettesRepo\\.|chargesRepo\\.|tableauAmortRepo\\."
---

<objective>
**Traçabilité + réconciliation (FIS-05 — slice 3, CRITIQUE audit).** Brancher le drill-down sources et la fonction pure de réconciliation snapshot/vivant sur le DTO et la vue.

**Décisions implémentées :**
- D-T6.1 → colonne "Sources" cliquable HTML.
- D-T6.2 → granularité par case cerfa.
- D-T6.3 → read-model construit à la génération (use case agrège via Recettes/Charges/Tableau repositories Phase 5).
- D-T6.4 (CRITIQUE) → snapshot fait foi, `reconcilier` retourne un SIGNAL (pas une nouvelle valeur), bandeau S5 visible si écart.

**Périmètre :**
- Nouvelle fonction pure `reconcilier(snapshot, sourcesVivantes): ResultatReconciliation` dans le domaine.
- Extension `CaseLiasseDto` avec `sources?: ReadonlyArray<SourceDto>` + `BrouillonLiasseDto.reconciliation?: ResultatReconciliation`.
- Extension `genererBrouillonLiasse` avec 3 nouvelles deps (`recettesRepo`, `chargesRepo`, `tableauAmortRepo`).
- Nouveaux partials : `partial-drill-down-sources.ejs` + `partial-bandeau-reconciliation.ejs`. Extension `partial-tableau-liasse-section.ejs` avec colonne 4 "Sources".

**Purpose** : sécuriser l'audit-friendly D-T6.4 (point d'attention critique Phase 6 — anti-pattern §3 + §4 strict).
**Output** : tableau case-par-case avec colonne Sources cliquable + bandeau S5 conditionnel.
</objective>

<execution_context>
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/workflows/execute-plan.md
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-liasse-2031-cfe/06-CONTEXT.md
@.planning/phases/06-liasse-2031-cfe/06-RESEARCH.md
@.planning/phases/06-liasse-2031-cfe/06-PATTERNS.md
@.planning/phases/06-liasse-2031-cfe/06-UI-SPEC.md
@.planning/phases/06-liasse-2031-cfe/06-VALIDATION.md
@.planning/phases/06-liasse-2031-cfe/06-01-SUMMARY.md
@practices/BDD_PRACTICES.md
@practices/ACCESSIBILITY.md
@practices/UX_DESIGN.md

# Repositories Phase 5 consommés (lecture seule)
@src/domain/fiscalite/recettes-repository.ts
@src/domain/fiscalite/charges-repository.ts
@src/domain/fiscalite/tableau-amortissement-repository.ts
@src/domain/fiscalite/qualification-fiscale.ts
@src/domain/fiscalite/amortissement-exercice.ts
@src/domain/_shared/money.ts

# Artefacts Wave 1 à étendre
@src/application/fiscalite/generer-brouillon-liasse.ts
@src/domain/fiscalite/liasse/case-liasse.ts
@src/web/views/pages/fiscalite/brouillon-liasse.ejs
@src/web/views/partials/partial-tableau-liasse-section.ejs

<interfaces>
# Money (rappel)
- egale(other: Money): boolean
- soustraire(other: Money): Money
- additionner(other: Money): Money
- static zero(): Money
- enEuros(): string

# Repositories Phase 5 (signatures à confirmer dans le code)
RecettesRepository : sommeRecettesAnnuelles(bailleurId, exercice): Promise<Money>
ChargesRepository : sommeChargesParCategorie(bailleurId, exercice): Promise<Record<QualificationFiscale, Money>>
TableauAmortissementRepository : listerParBienExercice(bienId, exercice): Promise<AmortissementExercice[]>

# QUALIFICATIONS_DEDUCTIBLES (constante Phase 5)
['entretien_reparation', 'amelioration', 'charge_courante_periodique']

# Types DTO (à étendre)
SourceDto { type: 'recette' | 'charge' | 'amortissement'; label: string; url: string; montant: Money }
CaseLiasseDto { ..., sources?: ReadonlyArray<SourceDto> }
BrouillonLiasseDto { ..., reconciliation?: ResultatReconciliation }
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: TDD fonction pure reconcilier() + types + propriétés fast-check</name>
  <files>
    src/domain/fiscalite/reconciliation.ts,
    tests/unit/fiscalite/reconciliation.test.ts
  </files>
  <read_first>
    src/domain/_shared/money.ts (méthodes egale, soustraire, additionner, zero),
    .planning/phases/06-liasse-2031-cfe/06-RESEARCH.md (§Pattern 5 — fonction pure reconcilier, §Validation Architecture — propriétés fast-check 1 et 2),
    .planning/phases/06-liasse-2031-cfe/06-PATTERNS.md (Pattern critique 10),
    .planning/phases/06-liasse-2031-cfe/06-CONTEXT.md (D-T6.4 CRITIQUE audit),
    @fast-check/vitest usage Phase 5 (par exemple tests Money fast-check existants).
  </read_first>
  <behavior>
    **TDD RED → GREEN → REFACTOR.**

    **RED — `tests/unit/fiscalite/reconciliation.test.ts` :**

    Tests déterministes :
    - `reconcilier(new Map(), new Map()).cohérent === true && nbPiecesModifiees === 0`.
    - `reconcilier(Map([['c1', Money.fromCentimes(100n)]]), Map([['c1', Money.fromCentimes(100n)]])).cohérent === true`.
    - `reconcilier(Map([['c1', Money.fromCentimes(100n)]]), Map([['c1', Money.fromCentimes(150n)]])).cohérent === false && nbPiecesModifiees === 1 && ecartsParCase[0].ecart.egale(Money.fromCentimes(50n))`.
    - Case du snapshot absente de sourcesVivantes → fallback `Money.zero()` côté vivant (RESEARCH §Pattern 5).
    - Test régression D-T6.4 : la fonction ne mute pas les Maps en entrée.
    - Test : return type `ReadonlyArray<EcartReconciliationParCase>` (vérification compilation TS).

    Propriétés fast-check (2 obligatoires, RESEARCH §Validation Architecture) :
    1. **Idempotence sur données identiques** : `fc.property(arbitraryMoneyMap, m => reconcilier(m, m).cohérent === true)`.
    2. **Symétrie de l'écart** : `fc.property(arbitraryMoneyMap, arbitraryMoneyMap, (s, v) => reconcilier(s, v).ecartsParCase.every(e => e.ecart.egale(e.valeurVivante.soustraire(e.valeurSnapshot))))`.

    **GREEN — `src/domain/fiscalite/reconciliation.ts`** :

    Interfaces exportées :
    - `EcartReconciliationParCase { readonly caseId: string; readonly valeurSnapshot: Money; readonly valeurVivante: Money; readonly ecart: Money; }`
    - `ResultatReconciliation { readonly cohérent: boolean; readonly nbPiecesModifiees: number; readonly ecartsParCase: ReadonlyArray<EcartReconciliationParCase>; }`

    Fonction pure exportée :
    - `export function reconcilier(snapshot: ReadonlyMap<string, Money>, sourcesVivantes: ReadonlyMap<string, Money>): ResultatReconciliation`.
    - Itère les entries du snapshot, pour chaque clé compare avec `sourcesVivantes.get(caseId) ?? Money.zero()`, si `!valeurSnap.egale(valeurViv)` push entrée `{ caseId, valeurSnapshot, valeurVivante, ecart: valeurViv.soustraire(valeurSnap) }`.
    - Return `{ cohérent: ecarts.length === 0, nbPiecesModifiees: ecarts.length, ecartsParCase: ecarts }`.

    Aucun import infra/Clock/repo. JSDoc cite D-T6.4 + anti-patterns §3 + §4 Phase 5.

    Commits : `test(06-03): tests reconcilier (RED)` + `feat(06-03): fonction pure reconcilier`.
  </behavior>
  <action>
    Sens de l'écart : `ecart = valeurVivante - valeurSnapshot`. Tolérance strict 0 (Money BigInt).

    **Anti-patterns à ne PAS reproduire :**
    - Ne PAS retourner une "valeur corrigée" — la fonction produit un SIGNAL.
    - Ne PAS importer Clock/Repo/Money de l'infra.
    - Ne PAS muter les Maps en entrée.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test -- --run tests/unit/fiscalite/reconciliation.test.ts && (grep -n "from.*infrastructure\|from.*clock\|from.*repository" src/domain/fiscalite/reconciliation.ts || true) | grep -v '^#' | wc -l | awk '$1 == 0'</automated>
  </verify>
  <done>
    - `pnpm test -- --run tests/unit/fiscalite/reconciliation.test.ts` exit 0 (≥ 6 tests + 2 propriétés fast-check).
    - `grep -n "from.*infrastructure\|from.*clock\|from.*repository" src/domain/fiscalite/reconciliation.ts` retourne 0 résultats.
    - 2 commits atomiques.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Extension use case avec drill-down sources + appel reconcilier + tests BDD traçabilité</name>
  <files>
    src/domain/fiscalite/liasse/case-liasse.ts,
    src/application/fiscalite/generer-brouillon-liasse.ts,
    tests/_builders/fiscalite.ts,
    tests/bdd/features/liasse-tracabilite.feature,
    tests/bdd/step_definitions/brouillon-liasse.steps.ts,
    tests/unit/fiscalite/generer-brouillon-liasse.test.ts
  </files>
  <read_first>
    src/domain/fiscalite/recettes-repository.ts (signature sommeRecettesAnnuelles + autres méthodes existantes),
    src/domain/fiscalite/charges-repository.ts (signature sommeChargesParCategorie + lister*),
    src/domain/fiscalite/tableau-amortissement-repository.ts (signature listerParBienExercice + dernierArdCumuleBailleur),
    src/domain/fiscalite/qualification-fiscale.ts (constante QUALIFICATIONS_DEDUCTIBLES),
    src/domain/fiscalite/declaration-annuelle.ts (rappel champs snapshot pour map de réconciliation),
    src/domain/fiscalite/amortissement-exercice.ts (type lignes COMPOSANT vs SYNTHESE_BIEN),
    src/application/fiscalite/exporter-pdf-recap.ts (pattern d'agrégation cross-BC : decl + bailleur + biens + tableauxAmort),
    .planning/phases/06-liasse-2031-cfe/06-PATTERNS.md (Pattern critique 7),
    .planning/phases/06-liasse-2031-cfe/06-RESEARCH.md (§Common Pitfalls §7 filtre QUALIFICATIONS_DEDUCTIBLES),
    .planning/phases/06-liasse-2031-cfe/06-CONTEXT.md (D-T6.1, D-T6.2, D-T6.3, D-T6.4).
  </read_first>
  <behavior>
    **TDD RED → GREEN → REFACTOR.**

    **RED — extension `case-liasse.ts`** : ajouter type `SourceDto { type: 'recette' | 'charge' | 'amortissement'; label: string; url: string; montant: Money }` et champ `sources?: ReadonlyArray<SourceDto>` sur `CaseLiasseDto` + champ `reconciliation?: ResultatReconciliation` sur `BrouillonLiasseDto`.

    **RED — `tests/bdd/features/liasse-tracabilite.feature` (7 scénarios @phase6 @phase6-liasse-tracabilite) :**
    - 1: snapshot == vivant → aucun bandeau réconciliation, drill-down listing sources OK.
    - 2: snapshot ≠ vivant (somme charges changé) → bandeau S5 affiché avec "1 pièce modifiée".
    - 3: drill-down recettes : N `Encaissement` listés avec URL `/encaissements/:id` + date + locataire + montant.
    - 4: drill-down charges : N `Justificatif` listés avec URL `/coffre/justificatifs/:id` + date + libellé + montant + catégorie.
    - 5: drill-down amortissement : N lignes `AmortissementExercice` listées avec URL `/biens/:id/fiscalite/amortissement/:exercice`.
    - 6: justificatif `non_qualifie` exclu du drill-down (pitfall §7).
    - 7: justificatif `non_deductible` exclu du drill-down (pitfall §7).

    **RED — extension `tests/unit/fiscalite/generer-brouillon-liasse.test.ts` :**
    - Test : DTO contient `cases[i].sources` non vide pour les cases mappées sur `recettesTotales`/`chargesAutresExternes`/`dotationAmortissement`.
    - Test : cases mappées sur `manuel` ou autres `source` non sourceables → `sources` absent ou vide.
    - Test : `dto.reconciliation` présent, `cohérent === true` si snapshot == vivant.
    - Test : `dto.reconciliation.cohérent === false` si on stub le recettesRepo pour retourner une somme différente.
    - Test : `chargesRepo.listerJustificatifsQualifiesParExercice` filtre les `non_qualifie` (assert via stub).

    **GREEN — extension `src/application/fiscalite/generer-brouillon-liasse.ts`** :

    1. Étendre `GenererBrouillonLiasseDeps` : ajouter `recettesRepo: RecettesRepository`, `chargesRepo: ChargesRepository`, `tableauAmortRepo: TableauAmortissementRepository`, `bienRepo: BienRepository`.
    2. Après `mapping = mappingProvider.pour(decl.exercice)`, **agréger sources vivantes** :
       - `sommeRecettes = await recettesRepo.sommeRecettesAnnuelles(decl.bailleurId, decl.exercice)`.
       - `chargesParCat = await chargesRepo.sommeChargesParCategorie(decl.bailleurId, decl.exercice)`.
       - Filtrer par `QUALIFICATIONS_DEDUCTIBLES` (sauf `amelioration` pour 2033-B).
       - `tableauxAmort = (await Promise.all(biens.map(b => tableauAmortRepo.listerParBienExercice(b.id, decl.exercice)))).flat()`.
    3. Construire `sourcesVivantesMap: Map<string, Money>` indexée par `caseId` du mapping :
       - Pour chaque case avec `source: 'recettesTotales'` → `sourcesVivantesMap.set(case.caseId, sommeRecettes)`.
       - Pour `source: 'chargesAutresExternes'` → `entretien + charge_courante` (exclut `amelioration`).
       - Pour `source: 'dotationAmortissement'` → `Σ tableauxAmort.filter(t => t.type === 'COMPOSANT').reduce(...)`.
    4. Construire `snapshotMap: Map<string, Money>` identique en clés avec les valeurs résolues depuis le snapshot.
    5. `reconciliation = reconcilier(snapshotMap, sourcesVivantesMap)`.
    6. Pour chaque case affichée, attacher la liste `sources: SourceDto[]` :
       - Récupérer les Encaissements de l'exercice via `encaissementRepo.listerParBailleurExercice(bailleurId, exercice)` (méthode à utiliser si elle existe — sinon ajouter une méthode read-only ou utiliser `RecettesRepository` étendu Plan 03).
       - Pour les charges : `chargesRepo.listerJustificatifsQualifiesParExercice(bailleurId, exercice)` filtrés sur `QUALIFICATIONS_DEDUCTIBLES`.
       - Pour amortissement : `tableauxAmort.filter(t => t.type === 'COMPOSANT')` mappés.
       - Chaque `SourceDto` a `url` cliquable absolu (`/encaissements/:id`, `/coffre/justificatifs/:id`, `/biens/:bienId/fiscalite/amortissement/:exercice`).
    7. Construire `BrouillonLiasseDto` enrichi : `cases[i].sources` + `dto.reconciliation`.

    **REFACTOR** : extraire `attacherSources(cases, mapping, encaissements, justificatifs, tableauxAmort): CaseLiasseDto[]` dans une fonction privée pure (testable) ou dans un fichier domaine `src/domain/fiscalite/liasse/sources-par-case.ts` si la complexité justifie.

    Commits :
    - `test(06-03): scenarios feature liasse-tracabilite (RED)`
    - `feat(06-03): extension genererBrouillonLiasse avec sources vivantes + reconcilier`
    - `refactor(06-03): extraire attacherSources si pertinent`
  </behavior>
  <action>
    Cross-BC : utiliser uniquement les ports déjà créés en Phase 5. Si une méthode `listerJustificatifsQualifiesParExercice` n'existe pas sur `ChargesRepository`, soit **étendre** ce port (recommandé — extension côté domaine pure + impl côté infra), soit utiliser `JustificatifRepository` existant et filtrer côté use case.

    **Vérifier dans les fichiers `read_first` quelle méthode existe avant d'écrire le code** (le pattern Plan 05-11 a déjà construit certains ports).

    **Filtrage QUALIFICATIONS_DEDUCTIBLES strict** :
    - Drill-down charges : exclure `non_qualifie` + `non_deductible` (pitfall §7).
    - Mapping 2033-B `chargesAutresExternes` : exclure aussi `amelioration` (pitfall §3, slice 1).

    **Décisions implémentées :**
    - D-T6.1 → `sources` cliquables avec URL.
    - D-T6.2 → drill-down par case.
    - D-T6.3 → read-model construit dans le use case.
    - D-T6.4 → snapshot fait foi, reconciliation produit signal.

    **À ne PAS faire :**
    - Ne PAS recalculer la VALEUR des cases depuis les sources vivantes (pitfall §2).
    - Ne PAS inclure `non_qualifie` / `non_deductible` dans `sources` (pitfall §7).
    - Ne PAS inclure `amelioration` dans 2033-B `chargesAutresExternes` (pitfall §3).
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test -- --run tests/unit/fiscalite/generer-brouillon-liasse.test.ts && pnpm test:bdd -- --tags "@phase6-liasse-tracabilite"</automated>
  </verify>
  <done>
    - `pnpm test:bdd -- --tags "@phase6-liasse-tracabilite"` 7 scénarios verts.
    - `pnpm test -- --run tests/unit/fiscalite/generer-brouillon-liasse.test.ts` exit 0 (≥ 5 nouveaux tests).
    - `grep -n "QUALIFICATIONS_DEDUCTIBLES\|qualifications-deductibles\|qualificationsDeductibles" src/application/fiscalite/generer-brouillon-liasse.ts` retourne ≥ 1 résultat (filtre appliqué).
    - 3 commits atomiques.
  </done>
</task>

<task type="execute">
  <name>Task 3: Partials drill-down + bandeau réconciliation + intégration vue + tests route 200/422</name>
  <files>
    src/web/views/partials/partial-drill-down-sources.ejs,
    src/web/views/partials/partial-bandeau-reconciliation.ejs,
    src/web/views/partials/partial-tableau-liasse-section.ejs,
    src/web/views/pages/fiscalite/brouillon-liasse.ejs,
    src/main.ts,
    tests/integration/web/route-liasse.test.ts
  </files>
  <read_first>
    src/web/views/partials/partial-tableau-liasse-section.ejs (état Wave 1 — 3 colonnes),
    src/web/views/pages/fiscalite/brouillon-liasse.ejs (état Wave 1),
    src/web/views/partials/banniere-warning.ejs (référence pour bord gauche destructive),
    src/web/views/partials/partial-indexation-banner.ejs (pattern aside role=alert assertive),
    src/main.ts (DI à étendre avec recettesRepo/chargesRepo/tableauAmortRepo déjà câblés Phase 5),
    .planning/phases/06-liasse-2031-cfe/06-UI-SPEC.md (§S4 drill-down + §S5 réconciliation + §Accessibilité §Sélecteurs ARIA + §Copywriting Contract),
    public/styles/app.css (tokens `--couleur-destructive-bg`, `--couleur-destructive`, `--couleur-neutre`),
    .planning/phases/06-liasse-2031-cfe/06-CONTEXT.md (D-T6.4 anti-bouton "Re-calculer").
  </read_first>
  <action>
    Implémenter les partials S4 et S5 + intégration dans la vue principale + DI complète + tests route 200/422.

    **`src/web/views/partials/partial-drill-down-sources.ejs`** (UI-SPEC §S4) :
    - Props : `sources: SourceDto[]`, `nbSources: number`.
    - Si `nbSources === 0` → rendre `<span style="color: var(--couleur-neutre)">—</span>` (cas case non sourceable, e.g. dateClotureExercice).
    - Sinon `<details><summary role="button" class="secondary" style="padding:8px 12px">Voir <%= nbSources %> source<%= nbSources > 1 ? 's' : '' %></summary><ul style="list-style:none; padding-left:0; margin-top:8px">` + iter `sources.forEach(s => `<li><a href="<%= s.url %>"><%= s.label %></a> — <%= formatMoney(s.montant) %></li>`)` + `</ul></details>`.
    - **Zéro JS framework** (UI-SPEC §S4 + Pattern 11).
    - Liens internes uniquement (audit-friendly).

    **`src/web/views/partials/partial-bandeau-reconciliation.ejs`** (UI-SPEC §S5 CRITIQUE) :
    - Props : `reconciliation: ResultatReconciliation`, `dateCloture: Temporal.PlainDateTime`.
    - **Rendu conditionnel** : ne rendre QUE si `reconciliation.nbPiecesModifiees > 0`. Le serveur ne passe le partial qu'en cas d'écart (pas de logique de masquage côté template).
    - `<aside role="alert" aria-live="assertive" aria-label="Réconciliation données snapshot ≠ sources vivantes" style="background: var(--couleur-destructive-bg); border-left: 4px solid var(--couleur-destructive); padding: 16px; margin-bottom: 24px;">`.
    - Icône `<span aria-hidden="true">✕</span>` + copie EXACTE UI-SPEC : "**Données modifiées depuis la clôture du <%= formatDate(dateCloture) %>.** Le brouillon reflète la clôture (snapshot immuable). <%= reconciliation.nbPiecesModifiees %> pièce<%= reconciliation.nbPiecesModifiees > 1 ? 's' : '' %> ont changé depuis cette date. Les valeurs ci-dessous restent celles validées à la clôture."
    - Lien accent "Voir le détail des modifications" → `#section-reconciliation-detail` ou simplement un anchor scroll vers la première case avec ecart (Wave 3 V1 — anchor suffit, Phase 7 améliorera).
    - **Aucun bouton "Re-calculer"** (anti-pattern §11 strict).

    **Extension `src/web/views/partials/partial-tableau-liasse-section.ejs`** :
    - Passer de 3 colonnes à 4 colonnes : ajouter `<th scope="col">Sources</th>`.
    - Pour chaque ligne, ajouter `<td><%- include('partial-drill-down-sources', { sources: c.sources || [], nbSources: (c.sources || []).length }) %></td>`.
    - Le `aria-label` du tableau reste inchangé (Wave 1 OK).

    **Extension `src/web/views/pages/fiscalite/brouillon-liasse.ejs`** :
    - Juste après `partial-bandeau-brouillon-liasse`, si `dto.reconciliation && !dto.reconciliation.cohérent` → `<%- include('../../partials/partial-bandeau-reconciliation', { reconciliation: dto.reconciliation, dateCloture: dto.clotureLe }) %>`.

    **Extension `src/main.ts`** :
    - Étendre l'instanciation de la route `registerFiscaliteLiasseRoutes` avec les 4 nouvelles deps : `recettesRepo`, `chargesRepo`, `tableauAmortRepo`, `bienRepo` (déjà tous instanciés Phase 5).
    - Étendre `src/web/routes/fiscalite/liasse.ts` interface `LiasseRouteDeps` en conséquence et propager au use case.

    **Tests intégration `tests/integration/web/route-liasse.test.ts` — étendre** :
    - Cas 200 + traçabilité : créer une déclaration clôturée réel avec 3 encaissements + 2 justificatifs qualifiés + 1 non_qualifie + 1 ligne amortissement. Hit GET `/fiscalite/declarations/:id/liasse`. Assert : 200 + HTML contient `<details>` × N + URL `/encaissements/...` + AUCUNE URL vers le justificatif `non_qualifie`.
    - Cas réconciliation : stub `RecettesRepository.sommeRecettesAnnuelles` retourne un montant différent du snapshot. Assert : HTML contient `<aside role="alert"` + "Données modifiées depuis la clôture".
    - Cas réconciliation absente : sommes identiques → assert `<aside role="alert"` ABSENT.

    **Décisions implémentées :**
    - UI-SPEC §S4 → `<details>` natif, 0 JS.
    - UI-SPEC §S5 → bandeau destructif conditionnel + copywriting exact.
    - UI-SPEC §Accessibilité : `role="alert"` + `aria-live="assertive"` + `aria-label` unique.

    **À ne PAS faire :**
    - Pas de bouton "Re-calculer" (anti-pattern §11).
    - Pas de modal ou de framework JS pour le drill-down (UI-SPEC §S4).
    - Pas de masquage côté client (le serveur décide si le partial est rendu).
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test -- --run tests/integration/web/route-liasse.test.ts && pnpm test:bdd -- --tags "@phase6-liasse-tracabilite" && (grep -rn "Re-calculer\|Recalculer\|Re-calculate" src/web/views/partials/partial-bandeau-reconciliation.ejs || true) | wc -l | awk '$1 == 0'</automated>
  </verify>
  <done>
    - `pnpm test -- --run tests/integration/web/route-liasse.test.ts` exit 0 (≥ 3 nouveaux cas dont 200 traçabilité + 2 cas réconciliation).
    - `pnpm test:bdd -- --tags "@phase6-liasse-tracabilite"` 7 scénarios verts.
    - `grep -n "Re-calculer\|Recalculer\|button.*recalcul" src/web/views/partials/partial-bandeau-reconciliation.ejs` retourne 0 résultats.
    - `grep -n "role=\"alert\".*aria-live=\"assertive\"" src/web/views/partials/partial-bandeau-reconciliation.ejs` retourne 1 résultat.
    - `grep -n "<details>" src/web/views/partials/partial-drill-down-sources.ejs` retourne 1 résultat.
    - 2 commits : `feat(06-03): partial drill-down + bandeau reconciliation + 4 colonnes tableau` + `feat(06-03): DI cross-BC repos + tests route traçabilité`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repositories Phase 5 → Use case Phase 6 | Lecture seule. Aucune mutation. |
| EJS render → réponse HTML | Échappement strict de `source.label` (issus de saisie utilisateur via `Justificatif.libelle`). |
| URLs sources drill-down | URLs construites côté serveur depuis IDs validés. Aucun input utilisateur direct injecté dans `href`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-LIASSE-W2-TRAC-01 | Tampering (XSS) | `source.label` contient `Justificatif.libelle` (saisie utilisateur Phase 4) | mitigate | EJS `<%= s.label %>` (auto-échappe HTML). Test asserte qu'un libellé `<script>alert(1)</script>` est rendu littéralement. |
| T-06-LIASSE-W2-TRAC-02 | Information Disclosure | Drill-down expose les IDs des Justificatifs / Encaissements | accept | V1 single-user — pas de cloisonnement multi-bailleur. URLs internes uniquement, pas d'IDs exposés au-delà de l'app. |
| T-06-LIASSE-W2-TRAC-03 | Tampering (intégrité fiscale CRITIQUE D-T6.4) | Tentation de remplacer `valeur` par `valeurVivante` dans le tableau case-par-case | mitigate | Test BDD régression (scénario 2 `liasse-tracabilite.feature`) : assert que `valeurAffichee === snapshot`, même en cas d'écart. Pitfall §2 + anti-pattern §3 + §11. |
| T-06-LIASSE-W2-TRAC-04 | DoS génération brouillon | Use case agrège N encaissements + M justificatifs + K composants à chaque rendu | accept | V1 LMNP single-user ≤ 100 biens × 12 encaissements/an × 50 justificatifs/an = ≤ 60k items max, négligeable. RESEARCH §T-06-LIASSE-03. |
| T-06-LIASSE-W2-TRAC-05 | Tampering (filtre) | Drill-down expose `non_qualifie` ou `non_deductible` | mitigate | Filtre `QUALIFICATIONS_DEDUCTIBLES` AU SEUL POINT de construction de `sources`. Test BDD scénarios 6 et 7 + pitfall §7. |
</threat_model>

<verification>
- `pnpm typecheck` exit 0.
- `pnpm test && pnpm test:bdd` tous verts (mapping-versionne + liasse-reel + liasse-tracabilite ≥ 17 scénarios actifs Wave 1+2+3).
- `pnpm lint:deps` exit 0.
- `grep -n "reconcilier\|reconciliation" src/domain/fiscalite/reconciliation.ts` montre l'absence d'imports infra.
- Manuel : 1) ouvrir une déclaration sans écart → pas de bandeau S5 + drill-down liste correctement les sources ; 2) modifier un Justificatif après clôture (changement de catégorie qualifiée) → bandeau S5 apparaît avec `nbPiecesModifiees: 1`.
</verification>

<success_criteria>
- ROADMAP success criterion 2 (FIS-05 traçabilité) : chaque ligne renvoie aux Encaissements/Justificatifs/TableauAmortissement qui la justifient.
- 100 % couverture sur `src/domain/fiscalite/reconciliation.ts` (BDD_PRACTICES.md §7).
- Anti-pattern D-T6.4 verrouillé par test BDD régression.
</success_criteria>

<output>
After completion, create `.planning/phases/06-liasse-2031-cfe/06-03-SUMMARY.md` listant :
- Méthodes Phase 5 utilisées + éventuelles extensions (e.g. `ChargesRepository.listerJustificatifsQualifiesParExercice` si ajoutée).
- Décision sur la portée de `sourcesVivantesMap` (recettes + charges déductibles + dotation amortissement total V1).
- Capture du bandeau S5 et du drill-down.
</output>
</content>
</invoke>