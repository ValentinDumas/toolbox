---
phase: 06-liasse-2031-cfe
plan: 07
type: tdd
wave: 2
depends_on: [06-06]
files_modified:
  - src/domain/fiscalite/cfe/alerte-cfe-j30.ts
  - src/application/fiscalite/lister-alertes-cfe-actives.ts
  - src/web/views/partials/partial-bandeau-cfe-echeance.ejs
  - src/web/views/pages/biens/detail.ejs
  - src/web/views/pages/fiscalite/index.ejs
  - src/web/routes/biens.ts
  - src/web/routes/fiscalite/racine.ts
  - src/main.ts
  - tests/_fakes/clock-fake.ts
  - tests/unit/fiscalite/alerte-cfe-j30.test.ts
  - tests/unit/fiscalite/lister-alertes-cfe-actives.test.ts
  - tests/integration/web/route-cfe-banner.test.ts
  - tests/bdd/features/cfe-alerte-echeance.feature
  - tests/bdd/step_definitions/cfe.steps.ts
autonomous: true
requirements: [FIS-06]
tags: [fiscalite, cfe, alerte, banner, clock, j30]

must_haves:
  truths:
    - "Une fonction pure calculerAlertesCfe(declarations, maintenant) retourne uniquement les DeclarationCfe dont l'échéance est dans la fenêtre [maintenant, maintenant + 30 jours] (D-CFE6.5)."
    - "Les statuts 'payee', 'exoneree_premiere_annee' et 'exoneree_commune' sont strictement filtrés AVANT calcul (pas de banner sur CFE payée ou exonérée — pitfall §5 RESEARCH)."
    - "Le banner est calculé À LA DEMANDE via Clock injecté à chaque rendu de page (PAS de cron, PAS de setInterval — D-CFE6.5, anti-pattern §6 RESEARCH)."
    - "Le partial partial-bandeau-cfe-echeance.ejs rend 3 variantes visuelles selon joursRestants : J-30 à J-8 (warning), J-7 à J-0 (warning forte), J+1 et plus (destructive — échéance dépassée, statut différent de payee)."
    - "Le banner est injecté sur la fiche Bien (en haut, au-dessus de la section CFE) ET sur la page racine /fiscalite (bloc 'Échéances CFE' UI-SPEC §S11)."
    - "Le banner contient un lien externe 'Régler la CFE sur impots.gouv.fr' avec target='_blank' rel='noopener noreferrer' (UI-SPEC §S10 + V9 ASVS)."
    - "Les tests BDD utilisent ClockFixe pour 8 scénarios déterministes (échéance 2026-12-15 + variations de today + variations de statut)."
    - "Le calcul est testé par fast-check pour la propriété de monotonie : pour date1 < date2 < dateEcheance, joursAvantEcheance(date1) > joursAvantEcheance(date2)."
  artifacts:
    - path: "src/domain/fiscalite/cfe/alerte-cfe-j30.ts"
      provides: "Fonction pure calculerAlertesCfe(declarations, maintenant) + DTO AlerteCfe + helper joursAvantEcheance + estAlerteActive"
      exports: ["calculerAlertesCfe", "joursAvantEcheance", "estAlerteActive", "AlerteCfe"]
    - path: "src/application/fiscalite/lister-alertes-cfe-actives.ts"
      provides: "Use case lecture qui injecte Clock + filtre statut + calcule alertes"
      exports: ["listerAlertesCfeActives"]
    - path: "src/web/views/partials/partial-bandeau-cfe-echeance.ejs"
      provides: "Banner contextuel 3 variantes (warning J-30 à J-8, warning forte J-7 à J-0, destructive J+1 et plus)"
    - path: "tests/bdd/features/cfe-alerte-echeance.feature"
      provides: "8 scénarios déterministes @phase6 @phase6-cfe-alerte via ClockFixe"
    - path: "tests/_fakes/clock-fake.ts"
      provides: "ClockFake (ou réutilisation ClockFixe existante Phase 3)"
  key_links:
    - from: "src/web/routes/biens.ts (GET /biens/:id)"
      to: "src/application/fiscalite/lister-alertes-cfe-actives.ts"
      via: "appel use case avec clock injecté + render banner conditionnel"
      pattern: "listerAlertesCfeActives\\("
    - from: "src/web/routes/fiscalite/racine.ts (GET /fiscalite)"
      to: "src/application/fiscalite/lister-alertes-cfe-actives.ts"
      via: "agrégation toutes alertes actives mono-bailleur"
      pattern: "listerAlertesCfeActives\\("
    - from: "src/domain/fiscalite/cfe/alerte-cfe-j30.ts (fonction pure)"
      to: "src/domain/_shared/clock.ts (port Clock)"
      via: "Clock paramètre fonction listerAlertesCfeActives"
      pattern: "clock\\.aujourdhui\\("
    - from: "src/web/views/partials/partial-bandeau-cfe-echeance.ejs"
      to: "src/web/views/pages/biens/detail.ejs et /fiscalite/index.ejs"
      via: "include conditionnel si alerte.length > 0"
      pattern: "include\\(.+partial-bandeau-cfe-echeance"
---

<objective>
**Phase Goal (slice 7 FIS-06)** :

**As a** bailleur LMNP single-user, **I want to** voir un bandeau d'alerte sur ma fiche Bien et sur ma page Fiscalité quand l'échéance de paiement CFE approche (J-30), **so that** je ne rate pas la date limite (généralement 15 décembre) sans avoir à configurer un cron, et je peux cliquer directement vers impots.gouv.fr pour régler.

**Décisions implémentées :**
- D-CFE6.5 -> Alerte = banner J-30 sur fiche Bien + page /fiscalite. Pattern Phase 3 D-90 banner IRL : calcul à la demande via `Clock`, pas de cron.
- Pattern miroir exact `partial-indexation-banner.ejs` (Phase 3 — `Clock` déterministe pour BDD).
- Fonction pure `calculerAlertesCfe` + use case avec `Clock` injecté (06-RESEARCH §Pattern 4 + Pitfall §5 — filtrage statut avant calcul).
- UI-SPEC §S10 : 3 variantes visuelles (warning, warning forte, destructive) selon `joursRestants`.

**Périmètre slice 7** :
- 1 fichier domaine pur (`alerte-cfe-j30.ts`) + 1 use case application + 1 partial EJS + intégrations dans 2 vues (fiche Bien + page Fiscalité racine) + 8 scénarios BDD + propriétés fast-check.
- **Dépend de 06-06** (consomme `DeclarationCfe`, `StatutCfe`, `DeclarationCfeRepository` + `cfeRepo` déjà câblé dans main.ts).
- **Pas de dashboard consolidé** (= Phase 7 DAS-02).

**Output** : banner d'échéance contextuel visible sur `/biens/:id` et `/fiscalite` quand au moins une CFE est dans la fenêtre J-30. 8 scénarios BDD déterministes verts + propriétés fast-check sur monotonie. Phase 6 success criterion 4 (FIS-06 alerte décembre) livré.
</objective>

<execution_context>
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/workflows/execute-plan.md
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-liasse-2031-cfe/06-CONTEXT.md
@.planning/phases/06-liasse-2031-cfe/06-RESEARCH.md
@.planning/phases/06-liasse-2031-cfe/06-PATTERNS.md
@.planning/phases/06-liasse-2031-cfe/06-UI-SPEC.md
@.planning/phases/06-liasse-2031-cfe/06-VALIDATION.md
@.planning/phases/06-liasse-2031-cfe/06-06-SUMMARY.md
@CLAUDE.md
@RISKS.md
@practices/DDD.md
@practices/BDD_PRACTICES.md
@practices/UI_DESIGN.md
@practices/UX_DESIGN.md
@practices/ACCESSIBILITY.md

# Patterns à cloner (miroirs exacts — voir 06-PATTERNS.md §Pattern critique 4)
@src/web/views/partials/partial-indexation-banner.ejs
@src/domain/_shared/clock.ts
@src/application/locatif/appliquer-indexation-irl.ts

# Artefacts Plan 06-06 consommés (FIS-06 slice 6)
@src/domain/fiscalite/cfe/declaration-cfe.ts
@src/domain/fiscalite/cfe/declaration-cfe-repository.ts
@src/domain/fiscalite/cfe/statut-cfe.ts
@src/application/fiscalite/lister-declarations-cfe-par-bien.ts
@src/web/views/pages/biens/detail.ejs
@src/web/views/pages/fiscalite/index.ejs

<interfaces>
# Clock port (Phase 1 — déjà en place)
interface Clock { aujourdhui(): Temporal.PlainDate; }
class ClockSysteme implements Clock { ... }
class ClockFixe implements Clock { constructor(dateFixe: Temporal.PlainDate); ... }   // ou ClockFake si présent

# Fonction pure domain (à créer Task 1)
interface AlerteCfe {
  readonly declarationCfeId: DeclarationCfeId;
  readonly bienId: BienId;
  readonly millesime: number;
  readonly joursRestants: number;      // peut être négatif (échéance dépassée)
  readonly dateEcheancePaiement: Temporal.PlainDate;
  readonly statutCfe: StatutCfe;        // pour le rendu de la variante (toujours 'non_deposee' ou 'deposee')
}

export function joursAvantEcheance(dateEcheance: Temporal.PlainDate, maintenant: Temporal.PlainDate): number;
export function estAlerteActive(d: DeclarationCfe, maintenant: Temporal.PlainDate): boolean;
export function calculerAlertesCfe(declarations: readonly DeclarationCfe[], maintenant: Temporal.PlainDate): AlerteCfe[];

# Use case application (à créer Task 1)
interface ListerAlertesCfeActivesDeps { cfeRepo: DeclarationCfeRepository; clock: Clock; }
export async function listerAlertesCfeActives(
  filtre: { bienId?: BienId },   // si bienId fourni : listerParBien ; sinon : agrégation mono-bailleur (V1 = parcours de tous les biens)
  deps: ListerAlertesCfeActivesDeps,
): Promise<AlerteCfe[]>;
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: TDD fonction pure calculerAlertesCfe + joursAvantEcheance + use case listerAlertesCfeActives (avec filtres statut + Clock injecté + propriétés fast-check)</name>
  <files>
    src/domain/fiscalite/cfe/alerte-cfe-j30.ts,
    src/application/fiscalite/lister-alertes-cfe-actives.ts,
    tests/unit/fiscalite/alerte-cfe-j30.test.ts,
    tests/unit/fiscalite/lister-alertes-cfe-actives.test.ts,
    tests/_fakes/clock-fake.ts
  </files>
  <read_first>
    src/domain/_shared/clock.ts (port Clock + ClockSysteme + ClockFixe — déjà en place Phase 1),
    src/web/views/partials/partial-indexation-banner.ejs (Pattern critique 4 — banner Phase 3 D-90 à cloner),
    src/application/locatif/appliquer-indexation-irl.ts (pattern Clock injecté + calcul à la demande Phase 3 D-90),
    src/domain/fiscalite/cfe/declaration-cfe.ts (entité consommée — créée Plan 06-06),
    src/domain/fiscalite/cfe/declaration-cfe-repository.ts (port consommé — créé Plan 06-06),
    src/domain/fiscalite/cfe/statut-cfe.ts (StatutCfe + STATUTS_CFE_VALIDES — créé Plan 06-06),
    src/application/fiscalite/lister-declarations-cfe-par-bien.ts (read use case Plan 06-06 à composer),
    .planning/phases/06-liasse-2031-cfe/06-PATTERNS.md (§Pattern critique 4 — banner J-30),
    .planning/phases/06-liasse-2031-cfe/06-RESEARCH.md (§Pattern 4 lignes 512-562 — fonction joursAvantEcheance + estAlerteActive + filtrage statut + §Common Pitfalls §5 + §Validation Architecture propriétés fast-check 3),
    .planning/phases/06-liasse-2031-cfe/06-CONTEXT.md (D-CFE6.5),
    .planning/phases/06-liasse-2031-cfe/06-VALIDATION.md (§Per-Task Verification Map — cfe-alerte-echeance + propriété 4 monotonie).
  </read_first>
  <behavior>
    **TDD RED -> GREEN -> REFACTOR.**

    **RED — `tests/unit/fiscalite/alerte-cfe-j30.test.ts` (au moins 12 tests vitest, dont 3 propriétés fast-check) :**

    Tests `joursAvantEcheance` :
    - `joursAvantEcheance(PlainDate('2026-12-15'), PlainDate('2026-11-15'))` -> `30` (positif futur).
    - `joursAvantEcheance(PlainDate('2026-12-15'), PlainDate('2026-12-15'))` -> `0` (jour J).
    - `joursAvantEcheance(PlainDate('2026-12-15'), PlainDate('2026-12-16'))` -> `-1` (échéance dépassée).
    - **Propriété fast-check (monotonie — VALIDATION §Propriété 4 + RESEARCH ligne 997)** : `forall date1, date2 telles que date1 < date2 < dateEcheance` -> `joursAvantEcheance(dateEcheance, date1) > joursAvantEcheance(dateEcheance, date2)`. Utiliser `fc.date` borné dans `[2024-01-01, 2030-12-31]`.

    Tests `estAlerteActive(d, maintenant)` (RESEARCH §Pattern 4 lignes 555-561 + Pitfall §5 lignes 887-900) :
    - Statut `payee` + J-5 (échéance future) -> `false`.
    - Statut `exoneree_premiere_annee` + J-5 -> `false`.
    - Statut `exoneree_commune` + J-15 -> `false`.
    - Statut `non_deposee` + J-30 -> `true`.
    - Statut `non_deposee` + J-31 -> `false` (hors fenêtre).
    - Statut `deposee` + J-7 -> `true`.
    - Statut `non_deposee` + J+10 (échéance dépassée depuis 10 jours) -> `true` (variante destructive UI-SPEC §S10).
    - Statut `non_deposee` + J-100 -> `false`.

    Tests `calculerAlertesCfe(declarations, maintenant)` :
    - Liste mixte (3 CFE : 1 payée + 1 non_deposee J-15 + 1 exoneree_commune J-5) -> retourne 1 seule `AlerteCfe` (la non_deposee J-15).
    - Liste vide -> retourne `[]`.
    - Tri du résultat : ordre déterministe par `joursRestants ASC` (plus urgent en premier).
    - Le DTO `AlerteCfe` contient `declarationCfeId`, `bienId`, `millesime`, `joursRestants`, `dateEcheancePaiement`, `statutCfe`.
    - **Propriété fast-check** : pour toute liste de DeclarationCfe, `calculerAlertesCfe(decls, maintenant).length <= decls.length` (filtrage strict).
    - **Propriété fast-check** : pour toute DeclarationCfe avec `statut in {payee, exoneree_*}`, la déclaration n'apparaît JAMAIS dans `calculerAlertesCfe([d], any)`.

    **RED — `tests/unit/fiscalite/lister-alertes-cfe-actives.test.ts` (au moins 4 tests) :**
    - Cas `bienId` fourni : appelle `cfeRepo.listerParBien(bienId)` -> filtre `calculerAlertesCfe` -> retourne alertes du bien.
    - Cas `bienId` absent (V1 mono-bailleur) : agrège alertes de tous les biens. **Note implémentation** : si `DeclarationCfeRepository` n'expose pas `listerToutes`, ajouter cette méthode au port + adapter SQLite (cohérence Plan 06-06). Sinon, le use case prend en entrée la liste des `Bien` et boucle sur `listerParBien`.
    - `Clock` injecté : ClockFixe('2026-11-15') + déclarations avec échéance 2026-12-15 statut non_deposee -> alerte J-30.
    - Tri par `joursRestants ASC` préservé après agrégation multi-biens.

    **RED — `tests/_fakes/clock-fake.ts`** : confirmer existence. Si présent (Phase 3 banner IRL), réutiliser. Sinon, créer wrapper sur `ClockFixe` (déjà en place dans `src/domain/_shared/clock.ts` ligne 22+ vu en interfaces).

    **GREEN — Implémentation :**

    1. **`src/domain/fiscalite/cfe/alerte-cfe-j30.ts` — fonction pure** :

       Imports : `Temporal` (`@js-temporal/polyfill`), `DeclarationCfe`, `StatutCfe`, `BienId`, `DeclarationCfeId`.

       - `export interface AlerteCfe { readonly declarationCfeId: DeclarationCfeId; readonly bienId: BienId; readonly millesime: number; readonly joursRestants: number; readonly dateEcheancePaiement: Temporal.PlainDate; readonly statutCfe: StatutCfe; }`.

       - `export function joursAvantEcheance(dateEcheance: Temporal.PlainDate, maintenant: Temporal.PlainDate): number` (RESEARCH ligne 530-538) :
         - `return Temporal.PlainDate.compare(dateEcheance, maintenant) === 0 ? 0 : maintenant.until(dateEcheance, { largestUnit: 'day' }).days;`.

       - `export function estAlerteActive(d: DeclarationCfe, maintenant: Temporal.PlainDate): boolean` (RESEARCH ligne 555-561 + Pitfall §5) :
         - `if (d.statut === 'payee' || d.statut === 'exoneree_premiere_annee' || d.statut === 'exoneree_commune') return false;`
         - `const jours = joursAvantEcheance(d.dateEcheancePaiement, maintenant);`
         - `return jours <= 30;` (la fenêtre inclut J+ négatif = échéance dépassée -> variante destructive UI-SPEC §S10).

       - `export function calculerAlertesCfe(declarations: readonly DeclarationCfe[], maintenant: Temporal.PlainDate): AlerteCfe[]` :
         - `return declarations.filter(d => estAlerteActive(d, maintenant)).map(d => ({ declarationCfeId: d.id, bienId: d.bienId, millesime: d.millesime, joursRestants: joursAvantEcheance(d.dateEcheancePaiement, maintenant), dateEcheancePaiement: d.dateEcheancePaiement, statutCfe: d.statut })).sort((a, b) => a.joursRestants - b.joursRestants);`.

       - **Aucun import infra/HTTP** : fonction pure, vit dans `src/domain/fiscalite/cfe/`.

    2. **`src/application/fiscalite/lister-alertes-cfe-actives.ts`** :

       - Interface `ListerAlertesCfeActivesCommande = { bienId?: BienId; }` (si `bienId` absent -> V1 mono-bailleur, parcours de toutes les déclarations).
       - Interface `ListerAlertesCfeActivesDeps { cfeRepo: DeclarationCfeRepository; clock: Clock; }`.
       - Algorithme :
         a) `maintenant = clock.aujourdhui()`.
         b) Si `commande.bienId` : `declarations = await cfeRepo.listerParBien(commande.bienId)`.
         c) Sinon : **V1 mono-bailleur** -> ajouter méthode `listerToutes(): Promise<DeclarationCfe[]>` au port `DeclarationCfeRepository` + adapter SQLite Plan 06-06 (extension légère **ou bien** prendre `bienIds: BienId[]` en input et boucler — choisir l'option la plus propre selon état Plan 06-06).
         d) `return calculerAlertesCfe(declarations, maintenant);` (tri déjà appliqué).

       - **Décision recommandée** : étendre le port `DeclarationCfeRepository` avec `listerToutes(): Promise<DeclarationCfe[]>` côté Plan 06-06 ou Task 1 ici (extension minimale). Documenter dans le `<read_first>` et le `must_haves` que cette extension est faite par ce plan si Plan 06-06 ne l'a pas livrée. **Pas une dépendance bloquante** : V1 mono-bailleur, méthode triviale en SQLite (`select * order by millesime desc`).

    3. **`tests/_fakes/clock-fake.ts`** :
       - Si fichier existe déjà (Phase 3) : ne pas dupliquer, importer depuis l'emplacement existant dans les tests Phase 6.
       - Si absent : créer un wrapper minimaliste : `export function clockFixeA(dateIso: string): Clock { return new ClockFixe(Temporal.PlainDate.from(dateIso)); }` (facade pratique pour les step definitions).

    **REFACTOR :**
    - Si `calculerAlertesCfe` dépasse 10 lignes -> extraire `toAlerteCfe(d, maintenant): AlerteCfe` privé.
    - Vérifier `pnpm lint:deps` : `src/domain/fiscalite/cfe/alerte-cfe-j30.ts` n'importe AUCUN module infra/HTTP/Zod.
    - Vérifier `grep -rE "setInterval|setTimeout|cron" src/domain/fiscalite/cfe/ src/application/fiscalite/lister-alertes-cfe-actives.ts` -> 0 résultat (anti-pattern §6 RESEARCH, D-CFE6.5).

    Commits atomiques :
    - `test(06-07): tests unit alerte-cfe-j30 + lister-alertes-cfe-actives + propriétés fast-check (RED)`
    - `feat(06-07): fonction pure calculerAlertesCfe + estAlerteActive + joursAvantEcheance`
    - `feat(06-07): use case listerAlertesCfeActives avec Clock injecté`
    - `test(06-07): clock-fake helper (si absent)`
  </behavior>
  <action>
    Cloner intégralement le pattern Phase 3 D-90 banner IRL (06-PATTERNS §Pattern critique 4 — `partial-indexation-banner.ejs` + `Clock` Phase 3) MAIS appliqué à la fenêtre J-30 et aux 5 statuts CFE. Filtrage statut **AVANT** calcul `joursRestants` (RESEARCH ligne 555-561 + Pitfall §5).

    **Décisions implémentées :**
    - D-CFE6.5 -> calcul à la demande via Clock, pas de cron.
    - RESEARCH §Pattern 4 -> signature exacte `joursAvantEcheance(dateEcheance, maintenant): number` via `Temporal.PlainDate.until({largestUnit: 'day'})`.
    - RESEARCH §Pitfall §5 -> filtre `payee/exoneree_*` AVANT calcul (pas après — gain de perf + clarté + déterminisme BDD).
    - VALIDATION §Propriété 4 -> monotonie testée par fast-check.

    **À ne PAS faire (anti-patterns) :**
    - Pas de `setInterval`, `setTimeout`, ou cron (anti-pattern §6 RESEARCH, D-CFE6.5).
    - Pas d'appel direct `Temporal.Now.plainDateISO()` dans le domaine (06-PATTERNS shared patterns ligne 699 — `Clock` injecté obligatoire).
    - Pas d'import infra/HTTP/Zod dans `src/domain/fiscalite/cfe/alerte-cfe-j30.ts`.
    - Pas de mutation des DeclarationCfe (immutabilité — fonction pure stricte).
    - Pas de side-effect dans `calculerAlertesCfe` (pas de log, pas de fetch).
    - Pas de calcul de pénalités de retard ou base imposable (R4.3 — banner = uniquement statut + échéance).
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test -- --run tests/unit/fiscalite/alerte-cfe-j30.test.ts tests/unit/fiscalite/lister-alertes-cfe-actives.test.ts && pnpm lint:deps && grep -rEn "from .+(pdfmake|kysely|fastify|zod|fs|path)" src/domain/fiscalite/cfe/alerte-cfe-j30.ts | grep -v '^#' | wc -l | awk '{exit ($1 == 0) ? 0 : 1}' && grep -rEn "setInterval|setTimeout|node-cron" src/domain/fiscalite/cfe/ src/application/fiscalite/lister-alertes-cfe-actives.ts | grep -v '^#' | wc -l | awk '{exit ($1 == 0) ? 0 : 1}' && grep -rEn "Temporal\\.Now" src/domain/fiscalite/cfe/alerte-cfe-j30.ts | grep -v '^#' | wc -l | awk '{exit ($1 == 0) ? 0 : 1}'</automated>
  </verify>
  <done>
    - `pnpm test -- --run tests/unit/fiscalite/alerte-cfe-j30.test.ts` exit 0 (au moins 12 tests dont 3 propriétés fast-check).
    - `pnpm test -- --run tests/unit/fiscalite/lister-alertes-cfe-actives.test.ts` exit 0 (au moins 4 cas).
    - `pnpm lint:deps` exit 0 (hexagonal strict — fonction pure dans domaine).
    - `grep -rE "from .+(pdfmake|kysely|fastify|zod)" src/domain/fiscalite/cfe/alerte-cfe-j30.ts` -> 0.
    - `grep -rE "setInterval|setTimeout|node-cron"` dans Plan 06-07 -> 0 (anti-pattern §6).
    - `grep -rE "Temporal\\.Now"` dans `alerte-cfe-j30.ts` -> 0 (Clock injecté obligatoire).
    - 3-4 commits atomiques.
  </done>
</task>

<task type="execute">
  <name>Task 2: Partial bandeau J-30 (3 variantes) + intégrations fiche Bien et page /fiscalite + 8 scénarios BDD ClockFixe + DI main.ts</name>
  <files>
    src/web/views/partials/partial-bandeau-cfe-echeance.ejs,
    src/web/views/pages/biens/detail.ejs,
    src/web/views/pages/fiscalite/index.ejs,
    src/web/routes/biens.ts,
    src/web/routes/fiscalite/racine.ts,
    src/main.ts,
    tests/integration/web/route-cfe-banner.test.ts,
    tests/bdd/features/cfe-alerte-echeance.feature,
    tests/bdd/step_definitions/cfe.steps.ts
  </files>
  <read_first>
    src/web/views/partials/partial-indexation-banner.ejs (Pattern critique 4 — clone exact pour 3 variantes warning / warning forte / destructive),
    src/web/routes/biens.ts (route GET /biens/:id existante à étendre — passer `alertesCfe` aux locals),
    src/web/routes/fiscalite/racine.ts (route GET /fiscalite existante à étendre — bloc 'Échéances CFE' UI-SPEC §S11),
    src/web/views/pages/biens/detail.ejs (vue déjà enrichie Plan 06-06 avec section CFE — banner positionné AU-DESSUS de la section),
    src/web/views/pages/fiscalite/index.ejs (vue Phase 5 — bloc 'Échéances CFE' à ajouter si au moins 1 alerte active),
    src/main.ts (DI `cfeRepo` + `clock` déjà câblés Phase 5 — vérifier passage aux nouvelles routes),
    public/styles/app.css (tokens --couleur-warning, --couleur-warning-bg, --couleur-destructive, --couleur-destructive-bg, --couleur-accent — déjà en place Phase 4 UI-1.3),
    .planning/phases/06-liasse-2031-cfe/06-UI-SPEC.md (§S10 banner 3 variantes + copywriting EXACT par variante + §S11 page Fiscalité + §Accessibilité ARIA roles + §Color tokens),
    .planning/phases/06-liasse-2031-cfe/06-CONTEXT.md (D-CFE6.5),
    .planning/phases/06-liasse-2031-cfe/06-VALIDATION.md (§Manual-Only Verifications — banner CFE J-30 vérification manuelle).
  </read_first>
  <action>
    Cloner `partial-indexation-banner.ejs` pour `partial-bandeau-cfe-echeance.ejs` avec 3 variantes selon `joursRestants`. Injecter dans 2 vues + étendre 2 routes pour calculer les alertes à chaque rendu (Clock injecté).

    **`src/web/views/partials/partial-bandeau-cfe-echeance.ejs`** (UI-SPEC §S10 + §Copywriting + §Accessibilité) :
    - Props : `alerte: AlerteCfe` (suppose `alerte` non null — le caller filtre avant include).
    - Trois variantes via switch sur `alerte.joursRestants` :

      **Variante 1 (J-30 à J-8) — warning** :
      ```
      <aside role="status" aria-live="polite" aria-label="Échéance CFE <%= alerte.millesime %>" style="background: var(--couleur-warning-bg); padding: 16px; border-left: 4px solid var(--couleur-warning); margin-bottom: 24px;">
        <strong>CFE <%= alerte.millesime %> — échéance dans <%= alerte.joursRestants %> jour(s)</strong> (<%= formatDate(alerte.dateEcheancePaiement) %>).
        Préparez le règlement sur impots.gouv.fr.
        <a href="https://www.impots.gouv.fr/professionnel/cfe" target="_blank" rel="noopener noreferrer" role="button">Régler la CFE sur impots.gouv.fr</a>
      </aside>
      ```

      **Variante 2 (J-7 à J-0) — warning forte avec icône** :
      ```
      <aside role="status" aria-live="polite" aria-label="Échéance CFE imminente <%= alerte.millesime %>" style="background: var(--couleur-warning-bg); padding: 16px; border-left: 4px solid var(--couleur-warning); margin-bottom: 24px;">
        <span aria-hidden="true">⚠</span>
        <strong>CFE <%= alerte.millesime %> — échéance imminente : <%= alerte.joursRestants %> jour(s) restant(s).</strong>
        Réglez sans délai.
        <a href="https://www.impots.gouv.fr/professionnel/cfe" target="_blank" rel="noopener noreferrer" role="button">Régler la CFE sur impots.gouv.fr</a>
      </aside>
      ```

      **Variante 3 (J+1 et plus — joursRestants < 0) — destructive avec icône** :
      ```
      <aside role="alert" aria-live="assertive" aria-label="Échéance CFE dépassée <%= alerte.millesime %>" style="background: var(--couleur-destructive-bg); padding: 16px; border-left: 4px solid var(--couleur-destructive); margin-bottom: 24px;">
        <span aria-hidden="true">✕</span>
        <strong>CFE <%= alerte.millesime %> — échéance dépassée depuis <%= Math.abs(alerte.joursRestants) %> jour(s).</strong>
        Régularisez immédiatement votre situation (pénalités de retard applicables).
        <a href="https://www.impots.gouv.fr/professionnel/cfe" target="_blank" rel="noopener noreferrer" role="button">Régler la CFE sur impots.gouv.fr</a>
      </aside>
      ```

    - **Copywriting strict UI-SPEC §S10 + §Copywriting Contract** — toute déviation = échec de revue.
    - **Lien externe sécurisé** : `target="_blank" rel="noopener noreferrer"` obligatoire (V9 ASVS + UI-SPEC §S9/§S10).
    - **ARIA** : `role="status"` pour J-30 à J-0 (info), `role="alert"` + `aria-live="assertive"` pour J+ (urgence) — UI-SPEC §Accessibilité 4.1.2.

    **`src/web/routes/biens.ts` — étendre la route GET `/biens/:id`** :
    - Ajouter aux deps : `cfeRepo: DeclarationCfeRepository`, `clock: Clock` (déjà passés Plan 06-06 / Phase 5).
    - Ajouter au handler `GET /biens/:id` :
      - `alertesCfeDuBien = await listerAlertesCfeActives({ bienId: bien.id }, { cfeRepo, clock })`.
      - Passer `alertesCfeDuBien` aux locals `reply.view(..., { ...existing, alertesCfeDuBien })`.

    **`src/web/views/pages/biens/detail.ejs` — étendre** (au-dessus de la section CFE Plan 06-06 — UI-SPEC §S10) :
    - `<% if (locals.alertesCfeDuBien && alertesCfeDuBien.length > 0) { %><% alertesCfeDuBien.forEach(function(a) { %><%- include('../../partials/partial-bandeau-cfe-echeance', { alerte: a }) %><% }) %><% } %>`.

    **`src/web/routes/fiscalite/racine.ts` — étendre la route GET `/fiscalite`** (UI-SPEC §S11 — bloc "Échéances CFE") :
    - Ajouter aux deps : `cfeRepo: DeclarationCfeRepository`, `clock: Clock`.
    - Handler : `alertesCfe = await listerAlertesCfeActives({}, { cfeRepo, clock })` (V1 mono-bailleur, pas de filtre bien).
    - Passer `alertesCfe` aux locals.

    **`src/web/views/pages/fiscalite/index.ejs` — étendre** (UI-SPEC §S11) :
    - Ajouter `<% if (locals.alertesCfe && alertesCfe.length > 0) { %><section aria-label="Échéances CFE"><h2>Échéances CFE</h2><% alertesCfe.forEach(function(a) { %><%- include('../../partials/partial-bandeau-cfe-echeance', { alerte: a }) %><% }) %></section><% } %>`.
    - Si aucune alerte : bloc masqué (pas d'empty state — UI-SPEC §S11 "Si aucune échéance active : bloc masqué").

    **`src/main.ts` — vérifier DI** :
    - `cfeRepo` et `clock` déjà câblés Plan 06-06 / Phase 5. **S'assurer** qu'ils sont bien passés aux deps de `registerBienRoutes` et `registerFiscaliteRoutes` (vérifier les `app.register` lignes 240+).

    **Tests intégration `tests/integration/web/route-cfe-banner.test.ts`** (au moins 4 cas) :
    - DB éphémère + Bailleur + Bien + DeclarationCfe (statut non_deposee, échéance 2026-12-15) via builders.
    - DI `ClockFixe('2026-11-15')` -> GET `/biens/:id` -> HTML contient `aria-label="Échéance CFE 2026"` + texte "30 jour(s)".
    - DI `ClockFixe('2026-12-08')` -> GET `/biens/:id` -> HTML contient `aria-label="Échéance CFE imminente 2026"` + "7 jour(s) restant(s)".
    - DI `ClockFixe('2026-12-20')` -> GET `/biens/:id` -> HTML contient `aria-label="Échéance CFE dépassée 2026"` + "5 jour(s)".
    - DI `ClockFixe('2026-12-08')` + statut `payee` -> GET `/biens/:id` -> HTML **ne contient PAS** `aria-label="Échéance CFE`.
    - DI `ClockFixe('2026-11-15')` + 2 biens avec CFE -> GET `/fiscalite` -> HTML contient `<section aria-label="Échéances CFE">` + 2 banners.

    **`tests/bdd/features/cfe-alerte-echeance.feature`** (8 scénarios @phase6 @phase6-cfe-alerte — VALIDATION.md §Per-Task lignes 1042-1049, RESEARCH lignes 1042-1049, déterministe via ClockFixe) :

    Feature en français. Background fixe `Etant donné une déclaration CFE 2026 avec échéance "15/12/2026"` (réutilisé).

    - Scénario 1 : `Et today vaut "2026-11-15"` `Et le statut est "Non déposée"` `Quand je visualise la fiche du Bien` `Alors un bandeau warning affiche "CFE 2026 — échéance dans 30 jour(s)"`.
    - Scénario 2 : `Et today vaut "2026-12-08"` `Et le statut est "Déposée"` `Quand je visualise la fiche du Bien` `Alors un bandeau warning forte affiche "échéance imminente : 7 jour(s) restant(s)"`.
    - Scénario 3 : `Et today vaut "2026-12-15"` `Et le statut est "Déposée"` `Quand je visualise la fiche du Bien` `Alors un bandeau warning forte affiche "0 jour(s) restant(s)"`.
    - Scénario 4 : `Et today vaut "2026-12-16"` `Et le statut est "Déposée"` `Quand je visualise la fiche du Bien` `Alors un bandeau destructive affiche "échéance dépassée depuis 1 jour(s)"`.
    - Scénario 5 : `Et today vaut "2026-12-08"` `Et le statut est "Payée"` `Quand je visualise la fiche du Bien` `Alors AUCUN bandeau d'échéance CFE n'est rendu`.
    - Scénario 6 : `Et today vaut "2026-12-08"` `Et le statut est "Exonérée — première année (CGI art. 1478)"` `Quand je visualise la fiche du Bien` `Alors AUCUN bandeau d'échéance CFE n'est rendu`.
    - Scénario 7 : `Et today vaut "2026-11-15"` `Et le statut est "Exonérée — décision de la commune"` `Quand je visualise la fiche du Bien` `Alors AUCUN bandeau d'échéance CFE n'est rendu`.
    - Scénario 8 (hors fenêtre) : `Et today vaut "2026-09-01"` `Et le statut est "Non déposée"` `Quand je visualise la fiche du Bien` `Alors AUCUN bandeau d'échéance CFE n'est rendu` (J-105, hors J-30).

    Compléter `tests/bdd/step_definitions/cfe.steps.ts` (créé Plan 06-06) avec :
    - Step `Etant donné une déclaration CFE 2026 avec échéance "{date}"` -> seed DB + injecter dans `this.world`.
    - Step `Et today vaut "{date}"` -> `this.world.clock = new ClockFixe(Temporal.PlainDate.from(date))`.
    - Step `Et le statut est "{libellé}"` -> mapper libellé fr -> StatutCfe enum (utiliser `Object.entries(LIBELLES_STATUT_CFE).find(...)`) puis update DeclarationCfe.
    - Step `Quand je visualise la fiche du Bien` -> requête Fastify inject `GET /biens/:id` avec clock fixe injecté.
    - Step `Alors un bandeau {type} affiche "{copie}"` -> regex sur HTML response body.
    - Step `Alors AUCUN bandeau d'échéance CFE n'est rendu` -> assert HTML response body NE contient PAS `aria-label="Échéance CFE`.

    **Décisions implémentées :**
    - D-CFE6.5 -> banner contextuel sur fiche Bien ET page Fiscalité, calcul à la demande via Clock.
    - UI-SPEC §S10 -> 3 variantes visuelles avec copies EXACTES.
    - UI-SPEC §S11 -> bloc "Échéances CFE" page racine `/fiscalite` (masqué si vide).
    - UI-SPEC §Accessibilité 4.1.2 -> `role="status"` (J-) vs `role="alert"` (J+) + `aria-live` cohérent.
    - V9 ASVS -> lien externe sécurisé `rel="noopener noreferrer"`.

    **À ne PAS faire :**
    - Pas de cron / setInterval / daemon background (anti-pattern §6 RESEARCH, D-CFE6.5).
    - Pas de calcul auto-trigger côté client (JS framework) -> render serveur conditionnel seulement (cohérent Phase 5 + Phase 3 banner IRL).
    - Pas de bouton "Marquer comme payée" dans le banner -> action passe par le formulaire CFE de Plan 06-06 (séparation responsabilités UX — UI-SPEC §S10 ne mentionne pas ce bouton).
    - Pas de calcul de pénalités de retard dans le banner -> UI-SPEC §Copywriting variante 3 mentionne uniquement "pénalités de retard applicables" comme information générale, jamais un montant calculé (R4.3).
    - Pas de nouveau token CSS (UI-SPEC §Design System verrouillé).
    - Pas de dashboard consolidé multi-échéances (= Phase 7 DAS-02 — UI-SPEC §S11 dit "Phase 7 consolidera").
    - Pas de modification de Plan 06-06 (la section CFE existante reste intacte, le banner s'insère au-dessus).
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test -- --run tests/integration/web/route-cfe-banner.test.ts && pnpm test:bdd -- --tags "@phase6-cfe-alerte" && grep -En "role=\"status\"|role=\"alert\"" src/web/views/partials/partial-bandeau-cfe-echeance.ejs | grep -v '^#' | wc -l | awk '{exit ($1 -ge 2) ? 0 : 1}' && grep -En "target=\"_blank\".+rel=\"noopener noreferrer\"" src/web/views/partials/partial-bandeau-cfe-echeance.ejs | grep -v '^#' | wc -l | awk '{exit ($1 -ge 1) ? 0 : 1}' && grep -rEn "setInterval|setTimeout|node-cron" src/web/views/partials/partial-bandeau-cfe-echeance.ejs src/web/routes/biens.ts src/web/routes/fiscalite/racine.ts | grep -v '^#' | wc -l | awk '{exit ($1 == 0) ? 0 : 1}' && pnpm lint:deps</automated>
  </verify>
  <done>
    - `pnpm test -- --run tests/integration/web/route-cfe-banner.test.ts` exit 0 (au moins 5 cas dont 3 variantes + statut payee masque + page /fiscalite).
    - `pnpm test:bdd -- --tags "@phase6-cfe-alerte"` 8 scénarios verts (déterministes ClockFixe).
    - `grep -E "role=\"status\"|role=\"alert\""` dans `partial-bandeau-cfe-echeance.ejs` -> au moins 2 résultats (3 variantes).
    - `grep -E "target=\"_blank\".+rel=\"noopener noreferrer\""` -> au moins 1 résultat (V9).
    - `grep -E "setInterval|setTimeout|node-cron"` dans Plan 06-07 -> 0 résultat (anti-pattern §6).
    - `pnpm lint:deps` exit 0.
    - Manuel : visiter `/biens/:id` avec une déclaration CFE proche échéance (ClockFixe dans dev) -> banner visible avec couleur warning + lien impots.gouv.fr cliquable.
    - 2 commits : `feat(06-07): partial-bandeau-cfe-echeance 3 variantes + intégrations fiche Bien et /fiscalite` + `test(06-07): scenarios BDD cfe-alerte-echeance + step definitions`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Navigateur -> Fastify GET `/biens/:id` et GET `/fiscalite` (rendu serveur) | Calcul des alertes côté serveur via `listerAlertesCfeActives` -> aucune donnée utilisateur en entrée (clock système ou ClockFixe en tests). Aucun input untrusted dans le banner. |
| Banner EJS -> lien externe `https://www.impots.gouv.fr/professionnel/cfe` | Lien externe ouvre nouvel onglet -> protection Referer/window.opener via `target="_blank" rel="noopener noreferrer"` (V9 ASVS). |
| EJS render `formatDate(dateEcheancePaiement)` + `alerte.joursRestants` -> HTML | Auto-échappement EJS `<%= %>` + types contrôlés (`number`, `Temporal.PlainDate`). Pas d'injection possible. |
| Fonction pure domain -> aucun I/O | `calculerAlertesCfe` n'a aucune frontière de confiance (déterministe). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-CFE7-01 | Repudiation (alerte ignorée) | Banner J-30 affiché mais utilisateur ne clique pas -> CFE non payée -> pénalités | accept | V1 single-user audit-friendly -> l'utilisateur est responsable de ses obligations. Le banner est un signal honnête (cf. R1.1 RISKS), pas une garantie. Phase 7 consolidera (notifications email/push DAS-02). |
| T-06-CFE7-02 | Tampering (Clock manipulation) | Tests utilisent ClockFixe -> risque qu'un ClockFixe fuie en prod | mitigate | DI prod = `ClockSysteme` (vérifié main.ts ligne 108). ClockFixe vit uniquement dans `tests/_fakes/` + step definitions BDD. `pnpm lint:deps` interdit l'import test depuis src/. |
| T-06-CFE7-03 | Information Disclosure (Referer) | Lien externe impots.gouv.fr fuite Referer si `noreferrer` absent | mitigate | `rel="noopener noreferrer"` obligatoire sur les 3 variantes du banner (V9 ASVS + UI-SPEC §S10 + §S9). Vérifié par grep dans `<done>`. |
| T-06-CFE7-04 | Information Disclosure (window.opener) | `target="_blank"` sans `noopener` -> nouveau tab peut redirect la page initiale | mitigate | `rel="noopener"` obligatoire (cf. T-06-CFE7-03). |
| T-06-CFE7-05 | DoS calcul alertes | `listerAlertesCfeActives({})` parcourt toutes les DeclarationCfe du bailleur | accept | V1 LMNP : <= 100 biens * <= 10 millésimes = 1000 itérations. Coût `calculerAlertesCfe` linéaire O(n), trivialement négligeable (RESEARCH §T-06-LIASSE-03 patrons similaires). |
| T-06-CFE7-06 | Fiscal misinformation (R4.3) | Banner J+ destructive cite "pénalités de retard applicables" -> risque de fausse information | mitigate | Copywriting UI-SPEC §S10 EXACT : "pénalités de retard **applicables**" (information générale, jamais un montant calculé). Aucun calcul de montant de pénalité dans le banner (`grep` -> 0). R4.3 honnêteté pédagogique. |
| T-06-CFE7-07 | Hexagonal violation | `Temporal.Now` direct dans le domaine au lieu de `Clock` injecté | mitigate | Vérifié par grep dans verify : `grep -rE "Temporal\\.Now" src/domain/fiscalite/cfe/alerte-cfe-j30.ts` -> 0 résultat (06-PATTERNS shared patterns ligne 699). |
| T-06-CFE7-08 | Cron implicite | Risque qu'un développeur ajoute `setInterval` "pour vérifier en arrière-plan" | mitigate | Vérifié par grep + scénarios BDD ClockFixe (déterministes -> impossibles avec cron). Anti-pattern §6 RESEARCH explicite. |
</threat_model>

<verification>
- `pnpm typecheck` exit 0.
- `pnpm test -- --run` tous verts (au moins 12 tests unit alerte + 4 tests use case + 5 tests integration banner).
- `pnpm test:bdd -- --tags "@phase6-cfe-alerte"` 8 scénarios verts.
- `pnpm lint:deps` exit 0 (hexagonal strict).
- `grep -rE "Temporal\\.Now|setInterval|setTimeout|node-cron" src/domain/fiscalite/cfe/alerte-cfe-j30.ts src/application/fiscalite/lister-alertes-cfe-actives.ts` -> 0 résultat (D-CFE6.5, anti-pattern §6).
- `grep -rE "from .+(pdfmake|kysely|fastify|zod|fs|path)" src/domain/fiscalite/cfe/alerte-cfe-j30.ts` -> 0 résultat.
- `grep -rE "calculer.*penalite|montant.*penalite" src/web/views/partials/partial-bandeau-cfe-echeance.ejs src/domain/fiscalite/cfe/alerte-cfe-j30.ts` -> 0 résultat (R4.3 absolu).
- Manuel : visiter `/biens/:id` avec une déclaration CFE proche échéance -> banner visible avec couleur warning + lien impots.gouv.fr -> clic ouvre nouvel onglet (Referer absent). Visiter `/fiscalite` avec 2 alertes actives -> section "Échéances CFE" visible avec 2 banners triés par urgence.
</verification>

<success_criteria>
- ROADMAP success criterion 4 (FIS-06) entièrement couvert : alerte sur l'échéance de paiement décembre via banner J-30 sur fiche Bien + page Fiscalité.
- 100% couverture sur `src/domain/fiscalite/cfe/alerte-cfe-j30.ts` (BDD_PRACTICES.md §7 — logique fiscale).
- Décision D-CFE6.5 entièrement couverte (banner Clock-driven, pas de cron).
- VALIDATION.md §Propriétés fast-check 3 (monotonie) + 4 (filtrage strict) validées.
- WCAG 2.1 AA respecté sur banner (sélecteurs ARIA UI-SPEC §S10, lien externe sécurisé, contrast tokens).
- Phase 6 success criteria 1-4 tous livrés (FIS-05 par Plans 06-01 à 06-05 + FIS-06 par Plans 06-06 et 06-07).
</success_criteria>

<output>
After completion, create `.planning/phases/06-liasse-2031-cfe/06-07-SUMMARY.md` listant :
- Pattern Clock-driven validé (aucun cron, aucun setInterval -- vérifié par grep).
- 3 variantes visuelles du banner avec capture ASCII (warning, warning forte, destructive).
- 8 scénarios BDD ClockFixe déterministes verts (liste des dates + statuts couverts).
- Propriétés fast-check validées : monotonie joursAvantEcheance + filtrage strict statuts payee/exoneree_*.
- Confirmation lien externe sécurisé `rel="noopener noreferrer"` sur les 3 variantes.
- Confirmation `grep` 0 résultat pour `calculer.*penalite|montant.*penalite` (R4.3 verrouillé).
- Backlog Phase 7 : `listerAlertesCfeActives` est consommable tel quel par DAS-02 (dashboard consolidé d'échéances) — pas de refactor nécessaire.
</output>
