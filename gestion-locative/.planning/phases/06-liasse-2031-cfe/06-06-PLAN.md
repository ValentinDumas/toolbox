---
phase: 06-liasse-2031-cfe
plan: 06
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/domain/_shared/identifiants.ts
  - src/domain/fiscalite/cfe/statut-cfe.ts
  - src/domain/fiscalite/cfe/declaration-cfe.ts
  - src/domain/fiscalite/cfe/declaration-cfe-repository.ts
  - src/domain/fiscalite/erreurs.ts
  - src/infrastructure/repositories/declaration-cfe-repository-sqlite.ts
  - src/infrastructure/db/kysely-types.ts
  - src/infrastructure/db/migrations/0023_phase6_declaration_cfe.sql
  - src/application/fiscalite/enregistrer-declaration-cfe.ts
  - src/application/fiscalite/modifier-declaration-cfe.ts
  - src/application/fiscalite/lister-declarations-cfe-par-bien.ts
  - src/web/schemas/cfe-schemas.ts
  - src/web/routes/biens/cfe.ts
  - src/web/views/pages/biens/cfe/nouvelle.ejs
  - src/web/views/pages/biens/cfe/editer.ejs
  - src/web/views/partials/partial-carte-cfe.ejs
  - src/web/views/partials/partial-badge-statut-cfe.ejs
  - src/web/views/partials/partial-aide-cfe.ejs
  - src/web/views/pages/biens/detail.ejs
  - src/web/helpers/formater-statut-cfe.ts
  - src/web/helpers/formater-millesime-cfe.ts
  - src/main.ts
  - tests/_builders/fiscalite.ts
  - tests/unit/fiscalite/declaration-cfe.test.ts
  - tests/integration/repositories/declaration-cfe-repository-sqlite.test.ts
  - tests/integration/web/route-cfe.test.ts
  - tests/bdd/features/cfe-suivi-declaratif.feature
  - tests/bdd/step_definitions/cfe.steps.ts
autonomous: true
requirements: [FIS-06]
tags: [fiscalite, cfe, suivi-declaratif, agregat-racine]

must_haves:
  truths:
    - "L'utilisateur peut enregistrer une DeclarationCfe sur un Bien pour un millésime donné (D-CFE6.1, D-CFE6.2, D-CFE6.3)."
    - "La fiche Bien affiche, sous Fiscalité, la liste antichronologique des DeclarationCfe du bien avec badge statut et échéance de paiement (UI-SPEC §S8)."
    - "Le statut prend strictement 5 valeurs : non_deposee, deposee, exoneree_premiere_annee, exoneree_commune, payee (D-CFE6.3)."
    - "Un invariant InvariantViolated empêche statut='deposee' sans dateDepotDeclaration et statut='payee' sans dateDepotDeclaration + montantAvisCentimes (D-CFE6.3)."
    - "Le formulaire CFE affiche l'aide pédagogique citant CGI art. 1478 (première année exonérée) sans calcul de base imposable (D-CFE6.4, R4.3)."
    - "DeclarationCfe est un agrégat racine du BC Fiscalité référant BienId par identifiant (D-CFE6.2 — pattern TicketTravaux), JAMAIS un sous-agrégat de Bien."
    - "La table SQLite declarations_cfe a UNIQUE (bien_id, millesime) — une seule déclaration CFE par bien par millésime."
    - "Aucun calcul de base imposable CFE n'apparaît dans le code (R4.3, anti-pattern §9 RESEARCH)."
  artifacts:
    - path: "src/domain/fiscalite/cfe/declaration-cfe.ts"
      provides: "Agrégat racine DeclarationCfe + factory creer() + modifier() copy-on-write + invariants"
      exports: ["DeclarationCfe", "DeclarationCfeProps"]
    - path: "src/domain/fiscalite/cfe/statut-cfe.ts"
      provides: "Type union StatutCfe + tableau STATUTS_CFE_VALIDES + LIBELLES_STATUT_CFE"
      exports: ["StatutCfe", "STATUTS_CFE_VALIDES", "LIBELLES_STATUT_CFE"]
    - path: "src/domain/fiscalite/cfe/declaration-cfe-repository.ts"
      provides: "Port DeclarationCfeRepository (enregistrer / trouverParId / trouverParBienMillesime / listerParBien)"
      exports: ["DeclarationCfeRepository"]
    - path: "src/infrastructure/repositories/declaration-cfe-repository-sqlite.ts"
      provides: "Adapter SQLite (versDomaine/versRow + upsert idempotent via onConflict bien_id+millesime)"
      exports: ["DeclarationCfeRepositorySqlite"]
    - path: "src/infrastructure/db/migrations/0023_phase6_declaration_cfe.sql"
      provides: "Table declarations_cfe avec CHECK statut + UNIQUE (bien_id, millesime) + FK biens(id)"
      contains: "CREATE TABLE IF NOT EXISTS declarations_cfe"
    - path: "src/application/fiscalite/enregistrer-declaration-cfe.ts"
      provides: "Use case POST création DeclarationCfe (charge Bien -> factory creer -> enregistrer)"
      exports: ["enregistrerDeclarationCfe"]
    - path: "src/application/fiscalite/modifier-declaration-cfe.ts"
      provides: "Use case PUT édition DeclarationCfe (charge -> modifier(patch) -> enregistre)"
      exports: ["modifierDeclarationCfe"]
    - path: "src/application/fiscalite/lister-declarations-cfe-par-bien.ts"
      provides: "Use case lecture (listerParBien) trié millésime DESC"
      exports: ["listerDeclarationsCfeParBien"]
    - path: "src/web/routes/biens/cfe.ts"
      provides: "Routes GET/POST CFE sur le Bien (Zod aux frontières + CSRF + flash session)"
    - path: "src/web/views/pages/biens/cfe/nouvelle.ejs"
      provides: "Formulaire création DeclarationCfe (aide pédagogique D-CFE6.4)"
    - path: "src/web/views/pages/biens/cfe/editer.ejs"
      provides: "Formulaire édition (mêmes champs pré-remplis)"
    - path: "src/web/views/partials/partial-carte-cfe.ejs"
      provides: "Carte liste CFE sur fiche Bien (UI-SPEC §S8)"
    - path: "src/web/views/partials/partial-badge-statut-cfe.ejs"
      provides: "Badge coloré statut CFE (5 valeurs, couleur + icône WCAG 1.4.1)"
    - path: "tests/bdd/features/cfe-suivi-declaratif.feature"
      provides: "6 scénarios @phase6 @phase6-cfe-suivi (création, transitions, invariants, exonération première année)"
  key_links:
    - from: "src/web/routes/biens/cfe.ts"
      to: "src/application/fiscalite/enregistrer-declaration-cfe.ts"
      via: "appel use case avec DI Fastify + Zod safeParse"
      pattern: "enregistrerDeclarationCfe\\("
    - from: "src/application/fiscalite/enregistrer-declaration-cfe.ts"
      to: "src/domain/fiscalite/cfe/declaration-cfe.ts (factory creer)"
      via: "DeclarationCfe.creer(props)"
      pattern: "DeclarationCfe\\.creer\\("
    - from: "src/infrastructure/repositories/declaration-cfe-repository-sqlite.ts"
      to: "src/domain/fiscalite/cfe/declaration-cfe-repository.ts (port)"
      via: "implements port"
      pattern: "implements DeclarationCfeRepository"
    - from: "src/web/views/pages/biens/detail.ejs"
      to: "src/web/views/partials/partial-carte-cfe.ejs"
      via: "include partial pour chaque DeclarationCfe du bien"
      pattern: "include\\(.+partial-carte-cfe"
    - from: "src/main.ts"
      to: "DeclarationCfeRepositorySqlite + routes CFE"
      via: "instanciation singleton + registerCfeRoutes"
      pattern: "new DeclarationCfeRepositorySqlite\\("
---

<objective>
**Phase Goal (slice 6 FIS-06)** :

**As a** bailleur LMNP single-user, **I want to** enregistrer et consulter l'état déclaratif de la CFE (1447-C-SD) pour chacun de mes biens (statut, date dépôt, montant d'avis, échéance paiement), **so that** je trace mes obligations CFE sans reproduire le formulaire 1447-C-SD et sans calculer la base imposable (relève de la commune).

**Décisions implémentées :**
- D-CFE6.1 -> Suivi déclaratif, PAS de reproduction du 1447-C-SD case-par-case.
- D-CFE6.2 -> Nouvel agrégat racine `DeclarationCfe` dans BC Fiscalité, référence `BienId` par identifiant (pattern miroir `TicketTravaux -> BienId`, jamais sous-agrégat de Bien).
- D-CFE6.3 -> 5 statuts + invariants `dateDepotDeclaration` (deposee) + `montantAvisCentimes` (payee).
- D-CFE6.4 -> Aide pédagogique première année exonérée (CGI art. 1478 II) — AUCUN calcul de base imposable (R4.3).
- D-CFE6.2 brand type -> `DeclarationCfeId` + `nouveauDeclarationCfeId()` étendu dans `identifiants.ts`.

**Périmètre slice 6** :
- Agrégat + repository + migration + 3 use cases + routes CRUD + 2 vues + 3 partials + 2 helpers + tests.
- **Indépendant des slices 1-5** (FIS-05) — Wave 1, parallélisable avec 06-01/02.
- **Pas de banner J-30** (= slice 7, Plan 06-07 — Wave 2).

**Output** : section CFE navigable sur `/biens/:id` (liste + carte + badge + CTA création) + formulaires GET/POST création et édition + 6 scénarios BDD verts + 100% couverture domaine.
</objective>

<execution_context>
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/workflows/execute-plan.md
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-liasse-2031-cfe/06-CONTEXT.md
@.planning/phases/06-liasse-2031-cfe/06-RESEARCH.md
@.planning/phases/06-liasse-2031-cfe/06-PATTERNS.md
@.planning/phases/06-liasse-2031-cfe/06-UI-SPEC.md
@.planning/phases/06-liasse-2031-cfe/06-VALIDATION.md
@CLAUDE.md
@RISKS.md
@LMNP.md
@practices/DDD.md
@practices/BDD_PRACTICES.md
@practices/UI_DESIGN.md
@practices/UX_DESIGN.md
@practices/ACCESSIBILITY.md

# Patterns à cloner (miroirs exacts — voir 06-PATTERNS.md §Pattern critique 3, 6, 8, 9)
@src/domain/travaux/ticket-travaux.ts
@src/domain/travaux/ticket-travaux-repository.ts
@src/infrastructure/repositories/ticket-travaux-repository-sqlite.ts
@src/infrastructure/repositories/declaration-annuelle-repository-sqlite.ts
@src/infrastructure/db/migrations/0016_phase5_declaration_annuelle.sql
@src/domain/fiscalite/erreurs.ts
@src/domain/_shared/identifiants.ts

# Routes et vues de référence
@src/web/routes/fiscalite/cloture.ts
@src/web/routes/biens.ts
@src/web/views/pages/biens/detail.ejs
@src/web/views/partials/empty-state.ejs
@src/web/views/partials/banniere-success.ejs
@src/web/views/partials/form-field.ejs
@src/web/views/partials/partial-badge-qualification.ejs
@src/web/schemas/fiscalite-schemas.ts

<interfaces>
# Brand types (étendre identifiants.ts — pattern miroir nouveauTicketTravauxId lignes 56-67)
export type DeclarationCfeId = string & { readonly __brand: 'DeclarationCfeId' };
export function nouveauDeclarationCfeId(): DeclarationCfeId;

# Statut union (D-CFE6.3)
type StatutCfe = 'non_deposee' | 'deposee' | 'exoneree_premiere_annee' | 'exoneree_commune' | 'payee';
const STATUTS_CFE_VALIDES: readonly StatutCfe[];
const LIBELLES_STATUT_CFE: Record<StatutCfe, string>;
  // 'non_deposee'              -> 'Non déposée'
  // 'deposee'                  -> 'Déposée'
  // 'exoneree_premiere_annee'  -> 'Exonérée — première année (CGI art. 1478)'
  // 'exoneree_commune'         -> 'Exonérée — décision de la commune'
  // 'payee'                    -> 'Payée'

# Agrégat racine (06-PATTERNS §Pattern critique 3)
interface DeclarationCfeProps {
  id?: DeclarationCfeId;
  bienId: BienId;
  millesime: number;                            // dans [2020, 2030]
  statut: StatutCfe;
  dateDepotDeclaration: Temporal.PlainDate | null;
  montantAvisCentimes: Money | null;
  dateEcheancePaiement: Temporal.PlainDate;     // typique 15/12/N (champ utilisateur — D-CFE6.3)
}
class DeclarationCfe {
  static creer(props: DeclarationCfeProps): DeclarationCfe;
  modifier(patch: Partial<Omit<DeclarationCfeProps, 'id' | 'bienId' | 'millesime'>>): DeclarationCfe;
}

# Erreurs typées
class DeclarationCfeIntrouvable extends Error { constructor(id: string); }

# Port repository
interface DeclarationCfeRepository {
  enregistrer(decl: DeclarationCfe, trxArg?: unknown): Promise<void>;  // upsert sur (bien_id, millesime)
  trouverParId(id: DeclarationCfeId): Promise<DeclarationCfe | null>;
  trouverParBienMillesime(bienId: BienId, millesime: number): Promise<DeclarationCfe | null>;
  listerParBien(bienId: BienId): Promise<DeclarationCfe[]>;  // tri millesime DESC
}

# Pattern Bien existant consommé (lecture seule)
interface BienRepository { trouverParId(id: BienId): Promise<Bien | null>; }
class Bien { readonly id: BienId; adresse: { enLigne(): string }; ... }
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: TDD agrégat DeclarationCfe + StatutCfe + brand DeclarationCfeId + erreur typée + builders</name>
  <files>
    src/domain/_shared/identifiants.ts,
    src/domain/fiscalite/cfe/statut-cfe.ts,
    src/domain/fiscalite/cfe/declaration-cfe.ts,
    src/domain/fiscalite/erreurs.ts,
    tests/unit/fiscalite/declaration-cfe.test.ts,
    tests/_builders/fiscalite.ts
  </files>
  <read_first>
    src/domain/travaux/ticket-travaux.ts (Pattern critique 3 — factory creer + invariants + brand + 'field' in patch),
    src/domain/_shared/identifiants.ts (brand types existants à étendre — lignes 56-67 TicketTravauxId à reproduire pour DeclarationCfeId),
    src/domain/_shared/erreurs.ts (InvariantViolated),
    src/domain/_shared/money.ts (Money BigInt centimes — méthodes egale/superieurA/fromCentimes),
    src/domain/fiscalite/erreurs.ts (pattern RegleFiscaleAbsente / DeclarationFigeeException pour DeclarationCfeIntrouvable),
    src/domain/fiscalite/declaration-annuelle.ts (pattern factory creer + invariants typés),
    .planning/phases/06-liasse-2031-cfe/06-PATTERNS.md (§Pattern critique 3 + §Pattern critique 8),
    .planning/phases/06-liasse-2031-cfe/06-CONTEXT.md (D-CFE6.2, D-CFE6.3),
    .planning/phases/06-liasse-2031-cfe/06-RESEARCH.md (§Pattern 3 lignes 419-510 modifier 'field' in patch — pattern critique nullables).
  </read_first>
  <behavior>
    **TDD RED -> GREEN -> REFACTOR.**

    **RED — `tests/unit/fiscalite/declaration-cfe.test.ts` (au moins 8 tests via vitest describe/it) :**
    - `DeclarationCfe.creer({ bienId, millesime: 2026, statut: 'non_deposee', dateDepotDeclaration: null, montantAvisCentimes: null, dateEcheancePaiement: Temporal.PlainDate.from('2026-12-15') })` retourne instance avec `id` auto-généré (UUID v4 + brand).
    - `creer` avec `millesime: 2019` ou `millesime: 2031` lève `InvariantViolated` (plage [2020, 2030] — 06-RESEARCH §Pattern 3).
    - `creer` avec `statut: 'deposee'` et `dateDepotDeclaration: null` lève `InvariantViolated` citant "statut='deposee' exige dateDepotDeclaration (D-CFE6.3)".
    - `creer` avec `statut: 'payee'` et `dateDepotDeclaration: null` ou `montantAvisCentimes: null` lève `InvariantViolated` citant "statut='payee' exige dateDepotDeclaration + montantAvisCentimes (D-CFE6.3)".
    - `creer` avec `statut: 'exoneree_premiere_annee'` SANS dateDepotDeclaration et SANS montantAvisCentimes : OK (exonération = simple statut documenté, D-CFE6.4).
    - `decl.modifier({ statut: 'deposee', dateDepotDeclaration: Temporal.PlainDate.from('2026-12-10') })` retourne nouvelle instance (immutabilité — copy-on-write) avec champs patchés + champs non patchés conservés.
    - `decl.modifier({ montantAvisCentimes: null })` (effacement explicite via pattern `'field' in patch`) : la nouvelle instance a `montantAvisCentimes === null` (PAS la valeur précédente — vérification critique anti-écrasement silencieux).
    - `decl.modifier({})` (patch vide) : retourne une instance équivalente (tous champs préservés).
    - Test brand : `nouveauDeclarationCfeId()` retourne une string conforme à `UUID_V4_REGEX` et le brand `__brand: 'DeclarationCfeId'` est respecté à la compilation.

    **GREEN — Implémentation :**

    1. **`src/domain/_shared/identifiants.ts` — étendre** (ajouter à la suite de Phase 5, ligne ~122) :
       - `export type DeclarationCfeId = string & { readonly __brand: 'DeclarationCfeId' };`
       - `export function nouveauDeclarationCfeId(): DeclarationCfeId { return crypto.randomUUID() as DeclarationCfeId; }`
       - Bloc commentaire : `// Phase 6 — identifiants CFE (FIS-06)`.

    2. **`src/domain/fiscalite/erreurs.ts` — étendre** (ajouter en fin de fichier) :
       - `export class DeclarationCfeIntrouvable extends Error { constructor(public readonly id: string) { super('Déclaration CFE introuvable : ' + id); this.name = 'DeclarationCfeIntrouvable'; } }`
       - Pattern miroir `RegleFiscaleAbsente` (déjà présent) — `this.name` obligatoire pour le type-guard côté route.

    3. **`src/domain/fiscalite/cfe/statut-cfe.ts` — nouveau fichier** :
       - `export type StatutCfe = 'non_deposee' | 'deposee' | 'exoneree_premiere_annee' | 'exoneree_commune' | 'payee';`
       - `export const STATUTS_CFE_VALIDES: readonly StatutCfe[] = ['non_deposee', 'deposee', 'exoneree_premiere_annee', 'exoneree_commune', 'payee'] as const;`
       - `export const LIBELLES_STATUT_CFE: Record<StatutCfe, string>` avec les 5 libellés français exacts UI-SPEC §S9.

    4. **`src/domain/fiscalite/cfe/declaration-cfe.ts` — nouveau fichier** (pattern miroir `ticket-travaux.ts`) :
       - Imports : `Temporal`, `InvariantViolated`, `Money`, `nouveauDeclarationCfeId`, `BienId`, `DeclarationCfeId`, `StatutCfe`, `STATUTS_CFE_VALIDES`.
       - Interface `DeclarationCfeProps` (cf. `<interfaces>` ci-dessus).
       - Classe `DeclarationCfe` avec `private constructor(id, props)` (pas d'instanciation directe — Pattern critique 3).
       - Champs publics `readonly` : `id`, `bienId`, `millesime`, `statut`, `dateDepotDeclaration`, `montantAvisCentimes`, `dateEcheancePaiement`.
       - **Factory `static creer(props)`** invariants ordonnés (RESEARCH §Pattern 3 lignes 472-489) :
         a) `STATUTS_CFE_VALIDES.includes(props.statut)` sinon `InvariantViolated('statut CFE invalide : ...')`.
         b) `props.millesime` dans `[2020, 2030]` sinon `InvariantViolated('millesime hors plage raisonnable (2020-2030) : ...')`.
         c) `statut === 'deposee' && dateDepotDeclaration === null` -> `InvariantViolated("DeclarationCfe statut='deposee' exige dateDepotDeclaration (D-CFE6.3)")`.
         d) `statut === 'payee' && (dateDepotDeclaration === null || montantAvisCentimes === null)` -> `InvariantViolated("DeclarationCfe statut='payee' exige dateDepotDeclaration + montantAvisCentimes (D-CFE6.3)")`.
         e) `return new DeclarationCfe(props.id ?? nouveauDeclarationCfeId(), {...props})`.
       - **Méthode `modifier(patch)` copy-on-write** pattern critique `'field' in patch` pour les nullables (RESEARCH lignes 492-507) :
         - `id`, `bienId`, `millesime` jamais patchables (cf. type `Omit<...>`).
         - `statut` : `'statut' in patch ? (patch.statut as StatutCfe) : this.statut`.
         - `dateDepotDeclaration` : `'dateDepotDeclaration' in patch ? patch.dateDepotDeclaration as Temporal.PlainDate | null : this.dateDepotDeclaration` — **pas `??`** (sinon `null` explicite écrase silencieusement).
         - `montantAvisCentimes` : idem pattern `'field' in patch`.
         - `dateEcheancePaiement` : `patch.dateEcheancePaiement ?? this.dateEcheancePaiement` (champ non nullable, `??` autorisé).
         - Délègue à `DeclarationCfe.creer({...})` pour re-valider tous les invariants après patch (défense en profondeur).

    5. **`tests/_builders/fiscalite.ts` — étendre** (sans casser builders Phase 5 lignes 102/202) :
       - `export function uneDeclarationCfe(overrides?: Partial<DeclarationCfeProps>): DeclarationCfe` avec defaults sûrs : `bienId` (UUID test fixe), `millesime: 2026`, `statut: 'non_deposee'`, `dateDepotDeclaration: null`, `montantAvisCentimes: null`, `dateEcheancePaiement: Temporal.PlainDate.from('2026-12-15')`.

    **REFACTOR (si nécessaire) :**
    - Garder fonctions < 30 lignes (SOFTWARE_CRAFTSMANSHIP.md gates CI).
    - Vérifier qu'aucun import infra/HTTP n'a fui dans `src/domain/fiscalite/cfe/`.

    Commits atomiques :
    - `test(06-06): tests unit DeclarationCfe + StatutCfe (RED)`
    - `feat(06-06): brand DeclarationCfeId + erreur DeclarationCfeIntrouvable`
    - `feat(06-06): agregat racine DeclarationCfe + StatutCfe + invariants D-CFE6.3`
    - `test(06-06): builder uneDeclarationCfe`
  </behavior>
  <action>
    Cloner `ticket-travaux.ts` pour `DeclarationCfe` (06-PATTERNS §Pattern critique 3, role-match exact sur référence BienId par identifiant + factory + invariants). Cloner `RegleFiscaleAbsente` pour `DeclarationCfeIntrouvable` (06-PATTERNS §Pattern critique 8). Ubiquitous language français strict (`StatutCfe`, `millesime`, `dateDepotDeclaration`, `dateEcheancePaiement`, `montantAvisCentimes` — JAMAIS `statusCfe`/`year`/`amount`).

    **Décisions implémentées :**
    - D-CFE6.2 -> agrégat racine BC Fiscalité (chemin `domain/fiscalite/cfe/`), brand `DeclarationCfeId`, référence `BienId` par identifiant (jamais sous-agrégat de Bien).
    - D-CFE6.3 -> 5 statuts strict + invariants typés `'deposee' implique dateDepotDeclaration`, `'payee' implique dateDepotDeclaration + montantAvisCentimes`.
    - D-CFE6.4 -> `exoneree_premiere_annee` autorisé sans dates ni montant (exonération = pure documentation).

    **À ne PAS faire (anti-patterns) :**
    - Pas d'import `pdfmake`/`kysely`/`fastify`/`zod`/`fs`/`path` dans `src/domain/fiscalite/cfe/` (anti-pattern §1 RESEARCH — `pnpm lint:deps` exit 0 obligatoire).
    - Pas de sous-agrégat `Bien.declarationsCfe[]` (anti-pattern §2 RESEARCH).
    - Pas de fonction `calculerBase*` ni `valeurLocative*` dans ce fichier (anti-pattern §9 RESEARCH, R4.3).
    - Pas de reproduction du formulaire 1447-C-SD case-par-case (anti-pattern §10 RESEARCH).
    - Pas de `??` pour les nullables dans `modifier()` — pattern `'field' in patch` obligatoire (RESEARCH ligne 510).
    - Pas de Zod dans le domaine (D-15).
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test -- --run tests/unit/fiscalite/declaration-cfe.test.ts && pnpm lint:deps && grep -rEn "from .+(pdfmake|kysely|fastify|zod|fs|path)" src/domain/fiscalite/cfe/ | grep -v '^#' | wc -l | awk '{exit ($1 == 0) ? 0 : 1}' && grep -rEn "calculer.*base|valeur.*locative" src/domain/fiscalite/cfe/ | grep -v '^#' | wc -l | awk '{exit ($1 == 0) ? 0 : 1}'</automated>
  </verify>
  <done>
    - `pnpm test -- --run tests/unit/fiscalite/declaration-cfe.test.ts` exit 0 (au moins 8 tests passent).
    - `pnpm lint:deps` exit 0 (hexagonal strict — aucun import infra dans `src/domain/fiscalite/cfe/`).
    - `grep -rE "from .+(pdfmake|kysely|fastify|zod|fs|path)" src/domain/fiscalite/cfe/` retourne 0 résultat.
    - `grep -rE "calculer.*base|valeur.*locative" src/domain/fiscalite/cfe/` retourne 0 résultat (R4.3).
    - 4 commits atomiques.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: TDD repository SQLite + migration 0023 + use cases application + 6 scénarios BDD suivi déclaratif</name>
  <files>
    src/infrastructure/db/migrations/0023_phase6_declaration_cfe.sql,
    src/infrastructure/db/kysely-types.ts,
    src/domain/fiscalite/cfe/declaration-cfe-repository.ts,
    src/infrastructure/repositories/declaration-cfe-repository-sqlite.ts,
    src/application/fiscalite/enregistrer-declaration-cfe.ts,
    src/application/fiscalite/modifier-declaration-cfe.ts,
    src/application/fiscalite/lister-declarations-cfe-par-bien.ts,
    tests/integration/repositories/declaration-cfe-repository-sqlite.test.ts,
    tests/bdd/features/cfe-suivi-declaratif.feature,
    tests/bdd/step_definitions/cfe.steps.ts,
    tests/_builders/fiscalite.ts
  </files>
  <read_first>
    src/infrastructure/repositories/declaration-annuelle-repository-sqlite.ts (Pattern critique 6 — versDomaine/versRow + DbOrTrx + transaction optionnelle),
    src/infrastructure/repositories/ticket-travaux-repository-sqlite.ts (pattern onConflict upsert — CFE éditable contrairement à DeclarationAnnuelle append-only),
    src/infrastructure/db/migrations/0016_phase5_declaration_annuelle.sql (Pattern critique 9 — BEGIN/COMMIT + CHECK + UNIQUE + FK),
    src/infrastructure/db/migrations/0022_phase5_ticket_qualifie_le.sql (dernière migration Phase 5 — confirme numérotation suivante 0023),
    src/infrastructure/db/kysely-types.ts (interface DeclarationsAnnuellesTable à cloner pour DeclarationsCfeTable),
    src/domain/fiscalite/declaration-annuelle-repository.ts (pattern port — méthodes à transposer en trouverParBienMillesime),
    src/application/fiscalite/exporter-pdf-recap.ts (Pattern critique 7 — DI deps + erreurs typées + orchestration),
    src/application/fiscalite/creer-declaration-corrigee.ts (pattern use case POST avec factory creer + repo enregistrer),
    tests/integration/repositories/declaration-annuelle-repository-sqlite.test.ts (pattern round-trip DB éphémère),
    tests/bdd/step_definitions/fiscalite-cloture.steps.ts (pattern step definitions à cloner pour cfe.steps.ts),
    .planning/phases/06-liasse-2031-cfe/06-PATTERNS.md (§Pattern critique 6, 7, 9),
    .planning/phases/06-liasse-2031-cfe/06-CONTEXT.md (D-CFE6.2, D-CFE6.3, D-CFE6.4),
    .planning/phases/06-liasse-2031-cfe/06-VALIDATION.md (§Per-Task Verification Map — scénarios cfe-suivi-declaratif).
  </read_first>
  <behavior>
    **TDD RED -> GREEN -> REFACTOR.**

    **RED — `tests/bdd/features/cfe-suivi-declaratif.feature` (6 scénarios @phase6 @phase6-cfe-suivi — VALIDATION.md) :**

    Feature et Background en français.

    - Scénario 1 : `Etant donné un Bien sans déclaration CFE` `Quand j'enregistre une déclaration CFE pour le millésime 2026 avec statut "Non déposée" et échéance "15/12/2026"` `Alors la liste des CFE du bien contient 1 entrée` `Et son statut est "Non déposée"` `Et son badge porte le libellé "Non déposée"`.
    - Scénario 2 : `Etant donné une déclaration CFE 2026 statut "Non déposée"` `Quand je modifie son statut en "Déposée" avec date de dépôt "10/12/2026"` `Alors la déclaration porte le statut "Déposée"` `Et la date de dépôt est "10/12/2026"`.
    - Scénario 3 (invariant D-CFE6.3) : `Etant donné une déclaration CFE en cours d'édition` `Quand je tente de passer au statut "Déposée" sans date de dépôt` `Alors la création échoue avec InvariantViolated citant "dateDepotDeclaration"` `Et le formulaire ré-affiche le message d'erreur`.
    - Scénario 4 (invariant D-CFE6.3) : `Quand je tente de passer au statut "Payée" sans date de dépôt et sans montant d'avis` `Alors la création échoue avec InvariantViolated citant "montantAvisCentimes"`.
    - Scénario 5 (D-CFE6.4 exonération première année) : `Etant donné un Bien en première année d'activité LMNP` `Quand j'enregistre une déclaration CFE statut "Exonérée — première année (CGI art. 1478)"` `Alors la déclaration est enregistrée sans date de dépôt ni montant` `Et le formulaire CFE affiche l'aide pédagogique citant "CGI art. 1478"` `Et l'aide précise que la base imposable n'est pas calculée par l'app`.
    - Scénario 6 (copy-on-write `'field' in patch`) : `Etant donné une déclaration CFE 2026 statut "Déposée" avec date dépôt "10/12/2026" et montant avis 320,00 €` `Quand je modifie uniquement la date d'échéance en "20/12/2026"` `Alors la date de dépôt reste "10/12/2026"` `Et le montant d'avis reste "320,00 €"` `Et le statut reste "Déposée"`.

    **RED — `tests/integration/repositories/declaration-cfe-repository-sqlite.test.ts` (au moins 4 cas) :**
    - Round-trip statut `non_deposee` : créer via builder -> `enregistrer` -> `trouverParId` -> assert égalité (Temporal `dateEcheancePaiement` round-trip ISO TEXT, montant null préservé).
    - Round-trip statut `payee` : créer avec `montantAvisCentimes = Money.fromCentimes(32000n)` + `dateDepotDeclaration` -> round-trip -> assert Money égalité via `.egale()`.
    - `trouverParBienMillesime(bienId, 2026)` retourne la déclaration ; `trouverParBienMillesime(bienId, 2027)` retourne `null`.
    - `listerParBien(bienId)` retourne 3 déclarations triées `millesime DESC` (2028, 2027, 2026).
    - **UNIQUE (bien_id, millesime)** : 2 `enregistrer` consécutifs sur le même couple -> upsert (la 2e version remplace, pas un doublon). Vérifier statut/dates patchés.

    **GREEN — Implémentation :**

    1. **`src/infrastructure/db/migrations/0023_phase6_declaration_cfe.sql`** (Pattern critique 9 RESEARCH lignes 583-607) :
       - Bloc `BEGIN; CREATE TABLE IF NOT EXISTS declarations_cfe (id TEXT PK, bien_id TEXT NOT NULL REFERENCES biens(id), millesime INTEGER NOT NULL, statut TEXT NOT NULL CHECK (statut IN ('non_deposee','deposee','exoneree_premiere_annee','exoneree_commune','payee')), date_depot_declaration TEXT NULL, montant_avis_centimes INTEGER NULL, date_echeance_paiement TEXT NOT NULL, UNIQUE (bien_id, millesime)); COMMIT;`
       - **Vérifier** que la migration précédente était `0022_phase5_ticket_qualifie_le.sql` (`ls src/infrastructure/db/migrations/`).

    2. **`src/infrastructure/db/kysely-types.ts` — étendre** :
       - Interface `DeclarationsCfeTable { id: string; bien_id: string; millesime: number; statut: StatutCfeRow; date_depot_declaration: string | null; montant_avis_centimes: number | null; date_echeance_paiement: string; }` (où `StatutCfeRow` est l'union des 5 valeurs).
       - Ajouter `declarations_cfe: DeclarationsCfeTable` au type `DB`.

    3. **`src/domain/fiscalite/cfe/declaration-cfe-repository.ts`** : interface comme `<interfaces>` ci-dessus. JSDoc cite Pattern critique 6.

    4. **`src/infrastructure/repositories/declaration-cfe-repository-sqlite.ts`** (pattern miroir `declaration-annuelle-repository-sqlite.ts` AVEC upsert au lieu d'append-only) :
       - `type DbOrTrx = Kysely<DB> | Transaction<DB>;`
       - `type Row` strict.
       - `enregistrer(decl, trxArg?)` : `db.insertInto('declarations_cfe').values({...}).onConflict((oc) => oc.columns(['bien_id', 'millesime']).doUpdateSet({...mêmes champs mutables...})).execute()`. **Critique : `onConflict` sur `(bien_id, millesime)` PAS sur `id`** (clé d'idempotence métier composite — édition d'une CFE existante doit fonctionner).
       - `versDomaine(row): DeclarationCfe` : `DeclarationCfe.creer({ id: row.id as DeclarationCfeId, bienId: row.bien_id as BienId, millesime: row.millesime, statut: row.statut, dateDepotDeclaration: row.date_depot_declaration ? Temporal.PlainDate.from(row.date_depot_declaration) : null, montantAvisCentimes: row.montant_avis_centimes !== null ? Money.fromCentimes(BigInt(row.montant_avis_centimes)) : null, dateEcheancePaiement: Temporal.PlainDate.from(row.date_echeance_paiement) })`.
       - `versRow(decl): Row` : symétrique — `.toSqliteInteger()` pour Money, `.toString()` ISO 8601 pour Temporal.
       - `trouverParId`, `trouverParBienMillesime`, `listerParBien` (`orderBy('millesime', 'desc')`).

    5. **`src/application/fiscalite/enregistrer-declaration-cfe.ts`** :
       - Interface `EnregistrerDeclarationCfeCommande { bienId: BienId; millesime: number; statut: StatutCfe; dateDepotDeclaration: Temporal.PlainDate | null; montantAvisCentimes: Money | null; dateEcheancePaiement: Temporal.PlainDate; }`.
       - Interface `EnregistrerDeclarationCfeDeps { bienRepo: BienRepository; cfeRepo: DeclarationCfeRepository; }`.
       - Algorithme : 1) `bien = await bienRepo.trouverParId(commande.bienId)` -> throw `BienIntrouvable` si null. 2) `decl = DeclarationCfe.creer({...commande})` (relaie `InvariantViolated`). 3) `await cfeRepo.enregistrer(decl)`. 4) `return decl`.

    6. **`src/application/fiscalite/modifier-declaration-cfe.ts`** :
       - Interface `ModifierDeclarationCfeCommande { id: DeclarationCfeId; patch: Partial<Omit<DeclarationCfeProps, 'id' | 'bienId' | 'millesime'>>; }`.
       - Algorithme : 1) `existante = await cfeRepo.trouverParId(commande.id)` -> throw `DeclarationCfeIntrouvable` si null. 2) `nouvelleVersion = existante.modifier(commande.patch)` (relaie `InvariantViolated`). 3) `await cfeRepo.enregistrer(nouvelleVersion)` (upsert remplace). 4) `return nouvelleVersion`.

    7. **`src/application/fiscalite/lister-declarations-cfe-par-bien.ts`** :
       - Wrapper léger sur `cfeRepo.listerParBien(bienId)` — déjà trié `millesime DESC` côté repo.
       - Sépare le port domaine de l'usage côté route (cohérence architecture Phase 5).

    8. **`tests/_builders/fiscalite.ts` — étendre** : `declarationCfeBuilder` chaînable si pertinent (facultatif — `uneDeclarationCfe` Task 1 suffit).

    9. **`tests/bdd/step_definitions/cfe.steps.ts` — nouveau** : pattern miroir `tests/bdd/step_definitions/fiscalite-cloture.steps.ts` (cf. PATTERNS Bloc 8). Steps Given/When/Then exclusivement français, `this.world.X` pour partager l'état entre steps.

    **REFACTOR :**
    - Vérifier que `enregistrerDeclarationCfe` et `modifierDeclarationCfe` n'importent rien depuis `infrastructure/` (lint:deps).
    - Extraire `versDomaine`/`versRow` en méthodes privées du repo SQLite (pattern existant).

    Commits atomiques :
    - `feat(06-06): migration 0023 declarations_cfe + kysely types`
    - `test(06-06): tests integration repo SQLite + scenarios BDD cfe-suivi-declaratif (RED)`
    - `feat(06-06): port + adapter SQLite DeclarationCfeRepository (upsert idempotent)`
    - `feat(06-06): use cases enregistrer/modifier/lister DeclarationCfe`
  </behavior>
  <action>
    Pattern miroir `DeclarationAnnuelleRepositorySqlite` pour la structure générale (06-PATTERNS §6 + §9), MAIS différence majeure : CFE permet l'édition donc `onConflict.doUpdateSet` (pas append-only). Pattern use case orchestrateur cross-BC miroir `exporter-pdf-recap.ts` (06-PATTERNS §7), MAIS simplifié (un seul repo en sortie + injection de Bien pour vérifier existence).

    **Décisions implémentées :**
    - D-CFE6.2 -> repo SQLite avec FK `bien_id REFERENCES biens(id)` + UNIQUE composite + CHECK statut 5 valeurs.
    - D-CFE6.3 -> round-trip Temporal/Money/null préservé.
    - Numérotation migration : `0023` confirmée (Phase 5 stop à `0022_phase5_ticket_qualifie_le.sql`).

    **À ne PAS faire (anti-patterns) :**
    - Pas de `onConflict.column('id')` — la clé d'idempotence métier est `(bien_id, millesime)` (D-CFE6.2 + ROADMAP).
    - Pas d'import `pdfmake`/`fastify`/`zod` dans `src/application/fiscalite/` (anti-pattern §1).
    - Pas de duplication des invariants côté use case — les invariants vivent dans `DeclarationCfe.creer()` (DDD strict).
    - Pas de Zod côté use case (D-15 — Zod uniquement frontières HTTP Task 3).
    - Pas d'auto-calcul `dateEcheancePaiement = 15/12/millesime` — c'est un champ utilisateur (D-CFE6.3 + RESEARCH §Don't Hand-Roll ligne 844 : "L'échéance est sur l'avis reçu de la commune, peut varier").
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test -- --run tests/integration/repositories/declaration-cfe-repository-sqlite.test.ts && pnpm test:bdd -- --tags "@phase6-cfe-suivi" && pnpm lint:deps && grep -En "onConflict.+column.+'id'" src/infrastructure/repositories/declaration-cfe-repository-sqlite.ts | grep -v '^#' | wc -l | awk '{exit ($1 == 0) ? 0 : 1}' && test -f src/infrastructure/db/migrations/0023_phase6_declaration_cfe.sql</automated>
  </verify>
  <done>
    - `pnpm test -- --run tests/integration/repositories/declaration-cfe-repository-sqlite.test.ts` exit 0 (au moins 4 cas dont upsert idempotent).
    - `pnpm test:bdd -- --tags "@phase6-cfe-suivi"` 6 scénarios verts.
    - `pnpm lint:deps` exit 0.
    - Migration `0023_phase6_declaration_cfe.sql` présente et exécutée par `pnpm migrate` (cf. CI).
    - `grep -E "onConflict.+column.+'id'"` dans le repo SQLite -> 0 résultat (upsert composite seulement).
    - 4 commits atomiques.
  </done>
</task>

<task type="execute">
  <name>Task 3: Routes Fastify GET/POST CFE + Zod schemas + 2 vues + 3 partials + 2 helpers + section CFE sur fiche Bien + DI main.ts</name>
  <files>
    src/web/schemas/cfe-schemas.ts,
    src/web/routes/biens/cfe.ts,
    src/web/views/pages/biens/cfe/nouvelle.ejs,
    src/web/views/pages/biens/cfe/editer.ejs,
    src/web/views/partials/partial-carte-cfe.ejs,
    src/web/views/partials/partial-badge-statut-cfe.ejs,
    src/web/views/partials/partial-aide-cfe.ejs,
    src/web/views/pages/biens/detail.ejs,
    src/web/helpers/formater-statut-cfe.ts,
    src/web/helpers/formater-millesime-cfe.ts,
    src/main.ts,
    tests/integration/web/route-cfe.test.ts
  </files>
  <read_first>
    src/web/routes/fiscalite/cloture.ts (pattern GET/POST Fastify + Zod safeParse + redirect + flash + CSRF + reply.view erreurs),
    src/web/routes/biens.ts (pattern routes Bien existantes — à NE PAS modifier, sauf besoin d'export router),
    src/web/schemas/fiscalite-schemas.ts (pattern Zod schemas lignes 102-119 — z.enum, z.coerce.number().int().min().max(), z.string().regex date ISO),
    src/web/views/pages/biens/detail.ejs (page à enrichir — sections actuelles : Documents, Travaux, à ajouter : CFE après Fiscalité),
    src/web/views/partials/empty-state.ejs (réutiliser pour fiche CFE vide UI-SPEC §S8),
    src/web/views/partials/banniere-success.ejs (flash post-création/modification UI-SPEC §S9),
    src/web/views/partials/form-field.ejs (réutiliser pour les 5 champs du formulaire CFE),
    src/web/views/partials/partial-badge-qualification.ejs (pattern badge coloré + icône — à cloner pour partial-badge-statut-cfe.ejs),
    src/web/helpers/format-money.ts + format-date.ts (helpers existants — réutiliser tels quels),
    src/main.ts (point d'injection DI à étendre + import migrations),
    public/styles/app.css (tokens couleur : --couleur-warning, --couleur-destructive, --couleur-success, --couleur-success-bg, --couleur-neutre-bg — déjà en place),
    .planning/phases/06-liasse-2031-cfe/06-UI-SPEC.md (§S8 fiche CFE, §S9 formulaire CFE, §Copywriting Contract, §Accessibilité, §Partials Inventaire),
    .planning/phases/06-liasse-2031-cfe/06-CONTEXT.md (D-CFE6.3, D-CFE6.4),
    .planning/phases/06-liasse-2031-cfe/06-RESEARCH.md (§Don't Hand-Roll, §Common Pitfalls §9 base imposable).
  </read_first>
  <action>
    Câbler routes GET/POST CFE sur la fiche Bien + vues + partials + section CFE intégrée à la page `/biens/:id` + DI main.ts. Le banner J-30 (UI-SPEC §S10) est livré par Plan 06-07, **PAS** ici.

    **`src/web/schemas/cfe-schemas.ts`** (Zod aux frontières HTTP — D-15) :
    - `enregistrerCfeSchema = z.object({ millesime: z.coerce.number().int().min(2020).max(2030), statut: z.enum(['non_deposee', 'deposee', 'exoneree_premiere_annee', 'exoneree_commune', 'payee']), dateDepotDeclaration: z.string().regex(/^\d{4}-\d{2}-\d{2}$/, 'Format AAAA-MM-JJ requis').nullable().or(z.literal('').transform(() => null)), montantAvisEuros: z.coerce.number().min(0).nullable().or(z.literal('').transform(() => null)), dateEcheancePaiement: z.string().regex(/^\d{4}-\d{2}-\d{2}$/, 'Format AAAA-MM-JJ requis') })`.
    - `modifierCfeSchema` : idem mais tous les champs `.optional()` (patch partiel).
    - Conversion `montantAvisEuros` (number euros frontend) -> `Money.fromEuros(n)` côté route (jamais dans schema).

    **`src/web/helpers/formater-statut-cfe.ts`** :
    - `export function formaterStatutCfe(statut: StatutCfe): string` -> retourne `LIBELLES_STATUT_CFE[statut]` (mapping libellé fr — pattern miroir helper `formater-categorie-charge.ts` Phase 5).
    - Tests unitaires recommandés (mais facultatifs — wrapper pur d'un mapping constant).

    **`src/web/helpers/formater-millesime-cfe.ts`** :
    - `export function formaterMillesimeCfe(millesime: number): string` -> `\`CFE ${millesime}\``.

    **`src/web/views/partials/partial-badge-statut-cfe.ejs`** (UI-SPEC §Color + §Accessibilité WCAG 1.4.1 — texte + icône doublent la couleur) :
    - Props : `statut: StatutCfe`.
    - Switch statut -> couleur token + icône :
      - `payee` -> `--couleur-success`, icône `✓`.
      - `exoneree_premiere_annee` / `exoneree_commune` -> `--couleur-success`, icône `✓`.
      - `deposee` -> `--couleur-warning`, icône `⚠`.
      - `non_deposee` -> `--couleur-warning`, icône `⚠`.
    - Rendu : `<span class="badge badge-{couleur}" aria-label="Statut : {libellé}">{icône aria-hidden="true"} {libellé}</span>`.

    **`src/web/views/partials/partial-aide-cfe.ejs`** (UI-SPEC §S9 — repliable, fond accent-bg, lien externe sécurisé) :
    - `<details><summary>Aide — Comprendre la CFE</summary><div>` avec copie EXACTE UI-SPEC Copywriting :
      "Quand déposer la 1447-C-SD ? À la **création** de votre activité LMNP, avant le 31 décembre de l'année de début. Votre **première année d'activité est exonérée** de CFE (CGI art. 1478). L'année suivante, vous recevrez un avis de votre commune en octobre/novembre — la base d'imposition est calculée par votre Service des Impôts des Entreprises (SIE), **pas par cette application**."
    - Lien externe : `<a href="https://www.impots.gouv.fr/contacts" target="_blank" rel="noopener noreferrer">Trouver mon SIE compétent</a>`.

    **`src/web/views/partials/partial-carte-cfe.ejs`** (UI-SPEC §S8 — carte secondary par millésime) :
    - Props : `declarationCfe: DeclarationCfe`, `bienId: BienId`.
    - En-tête : `<strong><%= formaterMillesimeCfe(d.millesime) %></strong>` + si `d.dateDepotDeclaration`, " — déposée le " + `formatDate(d.dateDepotDeclaration)` + " | échéance paiement : **" + `formatDate(d.dateEcheancePaiement)` + "**".
    - Badge à droite via `<%- include('partial-badge-statut-cfe', { statut: d.statut }) %>`.
    - Si `d.montantAvisCentimes !== null` : ligne secondaire 14px "Montant d'avis : " + `formatMoney(d.montantAvisCentimes)`.
    - Lien `<a href="/biens/<%= bienId %>/cfe/<%= d.id %>/editer" class="row-actions">Modifier</a>`.
    - Fond `--couleur-neutre-bg`, padding `md` (16px).

    **`src/web/views/pages/biens/cfe/nouvelle.ejs` + `editer.ejs`** (UI-SPEC §S9 — formulaire colonne unique) :
    - Layout : `<%- include('../../../partials/layout-debut', { titre: 'CFE — ' + bien.adresse.enLigne() + ' — ' + millesime, breadcrumbs: [{url:'/biens', label:'Biens'}, {url:'/biens/'+bien.id, label: bien.adresse.enLigne()}, {label: 'CFE ' + millesime}], navActive: 'biens' }) %>`.
    - `<%- include('../../../partials/partial-aide-cfe') %>` en haut (repliable).
    - `<form method="POST" action="/biens/<%= bien.id %>/cfe<%= isEdit ? '/' + d.id + '/modifier' : '' %>" aria-label="Déclaration CFE <%= millesime %>">` + champ caché CSRF (pattern `@fastify/csrf-protection`).
    - 5 champs via `form-field.ejs` (UI-SPEC §S9 table) :
      - `millesime` : input number 2020-2030 (required, pré-rempli query string ou existant).
      - `statut` : select 5 options avec libellés `LIBELLES_STATUT_CFE` (required).
      - `dateDepotDeclaration` : input date (optional). Aide : "Optionnel — uniquement si statut = déposée."
      - `montantAvisEuros` : input number step=0.01 min=0 (optional). Aide : "Renseignez le montant reçu de votre commune. Laissez vide si non reçu."
      - `dateEcheancePaiement` : input date (required). Aide : "Date limite de paiement (généralement le 15 décembre)."
    - Si erreurs `req.session.formErrors` : afficher inline via `aria-describedby` + message UI-SPEC Copywriting ("Une déclaration au statut 'Déposée' nécessite une date de dépôt. Renseignez la date avant d'enregistrer." pour scénario invariant 3).
    - CTA primaire : `<button type="submit">Enregistrer la déclaration CFE</button>`. Min height 44px (WCAG 2.5.5).
    - Lien secondaire `<a href="/biens/<%= bien.id %>">Annuler</a>`.

    **`src/web/routes/biens/cfe.ts`** (nouveau fichier — créer le dossier `src/web/routes/biens/`) :
    - Interface `CfeRouteDeps { bienRepo: BienRepository; cfeRepo: DeclarationCfeRepository; }`.
    - Export `registerBiensCfeRoutes(app: FastifyInstance, deps: CfeRouteDeps): void`.
    - **GET `/biens/:id/cfe/nouvelle`** : charge `bien` -> render `pages/biens/cfe/nouvelle.ejs` avec `millesime` optionnellement venant de `req.query.millesime` (sinon `clock.aujourdhui().year`).
    - **POST `/biens/:id/cfe`** :
      1. Zod `enregistrerCfeSchema.safeParse(req.body)` -> si erreur, redirect form avec `req.session.formErrors`.
      2. Convertir `montantAvisEuros` -> `Money.fromEuros(parsed.montantAvisEuros)` (ou null).
      3. Convertir `dateEcheancePaiement` (ISO string) -> `Temporal.PlainDate.from(...)`.
      4. `await enregistrerDeclarationCfe({ bienId, ...converted }, { bienRepo, cfeRepo })`.
      5. Catch `InvariantViolated` -> re-render formulaire avec message d'erreur.
      6. `req.session.banniereSuccess = 'Déclaration CFE ' + millesime + ' enregistrée.'` (auto-dismiss 5s pattern Phase 5).
      7. Redirect `/biens/:id`.
    - **GET `/biens/:id/cfe/:cfeId/editer`** : charge `bien` + `decl` via `cfeRepo.trouverParId` -> render `editer.ejs` avec valeurs pré-remplies. 404 si déclaration introuvable.
    - **POST `/biens/:id/cfe/:cfeId/modifier`** : Zod `modifierCfeSchema.safeParse` -> `modifierDeclarationCfe({ id, patch })` -> redirect avec bannière "Déclaration CFE {millésime} mise à jour.".

    **`src/web/views/pages/biens/detail.ejs` — étendre** (ajouter section CFE après section Fiscalité existante, UI-SPEC §S8) :
    - `<section aria-label="Déclarations CFE — <%= bien.adresse.enLigne() %>">`.
    - `<h2>Cotisation Foncière des Entreprises (CFE)</h2>`.
    - Si `declarationsCfe.length === 0` : `<%- include('../../partials/empty-state', { heading: 'Cotisation Foncière des Entreprises (CFE) — ' + (locals.millesimeCourant ?? new Date().getFullYear()), body: 'Aucune déclaration CFE enregistrée pour ce bien sur l\\'année. La CFE est due par tout LMNP (CGI art. 1447) — votre première année d\\'activité est exonérée ; vous recevrez ensuite un avis de votre commune en octobre/novembre.', ctaUrl: '/biens/' + bien.id + '/cfe/nouvelle', ctaLabel: 'Enregistrer une déclaration CFE' }) %>` (copywriting UI-SPEC exact).
    - Sinon : liste verticale `declarationsCfe.forEach(d => include('partial-carte-cfe', { declarationCfe: d, bienId: bien.id }))` + CTA secondary "Enregistrer une nouvelle déclaration CFE".
    - **Charger côté route biens.ts `/biens/:id`** : ajouter `declarationsCfe = await listerDeclarationsCfeParBien({ bienId: bien.id }, { cfeRepo })` et passer aux locals.

    **`src/main.ts` — DI** :
    - Instancier `cfeRepo = new DeclarationCfeRepositorySqlite(db)` (singleton).
    - Wire routes : `await app.register(async (instance) => registerBiensCfeRoutes(instance, { bienRepo, cfeRepo }))`.
    - Passer `cfeRepo` aux opts de la route biens.ts existante pour charger les `declarationsCfe` sur `/biens/:id`.

    **Tests intégration `tests/integration/web/route-cfe.test.ts`** :
    - DB éphémère + Bailleur + Bien via builders.
    - Cas POST création réussi (200 + redirect + flash session).
    - Cas POST invariant violé (statut='deposee' + dateDepot null) -> form ré-affichée + message d'erreur.
    - Cas GET `/biens/:id` après création -> HTML contient `<section aria-label="Déclarations CFE — ..."` + carte CFE + badge.
    - Cas POST modification réussie (statut deposee -> payee avec montant) -> upsert OK, redirect, flash bannière "mise à jour".

    **Décisions implémentées :**
    - D-CFE6.1, D-CFE6.2, D-CFE6.3, D-CFE6.4 -> formulaire complet, pas de reproduction 1447-C-SD case-par-case, pas de calcul base imposable.
    - UI-SPEC §S8 (carte CFE), §S9 (formulaire + aide), §Copywriting (libellés exacts), §Accessibilité (WCAG 1.4.1 + 2.5.5 + ARIA).
    - D-15 -> Zod uniquement `web/schemas/cfe-schemas.ts`.
    - V13 ASVS -> CSRF câblé via `@fastify/csrf-protection` (déjà en place).

    **À ne PAS faire :**
    - Pas de banner J-30 (= Plan 06-07).
    - Pas de calcul automatique `dateEcheancePaiement = 15/12/millesime` (champ utilisateur — D-CFE6.3).
    - Pas de calcul de base imposable / VLF (anti-pattern §9 RESEARCH, R4.3 — vérifier `grep -rE "calculer.*base|valeur.*locative" src/web/` -> 0).
    - Pas de Zod dans le use case ou le domaine (D-15).
    - Pas de nouveau token CSS — réutiliser strictement `--couleur-warning`, `--couleur-success`, `--couleur-neutre-bg`, `--couleur-accent` (UI-SPEC §Design System verrouillé).
    - Pas de JSON API exposée — vues HTML rendues serveur uniquement (cohérence Phase 5).
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test -- --run tests/integration/web/route-cfe.test.ts && grep -En "aria-label=\"Déclarations CFE" src/web/views/pages/biens/detail.ejs | wc -l | awk '{exit ($1 -ge 1) ? 0 : 1}' && grep -En "CGI art. 1478" src/web/views/partials/partial-aide-cfe.ejs | wc -l | awk '{exit ($1 -ge 1) ? 0 : 1}' && grep -rEn "calculer.*base|valeur.*locative" src/web/views/partials/partial-aide-cfe.ejs src/web/views/pages/biens/cfe/ src/web/routes/biens/cfe.ts | grep -v '^#' | wc -l | awk '{exit ($1 == 0) ? 0 : 1}' && pnpm lint:deps</automated>
  </verify>
  <done>
    - `pnpm test -- --run tests/integration/web/route-cfe.test.ts` exit 0 (au moins 4 cas dont POST création, POST invariant, GET fiche Bien avec CFE, POST modification upsert).
    - `grep "aria-label=\"Déclarations CFE"` dans `detail.ejs` -> au moins 1 résultat (UI-SPEC §Accessibilité).
    - `grep "CGI art. 1478"` dans `partial-aide-cfe.ejs` -> au moins 1 résultat (D-CFE6.4 copywriting).
    - `grep "calculer.*base|valeur.*locative"` dans tous les fichiers web Phase 6 -> 0 résultat (R4.3 anti-pattern §9).
    - `pnpm lint:deps` exit 0.
    - 2 commits : `feat(06-06): routes biens/cfe + schemas Zod + vues nouvelle/editer + partials S8/S9` + `feat(06-06): DI DeclarationCfeRepositorySqlite + integration section CFE sur fiche Bien`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Navigateur -> Fastify POST `/biens/:id/cfe` et `/biens/:id/cfe/:cfeId/modifier` | Inputs untrusted (form data) -> validation Zod (`enregistrerCfeSchema`, `modifierCfeSchema`) côté route + invariants `DeclarationCfe.creer` côté domaine. CSRF token obligatoire (V13 ASVS via `@fastify/csrf-protection`). |
| Navigateur -> Fastify GET `/biens/:id/cfe/nouvelle` et `/biens/:id/cfe/:cfeId/editer` | `req.params.id` + `req.params.cfeId` traités comme opaques (cast brand type) -> repo `trouverParId` paramétré Kysely (pas d'injection SQL). |
| EJS render -> réponse HTML | XSS : `bien.adresse.enLigne()`, `formaterStatutCfe(statut)`, `formatMoney`, `formatDate` -> auto-échappés par EJS `<%= %>`. Les partials utilisent `<%- include(...) %>` mais l'output des helpers est text-only. |
| Aide pédagogique `partial-aide-cfe.ejs` -> lien externe impots.gouv.fr | `target="_blank" rel="noopener noreferrer"` obligatoire (V9 + V13). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-CFE6-01 | Tampering (mass assignment) | POST `/biens/:id/cfe` accepte `id` ou `bienId` injecté dans le body | mitigate | Zod schema ne déclare PAS `id` ni `bienId` -> `safeParse` filtre les champs inattendus (`z.object` strict). L'id est généré côté domaine via `nouveauDeclarationCfeId()`. Le `bienId` vient de `req.params.id`, jamais du body. |
| T-06-CFE6-02 | Tampering (statut illégitime) | Body POST envoie un statut hors enum | mitigate | Zod `z.enum([5 valeurs])` rejette + invariant `STATUTS_CFE_VALIDES.includes(statut)` côté `DeclarationCfe.creer`. Défense en profondeur. |
| T-06-CFE6-03 | Tampering (millésime aberrant) | Body POST envoie millésime 9999 ou -1 | mitigate | Zod `min(2020).max(2030)` rejette + invariant côté domaine `[2020, 2030]`. |
| T-06-CFE6-04 | Information Disclosure | Message d'erreur `DeclarationCfeIntrouvable` cite l'ID brut | accept | Single-user local-first -> aucun PII exposé. `id` est un UUID v4 opaque, pas de risque d'énumération (V8 ASVS partial). |
| T-06-CFE6-05 | Tampering (cross-bien access) | POST `/biens/:bienA/cfe/:cfeIdDeBienB/modifier` -> modification croisée | mitigate | Use case `modifierDeclarationCfe` charge `existante` via `cfeRepo.trouverParId(cfeId)`. La route Task 3 doit vérifier `existante.bienId === req.params.id as BienId` AVANT de patcher. Documenté explicitement dans route. (Note : single-user V1 atténue, mais V4 ASVS partial restaure le contrat.) |
| T-06-CFE6-06 | Information Disclosure | Lien externe impots.gouv.fr fuite Referer | mitigate | `rel="noopener noreferrer"` sur les liens externes (UI-SPEC §S9 + partial-aide-cfe). |
| T-06-CFE6-07 | Hexagonal violation | Import infra (Zod/Kysely/Fastify) dans `src/domain/fiscalite/cfe/` | mitigate | `pnpm lint:deps` (dependency-cruiser) exit 0 obligatoire en pre-commit + verify gate (Phase 5.1 hardening complete). |
| T-06-CFE6-08 | Fiscal misinformation (R4.3) | Calcul d'une base imposable CFE erroné affiché à l'utilisateur | mitigate | **Anti-pattern §9 RESEARCH absolu** : aucun fichier Phase 6 ne contient `calculerBase*` ni `valeurLocative*` (vérifié par `grep -rE` dans verify). L'aide pédagogique cite explicitement "la base d'imposition est calculée par votre Service des Impôts des Entreprises (SIE), pas par cette application" (D-CFE6.4, UI-SPEC §S9). |
| T-06-CFE6-09 | Repudiation (audit trail CFE) | Modification statut sans trace | accept | V1 LMNP single-user audit-friendly -> historique non requis ROADMAP. Phase 7 (DAS-02) couvre l'historique consolidé si besoin. |
</threat_model>

<verification>
- `pnpm typecheck` exit 0.
- `pnpm test -- --run` tous verts (au moins 8 tests unit DeclarationCfe + 4 cas integration repo SQLite + 4 cas integration route).
- `pnpm test:bdd -- --tags "@phase6-cfe-suivi"` 6 scénarios verts.
- `pnpm lint:deps` exit 0 (hexagonal strict).
- `grep -rE "from .+(pdfmake|kysely|fastify|zod|fs|path)" src/domain/fiscalite/cfe/` -> 0 résultat.
- `grep -rE "calculer.*base|valeur.*locative" src/domain/fiscalite/cfe/ src/application/fiscalite/enregistrer-declaration-cfe.ts src/application/fiscalite/modifier-declaration-cfe.ts src/web/views/pages/biens/cfe/ src/web/views/partials/partial-aide-cfe.ejs src/web/routes/biens/cfe.ts` -> 0 résultat (R4.3 absolu).
- Migration `0023_phase6_declaration_cfe.sql` créée et appliquée par `pnpm migrate`.
- Manuel : visiter `/biens/:id` -> section "Cotisation Foncière des Entreprises (CFE)" visible (empty-state ou liste). Cliquer "Enregistrer une déclaration CFE" -> formulaire avec aide pédagogique citant CGI art. 1478. Soumettre statut "Non déposée" -> redirect + bannière success + carte visible.
</verification>

<success_criteria>
- ROADMAP success criterion 3 (FIS-06) partiellement couvert : suivi déclaratif CFE 1447-C-SD opérationnel (statuts, dates, montant). **Alerte décembre = Plan 06-07** complète FIS-06.
- 100% couverture sur `src/domain/fiscalite/cfe/declaration-cfe.ts` + `statut-cfe.ts` + use cases application (BDD_PRACTICES.md §7).
- Décisions D-CFE6.1, D-CFE6.2, D-CFE6.3, D-CFE6.4 entièrement couvertes (D-CFE6.5 = Plan 06-07).
- `dependency-cruiser` exit 0 (zéro import infra dans `src/domain/fiscalite/cfe/`).
- WCAG 2.1 AA respecté sur section CFE (sélecteurs ARIA UI-SPEC §S8/§S9, touch target 44px, contrast tokens).
</success_criteria>

<output>
After completion, create `.planning/phases/06-liasse-2031-cfe/06-06-SUMMARY.md` listant :
- Migration 0023 appliquée, table `declarations_cfe` schéma final.
- Décisions de mapping prises pour les 5 libellés statut (LIBELLES_STATUT_CFE).
- Comportement upsert UNIQUE (bien_id, millesime) vérifié par tests intégration.
- Capture ASCII de la section CFE sur fiche Bien (empty state + liste avec carte + badge).
- Confirmation `grep` 0 résultat pour `calculer.*base|valeur.*locative` (R4.3 verrouillé).
- Backlog pour Plan 06-07 : banner J-30 dépend de `listerDeclarationsCfeParBien` et `cfeRepo` injectables.
</output>
