---
phase: 06-liasse-2031-cfe
plan: 02
type: tdd
wave: 2
depends_on: [06-01]
files_modified:
  - src/domain/fiscalite/liasse/mapping-liasse-2026.ts
  - src/application/fiscalite/generer-brouillon-liasse.ts
  - src/web/views/pages/fiscalite/brouillon-liasse.ejs
  - src/web/views/pages/fiscalite/index.ejs
  - tests/_builders/fiscalite.ts
  - tests/bdd/features/brouillon-liasse-micro.feature
  - tests/bdd/step_definitions/brouillon-liasse.steps.ts
  - tests/unit/fiscalite/generer-brouillon-liasse.test.ts
autonomous: true
requirements: [FIS-05]
tags: [fiscalite, liasse, micro-bic, 2042-c-pro]

must_haves:
  truths:
    - "L'utilisateur peut consulter la vue brouillon-liasse pour une DeclarationAnnuelle clôturée en régime micro-BIC."
    - "En régime micro, seule la section 2042 C PRO est rendue (pas de 2031-SD ni d'annexes 2033) — D-L6.2."
    - "La case 2042 C PRO `5NI` (location meublée longue durée) porte les recettes brutes (decl.recettesTotales) — pas le net après abattement."
    - "Le bandeau S1 'Brouillon — à reporter' est aussi affiché en régime micro."
    - "Le bloc 'Brouillons de liasse' sur /fiscalite inclut désormais les déclarations clôturées micro-BIC (Wave 1 les filtrait)."
  artifacts:
    - path: "src/application/fiscalite/generer-brouillon-liasse.ts"
      provides: "Use case étendu : supporte 'micro_bic' (plus de throw provisoire Wave 1)"
      contains: "case 'micro_bic'"
    - path: "src/domain/fiscalite/liasse/mapping-liasse-2026.ts"
      provides: "Section 2042-C-PRO peuplée (cases 5NI principalement)"
      contains: "'2042-C-PRO'"
    - path: "tests/bdd/features/brouillon-liasse-micro.feature"
      provides: "3 scénarios couvrant 5NI + absence sections 2033 + bandeau S1"
  key_links:
    - from: "src/web/views/pages/fiscalite/brouillon-liasse.ejs"
      to: "dto.regimeApplique branche micro vs réel"
      via: "ordre des sections itérées"
      pattern: "regimeApplique === 'micro_bic'"
---

<objective>
**Brouillon micro-BIC (FIS-05 — slice 2).** Étendre l'orchestration Wave 1 pour supporter `regimeApplique === 'micro_bic'`. Le brouillon micro est minimaliste : une seule section `2042-C-PRO` avec **les recettes brutes** (D-L6.2, RESEARCH §Cerfa Case Mapping §2042 C PRO). Pas d'abattement 50 % à reporter dans la case (l'abattement est appliqué côté impots.gouv.fr, pas dans l'app — pédagogie R4.3).

**Périmètre :**
- Peupler la section `'2042-C-PRO'` du `mapping-liasse-2026.ts` avec la case `5NI` (location meublée longue durée — confirmé RESEARCH après changement LF 2024).
- Étendre `genererBrouillonLiasse` : si `regimeApplique === 'micro_bic'`, retourner un DTO avec **uniquement** la section 2042-C-PRO.
- Étendre la vue EJS : ordre des sections conditionnel.
- Lever le filtre `regimeApplique==='reel'` sur le bloc "Brouillons de liasse" page `/fiscalite`.

**Purpose** : compléter le périmètre D-L6.2 (couvre micro + réel) sans dupliquer de code.
**Output** : route HTML `/fiscalite/declarations/:id/liasse` rend désormais le brouillon pour les 2 régimes.
</objective>

<execution_context>
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/workflows/execute-plan.md
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-liasse-2031-cfe/06-CONTEXT.md
@.planning/phases/06-liasse-2031-cfe/06-RESEARCH.md
@.planning/phases/06-liasse-2031-cfe/06-PATTERNS.md
@.planning/phases/06-liasse-2031-cfe/06-UI-SPEC.md
@.planning/phases/06-liasse-2031-cfe/06-01-SUMMARY.md
@CLAUDE.md
@LMNP.md

# Artefacts Wave 1 à étendre
@src/domain/fiscalite/liasse/mapping-liasse-2026.ts
@src/domain/fiscalite/liasse/case-liasse.ts
@src/application/fiscalite/generer-brouillon-liasse.ts
@src/web/views/pages/fiscalite/brouillon-liasse.ejs
@src/web/views/pages/fiscalite/index.ejs

# Pattern Phase 5 micro-BIC
@src/application/fiscalite/calculer-micro-bic.ts
@src/domain/fiscalite/declaration-annuelle.ts

<interfaces>
# DeclarationAnnuelle (rappel — Phase 5)
- regimeApplique: 'micro_bic' | 'reel'
- recettesTotales: Money  // recettes brutes (CGI art. 50-0) — utilisées en l'état pour 5NI

# Mapping 2042-C-PRO (à peupler — RESEARCH §Cerfa Case Mapping §2042 C PRO)
# Case `5NI` (déclarant 1) — Location meublée longue durée
# - Source: rendimmo.fr + impots.gouv.fr (LF 2024 a supprimé `5ND`, remplacé par `5NI` pour la longue durée)
# - Valeur: decl.recettesTotales (recettes BRUTES, pas net 50%)
# - V1 single-user mono-déclarant → 5NI uniquement (5OI/5PI = indivision/PAC hors V1)
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: TDD section 2042-C-PRO du mapping + extension use case régime micro</name>
  <files>
    src/domain/fiscalite/liasse/mapping-liasse-2026.ts,
    src/application/fiscalite/generer-brouillon-liasse.ts,
    tests/_builders/fiscalite.ts,
    tests/bdd/features/brouillon-liasse-micro.feature,
    tests/bdd/step_definitions/brouillon-liasse.steps.ts,
    tests/unit/fiscalite/generer-brouillon-liasse.test.ts
  </files>
  <read_first>
    src/domain/fiscalite/liasse/mapping-liasse-2026.ts (état Wave 1),
    src/application/fiscalite/generer-brouillon-liasse.ts (état Wave 1),
    src/application/fiscalite/calculer-micro-bic.ts (rappel modèle micro),
    .planning/phases/06-liasse-2031-cfe/06-RESEARCH.md (§Cerfa Case Mapping §2042 C PRO — confirmer `5NI` post-LF 2024),
    .planning/phases/06-liasse-2031-cfe/06-CONTEXT.md (D-L6.2),
    .planning/phases/06-liasse-2031-cfe/06-UI-SPEC.md (§S1 — bandeau aussi en micro),
    https://www.impots.gouv.fr (formulaire 2042 C PRO millésime 2026 si accessible — sinon RESEARCH.md fait foi).
  </read_first>
  <behavior>
    **TDD RED → GREEN.**

    **RED — `tests/bdd/features/brouillon-liasse-micro.feature` (3 scénarios @phase6 @phase6-liasse-micro) :**
    - Scénario 1 : `Etant donné une DeclarationAnnuelle clôturée en régime micro-BIC avec recettes 18000 €` `Quand on génère le brouillon liasse` `Alors le brouillon contient la section "2042 C PRO"` `Et une case "5NI" porte la valeur "18000,00 €"` `Et le brouillon NE contient PAS de section "2031-SD"` `Et le brouillon NE contient PAS de section "Annexe 2033-A"`.
    - Scénario 2 : `Etant donné une DeclarationAnnuelle clôturée en régime micro-BIC avec recettes 18000 €` `Quand on visite la page brouillon liasse` `Alors la page affiche le bandeau "Brouillon liasse fiscale 2026"` `Et la valeur affichée pour 5NI est la recette brute (pas la valeur après abattement 50%)`.
    - Scénario 3 : `Etant donné 2 DeclarationAnnuelle clôturées (une micro et une réel) sur le bailleur` `Quand on visite la page /fiscalite` `Alors le bloc "Brouillons de liasse" liste les 2 déclarations`.

    **RED — extension `tests/unit/fiscalite/generer-brouillon-liasse.test.ts` :**
    - Test : `genererBrouillonLiasse` avec `decl.regimeApplique='micro_bic'` retourne `dto.regimeApplique='micro_bic'` + `dto.sections.length === 1` + `dto.sections[0].annexe === '2042-C-PRO'`.
    - Test : la case `5NI` dans la section 2042-C-PRO porte `valeur === decl.recettesTotales` (recettes brutes, **pas** `recettesTotales.multiplyByFraction(50, 100)`).
    - Test : pour micro, aucune section `'2031-SD'` ni `'2033-*'` ne figure dans `dto.sections`.

    **GREEN — `src/domain/fiscalite/liasse/mapping-liasse-2026.ts`** :
    - Peupler la section `'2042-C-PRO'` avec **au minimum** : `{ caseId: '5NI', libelleOfficiel: 'Locations meublées non professionnelles — Régime micro-BIC — Location meublée classique (longue durée)', annexe: '2042-C-PRO', source: 'recettesTotales' }`. **Le libellé OFFICIEL** est celui du cerfa 2042 C PRO millésime 2026. À vérifier sur le PDF — wording exact peut différer.
    - **Optionnel** : ajouter aussi `5NA` / `5NY` (régime réel — bénéfice / déficit reportés sur 2042) avec `source: 'beneficeFiscal'` / `'deficitFiscal'`. Si ajouté, mettre à jour Plan 01 doc OU laisser pour Plan 02 : ces cases relient la 2031-SD au 2042 C PRO en régime réel. Décision : **inclure dans Plan 02** car le périmètre 2042 vit ici.

    **GREEN — `src/application/fiscalite/generer-brouillon-liasse.ts`** :
    - Supprimer le throw provisoire Wave 1 `Régime micro-BIC : brouillon disponible Plan 02`.
    - Brancher sur `decl.regimeApplique` :
      - `'reel'` → comportement Wave 1 (sections 2031-SD + 2033-A/B/C/D + éventuellement 2042-C-PRO si cases `5NA`/`5NY` mappées).
      - `'micro_bic'` → ne construire QUE la section 2042-C-PRO avec les cases mappées `source ∈ {'recettesTotales'}` (ou `5OI`/`5PI` si mappées, hors V1 single-user).
    - **`resoudreValeurCase`** pour `source: 'recettesTotales'` retourne `decl.recettesTotales` — **PAS de calcul d'abattement** (R4.3 pédagogie : l'app affiche le brut, impots.gouv.fr applique l'abattement).

    **GREEN — `src/web/views/pages/fiscalite/brouillon-liasse.ejs`** :
    - L'itération `dto.sections.forEach(s => include('partial-tableau-liasse-section', { section:s, exercice }))` ne change pas — l'ordre est porté par le DTO (le use case construit les sections dans l'ordre cerfa). Vérifier que la vue ne suppose pas une longueur fixe.
    - Pas de bandeau S3 (postes manuels) pour la section 2042-C-PRO — `bandeauPostesManuels: false`.

    **GREEN — `src/web/views/pages/fiscalite/index.ejs`** :
    - Lever le filtre `regimeApplique === 'reel'` sur le bloc "Brouillons de liasse". Lister TOUTES les déclarations clôturées du bailleur.
  </behavior>
  <action>
    Périmètre minimal pour fermer la slice 2. **Décisions implémentées** :
    - D-L6.2 → micro-BIC supporté.
    - R4.3 (pédagogie sans fausse précision) → recettes BRUTES affichées en 5NI, pas le net.
    - LF 2024 (vérifié dans RESEARCH) → case `5NI` (et non `5ND` qui est supprimée).

    **Validation cerfa millésime 2026** : le case `5NI` doit être confirmé sur le PDF 2042 C PRO. Si le PDF officiel n'est pas accessible et que la confiance reste MEDIUM, conserver `5NI` (RESEARCH cite rendimmo.fr comme source fiable post-LF 2024). Documenter dans le SUMMARY.

    **À ne PAS faire :**
    - Pas de calcul d'abattement 50 % côté app (anti-pattern §3 + R4.3 + pitfall §2 RESEARCH).
    - Pas de section 2031-SD/2033 en régime micro.
    - Pas de `5OI`/`5PI` (indivision / personne à charge — hors V1 single-user, RESEARCH §Cerfa Case Mapping).
    - Pas de fenced code blocks dans `<action>`.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test -- --run tests/unit/fiscalite/generer-brouillon-liasse.test.ts && pnpm test:bdd -- --tags "@phase6-liasse-micro" && pnpm test:bdd -- --tags "@phase6-liasse-reel"</automated>
  </verify>
  <done>
    - `pnpm test:bdd -- --tags "@phase6-liasse-micro"` 3 scénarios verts.
    - `pnpm test:bdd -- --tags "@phase6-liasse-reel"` reste vert (régression).
    - `pnpm test -- --run tests/unit/fiscalite/generer-brouillon-liasse.test.ts` exit 0 (Wave 1 tests + ≥ 3 nouveaux).
    - `grep -n "5NI" src/domain/fiscalite/liasse/mapping-liasse-2026.ts` retourne ≥ 1 résultat.
    - `grep -n "multiplyByFraction(50" src/application/fiscalite/generer-brouillon-liasse.ts` retourne 0 résultat (pas d'abattement côté app).
    - 2 commits : `test(06-02): scenarios micro-BIC (RED)` + `feat(06-02): brouillon micro-BIC 2042 C PRO case 5NI`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reuse Wave 1 boundaries — pas de nouveau point d'entrée HTTP. | — |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-LIASSE-W2-MICRO-01 | Tampering (intégrité fiscale) | Tentation d'afficher les recettes nettes (après abattement 50 %) au lieu des brutes en case `5NI` | mitigate | Test unitaire explicit : `expect(dto.sections[0].cases[0].valeur).toEqual(decl.recettesTotales)` (PAS `multiplyByFraction(50,100)`). R4.3 pédagogie + cerfa officiel impose recettes brutes. |
| T-06-LIASSE-W2-MICRO-02 | Information Disclosure | Aucune nouvelle exposition de donnée — même surface que Wave 1. | accept | — |
</threat_model>

<verification>
- `pnpm typecheck` exit 0.
- `pnpm test && pnpm test:bdd` tous verts.
- Manuel : visiter `/fiscalite/declarations/{id}/liasse` pour une déclaration micro clôturée → section unique 2042 C PRO + recettes brutes affichées.
</verification>

<success_criteria>
- D-L6.2 entièrement couvert (réel + micro).
- ROADMAP success criterion 1 (FIS-05) : brouillon disponible pour les deux régimes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-liasse-2031-cfe/06-02-SUMMARY.md` notant le code `5NI` validé (ou `5ND` si différent), la copie officielle utilisée, l'absence de calcul d'abattement côté app, screenshot/ASCII de la vue micro.
</output>
</content>
</invoke>