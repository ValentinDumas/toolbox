---
phase: 05
plan: 05
plan_id: "05-05"
type: execute
wave: 5
depends_on: ["05-01", "05-02"]
files_modified:
  - src/application/fiscalite/detecter-bascule-lmp.ts
  - src/application/fiscalite/saisir-revenus-foyer.ts
  - src/web/schemas/fiscalite-schemas.ts
  - src/web/routes/fiscalite/revenus-foyer.ts
  - src/web/views/pages/fiscalite/revenus-foyer.ejs
  - src/web/views/partials/partial-verdict-fiscal.ejs
  - src/main.ts
  - tests/unit/fiscalite/detecter-bascule-lmp.test.ts
  - tests/unit/fiscalite/saisir-revenus-foyer.test.ts
  - tests/bdd/features/fiscalite-lmp-detection.feature
  - tests/bdd/step_definitions/fiscalite-lmp.steps.ts
autonomous: true
requirements: ["FIS-01"]
user_setup: []

mvp_split_rationale: |
  Vertical slice FIS-01 (détection LMP) : VO tri-état StatutLmnpLmp +
  use case pur detecter-bascule-lmp + écran saisie revenus foyer (G3.1) +
  partial bandeau verdict (S7). L'utilisateur peut saisir ses revenus actifs
  du foyer, voir le verdict tri-état (LMNP / Indéterminé / LMP probable),
  et le bandeau s'affiche sur les pages fiscales du contexte. Le snapshot
  annuel figé dans DeclarationAnnuelle.revenusFoyerSnapshot vient Plan 06
  (clôture). Plan léger — pure logique tri-état + 1 formulaire + 1 partial.

must_haves:
  truths:
    - "Le use case pur `detecterBasculeLmp({ recettes: Money, revenusFoyer: Money | null }, regles: RegleFiscale2026): VerdictLmp` retourne un état tri-valeur : (a) `'lmnp_confirme'` si recettes ≤ SEUIL_LMP_RECETTES (Money 23 000 €) ; (b) `'lmp_probable'` si recettes > SEUIL ET revenusFoyer !== null ET recettes > revenusFoyer (les deux critères STRICTS — CGI 155 IV) ; (c) `'lmnp_confirme'` si recettes > SEUIL ET revenusFoyer !== null ET recettes ≤ revenusFoyer ; (d) `'indetermine_revenus_foyer_manquants'` si recettes > SEUIL ET revenusFoyer === null — D-FIS-G3.3, D-FIS-G3.4."
    - "Cas limites obligatoires CONTEXT.md L245-247 : (a) recettes = Money.fromCentimes(2_299_999n) → 'lmnp_confirme' (juste sous seuil) ; (b) recettes = Money.fromCentimes(2_300_000n) (exactement 23 000 € — non-strict supérieur) → 'lmnp_confirme' ; (c) recettes = Money.fromCentimes(2_300_001n) ET revenusFoyer = 2_300_001n → 'lmnp_confirme' (égalité = critère b strict non rempli) ; (d) recettes = 2_400_000n ET revenusFoyer = 2_300_000n → 'lmp_probable' (les deux critères stricts remplis) ; (e) recettes = 2_400_000n ET revenusFoyer = null → 'indetermine_revenus_foyer_manquants'."
    - "Anti-sticky LMP : Plan N+1 réévalue les recettes et revenus de N+1 indépendamment ; aucune mémoire de l'état N (D-FIS-G3.4 — pas de base légale sticky depuis Conseil Constitutionnel n° 2009-587 DC). Pas d'agrégat ni de read-model de 'verdict cumulé'. Toute persistance de verdict se fait via snapshot dans DeclarationAnnuelle (Plan 06)."
    - "Le type exporté `VerdictLmp` est une union : `'lmnp_confirme' | 'lmp_probable' | 'indetermine_revenus_foyer_manquants'`. Le label français pour chaque valeur est dans LABELS_VERDICT_LMP : 'LMNP confirmé' / 'LMP probable' / 'Indéterminé (revenus foyer manquants)'."
    - "Use case `saisirRevenusFoyer(commande: { revenusActifsAnnuelsCourantEuros: number }, bailleurRepo, clock): Promise<void>` : (1) lookup bailleur singleton via bailleurRepo.trouver() → throw BailleurAbsent si null ; (2) bailleur.modifier({ revenusActifsAnnuelsCourant: Money.fromEuros(input) }) ; (3) si bailleur.fiscalitePremierAcces === null → modifier({ fiscalitePremierAcces: PlainDateTime.from(clock.aujourdhui()) }) ; (4) enregistrer. Pas de validation 'cohérence avec recettes' — c'est le caller (cloturer-exercice Plan 06) qui détecte le verdict."
    - "L'écran GET /fiscalite/revenus-foyer affiche le formulaire G3.1 : <input type=number step=0.01 name=revenusActifsAnnuelsCourantEuros> pré-rempli avec bailleur.revenusActifsAnnuelsCourant ; tooltip BOFIP-BIC-CHAMP-40-20 exhaustif (UI-SPEC §Copywriting L242 inclus salaires/BNC/BA/BIC autres/traitements/gérance ; exclus revenus fonciers/mobiliers/PV/BIC LMNP objet déclaration) ; bouton 'Enregistrer'. POST /fiscalite/revenus-foyer → use case saisirRevenusFoyer + redirect /fiscalite avec bannière succès."
    - "Le partial `partial-verdict-fiscal.ejs` (UI-SPEC §S7 L156-164) accepte variables `statut: VerdictLmp` + `annee: number` + propriétés optionnelles. Rend un `<section role=\"status\" aria-label=\"Verdict fiscal {annee}\">` avec couleur de fond + bord coloré + icône doublant la couleur (✓ succès / ⚠ warning / ✕ destructive) + texte explicite. Statut 'lmp_probable' utilise `role=\"alert\"` (changement urgent) ; 'lmnp_confirme' et 'indetermine' utilisent `role=\"status\"`. Lien externe vers annuaire des experts-comptables (nouvel onglet, `rel=\"noopener noreferrer\"`) pour statut 'lmp_probable'. Lien interne vers /fiscalite/revenus-foyer pour statut 'indetermine'."
    - "Le bandeau verdict est inclus en tête de page récap fiscal (Plan 06 le wirera) — pour Plan 05, le partial est livré + un endpoint de test GET /fiscalite/verdict?annee=N qui pré-affiche un verdict basé sur recettes calculées (RecettesRepository) + revenusFoyer actuel (sans encore figer dans DeclarationAnnuelle)."
    - "Couverture BDD @fis-01 @phase5 (cas limites L245-247) : (a) recettes 22_999.99 € → lmnp_confirme ; (b) recettes 23_000.00 € exact → lmnp_confirme ; (c) recettes 24_000 € + revenus 24_001 € → lmnp_confirme ; (d) recettes 24_000 € + revenus 23_000 € → lmp_probable ; (e) recettes 24_000 € + revenus null → indetermine_revenus_foyer_manquants ; (f) anti-sticky : 3 exercices (LMNP, LMP, LMNP) → 3 verdicts indépendants sans verrouillage."
  artifacts:
    - path: "src/application/fiscalite/detecter-bascule-lmp.ts"
      provides: "Use case pur tri-état VerdictLmp (CGI art. 155 IV)"
      exports: ["detecterBasculeLmp", "VerdictLmp", "LABELS_VERDICT_LMP"]
    - path: "src/application/fiscalite/saisir-revenus-foyer.ts"
      provides: "Use case enregistre revenusActifsAnnuelsCourant + trace fiscalitePremierAcces"
      exports: ["saisirRevenusFoyer"]
    - path: "src/web/routes/fiscalite/revenus-foyer.ts"
      provides: "Routes GET + POST /fiscalite/revenus-foyer + GET /fiscalite/verdict pré-affichage"
      contains: "revenus-foyer"
    - path: "src/web/views/pages/fiscalite/revenus-foyer.ejs"
      provides: "Formulaire G3.1 saisie revenus avec tooltip BOFIP-BIC-CHAMP-40-20"
      contains: "BOFIP-BIC-CHAMP-40-20"
    - path: "src/web/views/partials/partial-verdict-fiscal.ejs"
      provides: "Bandeau tri-état (S7) — WCAG 1.4.1 couleur + icône + texte"
      contains: "role=\"status\""
  key_links:
    - from: "detecterBasculeLmp use case"
      to: "RecettesRepository (Plan 02) + Bailleur.revenusActifsAnnuelsCourant (Plan 01)"
      via: "appel direct dans Plan 06 cloturer-exercice"
      pattern: "detecterBasculeLmp"
    - from: "S7 partial-verdict-fiscal.ejs"
      to: "Plan 06 récap annuel + dashboard Plan 07"
      via: "include sans dépendance externe — pure rendering"
      pattern: "partial-verdict-fiscal"
    - from: "POST /fiscalite/revenus-foyer"
      to: "Bailleur.modifier (extension Plan 01)"
      via: "saisirRevenusFoyer use case"
      pattern: "saisirRevenusFoyer"

tags: [fiscalite, lmnp, lmp-detection, fis-01, verdict-tri-etat, revenus-foyer, g3]
---

<objective>
Implémenter la détection de bascule LMNP → LMP (CGI art. 155 IV) en tri-état :
LMNP confirmé / Indéterminé / LMP probable. Pose le use case pur + le formulaire
de saisie des revenus du foyer + le partial bandeau verdict. Le bandeau est
ensuite consommé par le récap annuel (Plan 06) et le dashboard (Plan 07).

Purpose : FIS-01. Anti-sticky LMP (D-FIS-G3.4) — chaque exercice est évalué
indépendamment. Aucune base légale pour verrouiller LMP depuis Conseil
Constitutionnel 2009-587 DC.

Output :
  - 1 use case pur detecter-bascule-lmp (tri-état tous cas limites L245-247)
  - 1 use case saisir-revenus-foyer (extension Bailleur)
  - 1 route + 1 page G3.1 avec tooltip BOFIP-BIC-CHAMP-40-20
  - 1 partial verdict S7 (couleur + icône + texte, WCAG)
  - Couverture BDD @fis-01 100 % cas limites
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-CONTEXT.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-PATTERNS.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-UI-SPEC.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-01-PLAN.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-02-PLAN.md

# Sources canoniques fiscales (CITER en JSDoc + tests)
@LMNP.md
@CLAUDE.md
@practices/BDD_PRACTICES.md

# Patterns à répliquer
@src/domain/identite/bailleur.ts
@src/domain/_shared/money.ts
@src/web/routes/bailleur.ts
@src/web/views/pages/baux/formulaire.ejs
@src/web/views/partials/partial-badge-dpe.ejs
@src/application/encaissements/creer-encaissement.ts
</context>

<interfaces>
From src/domain/identite/bailleur.ts (étendu Plan 01) :
```typescript
class Bailleur {
  readonly regimeFiscal: 'micro_bic' | 'reel' | null;
  readonly revenusActifsAnnuelsCourant: Money | null;
  readonly fiscalitePremierAcces: Temporal.PlainDateTime | null;
  static creer(props: BailleurProps): Bailleur;
  modifier(patch: ModifierBailleurPatch): Bailleur;
}
```

From src/domain/fiscalite/regles/regles-2026.ts (Plan 01) :
```typescript
export const REGLES_2026 = {
  SEUIL_LMP_RECETTES: Money,    // 2_300_000n centimes = 23 000 €
  // ...
} as const;
```

From src/domain/_shared/money.ts (essentiel) :
```typescript
class Money {
  superieurA(other: Money): boolean;
  lte(other: Money): boolean;
  egale(other: Money): boolean;
}
```

From src/domain/fiscalite/erreurs.ts (Plan 01) :
```typescript
export class RevenusFoyerManquants extends Error;  // peut être utilisé par caller
```
</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Use case pur detecter-bascule-lmp TDD strict + saisir-revenus-foyer + tests cas limites</name>
  <files>
    src/application/fiscalite/detecter-bascule-lmp.ts,
    src/application/fiscalite/saisir-revenus-foyer.ts,
    tests/unit/fiscalite/detecter-bascule-lmp.test.ts,
    tests/unit/fiscalite/saisir-revenus-foyer.test.ts
  </files>
  <read_first>
    src/domain/fiscalite/regles/regles-2026.ts,
    src/domain/fiscalite/erreurs.ts,
    src/domain/identite/bailleur.ts,
    src/domain/_shared/money.ts,
    src/application/encaissements/creer-encaissement.ts,
    .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-CONTEXT.md
  </read_first>
  <behavior>
    RED → GREEN → REFACTOR.

    Test 1 (RED) — detecter-bascule-lmp.test.ts : recettes 22_999.99 € + revenusFoyer null → 'lmnp_confirme' (sous seuil — revenus pas requis).

    Test 2 (RED) : recettes 22_999.99 € + revenusFoyer 10_000 € → 'lmnp_confirme'.

    Test 3 (RED) : recettes exactement 23_000.00 € (Money.fromCentimes(2_300_000n)) + revenusFoyer null → 'lmnp_confirme' (égalité non strict supérieur).

    Test 4 (RED) : recettes 23_000.01 € (Money.fromCentimes(2_300_001n)) + revenusFoyer null → 'indetermine_revenus_foyer_manquants'.

    Test 5 (RED) : recettes 24_000 € + revenusFoyer 24_001 € → 'lmnp_confirme' (recettes ≤ revenusFoyer).

    Test 6 (RED) : recettes 24_000 € + revenusFoyer 24_000 € → 'lmnp_confirme' (égalité — critère b strict non rempli, CONTEXT.md L246 cas obligatoire).

    Test 7 (RED) : recettes 24_000 € + revenusFoyer 23_000 € → 'lmp_probable' (les deux critères stricts remplis).

    Test 8 (RED) : recettes 24_000 € + revenusFoyer null → 'indetermine_revenus_foyer_manquants'.

    Test 9 (RED) — anti-sticky : appel 3x avec inputs N (LMNP), N+1 (LMP), N+2 (LMNP) → 3 verdicts indépendants. Le use case PUR n'a pas d'état entre appels (vérifie par 3 assertions sur le même use case sans setup intermédiaire).

    Test 10 (RED) — saisir-revenus-foyer.test.ts : bailleur singleton sans revenusActifsAnnuelsCourant + saisirRevenusFoyer({ revenusActifsAnnuelsCourantEuros: 48_000 }) → bailleurRepo.enregistrer appelé avec un Bailleur dont revenusActifsAnnuelsCourant.toCentimes() === 4_800_000n et fiscalitePremierAcces set (premier accès — auparavant null).

    Test 11 (RED) : bailleur déjà avec fiscalitePremierAcces non null + saisirRevenusFoyer → fiscalitePremierAcces INCHANGÉ (D-FIS-G5.4 trace UNIQUEMENT le premier accès).

    Test 12 (RED) : bailleur.trouver() retourne null → throw BailleurAbsent (cf. src/domain/identite/erreurs.ts pattern existant Phase 2).

    GREEN — implémenter :
      - src/application/fiscalite/detecter-bascule-lmp.ts : fonction pure exportée + type VerdictLmp + LABELS_VERDICT_LMP.
      - src/application/fiscalite/saisir-revenus-foyer.ts : use case impure (bailleurRepo + clock).

    REFACTOR — JSDoc cite CGI 155 IV + Conseil Constitutionnel n° 2009-587 DC (suppression condition RCS) + D-FIS-G3.3 + G3.4.
  </behavior>
  <action>
    Lire src/application/encaissements/creer-encaissement.ts pour le pattern use
    case impure (signature avec repos + clock + business logic + return). Lire
    src/domain/fiscalite/regles/regles-2026.ts pour SEUIL_LMP_RECETTES.

    Créer src/application/fiscalite/detecter-bascule-lmp.ts :
      - `export type VerdictLmp = 'lmnp_confirme' | 'lmp_probable' | 'indetermine_revenus_foyer_manquants';`
      - `export const LABELS_VERDICT_LMP: Record<VerdictLmp, string> = { lmnp_confirme: 'LMNP confirmé', lmp_probable: 'LMP probable', indetermine_revenus_foyer_manquants: 'Indéterminé (revenus foyer manquants)' };`
      - `export function detecterBasculeLmp(input: { recettes: Money; revenusFoyer: Money | null }, regles: typeof REGLES_2026): VerdictLmp { ... }`
      - Logique : (1) si `!input.recettes.superieurA(regles.SEUIL_LMP_RECETTES)` → 'lmnp_confirme' ; (2) si `input.revenusFoyer === null` → 'indetermine_revenus_foyer_manquants' ; (3) si `input.recettes.superieurA(input.revenusFoyer)` → 'lmp_probable' ; (4) sinon → 'lmnp_confirme'.
      - JSDoc cite CGI art. 155 IV + D-FIS-G3.3 + D-FIS-G3.4 + Conseil Constitutionnel 2009-587 DC.

    Créer src/application/fiscalite/saisir-revenus-foyer.ts :
      - Signature `export async function saisirRevenusFoyer(commande: { revenusActifsAnnuelsCourantEuros: number }, repos: { bailleurRepo: BailleurRepository }, clock: Clock): Promise<void>`
      - (1) const bailleur = await repos.bailleurRepo.trouver() ; if (!bailleur) throw BailleurAbsent (réutiliser erreur existante src/domain/identite/erreurs.ts)
      - (2) const revenusActifs = Money.fromEuros(commande.revenusActifsAnnuelsCourantEuros)
      - (3) const patch: ModifierBailleurPatch = { revenusActifsAnnuelsCourant: revenusActifs } ; if (bailleur.fiscalitePremierAcces === null) patch.fiscalitePremierAcces = Temporal.PlainDateTime.from(clock.aujourdhui().toString() + 'T00:00:00')
      - (4) const bailleurModifie = bailleur.modifier(patch)
      - (5) await repos.bailleurRepo.enregistrer(bailleurModifie)

    Tests unit minimaux mais exhaustifs sur les cas limites L245-247.
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/fiscalite/detecter-bascule-lmp.test.ts tests/unit/fiscalite/saisir-revenus-foyer.test.ts && pnpm lint:deps</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- tests/unit/fiscalite/detecter-bascule-lmp.test.ts` exits 0 — au moins 9 cas verts (5 cas limites obligatoires + anti-sticky).
    - `pnpm test -- tests/unit/fiscalite/saisir-revenus-foyer.test.ts` exits 0 — au moins 3 cas (premier accès trace fiscalitePremierAcces ; accès subséquent ne touche pas fiscalitePremierAcces ; bailleur absent throw BailleurAbsent).
    - `grep -c "VerdictLmp\\|detecterBasculeLmp" src/application/fiscalite/detecter-bascule-lmp.ts` retourne au moins 2.
    - `grep -c "CGI art\\.? 155 IV\\|Conseil Constitutionnel\\|2009-587 DC\\|D-FIS-G3" src/application/fiscalite/detecter-bascule-lmp.ts` retourne au moins 2 (citations doctrinaires).
    - `grep -c "lmnp_confirme\\|lmp_probable\\|indetermine_revenus_foyer_manquants" src/application/fiscalite/detecter-bascule-lmp.ts` retourne au moins 3 (les 3 valeurs).
    - `pnpm lint:deps` exits 0 — pas d'import infra dans src/application/fiscalite/.
  </acceptance_criteria>
  <done>
    Use case pur detecter-bascule-lmp + saisir-revenus-foyer implémentés en TDD.
    100 % couverture cas limites CGI 155 IV. Anti-sticky LMP vérifié.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route + page G3.1 saisie revenus foyer + partial verdict S7 + steps BDD + main.ts wiring</name>
  <files>
    src/web/schemas/fiscalite-schemas.ts,
    src/web/routes/fiscalite/revenus-foyer.ts,
    src/web/views/pages/fiscalite/revenus-foyer.ejs,
    src/web/views/partials/partial-verdict-fiscal.ejs,
    src/main.ts,
    tests/bdd/features/fiscalite-lmp-detection.feature,
    tests/bdd/step_definitions/fiscalite-lmp.steps.ts
  </files>
  <read_first>
    src/web/routes/bailleur.ts,
    src/web/routes/encaissements.ts,
    src/web/schemas/bailleur-schemas.ts,
    src/web/views/pages/baux/formulaire.ejs,
    src/web/views/partials/partial-badge-dpe.ejs,
    src/web/views/partials/form-field.ejs,
    src/main.ts,
    .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-UI-SPEC.md,
    src/application/fiscalite/detecter-bascule-lmp.ts,
    src/application/fiscalite/saisir-revenus-foyer.ts
  </read_first>
  <action>
    Zod schema `saisirRevenusFoyerSchema` = z.object({ revenusActifsAnnuelsCourantEuros: z.coerce.number().min(0, 'Revenus du foyer doit être >= 0') }).

    Route Fastify `src/web/routes/fiscalite/revenus-foyer.ts` :
      - GET /fiscalite/revenus-foyer : lookup bailleur ; render pages/fiscalite/revenus-foyer.ejs avec valeur courante pré-remplie (bailleur.revenusActifsAnnuelsCourant)
      - POST /fiscalite/revenus-foyer : safeParse + appel saisirRevenusFoyer + bannière + redirect /fiscalite
      - GET /fiscalite/verdict?annee={N} : (pré-affichage utile pour développeur / debug) lookup bailleur + appel recettesRepo.sommeRecettesAnnuelles(bailleur.id, annee) + appel detecterBasculeLmp({ recettes, revenusFoyer: bailleur.revenusActifsAnnuelsCourant }, regleFiscale.pour(annee)) ; render template inline ou redirect /fiscalite avec data ; Plan 06 wirera ce verdict dans la page récap annuel via include partial.

    Page G3.1 `pages/fiscalite/revenus-foyer.ejs` (UI-SPEC §S8 étape 2 — réutilisé hors wizard L165-178 + Copywriting L242) :
      - include('layout-debut', { titre: 'Revenus du foyer', breadcrumbs: [{label:'Fiscalité', url:'/fiscalite'},{label:'Revenus du foyer'}], navActive: 'fiscalite' })
      - <h1>Revenus du foyer pour {annee}</h1>
      - <p>Cette information est utilisée pour évaluer le risque de bascule LMNP → LMP (CGI art. 155 IV).</p>
      - <form method="POST" action="/fiscalite/revenus-foyer">
        - <label for="revenusActifsAnnuelsCourantEuros">Revenus du travail et assimilés du foyer (€)</label>
        - <input type="number" step="0.01" min="0" name="revenusActifsAnnuelsCourantEuros" id="revenusActifsAnnuelsCourantEuros" value="{bailleur.revenusActifsAnnuelsCourant?.enEuros() || ''}" required aria-describedby="aide-revenus" >
        - <details id="aide-revenus"><summary>Quoi inclure ? quoi exclure ?</summary>
          <p><strong>Incluez :</strong> salaires nets imposables, BNC, BA, BIC autres que LMNP, traitements et pensions, gains de gérance.</p>
          <p><strong>Excluez :</strong> revenus fonciers, revenus mobiliers, plus-values, BIC LMNP objet de cette déclaration.</p>
          <p>Référence : BOFIP-BIC-CHAMP-40-20.</p>
        </details>
        - <button type="submit">Enregistrer</button>
        - <a href="/fiscalite" class="secondary">Annuler</a>
      </form>
      - include('layout-fin')

    Partial `partial-verdict-fiscal.ejs` (UI-SPEC §S7 L156-164 + pattern 05-PATTERNS.md L611-657) :
      - Variables attendues : statut (VerdictLmp), annee (number)
      - Mapping :
        - lmnp_confirme : bg --couleur-success-bg, fg --couleur-success, icône ✓, label 'Statut LMNP confirmé pour {annee}.', role 'status'
        - indetermine_revenus_foyer_manquants : bg --couleur-warning-bg, fg --couleur-warning, icône ⚠, label 'Statut indéterminé — revenus du foyer non renseignés.', lien interne 'Renseigner maintenant' → /fiscalite/revenus-foyer, role 'status'
        - lmp_probable : bg --couleur-destructive-bg, fg --couleur-destructive, icône ✕, label 'Risque LMP probable — consultez un expert-comptable.', lien externe 'Trouver un expert-comptable' → annuaire EC (target=_blank rel=noopener noreferrer), role 'alert'
      - <section role="..." aria-label="Verdict fiscal {annee}" style="background: {bg}; border-left: 4px solid {fg}; padding: 16px; margin-bottom: 16px;">
          <span aria-hidden="true">{icone}</span>
          <strong>{label}</strong>
          {lien optionnel selon statut}
        </section>
      - WCAG 1.4.1 : couleur doublée par icône (aria-hidden) + texte adjacent visible.

    Wiring src/main.ts :
      - import saisirRevenusFoyer + recettesRepo (déjà wiré Plan 02) + RegleFiscaleProvider (Plan 01)
      - appeler registerFiscaliteRevenusFoyerRoutes(app, { bailleurRepo, recettesRepo, regleFiscale, clock })

    Feature BDD `tests/bdd/features/fiscalite-lmp-detection.feature` (@fis-01 @phase5) :
      - Préambule : 'Etant donné l'application est prête pour la fiscalité LMNP avec clock fixe "2026-12-31"'
      - Scénarios :
        - 'Recettes 22_999.99 € → LMNP confirmé'
        - 'Recettes 23_000.00 € exact → LMNP confirmé (égalité non strict supérieur)'
        - 'Recettes 24_000 € + revenus foyer 24_001 € → LMNP confirmé'
        - 'Recettes 24_000 € + revenus foyer 24_000 € (égalité) → LMNP confirmé'
        - 'Recettes 24_000 € + revenus foyer 23_000 € → LMP probable'
        - 'Recettes 24_000 € sans revenus foyer saisis → Indéterminé'
        - 'Anti-sticky : N LMNP, N+1 LMP, N+2 LMNP — 3 verdicts indépendants'

    Steps `tests/bdd/step_definitions/fiscalite-lmp.steps.ts` :
      - Given 'des recettes annuelles {N} € pour exercice {N}' → fixtures encaissements (Phase 2)
      - Given 'des revenus du foyer {N} € enregistrés' → bailleurRepo.modifier({ revenusActifsAnnuelsCourant: Money.fromEuros(N) })
      - Given 'aucun revenu du foyer renseigné' → bailleurRepo.modifier({ revenusActifsAnnuelsCourant: null })
      - When 'on évalue le verdict LMNP/LMP pour {N}' → appel direct detecterBasculeLmp + recettesRepo.sommeRecettesAnnuelles
      - Then 'le verdict est "{lmnp_confirme|lmp_probable|indetermine_revenus_foyer_manquants}"' → assert
  </action>
  <verify>
    <automated>pnpm test -- tests/bdd && pnpm typecheck && pnpm lint:deps</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier src/web/routes/fiscalite/revenus-foyer.ts existe et expose GET + POST /fiscalite/revenus-foyer + GET /fiscalite/verdict.
    - Le fichier src/web/views/pages/fiscalite/revenus-foyer.ejs existe et contient `<form method="POST"` + tooltip `<details>` + reference 'BOFIP-BIC-CHAMP-40-20'.
    - Le fichier src/web/views/partials/partial-verdict-fiscal.ejs existe et :
      - contient `role="status"` ET `role="alert"` (selon statut)
      - contient `aria-hidden="true"` sur icône (WCAG 1.4.1)
      - contient les 3 mappings de couleur (--couleur-success-bg / --couleur-warning-bg / --couleur-destructive-bg)
      - contient lien externe avec `rel="noopener noreferrer"` pour statut lmp_probable
    - `grep -c "registerFiscaliteRevenusFoyer\\|/fiscalite/revenus-foyer" src/main.ts` retourne au moins 1.
    - `pnpm test -- tests/bdd` exits 0 — scénarios @fis-01 (au moins 6 + anti-sticky) verts.
    - `pnpm typecheck` exits 0.
    - `pnpm lint:deps` exits 0.
    - `grep -c "BOFIP-BIC-CHAMP-40-20" src/web/views/pages/fiscalite/revenus-foyer.ejs` retourne au moins 1.
  </acceptance_criteria>
  <done>
    Route GET/POST /fiscalite/revenus-foyer fonctionnelle. Partial verdict tri-état
    livré et prêt à être consommé par Plan 06 récap annuel. Couverture BDD @fis-01
    100 % (5 cas limites + anti-sticky). Citation doctrinaire BOFIP-BIC-CHAMP-40-20
    présente dans le tooltip d'aide.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| POST /fiscalite/revenus-foyer | Body utilisateur — validation Zod min(0). |
| Money.fromEuros(input) | Bornes JS-safe via Money.toSqliteInteger — accepte jusqu'à 9×10^13 €. |
| Verdict LMP rendering | Lien externe annuaire EC — target=_blank + rel=noopener noreferrer (prévention tabnabbing). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | POST sans CSRF | mitigate | preHandler app.csrfProtection (pattern Phase 2). |
| T-05-05-02 | Information Disclosure | Verdict LMP exposé visuellement | accept | Local-first single-user. Pas d'expo externe. |
| T-05-05-03 | Tampering | Sticky LMP introduit silencieusement | mitigate | Tests anti-sticky (Test 9 + scénario BDD). Le use case est PUR sans état. |
| T-05-05-04 | Tampering | Recommandation prescriptive en cas LMP (anti-pattern §9 CONTEXT.md) | mitigate | Copywriting figé : "Consultez un expert-comptable" — pas "déclarez-vous URSSAF". Aucun call to action concret côté Phase 5. |
| T-05-05-05 | Repudiation | Modification revenus foyer sans trace | accept | V1 single-user — pas de besoin d'audit-trail Bailleur. Le snapshot annuel (DeclarationAnnuelle.revenusFoyerSnapshot Plan 06) trace la valeur figée au moment de la clôture. |
| T-05-05-06 | DoS | POST revenus foyer répété | accept | Endpoint léger (1 UPDATE). Rate limit naturel single-user. |
| T-05-05-07 | Tabnabbing | Lien externe annuaire EC | mitigate | target=_blank + rel="noopener noreferrer" obligatoire. Vérifié dans acceptance_criteria. |
</threat_model>

<verification>
1. `pnpm test -- tests/unit/fiscalite/detecter-bascule-lmp.test.ts tests/unit/fiscalite/saisir-revenus-foyer.test.ts tests/bdd` → tous verts
2. `pnpm typecheck` → 0 erreur
3. `pnpm lint:deps` → 0 erreur
4. Manual : curl GET /fiscalite/revenus-foyer affiche formulaire ; POST sauvegarde + bannière succès
5. Citations : CGI 155 IV, BOFIP-BIC-CHAMP-40-20, Conseil Constitutionnel 2009-587 DC, D-FIS-G3.3, G3.4
</verification>

<success_criteria>
- detecterBasculeLmp use case pur : 100 % couverture cas limites L245-247
- saisirRevenusFoyer trace fiscalitePremierAcces correctement (G5.4)
- Page G3.1 fonctionnelle avec tooltip BOFIP-BIC-CHAMP-40-20
- Partial S7 verdict tri-état conforme WCAG 1.4.1 (couleur + icône + texte)
- Anti-sticky LMP vérifié (3 verdicts indépendants en BDD)
- Lien externe annuaire EC avec rel=noopener noreferrer
- L'utilisateur peut saisir ses revenus foyer et voir le verdict tri-état
</success_criteria>

<output>
Créer .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-05-SUMMARY.md
en suivant @.claude/get-shit-done/templates/summary.md.
</output>
</content>
</invoke>
