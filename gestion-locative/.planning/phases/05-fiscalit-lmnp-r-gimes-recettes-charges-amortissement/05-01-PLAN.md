---
phase: 05
plan: 01
plan_id: "05-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/0014_phase5_qualification_charges.sql
  - migrations/0015_phase5_bailleur_fiscalite.sql
  - migrations/0021_phase5_ticket_nature_fiscale.sql
  - src/infrastructure/db/kysely-types.ts
  - src/domain/_shared/identifiants.ts
  - src/domain/fiscalite/regles/regles-2026.ts
  - src/domain/fiscalite/regles/regle-fiscale-provider.ts
  - src/domain/fiscalite/erreurs.ts
  - src/domain/identite/bailleur.ts
  - src/infrastructure/repositories/bailleur-repository-sqlite.ts
  - src/web/views/partials/sidebar-nav.ejs
  - tests/unit/fiscalite/regles-2026.test.ts
  - tests/unit/identite/bailleur-fiscalite.test.ts
  - tests/integration/repositories/bailleur-repository-fiscalite.test.ts
autonomous: true
requirements: ["FIS-01", "FIS-02", "FIS-03", "FIS-04"]
user_setup: []

mvp_split_rationale: |
  Walking enabler Phase 5 — pose les fondations transverses obligatoires (règles
  fiscales versionnées + extension Bailleur + scaffolding migrations) sans encore
  livrer de capacité utilisateur visible (le slice utilisateur démarre Plan 02).
  Justification de l'exception à la règle "vertical end-to-end" : ces fondations
  sont consommées par TOUS les plans suivants (02..07) — sans elles, aucune slice
  ne peut tester ni boucler. Pattern précédent : Plan 02-01 (Phase 2) avait posé
  Bailleur + Clock + migration 0002 avant tout slice quittancement. Aucun slice
  livrable en isolation ici.

must_haves:
  truths:
    - "Le module domain/fiscalite/regles/regles-2026.ts exporte REGLES_2026 — un objet readonly contenant SEUIL_MICRO_BIC_LONGUE_DUREE (Money 8 360 000n centimes), PLANCHER_ABATTEMENT (Money 30 500n centimes), ABATTEMENT_LONGUE_DUREE_NUM/DEN (1n/2n BigInt — ratio 1/2), SEUIL_LMP_RECETTES (Money 2 300 000n centimes), LF_2025_DATE_EFFET_PV (Temporal.PlainDate 2025-02-15), DUREES_AMORTISSEMENT_ANS (terrain 0, gros_oeuvre 40, toiture_facade 25, installations_techniques 20, agencements_interieurs 15, mobilier 7) — sources citées en JSDoc (CGI art. 50-0, 155 IV, 39, 39 B ; BOFIP-BIC-AMT-20-40 ; LF 2025 art. 84 loi 2025-127) — D-LOCK-1."
    - "Le port RegleFiscaleProvider expose pour(annee: number): RegleFiscale2026 — interface minimale et versionnable ; implémentation par défaut (RegleFiscaleProviderEnMemoire) renvoie REGLES_2026 pour toute année >= 2026 et throw RegleFiscaleAbsente('année %d hors plage versionnée') sinon — pattern aligned avec port Clock (src/domain/_shared/clock.ts:6-43)."
    - "Bailleur est étendu avec regimeFiscal: 'micro_bic' | 'reel' | null (auto-déduit défaut), revenusActifsAnnuelsCourant: Money | null (pré-remplissage wizard G3.1), fiscalitePremierAcces: Temporal.PlainDateTime | null (trace première ouverture écran fiscal G5.4). Aucun invariant nouveau introduit côté domaine — la cohérence (regimeFiscal vs recettes) est portée par DeclarationAnnuelle (Plan 06) — D-LOCK-2 + D-FIS-G3.1 + D-FIS-G5.4."
    - "La migration 0014_phase5_qualification_charges.sql ajoute 4 colonnes à justificatifs (qualification_fiscale TEXT CHECK enum 5 valeurs, qualifie_le TEXT NULL, date_paiement TEXT NULL, parent_justificatif_id TEXT NULL REFERENCES justificatifs(id)) + 2 index partiels (idx_justificatifs_qualification_pending sur qualification_fiscale='non_qualifie' AND corbeille_le IS NULL ; idx_justificatifs_parent sur parent_justificatif_id IS NOT NULL) — appliquée avec succès via pnpm db:migrate — D-FIS-G2.1, G2.5, G2.6, G2.11."
    - "La migration 0015_phase5_bailleur_fiscalite.sql ajoute 3 colonnes à bailleur (regime_fiscal TEXT CHECK enum, revenus_actifs_annuels_courant_centimes INTEGER NULL, fiscalite_premier_acces TEXT NULL — ISO 8601 PlainDateTime) — appliquée avec succès."
    - "La migration 0021_phase5_ticket_nature_fiscale.sql étend tickets_travaux avec colonnes nature TEXT NULL CHECK ('acquisition_mobilier','entretien','amelioration','autre' — D-FIS-G1.2) et nature_fiscale TEXT NULL CHECK enum QualificationFiscale 5 valeurs — D-FIS-G2.3."
    - "kysely-types.ts reflète les 7 nouvelles colonnes (4 sur justificatifs, 3 sur bailleur, 2 sur tickets_travaux). BailleurTable contient regime_fiscal, revenus_actifs_annuels_courant_centimes, fiscalite_premier_acces. JustificatifsTable contient qualification_fiscale, qualifie_le, date_paiement, parent_justificatif_id. TicketsTravauxTable contient nature, nature_fiscale. Le typecheck pnpm typecheck passe sans erreur."
    - "BailleurRepositorySqlite (versDomaine et versRow) gère les 3 nouveaux champs Bailleur, conserve null comme null, et le test d'intégration prouve le round-trip (creer → enregistrer → trouver → propriétés identiques)."
    - "L'entrée Fiscalité (libellé 'Fiscalité', URL /fiscalite) est ajoutée à sidebar-nav.ejs après l'entrée Coffre — navActive='fiscalite' supporté."
    - "Le fichier src/domain/_shared/identifiants.ts exporte les brand types ComposantId, DeclarationAnnuelleId, DeclarationCorrigeeId, AmortissementExerciceId, ValorisationFiscaleId + factories nouveauComposantId(), nouveauDeclarationAnnuelleId(), nouveauDeclarationCorrigeeId(), nouveauAmortissementExerciceId(), nouveauValorisationFiscaleId() — pattern brand UUID v4 (identifiants.ts:69-94)."
    - "Le fichier src/domain/fiscalite/erreurs.ts exporte les classes d'erreur typées : RegleFiscaleAbsente, PrerequisCloturalNonSatisfaits, DeclarationFigeeException, ComposantsSommeIncoherente, RevenusFoyerManquants, JustificatifNonQualifie — toutes dérivent de Error avec name explicite (pattern src/domain/encaissements/erreurs.ts)."
    - "Couverture BDD/unit : tests/unit/fiscalite/regles-2026.test.ts vérifie chaque constante (valeur centimes BigInt, type, source juridique commentée). tests/unit/identite/bailleur-fiscalite.test.ts vérifie creer/modifier accepte regimeFiscal/revenusActifs/fiscalitePremierAcces null+non-null + copy-on-write. tests/integration/repositories/bailleur-repository-fiscalite.test.ts vérifie round-trip SQLite."
  artifacts:
    - path: "migrations/0014_phase5_qualification_charges.sql"
      provides: "ALTER justificatifs : qualification_fiscale (5 valeurs), qualifie_le, date_paiement, parent_justificatif_id + 2 index partiels"
      contains: "ALTER TABLE justificatifs ADD COLUMN qualification_fiscale"
    - path: "migrations/0015_phase5_bailleur_fiscalite.sql"
      provides: "ALTER bailleur : regime_fiscal, revenus_actifs_annuels_courant_centimes, fiscalite_premier_acces"
      contains: "ALTER TABLE bailleur ADD COLUMN regime_fiscal"
    - path: "migrations/0021_phase5_ticket_nature_fiscale.sql"
      provides: "ALTER tickets_travaux : nature (enum étendu acquisition_mobilier) + nature_fiscale (qualification fiscale 5 valeurs)"
      contains: "ALTER TABLE tickets_travaux ADD COLUMN nature"
    - path: "src/domain/fiscalite/regles/regles-2026.ts"
      provides: "Constantes fiscales 2026 versionnées + helpers de calcul (abattementLongueDuree, dureeAmortissement)"
      exports: ["REGLES_2026", "RegleFiscale2026", "TypeComposantBofip"]
    - path: "src/domain/fiscalite/regles/regle-fiscale-provider.ts"
      provides: "Port d'injection versionné par année + impl par défaut RegleFiscaleProviderEnMemoire"
      exports: ["RegleFiscaleProvider", "RegleFiscaleProviderEnMemoire"]
    - path: "src/domain/fiscalite/erreurs.ts"
      provides: "Familles d'erreurs typées Fiscalité (6 classes initiales)"
      exports: ["RegleFiscaleAbsente", "PrerequisCloturalNonSatisfaits", "DeclarationFigeeException", "ComposantsSommeIncoherente", "RevenusFoyerManquants", "JustificatifNonQualifie"]
    - path: "src/domain/identite/bailleur.ts"
      provides: "Bailleur étendu avec regimeFiscal, revenusActifsAnnuelsCourant, fiscalitePremierAcces (3 champs nullables)"
      exports: ["Bailleur"]
    - path: "src/domain/_shared/identifiants.ts"
      provides: "5 brand types Phase 5 + 5 factories"
      exports: ["ComposantId", "DeclarationAnnuelleId", "DeclarationCorrigeeId", "AmortissementExerciceId", "ValorisationFiscaleId", "nouveauComposantId", "nouveauDeclarationAnnuelleId", "nouveauDeclarationCorrigeeId", "nouveauAmortissementExerciceId", "nouveauValorisationFiscaleId"]
    - path: "src/infrastructure/db/kysely-types.ts"
      provides: "Tables Phase 5 mises à jour : justificatifs (+4 cols), bailleur (+3 cols), tickets_travaux (+2 cols)"
      contains: "qualification_fiscale"
    - path: "src/web/views/partials/sidebar-nav.ejs"
      provides: "Entrée 'Fiscalité' (URL /fiscalite) ajoutée après Coffre, navActive='fiscalite' supporté"
      contains: "navActive === 'fiscalite'"
  key_links:
    - from: "RegleFiscaleProvider"
      to: "tous use cases Plans 02..07"
      via: "injection use case (signature: regleFiscale: RegleFiscaleProvider)"
      pattern: "regleFiscale.pour\\("
    - from: "Bailleur (extended)"
      to: "Plans 04 (LMP detect) + 06 (clôture)"
      via: "BailleurRepository.trouver() retourne nouvelles propriétés"
      pattern: "bailleur\\.regimeFiscal|bailleur\\.revenusActifsAnnuelsCourant|bailleur\\.fiscalitePremierAcces"
    - from: "migrations 0014/0015/0021"
      to: "Plans 02 (qualification UI) + 04 (revenus foyer wizard) + 06 (snapshot clôture)"
      via: "ALTER TABLE structures consommées par adapters SQLite Plans 02-06"
      pattern: "qualification_fiscale|revenus_actifs_annuels_courant_centimes|nature_fiscale"

tags: [fiscalite, lmnp, walking-enabler, foundations, regles-versionnees]
---

<objective>
Poser les fondations fiscales transverses Phase 5 : (1) constantes 2026 versionnées
(REGLES_2026 + RegleFiscaleProvider port), (2) extension Bailleur (regime, revenus
foyer courant, premier accès fiscalité), (3) migrations SQL 0014/0015/0021 +
kysely-types mis à jour, (4) brand types identifiants nouveaux agrégats Phase 5,
(5) familles d'erreurs typées Fiscalité, (6) entrée sidebar nav. Aucun slice
utilisateur livré ici — c'est l'enabler nécessaire à tous les plans suivants.

Purpose : R1.1 (surveillance fiscale annuelle) impose des seuils versionnés ;
RegleFiscaleProvider permet d'évoluer vers regles-2027/2028 sans toucher le code
de calcul. L'extension Bailleur (D-LOCK-2 single-bailleur) précède toute logique
fiscale puisque les seuils micro-BIC et LMP s'apprécient au niveau du foyer
(CGI art. 50-0 et 155 IV s'appliquent au contribuable, pas au bien).

Output :
  - 3 migrations SQL appliquées (pnpm db:migrate vert)
  - 1 module règles versionnées + 1 port d'injection
  - 1 module erreurs typées (6 classes)
  - 1 Bailleur étendu round-trippable SQLite
  - 5 brand types identifiants nouveaux agrégats
  - 1 entrée sidebar Fiscalité prête à recevoir les routes des plans suivants
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-CONTEXT.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-PATTERNS.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-UI-SPEC.md

# Sources canoniques fiscales (CITER en JSDoc)
@CLAUDE.md
@LMNP.md
@RISKS.md
@practices/DDD.md
@practices/SOFTWARE_CRAFTSMANSHIP.md
@practices/BDD_PRACTICES.md

# Patterns à répliquer (interface contracts)
@src/domain/_shared/money.ts
@src/domain/_shared/clock.ts
@src/domain/_shared/identifiants.ts
@src/domain/_shared/erreurs.ts
@src/domain/identite/bailleur.ts
@src/domain/encaissements/erreurs.ts
@src/domain/locatif/bail-indexation.ts
@src/infrastructure/db/kysely-types.ts
@src/infrastructure/repositories/bailleur-repository-sqlite.ts
@migrations/0009_phase3_bail_indexations.sql
@migrations/0010_phase4_documents_travaux.sql
@src/web/views/partials/sidebar-nav.ejs
</context>

<interfaces>
<!-- Patterns clés à respecter. -->

From src/domain/_shared/money.ts:
```typescript
class Money {
  static fromCentimes(n: bigint): Money;     // refuse n < 0
  static fromEuros(n: number): Money;
  static zero(): Money;
  toSqliteInteger(): number;                  // assert overflow MAX_SAFE_INTEGER
  multiplyByFraction(num: bigint, den: bigint, mode?: 'banker'|'floor'|'ceil'): Money;
  multiplyByRatio(num: bigint, den: bigint, mode?: 'banker'|'floor'|'ceil'): Money;
}
```

From src/domain/_shared/clock.ts (pattern port à répliquer pour RegleFiscaleProvider) :
```typescript
export interface Clock { aujourdhui(): Temporal.PlainDate; }
export class ClockSysteme implements Clock { /* prod */ }
export class ClockFixe implements Clock { static du(iso: string): ClockFixe; /* tests */ }
```

From src/domain/identite/bailleur.ts (à étendre — préserver pattern copy-on-write):
```typescript
export class Bailleur {
  readonly id: BailleurId;
  readonly nomComplet: string;
  readonly adresse: Adresse;
  static creer(props: BailleurProps): Bailleur;
  modifier(patch: ModifierBailleurPatch): Bailleur;
}
```

From src/domain/_shared/identifiants.ts (pattern brand UUID v4 à répliquer):
```typescript
export type EncaissementId = string & { readonly __brand: 'EncaissementId' };
export function nouveauEncaissementId(): EncaissementId {
  return crypto.randomUUID() as EncaissementId;
}
```

From src/domain/encaissements/erreurs.ts (pattern classes typées) — répliquer la
forme `export class X extends Error { constructor(...) { super('...'); this.name = 'X'; } }`.

From src/infrastructure/db/kysely-types.ts:JustificatifsTable et BailleurTable —
**à étendre, ne pas reconstruire**.

From migrations/0010_phase4_documents_travaux.sql (analog migration) :
- BEGIN TRANSACTION / COMMIT
- CREATE TABLE IF NOT EXISTS / CREATE INDEX IF NOT EXISTS
- index partiels `WHERE corbeille_le IS NULL`
- ALTER TABLE … ADD COLUMN avec CHECK enum
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrations SQL Phase 5 (0014, 0015, 0021) + kysely-types + brand types identifiants</name>
  <files>
    migrations/0014_phase5_qualification_charges.sql,
    migrations/0015_phase5_bailleur_fiscalite.sql,
    migrations/0021_phase5_ticket_nature_fiscale.sql,
    src/infrastructure/db/kysely-types.ts,
    src/domain/_shared/identifiants.ts
  </files>
  <read_first>
    migrations/0010_phase4_documents_travaux.sql,
    migrations/0009_phase3_bail_indexations.sql,
    src/infrastructure/db/kysely-types.ts,
    src/domain/_shared/identifiants.ts,
    .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-CONTEXT.md,
    .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-PATTERNS.md
  </read_first>
  <behavior>
    - 0014 ajoute exactement 4 colonnes à justificatifs : qualification_fiscale TEXT NULL CHECK IN ('non_qualifie','entretien_reparation','amelioration','charge_courante_periodique','non_deductible') ; qualifie_le TEXT NULL ; date_paiement TEXT NULL ; parent_justificatif_id TEXT NULL REFERENCES justificatifs(id) ; + index partiel idx_justificatifs_qualification_pending (qualification_fiscale = 'non_qualifie' AND corbeille_le IS NULL) + index FK self idx_justificatifs_parent (parent_justificatif_id IS NOT NULL) ; BEGIN TRANSACTION / COMMIT ; commentaires JSDoc citant D-FIS-G2.1 + G2.5 + G2.6 + G2.11.
    - 0015 ajoute 3 colonnes à bailleur : regime_fiscal TEXT NULL CHECK IN ('micro_bic','reel') ; revenus_actifs_annuels_courant_centimes INTEGER NULL ; fiscalite_premier_acces TEXT NULL (ISO 8601 PlainDateTime) ; commentaires D-LOCK-2 + D-FIS-G3.1 + G5.4.
    - 0021 ajoute 2 colonnes à tickets_travaux : nature TEXT NULL CHECK IN ('acquisition_mobilier','entretien','amelioration','autre') ; nature_fiscale TEXT NULL CHECK enum qualification 5 valeurs ; commentaires D-FIS-G1.2 + G2.3.
    - kysely-types BailleurTable / JustificatifsTable / TicketsTravauxTable étendues — types alignés exactement avec migrations (nullable correctement, enums français).
    - identifiants.ts exporte 5 brand types Phase 5 (ComposantId, DeclarationAnnuelleId, DeclarationCorrigeeId, AmortissementExerciceId, ValorisationFiscaleId) + 5 factories `nouveauXxxId()` qui retournent `crypto.randomUUID() as XxxId` — préserver style existant L1-94.
  </behavior>
  <action>
    Créer 3 fichiers migrations dans migrations/ en suivant exactement le pattern
    de 0010_phase4_documents_travaux.sql : (1) en-tête commentaires citant les
    décisions D-FIS-G2.1/2.5/2.6/2.11/G1.2/G2.3 et D-LOCK-2/G3.1/G5.4 pertinentes
    pour chaque migration ; (2) BEGIN TRANSACTION ; (3) ALTER TABLE … ADD COLUMN …
    (avec CHECK enum quand applicable) ; (4) CREATE INDEX IF NOT EXISTS partiels ;
    (5) COMMIT.

    Pour 0014_phase5_qualification_charges.sql : 4 ALTER sur justificatifs (cf.
    section <behavior>). Référencer 05-PATTERNS.md L478-515 comme modèle textuel.

    Pour 0015_phase5_bailleur_fiscalite.sql : 3 ALTER sur bailleur. La colonne
    revenus_actifs_annuels_courant_centimes est INTEGER NULL (Money centimes
    nullable). fiscalite_premier_acces stocke PlainDateTime ISO 8601 (TEXT NULL).

    Pour 0021_phase5_ticket_nature_fiscale.sql : 2 ALTER sur tickets_travaux. La
    valeur 'acquisition_mobilier' dans l'enum nature est nouvelle (D-FIS-G1.2).
    Note : Phase 4 0010 avait laissé tickets_travaux SANS colonne nature (D-115
    différé). Ici on AJOUTE la colonne nature ET nature_fiscale.

    Étendre kysely-types.ts (NE PAS reconstruire) :
      - BailleurTable : ajouter regime_fiscal: 'micro_bic' | 'reel' | null ; revenus_actifs_annuels_courant_centimes: number | null ; fiscalite_premier_acces: string | null
      - JustificatifsTable : ajouter qualification_fiscale: 'non_qualifie' | 'entretien_reparation' | 'amelioration' | 'charge_courante_periodique' | 'non_deductible' | null ; qualifie_le: string | null ; date_paiement: string | null ; parent_justificatif_id: string | null
      - TicketsTravauxTable : ajouter nature: 'acquisition_mobilier' | 'entretien' | 'amelioration' | 'autre' | null ; nature_fiscale: 'non_qualifie' | 'entretien_reparation' | 'amelioration' | 'charge_courante_periodique' | 'non_deductible' | null
      - Aucune table nouvelle ici (DeclarationAnnuelle vient Plan 06, Composant vient Plan 03, etc.)

    Étendre identifiants.ts : ajouter aux types existants (L29-94 préservé) les
    5 brand types Phase 5 + 5 factories. Conserver le commentaire `// Phase 5 —
    identifiants nouveaux agrégats Fiscalité`.

    Appliquer les migrations en exécutant `pnpm db:migrate` localement pour valider.
    Si une migration échoue → corriger (ALTER TABLE … ADD COLUMN ne supporte pas
    IF NOT EXISTS en SQLite — le mécanisme appliquerMigrationsBrutes via meta
    table garantit l'idempotence ; une seconde exécution sera no-op).
  </action>
  <verify>
    <automated>pnpm db:migrate && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier migrations/0014_phase5_qualification_charges.sql existe et contient les 4 ALTER + 2 CREATE INDEX décrits dans <behavior>.
    - Le fichier migrations/0015_phase5_bailleur_fiscalite.sql existe et contient les 3 ALTER.
    - Le fichier migrations/0021_phase5_ticket_nature_fiscale.sql existe et contient les 2 ALTER avec enum nature incluant 'acquisition_mobilier'.
    - `pnpm db:migrate` retourne exit code 0 (migrations appliquées) ; relancée immédiatement, retourne aussi 0 (idempotence — meta table).
    - `grep -c "qualification_fiscale" src/infrastructure/db/kysely-types.ts` retourne au moins 1 (champ présent sur JustificatifsTable).
    - `grep -c "regime_fiscal" src/infrastructure/db/kysely-types.ts` retourne au moins 1.
    - `grep -c "nature_fiscale" src/infrastructure/db/kysely-types.ts` retourne au moins 1.
    - `grep -c "ComposantId" src/domain/_shared/identifiants.ts` retourne au moins 1.
    - `grep -c "DeclarationAnnuelleId" src/domain/_shared/identifiants.ts` retourne au moins 1.
    - `pnpm typecheck` exits 0 — typecheck passe sans erreur.
    - Aucun import infra/Kysely n'apparaît dans src/domain/_shared/identifiants.ts (depcruise rule).
  </acceptance_criteria>
  <done>
    Trois migrations SQL appliquées avec succès, kysely-types reflète les nouvelles
    colonnes, identifiants.ts expose les 5 brand types + factories Phase 5,
    typecheck passe.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Règles fiscales 2026 versionnées (regles-2026.ts) + RegleFiscaleProvider port + erreurs Fiscalité</name>
  <files>
    src/domain/fiscalite/regles/regles-2026.ts,
    src/domain/fiscalite/regles/regle-fiscale-provider.ts,
    src/domain/fiscalite/erreurs.ts,
    tests/unit/fiscalite/regles-2026.test.ts
  </files>
  <read_first>
    src/domain/_shared/irl.ts,
    src/domain/_shared/clock.ts,
    src/domain/_shared/money.ts,
    src/domain/encaissements/erreurs.ts,
    .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-CONTEXT.md,
    .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-PATTERNS.md,
    LMNP.md
  </read_first>
  <behavior>
    - REGLES_2026 est un `as const` exportant les 9 constantes locked du CONTEXT.md L220-240 : SEUIL_MICRO_BIC_LONGUE_DUREE = Money.fromCentimes(8_360_000n) ; PLANCHER_ABATTEMENT = Money.fromCentimes(30_500n) ; ABATTEMENT_LONGUE_DUREE_NUM = 1n + ABATTEMENT_LONGUE_DUREE_DEN = 2n ; SEUIL_LMP_RECETTES = Money.fromCentimes(2_300_000n) ; LF_2025_DATE_EFFET_PV = Temporal.PlainDate.from('2025-02-15') ; DUREES_AMORTISSEMENT_ANS = { terrain: 0, gros_oeuvre: 40, toiture_facade: 25, installations_techniques: 20, agencements_interieurs: 15, mobilier: 7 } ; ARD_DUREE_REPORT_SANS_LIMITE = true (juste pour traçabilité — CGI 39 B).
    - Le module exporte aussi TypeComposantBofip = 'terrain' | 'gros_oeuvre' | 'toiture_facade' | 'installations_techniques' | 'agencements_interieurs' | 'mobilier' (alignement liasse 2031 annexe 2033-B).
    - JSDoc en tête du fichier cite EXPLICITEMENT chaque source juridique avec article : CGI art. 50-0 / 39 / 39 B / 155 IV / 150 VB III ; BOFIP-BIC-AMT-20-40 ; LF 2025 art. 84 (loi 2025-127 du 14/02/2025).
    - RegleFiscaleProvider expose `pour(annee: number): RegleFiscale2026` qui throw RegleFiscaleAbsente si annee < 2026 OR annee > 2028 (révision triennale fin 2028). Note JSDoc : "à étendre regles-2029.ts à la révision suivante".
    - RegleFiscaleProviderEnMemoire (impl par défaut) renvoie REGLES_2026 pour 2026, 2027, 2028.
    - src/domain/fiscalite/erreurs.ts exporte 6 classes : RegleFiscaleAbsente (extends Error), PrerequisCloturalNonSatisfaits (champ public `bloquants: string[]`), DeclarationFigeeException, ComposantsSommeIncoherente (champs `attendu: Money, obtenu: Money`), RevenusFoyerManquants, JustificatifNonQualifie (champs `justificatifId: JustificatifId`). Chaque classe pose `this.name = 'XxxName'` dans le constructor.
    - Tests unit : couvrent toutes les valeurs + types des constantes + comportement RegleFiscaleProvider pour année hors plage. Test BDD `cas_limite_seuil_micro_bic` : Money.fromCentimes(8_359_999n).lte(REGLES_2026.SEUIL_MICRO_BIC_LONGUE_DUREE) === true ; Money.fromCentimes(8_360_001n).lte(SEUIL) === false (CONTEXT L242 cas limite obligatoire).
  </behavior>
  <action>
    Créer src/domain/fiscalite/regles/regles-2026.ts en suivant le pattern du VO
    `src/domain/_shared/irl.ts` (constantes versionnées par année). Imports : Money,
    Temporal. Le module est PUREMENT déclaratif — pas de logique, pas de classe.
    Inclure 1 helper `abattementLongueDuree(recettes: Money, regles: RegleFiscale2026): Money` ?
    NON — repousser au Plan 02 (use case calculer-micro-bic). Ici on expose
    UNIQUEMENT les constantes + type RegleFiscale2026.

    Créer src/domain/fiscalite/regles/regle-fiscale-provider.ts :
      - Interface RegleFiscaleProvider { pour(annee: number): RegleFiscale2026; }
      - Classe RegleFiscaleProviderEnMemoire qui détient une Map<number, RegleFiscale2026> initialisée à { 2026, 2027, 2028 } → REGLES_2026 (révision triennale 2026-2028 — BOFIP-BIC-50-0 confirmé via vérif WebSearch dans 05-DISCUSSION-LOG.md L530).
      - throw RegleFiscaleAbsente(annee) si année non couverte.

    Créer src/domain/fiscalite/erreurs.ts en suivant le pattern de
    src/domain/encaissements/erreurs.ts (une classe par cas, chacune pose name
    explicite, certaines exposent données contextuelles via champs public readonly).
    Ne PAS introduire de classe `FiscaliteError` parente — chaque erreur a ses
    propres champs métier (cf. PrerequisCloturalNonSatisfaits.bloquants).

    Créer tests/unit/fiscalite/regles-2026.test.ts couvrant :
      (1) chaque constante a la bonne valeur (centimes BigInt) ;
      (2) DUREES_AMORTISSEMENT_ANS expose les 6 types BOFIP avec leur durée canonique ;
      (3) RegleFiscaleProviderEnMemoire.pour(2026) retourne REGLES_2026 ;
      (4) RegleFiscaleProviderEnMemoire.pour(2025) throw RegleFiscaleAbsente ;
      (5) RegleFiscaleProviderEnMemoire.pour(2029) throw RegleFiscaleAbsente ;
      (6) cas limite seuil micro-BIC (Money 8_359_999n lte SEUIL vs 8_360_001n gt SEUIL).

    Aucun import infra dans aucun de ces fichiers (depcruise gate).
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/fiscalite/regles-2026.test.ts && pnpm lint:deps</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier src/domain/fiscalite/regles/regles-2026.ts existe et exporte REGLES_2026 + TypeComposantBofip + RegleFiscale2026 (alias type).
    - `grep -c "SEUIL_MICRO_BIC_LONGUE_DUREE.*8_360_000n" src/domain/fiscalite/regles/regles-2026.ts` retourne au moins 1.
    - `grep -c "PLANCHER_ABATTEMENT.*30_500n" src/domain/fiscalite/regles/regles-2026.ts` retourne au moins 1.
    - `grep -c "SEUIL_LMP_RECETTES.*2_300_000n" src/domain/fiscalite/regles/regles-2026.ts` retourne au moins 1.
    - `grep -c "DUREES_AMORTISSEMENT_ANS" src/domain/fiscalite/regles/regles-2026.ts` retourne au moins 1.
    - `grep -E "CGI art\\.? 50-0|CGI art\\.? 155 IV|CGI art\\.? 39 B|LF 2025 art\\.? 84|loi 2025-127" src/domain/fiscalite/regles/regles-2026.ts | wc -l` retourne au moins 5 (5 références juridiques citées).
    - Le fichier src/domain/fiscalite/regles/regle-fiscale-provider.ts existe et exporte RegleFiscaleProvider + RegleFiscaleProviderEnMemoire.
    - Le fichier src/domain/fiscalite/erreurs.ts exporte exactement 6 classes : RegleFiscaleAbsente, PrerequisCloturalNonSatisfaits, DeclarationFigeeException, ComposantsSommeIncoherente, RevenusFoyerManquants, JustificatifNonQualifie.
    - `pnpm test -- tests/unit/fiscalite/regles-2026.test.ts` exits 0 et exécute au moins 6 cas (constantes valeurs, durées amortissement, provider pour année OK, provider throw année hors plage, cas limite seuil — 2 cas).
    - `pnpm lint:deps` exits 0 — aucun import infra/Kysely/SQL dans src/domain/fiscalite/.
    - `grep -rE "import.*from.*infrastructure|import.*from.*kysely|require\\('kysely'\\)" src/domain/fiscalite/` retourne 0 résultat.
  </acceptance_criteria>
  <done>
    REGLES_2026 + provider port + 6 erreurs typées disponibles pour tous les
    plans Phase 5 suivants. Tests verts. Hexagonal strict respecté.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bailleur étendu (regimeFiscal + revenusActifs + fiscalitePremierAcces) + BailleurRepository round-trip + sidebar nav</name>
  <files>
    src/domain/identite/bailleur.ts,
    src/infrastructure/repositories/bailleur-repository-sqlite.ts,
    src/web/views/partials/sidebar-nav.ejs,
    tests/unit/identite/bailleur-fiscalite.test.ts,
    tests/integration/repositories/bailleur-repository-fiscalite.test.ts
  </files>
  <read_first>
    src/domain/identite/bailleur.ts,
    src/infrastructure/repositories/bailleur-repository-sqlite.ts,
    src/web/views/partials/sidebar-nav.ejs,
    src/domain/_shared/money.ts,
    src/domain/encaissements/encaissement.ts,
    .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-CONTEXT.md,
    .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-UI-SPEC.md
  </read_first>
  <behavior>
    - Bailleur garde son identité singleton (D-67) et son pattern copy-on-write. Trois nouveaux champs `readonly` : regimeFiscal: 'micro_bic' | 'reel' | null ; revenusActifsAnnuelsCourant: Money | null ; fiscalitePremierAcces: Temporal.PlainDateTime | null. Par défaut les trois sont null à la création.
    - `Bailleur.creer(props)` accepte les 3 nouveaux champs en optionnel (null par défaut). Aucun invariant croisé n'est introduit côté Bailleur (la cohérence regime vs recettes est portée par DeclarationAnnuelle au Plan 06).
    - `Bailleur.modifier(patch)` accepte les 3 nouveaux champs dans le patch — modifier(patch).regimeFiscal === patch.regimeFiscal si défini, sinon === this.regimeFiscal. Idem pour revenusActifs et fiscalitePremierAcces. Pattern copy-on-write strict (cf. bailleur.ts:42-48).
    - BailleurRepositorySqlite.versDomaine reconstruit Bailleur avec les 3 nouveaux champs (Money.fromCentimes pour revenus, Temporal.PlainDateTime.from pour fiscalite_premier_acces) ; null si row.regime_fiscal IS NULL ; idem autres champs.
    - BailleurRepositorySqlite.enregistrer écrit regime_fiscal/revenus_actifs_annuels_courant_centimes/fiscalite_premier_acces — round-trip TS↔SQLite préservé (null → null).
    - sidebar-nav.ejs ajoute un `<li>` après l'entrée Coffre avec lien vers /fiscalite et `aria-current="page"` quand `navActive === 'fiscalite'`. Le label visible est "Fiscalité" (français, ubiquitous language).
  </behavior>
  <action>
    Étendre src/domain/identite/bailleur.ts (NE PAS recréer) :
      - Étendre BailleurProps avec regimeFiscal?, revenusActifsAnnuelsCourant?, fiscalitePremierAcces?
      - Étendre ModifierBailleurPatch avec les 3 mêmes champs (tous optionnels)
      - Ajouter 3 propriétés `readonly` à la classe (initialiser depuis props avec `?? null` dans constructor)
      - Mettre à jour creer() pour propager les 3 champs (validation : aucune contrainte croisée — chaque champ accepte null OR valeur valide)
      - Mettre à jour modifier(patch) pour fusionner les 3 champs (cf. pattern existant L42-48 — `patch.field ?? this.field`)
      - Ajouter `regimeFiscal/revenusActifsAnnuelsCourant/fiscalitePremierAcces` à la signature du constructor privé

    Étendre src/infrastructure/repositories/bailleur-repository-sqlite.ts (NE PAS recréer) :
      - versDomaine : ajouter mapping row.regime_fiscal → propriété (typecast), row.revenus_actifs_annuels_courant_centimes → Money.fromCentimes(BigInt(n)) ou null, row.fiscalite_premier_acces → Temporal.PlainDateTime.from(s) ou null
      - enregistrer : ajouter regime_fiscal, revenus_actifs_annuels_courant_centimes (bailleur.revenusActifsAnnuelsCourant?.toSqliteInteger() ?? null), fiscalite_premier_acces (bailleur.fiscalitePremierAcces?.toString() ?? null) aux values inserées/mises à jour.

    Modifier src/web/views/partials/sidebar-nav.ejs :
      - Insérer un nouvel `<li>` après le lien Coffre, avec href="/fiscalite", label "Fiscalité"
      - Quand `navActive === 'fiscalite'`, ajouter `aria-current="page"` (cf. pattern pour autres entrées du fichier)
      - Pas de changement aux autres entrées
      - Position : après "Coffre", avant "Bailleur" si présent — sinon en fin de liste

    Créer tests/unit/identite/bailleur-fiscalite.test.ts couvrant :
      - creer sans les 3 nouveaux champs → les 3 sont null
      - creer avec regimeFiscal='reel' + revenusActifs=Money.fromEuros(50_000) + fiscalitePremierAcces=PlainDateTime → les 3 sont set
      - modifier(regimeFiscal='micro_bic') → retourne nouvelle instance avec regimeFiscal='micro_bic', autres champs identiques (copy-on-write)
      - modifier(revenusActifs=Money.fromEuros(60_000)) → autres champs préservés
      - modifier(fiscalitePremierAcces=PlainDateTime) → trace première ouverture

    Créer tests/integration/repositories/bailleur-repository-fiscalite.test.ts en
    suivant le pattern de tests/integration/repositories/encaissement-repository-sqlite.test.ts :
      - Setup in-memory : `new Database(':memory:')` + `Kysely<DB>` + `appliquerToutesMigrations(db, sqlite, MIGRATIONS_DIR)`
      - Test 1 : creer bailleur avec regimeFiscal=null → enregistrer → trouver → propriétés === null
      - Test 2 : modifier(regimeFiscal='reel', revenusActifsAnnuelsCourant=Money.fromEuros(48_000)) → enregistrer → trouver → propriétés round-trippées (cents preservés ; regimeFiscal='reel')
      - Test 3 : modifier(fiscalitePremierAcces=Temporal.PlainDateTime.from('2026-05-20T14:30:00')) → enregistrer → trouver → date round-trippée
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/identite/bailleur-fiscalite.test.ts tests/integration/repositories/bailleur-repository-fiscalite.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "regimeFiscal" src/domain/identite/bailleur.ts` retourne au moins 3 (déclaration + creer + modifier).
    - `grep -c "revenusActifsAnnuelsCourant" src/domain/identite/bailleur.ts` retourne au moins 3.
    - `grep -c "fiscalitePremierAcces" src/domain/identite/bailleur.ts` retourne au moins 3.
    - `grep -c "regime_fiscal" src/infrastructure/repositories/bailleur-repository-sqlite.ts` retourne au moins 2 (versDomaine + enregistrer).
    - `grep -c "navActive === 'fiscalite'\\|navActive == 'fiscalite'\\|navActive=='fiscalite'" src/web/views/partials/sidebar-nav.ejs` retourne au moins 1.
    - `grep -c "/fiscalite" src/web/views/partials/sidebar-nav.ejs` retourne au moins 1.
    - `pnpm test -- tests/unit/identite/bailleur-fiscalite.test.ts` exits 0 — au moins 5 cas verts.
    - `pnpm test -- tests/integration/repositories/bailleur-repository-fiscalite.test.ts` exits 0 — 3 cas verts (null round-trip ; valeur round-trip ; PlainDateTime round-trip).
    - `pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>
    Bailleur étendu avec 3 champs fiscaux nullable, round-trip SQLite vérifié,
    sidebar nav contient l'entrée Fiscalité. Aucun invariant croisé introduit
    (cohérence repoussée à DeclarationAnnuelle Plan 06).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL → DB | Les ALTER TABLE modifient le schéma persistant — une migration mal formée corrompt la base. |
| Kysely typecheck → runtime | Si kysely-types diverge des migrations, runtime peut écrire NULL dans un champ NOT NULL (corruption silencieuse). |
| Domain → Infra (depcruise) | Tout import infra dans domain/fiscalite/ casse l'hexagonal — DDD strict. |
| Provider RegleFiscaleProvider → consommateurs | Throw incontrôlé d'année hors plage casse les calculs en aval. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | Migration 0014 ALTER | mitigate | BEGIN TRANSACTION/COMMIT garantit l'atomicité ; le mécanisme `appliquerMigrationsBrutes` via meta table garantit qu'une migration n'est appliquée qu'une fois. |
| T-05-01-02 | Tampering | kysely-types divergence vs SQL | mitigate | pnpm typecheck en pre-commit + tests d'intégration repository round-trip prouvent que les noms de colonnes correspondent. |
| T-05-01-03 | Information Disclosure | revenus_actifs_annuels_courant_centimes en clair | accept | Local-first single-user (CLAUDE.md §Principes 1+2) — DB n'est jamais exposée hors disque utilisateur. Chiffrement DB au repos (SQLCipher) reporté V1.1 BAK-02 (RISKS.md). |
| T-05-01-04 | Tampering | RegleFiscaleProvider mauvaise année | mitigate | RegleFiscaleAbsente throw avec message explicite ("année %d hors plage versionnée 2026-2028") — fail-fast au plus tôt, jamais de calcul fiscal sur défaut silencieux. |
| T-05-01-05 | Repudiation | Bailleur.modifier() sans audit-trail | accept | Phase 5 introduit l'audit-trail SEULEMENT pour la fiscalité (DeclarationAnnuelle append-only Plan 06). Bailleur reste mutable — V1 single-user, le bailleur EST l'utilisateur, pas besoin de journaliser ses modifications de profil. |
| T-05-01-06 | Elevation of Privilege | depcruise import infra dans domain/fiscalite/ | mitigate | `pnpm lint:deps` en pre-commit + règle existante no-application-to-infra étendue automatiquement aux nouveaux fichiers. |
| T-05-01-07 | Denial of Service | migration infinie sur grosse DB | accept | Phase 5 ne migre pas de données — uniquement schéma. Local-first single-user, DB de l'ordre du Mo, ALTER en < 1s. |
</threat_model>

<verification>
1. `pnpm db:migrate` (idempotent, 2 runs successifs = OK)
2. `pnpm typecheck` → 0 erreur
3. `pnpm test -- tests/unit/fiscalite/ tests/unit/identite/bailleur-fiscalite.test.ts tests/integration/repositories/bailleur-repository-fiscalite.test.ts`
4. `pnpm lint:deps` → 0 erreur (hexagonal strict respecté)
5. `grep -rE "import.*from.*infrastructure|import.*from.*kysely" src/domain/fiscalite/` → 0 résultat
6. Citations juridiques présentes : CGI 50-0, 155 IV, 39 B, LF 2025 art. 84, BOFIP-BIC-AMT-20-40
</verification>

<success_criteria>
- 3 migrations Phase 5 appliquées (0014, 0015, 0021)
- kysely-types reflète exactement les nouvelles colonnes
- REGLES_2026 + RegleFiscaleProvider + 6 erreurs Fiscalité disponibles
- Bailleur étendu round-trip SQLite vérifié
- Sidebar nav contient l'entrée Fiscalité
- 5 brand types identifiants Phase 5 + factories exportés
- Hexagonal strict respecté (depcruise vert)
- typecheck + tests verts
</success_criteria>

<output>
Créer .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-01-SUMMARY.md
en suivant @.claude/get-shit-done/templates/summary.md.
</output>
</content>
</invoke>