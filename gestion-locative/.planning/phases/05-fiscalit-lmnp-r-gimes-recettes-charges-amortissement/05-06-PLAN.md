---
phase: 05
plan: 06
plan_id: "05-06"
type: execute
wave: 6
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05"]
files_modified:
  - migrations/0016_phase5_declaration_annuelle.sql
  - migrations/0017_phase5_declaration_corrigee.sql
  - src/infrastructure/db/kysely-types.ts
  - src/domain/fiscalite/declaration-annuelle.ts
  - src/domain/fiscalite/declaration-corrigee.ts
  - src/domain/fiscalite/declaration-annuelle-repository.ts
  - src/infrastructure/repositories/declaration-annuelle-repository-sqlite.ts
  - src/infrastructure/repositories/declaration-corrigee-repository-sqlite.ts
  - src/application/fiscalite/collecter-prerequis-cloture.ts
  - src/application/fiscalite/cloturer-exercice.ts
  - src/application/fiscalite/creer-declaration-corrigee.ts
  - src/application/fiscalite/choisir-regime.ts
  - src/web/schemas/fiscalite-schemas.ts
  - src/web/routes/fiscalite/cloture.ts
  - src/web/routes/fiscalite/racine.ts
  - src/web/views/pages/fiscalite/recap-annuel.ejs
  - src/web/views/pages/fiscalite/wizard-cloture/etape-1.ejs
  - src/web/views/pages/fiscalite/wizard-cloture/etape-2.ejs
  - src/web/views/pages/fiscalite/wizard-cloture/etape-3.ejs
  - src/web/views/pages/fiscalite/wizard-cloture/etape-4.ejs
  - src/web/views/pages/fiscalite/wizard-cloture/etape-5.ejs
  - src/web/views/pages/fiscalite/declaration-corrigee.ejs
  - src/web/views/partials/wizard-fiscalite-layout.ejs
  - src/web/views/partials/partial-prerequis-cloture.ejs
  - src/web/views/partials/partial-comparatif-regime.ejs
  - src/main.ts
  - tests/_builders/fiscalite.ts
  - tests/unit/fiscalite/cloturer-exercice.test.ts
  - tests/unit/fiscalite/choisir-regime.test.ts
  - tests/unit/fiscalite/creer-declaration-corrigee.test.ts
  - tests/integration/fiscalite/cloturer-exercice.spec.ts
  - tests/integration/repositories/declaration-annuelle-repository-sqlite.test.ts
  - tests/bdd/features/fiscalite-cloture.feature
  - tests/bdd/features/fiscalite-declaration-corrigee.feature
  - tests/bdd/step_definitions/fiscalite-cloture.steps.ts
autonomous: true
requirements: ["FIS-01", "FIS-02", "FIS-03", "FIS-04"]
user_setup: []

mvp_split_rationale: |
  Vertical slice clôture exercice : orchestrateur central qui combine TOUS les
  use cases purs des plans précédents (calculer-micro-bic, calculer-amortissement,
  detecter-bascule-lmp, recettes/charges agrégés) + nouveau snapshot
  DeclarationAnnuelle append-only + DeclarationCorrigee. Wizard S8 5 étapes
  (prérequis bloquants → revenus foyer → comparatif micro/réel → confirmation
  → clôture). Plan le plus lourd Phase 5 — 4 fichiers domain, 5 use cases,
  6 routes/vues. Justifie sa propre vague.

must_haves:
  truths:
    - "DeclarationAnnuelle est un agrégat racine APPEND-ONLY strict (D-FIS-G4.2) : aucune méthode de modification ou d'annulation. Champs : id (DeclarationAnnuelleId brand), bailleurId, exercice (number), regimeApplique ('micro_bic' | 'reel'), recettesTotales (Money), chargesQualifieesParCategorie (Record<QualificationFiscale, Money>), dotationAmortissement (Money), ardGenere (Money), ardConsomme (Money), revenusFoyerSnapshot (Money | null), statutLmnpLmp (VerdictLmp), composantsSnapshot (string JSON sérialisé — D-FIS-G4.2 'composants_snapshot_json conservé pour rejouer le calcul'), clotureLe (Temporal.PlainDate). Invariants : regimeApplique='reel' ⇒ valorisationFiscale du Bien existait à la clôture ; revenusFoyerSnapshot REQUIS si recettesTotales > SEUIL_LMP_RECETTES (D-FIS-G3.1) ; statutLmnpLmp ∈ VerdictLmp valeurs ; statut implicite='cloture' (la création EST la clôture). UNIQUE (bailleur_id, exercice) côté DB — D-FIS-G4.1, G4.2."
    - "DeclarationCorrigee est un agrégat racine APPEND-ONLY strict (D-FIS-G4.4) qui pointe vers une declarationOriginaleId : id, declarationOriginaleId (DeclarationAnnuelleId), motif (string non vide), recettesTotales/charges/dotation/ardGenere/ardConsomme/revenusFoyerSnapshot/statutLmnpLmp/regimeApplique (mêmes champs que DeclarationAnnuelle, valeurs corrigées), creeLe (PlainDateTime). N corrections successives supportées (multiples lignes pointant vers la même originale). La déclaration originale RESTE INTACTE."
    - "Use case `collecterPrerequisCloture(bailleurId, exercice, repos): Promise<{ bloquants: string[], avertissements: string[] }>` — vérifie tous les prérequis D-FIS-G4.1 sans throw : (a) Σ justificatifs non_qualifie pour exercice (corbeille_le IS NULL) = 0 sinon bloquant 'X justificatif(s) à qualifier' ; (b) Σ tickets statut ∉ ('clos','annule') = 0 sinon bloquant 'X ticket(s) à clore' ; (c) recettes > SEUIL_LMP_RECETTES AND bailleur.revenusActifsAnnuelsCourant === null sinon bloquant 'Revenus du foyer requis' ; (d) si regimeChoisi='reel' OR recettes > SEUIL_MICRO → check valorisationFiscale existe pour CHAQUE bien actif sinon bloquant 'Bien X non activé fiscalement'."
    - "Use case `cloturerExercice(commande, repos, clock, regleFiscale, db): Promise<{ declarationId, verdictLmp, regimeApplique }>` orchestrant : (1) collecterPrerequisCloture → throw PrerequisCloturalNonSatisfaits si bloquants ; (2) recettesRepo.sommeRecettesAnnuelles(bailleurId, exercice) → recettes ; (3) chargesRepo.sommeChargesParCategorie → chargesParCategorie ; (4) chargesDeductibles = Σ catégories sauf non_deductible / non_qualifie ; (5) detecterBasculeLmp(recettes, bailleur.revenusActifsAnnuelsCourant, regles) → verdictLmp ; (6) choisirRegime(recettes, commande.regimeChoisi, regles) → regimeApplique ; (7) si regimeApplique='reel' : composants = composantRepo.listerActifsPourBailleur(bailleurId, today) ; ardCumuleEnEntree = tableauAmortissementRepo.dernierArdCumule(bienId, exercice - 1) (par bien — Plan 06 prend la SOMME des biens, Plan 07 fera multi-bien S12) ; resultatAvantAmortissement = max(0, recettes − chargesDeductibles) ; tableau = calculerAmortissement(composants, exercice, regles, { resultatAvantAmortissement, ardCumuleEnEntree }) ; sinon (micro) : tableau vide, dotation/ARD à zéro ; (8) DeclarationAnnuelle.creer({ ...snapshots, composantsSnapshot: JSON.stringify(composants.map(c => c.toProps())), clotureLe: clock.aujourdhui() }) ; (9) db.transaction : declarationAnnuelleRepo.enregistrer(decl, trx) + tableauAmortissementRepo.enregistrerBatch(amortissementExercices, trx) ; (10) retour."
    - "Use case `choisirRegime(recettes, regimeChoisi: 'micro_bic'|'reel'|undefined, regles): 'micro_bic' | 'reel'` (D-FIS-G4.3) : (a) si recettes > SEUIL_MICRO_BIC_LONGUE_DUREE → 'reel' (forcé, override impossible) ; (b) si recettes ≤ SEUIL ET regimeChoisi='reel' → 'reel' (override choix utilisateur) ; (c) sinon → 'micro_bic' (défaut). Le comparateur micro vs réel est affiché en background à l'étape 3 wizard pour < seuil — BOFIP-BIC-DECLA-10-30."
    - "Use case `creerDeclarationCorrigee(commande: { declarationOriginaleId, motif, corrections: ... }, repos, clock, db): Promise<DeclarationCorrigeeId>` : (1) lookup declarationOriginale via declRepo.trouverParId → throw DeclarationOriginaleAbsente si null ; (2) lookup precedentes DeclarationCorrigee de cette originale (pour N corrections successives — récupère la 'dernière' = la corrigée la plus récente — pour cumul, ou simplement enregistre une nouvelle ligne sans cumul, ce qui est plus simple : chaque DeclarationCorrigee remplace conceptuellement la précédente mais toutes sont conservées en append-only) ; (3) recalcul des montants depuis les corrections (logique : la commande contient les NOUVELLES valeurs cibles, pas les deltas — le caller fournit les valeurs corrigées entières) ; (4) DeclarationCorrigee.creer ; (5) declCorrigeeRepo.enregistrer dans transaction. La declaration originale reste intouchée — D-FIS-G4.4. PAS de DELETE, PAS d'UPDATE de la précédente."
    - "Le wizard S8 (UI-SPEC §S8 L165-191) est composé de 5 étapes avec `aria-current=\"step\"` actif : (Étape 1) Prérequis — affiche partial-prerequis-cloture avec ✓/✕ par item, CTA 'Suivant' désactivé tant que bloquants ; (Étape 2) Revenus foyer — pré-rempli depuis bailleur.revenusActifsAnnuelsCourant, lien 'Modifier' vers /fiscalite/revenus-foyer ; (Étape 3) Comparatif micro vs réel — affiche partial-comparatif-regime (tableau 2 colonnes Recettes/Abattement|Charges/Amortissement/Résultat), même si recettes > SEUIL (force réel) le tableau s'affiche en lecture-seule ; (Étape 4) Confirmation — récapitulatif final (regime choisi/forcé, recettes, charges, dotation, verdict LMP) ; (Étape 5) Bouton 'Clôturer l'exercice {N}' qui POST → cloturerExercice + redirect /fiscalite/declarations/:id."
    - "Le récap annuel GET /fiscalite/declarations/:declarationId (post-clôture) affiche : (a) bandeau verdict S7 partial-verdict-fiscal (en tête) ; (b) montants snapshot (recettes/charges/dotation/résultat fiscal) en data table ; (c) lien 'Tableau d'amortissement' → /biens/:bienId/fiscalite/amortissement?annee=N (Plan 04) ; (d) section 'Exports' (boutons placeholder — Plan 07 wirera CSV+PDF) ; (e) section 'Créer une déclaration corrigée' avec lien vers /fiscalite/declarations/:id/corriger (Plan 06)."
    - "La page index /fiscalite (route racine) affiche : (a) bandeau verdict S7 (calculé à la volée si pas de DeclarationAnnuelle pour l'année courante) ; (b) bandeau compteur S2 'X justificatif(s) à qualifier' si > 0 ; (c) lien 'Clôturer l'exercice {anneeCourante}' (CTA principal) ; (d) liste des déclarations clôturées (1 ligne par DeclarationAnnuelle) avec lien drill-down ; (e) Empty state si aucune déclaration ('Activez la fiscalité d'un bien, puis clôturez votre premier exercice à la fin de l'année civile.' — UI-SPEC §Copywriting L229-230) + CTA 'Activer la fiscalité d'un bien' → /biens (liste pour choix)."
    - "Cas limite obligatoire CONTEXT.md L251 : soft-delete d'un encaissement APRÈS clôture → DeclarationAnnuelle inchangée (snapshot par valeur, audit-immuable). Test BDD : (1) clôturer N avec recettes 50k ; (2) annuler encaissement de 10k post-clôture ; (3) re-lire DeclarationAnnuelle.recettesTotales → toujours 50k. Pour corriger officiellement → DeclarationCorrigee."
    - "Cas limite CONTEXT.md L252 (anti-sticky LMP en clôture) : N : LMNP confirmé (recettes 24k / foyer 30k) ; N+1 : LMP probable (recettes 24k / foyer 20k) ; N+2 : LMNP confirmé (recettes 24k / foyer 25k). Trois DeclarationAnnuelle distinctes avec statutLmnpLmp respectifs 'lmnp_confirme', 'lmp_probable', 'lmnp_confirme'. Aucun couplage entre exercices."
    - "Couverture BDD @fis-cloture @phase5 : (a) clôture OK micro-BIC sans charges ; (b) clôture OK réel avec amortissement ; (c) prérequis bloquant : X justificatif non qualifié → throw + UI étape 1 reste ; (d) snapshot inchangé après soft-delete encaissement post-clôture ; (e) anti-sticky 3 exercices indépendants ; (f) impossibilité double clôture (UNIQUE) → throw DeclarationDejaExiste. Couverture @fis-declaration-corrigee : (g) création DeclarationCorrigee + originale intouchée ; (h) N corrections successives — toutes append."
  artifacts:
    - path: "migrations/0016_phase5_declaration_annuelle.sql"
      provides: "Table declarations_annuelles append-only (UNIQUE bailleur_id, exercice)"
      contains: "CREATE TABLE IF NOT EXISTS declarations_annuelles"
    - path: "migrations/0017_phase5_declaration_corrigee.sql"
      provides: "Table declarations_corrigees (FK declaration_originale_id → declarations_annuelles)"
      contains: "CREATE TABLE IF NOT EXISTS declarations_corrigees"
    - path: "src/domain/fiscalite/declaration-annuelle.ts"
      provides: "Agrégat racine append-only — aucune méthode mutator, factory creer avec invariants"
      exports: ["DeclarationAnnuelle"]
    - path: "src/domain/fiscalite/declaration-corrigee.ts"
      provides: "Agrégat racine append-only avec lien parent declarationOriginaleId"
      exports: ["DeclarationCorrigee"]
    - path: "src/application/fiscalite/cloturer-exercice.ts"
      provides: "Use case orchestrateur transaction Kysely englobante"
      exports: ["cloturerExercice", "CloturerExerciceCommande", "CloturerExerciceResultat"]
    - path: "src/application/fiscalite/collecter-prerequis-cloture.ts"
      provides: "Vérification prérequis bloquants D-FIS-G4.1 (justificatifs + tickets + revenus foyer + valorisation)"
      exports: ["collecterPrerequisCloture"]
    - path: "src/application/fiscalite/choisir-regime.ts"
      provides: "Use case pur : micro auto < seuil + override + réel forcé > seuil"
      exports: ["choisirRegime"]
    - path: "src/application/fiscalite/creer-declaration-corrigee.ts"
      provides: "Use case append-only (originale intouchée)"
      exports: ["creerDeclarationCorrigee", "DeclarationOriginaleAbsente"]
    - path: "src/web/routes/fiscalite/cloture.ts"
      provides: "Routes wizard 5 étapes + POST clôture + récap déclaration"
      contains: "cloturer-exercice"
    - path: "src/web/routes/fiscalite/racine.ts"
      provides: "Route GET /fiscalite (index)"
      contains: "/fiscalite"
    - path: "src/web/views/pages/fiscalite/recap-annuel.ejs"
      provides: "Récap post-clôture avec bandeau verdict + montants snapshot + liens drill-down"
      contains: "partial-verdict-fiscal"
    - path: "src/web/views/partials/wizard-fiscalite-layout.ejs"
      provides: "Layout 5 étapes wizard avec aria-current=step"
      contains: "aria-current=\"step\""
  key_links:
    - from: "POST /fiscalite/cloturer (étape 5 wizard)"
      to: "cloturerExercice use case"
      via: "Fastify route + Zod safeParse + transaction Kysely"
      pattern: "cloturerExercice"
    - from: "DeclarationAnnuelle (snapshot)"
      to: "tableauAmortissementRepo.enregistrerBatch (Plan 04)"
      via: "même db.transaction().execute"
      pattern: "transaction.*execute"
    - from: "DeclarationCorrigee.declarationOriginaleId"
      to: "DeclarationAnnuelle.id"
      via: "FK SQL REFERENCES declarations_annuelles(id)"
      pattern: "declaration_originale_id"
    - from: "GET /fiscalite (racine)"
      to: "RecettesRepository + ChargesRepository + DetecterBasculeLmp pour calcul à la volée si pas de DeclarationAnnuelle"
      via: "lookups multiples + render"
      pattern: "detecterBasculeLmp\\|sommeRecettesAnnuelles"

tags: [fiscalite, lmnp, cloture-exercice, declaration-annuelle, declaration-corrigee, wizard, snapshot-append-only, g4]
---

<objective>
Implémenter la clôture annuelle de l'exercice fiscal LMNP : agrégat
DeclarationAnnuelle append-only (snapshot par valeur), wizard 5 étapes (S8),
choix régime micro/réel, DeclarationCorrigee pour rectifications post-clôture.
C'est l'orchestrateur central Phase 5 qui combine TOUS les use cases purs
précédents (micro-BIC, amortissement, LMP) en 1 transaction transactionnelle
atomique.

Purpose : FIS-01/02/03/04 — finalise la chaîne. Sans clôture, aucun snapshot
durable, pas d'audit-trail, pas de base pour la liasse 2031 Phase 6. C'est
LA capacité utilisateur signature de Phase 5.

Output :
  - 2 migrations (0016 declarations_annuelles + 0017 declarations_corrigees)
  - 2 agrégats append-only (DeclarationAnnuelle + DeclarationCorrigee)
  - 5 use cases (cloturer-exercice + collecter-prerequis + choisir-regime + creer-declaration-corrigee + helpers)
  - Wizard S8 5 étapes + récap annuel + page racine /fiscalite
  - Couverture BDD @fis-cloture (6 scénarios + anti-sticky) + @fis-declaration-corrigee
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-CONTEXT.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-PATTERNS.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-UI-SPEC.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-01-PLAN.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-02-PLAN.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-03-PLAN.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-04-PLAN.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-05-PLAN.md

# Sources canoniques
@LMNP.md
@practices/BDD_PRACTICES.md

# Patterns à répliquer
@src/domain/locatif/bail-indexation.ts
@src/infrastructure/repositories/bail-indexation-repository-sqlite.ts
@src/application/locatif/appliquer-indexation-irl.ts
@src/web/routes/indexations.ts
@src/web/views/partials/wizard-irl-layout.ejs
@migrations/0009_phase3_bail_indexations.sql
@src/domain/_shared/money.ts
@src/domain/_shared/clock.ts
</context>

<interfaces>
From src/domain/fiscalite/regles/regles-2026.ts (Plan 01) :
```typescript
export const REGLES_2026 = {
  SEUIL_MICRO_BIC_LONGUE_DUREE: Money,
  SEUIL_LMP_RECETTES: Money,
  // ...
} as const;
```

From src/domain/fiscalite/erreurs.ts (Plan 01) :
```typescript
export class PrerequisCloturalNonSatisfaits extends Error {
  constructor(public readonly bloquants: string[]);
}
export class DeclarationFigeeException extends Error;
```

From src/application/fiscalite/calculer-micro-bic.ts (Plan 02) :
```typescript
export function calculerMicroBic(recettes: Money, regles): MicroBicResult;
export interface MicroBicResult { abattementApplique: Money; resultatImposable: Money; seuilDepasse: boolean; }
```

From src/application/fiscalite/calculer-amortissement.ts (Plan 04) :
```typescript
export function calculerAmortissement(
  composants: Composant[],
  exercice: number,
  regles: typeof REGLES_2026,
  options: { resultatAvantAmortissement: Money; ardCumuleEnEntree: Money }
): TableauAmortissementExercice;
```

From src/application/fiscalite/detecter-bascule-lmp.ts (Plan 05) :
```typescript
export type VerdictLmp = 'lmnp_confirme' | 'lmp_probable' | 'indetermine_revenus_foyer_manquants';
export function detecterBasculeLmp(
  input: { recettes: Money; revenusFoyer: Money | null },
  regles
): VerdictLmp;
```

From src/domain/fiscalite/recettes-repository.ts (Plan 02), charges-repository.ts (Plan 02),
composant-repository.ts (Plan 03), tableau-amortissement-repository.ts (Plan 04) :
ports déjà disponibles.

From src/domain/locatif/bail-indexation.ts (PATTERN AGRÉGAT APPEND-ONLY) :
```typescript
class BailIndexation {
  static creer(props): BailIndexation;       // factory uniquement
  // PAS DE annuler, modifier, etc.
}
```

From src/infrastructure/repositories/bail-indexation-repository-sqlite.ts (PATTERN ADAPTER APPEND-ONLY) :
```typescript
async enregistrer(decl, trx?) { await db.insertInto(...).values(...).execute(); /* PAS de .onConflict() */ }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Domaine — DeclarationAnnuelle + DeclarationCorrigee + ports + migrations 0016+0017 + adapters SQLite append-only + choisir-regime use case</name>
  <files>
    migrations/0016_phase5_declaration_annuelle.sql,
    migrations/0017_phase5_declaration_corrigee.sql,
    src/infrastructure/db/kysely-types.ts,
    src/domain/fiscalite/declaration-annuelle.ts,
    src/domain/fiscalite/declaration-corrigee.ts,
    src/domain/fiscalite/declaration-annuelle-repository.ts,
    src/infrastructure/repositories/declaration-annuelle-repository-sqlite.ts,
    src/infrastructure/repositories/declaration-corrigee-repository-sqlite.ts,
    src/application/fiscalite/choisir-regime.ts,
    tests/unit/fiscalite/choisir-regime.test.ts,
    tests/integration/repositories/declaration-annuelle-repository-sqlite.test.ts
  </files>
  <read_first>
    src/domain/locatif/bail-indexation.ts,
    src/infrastructure/repositories/bail-indexation-repository-sqlite.ts,
    src/domain/fiscalite/erreurs.ts,
    src/domain/fiscalite/regles/regles-2026.ts,
    src/domain/_shared/identifiants.ts,
    migrations/0009_phase3_bail_indexations.sql,
    .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-PATTERNS.md
  </read_first>
  <behavior>
    Migration 0016_phase5_declaration_annuelle.sql crée declarations_annuelles :
      id TEXT PRIMARY KEY,
      bailleur_id TEXT NOT NULL REFERENCES bailleur(id),
      exercice INTEGER NOT NULL,
      regime_applique TEXT NOT NULL CHECK (regime_applique IN ('micro_bic','reel')),
      recettes_totales_centimes INTEGER NOT NULL,
      charges_qualifiees_json TEXT NOT NULL,    -- JSON Record<QualificationFiscale, centimes>
      dotation_amortissement_centimes INTEGER NOT NULL DEFAULT 0,
      ard_genere_centimes INTEGER NOT NULL DEFAULT 0,
      ard_consomme_centimes INTEGER NOT NULL DEFAULT 0,
      revenus_foyer_snapshot_centimes INTEGER NULL,
      statut_lmnp_lmp TEXT NOT NULL CHECK (statut_lmnp_lmp IN ('lmnp_confirme','lmp_probable','indetermine_revenus_foyer_manquants')),
      composants_snapshot_json TEXT NOT NULL DEFAULT '[]',
      cloture_le TEXT NOT NULL
      + UNIQUE (bailleur_id, exercice)  -- une seule clôture par bailleur+année

    Migration 0017_phase5_declaration_corrigee.sql crée declarations_corrigees :
      id TEXT PRIMARY KEY,
      declaration_originale_id TEXT NOT NULL REFERENCES declarations_annuelles(id),
      motif TEXT NOT NULL,
      recettes_totales_centimes INTEGER NOT NULL,
      charges_qualifiees_json TEXT NOT NULL,
      dotation_amortissement_centimes INTEGER NOT NULL DEFAULT 0,
      ard_genere_centimes INTEGER NOT NULL DEFAULT 0,
      ard_consomme_centimes INTEGER NOT NULL DEFAULT 0,
      revenus_foyer_snapshot_centimes INTEGER NULL,
      statut_lmnp_lmp TEXT NOT NULL CHECK (statut_lmnp_lmp IN ('lmnp_confirme','lmp_probable','indetermine_revenus_foyer_manquants')),
      regime_applique TEXT NOT NULL CHECK (regime_applique IN ('micro_bic','reel')),
      cree_le TEXT NOT NULL
      + INDEX idx_decl_corr_originale (declaration_originale_id, cree_le DESC)
      -- NOTE : pas d'UNIQUE — N corrections successives autorisées

    Pattern append-only strict : aucun adapter ne contient onConflict (cf.
    bail-indexation-repository-sqlite.ts:40-58 et 05-PATTERNS.md L40-58 + L279-300).

    Agrégat DeclarationAnnuelle suit pattern bail-indexation.ts L32-117 :
      - 100 % readonly champs
      - Constructor privé
      - Factory `static creer(props)` avec invariants (cf. CONTEXT.md L208 invariants)
      - AUCUNE méthode mutator
      - JSDoc en-tête répliquant textuellement le commentaire bail-indexation.ts L32-46 (append-only strict, correction via DeclarationCorrigee)

    Agrégat DeclarationCorrigee idem mais ajoute champ readonly declarationOriginaleId.

    Use case pur `choisirRegime(recettes: Money, regimeChoisi: 'micro_bic'|'reel'|undefined, regles): 'micro_bic'|'reel'` :
      - si recettes.superieurA(regles.SEUIL_MICRO_BIC_LONGUE_DUREE) → 'reel'
      - sinon : si regimeChoisi === 'reel' → 'reel' ; sinon → 'micro_bic'

    Tests unit choisir-regime : 4 cas (seuil + 1c → réel forcé ignore override ; seuil exact → micro défaut ; override 'reel' < seuil → 'reel' ; undefined < seuil → 'micro_bic').

    Tests intégration declaration-annuelle-repository-sqlite : enregistrer +
    trouverParId + listerParBailleur + UNIQUE violation 2e clôture même
    (bailleur_id, exercice). Tests pour DeclarationCorrigee : enregistrer N
    successives + listerParDeclarationOriginale ordonnés DESC.
  </behavior>
  <action>
    Lire src/domain/locatif/bail-indexation.ts + src/infrastructure/repositories/bail-indexation-repository-sqlite.ts
    pour internaliser le pattern append-only.

    Créer 0016 + 0017 migrations bracketed BEGIN/COMMIT avec UNIQUE constraint
    sur declarations_annuelles et INDEX sur declarations_corrigees.

    Étendre kysely-types.ts avec DeclarationsAnnuellesTable + DeclarationsCorrigeesTable + DB interface.

    Créer src/domain/fiscalite/declaration-annuelle.ts :
      - JSDoc cite invariants D-FIS-G4.2 + CONTEXT.md L208
      - Interface DeclarationAnnuelleProps complète
      - Constructor privé + factory creer avec invariants :
        - regimeApplique ∈ ('micro_bic','reel')
        - statutLmnpLmp ∈ VerdictLmp valeurs
        - revenusFoyerSnapshot REQUIS si recettesTotales > SEUIL_LMP_RECETTES (D-FIS-G3.1) — sinon throw RevenusFoyerManquants
        - regimeApplique='reel' ⇒ composantsSnapshot ne doit pas être '[]' (au moins 1 composant dans le snapshot)
      - Méthode `toProps()` pour serialization tests.

    Créer src/domain/fiscalite/declaration-corrigee.ts (pattern similar) avec
    invariant motif.trim().length > 0.

    Créer src/domain/fiscalite/declaration-annuelle-repository.ts (port unique
    qui expose les méthodes pour DeclarationAnnuelle ET DeclarationCorrigee — ou
    2 ports séparés ; préférer 2 ports SOLID) :
      - DeclarationAnnuelleRepository : enregistrer(decl, trx?), trouverParId(id), trouverParBailleurExercice(bailleurId, exercice), listerParBailleur(bailleurId)
      - DeclarationCorrigeeRepository : enregistrer(corr, trx?), trouverParId, listerParDeclarationOriginale(originaleId)

    Créer 2 adapters SQLite en suivant bail-indexation-repository-sqlite.ts ligne par ligne (pattern append-only strict, PAS de onConflict).

    Créer src/application/fiscalite/choisir-regime.ts (use case pur).

    Tests unit + tests intégration in-memory + appliquerToutesMigrations.
  </action>
  <verify>
    <automated>pnpm db:migrate && pnpm test -- tests/unit/fiscalite/choisir-regime.test.ts tests/integration/repositories/declaration-annuelle-repository-sqlite.test.ts && pnpm typecheck && pnpm lint:deps</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm db:migrate` exits 0.
    - `pnpm typecheck` exits 0.
    - `pnpm test -- tests/unit/fiscalite/choisir-regime.test.ts` exits 0 — au moins 4 cas.
    - `pnpm test -- tests/integration/repositories/declaration-annuelle-repository-sqlite.test.ts` exits 0 — au moins 4 cas (enregistrer + trouver + UNIQUE 2e clôture + listerParBailleur).
    - `! grep -q "onConflict" src/infrastructure/repositories/declaration-annuelle-repository-sqlite.ts` (append-only strict).
    - `! grep -q "onConflict" src/infrastructure/repositories/declaration-corrigee-repository-sqlite.ts`.
    - `grep -c "append-only\\|aucune méthode" src/domain/fiscalite/declaration-annuelle.ts` retourne au moins 1.
    - `grep -c "declarations_annuelles\\|declarations_corrigees" src/infrastructure/db/kysely-types.ts` retourne au moins 2.
    - `pnpm lint:deps` exits 0.
  </acceptance_criteria>
  <done>
    Migrations 0016 + 0017 appliquées + agrégats append-only + adapters SQLite +
    use case pur choisir-regime + ports en place. Tests intégration prouvent
    UNIQUE violation 2e clôture et append-only N corrections.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Use cases orchestrateurs collecter-prerequis-cloture + cloturer-exercice + creer-declaration-corrigee (transaction)</name>
  <files>
    src/application/fiscalite/collecter-prerequis-cloture.ts,
    src/application/fiscalite/cloturer-exercice.ts,
    src/application/fiscalite/creer-declaration-corrigee.ts,
    tests/unit/fiscalite/cloturer-exercice.test.ts,
    tests/unit/fiscalite/creer-declaration-corrigee.test.ts,
    tests/integration/fiscalite/cloturer-exercice.spec.ts
  </files>
  <read_first>
    src/application/locatif/appliquer-indexation-irl.ts,
    src/application/fiscalite/calculer-micro-bic.ts,
    src/application/fiscalite/calculer-amortissement.ts,
    src/application/fiscalite/detecter-bascule-lmp.ts,
    src/application/fiscalite/choisir-regime.ts,
    src/domain/fiscalite/declaration-annuelle.ts,
    src/domain/fiscalite/declaration-corrigee.ts,
    src/domain/fiscalite/erreurs.ts,
    src/domain/encaissements/erreurs.ts,
    .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-PATTERNS.md
  </read_first>
  <action>
    `collecterPrerequisCloture(bailleurId, exercice, repos: { bailleurRepo, justificatifRepo, ticketRepo, valorisationRepo, bienRepo, recettesRepo, regleFiscale }, clock): Promise<{ bloquants: string[], avertissements: string[] }>` :
      (a) bailleur = await bailleurRepo.trouver() ; if (!bailleur) bloquants.push('Bailleur absent') ;
      (b) justificatifsNonQualifies = justificatifRepo.compterNonQualifiesPourAnnee(exercice) (méthode à AJOUTER au repo si pas déjà — COUNT(*) WHERE qualification_fiscale IS NULL OR qualification_fiscale='non_qualifie' AND corbeille_le IS NULL AND substr(coalesce(date_paiement, date_document), 1, 4) = exercice) ; if (count > 0) bloquants.push(`${count} justificatif(s) à qualifier pour ${exercice}`)
      (c) ticketsNonClos = ticketRepo.compterStatutsActifs() (COUNT statut IN ('ouvert','en_cours')) ; if (count > 0) bloquants.push(`${count} ticket(s) à clore`)
      (d) recettes = await recettesRepo.sommeRecettesAnnuelles(bailleurId, exercice) ; if (recettes > regles.SEUIL_LMP_RECETTES AND bailleur.revenusActifsAnnuelsCourant === null) bloquants.push('Revenus du foyer requis (recettes > 23 000 €)')
      (e) si recettes > regles.SEUIL_MICRO_BIC_LONGUE_DUREE : pour chaque Bien actif via bienRepo.listerActifs() : si valorisationRepo.trouverParBien(bien.id) === null → bloquants.push(`Bien ${bien.adresse.enLigne()} non activé fiscalement`)

    `cloturerExercice(commande: CloturerExerciceCommande, repos, clock, regleFiscale, db: Kysely<DB>): Promise<CloturerExerciceResultat>` (pattern src/application/locatif/appliquer-indexation-irl.ts L85-215) :
      CloturerExerciceCommande = { bailleurId, exercice, regimeChoisi?: 'micro_bic'|'reel' }
      Étapes :
      (1) prerequis = await collecterPrerequisCloture(...) → if (prerequis.bloquants.length > 0) throw new PrerequisCloturalNonSatisfaits(prerequis.bloquants)
      (2) Vérifier qu'aucune DeclarationAnnuelle (bailleurId, exercice) existe sinon throw DeclarationDejaExiste (idempotence — UNIQUE DB protège mais on prefer-fail-fast)
      (3) recettes = await recettesRepo.sommeRecettesAnnuelles(bailleurId, exercice)
      (4) chargesParCategorie = await chargesRepo.sommeChargesParCategorie(bailleurId, exercice)
      (5) chargesDeductibles = chargesParCategorie.entretien_reparation + amelioration + charge_courante_periodique (somme — exclure non_deductible et non_qualifie)
      (6) regles = regleFiscale.pour(exercice)
      (7) verdictLmp = detecterBasculeLmp({ recettes, revenusFoyer: bailleur.revenusActifsAnnuelsCourant }, regles)
      (8) regimeApplique = choisirRegime(recettes, commande.regimeChoisi, regles)
      (9) Si regimeApplique === 'reel' :
            composants = await composantRepo.listerActifsPourBailleur(bailleurId, clock.aujourdhui())
            // Pour Plan 06, ARD géré au niveau du Bailleur (pas par bien — V1 simple).
            // Plan 07 fera multi-bien S12 et possiblement répartition ARD par bien.
            ardCumuleEnEntree = await tableauAmortissementRepo.dernierArdCumule(/* synthese bailleur exercice-1 */)  // si pas implémenté au niveau bailleur, ardCumuleEnEntree = Money.zero() pour V1
            resultatAvantAmortissement = recettes.superieurA(chargesDeductibles) ? recettes.soustraire(chargesDeductibles) : Money.zero()
            tableau = calculerAmortissement(composants, exercice, regles, { resultatAvantAmortissement, ardCumuleEnEntree })
            dotationAmortissement = tableau.dotationAppliqueeTotale
            ardGenere = tableau.ardGenereTotal()
            ardConsomme = tableau.ardConsomme
            composantsSnapshot = JSON.stringify(composants.map(c => c.toProps()))
            amortissementExercicesLignes = tableau.dotationParComposant.map(... → AmortissementExercice.creer(...)) + 1 ligne SYNTHESE_BIEN par bien
          Sinon (micro_bic) :
            dotationAmortissement = Money.zero() ; ardGenere = Money.zero() ; ardConsomme = Money.zero() ; composantsSnapshot = '[]' ; amortissementExercicesLignes = []
      (10) declaration = DeclarationAnnuelle.creer({ ...tous champs, composantsSnapshot, clotureLe: clock.aujourdhui(), revenusFoyerSnapshot: bailleur.revenusActifsAnnuelsCourant ?? null })
      (11) await db.transaction().execute(async (trx) => { await declRepo.enregistrer(declaration, trx) ; if (amortissementExercicesLignes.length > 0) await tableauAmortRepo.enregistrerBatch(amortissementExercicesLignes, trx) })
      (12) Retour { declarationId: declaration.id, verdictLmp, regimeApplique }

    Erreurs exportées : DeclarationDejaExiste (nouvelle classe dans erreurs.ts ou ici).

    `creerDeclarationCorrigee(commande: { declarationOriginaleId, motif, corrections: Partial<DeclarationProps> }, repos, clock, db): Promise<DeclarationCorrigeeId>` :
      (1) declOriginale = await declRepo.trouverParId(commande.declarationOriginaleId) → if (null) throw DeclarationOriginaleAbsente
      (2) Fusion : nouvelles valeurs = corrections + declOriginale.toProps() pour les champs non fournis
      (3) DeclarationCorrigee.creer({ ...nouveaux champs, declarationOriginaleId, motif: commande.motif, creeLe: clock.aujourdhui() })
      (4) db.transaction → declCorrRepo.enregistrer(corr, trx) — pas de modification de declOriginale
      (5) Retour ID

    Tests unit cloturer-exercice : stubs des repos + clock fixe + regleFiscale en mémoire :
      - Test 1 : prérequis bloquants → throw PrerequisCloturalNonSatisfaits (NE persiste rien)
      - Test 2 : micro-BIC avec recettes 30k → declaration créée avec dotation/ARD à zero ; PAS de tableau amortissement enregistré
      - Test 3 : réel avec recettes 100k + 4 composants amortissables + résultat 50k → cloturer-exercice combine tous les calculs (verifier déclaration.dotationAmortissement === tableau.dotationAppliqueeTotale + amortissementExercicesLignes enregistrées)
      - Test 4 : double appel (même bailleur + exercice) → throw DeclarationDejaExiste OR Kysely UNIQUE violation
      - Test 5 : seuil dépassé (recettes 90k > 83.6k) → regimeApplique forcé 'reel' même si commande.regimeChoisi='micro_bic'

    Tests unit creer-declaration-corrigee :
      - Test 1 : declarationOriginaleId inexistant → throw DeclarationOriginaleAbsente
      - Test 2 : corrections partielles → declCorrigee a les nouveaux montants + declOriginale INTACTE (aucun update sur celle-ci, vérifier via stub)
      - Test 3 : N corrections successives → N lignes en base, toutes pointant vers même originaleId, ordonnées par creeLe DESC

    Tests intégration `tests/integration/fiscalite/cloturer-exercice.spec.ts` :
      - Setup in-memory + appliquerToutesMigrations
      - Seed : bailleur singleton + bien + 6 composants + valorisation + 3 encaissements (recettes 30k) + 2 justificatifs qualifiés (charges 5k)
      - Appel cloturerExercice({ bailleurId, exercice: 2026 }, repos, clock, regleFiscale, db)
      - Assert : 1 DeclarationAnnuelle persistée avec recettesTotales = 3_000_000n, chargesQualifieesParCategorie correctly serialized JSON, regimeApplique='micro_bic' (30k < 83.6k seuil), verdictLmp='lmnp_confirme', composantsSnapshot='[]' (micro ne snapshote pas composants car pas utiles)
      - Note : composantsSnapshot pour micro est documenté '[]' — l'invariant 'regimeApplique=reel ⇒ snapshot non vide' ne bloque PAS micro.
      - Soft-delete d'un encaissement post-clôture + relecture DeclarationAnnuelle → recettesTotales INCHANGÉ (audit-immuable, CONTEXT.md L251)
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/fiscalite/cloturer-exercice.test.ts tests/unit/fiscalite/creer-declaration-corrigee.test.ts tests/integration/fiscalite/cloturer-exercice.spec.ts && pnpm typecheck && pnpm lint:deps</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- tests/unit/fiscalite/cloturer-exercice.test.ts` exits 0 — au moins 5 cas verts.
    - `pnpm test -- tests/unit/fiscalite/creer-declaration-corrigee.test.ts` exits 0 — au moins 3 cas verts.
    - `pnpm test -- tests/integration/fiscalite/cloturer-exercice.spec.ts` exits 0 — au moins 2 cas (clôture micro + immutabilité post-soft-delete).
    - `grep -c "PrerequisCloturalNonSatisfaits\\|DeclarationDejaExiste\\|DeclarationOriginaleAbsente" src/application/fiscalite/cloturer-exercice.ts src/application/fiscalite/creer-declaration-corrigee.ts | wc -l` retourne au moins 3.
    - `grep -c "db\\.transaction\\(\\)\\.execute" src/application/fiscalite/cloturer-exercice.ts` retourne au moins 1.
    - `grep -c "calculerMicroBic\\|calculerAmortissement\\|detecterBasculeLmp\\|choisirRegime" src/application/fiscalite/cloturer-exercice.ts` retourne au moins 3 (orchestration des use cases purs).
    - `pnpm typecheck` exits 0.
    - `pnpm lint:deps` exits 0.
  </acceptance_criteria>
  <done>
    Use cases orchestrateurs cloturer-exercice + creer-declaration-corrigee +
    collecter-prerequis-cloture en place. Transaction Kysely englobante.
    Snapshot par valeur immuable vérifié en intégration. Append-only N
    corrections successives prouvé. Tous les cas limites CONTEXT.md L249-252
    couverts.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Routes wizard 5 étapes + récap annuel + page racine /fiscalite + 3 partials + steps BDD + main.ts wiring</name>
  <files>
    src/web/schemas/fiscalite-schemas.ts,
    src/web/routes/fiscalite/cloture.ts,
    src/web/routes/fiscalite/racine.ts,
    src/web/views/pages/fiscalite/recap-annuel.ejs,
    src/web/views/pages/fiscalite/wizard-cloture/etape-1.ejs,
    src/web/views/pages/fiscalite/wizard-cloture/etape-2.ejs,
    src/web/views/pages/fiscalite/wizard-cloture/etape-3.ejs,
    src/web/views/pages/fiscalite/wizard-cloture/etape-4.ejs,
    src/web/views/pages/fiscalite/wizard-cloture/etape-5.ejs,
    src/web/views/pages/fiscalite/declaration-corrigee.ejs,
    src/web/views/partials/wizard-fiscalite-layout.ejs,
    src/web/views/partials/partial-prerequis-cloture.ejs,
    src/web/views/partials/partial-comparatif-regime.ejs,
    src/main.ts,
    tests/bdd/features/fiscalite-cloture.feature,
    tests/bdd/features/fiscalite-declaration-corrigee.feature,
    tests/bdd/step_definitions/fiscalite-cloture.steps.ts
  </files>
  <read_first>
    src/web/routes/indexations.ts,
    src/web/views/partials/wizard-irl-layout.ejs,
    src/web/views/pages/baux/indexer/saisie.ejs,
    src/web/views/pages/baux/indexer/simulation.ejs,
    src/web/views/pages/baux/indexer/confirmation.ejs,
    src/web/views/partials/partial-inventaire-warnings.ejs,
    src/web/views/partials/data-table.ejs,
    src/web/views/partials/empty-state.ejs,
    src/web/views/partials/banniere-warning.ejs,
    src/web/views/partials/banniere-success.ejs,
    src/main.ts,
    .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-UI-SPEC.md,
    src/application/fiscalite/cloturer-exercice.ts,
    src/application/fiscalite/collecter-prerequis-cloture.ts
  </read_first>
  <action>
    Schémas Zod `src/web/schemas/fiscalite-schemas.ts` :
      - cloturerExerciceSchema = z.object({ exercice: z.coerce.number().int().min(2020).max(2100), regimeChoisi: z.enum(['micro_bic','reel']).optional() })
      - creerDeclarationCorrigeeSchema = z.object({ motif: z.string().min(1, 'Motif requis').max(2000), recettesTotalesEuros: z.coerce.number().min(0).optional(), /* autres corrections optionnelles */ })

    Routes Fastify `src/web/routes/fiscalite/cloture.ts` (pattern src/web/routes/indexations.ts wizard IRL) :
      - GET /fiscalite/cloturer/:exercice/etape/1 (Prérequis) : appel collecterPrerequisCloture → render etape-1.ejs avec { exercice, prerequis: { bloquants, avertissements }, ctaSuivantDisabled: prerequis.bloquants.length > 0 }
      - GET /fiscalite/cloturer/:exercice/etape/2 (Revenus foyer) : lookup bailleur → render etape-2.ejs avec { exercice, revenusActifsCourant, lienModifier: '/fiscalite/revenus-foyer' }
      - GET /fiscalite/cloturer/:exercice/etape/3 (Comparatif) : appel recettesRepo + chargesRepo + calculerMicroBic + recalculerTableauAmortissement (Plan 04) → render etape-3.ejs avec { exercice, micro: { resultat, abattement }, reel: { resultat, charges, dotation, ardConsomme }, regimeForce: recettes > SEUIL ? 'reel' : null }
      - GET /fiscalite/cloturer/:exercice/etape/4 (Confirmation) : recettes + verdictLmp + regimeChoisi (query string) → render etape-4.ejs
      - POST /fiscalite/cloturer/:exercice (étape 5 — submission finale) : safeParse(cloturerExerciceSchema) ; appel cloturerExercice ; gérer PrerequisCloturalNonSatisfaits → 400 + redirect /fiscalite/cloturer/:exercice/etape/1 avec session.banniereError ; gérer DeclarationDejaExiste → 409 + redirect /fiscalite/declarations/:exercice ; succès → req.session.banniereSuccess = 'Exercice {exercice} clôturé. Verdict : {verdictLabel}.' + redirect /fiscalite/declarations/:declarationId
      - GET /fiscalite/declarations/:id : récap post-clôture render recap-annuel.ejs
      - GET /fiscalite/declarations/:id/corriger : render declaration-corrigee.ejs (formulaire)
      - POST /fiscalite/declarations/:id/corriger : appel creerDeclarationCorrigee + redirect avec succès
      - Pattern try/catch : src/web/routes/encaissements.ts L173-196 (instanceof checks).

    Route racine `src/web/routes/fiscalite/racine.ts` :
      - GET /fiscalite : lookup bailleur ; anneeCourante = clock.aujourdhui().year ; lookup declarations = declRepo.listerParBailleur(bailleur.id) ; declarationAnneeCourante = declarations.find(d => d.exercice === anneeCourante) ; verdictLmp = declarationAnneeCourante ? declarationAnneeCourante.statutLmnpLmp : detecterBasculeLmp({ recettes: await recettesRepo.sommeRecettesAnnuelles, revenusFoyer: bailleur.revenusActifsAnnuelsCourant }, regleFiscale.pour(anneeCourante)) ; compteurNonQualifies = justificatifRepo.compterNonQualifiesPourAnnee(anneeCourante) ; render pages/fiscalite/index.ejs (à créer dans pages/fiscalite/ — partie de cette tâche) avec { declarations, anneeCourante, verdictLmp, compteurNonQualifies }
      - Trace fiscalitePremierAcces si null → bailleur.modifier({ fiscalitePremierAcces: ... }) + enregistrer (D-FIS-G5.4) — bandeau onboarding sera Plan 07 ; pour Plan 06 simple trace.

    Note : on omet pages/fiscalite/index.ejs explicitement listée pour rester
    léger ; à la place le récap-annuel.ejs sert pour /fiscalite/declarations/:id ;
    la racine /fiscalite affiche une vue très simple : titre + bandeau verdict +
    liste déclarations + CTA "Clôturer l'exercice {N}". On crée un index.ejs léger
    dans pages/fiscalite/.

    Pages wizard (UI-SPEC §S8 L165-191) — pattern src/web/views/pages/baux/indexer/ :
      - etape-1.ejs : include wizard-fiscalite-layout (currentStep=1) + include partial-prerequis-cloture(items) + CTA "Suivant" disabled tant que bloquants. Si bloquants.length === 0 → CTA actif vers /fiscalite/cloturer/:exercice/etape/2.
      - etape-2.ejs : currentStep=2 + form simple (révision revenus foyer) + lien "Modifier" externe vers /fiscalite/revenus-foyer + CTA "Suivant".
      - etape-3.ejs : currentStep=3 + include partial-comparatif-regime + select regimeChoisi (si recettes < SEUIL) ou texte "Régime réel forcé (recettes > 83 600 €)" + CTA "Suivant".
      - etape-4.ejs : currentStep=4 + récapitulatif final lecture-seule + CTA "Suivant" (passe à étape 5 = submission).
      - etape-5.ejs : currentStep=5 + `<form method="POST" action="/fiscalite/cloturer/{exercice}">` avec hidden regimeChoisi + bouton "Clôturer l'exercice {exercice}" en accent (CTA primaire). UI-SPEC §S8 L189-190.

    Récap annuel `recap-annuel.ejs` :
      - include layout-debut(titre: 'Déclaration fiscale ' + exercice, breadcrumbs, navActive: 'fiscalite')
      - include partial-verdict-fiscal({ statut: declaration.statutLmnpLmp, annee: declaration.exercice }) (Plan 05)
      - <section> avec data-table montants snapshot (Recettes / Charges / Dotation / Résultat / Régime appliqué)
      - <a href="/biens/{...}/fiscalite/amortissement?annee={exercice}">Voir le tableau d'amortissement</a> (Plan 04)
      - <section>"Exports" boutons placeholder (Plan 07 wirera) : <button disabled>Télécharger CSV</button> + <button disabled>Télécharger PDF</button>
      - <a href="/fiscalite/declarations/{id}/corriger">Créer une déclaration corrigée</a>
      - include layout-fin

    Page declaration-corrigee.ejs :
      - Bandeau warning : "La déclaration originale reste intacte. Cette correction crée un nouvel enregistrement." (UI-SPEC §S10 L200-202)
      - <form method="POST"> avec <textarea name=motif required> + N champs optionnels pour corrections (recettesEuros, etc.) + bouton "Créer la déclaration corrigée"

    Partials :
      - wizard-fiscalite-layout.ejs (pattern wizard-irl-layout.ejs L1-10) : <nav aria-label="Étapes de la clôture fiscale"><ol> avec 5 <li> dont 1 a aria-current="step" (currentStep) + libellés des 5 étapes.
      - partial-prerequis-cloture.ejs : <ul> avec items (each { ok: boolean, label: string, lienUrl?: string }) ; chaque item rendu <li><span aria-hidden="true">{ok?'✓':'✕'}</span> {label} {lienUrl ? '(<a href=lienUrl>résoudre</a>)' : ''}</li>. Couleur ok=success / non-ok=destructive. Pattern partial-inventaire-warnings.ejs.
      - partial-comparatif-regime.ejs : <table aria-label="Comparatif micro-BIC vs réel"> 2 colonnes + 4 lignes (Recettes, Abattement/Charges, Amortissement, Résultat fiscal). Note pédagogique sous le tableau citant D-FIS-G4.3 ("Choix libre tant que < seuil ; option réel renouvelable 1 an tacitement").

    Wiring src/main.ts :
      - import cloturerExercice + collecterPrerequisCloture + creerDeclarationCorrigee + nouveaux repos
      - appeler registerFiscaliteClotureRoutes(app, { ... bailleurRepo, justificatifRepo, ticketRepo, recettesRepo, chargesRepo, composantRepo, valorisationRepo, declRepo, declCorrRepo, tableauAmortRepo, bienRepo, clock, regleFiscale, db })
      - appeler registerFiscaliteRacineRoute(app, { ... })

    Steps BDD `fiscalite-cloture.steps.ts` (init in-memory pattern enc02.steps.ts) :
      - Given 'un Bailleur avec valorisation fiscale activée + composants + recettes annuelles {N} € pour exercice {N}' → fixtures combinées (Plan 01 + Plan 03)
      - When 'le bailleur clôture l\'exercice {N}' → POST simulation OR appel direct cloturerExercice
      - When 'le bailleur clôture l\'exercice {N} en régime "{micro_bic|reel}"' → variant avec override
      - Then 'la déclaration {N} a regime_applique = "{regime}"' → assert via declRepo
      - Then 'la déclaration {N} a recettes_totales = {N} centimes' → assert
      - Then 'la déclaration {N} a verdict = "{lmnp_confirme|lmp_probable|indetermine}"' → assert
      - Then 'la clôture est refusée car {N} justificatif(s) non qualifié(s)' → assert PrerequisCloturalNonSatisfaits + count check
      - Then 'la deuxième clôture lance DeclarationDejaExiste' → assert
      - Then 'le snapshot reste inchangé après annulation d\'un encaissement' → asserter recettesTotales reste identique

    Features BDD `tests/bdd/features/fiscalite-cloture.feature` + `tests/bdd/features/fiscalite-declaration-corrigee.feature` :
      - Couvrent CONTEXT.md L249-252 cas limites obligatoires + anti-sticky + N corrections successives.
  </action>
  <verify>
    <automated>pnpm test -- tests/bdd && pnpm typecheck && pnpm lint:deps</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier src/web/routes/fiscalite/cloture.ts existe et expose au moins 8 endpoints (5 GET étapes + POST clôture + GET récap + GET/POST corriger).
    - Le fichier src/web/routes/fiscalite/racine.ts existe et expose GET /fiscalite.
    - 5 fichiers etape-N.ejs existent dans src/web/views/pages/fiscalite/wizard-cloture/.
    - Le fichier src/web/views/pages/fiscalite/recap-annuel.ejs existe et include partial-verdict-fiscal.
    - 3 partials créés : wizard-fiscalite-layout.ejs (contient `aria-current="step"`), partial-prerequis-cloture.ejs (contient `aria-hidden="true"` sur icône), partial-comparatif-regime.ejs (contient `<table aria-label`).
    - `grep -c "registerFiscaliteCloture\\|registerFiscaliteRacine\\|/fiscalite/cloturer\\|/fiscalite/declarations" src/main.ts` retourne au moins 2.
    - `pnpm test -- tests/bdd` exits 0 — scénarios @fis-cloture (au moins 6) + @fis-declaration-corrigee (au moins 2) verts.
    - `pnpm typecheck` exits 0.
    - `pnpm lint:deps` exits 0.
    - Tests intégration : snapshot inchangé après soft-delete encaissement post-clôture (cas L251) vert.
    - Anti-sticky 3 exercices indépendants (cas L252) vert dans BDD.
  </acceptance_criteria>
  <done>
    Wizard S8 5 étapes fonctionnel. Route GET /fiscalite affiche bandeau verdict +
    liste déclarations + CTA Clôturer. Récap post-clôture avec liens drill-down
    vers tableau d'amortissement (Plan 04). Formulaire DeclarationCorrigee
    livré. Couverture BDD complète @fis-cloture + @fis-declaration-corrigee.
    L'utilisateur peut clôturer un exercice et créer une déclaration corrigée.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| POST /fiscalite/cloturer/:exercice | Body utilisateur + URL param exercice → validation Zod stricte. |
| DeclarationAnnuelle.creer | Invariants : revenusFoyerSnapshot REQUIS si recettes > 23k€, regime='reel' ⇒ snapshot non vide. |
| Append-only adapters | Aucun adapter ne contient onConflict. UNIQUE bailleur_id+exercice protège double clôture. |
| Transaction Kysely | DeclarationAnnuelle + AmortissementExercice batch dans la même transaction (atomicité). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering | Double clôture race | mitigate | UNIQUE (bailleur_id, exercice) côté DB + lookup préalable dans use case → DeclarationDejaExiste thrown. |
| T-05-06-02 | Tampering | Modification post-clôture | mitigate | Agrégat append-only strict (cf. acceptance criteria : pas de onConflict, pas de méthode mutator sur DeclarationAnnuelle). Correction → DeclarationCorrigee (nouvelle ligne). |
| T-05-06-03 | Tampering | Suppression encaissement post-clôture corrompt snapshot | mitigate | Snapshot par VALEUR (D-FIS-G4.2) : composants_snapshot_json + recettesTotales figés. Test BDD prouve immutabilité (cas L251). |
| T-05-06-04 | Information Disclosure | Erreurs internes exposées | accept | extraireErreurs pattern Phase 2 — formattage propre des chemins Zod. Pas de stack trace. |
| T-05-06-05 | Repudiation | Clôture sans audit-trail | mitigate | clotureLe (PlainDate) sur DeclarationAnnuelle + creeLe sur DeclarationCorrigee → audit-trail intégré. |
| T-05-06-06 | DoS | POST cloturer avec body énorme | mitigate | Zod max 2000 chars sur motif + coerce.number sur exercice bornée. |
| T-05-06-07 | Tampering | Transaction Kysely incomplete | mitigate | db.transaction().execute() englobe DeclarationAnnuelle.enregistrer + tableauAmortRepo.enregistrerBatch. Si l'un échoue, ROLLBACK total. |
| T-05-06-08 | Elevation of Privilege | Override regimeChoisi pour échapper réel forcé | mitigate | choisirRegime use case pur force 'reel' si recettes > SEUIL — override 'micro_bic' ignoré dans ce cas. Test BDD prouve cas limite. |
| T-05-06-09 | Tampering | DeclarationCorrigee modifie originale | mitigate | creer-declaration-corrigee NE MET PAS À JOUR declOriginale. Tests vérifient declOriginale.recettesTotales reste inchangée. |
</threat_model>

<verification>
1. `pnpm db:migrate` (idempotent)
2. `pnpm typecheck` → 0 erreur
3. `pnpm test -- tests/unit/fiscalite tests/integration/fiscalite tests/integration/repositories/declaration-annuelle-repository-sqlite.test.ts tests/bdd` → tous verts
4. `pnpm lint:deps` → 0 erreur
5. `! grep -q "onConflict" src/infrastructure/repositories/declaration-annuelle-repository-sqlite.ts src/infrastructure/repositories/declaration-corrigee-repository-sqlite.ts` (append-only strict double-check)
6. Manual : navigation /fiscalite → /fiscalite/cloturer/2026/etape/1 → 2 → 3 → 4 → 5 (POST) → /fiscalite/declarations/:id
7. Citations doctrinaires : CGI 39, 39 B, 50-0, 155 IV, LF 2025 art. 84, BOFIP-BIC-DECLA-10-30, D-FIS-G4.1..G4.4 dans tests & JSDoc
</verification>

<success_criteria>
- Migrations 0016 + 0017 appliquées (UNIQUE bailleur+exercice ; pas d'UNIQUE sur DeclarationCorrigee)
- DeclarationAnnuelle + DeclarationCorrigee agrégats append-only stricts (no mutator, no onConflict)
- Use case cloturer-exercice orchestre tous les use cases purs en 1 transaction
- Wizard S8 5 étapes fonctionnel avec aria-current=step
- Récap annuel avec bandeau verdict S7 + liens drill-down vers tableau d'amortissement
- Page racine /fiscalite avec verdict tri-état + liste déclarations + CTA Clôturer
- DeclarationCorrigee : originale intouchée, N corrections successives
- Snapshot immuable vérifié en intégration (cas L251 soft-delete post-clôture)
- Anti-sticky LMP 3 exercices indépendants vert en BDD (cas L252)
- L'utilisateur peut clôturer un exercice fiscal complet en mode autonome
</success_criteria>

<output>
Créer .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-06-SUMMARY.md
en suivant @.claude/get-shit-done/templates/summary.md.
</output>
</content>
</invoke>
