---
phase: 05
plan: 03
plan_id: "05-03"
type: execute
wave: 3
depends_on: ["05-01"]
files_modified:
  - migrations/0018_phase5_composant.sql
  - migrations/0020_phase5_valorisation_fiscale.sql
  - src/infrastructure/db/kysely-types.ts
  - src/domain/fiscalite/composant.ts
  - src/domain/fiscalite/valorisation-fiscale.ts
  - src/domain/fiscalite/composant-repository.ts
  - src/domain/patrimoine/bien.ts
  - src/infrastructure/repositories/composant-repository-sqlite.ts
  - src/infrastructure/repositories/bien-repository-sqlite.ts
  - src/application/fiscalite/activer-fiscalite-bien.ts
  - src/application/fiscalite/repartir-frais-acquisition.ts
  - src/web/schemas/fiscalite-schemas.ts
  - src/web/routes/fiscalite/composants.ts
  - src/web/views/pages/fiscalite/activer-fiscalite.ejs
  - src/web/views/partials/partial-composant-row.ejs
  - src/main.ts
  - tests/_builders/fiscalite.ts
  - tests/unit/fiscalite/composant.test.ts
  - tests/unit/fiscalite/valorisation-fiscale.test.ts
  - tests/unit/fiscalite/repartir-frais-acquisition.test.ts
  - tests/unit/fiscalite/activer-fiscalite-bien.test.ts
  - tests/integration/repositories/composant-repository-sqlite.test.ts
  - tests/bdd/features/fiscalite-composant.feature
  - tests/bdd/features/fiscalite-frais-acquisition.feature
  - tests/bdd/step_definitions/fiscalite-composants.steps.ts
autonomous: true
requirements: ["FIS-04"]
user_setup: []

mvp_split_rationale: |
  Vertical slice : modélisation Composant (sub-aggregate de Bien) +
  ValorisationFiscale (VO 1-1 avec Bien) + écran S3 "Activer la fiscalité
  réelle". Livre la capacité utilisateur de saisir la valorisation initiale
  d'un bien (6 composants BOFIP + quote-part terrain libre + frais
  d'acquisition répartis au prorata). NE LIVRE PAS le calcul d'amortissement
  ni la vue tableau S4 (Plan 04). Splitté pour respecter la borne ~50 %
  contexte par plan : ce plan touche déjà 8 nouveaux fichiers domain/infra +
  l'écran S3 (formulaire complexe avec invariant Σ).

must_haves:
  truths:
    - "L'utilisateur peut accéder à GET /biens/:bienId/fiscalite/activer qui affiche le formulaire S3 (5-section <fieldset>) avec 6 lignes composant (terrain + 5 amortissables) + saisie quote-part terrain (slider 0-30 %) + 2 inputs frais acquisition (notaire + agence) + bouton 'Activer la fiscalité réelle' désactivé tant que Σ composants ≠ prix acquisition — D-FIS-G1.1, G1.4, G1.8."
    - "POST /biens/:bienId/fiscalite/activer crée 1 ValorisationFiscale + 6 Composant initiaux (1 par type BOFIP, montantHt = montant saisi pour amortissables ; terrain montantHt = prix acquisition × quotePartTerrainRatio ; chaque composant origineKind='initial'+ dateAcquisition unique = date saisie ou today si non saisie) + persiste en transaction Kysely + redirige vers vue détail bien avec bannière succès — D-FIS-G1.1 à G1.4."
    - "Composant est un sub-aggregate de Bien : porte `id: ComposantId`, `bienId: BienId`, `type: TypeComposantBofip (6 valeurs)`, `montantHt: Money`, `dateAcquisition: Temporal.PlainDate`, `origineKind: 'initial' | 'amelioration' | 'acquisition_mobilier'`, `ticketId: TicketTravauxId | null` (lien vers travaux amélioration/mobilier), `dateSortie: Temporal.PlainDate | null` (Plan 07), `motifSortie: 'vente'|'mise_au_rebut'|'sinistre'|'autre' | null`. La durée d'amortissement n'est PAS stockée — elle est dérivée de regleFiscale.pour(annee).DUREES_AMORTISSEMENT_ANS[type] (versionneable annuelle)."
    - "Composant invariants — D-FIS-G1.1, G1.5 : (a) type ∈ TYPES_COMPOSANT_BOFIP ; (b) montantHt strict positif sauf si type='terrain' avec quote-part 0 OK (terrain non amortissable accepte montantHt=0) ; (c) si origineKind != 'initial' ALORS ticketId NOT NULL ; (d) si dateSortie != null ALORS dateSortie >= dateAcquisition."
    - "ValorisationFiscale VO 1-1 avec Bien : `bienId: BienId`, `prixAcquisition: Money`, `dateAcquisition: Temporal.PlainDate`, `fraisNotaire: Money`, `fraisAgence: Money`, `quotePartTerrainRatio: number ∈ [0, 0.30]` (D-FIS-G1.8), `activeLe: Temporal.PlainDateTime`. Méthode `fraisAcquisitionTotal(): Money = fraisNotaire.additionner(fraisAgence)`."
    - "Use case pur `repartirFraisAcquisition({ composants: Composant[], fraisTotal: Money }): Map<ComposantId, Money>` : (1) filtre composants amortissables (type != 'terrain' AND dateSortie === null) ; (2) calcule Σ montantHt des amortissables ; (3) pour chaque composant amortissable : quotePart = fraisTotal × (composant.montantHt / Σ amortissables) via Money.multiplyByFraction ; (4) Le DERNIER composant (ordre stable : ['gros_oeuvre','toiture_facade','installations_techniques','agencements_interieurs','mobilier']) absorbe l'écart d'arrondi pour garantir Σ quote-part = fraisTotal — D-FIS-G1.3."
    - "Use case `activer-fiscalite-bien` (orchestration) : (1) lookup bien ; (2) refus si ValorisationFiscale déjà existe (BienDejaActifFiscalement) ; (3) calcule terrain.montantHt = prixAcquisition × quotePartTerrainRatio (Money.multiplyByFraction) ; (4) vérifie Σ composants = prixAcquisition ; (5) calcule frais répartition via repartirFraisAcquisition ; (6) MAJ montantHt de chaque composant amortissable (composantInitial.montantHt + sa quote-part frais) ; (7) transaction : enregistrer ValorisationFiscale + 6 Composants ; (8) retourne { valorisationId, composantIds } — D-FIS-G1.3, G1.4."
    - "Bien est étendu avec `valorisationFiscale: ValorisationFiscale | null` (informationnel — lookup via ValorisationFiscaleRepository pas via Bien.valorisationFiscale ; le champ peut être null pour signaler 'fiscalité non activée'). Les `composants[]` ne sont PAS chargés dans Bien par défaut (lazy load via ComposantRepository) pour éviter de casser les performances Phases 1-4 — D-FIS-G1.4."
    - "ComposantRepository (port) expose : enregistrer(c, trx?), enregistrerBatch(cs[], trx?), trouverParId(id), listerActifsParBien(bienId, today): Promise<Composant[]> (filtre dateSortie === null OR dateSortie > today), listerParBien(bienId): Promise<Composant[]> (tous), listerActifsPourBailleur(bailleurId, today): Promise<Composant[]> (JOIN bien)."
    - "ValorisationFiscaleRepository (port) : enregistrer(vf, trx?), trouverParBien(bienId): Promise<ValorisationFiscale | null>."
    - "L'écran S3 (UI-SPEC §S3 L118-127) : (1) form en colonne unique ; (2) <fieldset> 'Prix d'acquisition' avec inputs prixAcquisition / fraisNotaire / fraisAgence ; (3) <fieldset> 'Répartition par composant' avec 5 inputs montant HT (gros_oeuvre / toiture_facade / installations_techniques / agencements_interieurs / mobilier — terrain calculé via slider) ; (4) <fieldset> 'Mobilier initial' (déjà couvert par mobilier dans répartition) ; (5) <fieldset> 'Quote-part terrain' avec slider + input numérique + tooltip BOFIP (texte courte) ; (6) Bouton 'Activer la fiscalité réelle' avec attribut disabled si Σ ≠ prix (validation côté serveur après POST + côté client minimal sans framework — re-render avec erreur)."
    - "Bandeau erreur invariant Σ : si Σ composants ≠ prixAcquisition (après ajout terrain calculé via slider), la POST renvoie 400 + re-affiche le formulaire avec ComposantsSommeIncoherente.attendu vs obtenu en `--couleur-destructive` ; l'utilisateur corrige et re-soumet — D-FIS-G1.1."
    - "Couverture BDD @fis-04 @phase5 : (a) activation OK avec prix 216_000 € (notaire 16k + agence 8k = 24k frais) → 6 composants persistés ; (b) cas limite G1.3 acquisition 200k € gros œuvre + 16k frais notaire + 8k frais agence → 24k frais répartis prorata avec dernier composant absorbant l'arrondi ; (c) cas limite G1.8 quote-part 30 % accepté, 30.01 % refusé ; (d) tentative d'activation 2x → BienDejaActifFiscalement."
  artifacts:
    - path: "migrations/0018_phase5_composant.sql"
      provides: "Table bien_composant (FK bien_id, type, montant_ht_centimes, date_acquisition, duree_amortissement_ans, origine_kind, ticket_id, date_sortie, motif_sortie)"
      contains: "CREATE TABLE IF NOT EXISTS bien_composant"
    - path: "migrations/0020_phase5_valorisation_fiscale.sql"
      provides: "Table bien_valorisation_fiscale (1-1 avec bien : prix_acquisition_centimes, frais_notaire, frais_agence, quote_part_terrain_ratio, active_le)"
      contains: "CREATE TABLE IF NOT EXISTS bien_valorisation_fiscale"
    - path: "src/domain/fiscalite/composant.ts"
      provides: "Sub-aggregate Composant + factory creer + invariants + sortir() (Plan 07) — VNC calculée par méthode pure"
      exports: ["Composant", "TypeComposantBofip", "OrigineKindComposant", "MotifSortieComposant"]
    - path: "src/domain/fiscalite/valorisation-fiscale.ts"
      provides: "VO ValorisationFiscale (prix + frais + quote-part terrain)"
      exports: ["ValorisationFiscale"]
    - path: "src/domain/fiscalite/composant-repository.ts"
      provides: "Port ComposantRepository + ValorisationFiscaleRepository"
      exports: ["ComposantRepository", "ValorisationFiscaleRepository"]
    - path: "src/application/fiscalite/repartir-frais-acquisition.ts"
      provides: "Use case pur prorata BOFIP-BIC-AMT-10-20 §110 + écart arrondi dernier composant"
      exports: ["repartirFraisAcquisition"]
    - path: "src/application/fiscalite/activer-fiscalite-bien.ts"
      provides: "Use case orchestration : crée ValorisationFiscale + 6 Composants en 1 transaction"
      exports: ["activerFiscaliteBien", "BienDejaActifFiscalement"]
    - path: "src/web/routes/fiscalite/composants.ts"
      provides: "Routes GET/POST /biens/:bienId/fiscalite/activer"
      contains: "activer-fiscalite"
    - path: "src/web/views/pages/fiscalite/activer-fiscalite.ejs"
      provides: "Formulaire S3 — UI-SPEC §S3"
      contains: "Quote-part terrain"
  key_links:
    - from: "POST /biens/:bienId/fiscalite/activer"
      to: "activer-fiscalite-bien use case → repartir-frais-acquisition + transaction Kysely"
      via: "Fastify route avec safeParse Zod"
      pattern: "activerFiscaliteBien|repartirFraisAcquisition"
    - from: "Bien (domain)"
      to: "Composant (sub-aggregate)"
      via: "ComposantRepository.listerActifsParBien(bienId, today) — pas chargé dans Bien.creer"
      pattern: "listerActifsParBien"
    - from: "regleFiscale Plan 01"
      to: "Composant.dureeAmortissementAns(type, regles)"
      via: "Méthode pure du Composant : `static dureeAmortissementAns(type, regles): number`"
      pattern: "DUREES_AMORTISSEMENT_ANS"

tags: [fiscalite, lmnp, composant, valorisation-fiscale, fis-04-partial, ddd, vertical-slice]
---

<objective>
Livrer la slice de modélisation fiscale par composants : Composant sub-aggregate
+ ValorisationFiscale VO + écran S3 "Activer la fiscalité réelle". Après ce
plan, l'utilisateur peut saisir la valorisation initiale d'un bien (6 composants
BOFIP, frais d'acquisition prorata, quote-part terrain libre 0-30 %) et la
persister. Le calcul d'amortissement et la vue tableau S4 suivent Plan 04.

Purpose : FIS-04 (partiel — la structure et la persistance). Le calcul est
volumineux (prorata jour près + ARD + plafond résultat) — il méritera son
propre plan TDD (Plan 04). Ici on fonde le modèle persistent.

Output :
  - 2 migrations SQL (0018 composant + 0020 valorisation-fiscale)
  - Composant + ValorisationFiscale agrégats/VO domain
  - 2 ports + 2 adapters SQLite (composant + valorisation)
  - 2 use cases (repartir-frais-acquisition pur + activer-fiscalite-bien orchestration)
  - Page S3 fonctionnelle (formulaire complexe avec invariant Σ)
  - Couverture BDD @fis-04 (activation + prorata frais + quote-part bornes + idempotence)
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-CONTEXT.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-PATTERNS.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-UI-SPEC.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-01-PLAN.md

# Sources canoniques (CITER en JSDoc + tests BDD)
@LMNP.md
@practices/BDD_PRACTICES.md
@practices/DDD.md

# Patterns à répliquer
@src/domain/documents/justificatif.ts
@src/domain/patrimoine/bien.ts
@src/domain/patrimoine/lot.ts
@src/domain/_shared/money.ts
@src/domain/_shared/irl.ts
@src/infrastructure/repositories/justificatif-repository-sqlite.ts
@src/infrastructure/repositories/bien-repository-sqlite.ts
@src/application/encaissements/creer-encaissement.ts
@src/application/locatif/appliquer-indexation-irl.ts
@src/web/routes/baux.ts
@src/web/views/pages/baux/formulaire.ejs
@migrations/0010_phase4_documents_travaux.sql
@migrations/0009_phase3_bail_indexations.sql
</context>

<interfaces>
From src/domain/fiscalite/regles/regles-2026.ts (Plan 01) :
```typescript
export const REGLES_2026 = {
  DUREES_AMORTISSEMENT_ANS: {
    terrain: 0,
    gros_oeuvre: 40,
    toiture_facade: 25,
    installations_techniques: 20,
    agencements_interieurs: 15,
    mobilier: 7,
  },
} as const;
export type TypeComposantBofip = keyof typeof REGLES_2026.DUREES_AMORTISSEMENT_ANS;
```

From src/domain/_shared/money.ts :
```typescript
class Money {
  multiplyByFraction(num: bigint, den: bigint, mode?: 'banker'|'floor'|'ceil'): Money;
  multiplyByRatio(num: bigint, den: bigint, mode?: 'banker'|'floor'|'ceil'): Money;
  additionner(other: Money): Money;
  soustraire(other: Money): Money;
  egale(other: Money): boolean;
  toSqliteInteger(): number;
}
```

From src/domain/documents/justificatif.ts (pattern factory + invariants + toProps) :
```typescript
class Justificatif {
  static creer(props: JustificatifProps): Justificatif;
  toProps(): JustificatifProps;
  mettreEnCorbeille(raison, today): Justificatif;  // copy-on-write
}
```

From src/domain/_shared/identifiants.ts (Plan 01) :
```typescript
export type ComposantId = string & { readonly __brand: 'ComposantId' };
export type ValorisationFiscaleId = string & { readonly __brand: 'ValorisationFiscaleId' };
export function nouveauComposantId(): ComposantId;
export function nouveauValorisationFiscaleId(): ValorisationFiscaleId;
```

From src/domain/fiscalite/erreurs.ts (Plan 01) :
```typescript
export class ComposantsSommeIncoherente extends Error {
  constructor(public readonly attendu: Money, public readonly obtenu: Money);
}
```
</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Domaine — Composant + ValorisationFiscale + ports + repartir-frais-acquisition use case (TDD pur)</name>
  <files>
    src/domain/fiscalite/composant.ts,
    src/domain/fiscalite/valorisation-fiscale.ts,
    src/domain/fiscalite/composant-repository.ts,
    src/application/fiscalite/repartir-frais-acquisition.ts,
    tests/_builders/fiscalite.ts,
    tests/unit/fiscalite/composant.test.ts,
    tests/unit/fiscalite/valorisation-fiscale.test.ts,
    tests/unit/fiscalite/repartir-frais-acquisition.test.ts
  </files>
  <read_first>
    src/domain/documents/justificatif.ts,
    src/domain/patrimoine/lot.ts,
    src/domain/_shared/money.ts,
    src/domain/_shared/irl.ts,
    src/domain/fiscalite/regles/regles-2026.ts,
    src/domain/fiscalite/erreurs.ts,
    src/domain/_shared/identifiants.ts,
    .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-CONTEXT.md,
    .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-PATTERNS.md
  </read_first>
  <behavior>
    RED → GREEN → REFACTOR.

    Test 1 (RED) — composant.test.ts : `Composant.creer({ bienId, type: 'gros_oeuvre', montantHt: Money.fromEuros(200_000), dateAcquisition: PlainDate.from('2026-03-15'), origineKind: 'initial' })` retourne un Composant avec id (brand ComposantId) défini.

    Test 2 (RED) : `Composant.creer({ type: 'gros_oeuvre', montantHt: Money.zero(), ... })` throw InvariantViolated ('montantHt strict positif sauf terrain').

    Test 3 (RED) : `Composant.creer({ type: 'terrain', montantHt: Money.zero(), ... })` OK (terrain accepte 0 — non amortissable).

    Test 4 (RED) : `Composant.creer({ type: 'invalide' as any, ... })` throw InvariantViolated ('type ∈ TYPES_COMPOSANT_BOFIP').

    Test 5 (RED) : `Composant.creer({ origineKind: 'amelioration', ticketId: null, ... })` throw InvariantViolated ('ticketId NOT NULL si origineKind != initial').

    Test 6 (RED) : `Composant.creer({ origineKind: 'initial', ticketId: 'xxx' as TicketId, ... })` OK — ticketId optionnel pour initial.

    Test 7 (RED) : `Composant.creer({ dateAcquisition: '2026-03-15', dateSortie: '2026-01-01', ... })` throw InvariantViolated ('dateSortie >= dateAcquisition').

    Test 8 (RED) : `composant.dureeAmortissementAns(regles)` retourne 40 pour gros_oeuvre, 0 pour terrain.

    Test 9 (RED) : `composant.sortir(motif, dateSortie, today)` retourne nouveau Composant avec dateSortie set ; throw 'déjà sorti' si appelé deux fois.

    Test 10 (RED) — valorisation-fiscale.test.ts : `ValorisationFiscale.creer({ prixAcquisition: Money.fromEuros(216_000), fraisNotaire: Money.fromEuros(16_000), fraisAgence: Money.fromEuros(8_000), quotePartTerrainRatio: 0.10, dateAcquisition: PlainDate, bienId, activeLe: PlainDateTime })` retourne VO ; `fraisAcquisitionTotal()` retourne Money.fromEuros(24_000).

    Test 11 (RED) : `quotePartTerrainRatio = 0.30` OK (borne max).

    Test 12 (RED) : `quotePartTerrainRatio = 0.31` throw InvariantViolated ('quotePartTerrainRatio ∈ [0, 0.30]' — D-FIS-G1.8).

    Test 13 (RED) : `quotePartTerrainRatio = -0.01` throw.

    Test 14 (RED) — repartir-frais-acquisition.test.ts : "exemple G1.3" : 4 composants amortissables (gros_oeuvre 150k, toiture 30k, instal 10k, agencements 5k, mobilier 5k = Σ 200k) + frais_total 24k → quotes-parts proportionnelles avec dernier composant (mobilier ordre stable) absorbant l'arrondi. Σ quote-parts === frais_total = Money.fromEuros(24_000).

    Test 15 (RED) : "1 seul composant amortissable" → reçoit 100 % des frais.

    Test 16 (RED) : "aucun composant amortissable (que terrain)" → retourne Map vide (frais perdus côté caller). NOTE : ce cas est conceptuellement bizarre ; le caller (activer-fiscalite-bien Task 2) refusera cette config (au moins 1 amortissable requis).

    Test 17 (RED) : "Σ amortissables = 0" → throw InvariantViolated (division par zéro).

    GREEN — implémenter :
      - composant.ts : factory creer + invariants + dureeAmortissementAns(regles) + sortir(motif, dateSortie, today) + toProps()
      - valorisation-fiscale.ts : factory creer + invariants + fraisAcquisitionTotal()
      - composant-repository.ts : interfaces ComposantRepository + ValorisationFiscaleRepository (ports, pas d'implémentation)
      - repartir-frais-acquisition.ts : fonction pure ; ordre stable composants amortissables : ['gros_oeuvre', 'toiture_facade', 'installations_techniques', 'agencements_interieurs', 'mobilier'] ; dernier absorbe l'écart.

    REFACTOR — extraire constante TYPES_COMPOSANT_BOFIP (readonly array) ; constante ORDRE_COMPOSANTS_AMORTISSABLES ; constantes MOTIFS_SORTIE_COMPOSANT. Documenter chaque méthode publique avec JSDoc citant la décision D-FIS-G1.X.

    Builders fiscalite.ts (étendre — déjà créé en Plan 02 Task 1) :
      - `unComposantGrosOeuvre(overrides)`, `unComposantMobilier(overrides)`, `uneValorisationFiscale(overrides)`.
  </behavior>
  <action>
    Lire d'abord src/domain/documents/justificatif.ts pour mémoriser le pattern
    factory + invariants + toProps + copy-on-write.

    Créer src/domain/fiscalite/composant.ts :
      - Imports : Temporal, InvariantViolated, ComposantId/BienId/TicketTravauxId types + factories, Money, TypeComposantBofip/RegleFiscale2026.
      - Const exportées : TYPES_COMPOSANT_BOFIP, ORIGINES_KIND_COMPOSANT, MOTIFS_SORTIE_COMPOSANT.
      - Interface ComposantProps avec tous les champs (cf. must_haves truth 3) + propsOptionnels (id, ticketId, dateSortie, motifSortie).
      - Classe Composant avec readonly champs + constructor privé + factory `static creer(props)` avec invariants (cf. behavior tests).
      - Méthode `dureeAmortissementAns(regles: RegleFiscale2026): number` qui retourne regles.DUREES_AMORTISSEMENT_ANS[this.type].
      - Méthode `sortir(motif: MotifSortieComposant, dateSortie: Temporal.PlainDate): Composant` copy-on-write ; throw 'déjà sorti' si this.dateSortie !== null.
      - Méthode `estAmortissable(): boolean` (type !== 'terrain' AND dateSortie === null).
      - Méthode `toProps(): ComposantProps`.

    Créer src/domain/fiscalite/valorisation-fiscale.ts :
      - VO immutable (pas de copy-on-write nécessaire — pure data).
      - Factory creer avec invariants : prix > 0, fraisNotaire/agence >= 0, quotePartTerrainRatio ∈ [0, 0.30].
      - Méthode fraisAcquisitionTotal(): Money.

    Créer src/domain/fiscalite/composant-repository.ts :
      - Interface ComposantRepository (méthodes définies dans truth 8 ci-dessus).
      - Interface ValorisationFiscaleRepository (méthodes définies dans truth 9).
      - Aucune implémentation — Task 2 implémente SQLite adapter.

    Créer src/application/fiscalite/repartir-frais-acquisition.ts :
      - Fonction pure `repartirFraisAcquisition(input: { composants: Composant[], fraisTotal: Money }): Map<ComposantId, Money>`
      - (1) filter amortissables = composants.filter(c => c.estAmortissable()) — préserver l'ordre d'entrée puis trier par ORDRE_COMPOSANTS_AMORTISSABLES
      - (2) si amortissables.length === 0 OR Σ amortissables.montantHt === 0 → return new Map() (caller décide quoi faire — activer-fiscalite-bien refuse)
      - (3) sommeAmortissable = amortissables.reduce((acc, c) => acc.additionner(c.montantHt), Money.zero())
      - (4) Pour chaque amortissable sauf le dernier : quotePart = fraisTotal.multiplyByFraction(c.montantHt.toCentimes(), sommeAmortissable.toCentimes()) — banker's rounding par défaut.
      - (5) Le dernier composant (ordre stable) reçoit : fraisTotal − Σ quote-parts précédentes (absorbe l'arrondi). Garantit Σ === fraisTotal exact au centime.
      - (6) Retourner Map<ComposantId, Money>.

    Tests unit minimaux pour chaque truth ; couvrir tous les cas limites des
    behavior tests 1..17. Pour test 14 (G1.3 exemple), affirmer : (a) Map.size === 5 ; (b) Σ valeurs === Money.fromEuros(24_000) exact ; (c) gros_oeuvre reçoit 24k × 150k/200k = 18k ; toiture 24k × 30k/200k = 3.6k ; etc. ; mobilier absorbe l'arrondi.

    Étendre tests/_builders/fiscalite.ts avec unComposantGrosOeuvre +
    uneValorisationFiscale (pattern tests/_builders/encaissements.ts).
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/fiscalite/composant.test.ts tests/unit/fiscalite/valorisation-fiscale.test.ts tests/unit/fiscalite/repartir-frais-acquisition.test.ts && pnpm lint:deps</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- tests/unit/fiscalite/composant.test.ts` exits 0 — au moins 9 cas (creer, montantHt zéro refusé sauf terrain, type invalide, origine != initial requiert ticketId, dateSortie >= dateAcquisition, dureeAmortissementAns, sortir, estAmortissable, toProps).
    - `pnpm test -- tests/unit/fiscalite/valorisation-fiscale.test.ts` exits 0 — au moins 4 cas (creer OK, quotePart 0.30 OK, 0.31 throw, fraisAcquisitionTotal addition).
    - `pnpm test -- tests/unit/fiscalite/repartir-frais-acquisition.test.ts` exits 0 — au moins 5 cas (exemple G1.3 exact, 1 seul amortissable, aucun amortissable, Σ=0 throw, ordre stable).
    - `grep -c "TYPES_COMPOSANT_BOFIP" src/domain/fiscalite/composant.ts` retourne au moins 1.
    - `grep -c "ComposantsSommeIncoherente\\|InvariantViolated" src/domain/fiscalite/composant.ts` retourne au moins 1.
    - `grep -c "quotePartTerrainRatio" src/domain/fiscalite/valorisation-fiscale.ts` retourne au moins 1.
    - `grep -c "D-FIS-G1" src/domain/fiscalite/composant.ts src/domain/fiscalite/valorisation-fiscale.ts src/application/fiscalite/repartir-frais-acquisition.ts | wc -l` retourne au moins 3 (citations décisions dans JSDoc).
    - `pnpm lint:deps` exits 0 — pas d'import infra dans src/domain/ ni src/application/fiscalite/repartir-frais-acquisition.ts.
  </acceptance_criteria>
  <done>
    Composant + ValorisationFiscale + ports + use case pur repartir-frais-acquisition
    implémentés en TDD. 100 % couverture des cas limites G1.1, G1.3, G1.5, G1.6
    (prep), G1.8. Builders disponibles pour Plans 04+.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrations 0018+0020 + adapters SQLite Composant + ValorisationFiscale + kysely-types + activer-fiscalite-bien use case</name>
  <files>
    migrations/0018_phase5_composant.sql,
    migrations/0020_phase5_valorisation_fiscale.sql,
    src/infrastructure/db/kysely-types.ts,
    src/infrastructure/repositories/composant-repository-sqlite.ts,
    src/application/fiscalite/activer-fiscalite-bien.ts,
    tests/integration/repositories/composant-repository-sqlite.test.ts,
    tests/unit/fiscalite/activer-fiscalite-bien.test.ts,
    tests/bdd/features/fiscalite-composant.feature,
    tests/bdd/features/fiscalite-frais-acquisition.feature
  </files>
  <read_first>
    migrations/0010_phase4_documents_travaux.sql,
    migrations/0009_phase3_bail_indexations.sql,
    src/infrastructure/repositories/encaissement-repository-sqlite.ts,
    src/infrastructure/repositories/justificatif-repository-sqlite.ts,
    src/infrastructure/db/kysely-types.ts,
    src/application/locatif/appliquer-indexation-irl.ts,
    src/domain/fiscalite/composant.ts,
    src/domain/fiscalite/valorisation-fiscale.ts,
    src/domain/fiscalite/composant-repository.ts,
    src/application/fiscalite/repartir-frais-acquisition.ts,
    .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-PATTERNS.md
  </read_first>
  <behavior>
    Migration 0018_phase5_composant.sql crée bien_composant :
      id TEXT PRIMARY KEY,
      bien_id TEXT NOT NULL REFERENCES bien(id),
      type TEXT NOT NULL CHECK (type IN ('terrain','gros_oeuvre','toiture_facade','installations_techniques','agencements_interieurs','mobilier')),
      montant_ht_centimes INTEGER NOT NULL CHECK (montant_ht_centimes >= 0),
      date_acquisition TEXT NOT NULL,
      origine_kind TEXT NOT NULL CHECK (origine_kind IN ('initial','amelioration','acquisition_mobilier')),
      ticket_id TEXT NULL REFERENCES tickets_travaux(id),
      date_sortie TEXT NULL,
      motif_sortie TEXT NULL CHECK (motif_sortie IS NULL OR motif_sortie IN ('vente','mise_au_rebut','sinistre','autre')),
      cree_le TEXT NOT NULL DEFAULT (datetime('now'))
    + index idx_bien_composant_bien (bien_id) WHERE date_sortie IS NULL ; + index idx_bien_composant_actifs (bien_id, date_sortie).

    Migration 0020_phase5_valorisation_fiscale.sql crée bien_valorisation_fiscale (1-1 avec bien) :
      id TEXT PRIMARY KEY,
      bien_id TEXT NOT NULL UNIQUE REFERENCES bien(id),
      prix_acquisition_centimes INTEGER NOT NULL CHECK (prix_acquisition_centimes > 0),
      date_acquisition TEXT NOT NULL,
      frais_notaire_centimes INTEGER NOT NULL CHECK (frais_notaire_centimes >= 0),
      frais_agence_centimes INTEGER NOT NULL CHECK (frais_agence_centimes >= 0),
      quote_part_terrain_ratio REAL NOT NULL CHECK (quote_part_terrain_ratio >= 0 AND quote_part_terrain_ratio <= 0.30),
      active_le TEXT NOT NULL

    kysely-types : étendre DB avec bien_composant: BienComposantTable + bien_valorisation_fiscale: BienValorisationFiscaleTable.

    Adapter `composant-repository-sqlite.ts` implémente les 2 ports (Composant + ValorisationFiscale) — peut être 1 classe `ComposantRepositorySqlite` qui implémente les deux interfaces, ou 2 classes. Préférer 2 classes pour SOLID (séparation).

    Pour ComposantRepositorySqlite :
      - enregistrer(c, trx?) : INSERT bien_composant + onConflict(id).doUpdate (pour gérer le pattern sortir() qui modifie dateSortie)
      - enregistrerBatch(cs[], trx) : itère + insertInto + values + execute (peut utiliser insertInto.values([...]) batch)
      - trouverParId(id) : SELECT + versDomaine
      - listerActifsParBien(bienId, today) : SELECT WHERE bien_id = ? AND (date_sortie IS NULL OR date_sortie > today.toString())
      - listerParBien(bienId) : SELECT WHERE bien_id = ?
      - listerActifsPourBailleur(bailleurId, today) : SELECT bien_composant bc JOIN bien b ON b.id = bc.bien_id WHERE (bc.date_sortie IS NULL OR bc.date_sortie > today) — D-LOCK-2 single-bailleur : pas de filtre bailleurId

    Pour ValorisationFiscaleRepositorySqlite :
      - enregistrer(vf, trx?) : INSERT bien_valorisation_fiscale (UNIQUE bien_id — si re-tentative, throw via DB ; le use case activer-fiscalite-bien gère via lookup préalable)
      - trouverParBien(bienId) : SELECT WHERE bien_id = ?

    Use case `activer-fiscalite-bien.ts` :
      - Signature : `activerFiscaliteBien(cmd: ActiverFiscaliteBienCommande, repos, clock, regleFiscale, db): Promise<ActiverFiscaliteBienResult>`
      - ActiverFiscaliteBienCommande : { bienId, prixAcquisition, dateAcquisition, fraisNotaire, fraisAgence, quotePartTerrainRatio, composantsAmortissables: Array<{ type, montantHt }> }
      - Étapes :
        (1) lookup bien via bienRepo.trouverParId → throw BienIntrouvable
        (2) lookup vf existante via valorisationRepo.trouverParBien → throw BienDejaActifFiscalement
        (3) terrainMontantHt = prixAcquisition × quotePartTerrainRatio (Money.multiplyByFraction avec ratio en BigInt : convertir 0.10 en (10n, 100n)) ; note : pour gérer ratio float → BigInt, utiliser Math.round(ratio * 10000) / 10000 et multiplyByFraction(rNum, 10000n)
        (4) construire 6 composants : 1 terrain + 5 amortissables passés en commande, tous origineKind='initial', dateAcquisition = cmd.dateAcquisition, ticketId=null
        (5) Σ composants.montantHt → vérifier === prixAcquisition (sinon throw ComposantsSommeIncoherente — réutiliser erreur Plan 01)
        (6) fraisTotal = fraisNotaire.additionner(fraisAgence)
        (7) repartition = repartirFraisAcquisition({ composants: 5 amortissables (sans terrain), fraisTotal })
        (8) Pour chaque amortissable : MAJ montantHt = montantHt + repartition.get(id) (nouveau Composant via creer)
        (9) ValorisationFiscale.creer({ bienId, prixAcquisition, dateAcquisition, fraisNotaire, fraisAgence, quotePartTerrainRatio, activeLe: PlainDateTime.from('now') })
        (10) transaction : valorisationRepo.enregistrer(vf, trx) ; composantRepo.enregistrerBatch([terrain, ...amortissablesAvecFrais], trx)
        (11) Retour : { valorisationId, composantIds: [...] }
      - Export erreur `BienDejaActifFiscalement extends Error`.

    Tests intégration `tests/integration/repositories/composant-repository-sqlite.test.ts` :
      - Setup in-memory + appliquerToutesMigrations
      - Insère 1 Bien (Phase 1) + 6 Composants + 1 ValorisationFiscale
      - Test 1 : enregistrerBatch + listerActifsParBien(bienId, today) retourne les 6 composants
      - Test 2 : sortir composant gros_oeuvre + enregistrer + listerActifsParBien après dateSortie retourne 5 composants
      - Test 3 : ValorisationFiscaleRepositorySqlite.trouverParBien retourne vf ; insertInto re-tentative throw (UNIQUE)

    Tests unit `tests/unit/fiscalite/activer-fiscalite-bien.test.ts` :
      - Stubs repos + clock + regleFiscale
      - Test 1 : cas G1.3 exact (prix 200k + frais 24k + 5 amortissables) → 6 composants persistés avec montantHt enrichi frais
      - Test 2 : Σ composants ≠ prix → throw ComposantsSommeIncoherente
      - Test 3 : 2e appel sur même bien → throw BienDejaActifFiscalement
      - Test 4 : quotePartTerrainRatio = 0.10 + prix 220k → terrain.montantHt = 22k ; 5 amortissables Σ = 198k ; total 220k OK

    Features BDD posées pour Task 3 :
      - fiscalite-composant.feature : @fis-04 @phase5 — 3 scénarios (création initiale, sortie composant, idempotence)
      - fiscalite-frais-acquisition.feature : @fis-04 @phase5 — 2 scénarios (cas G1.3 exemple + 1 seul amortissable reçoit tout)
  </behavior>
  <action>
    Lire migrations/0010_phase4_documents_travaux.sql + 0009_phase3_bail_indexations.sql
    pour cloner le pattern bracketed BEGIN/COMMIT + CREATE IF NOT EXISTS + index
    partiels.

    Créer 0018_phase5_composant.sql + 0020_phase5_valorisation_fiscale.sql avec
    le SQL exact ci-dessus. Commentaires en tête citant D-FIS-G1.1, G1.2, G1.5,
    G1.8 et BOFIP-BIC-AMT-20-40.

    Étendre src/infrastructure/db/kysely-types.ts (NE PAS reconstruire) :
      - Ajouter type BienComposantTable + BienValorisationFiscaleTable
      - Étendre interface DB avec ces 2 tables.

    Créer src/infrastructure/repositories/composant-repository-sqlite.ts :
      - 2 classes : ComposantRepositorySqlite (implements ComposantRepository) +
        ValorisationFiscaleRepositorySqlite (implements ValorisationFiscaleRepository)
      - Constructor : db: Kysely<DB>
      - Pattern versDomaine + versRow (cf. justificatif-repository-sqlite.ts)
      - Méthodes : cf. behavior ci-dessus
      - JOIN explicite pour listerActifsPourBailleur

    Créer src/application/fiscalite/activer-fiscalite-bien.ts (pattern src/application/locatif/appliquer-indexation-irl.ts L85-215) :
      - Étapes définies dans behavior section
      - Transaction Kysely englobante (db.transaction().execute)
      - Erreur BienDejaActifFiscalement exportée
      - JSDoc cite D-FIS-G1.1, G1.3, G1.4

    Tests intégration in-memory pattern enc02.steps.ts L71-79 :
      - new Database(':memory:')
      - Kysely instance + appliquerToutesMigrations(db, sqlite, MIGRATIONS_DIR)
      - Repos sous test + assertions

    Appliquer les migrations en local : `pnpm db:migrate` (idempotent).

    Compléter tests/_builders/fiscalite.ts avec `unBienComposantInitial(bienId, type, montant)` + `uneValorisationFiscaleEnregistree(bienId)`.

    Poser les fichiers features (sans steps — Task 3 wirera les steps + page S3).
  </action>
  <verify>
    <automated>pnpm db:migrate && pnpm test -- tests/integration/repositories/composant-repository-sqlite.test.ts tests/unit/fiscalite/activer-fiscalite-bien.test.ts && pnpm typecheck && pnpm lint:deps</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm db:migrate` exits 0 (migrations 0018 + 0020 appliquées).
    - `pnpm typecheck` exits 0.
    - `pnpm test -- tests/integration/repositories/composant-repository-sqlite.test.ts` exits 0 — au moins 3 cas verts.
    - `pnpm test -- tests/unit/fiscalite/activer-fiscalite-bien.test.ts` exits 0 — au moins 4 cas verts (cas G1.3, Σ incorrect, idempotence, quote-part terrain).
    - `grep -c "BienDejaActifFiscalement\\|ComposantsSommeIncoherente" src/application/fiscalite/activer-fiscalite-bien.ts` retourne au moins 2.
    - `grep -c "bien_composant\\|bien_valorisation_fiscale" src/infrastructure/db/kysely-types.ts` retourne au moins 2.
    - `pnpm lint:deps` exits 0 — hexagonal préservé.
    - `grep -c "transaction" src/application/fiscalite/activer-fiscalite-bien.ts` retourne au moins 1 (transaction Kysely englobante).
  </acceptance_criteria>
  <done>
    Migrations 0018 + 0020 appliquées + adapters SQLite Composant + ValorisationFiscale
    + use case activer-fiscalite-bien orchestrant tout en 1 transaction. Round-trip
    SQLite vérifié + cas G1.3 exemple BOFIP-BIC-AMT-10-20 §110 test BDD prêt.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route Fastify + page S3 "Activer fiscalité" + partial composant-row + steps BDD + main.ts wiring</name>
  <files>
    src/web/schemas/fiscalite-schemas.ts,
    src/web/routes/fiscalite/composants.ts,
    src/web/views/pages/fiscalite/activer-fiscalite.ejs,
    src/web/views/partials/partial-composant-row.ejs,
    src/main.ts,
    tests/bdd/step_definitions/fiscalite-composants.steps.ts
  </files>
  <read_first>
    src/web/routes/baux.ts,
    src/web/routes/encaissements.ts,
    src/web/routes/diagnostics.ts,
    src/web/schemas/encaissement-schemas.ts,
    src/web/views/pages/baux/formulaire.ejs,
    src/web/views/pages/biens/diagnostics/formulaire.ejs,
    src/web/views/partials/data-table.ejs,
    src/web/views/partials/form-field.ejs,
    src/web/views/partials/empty-state.ejs,
    src/main.ts,
    .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-UI-SPEC.md,
    src/application/fiscalite/activer-fiscalite-bien.ts
  </read_first>
  <action>
    Route Fastify `src/web/routes/fiscalite/composants.ts` :
      - GET /biens/:bienId/fiscalite/activer : lookup bien (404 si introuvable) ; lookup vf existante via valorisationRepo.trouverParBien(bienId) ; si vf existe → redirect /biens/:bienId/fiscalite (vue détail) ; sinon → render pages/fiscalite/activer-fiscalite.ejs avec bien + valeurs vides.
      - POST /biens/:bienId/fiscalite/activer : safeParse(activerFiscaliteSchema) ; appel activerFiscaliteBien use case ; gérer BienDejaActifFiscalement (redirect with banniereError) + ComposantsSommeIncoherente (400 + re-render formulaire avec erreur affichée en --couleur-destructive citant attendu vs obtenu) ; sinon banniereSuccess + redirect /biens/:bienId/fiscalite.

    Zod schema `activerFiscaliteSchema` :
      ```
      z.object({
        prixAcquisitionEuros: z.coerce.number().refine(n => n > 0),
        dateAcquisition: z.string().regex(/^\d{4}-\d{2}-\d{2}$/),
        fraisNotaireEuros: z.coerce.number().min(0),
        fraisAgenceEuros: z.coerce.number().min(0),
        quotePartTerrainRatio: z.coerce.number().min(0).max(0.30),
        composants: z.object({
          gros_oeuvre: z.coerce.number().min(0),
          toiture_facade: z.coerce.number().min(0),
          installations_techniques: z.coerce.number().min(0),
          agencements_interieurs: z.coerce.number().min(0),
          mobilier: z.coerce.number().min(0)
        })
      })
      ```

    Page S3 `pages/fiscalite/activer-fiscalite.ejs` (LIRE 05-UI-SPEC.md §S3 L118-127) :
      - include('layout-debut', { titre: 'Activer la fiscalité réelle — ' + bien.adresse.enLigne(), breadcrumbs: [...], navActive: 'fiscalite' })
      - <h1>Activer la fiscalité réelle pour <span>{adresse}</span></h1>
      - Texte d'intro : "Cette saisie est requise pour le régime réel ; le micro-BIC fonctionne sans valorisation." (D-FIS-G1.4)
      - <form method="POST" action="/biens/{bienId}/fiscalite/activer">
        <fieldset><legend>Prix d'acquisition</legend>
          <input type="number" step="0.01" name="prixAcquisitionEuros" required>
          <input type="date" name="dateAcquisition" required>
          <input type="number" step="0.01" name="fraisNotaireEuros">
          <input type="number" step="0.01" name="fraisAgenceEuros">
        </fieldset>
        <fieldset><legend>Quote-part terrain</legend>
          <input type="range" min="0" max="0.30" step="0.01" name="quotePartTerrainRatio" id="ratio-terrain">
          <input type="number" min="0" max="0.30" step="0.01" name="quotePartTerrainRatio" aria-controls="ratio-terrain">
          <details><summary>Aide</summary><p>Quote-part terrain entre 0 et 30 %. Le terrain n'est pas amortissable (BOFIP-BIC-AMT-20-40).</p></details>
        </fieldset>
        <fieldset><legend>Répartition par composant</legend>
          5x <% include partial-composant-row.ejs with composantType + montantInput %>
        </fieldset>
        <fieldset aria-label="Σ composants">
          <p>Σ composants saisis : {{Σ}} ; prix acquisition : {{prix}} ; <strong style="color: var(--couleur-destructive);" hidden id="erreur-somme">Σ ≠ prix — ajuster.</strong></p>
        </fieldset>
        <button type="submit" id="cta-activer">Activer la fiscalité réelle</button>
        <a href="/biens/{bienId}" class="secondary">Annuler</a>
      </form>
      - Bandeau succès/erreur via session si redirect
      - Si erreur ComposantsSommeIncoherente : afficher message "Le total des composants (X €) ne correspond pas au prix d'acquisition (Y €). Ajustez les montants avant d'activer." en --couleur-destructive — D-FIS-G1.1 (UI-SPEC §Copywriting Error state).
      - JS inline minimal (pas de framework) : addEventListener sur les inputs montant + ratio → recalcule Σ et toggle hidden sur #erreur-somme + disabled sur #cta-activer. Pas de validation côté client autoritaire — la source de vérité est le serveur (re-render avec erreur ou succès).
      - include('layout-fin')

    Partial `partial-composant-row.ejs` :
      - Variables : composantType (TypeComposantBofip), label (français), dureeAns (number), valeurInitiale (default '')
      - Rend : <label for="{type}-montant">{label} ({dureeAns} ans)</label><input type="number" step="0.01" min="0" name="composants[{type}]" id="{type}-montant" value="{valeur}">
      - aria-describedby si tooltip présent

    Wiring src/main.ts :
      - import ComposantRepositorySqlite + ValorisationFiscaleRepositorySqlite + activerFiscaliteBien
      - Wirer les nouveaux repos dans le DI bag avant registerFiscaliteCompoasntsRoutes
      - Appeler registerFiscaliteComposantsRoutes(app, { bienRepo, composantRepo, valorisationRepo, clock, regleFiscale, db })

    Steps BDD `tests/bdd/step_definitions/fiscalite-composants.steps.ts` :
      - Given 'un Bien sans valorisation fiscale' → insertion via bienRepo (réutiliser le bâtisseur Phase 1)
      - When 'le bailleur active la fiscalité réelle avec prix {N} € + frais notaire {N} € + frais agence {N} € + quote-part terrain {N} % + composants {gros_oeuvre, toiture, installations, agencements, mobilier}' → POST simulation inject Fastify OU appel direct du use case
      - Then '{N} composants sont persistés sur le bien {bien}' → assert via composantRepo.listerActifsParBien
      - Then 'la somme des montant_ht des composants amortissables = prix + frais — frais répartis correctement' → assert répartition cas G1.3
      - Then 'la deuxième tentative d\'activation lance BienDejaActifFiscalement' → assert error

    Compléter feature fiscalite-composant.feature + fiscalite-frais-acquisition.feature posées Task 2 avec les Scénario définis behaviour.
  </action>
  <verify>
    <automated>pnpm test -- tests/bdd && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier src/web/routes/fiscalite/composants.ts existe et expose au moins 2 routes (GET + POST /biens/:bienId/fiscalite/activer).
    - Le fichier src/web/views/pages/fiscalite/activer-fiscalite.ejs existe et contient `<form method="POST"` + 5 `<fieldset>` + `quotePartTerrainRatio` + au moins 5 partial-composant-row include calls.
    - Le fichier src/web/views/partials/partial-composant-row.ejs existe et a `aria-describedby` ou `<label for>` (a11y form labels).
    - `grep -c "registerFiscaliteComposants" src/main.ts` retourne au moins 1.
    - `pnpm test -- tests/bdd` exits 0 — scénarios @fis-04 (3 composant + 2 frais-acquisition) verts.
    - `pnpm typecheck` exits 0.
    - Le bandeau erreur Σ : `grep -c "Le total des composants" src/web/views/pages/fiscalite/activer-fiscalite.ejs` retourne au moins 1.
    - `grep -c "BOFIP\\|D-FIS-G1" src/web/views/pages/fiscalite/activer-fiscalite.ejs` retourne au moins 1 (citation doctrine dans tooltip).
  </acceptance_criteria>
  <done>
    Route + page S3 fonctionnelles. L'utilisateur peut activer la fiscalité d'un
    bien via le formulaire, voir le ratio terrain validé, la répartition des
    frais notaire+agence prorata, et l'invariant Σ avant validation. Couverture
    BDD @fis-04 (composant + frais-acquisition) verte.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form POST → activer-fiscalite-bien | Tous les montants sont en euros (input number) → coerce et validation Zod. Money construction côté serveur après safeParse. |
| Use case → transaction Kysely | 6 Composants + 1 ValorisationFiscale doivent être atomiques (tout ou rien). |
| Bien.composants[] lazy load | Pas chargé dans Bien.creer — lookup via repo dans use cases consommateurs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | POST activer-fiscalite double appel | mitigate | UNIQUE(bien_id) sur bien_valorisation_fiscale + lookup préalable dans use case lance BienDejaActifFiscalement. Idempotence DB. |
| T-05-03-02 | Tampering | Σ composants forgée côté client | mitigate | Validation côté serveur dans activer-fiscalite-bien → throw ComposantsSommeIncoherente. JS client purement informatif. |
| T-05-03-03 | Tampering | quotePartTerrainRatio négatif ou > 0.30 | mitigate | Zod schema .min(0).max(0.30) + invariant domain ValorisationFiscale.creer. Double défense. |
| T-05-03-04 | Tampering | frais acquisition + composants → overflow Money | mitigate | Money.toSqliteInteger throw si > MAX_SAFE_INTEGER (cf. money.ts:49-56). Pour 90 milliards € — irréaliste. |
| T-05-03-05 | Information Disclosure | Prix acquisition en clair | accept | Local-first single-user (D-LOCK-2). DB jamais exposée. |
| T-05-03-06 | Repudiation | Activation sans trace | mitigate | activeLe (PlainDateTime) snapshot dans ValorisationFiscale ; pas append-only mais immutable (1-1 bien, jamais modifié). Plan 07 ajoutera audit log si nécessaire. |
| T-05-03-07 | DoS | POST avec composants[0..N=1000] | mitigate | Zod schema fixe 5 composants (gros_oeuvre, toiture_facade, installations_techniques, agencements_interieurs, mobilier) — pas de répétition utilisateur possible. |
| T-05-03-08 | Tampering | Race POST activer + GET récap | accept | SQLite single-writer ; transaction garantit cohérence read-after-write côté SQLite (WAL mode si activé). |
</threat_model>

<verification>
1. `pnpm db:migrate` (idempotent)
2. `pnpm typecheck` → 0 erreur
3. `pnpm test -- tests/unit/fiscalite tests/integration/repositories/composant-repository-sqlite.test.ts tests/bdd` → verts
4. `pnpm lint:deps` → 0 erreur
5. Manual : curl GET /biens/{id}/fiscalite/activer renvoie 200 + formulaire ; POST avec corps valide crée 6 composants + 1 vf
6. Citations doctrinaires : BOFIP-BIC-AMT-20-40 + BOFIP-BIC-AMT-10-20 §110 + D-FIS-G1.1, G1.3, G1.4, G1.8 dans tests et JSDoc
</verification>

<success_criteria>
- 2 migrations Phase 5 appliquées (0018 composant, 0020 valorisation_fiscale)
- Composant + ValorisationFiscale + repos disponibles et round-trippables
- repartirFraisAcquisition cas G1.3 vert (Σ exact au centime)
- activer-fiscalite-bien orchestre tout en 1 transaction
- Page S3 fonctionnelle avec slider quote-part terrain + invariant Σ
- Erreur ComposantsSommeIncoherente UI : message exact + couleurs WCAG
- Couverture BDD @fis-04 (composant + frais-acquisition) verte
- L'utilisateur peut activer la fiscalité d'un bien et voir les 6 composants persistés
</success_criteria>

<output>
Créer .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-03-SUMMARY.md
en suivant @.claude/get-shit-done/templates/summary.md.
</output>
</content>
</invoke>