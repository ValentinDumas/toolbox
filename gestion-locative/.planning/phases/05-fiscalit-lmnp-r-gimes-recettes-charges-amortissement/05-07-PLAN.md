---
phase: 05
plan: 07
plan_id: "05-07"
type: execute
wave: 7
depends_on: ["05-03", "05-06"]
files_modified:
  - src/domain/fiscalite/composant.ts
  - src/application/fiscalite/sortir-composant.ts
  - src/application/fiscalite/exporter-csv-fiscal.ts
  - src/application/fiscalite/exporter-pdf-recap.ts
  - src/application/fiscalite/lister-vue-consolidee.ts
  - src/web/schemas/fiscalite-schemas.ts
  - src/web/routes/fiscalite/exports.ts
  - src/web/routes/fiscalite/composants.ts
  - src/web/routes/fiscalite/onboarding.ts
  - src/web/views/pages/fiscalite/vue-consolidee.ejs
  - src/web/views/pages/fiscalite/onboarding.ejs
  - src/web/views/pages/fiscalite/sortir-composant.ejs
  - src/web/views/partials/partial-onboarding-banner.ejs
  - src/infrastructure/pdf/recap-fiscal-doc-def.ts
  - src/main.ts
  - tests/unit/fiscalite/sortir-composant.test.ts
  - tests/unit/fiscalite/exporter-csv-fiscal.test.ts
  - tests/unit/fiscalite/lister-vue-consolidee.test.ts
  - tests/integration/fiscalite/exporter-pdf-recap.spec.ts
  - tests/bdd/features/fiscalite-multi-bien.feature
  - tests/bdd/features/fiscalite-sortie-composant.feature
  - tests/bdd/features/fiscalite-exports.feature
  - tests/bdd/step_definitions/fiscalite-multi-bien.steps.ts
autonomous: true
requirements: ["FIS-02", "FIS-03", "FIS-04"]
user_setup: []

mvp_split_rationale: |
  Vertical slice : zones grises G5 — vue consolidée multi-bien S12 (lecture
  agrégée des déclarations N par bailleur), sortie composant G5.2 (prorata +
  VNC conservée), exports CSV+PDF G5.3, onboarding fiscal progressif G5.4.
  Livre l'expérience finale du bailleur : il peut maintenant gérer plusieurs
  biens, retirer un composant en cours d'année, exporter ses données pour
  son expert-comptable et naviguer dans l'app via un onboarding doux.

must_haves:
  truths:
    - "Composant exposé une méthode `sortir(motif, dateSortie): Composant` (déjà spécifié Plan 03 Task 1 behavior test 9) — Plan 07 wire son use case applicatif sortir-composant + UI. Invariants : (a) déjà sorti → throw 'composant déjà sorti' ; (b) dateSortie >= dateAcquisition (déjà testé Plan 03) ; (c) si Bien soft-deleted (vente du bien entier) → tous les composants reçoivent même dateSortie (orchestration use case)."
    - "Use case `sortirComposant({ composantId, motif, dateSortie }, repos, clock): Promise<void>` : (1) lookup composant via composantRepo.trouverParId → null throw ComposantIntrouvable ; (2) composant.sortir(motif, dateSortie) → nouveau composant ; (3) composantRepo.enregistrer (upsert via onConflict pour modifier dateSortie). Le calcul d'amortissement Plan 04 inclut DÉJÀ le prorata jusqu'à dateSortie pour l'exercice de sortie + exclusion des exercices suivants (estActifPourExercice retourne false). Phase 5 ne calcule PAS la plus-value (D-LOCK-3) — Plan trace uniquement les amortissements gros œuvre cumulés via DeclarationAnnuelle composants_snapshot_json."
    - "Use case `listerVueConsolidee(bailleurId, exercice, repos): Promise<VueConsolideeBailleur>` : (1) lookup tous Biens actifs du bailleur via bienRepo.listerActifs() ; (2) pour chaque bien : recettes (via JOIN bail.bien — mais V1 single-bailleur = TOTAL recettes), charges qualifiées (par bien si bien_id présent sur Justificatif sinon proportionnel), dotation amortissement (composants du bien), résultat fiscal partiel ; (3) totaux consolidés Σ recettes / Σ charges / Σ dotation / Σ résultat ; (4) verdict LMP appliqué sur le TOTAL (D-LOCK-2 + D-FIS-G5.1 seuils sur consolidé). Retour : { biens: Array<{ bienId, adresse, recettes, charges, dotation, resultatFiscal, regime }>, totaux: { recettes, charges, dotation, resultatFiscal }, verdictLmp, anneeCourante }."
    - "Use case `exporterCsvFiscal({ declarationId }, repos): Promise<{ contenu: string, nomFichier: string }>` : (1) lookup DeclarationAnnuelle ; (2) compose CSV avec colonnes : Type ligne (recettes / charges_courantes / charges_entretien / charges_amelioration / dotation_amortissement / ard_consomme / ard_genere) ; Montant (€) ; Détail ; (3) header CSV : `Type;Montant en euros;Détail`. Le fichier est consommable par un expert-comptable (séparateur ;, encoding UTF-8 BOM, Excel-friendly). Nom fichier : declaration-fiscale-{exercice}.csv. Content-Disposition RFC 6266 attachment ; filename*=UTF-8''."
    - "Use case `exporterPdfRecap({ declarationId }, repos, pdfRenderer): Promise<Buffer>` : (1) lookup DeclarationAnnuelle + bailleur + tous biens du bailleur ; (2) construit docDef pdfmake (réutilise pattern src/infrastructure/pdf/avenant-irl-doc-def.ts — pdfmake déjà en dépendance Phases 2+3) ; (3) header (logo absent, identité bailleur) ; sections : verdict tri-état coloré, totaux par catégorie de charges, tableau d'amortissement par composant (lookup tableauAmortRepo.listerParBienExercice), comparatif micro vs réel (lookup MicroBicResult), pied avec date d'édition + clauses doctrinaires (CGI articles cités) ; (4) pdfRenderer.genererBuffer(docDef) ; nom fichier : recap-fiscal-{exercice}.pdf."
    - "Route GET /fiscalite/declarations/:id/exports/csv : appel exporterCsvFiscal + reply.header('Content-Type', 'text/csv; charset=utf-8') + reply.header('Content-Disposition', `attachment; filename*=UTF-8''declaration-fiscale-${exercice}.csv`) + reply.send(contenu). Route GET /fiscalite/declarations/:id/exports/pdf : appel exporterPdfRecap + reply.header('Content-Type', 'application/pdf') + Content-Disposition + reply.send(buffer)."
    - "Route GET /fiscalite/multi-bien?annee={N} : appel listerVueConsolidee + render pages/fiscalite/vue-consolidee.ejs avec tableau S12 (UI-SPEC §S12 L213-218) — 1 ligne par bien + ligne totaux en <tfoot> font-weight 600 + lien 'Voir le détail' par ligne."
    - "Route GET /biens/:bienId/fiscalite/composants/:composantId/sortir : render pages/fiscalite/sortir-composant.ejs (formulaire motif + dateSortie). POST → use case sortir-composant + redirect /biens/:bienId/fiscalite/amortissement."
    - "Route GET /fiscalite/onboarding (D-FIS-G5.4) : render pages/fiscalite/onboarding.ejs (UI-SPEC §S1 L101-108) avec 3 CTA 'Commencer' / 'Plus tard' / 'Tout ignorer' ; chacun trace bailleur.fiscalitePremierAcces via bailleurRepo.modifier (saisirRevenusFoyer existe déjà Plan 05 ; ici trace seule sans saisir). Bandeau onboarding `partial-onboarding-banner.ejs` affiché sur les pages Phase 1-4 SI bailleur.fiscalitePremierAcces === null (lazy lookup). Le partial est inclus dans sidebar-nav ou dans layout-debut conditionnellement."
    - "Couverture BDD @fis-multi-bien @phase5 (D-FIS-G5.1) : (a) bailleur avec 2 biens + recettes consolidées 90k € → vue consolidée affiche 2 lignes biens + ligne totaux + verdict appliqué sur 90k (> SEUIL_MICRO donc régime réel forcé sur le total) ; (b) seuil consolidé exact 83 599.99 € → micro éligible ; (c) seuil consolidé + 1 cent → réel forcé. Couverture @fis-sortie-composant : (d) composant gros_oeuvre sorti 2026-06-30 → dotation 2026 = annuité × 6/12 (prorata jusqu'à sortie) ; exercice 2027 le composant n'apparaît PLUS dans le tableau (estActif=false) ; VNC conservée dans composants_snapshot_json de DeclarationAnnuelle 2026 — D-FIS-G5.2. Couverture @fis-exports : (e) CSV téléchargé contient toutes les colonnes attendues + format UTF-8 BOM Excel-friendly ; (f) PDF téléchargé contient le verdict tri-état + tableau amortissement + signatures bailleur."
  artifacts:
    - path: "src/application/fiscalite/sortir-composant.ts"
      provides: "Use case : sortie composant (prorata + VNC conservée — préparation V1.1 SIM-02)"
      exports: ["sortirComposant", "ComposantIntrouvable"]
    - path: "src/application/fiscalite/exporter-csv-fiscal.ts"
      provides: "Use case export CSV expert-comptable (UTF-8 BOM, séparateur ;)"
      exports: ["exporterCsvFiscal"]
    - path: "src/application/fiscalite/exporter-pdf-recap.ts"
      provides: "Use case export PDF récap bailleur (pdfmake)"
      exports: ["exporterPdfRecap"]
    - path: "src/application/fiscalite/lister-vue-consolidee.ts"
      provides: "Use case vue agrégée multi-bien (seuils sur total consolidé)"
      exports: ["listerVueConsolidee", "VueConsolideeBailleur"]
    - path: "src/web/routes/fiscalite/exports.ts"
      provides: "Routes téléchargement CSV/PDF"
      contains: "Content-Disposition"
    - path: "src/web/routes/fiscalite/composants.ts"
      provides: "Route sortie composant (étend Plan 03)"
      contains: "sortir"
    - path: "src/web/routes/fiscalite/onboarding.ts"
      provides: "Route GET /fiscalite/onboarding (D-FIS-G5.4)"
      contains: "fiscalite_premier_acces"
    - path: "src/web/views/pages/fiscalite/vue-consolidee.ejs"
      provides: "Page S12 — vue consolidée multi-bien"
      contains: "data-table"
    - path: "src/web/views/pages/fiscalite/onboarding.ejs"
      provides: "Page S1 — Bienvenue dans Fiscalité LMNP"
      contains: "Commencer\\|Plus tard\\|Tout ignorer"
    - path: "src/infrastructure/pdf/recap-fiscal-doc-def.ts"
      provides: "DocDef pdfmake pour récap PDF fiscal"
      exports: ["construireRecapFiscal"]
  key_links:
    - from: "GET /fiscalite/declarations/:id/exports/csv"
      to: "exporterCsvFiscal use case"
      via: "Fastify route + Content-Disposition header"
      pattern: "exporterCsvFiscal"
    - from: "GET /fiscalite/multi-bien"
      to: "listerVueConsolidee → tous biens + déclarations agrégées"
      via: "JOIN bien + recettesRepo + chargesRepo"
      pattern: "listerVueConsolidee"
    - from: "Bandeau onboarding (sidebar-nav extension)"
      to: "bailleur.fiscalitePremierAcces lookup"
      via: "conditionnel dans layout-debut OU sidebar-nav"
      pattern: "fiscalitePremierAcces === null"

tags: [fiscalite, lmnp, multi-bien, sortie-composant, exports-csv-pdf, onboarding, g5]
---

<objective>
Livrer les capacités G5 finales : vue consolidée multi-bien S12 (seuils sur total),
sortie composant en cours d'exercice (prorata + VNC conservée pour SIM-02 V1.1),
exports CSV + PDF (expert-comptable + bailleur), onboarding progressif S1 (D-FIS-G5.4).
Après ce plan, le bailleur peut autonomement gérer plusieurs biens, retirer un
composant, exporter ses données fiscales pour son comptable et naviguer dans
l'app via un onboarding doux.

Purpose : D-FIS-G5.1 (multi-bien) + G5.2 (sortie composant) + G5.3 (exports) +
G5.4 (onboarding). Sans ces capacités, la phase reste limitée à 1 bien et
l'utilisateur ne peut pas extraire ses données pour son expert-comptable.

Output :
  - 4 use cases (sortir-composant, exporter-csv-fiscal, exporter-pdf-recap, lister-vue-consolidee)
  - 3 routes (exports, sortie composant, onboarding)
  - 3 pages (vue consolidée S12, onboarding S1, sortir composant)
  - 1 partial (onboarding-banner)
  - 1 docDef pdfmake (recap-fiscal)
  - Couverture BDD @fis-multi-bien + @fis-sortie-composant + @fis-exports
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-CONTEXT.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-PATTERNS.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-UI-SPEC.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-01-PLAN.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-03-PLAN.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-06-PLAN.md

# Patterns à répliquer
@src/infrastructure/pdf/avenant-irl-doc-def.ts
@src/application/encaissements/generer-quittance.ts
@src/web/routes/quittances.ts
@src/web/views/pages/biens/liste.ejs
@src/web/views/partials/data-table.ejs
@src/web/views/partials/empty-state.ejs
</context>

<interfaces>
From src/domain/fiscalite/composant.ts (Plan 03) :
```typescript
class Composant {
  readonly dateSortie: Temporal.PlainDate | null;
  readonly motifSortie: 'vente'|'mise_au_rebut'|'sinistre'|'autre' | null;
  sortir(motif, dateSortie: Temporal.PlainDate): Composant;
}
```

From src/domain/fiscalite/declaration-annuelle.ts (Plan 06) :
```typescript
class DeclarationAnnuelle {
  readonly composantsSnapshot: string;  // JSON serialized for VNC reconstruction
  readonly recettesTotales: Money;
  readonly statutLmnpLmp: VerdictLmp;
  // ...
}
```

From src/infrastructure/pdf/avenant-irl-doc-def.ts (pattern pdfmake) :
```typescript
export function construireAvenantIRL(bail, locataire, bailleur, ...): TDocDefinition;
```

From src/application/encaissements/generer-quittance.ts (pattern génération PDF buffer) :
```typescript
// PdfRenderer port + buffer + content-disposition
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Use cases sortir-composant + lister-vue-consolidee + tests</name>
  <files>
    src/application/fiscalite/sortir-composant.ts,
    src/application/fiscalite/lister-vue-consolidee.ts,
    src/web/schemas/fiscalite-schemas.ts,
    tests/unit/fiscalite/sortir-composant.test.ts,
    tests/unit/fiscalite/lister-vue-consolidee.test.ts,
    tests/bdd/features/fiscalite-multi-bien.feature,
    tests/bdd/features/fiscalite-sortie-composant.feature
  </files>
  <read_first>
    src/domain/fiscalite/composant.ts,
    src/domain/fiscalite/composant-repository.ts,
    src/domain/patrimoine/bien-repository.ts,
    src/domain/fiscalite/recettes-repository.ts,
    src/domain/fiscalite/charges-repository.ts,
    src/application/fiscalite/calculer-micro-bic.ts,
    src/application/fiscalite/detecter-bascule-lmp.ts,
    src/application/encaissements/creer-encaissement.ts,
    .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-CONTEXT.md,
    .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-UI-SPEC.md
  </read_first>
  <action>
    Tests RED → GREEN.

    sortir-composant.test.ts :
      Test 1 : composantRepo.trouverParId retourne null → throw ComposantIntrouvable.
      Test 2 : composant déjà sorti (composant.sortir lance erreur) → throw propagé.
      Test 3 : sortie OK (motif='vente', dateSortie=PlainDate) → composantRepo.enregistrer appelé avec nouvelle instance dont dateSortie set.

    lister-vue-consolidee.test.ts (stubs repos) :
      Test 4 : 0 biens actifs → retour { biens: [], totaux: zero, verdictLmp: 'lmnp_confirme', anneeCourante }.
      Test 5 : 2 biens + recettes 90k consolidées + revenus foyer 100k → verdictLmp='lmnp_confirme' (recettes < revenus foyer), seuil consolidé > 83.6k donc régime réel sur total. Vérifier que les 2 biens apparaissent avec leurs propres recettes/charges (pour V1 single-bailleur, recettes par bien = approx — sinon proportionnel sur baux).
      Test 6 : seuil exact 83 599.99 € consolidé → micro éligible.
      Test 7 : 1 bien + recettes 90k + revenus foyer 80k → verdictLmp='lmp_probable'.

    Schémas Zod :
      - sortirComposantSchema = z.object({ motif: z.enum(['vente','mise_au_rebut','sinistre','autre']), dateSortieIso: z.string().regex(/^\d{4}-\d{2}-\d{2}$/) })
      - exporterFiscalSchema (vide ou query.format) — pas nécessaire si export sur GET.

    Use case `sortirComposant({ composantId, motif, dateSortie }, repos: { composantRepo }, clock): Promise<void>` :
      - lookup composant
      - composant.sortir(motif, dateSortie)
      - composantRepo.enregistrer (upsert ; en SQLite ALTER UPDATE via versRow inclut maintenant dateSortie + motifSortie)
      - JSDoc cite D-FIS-G5.2 + LF 2025 art. 84 (réintégration gros œuvre dans PV future SIM-02).

    Use case `listerVueConsolidee(bailleurId, exercice, repos: { bienRepo, recettesRepo, chargesRepo, composantRepo, valorisationRepo, regleFiscale, bailleurRepo }, clock): Promise<VueConsolideeBailleur>` :
      - (1) bailleur = bailleurRepo.trouver() (singleton)
      - (2) biensActifs = bienRepo.listerActifs()
      - (3) Pour chaque bien :
        - recettesBien = recettesRepo.sommeRecettesAnnuelles(bailleurId, exercice) — note V1 single-bailleur : `recettesRepo.sommeRecettesAnnuelles` retourne le TOTAL sans filtre bien. Pour V1.1 multi-bien sur le bailleur : extension à `sommeRecettesAnnuellesParBien(bienId, exercice)` (TODO V1.1). En V1, on attribue ARBITRAIREMENT 100 % à chaque bien (cas atypique avec 1 seul bien réellement loué) OU on documente clairement la limitation : "V1 affiche recettes totales par bailleur ; ventilation par bien V1.1". Pour Plan 07 : afficher recettes au niveau bailleur en haut et 0 € par bien pour V1 — OU coupler à `sommeRecettesAnnuellesParBail` (extension future). DÉCISION V1 SIMPLE : afficher recettes totales bailleur + 1 ligne par bien avec recettes='—' et préciser dans une note "Ventilation par bien à venir V1.1 (besoin du multi-bailleur)". Le focus Plan 07 G5.1 est sur la cohérence des SEUILS sur le total — pas sur la ventilation parfaite par bien.
        - chargesBien (par bien) : chargesRepo extension `sommeChargesParBien(bienId, exercice)` (NOUVELLE méthode à ajouter au repo + adapter SQLite — colonne bien_id présent sur justificatifs)
        - composantsBien = composantRepo.listerActifsParBien(bienId, today)
        - dotationBien = calculée via recalculerTableauAmortissement (Plan 04) si valorisationFiscale présente, sinon 0
      - (4) totaux : Σ recettes (= total bailleur — pas Σ par bien en V1) ; Σ chargesParBien ; Σ dotationsParBien
      - (5) regimeApplique : recettesTotaux > SEUIL ? 'reel' : 'micro_bic'
      - (6) verdictLmp = detecterBasculeLmp({ recettes: totalRecettes, revenusFoyer: bailleur.revenusActifsAnnuelsCourant }, regles)
      - Retour structure complète.

    Note : `sommeChargesParBien` extension du ChargesRepository — ajouter cette méthode au port + adapter + test intégration léger (5 cas) dans cette tâche OU différer Plan 08. PRÉFÉRER : ajouter ici car requis par S12.

    Features BDD posées (steps Task 2) :
      - fiscalite-multi-bien.feature : @fis-multi-bien @phase5 — 3 scénarios CONTEXT.md L242-244 sur seuil consolidé + 1 sticky LMP
      - fiscalite-sortie-composant.feature : @fis-sortie-composant @phase5 — 3 scénarios (CONTEXT.md L249-250 + VNC conservée)
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/fiscalite/sortir-composant.test.ts tests/unit/fiscalite/lister-vue-consolidee.test.ts && pnpm lint:deps</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- tests/unit/fiscalite/sortir-composant.test.ts` exits 0 — au moins 3 cas.
    - `pnpm test -- tests/unit/fiscalite/lister-vue-consolidee.test.ts` exits 0 — au moins 4 cas (0 biens, 2 biens consolidé > seuil, seuil exact, LMP probable).
    - `grep -c "sortirComposant\\|ComposantIntrouvable" src/application/fiscalite/sortir-composant.ts` retourne au moins 2.
    - `grep -c "listerVueConsolidee\\|VueConsolideeBailleur" src/application/fiscalite/lister-vue-consolidee.ts` retourne au moins 2.
    - `grep -c "D-FIS-G5\\|G5.1\\|G5.2" src/application/fiscalite/sortir-composant.ts src/application/fiscalite/lister-vue-consolidee.ts | wc -l` retourne au moins 2.
    - `pnpm lint:deps` exits 0 — hexagonal préservé.
    - `sommeChargesParBien` méthode présente sur ChargesRepository port + adapter SQLite : `grep -c "sommeChargesParBien" src/domain/fiscalite/charges-repository.ts src/infrastructure/repositories/charges-repository-sqlite.ts | head -2` retourne 2 fichiers.
  </acceptance_criteria>
  <done>
    Use cases sortir-composant + lister-vue-consolidee + extension
    sommeChargesParBien en place. Tests unit verts couvrant cas limites
    G5.1/G5.2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Use cases exporter-csv-fiscal + exporter-pdf-recap + docDef pdfmake + routes téléchargement</name>
  <files>
    src/application/fiscalite/exporter-csv-fiscal.ts,
    src/application/fiscalite/exporter-pdf-recap.ts,
    src/infrastructure/pdf/recap-fiscal-doc-def.ts,
    src/web/routes/fiscalite/exports.ts,
    tests/unit/fiscalite/exporter-csv-fiscal.test.ts,
    tests/integration/fiscalite/exporter-pdf-recap.spec.ts,
    tests/bdd/features/fiscalite-exports.feature
  </files>
  <read_first>
    src/application/encaissements/generer-quittance.ts,
    src/application/encaissements/generer-avis-echeance.ts,
    src/infrastructure/pdf/avenant-irl-doc-def.ts,
    src/web/routes/quittances.ts,
    src/domain/fiscalite/declaration-annuelle.ts,
    src/domain/fiscalite/declaration-annuelle-repository.ts,
    src/domain/fiscalite/tableau-amortissement-repository.ts,
    src/domain/identite/bailleur.ts,
    src/domain/patrimoine/bien-repository.ts,
    src/application/encaissements/pdf-renderer.ts
  </read_first>
  <action>
    Use case `exporterCsvFiscal({ declarationId }, repos: { declRepo, tableauAmortRepo }): Promise<{ contenu: string, nomFichier: string }>` :
      - (1) decl = declRepo.trouverParId → null throw DeclarationIntrouvable (nouvelle erreur ou réutiliser DeclarationOriginaleAbsente)
      - (2) chargesParCategorie = JSON.parse(decl.chargesQualifieesJson) → { entretien_reparation: centimes, ... }
      - (3) Construire le CSV (séparateur ;, encoding UTF-8 avec BOM \uFEFF pour Excel) :
        ```
        \uFEFFType;Montant en euros;Détail
        Recettes annuelles;{recettes.enEuros()};Encaissements bruts annule_le IS NULL
        Charges entretien/réparation;{chargesParCategorie.entretien_reparation.enEuros()};Réparations courantes
        Charges amélioration;{chargesParCategorie.amelioration.enEuros()};Immobilisations amortissables
        Charges courantes périodiques;{chargesParCategorie.charge_courante_periodique.enEuros()};Assurance, TF nette, syndic, intérêts emprunt
        Dotation amortissement;{decl.dotationAmortissement.enEuros()};Plafonnée au résultat (CGI 39)
        ARD généré;{decl.ardGenere.enEuros()};Reportable sans limite (CGI 39 B)
        ARD consommé;{decl.ardConsomme.enEuros()};Imputé sur résultat exercice
        Résultat fiscal;{recettes − Σ chargesDeductibles − dotationAmortissement};Régime {regimeApplique}
        Statut LMNP/LMP;{statutLmnpLmp label};Cf. CGI 155 IV
        ```
      - (4) nomFichier = `declaration-fiscale-${decl.exercice}.csv`
      - (5) return { contenu, nomFichier }

    Use case `exporterPdfRecap({ declarationId }, repos, pdfRenderer): Promise<{ buffer: Buffer, nomFichier: string }>` :
      - (1) lookup decl + bailleur + biensActifs
      - (2) Pour régime réel : lookup tableauAmortRepo.listerParBienExercice pour chaque bien
      - (3) docDef = construireRecapFiscal(decl, bailleur, biens, tableauxAmortissement)
      - (4) buffer = await pdfRenderer.genererBuffer(docDef)
      - (5) nomFichier = `recap-fiscal-${decl.exercice}.pdf`

    src/infrastructure/pdf/recap-fiscal-doc-def.ts :
      - export function construireRecapFiscal(decl, bailleur, biens, tableauxAmortissement): TDocDefinition
      - Structure pdfmake :
        - header : "Récapitulatif fiscal LMNP — Exercice {decl.exercice}"
        - section bailleur : nom + adresse
        - section verdict : tableau coloré tri-état (CSS color via fillColor pdfmake — vert/orange/rouge)
        - section totaux : tableau 2 colonnes Type/Montant (Recettes, Σ Charges, Dotation, Résultat fiscal)
        - section amortissement (régime réel) : 1 tableau par bien avec dotation par composant
        - section comparatif micro vs réel (lecture-seule)
        - footer : date d'édition + clauses CGI 39 / 39 B / 50-0 / 155 IV citées
      - Pattern : src/infrastructure/pdf/avenant-irl-doc-def.ts (déjà en place — pdfmake déjà en dépendance Phases 2+3 D-FIS-G5.3)

    Routes Fastify `src/web/routes/fiscalite/exports.ts` :
      - GET /fiscalite/declarations/:id/exports/csv :
          ```
          const { contenu, nomFichier } = await exporterCsvFiscal({ declarationId }, repos);
          return reply
            .header('Content-Type', 'text/csv; charset=utf-8')
            .header('Content-Disposition', `attachment; filename*=UTF-8''${encodeURIComponent(nomFichier)}`)
            .send(contenu);
          ```
      - GET /fiscalite/declarations/:id/exports/pdf : idem avec buffer.

    Tests unit exporter-csv-fiscal :
      Test 1 : decl micro-BIC + 50k recettes + 0 charges (micro abattement implicite) → CSV contient ligne "Recettes annuelles;50 000,00 €" et "Régime micro_bic".
      Test 2 : decl réel + 100k recettes + charges variées + 4 composants → CSV contient toutes les colonnes attendues + UTF-8 BOM.
      Test 3 : decl introuvable → throw.

    Tests intégration exporter-pdf-recap :
      Setup in-memory + appliquerToutesMigrations + seed (1 bailleur, 1 bien, 6 composants, 1 DeclarationAnnuelle réel)
      Test : appel exporterPdfRecap → buffer non vide ; buffer commence par "%PDF-" (magic bytes) ; buffer.length > 1000 (le PDF a du contenu) ; nomFichier === `recap-fiscal-${exercice}.pdf`.

    Feature `tests/bdd/features/fiscalite-exports.feature` (@fis-exports @phase5) :
      - Scénario : "Téléchargement CSV : contient Recettes / Charges / Dotation / Résultat / Verdict avec format français des montants"
      - Scénario : "Téléchargement PDF : contient identité bailleur + verdict tri-état + tableau d'amortissement"
      - Scénario : "Content-Disposition RFC 6266 avec UTF-8 encoded nom fichier" (vérifie header)
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/fiscalite/exporter-csv-fiscal.test.ts tests/integration/fiscalite/exporter-pdf-recap.spec.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- tests/unit/fiscalite/exporter-csv-fiscal.test.ts` exits 0 — au moins 3 cas.
    - `pnpm test -- tests/integration/fiscalite/exporter-pdf-recap.spec.ts` exits 0 — au moins 1 cas (buffer non vide + magic bytes).
    - `grep -c "exporterCsvFiscal\\|exporterPdfRecap" src/application/fiscalite/exporter-csv-fiscal.ts src/application/fiscalite/exporter-pdf-recap.ts | wc -l` retourne au moins 2 (1 par fichier).
    - `grep -c "construireRecapFiscal" src/infrastructure/pdf/recap-fiscal-doc-def.ts` retourne au moins 1.
    - `grep -c "Content-Disposition\\|attachment; filename\\*=UTF-8" src/web/routes/fiscalite/exports.ts` retourne au moins 2.
    - `grep -c "CGI art\\.? 39\\|CGI art\\.? 50-0\\|CGI art\\.? 155 IV" src/infrastructure/pdf/recap-fiscal-doc-def.ts` retourne au moins 2 (citations doctrinaires dans le PDF).
    - `pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>
    Use cases exporter-csv-fiscal + exporter-pdf-recap + docDef pdfmake livrés.
    Routes téléchargement avec headers Content-Disposition conformes RFC 6266.
    Buffer PDF validé (magic bytes).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Routes + pages multi-bien S12 + onboarding S1 + sortir-composant + bandeau onboarding + steps BDD + main.ts wiring</name>
  <files>
    src/web/routes/fiscalite/composants.ts,
    src/web/routes/fiscalite/onboarding.ts,
    src/web/views/pages/fiscalite/vue-consolidee.ejs,
    src/web/views/pages/fiscalite/onboarding.ejs,
    src/web/views/pages/fiscalite/sortir-composant.ejs,
    src/web/views/partials/partial-onboarding-banner.ejs,
    src/web/views/partials/layout-debut.ejs,
    src/main.ts,
    tests/bdd/step_definitions/fiscalite-multi-bien.steps.ts
  </files>
  <read_first>
    src/web/routes/biens.ts,
    src/web/routes/fiscalite/composants.ts,
    src/web/views/pages/biens/liste.ejs,
    src/web/views/partials/data-table.ejs,
    src/web/views/partials/empty-state.ejs,
    src/web/views/partials/form-field.ejs,
    src/web/views/partials/layout-debut.ejs,
    src/main.ts,
    .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-UI-SPEC.md,
    src/application/fiscalite/lister-vue-consolidee.ts,
    src/application/fiscalite/sortir-composant.ts
  </read_first>
  <action>
    Étendre src/web/routes/fiscalite/composants.ts (Plan 03 expose déjà GET/POST /biens/:bienId/fiscalite/activer) :
      - GET /biens/:bienId/fiscalite/composants/:composantId/sortir : lookup composant ; render pages/fiscalite/sortir-composant.ejs avec { bien, composant }
      - POST /biens/:bienId/fiscalite/composants/:composantId/sortir : safeParse(sortirComposantSchema) ; appel sortirComposant use case ; bannière succès + redirect /biens/:bienId/fiscalite/amortissement

    Route Fastify `src/web/routes/fiscalite/onboarding.ts` :
      - GET /fiscalite/onboarding : render pages/fiscalite/onboarding.ejs (UI-SPEC §S1)
      - POST /fiscalite/onboarding (action: commencer | plus_tard | ignorer) : lookup bailleur ; bailleur.modifier({ fiscalitePremierAcces: PlainDateTime.from(...) }) si null ; redirect selon action :
        - 'commencer' → /fiscalite/revenus-foyer (étape suivante recommandée)
        - 'plus_tard' → / (racine app)
        - 'ignorer' → / (racine app) + session.banniere "Vous pourrez réactiver l'onboarding depuis la page Fiscalité."

    Étendre src/web/routes/fiscalite/composants.ts ou créer src/web/routes/fiscalite/multi-bien.ts :
      - GET /fiscalite/multi-bien?annee={N} : appel listerVueConsolidee + render pages/fiscalite/vue-consolidee.ejs

    Pages :
      - `pages/fiscalite/vue-consolidee.ejs` (UI-SPEC §S12 L213-218) :
        - include layout-debut(titre: 'Vue consolidée fiscale {annee}', breadcrumbs, navActive: 'fiscalite')
        - include partial-verdict-fiscal(statut: vue.verdictLmp, annee)
        - <table aria-label="Vue consolidée multi-bien {annee}">
            <thead><tr><th>Bien</th><th class="numeric">Recettes</th><th class="numeric">Charges qualifiées</th><th class="numeric">Dotation</th><th class="numeric">Résultat fiscal</th><th>Régime</th><th>Actions</th></tr></thead>
            <tbody>
              <% vue.biens.forEach(b => { %>
                <tr><td><%= b.adresse %></td><td class="numeric"><%= formatMoney(b.recettes) %></td>... <td><a href="/biens/<%= b.bienId %>/fiscalite">Voir le détail</a></td></tr>
              <% }) %>
            </tbody>
            <tfoot><tr><td><strong>Totaux</strong></td><td class="numeric"><%= formatMoney(vue.totaux.recettes) %></td>... </tr></tfoot>
          </table>
        - Note explicative sous le tableau : "Seuils micro-BIC et LMP appréciés sur le total consolidé (D-LOCK-2)."
        - include layout-fin

      - `pages/fiscalite/onboarding.ejs` (UI-SPEC §S1 L101-108) :
        - <section style="background: var(--couleur-accent-bg); padding: 24px;">
          <h1>Bienvenue dans Fiscalité LMNP</h1>
          <p>Centralisez vos charges, calculez votre amortissement par composant, anticipez le verdict LMNP/LMP. Local-first, single-user, à jour LF 2026.</p>
          <form method="POST" action="/fiscalite/onboarding">
            <button name="action" value="commencer" type="submit">Commencer</button>
            <button name="action" value="plus_tard" type="submit" class="secondary">Plus tard</button>
            <button name="action" value="ignorer" type="submit" style="background: transparent; border: none; color: var(--couleur-accent); text-decoration: underline;">Tout ignorer</button>
          </form>
        </section>
        - Hick's Law respecté (3 choix maximum)

      - `pages/fiscalite/sortir-composant.ejs` :
        - include layout-debut(titre: 'Sortir composant', breadcrumbs)
        - <h1>Sortir le composant {composant.type label} du bien {bien.adresse}</h1>
        - Bandeau warning : "Cette opération est permanente. Le composant disparaîtra du tableau d'amortissement à partir de l'exercice suivant la date de sortie. La VNC sera conservée dans les déclarations annuelles pour la plus-value future (LF 2025 art. 84)."
        - <form method="POST">
          <select name="motif" required>
            <option value="vente">Vente</option>
            <option value="mise_au_rebut">Mise au rebut</option>
            <option value="sinistre">Sinistre</option>
            <option value="autre">Autre</option>
          </select>
          <input type="date" name="dateSortieIso" required>
          <button type="submit">Sortir le composant</button>
          <a href="/biens/{bienId}/fiscalite/amortissement" class="secondary">Annuler</a>
        </form>

    Partial `partial-onboarding-banner.ejs` :
      - Vue conditionnelle : si bailleur.fiscalitePremierAcces === null → afficher
      - <section role="banner" aria-label="Onboarding fiscal" style="background: var(--couleur-accent-bg); padding: 12px;">
          <strong>Activez la fiscalité LMNP</strong> — calculez votre amortissement, suivez vos charges, anticipez le verdict LMNP/LMP.
          <a href="/fiscalite/onboarding">Commencer</a>
        </section>
      - Inclusion : dans layout-debut.ejs juste avant <main> — conditionné par locals.afficherOnboardingBanner (booléen passé par les routes Phase 1-4 qui souhaitent l'afficher). Pour Plan 07 : modifier layout-debut.ejs pour inclure conditionnellement le bandeau.

    Modifier `src/web/views/partials/layout-debut.ejs` :
      - Ajouter avant <main> : `<%- typeof afficherOnboardingBanner !== 'undefined' && afficherOnboardingBanner ? include('partial-onboarding-banner') : '' %>`
      - Note : pour V1 simple, n'afficher le bandeau que sur les pages explicites (ex. /, /biens/list) en passant `afficherOnboardingBanner: bailleur.fiscalitePremierAcces === null` depuis ces routes.

    Wiring src/main.ts :
      - import lister-vue-consolidee + sortir-composant + exporter-csv-fiscal + exporter-pdf-recap + PDF renderer (déjà wiré Phase 3)
      - registerFiscaliteMultiBienRoute(app, { ... })
      - registerFiscaliteExportsRoutes(app, { ... })
      - registerFiscaliteOnboardingRoute(app, { ... })
      - Étendre registerFiscaliteComposantsRoutes (Plan 03) avec les routes sortir

    Steps BDD `tests/bdd/step_definitions/fiscalite-multi-bien.steps.ts` :
      - Given 'un Bailleur avec {N} biens actifs' → insertion via bienRepo
      - Given 'recettes consolidées {N} € pour exercice {N}' → fixtures
      - Given 'composant {type} {N} € acquis le {date} du bien {bien}' → insertion via composantRepo
      - When 'le bailleur sort le composant {type} du bien {bien} avec motif "{motif}" date {date}' → POST simulation
      - When 'le bailleur télécharge le CSV de la déclaration {N}' → curl simulation OU appel direct exporterCsvFiscal
      - When 'le bailleur télécharge le PDF de la déclaration {N}' → idem
      - Then 'la vue consolidée {N} affiche {N} ligne(s) bien + 1 ligne totaux' → assert
      - Then 'le seuil consolidé > 83 600 € → régime réel forcé' → assert
      - Then 'le composant {type} a date_sortie = {date}' → assert via composantRepo
      - Then 'le CSV contient une ligne "Recettes annuelles;{N} €"' → assert contenu
      - Then 'le PDF buffer commence par %PDF-' → assert magic bytes

    Wirer les scénarios @fis-multi-bien + @fis-sortie-composant + @fis-exports posés Tasks 1+2.
  </action>
  <verify>
    <automated>pnpm test -- tests/bdd && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier src/web/routes/fiscalite/onboarding.ts existe et expose GET + POST /fiscalite/onboarding.
    - 3 pages créées : vue-consolidee.ejs (S12), onboarding.ejs (S1), sortir-composant.ejs.
    - partial-onboarding-banner.ejs existe et contient `role="banner"` + lien vers /fiscalite/onboarding.
    - layout-debut.ejs modifié pour include conditionnel partial-onboarding-banner.
    - `grep -c "Vue consolidée multi-bien" src/web/views/pages/fiscalite/vue-consolidee.ejs` retourne au moins 1.
    - `grep -c "Commencer\\|Plus tard\\|Tout ignorer" src/web/views/pages/fiscalite/onboarding.ejs` retourne au moins 3.
    - `grep -c "registerFiscaliteMultiBienRoute\\|registerFiscaliteExportsRoutes\\|registerFiscaliteOnboardingRoute" src/main.ts` retourne au moins 3.
    - `pnpm test -- tests/bdd` exits 0 — scénarios @fis-multi-bien (au moins 3) + @fis-sortie-composant (au moins 2) + @fis-exports (au moins 2) verts.
    - `pnpm typecheck` exits 0.
    - Tableau S12 contient ligne `<tfoot>` avec totaux (font-weight: 600) : `grep -c "tfoot" src/web/views/pages/fiscalite/vue-consolidee.ejs` retourne au moins 1.
    - Note explicative D-LOCK-2 présente : `grep -c "D-LOCK-2\\|seuils.*consolidé\\|consolidé" src/web/views/pages/fiscalite/vue-consolidee.ejs` retourne au moins 1.
    - Bandeau warning sur sortir-composant.ejs cite "LF 2025 art. 84" : `grep -c "LF 2025 art\\.? 84\\|VNC" src/web/views/pages/fiscalite/sortir-composant.ejs` retourne au moins 1.
  </acceptance_criteria>
  <done>
    Vue consolidée multi-bien S12 + onboarding S1 + sortir-composant + bandeau
    onboarding + exports CSV/PDF tous wirés. L'utilisateur peut autonomement
    naviguer dans la fiscalité de tous ses biens, sortir un composant, exporter
    son récap fiscal annuel (CSV/PDF). Couverture BDD complète G5.1/G5.2/G5.3/G5.4.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GET /fiscalite/declarations/:id/exports/csv | Téléchargement attaché — RFC 6266 + UTF-8 filename encoding (pattern Phase 4 CR-04). |
| GET /fiscalite/declarations/:id/exports/pdf | Buffer PDF — Content-Type strict application/pdf. |
| POST /biens/:bienId/fiscalite/composants/:composantId/sortir | Body utilisateur — Zod schema avec dates ISO + enum motif. |
| layout-debut.ejs include conditionnel | Locals.afficherOnboardingBanner doit être boolean — pas d'injection EJS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Information Disclosure | Export PDF contient montants en clair | accept | Local-first single-user — le bailleur télécharge son propre PDF. Pas d'accès externe. |
| T-05-07-02 | Tampering | POST sortir-composant motif invalide | mitigate | Zod schema enum strict + invariants Composant.sortir côté domaine (déjà vérifié Plan 03). |
| T-05-07-03 | Information Disclosure | Content-Disposition filename pas encodé UTF-8 | mitigate | RFC 6266 : filename*=UTF-8'' obligatoire, encodeURIComponent appliqué. Pattern Phase 4 CR-04+CR-05. |
| T-05-07-04 | Tampering | CSV séparateur ; injection (CSV injection / formula injection) | mitigate | Toutes les valeurs montant viennent de Money.enEuros() (formaté Intl.NumberFormat — pas de = / @ / + / -). Le motif et autres champs viennent de domain (validated). |
| T-05-07-05 | DoS | Téléchargement PDF avec 10 ans d'historique | accept | Buffer pdfmake taille raisonnable (< 10 Mo pour 10 ans). Pas de limite imposée V1. |
| T-05-07-06 | Repudiation | Sortie composant sans trace | mitigate | dateSortie + motifSortie persistés sur Composant ; la DeclarationAnnuelle de l'exercice de sortie contient composants_snapshot_json avec dateSortie figée. Audit-trail intact. |
| T-05-07-07 | Tampering | Onboarding action manipulée (autres valeurs que commencer/plus_tard/ignorer) | mitigate | Zod enum strict + bailleur.fiscalitePremierAcces idempotent (1ère pose seulement). |
| T-05-07-08 | Information Disclosure | Vue consolidée expose plusieurs biens dans la même page | accept | Local-first single-user — bailleur EST l'utilisateur, exposition au bailleur lui-même = OK. |
</threat_model>

<verification>
1. `pnpm test -- tests/unit/fiscalite tests/integration/fiscalite tests/bdd` → tous verts
2. `pnpm typecheck` → 0 erreur
3. `pnpm lint:deps` → 0 erreur
4. Manual : curl GET /fiscalite/multi-bien?annee=2026 retourne tableau consolidé ; POST /biens/X/fiscalite/composants/Y/sortir sort le composant ; GET /fiscalite/declarations/Z/exports/csv télécharge un CSV avec UTF-8 BOM ; GET .../exports/pdf télécharge un PDF (vérifier magic bytes %PDF- début du buffer).
5. Citations doctrinaires : LF 2025 art. 84, CGI 39 / 39 B / 50-0 / 155 IV, D-FIS-G5.1, G5.2, G5.3, G5.4 dans JSDoc/code/PDF/tests.
6. `! grep -q $'\\xef\\xbb\\xbf' src/application/fiscalite/exporter-csv-fiscal.ts` — le caractère BOM doit être inséré PROGRAMMATIQUEMENT via '\\uFEFF' string literal en TS, jamais hardcodé dans des fichiers source.
</verification>

<success_criteria>
- Use case sortir-composant + lister-vue-consolidee + exporter-csv-fiscal + exporter-pdf-recap livrés
- Routes téléchargement CSV/PDF avec headers RFC 6266
- Page S12 vue consolidée multi-bien (seuils sur total D-LOCK-2)
- Page S1 onboarding 3 CTA (Hick's Law)
- Page sortir-composant avec warning LF 2025
- Bandeau onboarding inclus conditionnellement dans layout-debut
- Sortie composant : prorata exercice de sortie + composant exclu exercices suivants + VNC conservée
- Couverture BDD @fis-multi-bien + @fis-sortie-composant + @fis-exports verte
- L'utilisateur peut autonomement gérer plusieurs biens et exporter ses données
</success_criteria>

<output>
Créer .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-07-SUMMARY.md
en suivant @.claude/get-shit-done/templates/summary.md.
</output>
