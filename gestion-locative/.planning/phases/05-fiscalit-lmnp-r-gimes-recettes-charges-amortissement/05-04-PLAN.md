---
phase: 05
plan: 04
plan_id: "05-04"
type: tdd
wave: 4
depends_on: ["05-01", "05-03"]
files_modified:
  - migrations/0019_phase5_amortissement_exercice.sql
  - src/infrastructure/db/kysely-types.ts
  - src/domain/fiscalite/tableau-amortissement.ts
  - src/domain/fiscalite/ard.ts
  - src/domain/fiscalite/amortissement-exercice.ts
  - src/domain/fiscalite/tableau-amortissement-repository.ts
  - src/infrastructure/repositories/tableau-amortissement-repository-sqlite.ts
  - src/application/fiscalite/calculer-amortissement.ts
  - src/application/fiscalite/recalculer-tableau-amortissement.ts
  - src/web/routes/fiscalite/amortissement.ts
  - src/web/views/pages/fiscalite/tableau-amortissement.ejs
  - src/web/views/partials/partial-tableau-amortissement.ejs
  - src/main.ts
  - tests/_builders/fiscalite.ts
  - tests/unit/fiscalite/calculer-amortissement.test.ts
  - tests/unit/fiscalite/tableau-amortissement.test.ts
  - tests/unit/fiscalite/ard.test.ts
  - tests/integration/repositories/tableau-amortissement-repository-sqlite.test.ts
  - tests/bdd/features/fiscalite-amortissement.feature
  - tests/bdd/features/fiscalite-ard.feature
  - tests/bdd/step_definitions/fiscalite-amortissement.steps.ts
autonomous: true
requirements: ["FIS-04"]
user_setup: []

mvp_split_rationale: |
  Plan TDD dédié au calcul d'amortissement par composant — feature unique avec
  contrats d'I/O bien définis (Composant[] + année + RegleFiscale → tableau
  d'amortissement avec dotation théorique / appliquée / ARD généré / cumulé /
  consommé). Logique fiscale critique : prorata jour près (D-FIS-G1.6), plafond
  résultat avant amortissement (D-LOCK-1 + CGI 39), ARD reportable sans limite
  (CGI 39 B). 100 % couverture obligatoire — chaque exception du droit a son
  scénario dédié (practices/BDD_PRACTICES.md). Heavy plan : 40 % de contexte
  pour permettre le cycle RED→GREEN→REFACTOR sans dégrader.

must_haves:
  truths:
    - "Le use case pur `calculerAmortissement(composants: Composant[], exercice: number, regles: RegleFiscale2026, options: { resultatAvantAmortissement: Money, ardCumuleEnEntree: Money }): TableauAmortissementExercice` retourne pour chaque composant ACTIF cette année (dateAcquisition <= 31 déc exercice AND (dateSortie === null OR dateSortie >= 1 janv exercice)) : (a) dotationTheorique = montantHt × (joursDetentionDansExercice / 365n) ÷ dureeAmortissementAns ; (b) dotationAppliquee = min(dotationTheorique, plafond restant) ; (c) ardGenere = dotationTheorique − dotationAppliquee ; (d) la somme des dotationAppliquee est plafonnée à `resultatAvantAmortissement` (D-LOCK-1, CGI 39). Tout dépassement génère ARD reportable. Le terrain a toujours dotationTheorique = Money.zero() (DUREES_AMORTISSEMENT_ANS.terrain = 0 — non amortissable)."
    - "Cas limite obligatoire G1.6 (CONTEXT.md L249) : Composant { type:gros_oeuvre, montantHt: Money.fromEuros(200_000), dateAcquisition: 2026-03-15 } exercice 2026 → dotationTheorique = 200_000 × (292/365) ÷ 40 = 4_000 € exact (banker's rounding via Money.multiplyByFraction(292n, 365n).multiplyByRatio(1n, 40n))."
    - "Cas limite (CONTEXT.md L250) : Composant acquis 2026-01-01 sorti 2026-06-30 → joursDetention = 181 ; dotationTheorique = montantHt × (181/365) ÷ dureeAns ; le composant disparaît du tableau exercice 2027 (puisque dateSortie < 1 janv 2027)."
    - "Cas limite : exercice N avec resultatAvantAmortissement = Money.zero() → toutes dotationsAppliquees = Money.zero() ET tous les composants génèrent 100 % de dotationTheorique en ARD. La VNC du composant DIMINUE quand même de la dotationTheorique (D-LOCK-1 + LMNP.md §95) — le bailleur a 'utilisé' l'amortissement même s'il a généré de l'ARD."
    - "Cas limite : exercice N+1 avec resultatAvantAmortissement = Money.fromEuros(10_000) + ardCumuleEnEntree = Money.fromEuros(15_000) → priorité ABSOLUE consommation ARD avant nouvelle dotation : ardConsomme = min(ardCumuleEnEntree, resultatAvantAmortissement) = 10_000 € ; reste = 0 ; donc TOUTES les nouvelles dotations N+1 deviennent ARD générées (ardGenereExerciceN1 = Σ dotationTheorique N+1). ardCumuleSortie = (ardCumuleEnEntree − ardConsomme) + ardGenereExerciceN1. Ordre déterministe testable."
    - "AmortissementExercice (read-model matérialisé append-only, D-FIS-G1.7) : 1 ligne par (bien_id, composant_id, exercice) avec colonnes dotation_theorique_centimes, dotation_appliquee_centimes, ard_genere_centimes (Money), cree_le. Plus 1 ligne agrégée par (bien_id, exercice, type='SYNTHESE_BIEN') avec ard_cumule_disponible_centimes + ard_consomme_centimes. Le caller (cloturer-exercice Plan 06) appelle recalculerTableauAmortissement qui produit ces lignes et delegue enregistrerBatch."
    - "Le port TableauAmortissementRepository expose : `enregistrerBatch(lignes: AmortissementExercice[], trx?): Promise<void>` (append-only, PAS de onConflict — réinsertion = UNIQUE violation) ; `listerParBienExercice(bienId, exercice): Promise<AmortissementExercice[]>` ; `dernierArdCumule(bienId, exerciceMax): Promise<Money>` (lecture du dernier ARD cumulé pour préparer N+1)."
    - "L'aggregate VO TableauAmortissement (collection immutable) expose : `dotationTotale(): Money`, `ardGenereTotal(): Money`, `ardConsommeTotal(): Money`, `dotationParComposant: ReadonlyArray<{ composantId, dotationTheorique, dotationAppliquee, ardGenere }>` — couche présentation et tests."
    - "VO `ARD` (montant + exercice de génération) : `creer(montant, exerciceGeneration)` invariants montant >= 0 et exerciceGeneration > 0 ; `consommer(montantConsomme): { reste: ARD, consomme: Money }` immutable (retourne nouvelle ARD + Money consommé) — pour traçabilité audit-trail (V1.1 SIM-02)."
    - "Le calcul est PUR — pas d'I/O, pas de side-effect. Le use case orchestrateur (Plan 06 cloturerExercice) appelle (1) recettesRepo.sommeRecettesAnnuelles, (2) chargesRepo.sommeChargesParCategorie, (3) calcule resultatAvantAmortissement = recettes − chargesDeductibles, (4) appelle calculerAmortissement, (5) appelle tableauAmortissementRepo.enregistrerBatch dans la même transaction que DeclarationAnnuelle."
    - "Le use case `recalculerTableauAmortissement(bienId, exercice, repos, regleFiscale, db): Promise<void>` (lecture-seule extrapolation) compose lookup composants + lookup recettes/charges + appelle calculerAmortissement + enregistreBatch en lecture-seule pour PRÉ-AFFICHAGE (D-FIS-G1.7 — read-model matérialisé). Utilisé par la route S4 avant clôture."
    - "L'UI S4 GET /biens/:bienId/fiscalite/amortissement?annee={N} affiche le tableau d'amortissement read-model : colonnes Exercice, Composant, Dotation théorique, Dotation appliquée, ARD généré, ARD cumulé disponible, ARD consommé — toutes 'numeric' (font-variant: tabular-nums) + bandeau pédagogique ARD (CGI 39 B) — UI-SPEC §S4 L130-136."
    - "Couverture BDD @fis-04-amortissement @phase5 (cas limites CONTEXT.md L249-252) : (a) gros_oeuvre 200k € acquis 2026-03-15 → dotation 2026 = 4_000 € ; (b) terrain → dotation toujours 0 ; (c) acquis 2026-01-01 sorti 2026-06-30 → prorata 6 mois ; (d) résultat insuffisant → 100 % en ARD ; (e) ARD reportée consommée en priorité année N+1. Couverture @fis-04-ard : (f) ARD cumulé disponible inchangé si soft-delete encaissement post-clôture."
  artifacts:
    - path: "migrations/0019_phase5_amortissement_exercice.sql"
      provides: "Table amortissement_exercice (read-model append-only)"
      contains: "CREATE TABLE IF NOT EXISTS amortissement_exercice"
    - path: "src/domain/fiscalite/amortissement-exercice.ts"
      provides: "VO AmortissementExercice (1 ligne du read-model) + factory + serialization"
      exports: ["AmortissementExercice"]
    - path: "src/domain/fiscalite/tableau-amortissement.ts"
      provides: "VO collection immutable + helpers d'agrégation (dotationTotale, ardGenereTotal, ardConsommeTotal)"
      exports: ["TableauAmortissementExercice"]
    - path: "src/domain/fiscalite/ard.ts"
      provides: "VO ARD (montant + exerciceGeneration) + consommer immutable"
      exports: ["ARD"]
    - path: "src/domain/fiscalite/tableau-amortissement-repository.ts"
      provides: "Port repository (append-only batch + lookup par bien/exercice + dernierArdCumule)"
      exports: ["TableauAmortissementRepository"]
    - path: "src/application/fiscalite/calculer-amortissement.ts"
      provides: "Use case PUR : composants[] + exercice + regles + resultatAvantAmortissement + ardCumuleEnEntree → TableauAmortissementExercice"
      exports: ["calculerAmortissement"]
    - path: "src/application/fiscalite/recalculer-tableau-amortissement.ts"
      provides: "Use case orchestration (utile pour S4 avant clôture)"
      exports: ["recalculerTableauAmortissement"]
    - path: "src/web/routes/fiscalite/amortissement.ts"
      provides: "Route GET /biens/:bienId/fiscalite/amortissement?annee={N}"
      contains: "tableau-amortissement"
    - path: "src/web/views/pages/fiscalite/tableau-amortissement.ejs"
      provides: "Page S4 — UI-SPEC §S4"
      contains: "tableau-amortissement"
    - path: "src/web/views/partials/partial-tableau-amortissement.ejs"
      provides: "Partial data-table spécialisé pour tableau d'amortissement"
      contains: "data-table"
  key_links:
    - from: "calculerAmortissement use case"
      to: "Money.multiplyByFraction(jours, 365n).multiplyByRatio(1n, dureeAns)"
      via: "prorata temporis au jour près (D-FIS-G1.6)"
      pattern: "multiplyByFraction.*365n"
    - from: "AmortissementExercice (read-model)"
      to: "TableauAmortissementRepository.enregistrerBatch + onConflict ABSENT (append-only)"
      via: "transaction Kysely depuis cloturer-exercice (Plan 06)"
      pattern: "enregistrerBatch"
    - from: "S4 tableau-amortissement.ejs"
      to: "GET /biens/:bienId/fiscalite/amortissement?annee={N}"
      via: "URL handler + recalculerTableauAmortissement use case"
      pattern: "fiscalite/amortissement"

tags: [fiscalite, lmnp, amortissement-composant, ard, fis-04, tdd-strict, calcul-pur]
---

<objective>
Implémenter en TDD strict le calcul d'amortissement par composant LMNP : prorata
temporis au jour près + plafonnement au résultat avant amortissement + ARD
reportable sans limite. Read-model matérialisé `AmortissementExercice` +
TableauAmortissementRepository. Vue S4 fonctionnelle.

Purpose : FIS-04 (cœur du calcul). Composé de logique fiscale critique avec
nombreuses exceptions du droit — chaque cas du CONTEXT.md L249-252 a son
scénario dédié. Type TDD strict (RED/GREEN/REFACTOR avec gates Cucumber) —
ce calcul ne tolère aucune approximation : le bailleur fiscaliste compare au
centime avec son Excel.

Output :
  - 1 migration (0019 amortissement_exercice append-only)
  - 3 VOs domain (AmortissementExercice, TableauAmortissement, ARD)
  - 1 port + adapter SQLite
  - 2 use cases (calculerAmortissement pur + recalculerTableauAmortissement)
  - 1 route + 1 page S4 + 1 partial tableau
  - Couverture BDD 100 % cas limites G1.6, G1.7, plafond résultat
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-CONTEXT.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-PATTERNS.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-UI-SPEC.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-01-PLAN.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-03-PLAN.md

# Sources canoniques (CITER en JSDoc + tests BDD)
@LMNP.md
@practices/BDD_PRACTICES.md
@practices/DDD.md

# Patterns à répliquer
@src/domain/_shared/money.ts
@src/domain/locatif/bail-indexation.ts
@src/domain/encaissements/echeance-loyer.ts
@src/infrastructure/repositories/bail-indexation-repository-sqlite.ts
@src/application/locatif/simuler-indexation-irl.ts
@migrations/0009_phase3_bail_indexations.sql
@migrations/0010_phase4_documents_travaux.sql
@src/web/routes/baux.ts
@src/web/views/partials/data-table.ejs
@src/web/views/pages/biens/liste.ejs
</context>

<feature>
  <name>Calcul d'amortissement par composant LMNP (FIS-04)</name>
  <files>
    src/domain/fiscalite/amortissement-exercice.ts,
    src/domain/fiscalite/tableau-amortissement.ts,
    src/domain/fiscalite/ard.ts,
    src/application/fiscalite/calculer-amortissement.ts,
    tests/unit/fiscalite/calculer-amortissement.test.ts,
    tests/unit/fiscalite/tableau-amortissement.test.ts,
    tests/unit/fiscalite/ard.test.ts
  </files>
  <behavior>
    Entrée : `composants: Composant[], exercice: number, regles: RegleFiscale2026, options: { resultatAvantAmortissement: Money, ardCumuleEnEntree: Money }`

    Pour chaque composant ACTIF cette année :
      - Si type === 'terrain' → dotationTheorique = Money.zero() (DUREES_AMORTISSEMENT_ANS.terrain = 0)
      - Sinon → jourDebut = max(dateAcquisition, PlainDate.from(exercice + '-01-01')) ; jourFin = min(dateSortie ?? PlainDate.from(exercice + '-12-31'), PlainDate.from(exercice + '-12-31')) ; jours = jourFin - jourDebut + 1 ; dotationTheorique = montantHt × (jours/365) ÷ dureeAns (Money.multiplyByFraction(jours, 365n).multiplyByRatio(1n, BigInt(dureeAns)) banker's rounding)

    Allocation plafond résultat + consommation ARD :
      (1) Calculer dotationTheoriqueTotale = Σ dotationsTheoriques
      (2) Consommer ARD prioritairement : ardConsomme = min(ardCumuleEnEntree, resultatAvantAmortissement). plafondRestant = resultatAvantAmortissement − ardConsomme.
      (3) Allouer dotationsTheoriques jusqu'à plafondRestant : si dotationTheoriqueTotale ≤ plafondRestant → toutes appliquées en plein ; sinon → allocation proportionnelle (ou ordre stable des composants ? D-FIS-G1.7 ne précise pas — la convention raisonnable est de plafonner la totale, pas répartition par composant ; chaque composant reçoit dotationAppliquee = dotationTheorique × min(plafondRestant/dotationTheoriqueTotale, 1) en proportion banker's rounding).
      Note : la convention de répartition proportionnelle pourrait être inadaptée si le bailleur veut prioriser certains composants. En V1 simple : proportionnelle. V1.1 : option utilisateur.
      (4) ardGenereExercice = dotationTheoriqueTotale − Σ dotationsAppliquees + (ardCumuleEnEntree − ardConsomme) — le reste d'ARD non consommé reste reporté.
      (5) Σ valeurs : ardCumuleEnSortie = (ardCumuleEnEntree − ardConsomme) + (dotationTheoriqueTotale − Σ dotationsAppliquees)

    Sortie : `TableauAmortissementExercice` immutable :
      - `dotationParComposant: ReadonlyArray<{ composantId, dotationTheorique, dotationAppliquee, ardGenereComposant }>`
      - `ardConsomme: Money`
      - `ardCumuleEnEntree: Money`
      - `ardCumuleEnSortie: Money`
      - `dotationAppliqueeTotale: Money`
      - `dotationTheoriqueTotale: Money`

    Cases limites obligatoires (cf. must_haves truths 2-5).

    Test 1 (RED) : cas limite G1.6 — composant gros_oeuvre 200k €, acquis 2026-03-15, exercice 2026, resultatAvantAmortissement 10k €, ardCumuleEnEntree 0 € → dotationTheorique = 4_000 € (jours = 292), dotationAppliquee = 4_000 € (plafond restant 10k suffisant), ardGenere = 0, ardCumuleEnSortie = 0.

    Test 2 (RED) : terrain → dotationTheorique = 0 toujours.

    Test 3 (RED) : Composant acquis 2026-01-01 sorti 2026-06-30 → jours = 181 ; dotationTheorique = montantHt × (181/365) ÷ dureeAns.

    Test 4 (RED) : resultatAvantAmortissement = 0 → dotationsAppliquees toutes = 0 ; ardGenere = Σ dotationsTheoriques ; ardCumuleEnSortie = ardCumuleEnEntree + Σ dotationsTheoriques.

    Test 5 (RED) : ardCumuleEnEntree = 15k € + resultatAvantAmortissement = 10k € + composants génèrent dotationTheoriqueTotale 8k € → ardConsomme = 10k (capped par résultat) ; plafondRestant = 0 ; dotationsAppliquees toutes = 0 ; ardGenereExerciceTotal = 8k + (15-10)k = 13k.

    Test 6 (RED) : composant acquis avant exercice (2024-01-01) sorti après (null), exercice 2026 → jours = 365.

    Test 7 (RED) : composant sorti avant exercice (dateSortie = 2024-06-30), exercice 2026 → composant EXCLU du tableau (estActif(exercice) === false).

    Test 8 (RED) : plafond résultat insuffisant → allocation proportionnelle ; somme des dotationsAppliquees ≤ plafondRestant (egale OU plus petite à 1 centime près en cas d'arrondi banker's).

    Test 9 (RED) — ard.test.ts : `ARD.creer(Money.fromEuros(5_000), 2026)` retourne VO ; `consommer(Money.fromEuros(3_000))` retourne `{ reste: ARD(2_000, 2026), consomme: Money.fromEuros(3_000) }`. Test invariant : montant négatif throw, exerciceGeneration <= 0 throw.

    Test 10 (RED) — tableau-amortissement.test.ts : helpers d'agrégation `dotationTotale()`, `ardGenereTotal()` retournent Σ exact.

    GREEN — implémenter dans cet ordre : (1) ARD VO, (2) AmortissementExercice VO, (3) TableauAmortissementExercice (collection), (4) calculerAmortissement pur.

    REFACTOR — extraire helpers `joursDansExercice(dateAcquisition, dateSortie, exercice)` (pur) ; `estActifPourExercice(composant, exercice)` (pur). Documenter les conventions de plafonnement (D-FIS-G1.7) et de consommation ARD prioritaire (CGI 39 B).
  </behavior>
  <implementation>
    Étape RED 1 :
      - Créer tests/unit/fiscalite/ard.test.ts avec tests 9 (3 cas : creer + consommer + invariants)
      - Vérifier `pnpm test -- tests/unit/fiscalite/ard.test.ts` ÉCHOUE (module n'existe pas)
      - Créer minimum src/domain/fiscalite/ard.ts pour passer les tests (factory + consommer + invariants)
      - Vérifier `pnpm test -- tests/unit/fiscalite/ard.test.ts` PASSE
      - Commit : `test(05-04): RED → GREEN ARD VO`

    Étape RED 2 :
      - Créer tests/unit/fiscalite/tableau-amortissement.test.ts avec test 10 (3 cas helpers d'agrégation)
      - Créer minimum src/domain/fiscalite/amortissement-exercice.ts (VO 1 ligne) + tableau-amortissement.ts (VO collection avec helpers)
      - Vérifier passe ; commit.

    Étape RED 3 :
      - Créer tests/unit/fiscalite/calculer-amortissement.test.ts avec tests 1-8 (cas limites obligatoires)
      - Vérifier ÉCHEC
      - Implémenter src/application/fiscalite/calculer-amortissement.ts AVEC helpers purs joursDansExercice + estActifPourExercice
      - Itérer cas par cas (test → impl → green) jusqu'à 8/8
      - Commit final : `feat(05-04): implement calculerAmortissement use case pur`

    Étape REFACTOR :
      - Extraire helpers en partie haute du fichier ou dans `src/domain/fiscalite/helpers-amortissement.ts` si > 50 lignes
      - JSDoc complet citant CGI 39, 39 B, BOFIP-BIC-AMT-20-10
      - Vérifier `pnpm test` reste vert
      - Commit : `refactor(05-04): extract helpers amortissement + JSDoc`

    Aucun mock complexe — le use case est PUR. Tests reçoivent simplement des
    Composant builders (cf. tests/_builders/fiscalite.ts).
  </implementation>
</feature>

<additional_tasks>

<task type="auto" tdd="true">
  <name>Task 2: Migration 0019 + AmortissementExercice adapter SQLite + TableauAmortissementRepository</name>
  <files>
    migrations/0019_phase5_amortissement_exercice.sql,
    src/infrastructure/db/kysely-types.ts,
    src/domain/fiscalite/tableau-amortissement-repository.ts,
    src/infrastructure/repositories/tableau-amortissement-repository-sqlite.ts,
    src/application/fiscalite/recalculer-tableau-amortissement.ts,
    tests/integration/repositories/tableau-amortissement-repository-sqlite.test.ts,
    tests/bdd/features/fiscalite-amortissement.feature,
    tests/bdd/features/fiscalite-ard.feature
  </files>
  <read_first>
    migrations/0009_phase3_bail_indexations.sql,
    src/infrastructure/repositories/bail-indexation-repository-sqlite.ts,
    src/domain/locatif/bail-indexation.ts,
    src/domain/fiscalite/amortissement-exercice.ts,
    src/application/fiscalite/calculer-amortissement.ts,
    .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-PATTERNS.md
  </read_first>
  <behavior>
    Migration 0019_phase5_amortissement_exercice.sql crée amortissement_exercice :
      id TEXT PRIMARY KEY,
      bien_id TEXT NOT NULL REFERENCES bien(id),
      composant_id TEXT NULL REFERENCES bien_composant(id), -- NULL pour ligne SYNTHESE_BIEN
      exercice INTEGER NOT NULL,
      type_ligne TEXT NOT NULL CHECK (type_ligne IN ('COMPOSANT','SYNTHESE_BIEN')),
      dotation_theorique_centimes INTEGER NOT NULL DEFAULT 0,
      dotation_appliquee_centimes INTEGER NOT NULL DEFAULT 0,
      ard_genere_centimes INTEGER NOT NULL DEFAULT 0,
      ard_cumule_disponible_centimes INTEGER NULL,  -- non null si type_ligne='SYNTHESE_BIEN'
      ard_consomme_centimes INTEGER NULL,
      cree_le TEXT NOT NULL DEFAULT (datetime('now'))
    + UNIQUE (bien_id, composant_id, exercice) -- append-only protection
    + index idx_amort_bien_exercice (bien_id, exercice)

    Pattern append-only strict : (1) le adapter NE fait PAS de onConflict ; (2)
    une réinsertion pour (bien_id, composant_id, exercice) lève UNIQUE violation.

    Repository :
      - enregistrerBatch(lignes: AmortissementExercice[], trx?): INSERT batch ; PAS de onConflict (append-only strict, pattern bail-indexation-repository-sqlite.ts L40-58).
      - listerParBienExercice(bienId, exercice) : SELECT WHERE bien_id = ? AND exercice = ? → versDomaine[]
      - dernierArdCumule(bienId, exerciceMax) : SELECT ard_cumule_disponible_centimes FROM amortissement_exercice WHERE bien_id = ? AND type_ligne = 'SYNTHESE_BIEN' AND exercice = ? ORDER BY exercice DESC LIMIT 1 ; retourne Money.zero() si aucun (premier exercice).

    Use case `recalculerTableauAmortissement(bienId, exercice, repos, regleFiscale, clock, db): Promise<TableauAmortissementExercice>` :
      - (1) lookup composants via composantRepo.listerActifsParBien(bienId, today)
      - (2) lookup recettes/charges via recettesRepo + chargesRepo (Plan 02)
      - (3) lookup ardCumuleEnEntree = await tableauAmortissementRepo.dernierArdCumule(bienId, exercice - 1)
      - (4) resultatAvantAmortissement = recettes − chargesDeductibles (max(0, ...))
      - (5) appel calculerAmortissement pur (Task 1)
      - (6) retour TableauAmortissementExercice (NE PAS PERSISTER ICI — la persistance c'est cloturer-exercice Plan 06 ; recalculer-tableau est lecture-seule pour pré-affichage)

    Tests intégration tableau-amortissement-repository-sqlite.test.ts :
      - Setup in-memory + appliquerToutesMigrations
      - Insère 1 bien + 2 composants + 1 batch de 3 AmortissementExercice (2 COMPOSANT + 1 SYNTHESE_BIEN)
      - listerParBienExercice retourne 3 lignes
      - 2e enregistrerBatch sur même (bien, composant, exercice) → throw UNIQUE
      - dernierArdCumule retourne la valeur du SYNTHESE_BIEN exercice

    Feature `tests/bdd/features/fiscalite-amortissement.feature` (@fis-04-amortissement @phase5) :
      - Scénario : gros_oeuvre 200k € acquis 2026-03-15, résultat 10k, ARD entrée 0 → dotation 4k €
      - Scénario : terrain dotation toujours 0
      - Scénario : sortie composant mi-année → prorata
      - Scénario : résultat 0 → 100 % ARD

    Feature `tests/bdd/features/fiscalite-ard.feature` (@fis-04-ard @phase5) :
      - Scénario : ARD reportée année N+1 consommée en priorité
      - Scénario : ARD cumulé inchangé après soft-delete encaissement post-clôture (test ajouté Plan 06 après cloturer-exercice)
  </behavior>
  <action>
    Créer 0019_phase5_amortissement_exercice.sql avec le DDL ci-dessus. Pattern
    bracketed BEGIN/COMMIT + CREATE TABLE IF NOT EXISTS + UNIQUE constraint.

    Étendre kysely-types.ts avec AmortissementExerciceTable.

    Créer src/domain/fiscalite/tableau-amortissement-repository.ts (port).

    Créer src/infrastructure/repositories/tableau-amortissement-repository-sqlite.ts
    en suivant pattern src/infrastructure/repositories/bail-indexation-repository-sqlite.ts.
    enregistrerBatch utilise insertInto.values([]).execute() — PAS de onConflict.

    Créer src/application/fiscalite/recalculer-tableau-amortissement.ts qui
    orchestre les lookups et appelle calculerAmortissement (Task 1) — fonction
    impure (I/O lookups) qui retourne le VO TableauAmortissementExercice.

    Tests intégration in-memory pattern enc02.steps.ts L71-79.

    Poser les 2 fichiers .feature sans steps (steps wirés Task 3 et Plan 06 pour
    cloture).

    Appliquer la migration : `pnpm db:migrate`.
  </action>
  <verify>
    <automated>pnpm db:migrate && pnpm test -- tests/integration/repositories/tableau-amortissement-repository-sqlite.test.ts && pnpm typecheck && pnpm lint:deps</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm db:migrate` exits 0.
    - `pnpm typecheck` exits 0.
    - `pnpm test -- tests/integration/repositories/tableau-amortissement-repository-sqlite.test.ts` exits 0 — au moins 3 cas (batch + UNIQUE + dernierArdCumule).
    - `grep -c "append-only\\|onConflict" src/infrastructure/repositories/tableau-amortissement-repository-sqlite.ts` (le mot 'append-only' présent OU 'onConflict' ABSENT — vérifier l'absence de onConflict via `! grep -q "onConflict"`) → l'adapter NE doit PAS contenir onConflict.
    - `grep -c "recalculerTableauAmortissement" src/application/fiscalite/recalculer-tableau-amortissement.ts` retourne au moins 1.
    - `pnpm lint:deps` exits 0.
  </acceptance_criteria>
  <done>
    Migration 0019 appliquée + AmortissementExercice round-trippable + repo
    append-only strict + use case recalculer-tableau-amortissement disponible
    pour S4 (Task 3) et cloturer-exercice (Plan 06).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Page S4 tableau d'amortissement + route + partial + main.ts + steps BDD calculer-amortissement</name>
  <files>
    src/web/routes/fiscalite/amortissement.ts,
    src/web/views/pages/fiscalite/tableau-amortissement.ejs,
    src/web/views/partials/partial-tableau-amortissement.ejs,
    src/main.ts,
    tests/bdd/step_definitions/fiscalite-amortissement.steps.ts
  </files>
  <read_first>
    src/web/routes/baux.ts,
    src/web/views/partials/data-table.ejs,
    src/web/views/pages/biens/liste.ejs,
    src/application/fiscalite/recalculer-tableau-amortissement.ts,
    src/application/fiscalite/calculer-amortissement.ts,
    src/main.ts,
    tests/bdd/step_definitions/enc02.steps.ts,
    .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-UI-SPEC.md
  </read_first>
  <action>
    Route GET /biens/:bienId/fiscalite/amortissement?annee={N} :
      - lookup bien (404 si introuvable) ; lookup vf (302 redirect /biens/:bienId/fiscalite/activer si pas de vf — bien n'est pas activé)
      - exercice = parseInt(req.query.annee) ; défaut année courante (clock.aujourdhui().year)
      - appel recalculerTableauAmortissement(bienId, exercice, repos, regleFiscale, clock, db)
      - render pages/fiscalite/tableau-amortissement.ejs avec { bien, exercice, tableau, lignes: tableau.dotationParComposant }

    Page `pages/fiscalite/tableau-amortissement.ejs` (UI-SPEC §S4 L130-136) :
      - include('layout-debut', { titre: 'Amortissement — ' + bien.adresse.enLigne() + ' ' + exercice, breadcrumbs, navActive: 'fiscalite' })
      - <h1>Tableau d'amortissement</h1> + sous-titre <h2>{bien.adresse} — exercice {exercice}</h2>
      - Liens "Année préc/suiv"
      - Bandeau pédagogique : "L'ARD est reportable sans limite (CGI art. 39 B). Il sera imputé en priorité sur les exercices futurs." — fond --couleur-neutre-bg + texte 14px
      - include partial-tableau-amortissement (avec colonnes Exercice, Composant, Dotation théorique, Dotation appliquée, ARD généré, ARD cumulé disponible, ARD consommé)
      - <tfoot> avec totaux (font-weight: 600)
      - empty-state.ejs si tableau.dotationParComposant.length === 0

    Partial `partial-tableau-amortissement.ejs` (réutilise data-table.ejs) :
      - Variables : ariaLabel, lignes (Array de ligne { composantLabel, dotationTheorique, dotationAppliquee, ardGenere }), ardCumuleDisponible, ardConsomme, exercice
      - Inclusion : `<%- include('data-table', { ariaLabel: 'Tableau d\'amortissement '+...+' '+exercice, colonnes: [...], lignes: [...] }) %>` (pattern 05-PATTERNS.md L688-711)
      - Toutes colonnes montant : class="numeric" ; tabular-nums via CSS app.css existant.

    Wiring src/main.ts : import recalculerTableauAmortissement + repos + RegleFiscaleProvider ; appeler registerFiscaliteAmortissementRoutes(app, { ... }).

    Steps BDD `fiscalite-amortissement.steps.ts` :
      - Given 'un Bien avec valorisation fiscale activée le {date}' → insertion via vfRepo (Plan 03 task 2)
      - Given 'un Composant {type} de {N} € acquis le {date}' → insertion via composantRepo
      - Given 'des recettes annuelles {N} € pour exercice {N}' → fixtures encaissements/echeances/bail (Phase 2)
      - Given 'des charges qualifiées {N} € pour exercice {N}' → fixtures justificatifs qualifiés (Plan 02)
      - When 'on demande le tableau d\'amortissement {N}' → appel direct recalculerTableauAmortissement ou GET inject Fastify
      - Then 'le composant {type} a dotation théorique {N} € pour exercice {N}' → assert
      - Then 'le composant {type} a dotation appliquée {N} € et ARD généré {N} €' → assert
      - Then 'ARD cumulé disponible exercice {N} = {N} €' → assert

    Wirer les scénarios @fis-04-amortissement et @fis-04-ard posés Task 2.
  </action>
  <verify>
    <automated>pnpm test -- tests/bdd && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier src/web/routes/fiscalite/amortissement.ts existe et expose GET /biens/:bienId/fiscalite/amortissement.
    - Le fichier src/web/views/pages/fiscalite/tableau-amortissement.ejs existe et inclut layout-debut/fin.
    - `grep -c "CGI art. 39 B\\|ARD\\|reportable" src/web/views/pages/fiscalite/tableau-amortissement.ejs` retourne au moins 2 (bandeau pédagogique + citation).
    - Le fichier src/web/views/partials/partial-tableau-amortissement.ejs existe et include data-table.
    - `grep -c "registerFiscaliteAmortissement\\|/fiscalite/amortissement" src/main.ts` retourne au moins 1.
    - `pnpm test -- tests/bdd` exits 0 — scénarios @fis-04-amortissement (au moins 4) + @fis-04-ard (au moins 1) verts.
    - `pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>
    Page S4 fonctionnelle. L'utilisateur peut consulter le tableau d'amortissement
    de son bien pour une année donnée AVANT toute clôture (pré-affichage
    lecture-seule). Couverture BDD @fis-04 verte avec tous les cas limites
    G1.6/G1.7/plafond résultat/ARD reportée.
  </done>
</task>

</additional_tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| calculerAmortissement (pur) → caller | Function pure : input dictates output. Aucun I/O, aucun side-effect. |
| Read-model AmortissementExercice → DB | Append-only strict (UNIQUE bien_id+composant_id+exercice). Une réinsertion = erreur. |
| Pre-affichage S4 → recalcul tableau | Use case recalculer-tableau-amortissement N'ENREGISTRE PAS (lecture-seule). Évite la corruption pré-clôture. |
| Multiplication BigInt × jours | Money.multiplyByFraction borne num/den (0 ≤ num ≤ den) → throw si jours > 365. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | Recalcul incohérent entre 2 affichages | mitigate | Le use case est PUR — pour mêmes inputs, mêmes outputs. ardCumuleEnEntree lu de DB → cohérent tant que pas de clôture concurrent. SQLite single-writer + transaction Kysely garantit l'isolement. |
| T-05-04-02 | Tampering | Append-only violé par onConflict | mitigate | Adapter NE doit PAS contenir onConflict. Vérification automatisée dans acceptance_criteria. |
| T-05-04-03 | Repudiation | Calcul amortissement sans trace | mitigate | À la clôture (Plan 06), DeclarationAnnuelle persiste composants_snapshot_json + le tableau dans AmortissementExercice (append-only). Avant clôture, recalcul lecture-seule à chaque requête. |
| T-05-04-04 | Information Disclosure | Erreur arithmétique > MAX_SAFE_INTEGER | mitigate | Money.toSqliteInteger throw si overflow. Pour bailleurs LMNP V1 (loyers < 100k €/mois → recettes < 1.2M €/an), aucun overflow possible. |
| T-05-04-05 | DoS | Composants[] avec 1000 entrées | accept | Limite naturelle : 5 amortissables fixes BOFIP + N améliorations (rare, < 10). Pas de borne explicite. |
| T-05-04-06 | Tampering | Calcul prorata jours float au lieu BigInt | mitigate | Money.multiplyByFraction(jours: bigint, 365n: bigint) — toute la chaîne est BigInt. Aucun float autorisé dans src/domain/fiscalite/ (anti-pattern §1 CONTEXT.md). |
| T-05-04-07 | Elevation of Privilege | Modification post-clôture du calcul | mitigate | Plan 06 verrouille DeclarationAnnuelle.statut = 'cloture'. Toute modif passe par DeclarationCorrigee (append-only nouvelle ligne). |
</threat_model>

<verification>
1. `pnpm test -- tests/unit/fiscalite/` → 100 % cas limites G1.6, G1.7, plafond résultat, ARD reportée
2. `pnpm typecheck` → 0 erreur
3. `pnpm lint:deps` → 0 erreur (depuis src/domain/ et src/application/fiscalite/)
4. `pnpm test -- tests/bdd` → scénarios @fis-04-amortissement + @fis-04-ard verts
5. `grep -rE "import.*from.*infrastructure|import.*from.*kysely" src/domain/fiscalite/ src/application/fiscalite/calculer-amortissement.ts` → 0 (use case pur)
6. `! grep -q "onConflict" src/infrastructure/repositories/tableau-amortissement-repository-sqlite.ts` (append-only strict)
7. Citations doctrinaires : CGI 39, 39 B, BOFIP-BIC-AMT-20-10, BOFIP-BIC-AMT-20-40, D-FIS-G1.6, G1.7 dans tests & JSDoc
</verification>

<success_criteria>
- Migration 0019 amortissement_exercice appliquée (append-only strict)
- ARD VO + AmortissementExercice + TableauAmortissementExercice immutables
- calculerAmortissement PUR — 100 % couverture cas limites CONTEXT.md L249-252
- tableau-amortissement-repository-sqlite : append-only (NE PAS contenir onConflict)
- Use case recalculer-tableau-amortissement lecture-seule pour pré-affichage S4
- Page S4 fonctionnelle avec bandeau pédagogique ARD
- L'utilisateur peut consulter son tableau d'amortissement pour une année donnée
- Citations doctrinaires complètes (CGI 39 / 39 B / BOFIP-BIC-AMT-20-10 et 20-40)
</success_criteria>

<output>
Créer .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-04-SUMMARY.md
en suivant @.claude/get-shit-done/templates/summary.md.
</output>
</content>
</invoke>