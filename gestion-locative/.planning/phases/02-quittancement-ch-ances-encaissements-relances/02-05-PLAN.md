---
phase: 02
plan: 05
plan_id: "02-05"
type: execute
wave: 5
depends_on: ["02-01", "02-02", "02-03", "02-04"]
files_modified:
  - src/domain/encaissements/impaye.ts
  - src/application/encaissements/lister-impayes.ts
  - src/web/routes/impayes.ts
  - src/web/views/pages/impayes/liste.ejs
  - src/web/views/partials/sidebar-nav.ejs
  - src/main.ts
  - tests/unit/encaissements/lister-impayes.test.ts
  - tests/integration/repositories/echeance-loyer-repository-sqlite.test.ts
  - tests/bdd/features/quittancement.feature
  - tests/bdd/step_definitions/quittancement.steps.ts
autonomous: true
requirements: ["ENC-04"]
user_setup: []

mvp_split_rationale: |
  ENC-04 (impayés) est un calcul dérivé pur — pas de nouvelle table ni de nouvelle action utilisateur destructive.
  C'est essentiellement une nouvelle VUE des EcheanceLoyer existantes. Plan léger (1-2 tasks, ~20% contexte).
  WAVE 5 SÉQUENTIEL APRÈS 02-04 : initialement envisagé en parallèle wave 4 avec 02-04, mais 4 fichiers partagés
  (sidebar-nav.ejs, main.ts, quittancement.feature, quittancement.steps.ts) imposent une exécution séquentielle
  conformément à la règle "same-wave plans must have zero files_modified overlap" du dependency_graph. La
  dépendance explicite sur 02-04 garantit que les ajouts sidebar/main.ts/feature/steps sont mergeables proprement.

must_haves:
  truths:
    - "L'utilisateur peut accéder à GET /impayes (depuis sidebar) listant toutes les EcheanceLoyer en retard ou partiellement payées, triées par jour d'échéance ASC (plus anciennes en premier)."
    - "Le calcul 'en retard' est dérivé via Clock : `statut != 'payee' && statut != 'annulee' && jour_echeance_attendue < clock.aujourdhui()`."
    - "Chaque ligne affiche : locataire (nom complet), bail (id court), période, total dû, montant déjà payé (somme actifs), reste dû, nombre de jours de retard (today - jour_echeance_attendue), badge statut couleur (rouge/ambre/vert per UI-SPEC §Color)."
    - "La page affiche un total global 'X € impayés sur Y locataires'."
    - "Si aucun impayé : empty state 'Tous les loyers sont à jour 🎉' (UI-SPEC §Empty States)."
    - "Filtre par locataire optionnel via query param `?locataire=:id` — réduit la liste aux échéances de ce locataire."
    - "Page READ-ONLY — aucune action destructive ni modification. Liens vers fiche échéance (pour saisir un encaissement) et fiche bail."
  artifacts:
    - path: "src/domain/encaissements/impaye.ts"
      provides: "Type Impaye DTO + helpers de calcul dérivé (estEnRetard, joursDeRetard, resteDu)"
      exports: ["Impaye", "calculerImpaye"]
    - path: "src/application/encaissements/lister-impayes.ts"
      provides: "Use case agrégation EcheanceLoyer non payees + sommes encaissements actifs + filtres"
      exports: ["listerImpayes"]
    - path: "src/web/routes/impayes.ts"
      provides: "Routes GET /impayes (+ optional ?locataire=)"
      exports: ["plugin"]
    - path: "src/web/views/pages/impayes/liste.ejs"
      provides: "Page liste impayés avec total global + table colorée"
  key_links:
    - from: "src/application/encaissements/lister-impayes.ts"
      to: "src/domain/encaissements/echeance-loyer-repository.ts"
      via: "echeanceLoyerRepo.listerNonPayees() puis pour chaque échéance encaissementRepo.sommePaieeParEcheance"
      pattern: "listerNonPayees"
    - from: "src/web/routes/impayes.ts"
      to: "src/domain/_shared/clock.ts"
      via: "calcul en_retard via clock.aujourdhui() — dérivé non stocké"
      pattern: "aujourdhui"
    - from: "src/web/views/pages/impayes/liste.ejs"
      to: "src/web/routes/echeances.ts"
      via: "lien 'Voir l'échéance' vers fiche pour saisir un encaissement"
      pattern: "/echeances/${id}"
---

<objective>
Vertical slice ENC-04 : page Impayés agrégeant les EcheanceLoyer non entièrement payées avec leurs montants restants dus, jours de retard, et locataires. Lecture seule.

Purpose: ENC-04 = visibilité du suivi des paiements pour l'utilisateur. Pas de nouvelle persistance — calcul dérivé pur. C'est la page d'entrée pour la suggestion de relance (plan 02-06).
Output: GET /impayes + filtre locataire + lien depuis sidebar + empty state si tout est à jour.
</objective>

<execution_context>
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/workflows/execute-plan.md
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-CONTEXT.md
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-RESEARCH.md
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-PATTERNS.md
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-UI-SPEC.md
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-01-PLAN.md
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-02-PLAN.md
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-03-PLAN.md
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-04-PLAN.md
@CLAUDE.md
@BDD_PRACTICES.md
@UI_DESIGN.md
@UX_DESIGN.md
@ACCESSIBILITY.md
@src/domain/_shared/clock.ts
@src/domain/encaissements/echeance-loyer.ts
@src/domain/encaissements/echeance-loyer-repository.ts
@src/domain/encaissements/encaissement-repository.ts
@src/domain/locatif/bail.ts
@src/domain/locatif/locataire.ts
@src/domain/locatif/locataire-repository.ts
@src/infrastructure/repositories/echeance-loyer-repository-sqlite.ts
@src/web/routes/echeances.ts
@src/web/views/pages/echeances/liste.ejs
@src/web/views/partials/sidebar-nav.ejs
@src/web/views/partials/data-table.ejs
@src/web/views/partials/empty-state.ejs
</context>

<interfaces>
Contrats clés (Phases 02-01..02-04) :

- `Impaye` DTO (lecture seule, pas un agrégat persisté) : `{ echeanceId, bailId, locataireId, locataireNomComplet, periodeDebut, periodeFin, jourEcheanceAttendue, total: Money, sommePaiee: Money, resteDu: Money, statut: StatutEcheanceLoyer, joursDeRetard: number, estEnRetard: boolean }`.
- `calculerImpaye(echeance: EcheanceLoyer, sommePaiee: Money, locataire: Locataire, bail: Bail, today: PlainDate): Impaye` — fonction pure (testable sans I/O).
  - `joursDeRetard = today > jourEcheanceAttendue ? Temporal.PlainDate.until(jourEcheanceAttendue, today).total({unit: 'day'}) : 0`.
  - `resteDu = total.soustraire(sommePaiee)` SI sommePaiee.lte(total), sinon `Money.zero()` (sur-paiement = 0 reste dû).
  - `estEnRetard = statut !== 'payee' && statut !== 'annulee' && Temporal.PlainDate.compare(today, jourEcheanceAttendue) > 0`.
- `listerImpayes(filtres: { locataireId?: LocataireId }, repos: { echeanceLoyerRepo, encaissementRepo, bailRepo, locataireRepo }, clock): Promise<Impaye[]>`.
  - echeanceLoyerRepo.listerNonPayees() → toutes les echeance.statut IN ('en_attente', 'partiellement_payee') AND annule_le IS NULL.
  - Pour chaque échéance : lookup bail (via echeance.bailId) + locataire (bail.locataireId) + sommePaiee (encaissementRepo.sommePaieeParEcheance).
  - Si filtres.locataireId : ne garder que les Impaye dont bail.locataireId === filtres.locataireId.
  - Tri par jour_echeance_attendue ASC (plus anciens en premier).
- Page `/impayes/liste.ejs` : data-table colonnes [Locataire, Bail, Période, Total, Payé, Reste dû, Statut+Retard, Actions]. Empty state si liste vide (UI-SPEC §Empty States).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests rouges Wave 0 — calculerImpaye + listerImpayes + BDD ENC-04</name>
  <read_first>
    - tests/unit/encaissements/recalculer-statut-echeance.test.ts (analog — patterns mock repos)
    - tests/_builders/encaissements.ts (créés 02-02..02-04)
    - tests/_builders/locatif.ts (unBailValide, unLocataireValide)
    - tests/integration/repositories/echeance-loyer-repository-sqlite.test.ts (extension à ajouter — listerNonPayees)
    - tests/bdd/features/quittancement.feature (à étendre)
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-RESEARCH.md (§Topic 3 sommePaieeParEcheance — déjà implémenté 02-03)
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-UI-SPEC.md (§Screen Inventory "Impayés" + §Color rules statut couleur + §Empty States "Tous les loyers sont à jour" + §Component Patterns "Badge statut")
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-PATTERNS.md (§routes/impayes + §views/impayes/liste.ejs)
  </read_first>
  <behavior>
    - T1 calculerImpaye.test (unit) : echeance statut='en_attente', total 700€, sommePaiee 0€, today < jourEcheanceAttendue → Impaye.resteDu=700€, joursDeRetard=0, estEnRetard=false.
    - T2 : statut='partiellement_payee', total 700€, sommePaiee 300€, today < jourEcheanceAttendue → resteDu=400€, joursDeRetard=0, estEnRetard=false.
    - T3 : statut='en_attente', total 700€, sommePaiee 0€, today > jourEcheanceAttendue de 5 jours → resteDu=700€, joursDeRetard=5, estEnRetard=true.
    - T4 : statut='partiellement_payee', total 700€, sommePaiee 300€, today > jourEcheanceAttendue de 35 jours → joursDeRetard=35, estEnRetard=true.
    - T5 : statut='payee' (sommePaiee == total OU sur-paiement) → estEnRetard=false (filtré upstream — testé pour confirmer que la fonction reste correcte si appelée par erreur sur une payée).
    - T6 : statut='annulee' → estEnRetard=false.
    - T7 : sommePaiee > total (sur-paiement) → resteDu = Money.zero().
    - T8 listerImpayes.test (unit) : 3 echeances (en_attente, partiellement_payee, payee), listerNonPayees retourne les 2 premières. Pour chaque, calcul Impaye, retour ordonné par jour_echeance_attendue ASC.
    - T9 listerImpayes.test : filtres locataireId X → retour ne contient que les Impaye dont bail.locataireId === X.
    - T10 integration echeance-loyer-repo : `listerNonPayees` retourne EcheanceLoyer avec statut ∈ {'en_attente', 'partiellement_payee'} AND annule_le IS NULL, ordonné par jour_echeance_attendue ASC. Pour test, créer 4 échéances en DB (1 payee, 1 annulee, 1 en_attente, 1 partiellement_payee) et vérifier que 2 sont retournées.
    - T11 BDD @enc-04 "Page Impayés vide" : Given Bail activé 12 échéances + Encaissement 700€ exact sur chaque échéance (12 paiements complets), all statut='payee' → GET /impayes affiche empty state "Tous les loyers sont à jour".
    - T12 BDD @enc-04 "Liste impayés mixte" : Given Bail activé 12 échéances + 3 encaissements (1 exact pour echeance 1, 1 partiel pour echeance 2, rien pour echeances 3-12), ClockFixe au 15 mai 2026 (après date d'échéance des 3 premières) → GET /impayes liste 11 lignes (12-1 payée), avec :
      - 1ère ligne : echeance 2 partiellement_payee, reste dû = 700€ - payé.
      - Lignes 2-11 : echeances 3-12 en_attente, certaines en retard (selon clock).
      - Total global affiché en haut de page.
    - T13 BDD @enc-04 "Filtre locataire" : Given 2 baux avec impayés (différents locataires), GET /impayes?locataire={id1} → seules les échéances liées au locataire 1 sont affichées.
    - T14 BDD @enc-04 "Tri par ancienneté" : impayés triés du plus ancien (jour_echeance_attendue le plus petit) au plus récent.
  </behavior>
  <action>
    TDD outside-in — créer EXCLUSIVEMENT les tests. ROUGES attendus.

    Créer/modifier :
    1. `tests/unit/encaissements/lister-impayes.test.ts` (NOUVEAU) : T1-T9. Pour les fonctions pures (T1-T7) tester directement `calculerImpaye(echeance, sommePaiee, locataire, bail, today)`. Pour T8-T9 mocker les repos (vitest mock ou stubs manuels). ClockFixe pour today.
    2. ÉTENDRE `tests/integration/repositories/echeance-loyer-repository-sqlite.test.ts` (créé 02-02) avec T10. Builder approprié.
    3. ÉTENDRE `tests/bdd/features/quittancement.feature` avec 4 scenarios tag `@enc-04`.
    4. **ÉTENDRE** (pas écraser — M3 plan-checker) `tests/bdd/step_definitions/quittancement.steps.ts` (créé 02-02, étendu 02-03) avec steps pour Then la page affiche "Tous les loyers sont à jour" / contient X lignes impayées / total = Y €.
       - **IMPORTANT** : NE PAS écraser les steps `Given un Bail activé`, `Given un Encaissement de X€`, `Given une échéance partiellement_payee` etc. déjà ajoutés en 02-02 et 02-03.
       - Si conflit de nom de step (improbable car les steps `@enc-04` portent sur la page /impayes spécifiquement) → préfixer par contexte : `impaye_la_page_affiche_X_lignes`, `impaye_total_global_egal_Y`, etc.
       - Tests rouges après l'extension : `pnpm test:bdd --tags @enc-04` échoue, mais les tags `@enc-02` et `@enc-03` continuent de passer (non-régression Wave 0 → Wave 5).

    Tests ÉCHOUENT (uniquement les nouveaux scenarios @enc-04). Commit : `test(02-05): tests rouges calculerImpaye + listerImpayes + ENC-04 (Wave 0)`.
  </action>
  <verify>
    <automated>cd /Users/valentinshodo/Projects/toolbox/gestion-locative &amp;&amp; pnpm test 2>&amp;1 | grep -E "FAIL|fail" | head -15 ; ls tests/unit/encaissements/lister-impayes.test.ts</automated>
  </verify>
  <done>
    - Tests Wave 0 ROUGES (1 fichier test unit + extensions integration + BDD feature).
    - Commit créé.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Domain calculerImpaye + use case listerImpayes + route + view + sidebar + faire passer tous les tests au vert</name>
  <read_first>
    - src/domain/encaissements/echeance-loyer.ts
    - src/domain/encaissements/echeance-loyer-repository.ts (méthode listerNonPayees déjà déclarée 02-02 — vérifier qu'elle filtre statut ∈ {'en_attente','partiellement_payee'} ; sinon corriger)
    - src/domain/encaissements/encaissement-repository.ts (sommePaieeParEcheance — créé 02-03)
    - src/application/encaissements/recalculer-statut-echeance.ts (analog use case multi-repos)
    - src/web/routes/encaissements.ts (analog plugin Fastify)
    - src/web/views/pages/encaissements/liste.ejs (analog data-table avec stats)
    - src/web/views/partials/statut-badge.ejs (créé 02-02)
    - src/web/views/partials/empty-state.ejs
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-UI-SPEC.md (§Screen Inventory "Impayés" + §Color "Retard" rouge / "partiellement_payee" ambre / "en_attente" gris + §Empty State "Tous les loyers sont à jour" + §PDF Layout (n/a — page sans PDF))
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-PATTERNS.md (§routes/impayes + §views/impayes/liste)
  </read_first>
  <action>
    Créer/modifier :

    1. `src/domain/encaissements/impaye.ts` (NOUVEAU) :
       - `export interface Impaye { echeanceId: EcheanceLoyerId; bailId: BailId; locataireId: LocataireId; locataireNomComplet: string; periodeDebut: Temporal.PlainDate; periodeFin: Temporal.PlainDate; jourEcheanceAttendue: Temporal.PlainDate; total: Money; sommePaiee: Money; resteDu: Money; statut: StatutEcheanceLoyer; joursDeRetard: number; estEnRetard: boolean }`.
       - `export function calculerImpaye(echeance: EcheanceLoyer, sommePaiee: Money, locataire: Locataire, bail: Bail, today: Temporal.PlainDate): Impaye` — fonction pure :
         - `const resteDu = sommePaiee.lte(echeance.total) ? echeance.total.soustraire(sommePaiee) : Money.zero();`
         - `const estEnRetard = echeance.statut !== 'payee' && echeance.statut !== 'annulee' && Temporal.PlainDate.compare(today, echeance.jourEcheanceAttendue) > 0;`
         - `const joursDeRetard = estEnRetard ? Math.floor(echeance.jourEcheanceAttendue.until(today, { largestUnit: 'day' }).total({ unit: 'day' })) : 0;`
         - Retourner objet complet.

    2. `src/application/encaissements/lister-impayes.ts` (NOUVEAU) :
       - Signature : `listerImpayes(filtres: { locataireId?: LocataireId }, repos: { echeanceLoyerRepo, encaissementRepo, bailRepo, locataireRepo }, clock: Clock): Promise<Impaye[]>`.
       - echeancesNonPayees = await echeanceLoyerRepo.listerNonPayees().
       - Map en parallèle : `await Promise.all(echeancesNonPayees.map(async (e) => { bail = bailRepo.trouverParId(e.bailId); locataire = locataireRepo.trouverParId(bail.locataireId); sommePaiee = await encaissementRepo.sommePaieeParEcheance(e.id); return calculerImpaye(e, sommePaiee, locataire, bail, clock.aujourdhui()); }))`.
       - Filtre : si filtres.locataireId, `impayes = impayes.filter(i => i.locataireId === filtres.locataireId)`.
       - Tri : `impayes.sort((a, b) => Temporal.PlainDate.compare(a.jourEcheanceAttendue, b.jourEcheanceAttendue))`.
       - Retourner.

    3. VÉRIFIER `src/infrastructure/repositories/echeance-loyer-repository-sqlite.ts` (créé 02-02) :
       - `listerNonPayees()` doit filtrer `statut IN ('en_attente', 'partiellement_payee') AND annule_le IS NULL` (pas 'payee' ni 'annulee').
       - Si pas conforme, corriger (probablement filtrait juste `statut != 'payee'` — à étendre pour exclure 'annulee').
       - Test T10 valide.

    4. `src/web/routes/impayes.ts` (NOUVEAU) :
       - `export async function plugin(app, opts: { echeanceLoyerRepo, encaissementRepo, bailRepo, locataireRepo, clock }): Promise<void>`.
       - `GET /impayes` : `const { locataire } = req.query as { locataire?: string }`. `const filtres = locataire ? { locataireId: locataire as LocataireId } : {}`. `const impayes = await listerImpayes(filtres, opts, opts.clock)`. `const totalGlobal = impayes.reduce((acc, i) => acc.additionner(i.resteDu), Money.zero())`. `const locatairesUniques = new Set(impayes.map(i => i.locataireId))`. `const allLocataires = await opts.locataireRepo.listerTous()` (pour le filtre select). reply.view('pages/impayes/liste.ejs', { impayes, totalGlobal, nbLocataires: locatairesUniques.size, allLocataires, locataireFiltre: locataire, navActive: 'impayes' }).

    5. `src/web/views/pages/impayes/liste.ejs` (NOUVEAU) :
       - layout-debut titre "Impayés", breadcrumbs, navActive 'impayes'.
       - `<h1>Impayés et retards</h1>`.
       - Si impayes.length === 0 : empty-state.ejs `{ heading: "Tous les loyers sont à jour", body: "Aucun retard ou impayé détecté à ce jour.", ctaLabel: null, ctaUrl: null }` (UI-SPEC §Empty States).
       - Sinon :
         - Stats en tête : `<p><strong><%= formatMoney(totalGlobal) %></strong> impayés sur <%= nbLocataires %> locataire<%= nbLocataires > 1 ? 's' : '' %>.</p>`.
         - Filtre locataire : `<form method="GET" action="/impayes">` avec `<select name="locataire">` (toujours-Toutes + chaque locataire). Soumission GET.
         - data-table.ejs avec colonnes :
           - "Locataire" : nomComplet + lien fiche locataire `/locataires/${id}`.
           - "Bail" : id court (8 chars) + lien `/baux/${id}`.
           - "Période" : formatPeriode(periodeDebut).
           - "Total" : formatMoney(total) (right-aligned).
           - "Payé" : formatMoney(sommePaiee).
           - "Reste dû" : formatMoney(resteDu) — couleur rouge si > 0.
           - "Statut" : statut-badge.ejs avec libelle "Partiel" si partiellement_payee, "En retard" si estEnRetard, "En attente" sinon. Couleur per UI-SPEC §Color (rouge si estEnRetard, ambre si partiel, gris si en_attente).
           - "Retard" : si estEnRetard, "X jours" en rouge ; sinon "—".
           - "Actions" : lien "Saisir un encaissement" `/encaissements/nouveau?echeance=${echeanceId}` + lien "Voir échéance" `/echeances/${echeanceId}` (placeholder pour fiche échéance future).
       - layout-fin.

    6. ÉTENDRE `src/web/views/partials/sidebar-nav.ejs` : ajouter sous-lien "Impayés" → /impayes dans le `<details>` "Encaissements" (entre "Quittances" et le suivant).

    7. ÉTENDRE `src/main.ts` :
       - Import `plugin as impayesPlugin`.
       - Registrer `await app.register(impayesPlugin, { echeanceLoyerRepo, encaissementRepo, bailRepo, locataireRepo, clock });`.

    Sécurité (cf. <threat_model>) :
    - Query param `locataire` : typé string UUID-shaped — utiliser `as LocataireId` après cast simple ; pas de SQL injection (Kysely paramétré). Si la chaîne n'est pas un UUID valide, le filtre renvoie simplement une liste vide.
    - Page READ-ONLY — aucune écriture déclenchée depuis cette page.

    Vérifs : `pnpm test:bdd -- --tags @enc-04` 4 scenarios VERTS. `pnpm test` tout VERT. `pnpm tsc --noEmit` 0 erreur.

    Commit : `feat(02-05): page Impayés + use case listerImpayes (ENC-04)`.
  </action>
  <verify>
    <automated>cd /Users/valentinshodo/Projects/toolbox/gestion-locative &amp;&amp; pnpm tsc --noEmit &amp;&amp; pnpm lint &amp;&amp; pnpm lint:deps &amp;&amp; pnpm test &amp;&amp; pnpm test:bdd -- --tags @enc-04</automated>
  </verify>
  <done>
    - Impaye DTO + calculerImpaye fonction pure.
    - listerImpayes use case avec filtres.
    - Route /impayes + view liste avec stats globales + table colorée + empty state.
    - Sidebar étendue.
    - 4 scenarios BDD `@enc-04` VERTS.
    - Tous tests Phase 1 + 02-01..02-04 toujours verts.
    - Commit créé.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigateur user → Fastify GET /impayes?locataire= | Query param non-trusted — utilisé comme filtre Kysely paramétré |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05-01 | Tampering (V5) | GET /impayes?locataire= avec UUID malformé | LOW | mitigate | Pas de validation stricte côté route ; le filtre simple Kysely `where('locataire_id','=', filtreUuid)` retourne juste une liste vide si l'UUID n'existe pas. Pas d'injection (paramétré). |
| T-02-05-02 | Information disclosure | Page Impayés expose noms locataires + montants impayés | LOW | accept | Mono-user V1 — pas d'auth multi-comptes. Données affichées sont celles de l'utilisateur lui-même. RGPD : si partage URL, c'est sa responsabilité. |
| T-02-05-03 | DoS — calcul listerImpayes pour N échéances | N grand (par exemple 200 échéances sur plusieurs baux) → N+1 query | LOW | accept | V1 mono-user LMNP — N typiquement < 100 (12 échéances par bail, < 10 baux). Promise.all en parallèle. Si performance devient un problème (Phase 7+), optimiser via une seule query SQL joint avec SUM agrégé groupBy echeance_id. |
| T-02-05-04 | Integrity — données stale | Page Impayés affiche un statut qui vient d'être modifié (autre onglet ouvert) | LOW | accept | Re-render à chaque GET. Pas de cache. |
</threat_model>

<verification>
- `pnpm tsc --noEmit` exit 0
- `pnpm lint` 0 warning
- `pnpm lint:deps` 0 violation
- `pnpm test` tout VERT
- `pnpm test:bdd -- --tags @enc-04` 4 scenarios PASSED
- calculerImpaye 100 % couvert (fonction pure → tests unit exhaustifs)
- Statut dérivé "en_retard" calculé à la route (D-55 maintenu)
</verification>

<success_criteria>
- ENC-04 satisfait : impayés et retards calculés et affichés par locataire et par période.
- D-55 satisfait : "en_retard" reste dérivé non stocké.
- Page READ-ONLY (pas d'écriture déclenchée).
- Empty state "Tous les loyers sont à jour" satisfait UI-SPEC.
- Tri par ancienneté + filtre locataire optionnel.
- 100 % couverture calculerImpaye (fonction pure → utile Phase 5 quand recettes agrégées).
- Ubiquitous language français (Impaye, joursDeRetard, resteDu, estEnRetard).
</success_criteria>

<output>
After completion, create `.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-05-SUMMARY.md` listant :
- 2 commits (tests rouges / domain+app+web)
- Patterns établis : DTO de lecture (`Impaye`) vs agrégat persisté, fonction pure `calculerImpaye` testable sans I/O, agrégation use case multi-repos parallel (Promise.all)
- Dépendances pour plan 02-06 : Impaye DTO consommé pour calculer les niveaux de relance disponibles (J+10, J+30, J+60 → mapping sur impaye.joursDeRetard)
</output>
