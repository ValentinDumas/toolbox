---
phase: 02
plan: 03
plan_id: "02-03"
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - migrations/0004_phase2_encaissement.sql
  - src/infrastructure/db/kysely-types.ts
  - src/domain/_shared/money.ts
  - src/domain/encaissements/encaissement.ts
  - src/domain/encaissements/encaissement-repository.ts
  - src/domain/encaissements/erreurs.ts
  - src/application/encaissements/creer-encaissement.ts
  - src/application/encaissements/annuler-encaissement.ts
  - src/application/encaissements/recalculer-statut-echeance.ts
  - src/application/encaissements/lister-encaissements.ts
  - src/infrastructure/repositories/encaissement-repository-sqlite.ts
  - src/web/schemas/encaissement-schemas.ts
  - src/web/routes/encaissements.ts
  - src/web/views/pages/encaissements/liste.ejs
  - src/web/views/pages/encaissements/formulaire.ejs
  - src/web/views/pages/encaissements/fiche.ejs
  - src/web/views/pages/echeances/liste.ejs
  - src/web/views/partials/warning-live.ejs
  - src/web/views/partials/sidebar-nav.ejs
  - src/main.ts
  - tests/_builders/encaissements.ts
  - tests/unit/_shared/money.test.ts
  - tests/unit/encaissements/encaissement.test.ts
  - tests/unit/encaissements/creer-encaissement.test.ts
  - tests/unit/encaissements/annuler-encaissement.test.ts
  - tests/unit/encaissements/recalculer-statut-echeance.test.ts
  - tests/integration/repositories/encaissement-repository-sqlite.test.ts
  - tests/bdd/features/quittancement.feature
  - tests/bdd/step_definitions/quittancement.steps.ts
autonomous: true
requirements: ["ENC-03"]
user_setup: []

must_haves:
  truths:
    - "Saisie Encaissement via POST /encaissements (date, montantEuros, signe='positif'|'compensateur', mode, echeanceId)."
    - "Paiement partiel (montant < echeance.total) → statut 'partiellement_payee', PAS de quittance émise (ENC-03 invariant)."
    - "Paiement exact (== total) → statut 'payee'."
    - "Sur-paiement (> total) → statut 'payee' + warning visible 'Trop-perçu de X €' (D-59 non-bloquant)."
    - "Cardinalité N:1 — plusieurs Encaissement par EcheanceLoyer (D-57), somme actifs (annule_le IS NULL) détermine statut."
    - "Annulation = soft-delete (annule_le + raison_annulation set, PAS de DELETE) puis recalcul auto statut (D-60)."
    - "Correction = Encaissement compensateur Money négatif via Money.compensateur(positif) — pas d'UPDATE du montant existant."
    - "Date encaissement permissive avec warnings (D-61) si date < bail.dateDebut ou > today+90j."
    - "Page GET /encaissements liste tous les encaissements (annulés inclus, distingués visuellement)."
    - "Fiche GET /encaissements/:id avec bouton 'Annuler' + modale confirmation (raison required, min 3 chars)."
  artifacts:
    - path: "migrations/0004_phase2_encaissement.sql"
      provides: "Table encaissement (soft-delete annule_le)"
      contains: "CREATE TABLE IF NOT EXISTS encaissement"
    - path: "src/domain/_shared/money.ts (extension)"
      provides: "Money.compensateur(positif) + Money.negation() + Money.estNegatif()"
      exports: ["Money.compensateur"]
    - path: "src/domain/encaissements/encaissement.ts"
      provides: "Agrégat Encaissement + annuler() + ModeEncaissement enum"
      exports: ["Encaissement", "ModeEncaissement"]
    - path: "src/application/encaissements/creer-encaissement.ts"
      provides: "Use case + cross-aggregate (échéance non annulée, bail actif) + recalcul statut + warnings D-61"
      exports: ["creerEncaissement"]
    - path: "src/application/encaissements/annuler-encaissement.ts"
      provides: "Use case soft-delete + recalcul statut"
      exports: ["annulerEncaissement"]
    - path: "src/application/encaissements/recalculer-statut-echeance.ts"
      provides: "Use case interne — somme actifs → statut"
      exports: ["recalculerStatutEcheance"]
    - path: "src/web/routes/encaissements.ts"
      provides: "Routes /encaissements + /encaissements/nouveau + /encaissements/:id + /encaissements/:id/annuler"
      exports: ["plugin"]
  key_links:
    - from: "src/application/encaissements/creer-encaissement.ts"
      to: "src/application/encaissements/recalculer-statut-echeance.ts"
      via: "appelle recalculerStatutEcheance(echeanceId, repos) après persistance"
      pattern: "recalculerStatutEcheance"
    - from: "src/application/encaissements/recalculer-statut-echeance.ts"
      to: "src/infrastructure/repositories/encaissement-repository-sqlite.ts"
      via: "sommePaieeParEcheance(id) — SUM montant_centimes WHERE annule_le IS NULL"
      pattern: "sommePaieeParEcheance"
    - from: "src/application/encaissements/annuler-encaissement.ts"
      to: "src/application/encaissements/recalculer-statut-echeance.ts"
      via: "annulation déclenche recalcul (payee → partiellement_payee si applicable)"
      pattern: "recalculerStatutEcheance"
    - from: "src/web/routes/encaissements.ts"
      to: "src/web/views/partials/warning-live.ejs"
      via: "surPaiement et warnings D-61 affichés via partial aria-live"
      pattern: "warning-live"
---

<objective>
Vertical slice ENC-03 : saisie d'Encaissement avec soft-delete (D-60), paiement partiel/exact/sur-paiement, compensateur Money négatif, recalcul automatique du statut EcheanceLoyer.

Purpose: ENC-03 = cœur audit-friendly Phase 2. Soft-delete + compensateur garantit traçabilité opposable à l'administration fiscale et au locataire. Le recalcul automatique du statut maintient la cohérence sans intervention.
Output: Formulaire saisie + page liste + page fiche + soft-delete + warnings non-bloquants.
</objective>

<execution_context>
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/workflows/execute-plan.md
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-CONTEXT.md
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-RESEARCH.md
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-PATTERNS.md
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-UI-SPEC.md
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-01-PLAN.md
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-02-PLAN.md
@.planning/phases/01-activation-bien-locataire-bail/01-LEARNINGS.md
@CLAUDE.md
@BDD_PRACTICES.md
@UI_DESIGN.md
@UX_DESIGN.md
@ACCESSIBILITY.md
@src/domain/_shared/money.ts
@src/domain/_shared/clock.ts
@src/domain/encaissements/echeance-loyer.ts
@src/domain/encaissements/echeance-loyer-repository.ts
@src/domain/encaissements/erreurs.ts
@src/application/encaissements/activer-bail.ts
@src/infrastructure/repositories/echeance-loyer-repository-sqlite.ts
@src/web/routes/baux.ts
@src/web/routes/echeances.ts
@src/web/views/pages/echeances/liste.ejs
@src/web/views/pages/baux/formulaire.ejs
@src/web/schemas/bail-schemas.ts
@src/web/views/partials/form-field.ejs
@src/web/views/partials/confirm-dialog.ejs
</context>

<interfaces>
Contrats clés (Phase 1 + 02-01 + 02-02) :

- Money à étendre : `static compensateur(positif: Money): Money` factory créant Money à centimes NÉGATIFS (constructeur privé bypassant `fromCentimes`). Méthode `negation(): Money` (alias `Money.compensateur(this)`). Méthode `estNegatif(): boolean`. RESEARCH §Topic 3 Pitfall 5.
- `Encaissement` props : { id?, echeanceId, montant: Money (peut être négatif), date: PlainDate, mode: ModeEncaissement, annuleLe?: PlainDate | null, raisonAnnulation?: string | null }.
- `ModeEncaissement = 'virement' | 'cheque' | 'especes' | 'prelevement' | 'autre'` (D-58).
- `Encaissement.creer(props)` valide mode ∈ liste. PAS de validation date (D-61 permissive). PAS de validation signe (compensateurs autorisés).
- `Encaissement.annuler(raison: string, annuleLe: PlainDate): Encaissement` copy-on-write, throw si déjà annulé.
- `EncaissementRepository` : enregistrer, trouverParId, listerParEcheance(id, {inclureAnnules}), listerTous({inclureAnnules}), sommePaieeParEcheance(id): Money (SUM montant WHERE annule_le IS NULL). PAS de supprimer.
- `recalculerStatutEcheance(echeanceId, repos)` : somme = sommePaieeParEcheance ; si zero → 'en_attente' ; si <total → 'partiellement_payee' ; si ==total → 'payee' ; si >total → 'payee' + surPaiement = somme.soustraire(total). Persister via mettreAJourStatut. Retourner { statut, sommePaiee, surPaiement: Money | null }.
- `creerEncaissement(commande, repos, clock)` : cross-aggregate echeance.annuleLe must be null, bail.actifDepuis must be not null. Warnings D-61 (date < bail.dateDebut, date > today+90j). Retourne { encaissementId, warnings, statut, surPaiement }.
- `annulerEncaissement(commande, repos, clock)` : lookup + annuler + persister + recalcul. Retourne { ancienStatut, nouveauStatut, rebasculee }.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests rouges Wave 0 — Money.compensateur + Encaissement + use cases + integration + BDD ENC-03</name>
  <read_first>
    - tests/unit/_shared/money.test.ts
    - tests/_builders/encaissements.ts (créé partiellement 02-02)
    - tests/unit/encaissements/echeance-loyer.test.ts (créé 02-02 — analog)
    - tests/integration/repositories/echeance-loyer-repository-sqlite.test.ts (créé 02-02 — analog)
    - tests/bdd/features/quittancement.feature (créé 02-02 — à étendre)
    - tests/bdd/step_definitions/quittancement.steps.ts (créé 02-02 — à étendre)
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-RESEARCH.md (§Topic 3 soft-delete + recalculerStatutEcheance + §Topic 4 Pitfall 5 Money négatifs)
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-PATTERNS.md (§encaissement.ts + §creer-encaissement.ts + §annuler-encaissement.ts + §encaissement-repository-sqlite.ts)
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-UI-SPEC.md (§Saisie encaissement + §Warning aria-live + §Destructive Actions Annuler encaissement)
  </read_first>
  <behavior>
    - T1 money.test : `Money.compensateur(fromCentimes(85000n))` → centimes -85000n.
    - T2 money.test : `fromCentimes(85000n).negation()` → centimes -85000n.
    - T3 money.test : `fromCentimes(-100n)` throw InvariantViolated (factory inchangée).
    - T4 money.test : `fromCentimes(80000n).additionner(compensateur(fromCentimes(30000n)))` → 50000n positif.
    - T5 money.test : `compensateur(fromCentimes(80000n)).additionner(fromCentimes(30000n))` → -50000n (négatif accepté).
    - T6 encaissement.test : `Encaissement.creer(propsValide)` retourne agrégat.
    - T7 encaissement.test : mode invalide ('bitcoin') throw InvariantViolated.
    - T8 encaissement.test : `e.annuler('raison', PlainDate)` retourne nouveau Encaissement annulé.
    - T9 encaissement.test : annuler un déjà annulé throw InvariantViolated('Cet encaissement est déjà annulé').
    - T10 creer-encaissement.test : echeance 700€, montant 300€ → statut 'partiellement_payee'.
    - T11 creer-encaissement.test : montant 700€ → 'payee'.
    - T12 creer-encaissement.test : montant 800€ → 'payee' + surPaiement = Money.fromEuros(100).
    - T13 creer-encaissement.test : date < bail.dateDebut → warning 'La date de paiement est antérieure à la date de début du bail'.
    - T14 creer-encaissement.test : date > today + 90j (ClockFixe) → warning 'Cette date semble trop avancée'.
    - T15 creer-encaissement.test : echeance.annuleLe != null → throw InvariantViolated('Impossible d\'encaisser sur une échéance annulée').
    - T16 creer-encaissement.test : bail.actifDepuis === null → throw InvariantViolated('Le bail n\'est pas activé').
    - T17 annuler-encaissement.test : 2 encaissements (700€+100€, statut payee), annuler 1er → recalcul somme=100€ → 'partiellement_payee', rebasculee=true.
    - T18 annuler-encaissement.test : annuler le seul encaissement → 'en_attente'.
    - T19 recalculer.test : somme=0 → 'en_attente' ; <total → 'partiellement_payee' ; ==total → 'payee' ; >total → 'payee' + surPaiement.
    - T20 recalculer.test : compensateur (encaissement1 800€, encaissement2 compensateur -200€) → somme 600€ → 'partiellement_payee'.
    - T21 integration : roundtrip Encaissement Money négatif (compensateur).
    - T22 integration : sommePaieeParEcheance retourne Money.zero quand aucun ; exclut annule_le NOT NULL.
    - T23 BDD @enc-03 "Paiement partiel ne quittance pas" : POST /encaissements montant 300€, GET /baux/:id/echeances montre badge partiellement_payee + AUCUN bouton 'Générer quittance'.
    - T24 BDD @enc-03 "Paiement exact => statut payee" : montant 700€, badge payee + bouton 'Générer quittance' présent.
    - T25 BDD @enc-03 "Sur-paiement affiche warning" : montant 800€, redirect avec banniereWarning visible 'Trop-perçu de 100,00 €' (rendu via warning-live.ejs aria-live polite).
    - T26 BDD @enc-03 "Annulation soft-delete recalcule" : 2 encaissements (statut payee), POST /encaissements/:id/annuler avec raison 'Erreur saisie', table encaissement: 2 lignes (1 avec annule_le NOT NULL), echeance.statut='en_attente'.
    - T27 BDD @enc-03 "Compensateur restaure suivi" : encaissement 800€ (warning surpaiement), créer un compensateur -100€ via form (case 'Encaissement compensateur'), echeance.statut='payee' sans warning, somme actifs=700€.
  </behavior>
  <action>
    TDD outside-in strict — créer EXCLUSIVEMENT tests. ROUGES attendus.

    Créer/modifier :
    1. ÉTENDRE `tests/unit/_shared/money.test.ts` avec Tests 1-5 (describe('Money.compensateur')).
    2. ÉTENDRE `tests/_builders/encaissements.ts` avec `unEncaissementValide(overrides)`. Defaults : montant Money.fromEuros(700), mode 'virement', date PlainDate.from('2026-05-05'), annuleLe null, raisonAnnulation null. echeanceId requis (toujours fourni).
    3. `tests/unit/encaissements/encaissement.test.ts` (NOUVEAU) : Tests 6-9.
    4. `tests/unit/encaissements/creer-encaissement.test.ts` (NOUVEAU) : Tests 10-16. ClockFixe.du('2026-05-15') baseline.
    5. `tests/unit/encaissements/annuler-encaissement.test.ts` (NOUVEAU) : Tests 17-18.
    6. `tests/unit/encaissements/recalculer-statut-echeance.test.ts` (NOUVEAU) : Tests 19-20.
    7. `tests/integration/repositories/encaissement-repository-sqlite.test.ts` (NOUVEAU) : Tests 21-22. appliquerMigrations 0001+0002+0003+0004 (chaînés).
    8. ÉTENDRE `tests/bdd/features/quittancement.feature` (créé 02-02) avec 5 scenarios tag `@enc-03`.
    9. ÉTENDRE `tests/bdd/step_definitions/quittancement.steps.ts` avec nouveaux steps (POST encaissements + SELECT DB count + annulation).

    Tests ÉCHOUENT. Commit : `test(02-03): tests rouges Money.compensateur + Encaissement + ENC-03 (Wave 0)`.
  </action>
  <verify>
    <automated>cd /Users/valentinshodo/Projects/toolbox/gestion-locative &amp;&amp; pnpm test 2>&amp;1 | grep -E "FAIL|fail" | head -20 ; ls tests/unit/encaissements/encaissement.test.ts tests/unit/encaissements/creer-encaissement.test.ts tests/unit/encaissements/annuler-encaissement.test.ts tests/unit/encaissements/recalculer-statut-echeance.test.ts tests/integration/repositories/encaissement-repository-sqlite.test.ts</automated>
  </verify>
  <done>
    - Tests Wave 0 ROUGES (5 fichiers test unit + 1 integration + extension money.test + extension BDD feature).
    - Commit créé.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Money.compensateur + Encaissement domain + repository + 3 use cases (passer tests unit/integration au vert)</name>
  <read_first>
    - src/domain/_shared/money.ts
    - src/domain/encaissements/echeance-loyer.ts (analog factory)
    - src/domain/encaissements/echeance-loyer-repository.ts (analog port)
    - src/infrastructure/repositories/echeance-loyer-repository-sqlite.ts (analog SUM Kysely + versDomaine)
    - src/application/encaissements/activer-bail.ts (analog use case multi-repos)
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-RESEARCH.md (§Topic 3 soft-delete code complet + sommePaieeParEcheance Kysely + Money.negation)
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-PATTERNS.md (§encaissement.ts + §creer-encaissement + §annuler-encaissement + §encaissement-repository-sqlite)
    - Tests rouges Task 1
  </read_first>
  <action>
    Créer/modifier dans cet ordre :

    1. `migrations/0004_phase2_encaissement.sql` :
       - CREATE TABLE encaissement (id TEXT PK, echeance_id TEXT NOT NULL REFERENCES echeance_loyer(id), montant_centimes INTEGER NOT NULL, date TEXT NOT NULL, mode TEXT NOT NULL CHECK (mode IN ('virement','cheque','especes','prelevement','autre')), annule_le DATETIME NULL, raison_annulation TEXT NULL, cree_le DATETIME DEFAULT CURRENT_TIMESTAMP).
       - NOTE : pas de CHECK montant_centimes >= 0 (compensateurs autorisés D-60).
       - CREATE INDEX idx_encaissement_echeance_actif ON encaissement(echeance_id, annule_le) WHERE annule_le IS NULL (accélère sommePaieeParEcheance).

    2. `src/infrastructure/db/kysely-types.ts` : ajouter `encaissement: EncaissementTable`.

    3. ÉTENDRE `src/domain/_shared/money.ts` :
       - Conserver `fromCentimes` (refuse négatifs Phase 1).
       - AJOUTER `static compensateur(positif: Money): Money` qui utilise le constructeur privé `new Money(-positif.centimes)` (bypass de fromCentimes).
       - AJOUTER `negation(): Money` instance method : `return Money.compensateur(Money.fromCentimes(-this.centimes >= 0n ? -this.centimes : this.centimes))` — alias d'ergonomie.
       - AJOUTER `estNegatif(): boolean` retourne `this.centimes < 0n`.
       - VÉRIFIER que `additionner(other)` utilise le constructeur privé sans throw — Phase 1 le fait déjà (`return new Money(this.centimes + other.centimes)`). Si jamais une revalidation existe, la retirer pour accepter les résultats négatifs.
       - LAISSER `soustraire` intact (throw si négatif — utilisé pour dépôt de garantie etc.).
       - VÉRIFIER que `enEuros()` rend correctement les négatifs (Intl.NumberFormat fr-FR style currency gère natif → "-100,00 €").

    4. `src/domain/encaissements/erreurs.ts` (ÉTENDRE) :
       - `EncaissementIntrouvable extends Error`.
       - `EncaissementDejaAnnule extends Error`.
       - `EcheanceAnnulee extends Error` (impossible d'encaisser).
       - `BailNonActif extends Error` (impossible d'encaisser).

    5. `src/domain/encaissements/encaissement.ts` (NOUVEAU) :
       - `type ModeEncaissement = 'virement' | 'cheque' | 'especes' | 'prelevement' | 'autre'`.
       - `interface EncaissementProps { id?: EncaissementId; echeanceId: EcheanceLoyerId; montant: Money; date: Temporal.PlainDate; mode: ModeEncaissement; annuleLe?: Temporal.PlainDate | null; raisonAnnulation?: string | null }`.
       - Classe avec readonly props + private constructor + static `creer` : valide mode (`modesValides.includes(props.mode)` sinon InvariantViolated). id défaut nouveauEncaissementId. annuleLe et raisonAnnulation défauts null.
       - Méthode `annuler(raison: string, annuleLe: Temporal.PlainDate): Encaissement` : si this.annuleLe !== null throw EncaissementDejaAnnule. Retourne `Encaissement.creer({...this.toProps(), annuleLe, raisonAnnulation: raison})`.
       - Helper `estCompensateur(): boolean` → this.montant.estNegatif().

    6. `src/domain/encaissements/encaissement-repository.ts` (NOUVEAU) :
       - Interface `EncaissementRepository { enregistrer(e): Promise<void>; trouverParId(id): Promise<Encaissement | null>; listerParEcheance(echeanceId, opts?: {inclureAnnules?: boolean}): Promise<Encaissement[]>; listerTous(opts?: {inclureAnnules?: boolean}): Promise<Encaissement[]>; sommePaieeParEcheance(echeanceId): Promise<Money> }`.

    7. `src/infrastructure/repositories/encaissement-repository-sqlite.ts` (NOUVEAU) :
       - Constructor (db).
       - `enregistrer(e)` : insertInto('encaissement').values({ id, echeance_id, montant_centimes: Number(e.montant.toCentimes()), date: e.date.toString(), mode, annule_le: e.annuleLe?.toString() ?? null, raison_annulation: e.raisonAnnulation ?? null }).onConflict(oc=>oc.column('id').doUpdateSet({ annule_le, raison_annulation })).execute().
       - `sommePaieeParEcheance(id)` : `selectFrom('encaissement').select(eb => eb.fn.sum('montant_centimes').as('total')).where('echeance_id','=',id).where('annule_le','is',null).executeTakeFirst()`. Retourner : si total >= 0 → Money.fromCentimes(BigInt(total||0)), sinon Money.compensateur(Money.fromCentimes(BigInt(-total))).
       - `versDomaine(row)` : montant si row.montant_centimes >= 0 → Money.fromCentimes(BigInt(row.montant_centimes)), sinon Money.compensateur(Money.fromCentimes(BigInt(-row.montant_centimes))). annuleLe : null ou Temporal.PlainDate.from.

    8. `src/application/encaissements/recalculer-statut-echeance.ts` (NOUVEAU) :
       - `recalculerStatutEcheance(echeanceId, echeanceLoyerRepo, encaissementRepo): Promise<{ statut, sommePaiee, surPaiement: Money | null }>`.
       - sommePaiee = encaissementRepo.sommePaieeParEcheance(echeanceId).
       - echeance = echeanceLoyerRepo.trouverParId(echeanceId).
       - Si sommePaiee.egale(Money.zero()) → statut 'en_attente', surPaiement null.
       - Sinon si sommePaiee.lt(echeance.total) → 'partiellement_payee', surPaiement null.
       - Sinon si sommePaiee.egale(echeance.total) → 'payee', surPaiement null.
       - Sinon (somme > total) → 'payee', surPaiement = sommePaiee.soustraire(echeance.total).
       - echeanceLoyerRepo.mettreAJourStatut(echeanceId, statut). Retourner objet.

    9. `src/application/encaissements/creer-encaissement.ts` (NOUVEAU) :
       - Signature : `creerEncaissement(commande: { echeanceId, montantCentimesPositifs: bigint, signe: 'positif' | 'compensateur', date: Temporal.PlainDate, mode: ModeEncaissement }, echeanceLoyerRepo, encaissementRepo, bailRepo, clock): Promise<{ encaissementId, warnings: string[], statut: StatutEcheanceLoyer, surPaiement: Money | null }>`.
       - Lookup echeance (EcheanceLoyerIntrouvable). Si echeance.annuleLe !== null → throw EcheanceAnnulee('Impossible d\'encaisser sur une échéance annulée').
       - Lookup bail. Si bail.actifDepuis === null → throw BailNonActif('Le bail n\'est pas activé').
       - positif = Money.fromCentimes(commande.montantCentimesPositifs).
       - montant = commande.signe === 'compensateur' ? Money.compensateur(positif) : positif.
       - warnings : Temporal.PlainDate.compare(commande.date, bail.dateDebut) < 0 → push warning ; Temporal.PlainDate.compare(commande.date, clock.aujourdhui().add({days:90})) > 0 → push warning.
       - encaissement = Encaissement.creer({...}).
       - encaissementRepo.enregistrer(encaissement).
       - { statut, surPaiement } = await recalculerStatutEcheance(commande.echeanceId, echeanceLoyerRepo, encaissementRepo).
       - Retourner.

    10. `src/application/encaissements/annuler-encaissement.ts` (NOUVEAU) :
        - Signature : `annulerEncaissement(commande: { id, raison }, encaissementRepo, echeanceLoyerRepo, clock): Promise<{ ancienStatut, nouveauStatut, rebasculee: boolean }>`.
        - Lookup encaissement (sinon EncaissementIntrouvable). echeance = lookup. ancienStatut = echeance.statut.
        - encaissementAnnule = encaissement.annuler(commande.raison, clock.aujourdhui()).
        - encaissementRepo.enregistrer(encaissementAnnule) (upsert).
        - { statut: nouveauStatut } = await recalculerStatutEcheance(encaissement.echeanceId, echeanceLoyerRepo, encaissementRepo).
        - Retourner { ancienStatut, nouveauStatut, rebasculee: ancienStatut !== nouveauStatut }.

    11. `src/application/encaissements/lister-encaissements.ts` (NOUVEAU) : wrapper `listerEncaissements({inclureAnnules}, encaissementRepo)`.

    Vérifs : Tests unit + integration Task 1 VERTS. `pnpm tsc --noEmit` 0 erreur. `pnpm lint:deps` 0 violation.

    Commit : `feat(02-03): encaissement domain + use cases + soft-delete + compensateur`.
  </action>
  <verify>
    <automated>cd /Users/valentinshodo/Projects/toolbox/gestion-locative &amp;&amp; pnpm tsc --noEmit &amp;&amp; pnpm lint:deps &amp;&amp; pnpm test:unit run tests/unit/_shared/money.test.ts tests/unit/encaissements/ &amp;&amp; pnpm test:integration run tests/integration/repositories/encaissement-repository-sqlite.test.ts</automated>
  </verify>
  <done>
    - Money.compensateur + negation + estNegatif implémentés.
    - Encaissement + annuler() soft-delete.
    - EncaissementRepository SQLite + sommePaieeParEcheance.
    - 3 use cases (creer, annuler, recalculer) + lister-encaissements wrapper.
    - Migration 0004 + Kysely types étendus.
    - Tests unit + integration Wave 0 VERTS.
    - Commit créé.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Routes + views (formulaire/liste/fiche) + warning-live partial + extension page échéances + sidebar + BDD verts</name>
  <read_first>
    - src/web/routes/baux.ts (analog plugin Fastify)
    - src/web/schemas/bail-schemas.ts (analog Zod schema)
    - src/web/views/pages/baux/formulaire.ejs (analog 1 colonne label-au-dessus)
    - src/web/views/pages/baux/detail.ejs (analog fiche)
    - src/web/views/pages/baux/liste.ejs (analog data-table)
    - src/web/views/partials/confirm-dialog.ejs (analog modale destructive)
    - src/web/views/partials/form-field.ejs
    - src/web/views/pages/echeances/liste.ejs (créé 02-02 — à étendre)
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-UI-SPEC.md (§Saisie encaissement + §Encaissements global + §Fiche échéance + §Zone warning aria-live + §Copywriting CTAs + §Error/Warning States + §Destructive Actions Annuler + §Validation formulaire)
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-PATTERNS.md (§routes/encaissements + §encaissement-schemas + §views/encaissements/formulaire)
    - Tests rouges BDD Task 1
  </read_first>
  <action>
    Créer/modifier :

    1. `src/web/schemas/encaissement-schemas.ts` (NOUVEAU) :
       - `encaissementFormSchema = z.object({ echeanceId: z.string().uuid('Sélectionnez une échéance valide'), montantEuros: z.coerce.number().refine(n => n > 0, 'Le montant doit être supérieur à 0 €'), signe: z.enum(['positif','compensateur']).default('positif'), date: z.string().regex(/^\d{4}-\d{2}-\d{2}$/, 'Format AAAA-MM-JJ'), mode: z.enum(['virement','cheque','especes','prelevement','autre'], { errorMap: () => ({ message: 'Sélectionnez un mode de paiement' }) }) })`.
       - Annuler-form : `annulationFormSchema = z.object({ raison: z.string().trim().min(3, 'Au moins 3 caractères') })`.

    2. `src/web/views/partials/warning-live.ejs` (NOUVEAU) :
       - Pattern UI-SPEC §Zone warning aria-live : `<% if (locals.warning) { %><aside role="status" aria-live="polite" class="warning-zone"><span aria-hidden="true">⚠</span> <%= locals.warning %></aside><% } %>`.

    3. `src/web/routes/encaissements.ts` (NOUVEAU) :
       - `export async function plugin(app, opts: { encaissementRepo, echeanceLoyerRepo, bailRepo, locataireRepo, bienRepo, clock }): Promise<void>`.
       - `GET /encaissements` : lire bannières session (lire+vider Phase 1 LESSON), `listerEncaissements({inclureAnnules: true}, encaissementRepo)`, reply.view('pages/encaissements/liste.ejs', { encaissements, navActive: 'encaissements', banniereSuccess, banniereWarning }).
       - `GET /encaissements/nouveau` (avec query optionnel `?echeance=:id`) : si query, lookup echeance + bail + locataire. Sinon `listerNonPayees` pour select. reply.view('pages/encaissements/formulaire.ejs', { echeance, echeancesOuvertes, dateParDefaut: clock.aujourdhui().toString(), navActive: 'encaissements', erreurs: {}, valeurs: {} }).
       - `POST /encaissements` : safeParse. Si !success → re-render. Si success : `const montantCentimes = BigInt(Math.round(montantEuros * 100))` puis `await creerEncaissement({echeanceId, montantCentimesPositifs: montantCentimes, signe, date: Temporal.PlainDate.from(date), mode}, echeanceLoyerRepo, encaissementRepo, bailRepo, clock)`. Construire `banniereWarning` si surPaiement (`Trop-perçu de ${surPaiement.enEuros()}. Pense à ajuster la prochaine échéance.`) + concaténer warnings D-61 si présents. Set `req.session.banniereSuccess = 'Encaissement enregistré.'` + banniereWarning. reply.redirect('/encaissements/' + encaissementId). Catch EcheanceAnnulee/BailNonActif/EcheanceLoyerIntrouvable → re-render avec erreurs._global = err.message.
       - `GET /encaissements/:id` : lookup encaissement (404 sinon). lookup echeance. reply.view('pages/encaissements/fiche.ejs', { encaissement, echeance, navActive: 'encaissements', banniereSuccess, banniereWarning }).
       - `POST /encaissements/:id/annuler` : safeParse(annulationFormSchema). Si !success re-render fiche avec erreurs. Si success `await annulerEncaissement({id, raison}, encaissementRepo, echeanceLoyerRepo, clock)`. Set session banniereSuccess = `Encaissement annulé. ${result.rebasculee ? 'Le statut de l\'échéance a été recalculé (' + result.nouveauStatut + ').' : ''}`. reply.redirect('/encaissements/' + id).

    4. `src/web/views/pages/encaissements/formulaire.ejs` (NOUVEAU) :
       - layout-debut titre "Saisir un encaissement", breadcrumbs, navActive 'encaissements'.
       - Si `echeance` (contexte pré-rempli) : afficher info locataire + période + total dû en encart.
       - Formulaire 1 colonne label-au-dessus (D-49) :
         - `echeanceId` : si pré-rempli, hidden ; sinon select avec echeancesOuvertes label "Locataire — Période — Total dû".
         - `montantEuros` : input type="number" step="0.01" min="0.01" required + helper "Saisir un montant positif (cocher la case ci-dessous pour un compensateur).".
         - `signe` : `<label><input type="checkbox" name="signe" value="compensateur" /> Encaissement compensateur (correction d'un montant antérieur)</label>` — si pas coché valeur 'positif' par défaut.
         - `date` : input type="date" required, défaut dateParDefaut.
         - `mode` : select required 5 options.
       - Erreurs inline via form-field.ejs.
       - Bouton primaire "Enregistrer l'encaissement" (UI-SPEC §Copywriting).
       - layout-fin.

    5. `src/web/views/pages/encaissements/liste.ejs` (NOUVEAU) :
       - layout-debut + bannières + data-table colonnes [Date, Locataire+Période, Montant, Mode, Statut].
       - Statut visuel : ligne actif (texte normal) ; ligne annulée → CSS `.encaissement-annule` (opacity 0.55 + text-decoration line-through, UI-SPEC §Color "annulee"). Compensateur : ajouter badge "Compensateur" (badge ambre) si encaissement.estCompensateur().
       - Bouton "Saisir un encaissement" en CTA primaire si liste non vide.
       - Empty state si liste vide (UI-SPEC §Empty States).
       - layout-fin.

    6. `src/web/views/pages/encaissements/fiche.ejs` (NOUVEAU) :
       - layout-debut + bannières.
       - `<h1>Encaissement du <%= formatDate(encaissement.date) %></h1>` + montant en grand (28px display) si actif, gris/barré si annulé.
       - Détails : montant (avec signe si compensateur), mode, échéance liée (lien vers fiche échéance), date saisie.
       - Si annulé : warning-live.ejs partial avec message "Encaissement annulé le ${annuleLe}. Raison : ${raisonAnnulation}.".
       - Si actif : bouton "Annuler cet encaissement" → ouvre `<dialog>` (confirm-dialog.ejs analog) avec form POST /encaissements/:id/annuler + textarea raison required. Focus par défaut sur "Annuler" (safer default).
       - layout-fin.

    7. ÉTENDRE `src/web/views/pages/echeances/liste.ejs` (créé 02-02) :
       - Lire+vider bannières session côté route GET /baux/:id/echeances (Phase 1 LESSON — pas dans preHandler). Passer dans viewModel.
       - Top de page : `<%- include('../../partials/warning-live', { warning: locals.banniereWarning }) %>` + bannière succès.
       - Actions par ligne (statut !== 'annulee' && statut !== 'payee') : bouton/lien "Saisir un encaissement" → `/encaissements/nouveau?echeance=${e.id}`.
       - Bouton "Générer quittance" : afficher seulement si `e.statut === 'payee'` (placeholder lien `/quittances/nouveau?echeance=${e.id}` pour le plan 02-04). Le bouton existe mais cliquer mènera vers 404 jusqu'au plan 02-04 — c'est OK pour le BDD test T24.

    8. ÉTENDRE `src/web/views/partials/sidebar-nav.ejs` :
       - Ajouter sous-lien "Encaissements" pointant vers `/encaissements`.
       - Envelopper "Échéances" + "Encaissements" dans `<details><summary>Encaissements</summary>...</details>` (UI-SPEC §Layout Shell — group dépliable HTML natif, pas de JS).
       - L'attribut `open` est ajouté si `navActive` commence par 'encaissements' OU 'echeances'.

    9. ÉTENDRE `src/main.ts` :
       - Imports : `EncaissementRepositorySqlite`, `plugin as encaissementsPlugin`.
       - Instancier `const encaissementRepo = new EncaissementRepositorySqlite(db);`.
       - Registrer `await app.register(encaissementsPlugin, { encaissementRepo, echeanceLoyerRepo, bailRepo, locataireRepo, bienRepo, clock });`.

    Sécurité (cf. <threat_model>) :
    - Zod strict : echeanceId UUID, montantEuros > 0, mode enum 5 valeurs (V5).
    - raisonAnnulation : trim + min(3) — pas d'injection (Kysely paramétré).
    - Soft-delete + compensateur (D-60 audit-friendly) — pas d'UPDATE direct.
    - Cross-aggregate check au use case : echeance non annulee + bail actif.

    Vérifs : `pnpm test:bdd -- --tags @enc-03` 5 scenarios VERTS. `pnpm test` tout VERT. `pnpm tsc --noEmit` 0 erreur. `pnpm lint` 0 warning.

    Commit : `feat(02-03): saisie + annulation Encaissement + warnings + page liste/fiche (ENC-03)`.
  </action>
  <verify>
    <automated>cd /Users/valentinshodo/Projects/toolbox/gestion-locative &amp;&amp; pnpm tsc --noEmit &amp;&amp; pnpm lint &amp;&amp; pnpm lint:deps &amp;&amp; pnpm test &amp;&amp; pnpm test:bdd -- --tags @enc-03</automated>
  </verify>
  <done>
    - Routes /encaissements + /encaissements/nouveau + /encaissements/:id + /encaissements/:id/annuler.
    - Views formulaire (avec signe positif/compensateur), liste (visuel annulés), fiche (modale annulation).
    - Partial warning-live.ejs.
    - Extension page échéances avec actions + bannières + bouton 'Générer quittance' visible si payee.
    - Sidebar dépliable Encaissements.
    - 5 scenarios BDD `@enc-03` VERTS.
    - Tous tests Phase 1 + 02-01 + 02-02 EXISTANTS toujours verts.
    - Commit créé.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigateur user → Fastify POST /encaissements | Saisie montant/date/mode/echeanceId — input non-trusted |
| navigateur user → Fastify POST /encaissements/:id/annuler | Saisie raison annulation — input non-trusted |
| Fastify → SQLite | Persistence Kysely paramétré (V5 Input Validation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03-01 | Tampering (V5) | POST /encaissements — montantEuros NaN / négatif / non-number | LOW | mitigate | Zod `z.coerce.number().refine(n => n > 0)` + cast BigInt `Math.round * 100` rejette NaN |
| T-02-03-02 | Tampering | POST /encaissements — mode arbitraire ('bitcoin', 'eth') | LOW | mitigate | Zod `z.enum(['virement','cheque','especes','prelevement','autre'])` + domain `Encaissement.creer` lève InvariantViolated (double barrière) + CHECK constraint SQLite |
| T-02-03-03 | Tampering — fraude rétroactive | POST /encaissements avec date < bail.dateDebut | LOW | accept (D-61) | Warning visible non-bloquant pour usage mono-user LMNP réel (rattrapage admin fréquent). Pas de reject — UX choice. |
| T-02-03-04 | Tampering | POST /encaissements/:id/annuler — UPDATE destructif via SQL injection ? | LOW | mitigate | Kysely paramétré + soft-delete (annule_le SET + raison_annulation SET) — JAMAIS de DELETE. PDF originaux préservés. |
| T-02-03-05 | Repudiation — locataire conteste un encaissement | utilisateur supprime un Encaissement | MED | mitigate | Soft-delete obligatoire (D-60) — l'historique annulé reste consultable. Pour corriger un montant : compensateur Money négatif (audit trail explicite). |
| T-02-03-06 | Information disclosure — XSS via raisonAnnulation | utilisateur saisit `<script>` dans raison | LOW | mitigate | EJS échappe par défaut `<%= %>`. Aucun usage de `<%- %>` sur raisonAnnulation dans fiche.ejs |
| T-02-03-07 | Integrity — recalcul statut incorrect après annulation | edge case : annuler un compensateur déjà annulé | MED | mitigate | `Encaissement.annuler` throw `EncaissementDejaAnnule` si this.annuleLe !== null. Test unitaire couvert (T9). |
</threat_model>

<verification>
- `pnpm tsc --noEmit` exit 0
- `pnpm lint` 0 warning
- `pnpm lint:deps` 0 violation (domaine pur — pas d'import infra dans domain/encaissements)
- `pnpm test` tout VERT (incluant Money.compensateur, fast-check non-régression, integration roundtrip négatif)
- `pnpm test:bdd -- --tags @enc-03` 5 scenarios PASSED
- Migration 0004 idempotente
- Pas de CHECK montant_centimes >= 0 (compensateurs autorisés)
- Soft-delete vérifié : `SELECT COUNT(*) FROM encaissement WHERE annule_le IS NOT NULL` > 0 après scenario T26
</verification>

<success_criteria>
- ENC-03 satisfait : saisie Encaissement (date/montant/mode/echeance_id), paiement partiel n'émet pas de quittance.
- D-57 satisfait : cardinalité N:1, plusieurs Encaissement par EcheanceLoyer, somme actifs détermine statut.
- D-58 satisfait : mode ∈ 5 valeurs strictes.
- D-59 satisfait : sur-paiement + warning visible non-bloquant.
- D-60 satisfait : soft-delete + compensateur (jamais UPDATE/DELETE destructif sur Encaissement).
- D-61 satisfait : date permissive + warnings.
- D-62 satisfait : pas d'enum statut sur Encaissement V1 (annulé = annule_le NOT NULL).
- 100 % couverture Money.compensateur + recalculerStatutEcheance (règles financières → critère projet).
- Ubiquitous language français maintenu.
- Domain pur (pas d'import infra dans `src/domain/encaissements/`).
</success_criteria>

<output>
After completion, create `.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-03-SUMMARY.md` listant :
- 3 commits (tests rouges / domain+app / web+BDD)
- Patterns établis : soft-delete + compensateur, recalcul statut idempotent, warning-live.ejs partial
- Surprises éventuelles
- Dépendances pour plans suivants : EcheanceLoyer.statut === 'payee' déclenche la condition d'émission Quittance (plan 02-04), `listerNonPayees()` consommé par plan 02-05 (Impayés)
</output>
