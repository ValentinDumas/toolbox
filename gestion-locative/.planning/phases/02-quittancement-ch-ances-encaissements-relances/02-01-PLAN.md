---
phase: 02
plan: 01
plan_id: "02-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/0002_phase2_bailleur_bail_ext.sql
  - src/infrastructure/db/kysely-types.ts
  - src/infrastructure/db/database.ts
  - src/domain/_shared/identifiants.ts
  - src/domain/_shared/clock.ts
  - src/domain/identite/bailleur.ts
  - src/domain/identite/bailleur-repository.ts
  - src/domain/identite/erreurs.ts
  - src/domain/locatif/bail.ts
  - src/domain/locatif/activite-bail-detector.ts
  - src/infrastructure/repositories/bailleur-repository-sqlite.ts
  - src/infrastructure/repositories/bail-repository-sqlite.ts
  - src/infrastructure/repositories/activite-bail-detector-sqlite.ts
  - src/application/identite/creer-ou-maj-bailleur.ts
  - src/application/locatif/supprimer-bail.ts
  - src/application/locatif/desactiver-bail.ts
  - src/web/schemas/bailleur-schemas.ts
  - src/web/routes/bailleur.ts
  - src/web/routes/baux.ts
  - src/web/views/pages/bailleur/profil.ejs
  - src/web/views/pages/baux/detail.ejs
  - src/web/views/partials/sidebar-nav.ejs
  - src/main.ts
  - tests/_builders/identite.ts
  - tests/_world/monde-phase2.ts
  - tests/unit/_shared/clock.test.ts
  - tests/unit/identite/bailleur.test.ts
  - tests/unit/locatif/bail.test.ts
  - tests/unit/locatif/desactiver-bail.test.ts
  - tests/unit/locatif/supprimer-bail.test.ts
  - tests/integration/repositories/bailleur-repository-sqlite.test.ts
  - tests/bdd/features/bailleur.feature
  - tests/bdd/features/quittancement.feature
  - tests/bdd/step_definitions/bailleur.steps.ts
  - tests/bdd/step_definitions/quittancement.steps.ts
autonomous: true
requirements: ["D-74"]
user_setup: []
mvp_split_rationale: |
  Walking enabler couplé à une vraie valeur user : la page /bailleur (nécessaire avant toute quittance loi 89 art. 21).
  Pose simultanément (D-67, D-51, D-53, port Clock RESEARCH §6) car les 4 capacités sont sur la même migration
  SQL et la même injection dans creerApp — les séparer ferait 3 plans plus coûteux en contexte total.

must_haves:
  truths:
    - "L'utilisateur peut accéder à /bailleur depuis la sidebar et saisir nom complet + adresse rue/code postal/ville."
    - "Le profil bailleur est persisté en SQLite ; 2e visite de /bailleur pré-remplit les champs."
    - "Un 2e Bailleur (race ou bug) est rejeté par UNIQUE(singleton_marker) ET par le use case (double barrière)."
    - "Bail.activer(actifDepuis: PlainDate, jourEcheance: 1..28) retourne un Bail activé (copy-on-write); throw InvariantViolated si jourEcheance hors [1,28]."
    - "Le port Clock existe (src/domain/_shared/clock.ts) avec ClockSysteme (Temporal.Now.plainDateISO) et ClockFixe (factory ClockFixe.du(iso))."
    - "Port ActiviteBailDetector (src/domain/locatif/activite-bail-detector.ts) injecté dans supprimerBail (D-74) ; adapter SQLite v0 retourne toujours false en 02-01 (étendu en 02-02 avec count echeance_loyer, en 02-03 avec encaissement, en 02-04 avec quittance)."
    - "POST /baux/:id/supprimer refuse la suppression (redirige avec banniereWarning) si ActiviteBailDetector.aDeLActivite(bailId) === true (D-74)."
    - "POST /baux/:id/desactiver bascule actif_depuis = null via desactiverBail use case (D-74 alternative non destructive) ; bouton 'Désactiver' visible sur la fiche bail si bail.actifDepuis !== null."
    - "src/web/views/pages/baux/detail.ejs affiche le bouton 'Supprimer' uniquement si aDeLActivite=false ET actifDepuis=null ; sinon affiche le bouton 'Désactiver' + message conditionnel 'Ce bail a déjà de l'activité, il ne peut plus être supprimé. Vous pouvez en revanche le désactiver.'"
    - "creerApp(db, { clock }) injecte Clock; ClockSysteme par défaut si omis."
    - "Migration 0002 ajoute table bailleur + ALTER bail (actif_depuis TEXT NULL, jour_echeance INTEGER 1..28 DEFAULT 1)."
    - "tests:bdd --tags @bailleur passe (premier renseignement + mise à jour + protection singleton)."
  artifacts:
    - path: "migrations/0002_phase2_bailleur_bail_ext.sql"
      provides: "Migration Phase 2 partielle — bailleur + ALTER bail"
      contains: "CREATE TABLE IF NOT EXISTS bailleur, ALTER TABLE bail ADD COLUMN actif_depuis, ALTER TABLE bail ADD COLUMN jour_echeance"
    - path: "src/domain/_shared/clock.ts"
      provides: "Port Clock + ClockSysteme + ClockFixe"
      exports: ["Clock", "ClockSysteme", "ClockFixe"]
    - path: "src/domain/identite/bailleur.ts"
      provides: "Agrégat Bailleur singleton"
      exports: ["Bailleur"]
    - path: "src/web/routes/bailleur.ts"
      provides: "Routes GET/POST /bailleur"
      exports: ["plugin"]
    - path: "tests/_world/monde-phase2.ts"
      provides: "Cucumber World Phase 2 — ré-utilisable plans 02-02..02-06"
      exports: ["MondePhase2"]
    - path: "src/domain/locatif/activite-bail-detector.ts"
      provides: "Port domaine ActiviteBailDetector (D-74) — détecte si un Bail a de l'activité (échéance, encaissement, quittance)"
      exports: ["ActiviteBailDetector"]
    - path: "src/infrastructure/repositories/activite-bail-detector-sqlite.ts"
      provides: "Adapter SQLite v0 (D-74) — retourne false en 02-01 ; étendu progressivement en 02-02 (echeance_loyer), 02-03 (encaissement), 02-04 (quittance)"
      exports: ["ActiviteBailDetectorSqlite"]
    - path: "src/application/locatif/desactiver-bail.ts"
      provides: "Use case desactiverBail — bascule actif_depuis = null sans toucher aux données (D-74 alternative)"
      exports: ["desactiverBail"]
  key_links:
    - from: "src/main.ts"
      to: "src/domain/_shared/clock.ts"
      via: "creerApp(db, { clock = new ClockSysteme() })"
      pattern: "ClockSysteme"
    - from: "src/web/routes/bailleur.ts"
      to: "src/application/identite/creer-ou-maj-bailleur.ts"
      via: "POST /bailleur appelle creerOuMajBailleur(commande, bailleurRepo)"
      pattern: "creerOuMajBailleur"
    - from: "src/infrastructure/repositories/bailleur-repository-sqlite.ts"
      to: "migrations/0002_phase2_bailleur_bail_ext.sql"
      via: "INSERT INTO bailleur(id, singleton_marker='unique_bailleur', ...)"
      pattern: "singleton_marker"
    - from: "src/domain/locatif/bail.ts"
      to: "Bail.activer"
      via: "copy-on-write Bail.creer({...this.toProps(), actifDepuis, jourEcheance})"
      pattern: "activer"
    - from: "src/web/views/partials/sidebar-nav.ejs"
      to: "/bailleur"
      via: "lien Profil bailleur + aria-current si navActive==='bailleur'"
      pattern: "Profil bailleur"
    - from: "src/application/locatif/supprimer-bail.ts"
      to: "src/domain/locatif/activite-bail-detector.ts"
      via: "avant de supprimer : if (await activiteBailDetector.aDeLActivite(id)) throw InvariantViolated('Bail avec activité ne peut être supprimé')"
      pattern: "aDeLActivite"
    - from: "src/web/views/pages/baux/detail.ejs"
      to: "src/web/routes/baux.ts"
      via: "affichage conditionnel bouton Supprimer (uniquement si !aDeLActivite && actifDepuis===null) ou Désactiver (si actifDepuis!==null)"
      pattern: "data-open-dialog=\\"dialog-desactiver-bail\\""
---

<objective>
Walking enabler Phase 2 livrant une vraie valeur user (page /bailleur — saisie profil bailleur, obligatoire avant émission quittance loi 89 art. 21) et posant 5 fondations techniques couplées :
- Port `Clock` + `ClockSysteme` + `ClockFixe` (déterminisme BDD seuils J+X — plans 02-05/02-06).
- Extension `Bail.actif_depuis` + `Bail.jour_echeance` + `Bail.activer()` (préparation génération EcheanceLoyer — plan 02-02).
- Migration `0002_phase2_bailleur_bail_ext.sql` (ALTER bail + table bailleur singleton).
- World Cucumber Phase 2 réutilisable par tous les plans suivants.
- Port `ActiviteBailDetector` + adapter v0 + extension `supprimerBail` + nouveau `desactiverBail` + extension fiche bail (D-74 — suppression refusée si activité, alternative `Désactiver`).

Purpose: 5 capacités fortement couplées (même migration SQL, même injection dans creerApp). Walking enabler + valeur user effective (D-67 → page /bailleur). D-74 livré dès 02-01 parce que la route `/baux/:id/supprimer` existe déjà en Phase 1 et doit être neutralisée AVANT que les plans 02-02 à 02-04 créent les premières échéances/encaissements/quittances (sinon brèche temporaire où la suppression détruirait des artefacts).
Output: Page /bailleur fonctionnelle + 5 fondations techniques.
</objective>

<execution_context>
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/workflows/execute-plan.md
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-CONTEXT.md
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-RESEARCH.md
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-PATTERNS.md
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-UI-SPEC.md
@.planning/phases/01-activation-bien-locataire-bail/01-LEARNINGS.md
@CLAUDE.md
@DDD.md
@BDD_PRACTICES.md
@UI_DESIGN.md
@UX_DESIGN.md
@ACCESSIBILITY.md
@src/domain/_shared/identifiants.ts
@src/domain/_shared/adresse.ts
@src/domain/_shared/erreurs.ts
@src/domain/locatif/bail.ts
@src/infrastructure/repositories/bail-repository-sqlite.ts
@src/infrastructure/db/kysely-types.ts
@src/infrastructure/db/database.ts
@src/main.ts
@src/web/routes/baux.ts
@src/web/schemas/bail-schemas.ts
@src/web/views/partials/sidebar-nav.ejs
@src/web/views/partials/form-field.ejs
@src/web/views/partials/layout-debut.ejs
@src/web/views/partials/banniere-success.ejs
@src/web/views/pages/baux/formulaire.ejs
@tests/_builders/locatif.ts
@tests/bdd/features/activation.feature
@tests/bdd/step_definitions/activation.steps.ts
@tests/integration/repositories/bail-repository-sqlite.test.ts
@migrations/0001_init.sql
</context>

<interfaces>
Contrats que l'executor doit respecter (extrait codebase Phase 1) :

- Pattern brand types (identifiants.ts) : `type XId = string & { readonly __brand: 'XId' }` + factory `nouveauXId()`. À ajouter : `BailleurId`, `EcheanceLoyerId`, `EncaissementId`, `QuittanceId`, `RelanceId` (anti-thrash — les 4 derniers consommés plans suivants).
- Pattern VO Adresse : `Adresse.creer({ rue, codePostal, ville })` retourne Adresse — à réutiliser tel quel pour `Bailleur.adresse`.
- Pattern Bail.creer (lignes 90-138) : factory + InvariantViolated. À étendre : champs optionnels `actifDepuis?: Temporal.PlainDate | null`, `jourEcheance?: number` (défaut 1). Méthode `activer(actifDepuis, jourEcheance)` valide jourEcheance ∈ [1,28] sinon `InvariantViolated('Le jour d\'échéance doit être entre 1 et 28 (D-53)')`.
- Pattern Kysely `DB` interface : ajouter `bailleur: BailleurTable` (id, singleton_marker, nom_complet, rue, code_postal, ville, cree_le, modifie_le). Étendre `bail` avec `actif_depuis: string | null`, `jour_echeance: number`.
- Pattern creerApp : signature actuelle `creerApp(db: Kysely<DB>): Promise<FastifyInstance>`. Cible : `creerApp(db, opts?: { clock?: Clock }): Promise<FastifyInstance>` avec défaut `new ClockSysteme()`.
- Clock interface (RESEARCH §6) : `interface Clock { aujourdhui(): Temporal.PlainDate }`, `class ClockSysteme implements Clock`, `class ClockFixe implements Clock { static du(iso: string): ClockFixe }`.
- Routes Fastify plugin pattern : `export async function plugin(app, opts): Promise<void>` (cf. baux.ts).
- EJS layout split : `<%- include('../../partials/layout-debut', {...}) %>` puis contenu puis `<%- include('../../partials/layout-fin') %>` (Phase 1 LEARNING — pas de template literal).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests rouges Wave 0 — BDD feature bailleur + tests unit/integration + builders + World Phase 2</name>
  <read_first>
    - tests/bdd/features/activation.feature
    - tests/bdd/step_definitions/activation.steps.ts
    - tests/_builders/locatif.ts
    - tests/integration/repositories/bail-repository-sqlite.test.ts
    - tests/unit/locatif/bail.test.ts
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-PATTERNS.md (§monde-phase2.ts + §quittancement.feature)
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-RESEARCH.md (§Topic 6 Clock)
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-UI-SPEC.md (§Screen Inventory + §Copywriting "Profil bailleur")
    - src/main.ts (signature creerApp à étendre)
  </read_first>
  <behavior>
    - Test 1 (unit clock.test.ts) : `ClockSysteme.aujourdhui()` retourne un Temporal.PlainDate.
    - Test 2 (unit clock.test.ts) : `ClockFixe.du('2026-05-01').aujourdhui()` égale `Temporal.PlainDate.from('2026-05-01')`.
    - Test 3 (unit bailleur.test.ts) : `Bailleur.creer({ nomComplet, adresse })` valide.
    - Test 4 (unit bailleur.test.ts) : `Bailleur.creer({ nomComplet: '   ' })` throw InvariantViolated.
    - Test 5 (unit bailleur.test.ts) : `bailleur.modifier({ nomComplet })` retourne nouveau Bailleur (copy-on-write).
    - Test 6 (unit bail.test.ts extension) : `bail.activer(PlainDate, 5)` retourne Bail activé avec actifDepuis et jourEcheance=5.
    - Test 7 (unit bail.test.ts extension) : `bail.activer(date, 29)` throw InvariantViolated('Le jour d\'échéance doit être entre 1 et 28 (D-53)').
    - Test 8 (unit bail.test.ts extension) : `bail.activer(date, 0)` throw idem.
    - Test 9 (integration bailleur-repository-sqlite.test.ts) : `enregistrer(bailleur) → trouver()` retourne bailleur; trouver() sur DB vide retourne null.
    - Test 10 (integration) : 2e INSERT direct via Kysely (avec singleton_marker identique) rejette avec 'UNIQUE constraint failed'.
    - Test 11 (integration) : roundtrip Adresse via repository (rue, code_postal, ville préservés).
    - Test 12 (BDD bailleur.feature) : Scenario "Premier renseignement" — GET /bailleur formulaire vide ; POST valide redirect /bailleur avec bannière succès ; GET 2e visite pré-remplit.
    - Test 13 (BDD bailleur.feature) : Scenario "Mise à jour profil existant" — 1 seul row en DB après 2 POST.
    - Test 14 (BDD bailleur.feature) : Scenario "Singleton — protection DB" — vérifier au niveau DB qu'un 2e INSERT direct rejette UNIQUE.
  </behavior>
  <action>
    TDD outside-in strict (BDD_PRACTICES §5) : créer EXCLUSIVEMENT les fichiers de test (tests rouges). Aucun code de production.

    Créer :
    1. `tests/_world/monde-phase2.ts` — interface MondePhase2 extends World (champs: app, db, sqlite, clock=ClockFixe.du('2026-05-01'), dernierStatut, derniereUrl, dernierCorps, cookies). Helper `creerMondePhase2()`. Pattern PATTERNS.md §monde-phase2. Before hook applique 0001 PUIS 0002 séquentiellement. SESSION_SECRET = 'test-secret-for-cucumber-tests-32chars!!' (Phase 1 pattern).
    2. `tests/_builders/identite.ts` — `unBailleurValide(overrides?)` + `uneAdresseValide(overrides?)`. Defaults : nomComplet 'Jean Dupont', adresse {rue '12 rue de la Paix', codePostal '75002', ville 'Paris'}.
    3. `tests/unit/_shared/clock.test.ts` — Tests 1-2.
    4. `tests/unit/identite/bailleur.test.ts` — Tests 3-5.
    5. ÉTENDRE `tests/unit/locatif/bail.test.ts` — AJOUTER Tests 6-8 dans describe('Bail') existant sans modifier les tests passants.
    6. `tests/integration/repositories/bailleur-repository-sqlite.test.ts` — Tests 9-11. Pattern bail-repository-sqlite.test.ts lignes 1-50. MIGRATIONS_PATH applique 0001 PUIS 0002.
    7. `tests/bdd/features/bailleur.feature` — Tests 12-14. Keywords Gherkin anglais + textes français (Phase 1 LESSON). Tag global `@bailleur`. Steps avec `/` en regex côté .steps.
    8. `tests/bdd/step_definitions/bailleur.steps.ts` — Step definitions. Pattern activation.steps.ts (Before hook + app.inject + MondePhase2). Le test 14 fait un INSERT direct via this.db.insertInto('bailleur').values({...singleton_marker identique}).execute() puis try/catch sur le message 'UNIQUE constraint failed'.

    Vérifs : tests doivent ÉCHOUER (rouge). `pnpm tsc --noEmit` peut afficher "Cannot find module" sur imports prod (attendu).

    Commit : `test(02-01): tests rouges bailleur + clock + extension bail (Wave 0)`.
  </action>
  <verify>
    <automated>cd /Users/valentinshodo/Projects/toolbox/gestion-locative &amp;&amp; pnpm test 2>&amp;1 | grep -E "FAIL|fail" | head -20; pnpm test:bdd 2>&amp;1 | grep -E "fail|undefined" | head -20; ls tests/_world/monde-phase2.ts tests/_builders/identite.ts tests/unit/_shared/clock.test.ts tests/unit/identite/bailleur.test.ts tests/integration/repositories/bailleur-repository-sqlite.test.ts tests/bdd/features/bailleur.feature tests/bdd/step_definitions/bailleur.steps.ts</automated>
  </verify>
  <done>
    - 7 fichiers test/builder/world existent.
    - Extension de tests/unit/locatif/bail.test.ts avec 3 tests activer().
    - Tests ROUGES (échec attendu, code prod non écrit).
    - Commit `test(02-01): tests rouges...` créé.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration 0002 + Clock + Bailleur + extension Bail (faire passer les tests Wave 0 au vert)</name>
  <read_first>
    - migrations/0001_init.sql (conventions SQL)
    - src/infrastructure/db/database.ts (signature appliquerMigrationsBrutes)
    - src/infrastructure/db/kysely-types.ts (interface DB)
    - src/domain/_shared/identifiants.ts (pattern brand types)
    - src/domain/_shared/adresse.ts (analog VO factory)
    - src/domain/locatif/bail.ts (extension actifDepuis + jourEcheance + activer)
    - src/domain/locatif/bail-repository.ts (analog port)
    - src/infrastructure/repositories/bail-repository-sqlite.ts (analog adapter — versDomaine/versRow)
    - src/main.ts (extension creerApp opts.clock)
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-RESEARCH.md (§Topic 5 Singleton + §Topic 6 Clock complete code)
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-PATTERNS.md (§migrations/0002 + §bailleur.ts + §extension Bail)
  </read_first>
  <action>
    Faire passer au VERT les tests de Task 1 en créant le code de production minimal.

    Décisions tranchées par le planner :
    - DP-07 RÉSOLU : Bailleur dans `src/domain/identite/` (nouveau BC) — argumentaire RESEARCH §Recommandations.
    - DP-08 RÉSOLU : compteur quittance en table `meta` clé `compteur_quittance_AAAA` (impl plan 02-04).
    - DP-09 RÉSOLU : nom fichier PDF quittance `quittance-{numero}-{periode}-{slug}.pdf` (impl plan 02-04).
    - DP-10 RÉSOLU : `Money.multiplyByFraction(num, den, mode='banker')` (impl plan 02-02).
    - Découpage migrations : 1 fichier par plan (0002_phase2_bailleur_bail_ext.sql ici ; plans suivants 0003, 0004…). `main.ts` doit lire tous les `migrations/*.sql` triés alphabétiquement et les exécuter séquentiellement.

    Créer/modifier dans cet ordre :

    1. `migrations/0002_phase2_bailleur_bail_ext.sql` : ALTER TABLE bail ADD COLUMN actif_depuis TEXT NULL ; ALTER TABLE bail ADD COLUMN jour_echeance INTEGER NOT NULL DEFAULT 1 CHECK (jour_echeance >= 1 AND jour_echeance <= 28) ; CREATE TABLE bailleur(id TEXT PK, singleton_marker TEXT NOT NULL DEFAULT 'unique_bailleur', nom_complet TEXT NOT NULL, rue/code_postal/ville TEXT NOT NULL, cree_le/modifie_le DATETIME DEFAULT CURRENT_TIMESTAMP, UNIQUE(singleton_marker)). Cf. PATTERNS.md §migrations excerpt.

    2. Modifier `src/infrastructure/db/database.ts` : étendre `appliquerMigrationsBrutes` OU créer helper `appliquerToutesMigrations(db, sqlite, migrationsDir)` qui lit `fs.readdirSync(migrationsDir).filter(f=>f.endsWith('.sql')).sort()` et applique chaque fichier dans l'ordre. Garder rétro-compat de `appliquerMigrationsBrutes(db, sqlite, singleFilePath)` pour les tests Phase 1.

    3. `src/infrastructure/db/kysely-types.ts` : ajouter `bailleur: BailleurTable { id, singleton_marker, nom_complet, rue, code_postal, ville, cree_le, modifie_le }`. Étendre `BailTable` avec `actif_depuis: string | null` et `jour_echeance: number`.

    4. `src/domain/_shared/identifiants.ts` (extension — préserver exports existants) : AJOUTER `BailleurId`, `EcheanceLoyerId`, `EncaissementId`, `QuittanceId`, `RelanceId` brand types + factories `nouveauXId()` (anti-thrash : tous les 5 — les 4 derniers consommés plans suivants).

    5. `src/domain/_shared/clock.ts` (NOUVEAU) : interface `Clock { aujourdhui(): Temporal.PlainDate }`, classe `ClockSysteme` (retourne `Temporal.Now.plainDateISO()`), classe `ClockFixe` (constructor PlainDate, static `du(iso: string)`). Code complet RESEARCH §Topic 6.

    6. `src/domain/identite/erreurs.ts` (NOUVEAU) : `export class BailleurAbsent extends Error` (utile plan 02-04).

    7. `src/domain/identite/bailleur.ts` (NOUVEAU) : agrégat `Bailleur` avec props readonly (id, nomComplet, adresse:Adresse), private constructor, static `creer({nomComplet, adresse, id?})` (invariant: nomComplet.trim() !== ''), méthode `modifier(patch: { nomComplet?, adresse? }): Bailleur` (copy-on-write via Bailleur.creer). Pattern analog Adresse + Bail.modifier.

    8. `src/domain/identite/bailleur-repository.ts` (NOUVEAU) : port `interface BailleurRepository { trouver(): Promise<Bailleur | null>; enregistrer(bailleur: Bailleur): Promise<void>; mettreAJour(bailleur: Bailleur): Promise<void> }`. Pas de listerTous/supprimer (singleton).

    9. `src/infrastructure/repositories/bailleur-repository-sqlite.ts` (NOUVEAU) : adapter Kysely. Méthodes :
       - `trouver()` : selectFrom('bailleur').selectAll().executeTakeFirst() → versDomaine ou null.
       - `enregistrer(bailleur)` : insertInto('bailleur').values({ id, singleton_marker: 'unique_bailleur', nom_complet, rue, code_postal, ville }).execute(). La contrainte UNIQUE protège.
       - `mettreAJour(bailleur)` : updateTable('bailleur').set({ nom_complet, rue, code_postal, ville, modifie_le: new Date().toISOString() }).where('id', '=', bailleur.id).execute().
       - Helper privé `versDomaine(row)` retourne `Bailleur.creer({ id: row.id as BailleurId, nomComplet: row.nom_complet, adresse: Adresse.creer({ rue, codePostal: row.code_postal, ville }) })`.

    10. `src/domain/locatif/bail.ts` (ÉTENDRE) :
        - BailProps : ajouter `actifDepuis?: Temporal.PlainDate | null`, `jourEcheance?: number` (optionnels).
        - Classe : `readonly actifDepuis: Temporal.PlainDate | null` (défaut null si absent), `readonly jourEcheance: number` (défaut 1).
        - `creer()` : si jourEcheance fourni, valider 1..28 (sinon `InvariantViolated('Le jour d\'échéance doit être entre 1 et 28 (D-53)')`).
        - `modifier()` patch étendu : `actifDepuis?`, `jourEcheance?` (préserver si non patchés — attention `actifDepuis !== undefined` pour distinguer null intentionnel de absence patch).
        - AJOUTER méthode publique `activer(actifDepuis: Temporal.PlainDate, jourEcheance: number): Bail` qui appelle `Bail.creer({...this.toProps(), actifDepuis, jourEcheance})`.
        - AJOUTER méthode publique `desactiver(): Bail` (D-74) qui retourne `Bail.creer({...this.toProps(), actifDepuis: null, jourEcheance: this.jourEcheance})`. Préserve les autres champs ; bascule simplement `actifDepuis` à `null`. Pas de validation supplémentaire (un Bail désactivé reste valide en tant que brouillon — c'est l'état initial Phase 1).
        - Helper privé `toProps()` retournant un BailProps complet (DRY pour activer + modifier + desactiver).

    11. `src/infrastructure/repositories/bail-repository-sqlite.ts` (ÉTENDRE) :
        - versDomaine : lire row.actif_depuis (TEXT NULL → Temporal.PlainDate.from si non null, sinon null), row.jour_echeance (INTEGER).
        - versRow / INSERT/UPDATE : écrire `actif_depuis: bail.actifDepuis?.toString() ?? null`, `jour_echeance: bail.jourEcheance`.
        - Préserver TOUS les tests Phase 1 existants (non-régression).

    12. `src/main.ts` (MODIFIER) :
        - Import `Clock, ClockSysteme` depuis `./domain/_shared/clock.js`.
        - Changer signature : `creerApp(db: Kysely<DB>, opts: { clock?: Clock } = {}): Promise<FastifyInstance>`.
        - `const clock = opts.clock ?? new ClockSysteme();`.
        - Passer `clock` dans les opts des plugins routes (préparation plans suivants ; pas utilisé V1 par bailleur).
        - Adapter `demarrer()` pour utiliser le nouveau helper de migrations multi-fichiers : pointer vers le répertoire `migrations/`, plus juste `0001_init.sql`.

    Vérifs : `pnpm tsc --noEmit` (0 erreur), `pnpm lint:deps` (0 violation — domain pur), `pnpm test:unit run tests/unit/_shared/clock.test.ts tests/unit/identite/bailleur.test.ts tests/unit/locatif/bail.test.ts` (VERT), `pnpm test:integration run tests/integration/repositories/bailleur-repository-sqlite.test.ts tests/integration/repositories/bail-repository-sqlite.test.ts` (VERT — incluant non-régression Phase 1).

    Commit : `feat(02-01): clock + bailleur + extension bail`.
  </action>
  <verify>
    <automated>cd /Users/valentinshodo/Projects/toolbox/gestion-locative &amp;&amp; pnpm tsc --noEmit &amp;&amp; pnpm lint:deps &amp;&amp; pnpm test 2>&amp;1 | grep -E "PASS|FAIL" | head -20</automated>
  </verify>
  <done>
    - Migration 0002 appliquée par main.ts au démarrage.
    - clock.ts, bailleur.ts, bailleur-repository.ts, BailleurRepositorySqlite existent.
    - Bail.activer + champs actifDepuis/jourEcheance, repository étendu.
    - Tous tests unit/integration Wave 0 VERTS.
    - Tests Phase 1 EXISTANTS toujours verts (non-régression).
    - pnpm lint:deps 0 violation (domaine pur).
    - Commit `feat(02-01): clock + bailleur + extension bail` créé.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Use case + route + view /bailleur + sidebar + faire passer la BDD bailleur.feature au vert</name>
  <read_first>
    - src/web/routes/baux.ts (analog plugin Fastify — opts injection + GET/POST + safeParse + try/catch)
    - src/web/schemas/bail-schemas.ts (analog Zod schema)
    - src/web/views/pages/baux/formulaire.ejs (analog formulaire 1 colonne label-au-dessus)
    - src/web/views/partials/form-field.ejs (partial réutilisable)
    - src/web/views/partials/sidebar-nav.ejs (lignes 1-13 — pattern aria-current)
    - src/web/views/partials/banniere-success.ejs (pattern bannière succès)
    - src/application/locatif/modifier-bail.ts (analog upsert pattern lookup → modifier ou créer → persist)
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-UI-SPEC.md (§Screen Inventory "Profil bailleur" + §Copywriting "Profil bailleur" + §Empty State "Profil bailleur" + §Validation formulaire encaissement — réutilisable)
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-PATTERNS.md (§creer-ou-maj-bailleur.ts + §routes/bailleur.ts + §sidebar-nav.ejs extension)
    - tests/bdd/features/bailleur.feature (contrats BDD à faire passer)
    - tests/bdd/step_definitions/bailleur.steps.ts
  </read_first>
  <action>
    Faire passer au VERT la BDD `bailleur.feature` en livrant le slice complet use case → route → view → sidebar.

    Créer/modifier :

    1. `src/application/identite/creer-ou-maj-bailleur.ts` (NOUVEAU) :
       - Signature : `creerOuMajBailleur(commande: { nomComplet: string; adresse: Adresse }, bailleurRepo: BailleurRepository): Promise<BailleurId>`.
       - Pattern upsert : `const existant = await bailleurRepo.trouver()` ; si null, `const bailleur = Bailleur.creer({nomComplet, adresse}); await bailleurRepo.enregistrer(bailleur); return bailleur.id` ; sinon `const modif = existant.modifier({ nomComplet, adresse }); await bailleurRepo.mettreAJour(modif); return modif.id`.

    2. `src/web/schemas/bailleur-schemas.ts` (NOUVEAU) :
       - `bailleurFormSchema` : `z.object({ nomComplet: z.string().trim().min(1, 'Le nom complet est requis'), rue: z.string().trim().min(1, 'La rue est requise'), codePostal: z.string().trim().regex(/^\d{5}$/, 'Code postal à 5 chiffres'), ville: z.string().trim().min(1, 'La ville est requise') })`.
       - `type BailleurFormData = z.infer<typeof bailleurFormSchema>`.

    3. `src/web/routes/bailleur.ts` (NOUVEAU) :
       - `export async function plugin(app: FastifyInstance, opts: { bailleurRepo: BailleurRepository }): Promise<void>`.
       - `GET /bailleur` : `const bailleur = await opts.bailleurRepo.trouver()`. `reply.view('pages/bailleur/profil.ejs', { bailleur, navActive: 'bailleur', erreurs: {}, valeurs: {} })`.
       - `POST /bailleur` : safeParse body via bailleurFormSchema. Si !success → re-render avec erreurs + valeurs. Si success → `await creerOuMajBailleur({nomComplet, adresse: Adresse.creer({rue, codePostal, ville})}, opts.bailleurRepo)`. Session : `req.session.banniereSuccess = 'Profil bailleur enregistré.'` puis `reply.redirect('/bailleur')`. Catch try/catch : re-render avec erreurs._global.
       - Pattern erreurs et helper `extraireErreurs(issues)` à reprendre tel quel de routes/baux.ts.

    4. `src/web/views/pages/bailleur/profil.ejs` (NOUVEAU) :
       - layout-debut avec `titre: 'Profil bailleur'`, breadcrumbs `[{ label: 'Profil bailleur' }]`, `navActive: 'bailleur'`.
       - Si `locals.banniereSuccess` : `<%- include('../../partials/banniere-success', { message: banniereSuccess }) %>`.
       - `<h1>Profil bailleur</h1>`. Texte explicatif sur les mentions obligatoires (cf. UI-SPEC §Empty State : "Renseignez vos coordonnées de bailleur avant d'émettre votre première quittance (mentions légales obligatoires).").
       - `<form method="POST" action="/bailleur">` 1 colonne label-au-dessus. Champs : nomComplet (input text), rue (text), codePostal (text pattern '\d{5}'), ville (text). Pré-remplis avec `bailleur?.nomComplet`, `bailleur?.adresse.rue`, etc., OU `valeurs.nomComplet` si re-render après erreur. Erreurs : `<%- include('../../partials/form-field', { name: 'nomComplet', label: 'Nom complet', value: valeurs.nomComplet ?? bailleur?.nomComplet, erreur: erreurs.nomComplet }) %>`.
       - Bouton primaire : "Enregistrer le profil" (UI-SPEC §Copywriting). aria-describedby si erreur globale.
       - layout-fin.

    5. `src/web/views/partials/sidebar-nav.ejs` (ÉTENDRE) :
       - Ajouter `<li><a href="/bailleur" <% if (locals.navActive === 'bailleur') { %>aria-current="page"<% } %>>Profil bailleur</a></li>` à la liste existante (cf. PATTERNS.md §sidebar-nav extension).
       - PAS encore le groupe "Encaissements" (sera ajouté plans 02-02..02-06 quand les routes existeront — éviter liens cassés).

    6. `src/main.ts` (MODIFIER) :
       - Import `BailleurRepositorySqlite` + `plugin as bailleurPlugin` depuis `./web/routes/bailleur.js`.
       - Instancier `const bailleurRepo = new BailleurRepositorySqlite(db);`.
       - Registrer : `await app.register(bailleurPlugin, { bailleurRepo });`.

    7. Vérifier que la route gère bien la session bannière (Phase 1 LEARNING — preHandler ne gère PAS la session). Lire+vider la session dans `GET /bailleur` :
       ```
       const banniere = req.session.banniereSuccess;
       req.session.banniereSuccess = undefined;
       ```
       Passer dans le viewModel.

    Sécurité — points STRIDE pertinents pour cette route (cf. <threat_model>) :
    - Validation stricte Zod nomComplet/rue (V5 Input Validation).
    - codePostal regex /^\d{5}$/ (rejet injection).
    - PAS d'échappement manuel HTML — Pico.css + EJS échappent par défaut.
    - Session secret fail-fast déjà en place (Phase 1 D-26).

    Vérifs : `pnpm test:bdd -- --tags @bailleur` (VERT — 3 scenarios). `pnpm tsc --noEmit` 0 erreur. `pnpm lint` 0 warning. `pnpm lint:deps` 0 violation.

    Manual sanity check : `pnpm dev` → http://127.0.0.1:7878/bailleur → saisir profil → recharger → champs pré-remplis (mais cette vérif est dans les BDD scenarios, pas requise manuellement).

    Commit : `feat(02-01): page profil bailleur + sidebar + use case upsert`.
  </action>
  <verify>
    <automated>cd /Users/valentinshodo/Projects/toolbox/gestion-locative &amp;&amp; pnpm tsc --noEmit &amp;&amp; pnpm lint &amp;&amp; pnpm lint:deps &amp;&amp; pnpm test &amp;&amp; pnpm test:bdd -- --tags @bailleur</automated>
  </verify>
  <done>
    - GET /bailleur affiche formulaire (vide à la première visite, pré-rempli après).
    - POST /bailleur valide saisie, persiste, redirige avec bannière succès.
    - 2e POST sur profil existant met à jour (1 seul row en DB).
    - Sidebar contient lien "Profil bailleur" avec aria-current actif.
    - `pnpm test:bdd --tags @bailleur` PASSE 3 scenarios.
    - Tous tests Phase 1 + Wave 0 EXISTANTS toujours verts.
    - 0 violation lint/deps, 0 erreur TS.
    - Commit `feat(02-01): page profil bailleur...` créé.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: D-74 — Port ActiviteBailDetector + adapter v0 + supprimerBail extension + desactiverBail + extension fiche bail</name>
  <read_first>
    - src/application/locatif/supprimer-bail.ts (use case actuel à étendre)
    - src/web/routes/baux.ts (route POST /baux/:id/supprimer ligne 372-384 — pattern try/catch BailIntrouvable)
    - src/web/views/pages/baux/detail.ejs (vue actuelle — boutons Modifier/Supprimer + dialog suppression lignes 70-82 — à étendre)
    - src/web/views/partials/confirm-dialog.ejs (pattern modale réutilisable)
    - src/domain/locatif/bail.ts (agrégat — `actifDepuis` ajouté par Task 2)
    - src/domain/locatif/bail-repository.ts (port Phase 1)
    - src/domain/_shared/erreurs.ts (InvariantViolated)
    - src/application/locatif/modifier-bail.ts (analog use case avec lookup + erreur)
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-CONTEXT.md (§D-74 texte exact)
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-UI-SPEC.md (§Destructive Actions / §Copywriting boutons)
  </read_first>
  <behavior>
    - T-D74-1 (unit activite-bail-detector adapter v0) : `ActiviteBailDetectorSqlite.aDeLActivite(bailId)` retourne `false` en 02-01 (aucune table échéance/encaissement/quittance ne contribue encore — sera étendu plans 02-02 à 02-04).
    - T-D74-2 (unit supprimer-bail.test) : ÉTENDRE le test existant — fournir un stub `ActiviteBailDetector` qui retourne `false` → suppression OK.
    - T-D74-3 (unit supprimer-bail.test) : stub retourne `true` → throw `InvariantViolated` avec message exactement `"Bail avec activité ne peut être supprimé"`. Aucune ligne supprimée (assertion via mock repo).
    - T-D74-4 (unit desactiver-bail.test) : `desactiverBail(bailId, bailRepo)` lookup + bail.modifier({ actifDepuis: null }) (ou méthode dédiée `desactiver()` si modifier ne supporte pas null intentionnel) puis persist. Retourne void. BailIntrouvable si absent.
    - T-D74-5 (BDD @D-74 quittancement.feature) : Scenario "Suppression d'un Bail avec activité refusée" — Given un Bail actif (placeholder : utiliser un stub d'ActiviteBailDetector qui retourne true via injection World ; pour 02-01 où aucune table activité n'existe encore, on peut soit injecter directement un stub côté test, soit ajouter une ligne factice ; DÉCISION PLANNER : injecter un stub via le World — `this.activiteBailDetector = { aDeLActivite: async () => true }` avant register). When POST /baux/:id/supprimer. Then redirect /baux/:id avec banniereWarning "Ce bail a déjà de l'activité, il ne peut plus être supprimé. Vous pouvez en revanche le désactiver." + bail toujours présent en DB.
    - T-D74-6 (BDD @D-74) : Scenario "Désactivation d'un Bail actif" — Given un Bail avec actif_depuis non null. When POST /baux/:id/desactiver (modale confirmée). Then bail.actifDepuis === null en DB + banniereSuccess "Bail désactivé. Vous pouvez le réactiver depuis la fiche.".
  </behavior>
  <action>
    Étendre l'infrastructure D-74 (texte exact CONTEXT.md : "Suppression d'un Bail avec activité = refusée. Bouton 'Supprimer' désactivé avec message + bouton 'Désactiver' alternatif (actif_depuis = null)").

    Créer/modifier :

    1. `src/domain/locatif/activite-bail-detector.ts` (NOUVEAU — port domaine) :
       ```
       export interface ActiviteBailDetector {
         /** Retourne true si le Bail a au moins une EcheanceLoyer, un Encaissement ou une Quittance (active ou annulé). D-74 — suppression refusée. */
         aDeLActivite(bailId: BailId): Promise<boolean>;
       }
       ```
       Import `BailId` depuis `../_shared/identifiants.js`. Pas d'autre import (domain pur).

    2. `src/infrastructure/repositories/activite-bail-detector-sqlite.ts` (NOUVEAU — adapter SQLite v0 walking-enabler) :
       - Constructor `(private readonly db: Kysely<DB>)`.
       - `async aDeLActivite(bailId: BailId): Promise<boolean>` : en 02-01, retourner simplement `false` (commenter : "En 02-01 aucune table d'activité n'existe encore. Plan 02-02 étendra avec count(echeance_loyer where bail_id=?), 02-03 avec count(encaissement via echeance), 02-04 avec count(quittance via echeance). Pattern progressif : OR logique entre les 3 sous-requêtes pour court-circuiter dès qu'une activité est détectée.").
       - NOTE : ne PAS générer le code des plans suivants ici ; juste l'adapter v0 minimal qui pose le port pour que `supprimerBail` puisse l'injecter dès aujourd'hui.

    3. `src/application/locatif/supprimer-bail.ts` (ÉTENDRE) :
       - Nouvelle signature : `supprimerBail(id: BailId, bailRepo: BailRepository, activiteBailDetector: ActiviteBailDetector): Promise<void>`.
       - Lookup bail (sinon BailIntrouvable — comportement Phase 1 préservé).
       - Avant `bailRepo.supprimer(id)` : `if (await activiteBailDetector.aDeLActivite(id)) throw new InvariantViolated('Bail avec activité ne peut être supprimé');`. Message exact requis pour cohérence test T-D74-3 + BDD T-D74-5.
       - Puis `bailRepo.supprimer(id)` comme avant.

    4. `src/application/locatif/desactiver-bail.ts` (NOUVEAU) :
       - Signature : `desactiverBail(id: BailId, bailRepo: BailRepository): Promise<void>`.
       - Lookup bail (sinon BailIntrouvable).
       - PROBLÈME : `Bail.modifier` Phase 1 ne supporte PAS `actifDepuis` dans `ModifierBailPatch`. Task 2 étend `modifier` pour accepter `actifDepuis?: Temporal.PlainDate | null` (avec distinction `null` intentionnel via `actifDepuis !== undefined`). On l'utilise : `const bailDesactive = bail.modifier({ actifDepuis: null });`.
       - ALTERNATIVE plus explicite et plus sûre : ajouter `Bail.desactiver(): Bail` méthode publique qui retourne `Bail.creer({...this.toProps(), actifDepuis: null, jourEcheance: this.jourEcheance})`. **DÉCISION PLANNER : ajouter `Bail.desactiver()` méthode publique** (Task 2 doit ajouter cette méthode après `activer()`). Le use case appelle `bail.desactiver()` puis `bailRepo.enregistrer(bailDesactive)`.
       - Persister : si `BailRepository` Phase 1 a `enregistrer` (analog upsert) → l'utiliser. Sinon adapter approprié.

    5. AJOUTER à Task 2 (en amont, à intégrer mentalement quand la Task 2 sera exécutée) — voir mise à jour ci-dessous : `Bail.desactiver(): Bail`.

    6. `src/web/routes/baux.ts` (ÉTENDRE) :
       - Modifier la route POST /baux/:id/supprimer existante (lignes 372-384) pour catcher `InvariantViolated` :
         ```
         try {
           await supprimerBail(id as BailId, opts.bailRepo, opts.activiteBailDetector);
           return reply.redirect('/baux');
         } catch (err) {
           if (err instanceof BailIntrouvable) return reply.code(404).send(err.message);
           if (err instanceof InvariantViolated && err.message === 'Bail avec activité ne peut être supprimé') {
             req.session.banniereWarning = "Ce bail a déjà de l'activité, il ne peut plus être supprimé. Vous pouvez en revanche le désactiver.";
             return reply.redirect('/baux/' + id);
           }
           throw err;
         }
         ```
       - AJOUTER route `POST /baux/:id/desactiver` :
         ```
         try {
           await desactiverBail(id as BailId, opts.bailRepo);
           req.session.banniereSuccess = 'Bail désactivé. Vous pouvez le réactiver depuis la fiche.';
           return reply.redirect('/baux/' + id);
         } catch (err) {
           if (err instanceof BailIntrouvable) return reply.code(404).send(err.message);
           throw err;
         }
         ```
       - Étendre la signature `plugin(app, opts)` pour inclure `activiteBailDetector: ActiviteBailDetector` dans `opts`.

    7. `src/web/views/pages/baux/detail.ejs` (ÉTENDRE) :
       - Passer dans le viewModel (depuis GET /baux/:id) : `aDeLActivite: await opts.activiteBailDetector.aDeLActivite(bail.id)` + `banniereSuccess` + `banniereWarning` (lire+vider session — Phase 1 LESSON).
       - En tête de la vue, juste après `<h1>` : `<%- include('../../partials/banniere-success', { message: locals.banniereSuccess }) %>` si défini + `<%- include('../../partials/banniere-warning', ...) %>` si applicable (utiliser un partial banniere-warning.ejs analog si pas encore créé — ou inliner `<aside role="status" aria-live="polite" class="warning-zone">⚠ <%= banniereWarning %></aside>`).
       - REMPLACER le bloc des boutons (lignes 70-82) par :
         ```
         <p>
           <a href="/baux/<%= bail.id %>/modifier" role="button">Modifier le bail</a>
           &nbsp;
           <% if (locals.aDeLActivite || bail.actifDepuis !== null) { %>
             <% if (bail.actifDepuis !== null) { %>
               <button type="button" data-open-dialog="dialog-desactiver-bail">Désactiver</button>
             <% } %>
             <% if (locals.aDeLActivite) { %>
               <p><small>Ce bail a déjà de l'activité (échéances, encaissements ou quittances). Il ne peut plus être supprimé. Vous pouvez en revanche le désactiver.</small></p>
             <% } %>
           <% } else { %>
             <button type="button" class="destructif" data-open-dialog="dialog-supprimer-bail">Supprimer</button>
           <% } %>
         </p>

         <% if (bail.actifDepuis !== null) { %>
           <%- include('../../partials/confirm-dialog', {
             id: 'dialog-desactiver-bail',
             formAction: '/baux/' + bail.id + '/desactiver',
             message: 'Désactiver ce bail ? Les échéances futures ne seront plus émises mais l\'historique est préservé.',
             confirmLabel: 'Désactiver le bail',
             cancelLabel: 'Annuler'
           }) %>
         <% } %>

         <% if (!locals.aDeLActivite && bail.actifDepuis === null) { %>
           <%- include('../../partials/confirm-dialog', {
             id: 'dialog-supprimer-bail',
             formAction: '/baux/' + bail.id + '/supprimer',
             message: 'Supprimer ce bail ? Cette action est irréversible.',
             confirmLabel: 'Supprimer définitivement',
             cancelLabel: 'Annuler'
           }) %>
         <% } %>
         ```
       - Le dialog "dialog-supprimer-bail" Phase 1 (lignes 76-82) est encapsulé dans le `<% if (!aDeLActivite && actifDepuis === null) %>` — préserve la non-régression Phase 1 quand aDeLActivite=false ET actifDepuis=null.

    8. ÉTENDRE `src/main.ts` :
       - Import `ActiviteBailDetectorSqlite` depuis `./infrastructure/repositories/activite-bail-detector-sqlite.js`.
       - Instancier `const activiteBailDetector = new ActiviteBailDetectorSqlite(db);`.
       - Passer `activiteBailDetector` dans les opts du `bauxPlugin`.

    9. ÉTENDRE `tests/_world/monde-phase2.ts` (créé Task 1) :
       - Ajouter optionnellement un override `this.activiteBailDetector?: ActiviteBailDetector` qui, si fourni, remplace l'adapter SQLite par un stub côté BDD. Pour T-D74-5 : un step `Given le détecteur d'activité signale une activité existante` pose `this.activiteBailDetector = { aDeLActivite: async () => true }`. À ré-injecter dans `creerApp` au moment du Before hook ou via un override `app.decorate` — le plus simple : la Before hook recrée `creerApp` avec un override d'opts.
       - NOTE : ce stub n'est nécessaire qu'en 02-01 (parce qu'aucune table d'activité n'existe encore). À partir de 02-02 (echeance_loyer présent), on pourra créer une vraie échéance via le World pour rendre `aDeLActivite=true` naturellement. Préférer cette approche dès 02-02 pour ne pas dépendre du stub.

    10. `tests/bdd/features/quittancement.feature` (NOUVEAU fichier — créé ici ; plans 02-02..02-06 l'étendront) :
        - Scenario T-D74-5 et T-D74-6 tagués `@D-74`.
        - Préfixe header : `@D-74 @phase2`.

    11. `tests/bdd/step_definitions/quittancement.steps.ts` (NOUVEAU fichier) :
        - Step definitions pour T-D74-5/T-D74-6 (POST /baux/:id/supprimer + POST /baux/:id/desactiver + Then la base contient encore le bail + Then bail.actif_depuis IS NULL).
        - NOTE : ce fichier sera ÉTENDU (pas écrasé) par 02-02..02-05 — voir M3 ci-dessous.

    Sécurité (cf. <threat_model>) :
    - Le bouton Supprimer est désactivé/caché côté UI ET côté serveur (defense in depth). Si un attaquant force un POST direct, le use case throw InvariantViolated.
    - La modale Désactiver suit le pattern destructif Phase 1 (confirmation explicite avant action) — cohérent ACCESSIBILITY.md §destructive.

    Vérifs : `pnpm test:bdd -- --tags @D-74` 2 scenarios VERTS. `pnpm tsc --noEmit` 0 erreur. Tests Phase 1 toujours verts (la signature de `supprimerBail` change, mais le test Phase 1 doit être adapté pour fournir le stub `aDeLActivite: false`).

    Commit : `feat(02-01): D-74 — port ActiviteBailDetector + suppression refusée si activité + désactiver`.
  </action>
  <verify>
    <automated>cd /Users/valentinshodo/Projects/toolbox/gestion-locative &amp;&amp; pnpm tsc --noEmit &amp;&amp; pnpm lint &amp;&amp; pnpm lint:deps &amp;&amp; pnpm test &amp;&amp; pnpm test:bdd -- --tags @D-74</automated>
  </verify>
  <done>
    - Port ActiviteBailDetector créé dans domain/locatif/.
    - Adapter SQLite v0 retourne false (sera étendu plans 02-02 à 02-04).
    - supprimerBail étend signature pour injecter le port + throw InvariantViolated si activité.
    - desactiverBail use case créé.
    - Bail.desactiver() méthode publique ajoutée en Task 2 (rappel intégré).
    - Routes POST /baux/:id/supprimer et POST /baux/:id/desactiver fonctionnelles.
    - View detail.ejs affichage conditionnel Supprimer/Désactiver selon aDeLActivite + actifDepuis.
    - 2 scenarios BDD @D-74 VERTS.
    - Tests Phase 1 supprimerBail adaptés (stub aDeLActivite: false) — non-régression respectée.
    - Commit créé.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigateur user → Fastify POST /bailleur | Saisie utilisateur (nomComplet, rue, codePostal, ville) traverse cette frontière |
| Fastify → SQLite | Persistance — protégée par Kysely paramétré (jamais sql.raw avec input user) |
| Application bug → DB direct | Race / bug applicatif tentant 2e bailleur — protégé par UNIQUE(singleton_marker) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01-01 | Tampering (V5 Input Validation) | POST /bailleur — nomComplet/rue/ville arbitrary strings | MED | mitigate | Zod `bailleurFormSchema` valide `z.string().trim().min(1)` côté HTTP ; côté domaine `Bailleur.creer` valide `nomComplet.trim() !== ''` (double barrière) |
| T-02-01-02 | Tampering | POST /bailleur — codePostal injection / format invalide | LOW | mitigate | Zod regex `/^\d{5}$/` strict (5 chiffres uniquement) |
| T-02-01-03 | Tampering | INSERT 2e bailleur (race / bug applicatif) | MED | mitigate | UNIQUE(singleton_marker) DB + use case `creerOuMajBailleur` qui appelle `trouver()` avant `enregistrer()` (double barrière — RESEARCH §5) |
| T-02-01-04 | Information disclosure | XSS via nomComplet contenant `<script>` rendu dans EJS | LOW | mitigate | EJS échappe par défaut (`<%= %>`) ; aucun usage de `<%- %>` sur input user dans profil.ejs |
| T-02-01-05 | Denial of Service | Migration 0002 plante au démarrage (CHECK constraint conflit avec bails existants Phase 1) | LOW | accept | Phase 1 a 0 bail créé jusqu'à activation ; DEFAULT 1 sur jour_echeance évite NOT NULL fail ; tests intégration valident l'idempotence des migrations (CREATE TABLE IF NOT EXISTS) |
| T-02-01-06 | Tampering — bypass D-74 par requête forgée | POST /baux/:id/supprimer direct (sans passer par la fiche UI) → tentative de supprimer un bail avec activité | MED | mitigate | Defense in depth : (1) UI masque le bouton si aDeLActivite=true ; (2) use case `supprimerBail` throw `InvariantViolated('Bail avec activité ne peut être supprimé')` même si requête arrive directement ; (3) au plan 02-02 et suivants, l'adapter SQLite vérifie réellement les tables echeance_loyer/encaissement/quittance avant de retourner false |
| T-02-01-07 | Repudiation — bail désactivé puis ré-activé sans trace | Désactivation/réactivation = silent state change, audit-friendly ? | LOW | accept | V1 : `actif_depuis = null` est observable côté DB ; les `EcheanceLoyer` générées AVANT désactivation restent intactes (D-74 préserve l'historique). Phase 4 ajoutera un journal d'événements (`event_log`) avec retentions 10 ans pour traçabilité fiscale complète |
</threat_model>

<verification>
- `pnpm tsc --noEmit` exit 0
- `pnpm lint` 0 warning
- `pnpm lint:deps` 0 violation (domaine pur — `bailleur.ts` n'importe aucun module technique)
- `pnpm test` (unit + integration) tout VERT — incluant tests Phase 1 existants (non-régression)
- `pnpm test:bdd -- --tags @bailleur` 3 scenarios PASSED
- `pnpm test:bdd -- --tags @D-74` 2 scenarios PASSED
- Migration 0002 appliquée idempotente au démarrage (`main.ts` lit tous les `migrations/*.sql` triés)
- Sidebar contient lien "Profil bailleur" — vérifiable via `grep -E "Profil bailleur" src/web/views/partials/sidebar-nav.ejs`
- Route POST /baux/:id/desactiver fonctionnelle ; route POST /baux/:id/supprimer refuse si activité (D-74)
</verification>

<success_criteria>
- Page /bailleur fonctionnelle (saisie + persistance + pré-remplissage 2e visite).
- Port Clock + ClockSysteme + ClockFixe utilisables par plans 02-02..02-06.
- Bail.activer(actifDepuis, jourEcheance) disponible pour plan 02-02 (génération EcheanceLoyer).
- Migration 0002 conforme au pattern Phase 1, ALTER bail backward-compat avec tests existants.
- Cucumber World Phase 2 réutilisable par plans 02-02..02-06.
- DP-07/DP-08/DP-09/DP-10 résolus par le planner (placement Bailleur identite/, compteur meta, slug PDF, Money.multiplyByFraction banker's).
- 100 % couverture invariants Bailleur (validation nomComplet non-vide, singleton DB protégé).
- Aucun import technique dans `src/domain/` (vérifié par `pnpm lint:deps`).
- Ubiquitous language français (Bailleur, Bail, actifDepuis, jourEcheance, nomComplet, adresse, desactiver, activiteBailDetector).
- D-74 satisfait : suppression refusée si activité (use case throw + UI condition), bouton Désactiver disponible, message UI explicatif, scenarios BDD @D-74 PASSED.
</success_criteria>

<output>
After completion, create `.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-01-SUMMARY.md` listant :
- 4 commits créés (tests rouges Wave 0 / feat clock+bailleur+ext bail / feat page profil / feat D-74 port + suppression refusée + désactiver)
- Fichiers créés (24 fichiers) et modifiés (3 fichiers : main.ts, bail.ts, bail-repository-sqlite.ts, identifiants.ts, kysely-types.ts, sidebar-nav.ejs, database.ts)
- Patterns établis : Clock injection, Singleton DDD (use case + DB UNIQUE), upsert use case, migration multi-fichiers
- Surprises éventuelles (à comparer aux Phase 1 LEARNINGS)
- DP-07/08/09/10 résolus
- Dépendances pour plans suivants : EcheanceLoyerId/EncaissementId/QuittanceId/RelanceId déjà exportés (anti-thrash), Clock injecté dans creerApp, Bail.activer disponible
</output>
