---
phase: 02
plan: 06
plan_id: "02-06"
type: execute
wave: 6
depends_on: ["02-01", "02-02", "02-03", "02-04", "02-05"]
files_modified:
  - migrations/0006_phase2_relance.sql
  - src/infrastructure/db/kysely-types.ts
  - src/domain/encaissements/relance.ts
  - src/domain/encaissements/relance-repository.ts
  - src/domain/encaissements/erreurs.ts
  - src/application/encaissements/calculer-relance-disponible.ts
  - src/application/encaissements/enregistrer-relance.ts
  - src/application/encaissements/lister-relances.ts
  - src/infrastructure/repositories/relance-repository-sqlite.ts
  - src/infrastructure/pdf/mise-en-demeure-doc-def.ts
  - src/helpers/build-mailto.ts
  - src/domain/encaissements/template-renderer.ts
  - src/infrastructure/templates/template-renderer-ejs.ts
  - src/web/schemas/relance-schemas.ts
  - src/web/routes/relances.ts
  - src/web/views/pages/relances/liste.ejs
  - src/web/views/partials/relance-action.ejs
  - src/web/views/pages/impayes/liste.ejs
  - src/web/views/partials/sidebar-nav.ejs
  - src/main.ts
  - templates/relances/01-amiable.ejs
  - templates/relances/02-ferme.ejs
  - templates/relances/03-mise-en-demeure.ejs
  - tests/_builders/encaissements.ts
  - tests/unit/encaissements/relance.test.ts
  - tests/unit/encaissements/calculer-relance-disponible.test.ts
  - tests/unit/encaissements/enregistrer-relance.test.ts
  - tests/unit/helpers/build-mailto.test.ts
  - tests/integration/repositories/relance-repository-sqlite.test.ts
  - tests/integration/pdf/mise-en-demeure.test.ts
  - tests/bdd/features/relances.feature
  - tests/bdd/step_definitions/relances.steps.ts
autonomous: true
requirements: ["ENC-05"]
user_setup: []

must_haves:
  truths:
    - "3 niveaux de relance : 1=amiable (J+10), 2=relance ferme (J+30), 3=mise en demeure (J+60) — D-68, seuils stockés en constante de domaine."
    - "Chaînage strict (D-71) : niveau 2 disponible uniquement si niveau 1 a été envoyé ; niveau 3 disponible uniquement si niveau 2 a été envoyé. Impossible de sauter au niveau 3 sans avoir envoyé 1 puis 2."
    - "Canal hybride (D-69) : niveaux 1 et 2 → URI mailto: (RFC 6068, encodeURIComponent, %0D%0A pour CRLF Windows Outlook) ; niveau 3 → PDF imprimable (LR/AR par la poste)."
    - "Templates fixes V1 dans `templates/relances/*.ejs` (D-70) — rendus côté serveur via `ejs.render(fs.readFileSync(path), variables)`."
    - "Variables interpolées : prenom_locataire, nom_locataire, periode_impayee (ex. 'mai 2026'), montant_du (ex. '850,50 €'), date_echeance_initiale, nom_bailleur, adresse_bailleur."
    - "Calcul niveau disponible (D-71) à la demande dans la route : `niveauDisponible = 1` si `today >= jourEcheanceAttendue + 10j` ; `= 2` si relance 1 envoyée ET `today >= +30j` ; `= 3` si relance 2 envoyée ET `today >= +60j`. Pas de cron."
    - "Enregistrement Relance : POST /relances avec echeanceId + niveau, snapshot du contenu (`contenuSnapshot` JSON stringifié), `envoyeeLe = clock.aujourdhui()` (D-71)."
    - "Suggestion contextuelle visible sur fiche échéance (page Impayés étendue) : bouton 'Lancer la relance niveau N' ou 'Télécharger la mise en demeure PDF' selon niveau."
    - "Page GET /relances liste toutes les relances émises (échéance, locataire, niveau, date envoyée)."
    - "PDF mise en demeure (niveau 3) conforme art. 1344 Code civil avec mentions : identité bailleur + locataire, référence bail, montant total dû, délai 8 jours, signature, mention LR/AR (D-69 + UI-SPEC §PDF Layout)."
  artifacts:
    - path: "migrations/0006_phase2_relance.sql"
      provides: "Table relance (niveau, canal, envoyee_le, contenu_snapshot)"
      contains: "CREATE TABLE IF NOT EXISTS relance"
    - path: "src/domain/encaissements/relance.ts"
      provides: "Agrégat Relance + NiveauRelance enum (1|2|3) + CanalRelance enum"
      exports: ["Relance", "NiveauRelance", "CanalRelance"]
    - path: "src/application/encaissements/calculer-relance-disponible.ts"
      provides: "Use case pur — calcule niveau disponible selon seuils J+10/J+30/J+60 + chaînage strict"
      exports: ["calculerRelanceDisponible", "SEUILS_RELANCE_JOURS"]
    - path: "src/application/encaissements/enregistrer-relance.ts"
      provides: "Use case create Relance + génération mailto (1-2) ou PDF (3) + snapshot contenu"
      exports: ["enregistrerRelance"]
    - path: "src/helpers/build-mailto.ts"
      provides: "Helper buildMailto(params) RFC 6068 + truncation 1900 chars"
      exports: ["buildMailto", "MailtoParams"]
    - path: "src/infrastructure/pdf/mise-en-demeure-doc-def.ts"
      provides: "TDocumentDefinitions mise en demeure (Code civil art. 1344)"
      exports: ["construireMiseEnDemeure"]
    - path: "templates/relances/01-amiable.ejs / 02-ferme.ejs / 03-mise-en-demeure.ejs"
      provides: "Templates textuels EJS avec variables interpolées (D-70)"
  key_links:
    - from: "src/application/encaissements/calculer-relance-disponible.ts"
      to: "src/domain/_shared/clock.ts"
      via: "compare clock.aujourdhui() avec jourEcheanceAttendue + SEUILS_RELANCE_JOURS[niveau]"
      pattern: "aujourdhui"
    - from: "src/application/encaissements/enregistrer-relance.ts"
      to: "templates/relances/*.ejs"
      via: "ejs.render(fs.readFileSync(path), variables)"
      pattern: "templates/relances"
    - from: "src/web/routes/relances.ts"
      to: "src/helpers/build-mailto.ts"
      via: "niveaux 1-2 : mailtoUri = buildMailto({ to, subject, body: contenuRendu })"
      pattern: "buildMailto"
    - from: "src/web/routes/relances.ts"
      to: "src/infrastructure/pdf/mise-en-demeure-doc-def.ts"
      via: "niveau 3 : pdfRenderer.genererBuffer(construireMiseEnDemeure(...))"
      pattern: "construireMiseEnDemeure"
    - from: "src/web/views/pages/impayes/liste.ejs"
      to: "src/web/views/partials/relance-action.ejs"
      via: "rendu conditionnel bouton relance par ligne selon calculerRelanceDisponible"
      pattern: "relance-action"
---

<objective>
Vertical slice ENC-05 : 3 niveaux de relance escaladée (amiable → ferme → mise en demeure) avec chaînage strict, canal hybride (mailto natif niveaux 1-2 + PDF imprimable niveau 3), templates EJS, et suggestion contextuelle sur la page Impayés.

Purpose: ENC-05 conclut la Phase 2 — le bailleur dispose d'un outil de relance pré-judiciaire conforme à la pratique française. Mailto natif évite la complexité SMTP + secret en .env (local-first cohérent). PDF imprimable pour la mise en demeure (LR/AR par la poste légalement requis).
Output: Boutons "Lancer relance niveau N" actifs sur page Impayés selon seuils ; envoi déclaré (snapshot contenu en DB) ; PDF mise en demeure téléchargeable.
</objective>

<execution_context>
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/workflows/execute-plan.md
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-CONTEXT.md
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-RESEARCH.md
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-PATTERNS.md
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-UI-SPEC.md
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-01-PLAN.md
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-02-PLAN.md
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-03-PLAN.md
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-04-PLAN.md
@.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-05-PLAN.md
@CLAUDE.md
@BDD_PRACTICES.md
@UI_DESIGN.md
@UX_DESIGN.md
@ACCESSIBILITY.md
@src/domain/_shared/clock.ts
@src/domain/_shared/identifiants.ts
@src/domain/encaissements/echeance-loyer.ts
@src/domain/encaissements/echeance-loyer-repository.ts
@src/domain/encaissements/encaissement-repository.ts
@src/domain/encaissements/impaye.ts
@src/domain/identite/bailleur.ts
@src/application/encaissements/lister-impayes.ts
@src/infrastructure/pdf/pdf-renderer-pdfmake.ts
@src/infrastructure/pdf/avis-echeance-doc-def.ts
@src/infrastructure/pdf/quittance-doc-def.ts
@src/helpers/format-periode.ts
@src/web/routes/impayes.ts
@src/web/views/pages/impayes/liste.ejs
@src/web/views/partials/statut-badge.ejs
</context>

<interfaces>
Contrats clés :

- `NiveauRelance = 1 | 2 | 3` ; `CanalRelance = 'email' | 'pdf'`.
- `SEUILS_RELANCE_JOURS: Record<NiveauRelance, number> = { 1: 10, 2: 30, 3: 60 }` (D-68).
- `Relance` props : `{ id?, echeanceId, niveau, canal, envoyeeLe: PlainDate, contenuSnapshot: string (JSON), annuleLe?: PlainDate | null }`.
- `Relance.creer(props)` valide : niveau ∈ {1,2,3} ; canal ∈ {email, pdf}. id défaut nouveauRelanceId. annuleLe défaut null.
- `Relance.annuler(annuleLe): Relance` (soft-delete si erreur, D-71 — copy-on-write).
- `RelanceRepository` port : `enregistrer`, `trouverParId`, `listerParEcheance(echeanceId, opts?: {inclureAnnulees})` (filtre annule_le IS NULL par défaut), `listerToutes(opts?: {inclureAnnulees})`. PAS de `supprimer`.
- `calculerRelanceDisponible(echeance: EcheanceLoyer, relancesEnvoyees: Relance[], today: PlainDate): NiveauRelance | null` — fonction PURE :
  - Tri relances par niveau ASC (filtre annule_le IS NULL).
  - dernierNiveauEnvoye = max niveau ∈ relances actives ou 0 si vide.
  - niveauCandidat = dernierNiveauEnvoye + 1 (mais ≤ 3).
  - Si echeance.statut === 'payee' ou 'annulee' → retourner null.
  - Vérifier seuil : `seuil = jourEcheanceAttendue.add({days: SEUILS_RELANCE_JOURS[niveauCandidat]})`. Si `today >= seuil` → retourner niveauCandidat ; sinon null.
- `enregistrerRelance(commande: { echeanceId, niveau }, repos, templateRenderer: TemplateRenderer, pdfRenderer, clock): Promise<{ relanceId, canal, mailtoUri?: string, pdfBuffer?: Buffer }>` (M4 — injection port TemplateRenderer) :
  - Vérification chaînage strict via calculerRelanceDisponible (sinon throw `RelanceNiveauNonDisponible`).
  - lookup echeance + bail + locataire + bailleur (pour interpoler templates).
  - sommePaiee = encaissementRepo.sommePaieeParEcheance ; resteDu = total.soustraire(sommePaiee).
  - Variables template : { prenom_locataire, nom_locataire, adresse_locataire, periode_impayee, montant_du, date_echeance_initiale, nom_bailleur, adresse_bailleur }.
  - templatePath = `templates/relances/0{niveau}-{slug}.ejs` (01-amiable, 02-ferme, 03-mise-en-demeure).
  - contenuRendu = templateRenderer.rendre(niveau, variables) (port `TemplateRenderer` injecté ; adapter `TemplateRendererEjs` lit le fichier et appelle `ejs.render` — M4 plan-checker hexagonal split).
  - canal = niveau === 3 ? 'pdf' : 'email'.
  - **Structure `contenuSnapshot` (D-69 audit-friendly + H4 plan-checker)** : objet TypeScript stocké en TEXT JSON dans la colonne `contenu_snapshot` :
    ```
    interface ContenuSnapshotV1 {
      variables: VariablesRelance;        // toutes les valeurs interpolées au moment de l'envoi
      contenuRendu: string;               // le texte complet rendu par ejs.render (objet + corps)
      mailtoUri: string | null;           // l'URI mailto pour niveaux 1-2 (réutilisable post-envoi) ; null pour niveau 3 (PDF)
      version: 'v1';                      // versionnement explicite pour migrations futures
    }
    ```
  - Pour les canaux email (niveaux 1, 2) : `mailtoUri = buildMailto({to: locataire.email, subject: extraireSujet(contenuRendu), body: extraireCorps(contenuRendu)})`. Pour niveau 3 (canal pdf) : `mailtoUri = null`.
  - `relance = Relance.creer({ echeanceId, niveau, canal, envoyeeLe: clock.aujourdhui(), contenuSnapshot: JSON.stringify({ variables, contenuRendu, mailtoUri, version: 'v1' } satisfies ContenuSnapshotV1) }).`
  - relanceRepo.enregistrer(relance).
  - Si canal === 'email' : retourner `{ relanceId, canal: 'email', mailtoUri }` (déjà calculé et stocké dans snapshot).
  - Si canal === 'pdf' : `docDef = construireMiseEnDemeure(...) ; buffer = pdfRenderer.genererBuffer(docDef) ; retourner { relanceId, canal: 'pdf', pdfBuffer: buffer }`. NOTE : PDF mise en demeure NON persisté sur disque (différent de Quittance D-66 / on-the-fly comme avis ; ou OUI persisté pour archivage ? DÉCISION PLANNER : PDF NON persisté en V1 — l'utilisateur télécharge, imprime, poste. Si besoin de réimpression future, regénérer depuis la fiche relance — le contenuSnapshot suffit pour reproduire à l'identique. Cohérent avec D-66 esprit "on-the-fly").
- `buildMailto({ to, subject, body }): string` (RESEARCH §Topic 2) :
  - `encodeURIComponent` sur subject + body.
  - `body.replaceAll('%0A', '%0D%0A')` après encodage (CRLF Windows).
  - Truncate body à 1900 chars encodés + mention `[Message tronqué — voir courrier joint si nécessaire]`.
  - Retourne `mailto:${to}?subject=${subject}&body=${body}` ou avec `cc` si fourni.
- `extraireSujet / extraireCorps` : parser le contenuRendu format `Objet : XXX\n\nXXX...` — première ligne (après "Objet : ") est le sujet, le reste est le corps.
- Page `/impayes/liste.ejs` étendue : pour chaque impayé non payé/annulé, calculer niveauDisponible via `calculerRelanceDisponible(echeance, relancesActives, clock.aujourdhui())`. Si niveauDisponible !== null → afficher bouton via partial `relance-action.ejs`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests rouges Wave 0 — Relance domain + use cases + buildMailto + integration + BDD ENC-05</name>
  <read_first>
    - tests/_builders/encaissements.ts (à étendre)
    - tests/unit/encaissements/quittance.test.ts (analog domain factory)
    - tests/unit/encaissements/recalculer-statut-echeance.test.ts (analog use case test avec mocks)
    - tests/integration/repositories/encaissement-repository-sqlite.test.ts (analog setup)
    - tests/integration/pdf/quittance.test.ts (analog test buffer mentions)
    - tests/bdd/features/quittancement.feature (à étendre OU créer relances.feature séparée — DÉCISION PLANNER : créer `relances.feature` séparée pour clarté et permettre tagging @relance distinct)
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-RESEARCH.md (§Topic 2 buildMailto + §Topic 7 mise en demeure PDF complet)
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-PATTERNS.md (§relance.ts + §enregistrer-relance + §calculer-relance + §routes/relances + §templates/relances + §build-mailto)
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-UI-SPEC.md (§Screen Inventory "Relances" + §Component Patterns "Bouton conditionnel relance" + §Templates EJS Relances templates 1/2/3 contenus exacts + §PDF Layout "Mise en demeure" + §Copywriting "Lancer la relance amiable/ferme" / "Télécharger la mise en demeure PDF" + §Empty States "Aucune relance envoyée")
  </read_first>
  <behavior>
    - T1 relance.test : `Relance.creer({echeanceId, niveau: 1, canal: 'email', envoyeeLe: PlainDate, contenuSnapshot: '{...}'})` valide.
    - T2 : niveau hors {1,2,3} → throw InvariantViolated.
    - T3 : canal hors {'email','pdf'} → throw InvariantViolated.
    - T4 : `relance.annuler(PlainDate)` retourne nouvelle Relance avec annuleLe set.
    - T5 calculer-relance-disponible.test : echeance.statut='payee' → null (pas de relance sur échéance payée).
    - T6 : statut='annulee' → null.
    - T7 : statut='en_attente', today = jourEcheanceAttendue + 5j, aucune relance → null (J+10 pas atteint).
    - T8 : statut='en_attente', today = +10j, aucune relance → 1 (niveau 1 disponible).
    - T9 : statut='en_attente', today = +29j, relance 1 envoyée → null (J+30 pas atteint pour niveau 2).
    - T10 : statut='en_attente', today = +30j, relance 1 envoyée → 2 (niveau 2 disponible).
    - T11 : statut='en_attente', today = +60j, relance 1 envoyée mais PAS 2 → 2 (chaînage strict — saut interdit, le niveau 3 nécessite niveau 2 envoyé).
    - T12 : today = +60j, relances 1 ET 2 envoyées → 3.
    - T13 : today = +100j, relances 1, 2, 3 toutes envoyées → null (toutes niveaux épuisés).
    - T14 : today = +30j, relance 1 envoyée PUIS annulée (annuleLe !== null) → 1 (la relance 1 annulée ne compte pas — peut être ré-envoyée niveau 1).
    - T15 enregistrer-relance.test : niveau 1, echeance impayée, locataire X, bailleur Y → retourne `{ relanceId, canal: 'email', mailtoUri: 'mailto:locataire@x.com?subject=...&body=...' }` avec subject + body encodés.
    - T16 : tentative niveau 3 sans niveau 2 envoyé → throw RelanceNiveauNonDisponible.
    - T17 : niveau 3, retourne `{ canal: 'pdf', pdfBuffer: Buffer }`. Buffer commence par %PDF-.
    - T18 build-mailto.test : `buildMailto({to: 'jean@x.com', subject: 'Rappel loyer', body: 'Ligne 1\nLigne 2'})` retourne `mailto:jean@x.com?subject=Rappel%20loyer&body=Ligne%201%0D%0ALigne%202`.
    - T19 build-mailto.test : accents `'Bonjour René'` encodés `'Bonjour%20Ren%C3%A9'` (UTF-8 + percent).
    - T20 build-mailto.test : body très long (3000 chars) tronqué à 1900 chars encodés + mention `[Message tronqué...]`.
    - T21 integration relance-repo.test : roundtrip Relance via SQLite. listerParEcheance filtre annule_le IS NULL par défaut.
    - T22 integration pdf mise-en-demeure.test : buffer commence par %PDF-, contient "MISE EN DEMEURE DE PAYER", "délai de 8 (huit) jours", "voies de droit", "lettre recommandée avec accusé de réception" (mentions essentielles Code civil art. 1344 + pratique pré-judiciaire — voir RESEARCH §Topic 7).
    - T23 BDD @enc-05 "Niveau 1 disponible à J+10" : Given Bail activé, ClockFixe '2026-05-01' (jourEcheanceAttendue = 2026-05-05) + echeance en_attente, ClockFixe '2026-05-15' (J+10), GET /impayes → bouton "Lancer la relance amiable" présent ; cliquer (POST /relances) → relance 1 enregistrée en DB + redirection vers fiche échéance ou /relances avec banniereSuccess + mailtoUri affiché pour l'utilisateur.
    - T24 BDD @enc-05 "Niveau 2 attend niveau 1" : Given echeance impayée + relance 1 envoyée le 2026-05-15, ClockFixe '2026-06-04' (J+30 du 2026-05-05), GET /impayes → bouton "Lancer la relance ferme" présent (niveau 2 disponible).
    - T25 BDD @enc-05 "Chaînage strict : impossible de sauter niveau 3" : Given echeance impayée, aucune relance, ClockFixe '2026-07-15' (J+71), GET /impayes → bouton "Lancer la relance amiable" présent (niveau 1) — PAS bouton niveau 3.
    - T26 BDD @enc-05 "Mise en demeure PDF" : Given echeance + relance 1 envoyée + relance 2 envoyée, ClockFixe '2026-07-04' (J+60), bouton "Télécharger la mise en demeure PDF" → POST /relances niveau 3 → réponse PDF avec Content-Type application/pdf, body buffer contient "MISE EN DEMEURE".
    - T27 BDD @enc-05 "Page Relances vide" : GET /relances avec aucune relance → empty state "Aucune relance envoyée" (UI-SPEC §Empty States).
    - T28 BDD @enc-05 "Suggestion désactive après envoi" : Given relance 1 envoyée à J+10, ClockFixe = J+10 toujours, GET /impayes → bouton niveau 1 PLUS visible (relance 1 déjà envoyée — niveau suivant pas encore disponible car J+30 pas atteint).
    - T29 BDD `@enc-05 @D-69` "Téléchargement PDF mise en demeure depuis fiche relance" :
      Given un bail actif avec un locataire et une échéance impayée depuis 65 jours
      Given une Relance niveau 3 enregistrée hier (envoyeeLe = today - 1)
      When je télécharge le PDF depuis la fiche /relances/:id (lien `Télécharger la mise en demeure (PDF)` → GET /relances/:id/pdf)
      Then je reçois un PDF avec Content-Type application/pdf
      And le PDF contient la mention juridique "MISE EN DEMEURE" et le montant impayé exact (resteDu formaté en euros)
      And le compteur de relances en DB n'est pas incrémenté (SELECT COUNT(*) FROM relance WHERE echeance_id = X — toujours 1, pas 2)
      And l'envoyeeLe de la Relance existante reste inchangé (today - 1, pas today)
      And le test T29.bis : si le locataire est supprimé entre l'envoi et la regénération → GET /relances/:id/pdf retourne 404 "Document indisponible — une entité référencée a été supprimée" (robustesse documentée H4).
  </behavior>
  <action>
    TDD outside-in — créer EXCLUSIVEMENT les tests. ROUGES attendus.

    Créer/modifier :
    1. ÉTENDRE `tests/_builders/encaissements.ts` avec `uneRelanceValide(overrides?)`. Defaults : niveau 1, canal 'email', envoyeeLe PlainDate.from('2026-05-15'), contenuSnapshot '{"version":"v1"}', annuleLe null.
    2. `tests/unit/encaissements/relance.test.ts` (NOUVEAU) : T1-T4.
    3. `tests/unit/encaissements/calculer-relance-disponible.test.ts` (NOUVEAU) : T5-T14. Fonction PURE — pas de mocks nécessaires.
    4. `tests/unit/encaissements/enregistrer-relance.test.ts` (NOUVEAU) : T15-T17. Mocks pour pdfRenderer + repos.
    5. `tests/unit/helpers/build-mailto.test.ts` (NOUVEAU) : T18-T20.
    6. `tests/integration/repositories/relance-repository-sqlite.test.ts` (NOUVEAU) : T21. Migrations 0001..0006.
    7. `tests/integration/pdf/mise-en-demeure.test.ts` (NOUVEAU) : T22.
    8. `tests/bdd/features/relances.feature` (NOUVEAU — fichier séparé) : T23-T29 tag `@enc-05` (T29 + T29.bis taggés `@enc-05 @D-69`).
    9. `tests/bdd/step_definitions/relances.steps.ts` (NOUVEAU — fichier séparé per DÉCISION PLANNER ; **M3 plan-checker** confirme la décision).
       - **IMPORTANT (M3)** : NE PAS écraser `quittancement.steps.ts` (créé 02-02, étendu 02-03, 02-04, 02-05). Le runner Cucumber charge tous les fichiers .steps.ts via `loadPaths: ['tests/bdd/step_definitions/**/*.steps.ts']` (config Phase 1). Donc `relances.steps.ts` coexiste avec `quittancement.steps.ts`.
       - Si conflit de nom de step (Given/When/Then identique) → Cucumber throw `MultipleDefinedSteps`. PRÉFIXER les steps relances par contexte (`relance_je_telecharge_le_PDF`, `relance_le_bouton_niveau_X_est_visible`, etc.) pour éviter les collisions.
       - Réutilisation des steps Given de base (`Given un Bail activé`, `Given un Encaissement de X€`) : ne PAS les redéfinir dans relances.steps.ts. Cucumber les trouvera dans quittancement.steps.ts puisque tous les .steps.ts sont chargés ensemble. Vérifié au boot du runner.

    Tests ÉCHOUENT. Commit : `test(02-06): tests rouges Relance + buildMailto + ENC-05 (Wave 0)`.
  </action>
  <verify>
    <automated>cd /Users/valentinshodo/Projects/toolbox/gestion-locative &amp;&amp; pnpm test 2>&amp;1 | grep -E "FAIL|fail" | head -15 ; ls tests/unit/encaissements/relance.test.ts tests/unit/encaissements/calculer-relance-disponible.test.ts tests/unit/encaissements/enregistrer-relance.test.ts tests/unit/helpers/build-mailto.test.ts tests/integration/repositories/relance-repository-sqlite.test.ts tests/integration/pdf/mise-en-demeure.test.ts tests/bdd/features/relances.feature</automated>
  </verify>
  <done>
    - Tests Wave 0 ROUGES (6 fichiers test + extension builder + BDD feature).
    - Commit créé.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Relance domain + repository + 3 use cases + buildMailto + mise-en-demeure-doc-def + templates EJS (passer unit/integration au vert)</name>
  <read_first>
    - src/domain/encaissements/quittance.ts (analog factory + annuler)
    - src/domain/encaissements/quittance-repository.ts (analog port)
    - src/infrastructure/repositories/quittance-repository-sqlite.ts (analog adapter)
    - src/application/encaissements/generer-quittance.ts (analog use case multi-repos + PDF)
    - src/infrastructure/pdf/quittance-doc-def.ts (analog docDef pdfmake)
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-RESEARCH.md (§Topic 2 buildMailto code complet + §Topic 7 docDocDef mise en demeure structure complète + §Topic 6 Clock injection World)
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-PATTERNS.md (§relance.ts + §enregistrer-relance + §calculer-relance + §relance-repository-sqlite + §templates/relances + §build-mailto + §helpers/render-template)
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-UI-SPEC.md (§Templates EJS Relances 1/2/3 contenu EXACT à reproduire dans les fichiers .ejs)
    - Tests rouges Task 1
  </read_first>
  <action>
    Créer/modifier dans cet ordre :

    1. `migrations/0006_phase2_relance.sql` :
       - `CREATE TABLE IF NOT EXISTS relance (id TEXT PRIMARY KEY, echeance_id TEXT NOT NULL REFERENCES echeance_loyer(id), niveau INTEGER NOT NULL CHECK (niveau IN (1,2,3)), canal TEXT NOT NULL CHECK (canal IN ('email','pdf')), envoyee_le TEXT NOT NULL, contenu_snapshot TEXT NOT NULL, annule_le DATETIME NULL, cree_le DATETIME DEFAULT CURRENT_TIMESTAMP);`.
       - `CREATE INDEX idx_relance_echeance_actif ON relance(echeance_id) WHERE annule_le IS NULL;` (accélère calculerRelanceDisponible).

    2. `src/infrastructure/db/kysely-types.ts` : ajouter `relance: RelanceTable`.

    3. `src/domain/encaissements/erreurs.ts` (ÉTENDRE) :
       - `RelanceIntrouvable extends Error`.
       - `RelanceNiveauNonDisponible extends Error` (avec message paramétrique sur niveau demandé + niveau disponible).

    4. `src/domain/encaissements/relance.ts` (NOUVEAU) :
       - `export type NiveauRelance = 1 | 2 | 3`.
       - `export type CanalRelance = 'email' | 'pdf'`.
       - `interface RelanceProps { id?: RelanceId; echeanceId: EcheanceLoyerId; niveau: NiveauRelance; canal: CanalRelance; envoyeeLe: Temporal.PlainDate; contenuSnapshot: string; annuleLe?: Temporal.PlainDate | null }`.
       - Classe avec props readonly + factory `creer` :
         - Valide niveau ∈ [1,2,3] (sinon InvariantViolated `Niveau de relance invalide : ${niveau}. Valeurs : 1, 2, 3`).
         - Valide canal ∈ ['email','pdf'] (sinon InvariantViolated).
         - id défaut nouveauRelanceId.
         - annuleLe défaut null.
       - Méthode `annuler(annuleLe: Temporal.PlainDate): Relance` (copy-on-write, throw si déjà annulée).

    5. `src/domain/encaissements/relance-repository.ts` (NOUVEAU) :
       - Interface `RelanceRepository { enregistrer(r): Promise<void>; trouverParId(id): Promise<Relance | null>; listerParEcheance(echeanceId, opts?: {inclureAnnulees?: boolean}): Promise<Relance[]>; listerToutes(opts?: {inclureAnnulees?: boolean}): Promise<Relance[]> }`.

    6. `src/infrastructure/repositories/relance-repository-sqlite.ts` (NOUVEAU) :
       - Constructor (db). Pattern enregistrer + listerParEcheance + listerToutes + versDomaine identique aux autres repos.

    7. `src/helpers/build-mailto.ts` (NOUVEAU) :
       - `export interface MailtoParams { to: string; subject: string; body: string; cc?: string }`.
       - `export function buildMailto(params: MailtoParams): string` :
         - `const encoderCorps = (body: string): string => encodeURIComponent(body).replaceAll('%0A', '%0D%0A');`.
         - subject = encodeURIComponent(params.subject).
         - cc = params.cc ? `&cc=${encodeURIComponent(params.cc)}` : ''.
         - bodyEncode = encoderCorps(params.body).
         - LIMITE_CORPS = 1900. Si bodyEncode.length > LIMITE → tronquer + mention (RESEARCH §Topic 2).
         - Retour `mailto:${to}?subject=${subject}${cc}&body=${bodyEncode}`.

    8. **M4 plan-checker — Refactor en port hexagonal** : remplacer le helper monolithique par un port domaine + adapter infrastructure (cohérent DDD strict, prévient l'erreur `fs.readFileSync` dans application/).

       a. `src/domain/encaissements/template-renderer.ts` (NOUVEAU — port domaine, AUCUN import fs/ejs) :
       ```
       import type { NiveauRelance } from './relance.js';

       export interface VariablesRelance {
         prenom_locataire: string;
         nom_locataire: string;
         adresse_locataire: string;
         periode_impayee: string;
         montant_du: string;
         date_echeance_initiale: string;
         nom_bailleur: string;
         adresse_bailleur: string;
       }

       export interface TemplateRenderer {
         rendre(niveau: NiveauRelance, variables: VariablesRelance): string;
       }

       /** Helpers purs — pas d'I/O. Parsent un contenu rendu déjà retourné par `rendre`. */
       export function extraireSujet(rendu: string): string {
         return rendu.split('\n')[0].replace('Objet : ', '').trim();
       }
       export function extraireCorps(rendu: string): string {
         return rendu.split('\n').slice(2).join('\n').trim();
       }
       ```

       b. `src/infrastructure/templates/template-renderer-ejs.ts` (NOUVEAU — adapter, lit fs + ejs.render) :
       ```
       import fs from 'node:fs';
       import path from 'node:path';
       import ejs from 'ejs';
       import type { TemplateRenderer, VariablesRelance } from '../../domain/encaissements/template-renderer.js';
       import type { NiveauRelance } from '../../domain/encaissements/relance.js';

       export class TemplateRendererEjs implements TemplateRenderer {
         constructor(private readonly templatesDir: string) {}

         rendre(niveau: NiveauRelance, variables: VariablesRelance): string {
           const nomFichier = niveau === 1 ? '01-amiable.ejs' : niveau === 2 ? '02-ferme.ejs' : '03-mise-en-demeure.ejs';
           const templatePath = path.join(this.templatesDir, nomFichier);
           const contenuRaw = fs.readFileSync(templatePath, 'utf8');
           return ejs.render(contenuRaw, variables);
         }
       }
       ```
       - `templatesDir` injecté via main.ts : `path.join(import.meta.dirname, '..', '..', '..', 'templates', 'relances')` (résolu absolu pour éviter dépendance au CWD).
       - L'adapter peut être substitué dans les tests par un stub `class TemplateRendererStub implements TemplateRenderer { constructor(private readonly contenuFixe: string) {} rendre() { return this.contenuFixe; } }` — utile pour les tests unit de `enregistrerRelance` qui ne veulent pas dépendre du filesystem.

       c. Le use case `enregistrerRelance` (étape 12 ci-dessous) injecte `templateRenderer: TemplateRenderer` au lieu d'appeler le helper directement. Application/ n'importe plus `fs` ni `ejs` — vérifié par `pnpm lint:deps` (dependency-cruiser doit rejeter tout import `fs`/`ejs` depuis `src/application/`).

       d. **Vérification dependency-cruiser** (rule strict) : ajouter dans `.dependency-cruiser.cjs` (ou équivalent existant Phase 1) une règle :
       ```
       {
         name: 'no-fs-in-application',
         severity: 'error',
         from: { path: '^src/application/' },
         to: { path: '^(fs|node:fs|ejs)$' },
       }
       ```
       Cette règle prévient toute régression future. Si déjà présente Phase 1, vérifier qu'elle couvre `application/` et pas seulement `domain/`.

    9. Créer les fichiers templates :
       - `templates/relances/01-amiable.ejs` : contenu EXACT UI-SPEC §Templates EJS Relances `01-amiable.ejs`.
       - `templates/relances/02-ferme.ejs` : contenu EXACT UI-SPEC `02-ferme.ejs`.
       - `templates/relances/03-mise-en-demeure.ejs` : pour le PDF, le template fournit le corps textuel inséré dans le PDF docDef. Contenu : version textuelle structurée des paragraphes (corps juridique + clôture). Le titre, le tableau impayés et la mise en page sont gérés directement par pdfmake docDef (mise-en-demeure-doc-def.ts).

    10. `src/infrastructure/pdf/mise-en-demeure-doc-def.ts` (NOUVEAU) :
        - `export function construireMiseEnDemeure(echeance, encaissementsLies: Encaissement[], bailleur, locataire, bien, adresseBien, bail, montantTotalDu: Money, today: Temporal.PlainDate): TDocumentDefinitions`.
        - Structure conforme RESEARCH §Topic 7 + UI-SPEC §PDF Layout "Mise en demeure" :
          - Bailleur en haut à gauche, locataire à droite.
          - Titre centré "MISE EN DEMEURE DE PAYER" fontSize 14 bold.
          - Référence bail (date début, adresse bien, durée, loyer).
          - Corps juridique (8 jours, voies de droit — sans mention contentieuse hors-scope).
          - Tableau détail impayé (période, montant dû, total bold).
          - Clôture (lieu, date, signature).
          - Footer "Ce courrier doit être envoyé en lettre recommandée avec accusé de réception (AR)." italique.

    11. `src/application/encaissements/calculer-relance-disponible.ts` (NOUVEAU) :
        - `export const SEUILS_RELANCE_JOURS: Record<NiveauRelance, number> = { 1: 10, 2: 30, 3: 60 };`.
        - `export function calculerRelanceDisponible(echeance: EcheanceLoyer, relancesActives: Relance[], today: Temporal.PlainDate): NiveauRelance | null` — fonction PURE :
          - Si echeance.statut === 'payee' ou 'annulee' → return null.
          - actives = relancesActives.filter(r => r.annuleLe === null).sort((a,b) => a.niveau - b.niveau).
          - dernierNiveau = actives.length === 0 ? 0 : actives[actives.length-1].niveau.
          - if (dernierNiveau === 3) return null.
          - niveauCandidat = (dernierNiveau + 1) as NiveauRelance.
          - seuil = echeance.jourEcheanceAttendue.add({ days: SEUILS_RELANCE_JOURS[niveauCandidat] }).
          - return Temporal.PlainDate.compare(today, seuil) >= 0 ? niveauCandidat : null.

    12. `src/application/encaissements/enregistrer-relance.ts` (NOUVEAU) :
        - Signature : `enregistrerRelance(commande: { echeanceId, niveau }, repos, templateRenderer: TemplateRenderer, pdfRenderer, clock): Promise<{ relanceId, canal, mailtoUri?: string, pdfBuffer?: Buffer }>` (M4 — injection port TemplateRenderer).
        - lookup echeance + bail + locataire + bailleur (bailleur peut être null → BailleurAbsent — surtout requis pour le PDF qui contient l'expéditeur ; pour mailto, le `to` est locataire.email, mais le contenu du body référence "nom_bailleur" donc bailleur null = throw BailleurAbsent même pour niveau 1).
        - relancesExistantes = await relanceRepo.listerParEcheance(commande.echeanceId).
        - niveauDispo = calculerRelanceDisponible(echeance, relancesExistantes, clock.aujourdhui()).
        - Si niveauDispo !== commande.niveau → throw RelanceNiveauNonDisponible(`Le niveau ${commande.niveau} n'est pas disponible (chaînage strict ou seuil non atteint). Disponible : ${niveauDispo ?? 'aucun'}.`).
        - sommePaiee = encaissementRepo.sommePaieeParEcheance(echeance.id).
        - resteDu = echeance.total.soustraire(sommePaiee).
        - variables : { prenom_locataire: locataire.prenom, nom_locataire: locataire.nom, adresse_locataire: `${locataire.adresse.rue}, ${locataire.adresse.codePostal} ${locataire.adresse.ville}`, periode_impayee: formatPeriode(echeance.periodeDebut), montant_du: resteDu.enEuros(), date_echeance_initiale: formatDate(echeance.jourEcheanceAttendue), nom_bailleur: bailleur.nomComplet, adresse_bailleur: `${bailleur.adresse.rue}, ${bailleur.adresse.codePostal} ${bailleur.adresse.ville}` }.
        - contenuRendu = templateRenderer.rendre(commande.niveau, variables). **(M4 plan-checker : injection du port `TemplateRenderer` au use case ; plus aucun import `fs` ni `ejs` depuis `src/application/encaissements/enregistrer-relance.ts`.)**
        - canal = commande.niveau === 3 ? 'pdf' : 'email'.
        - **AVANT Relance.creer** : si canal === 'email', calculer `mailtoUri = buildMailto({ to: locataire.email, subject: extraireSujet(contenuRendu), body: extraireCorps(contenuRendu) })`. Si canal === 'pdf', `mailtoUri = null`.
        - relance = Relance.creer({ echeanceId: echeance.id, niveau: commande.niveau, canal, envoyeeLe: clock.aujourdhui(), contenuSnapshot: JSON.stringify({ variables, contenuRendu, mailtoUri, version: 'v1' }) }).
        - await relanceRepo.enregistrer(relance).
        - Si canal === 'email' :
          - Retourner `{ relanceId: relance.id, canal: 'email', mailtoUri }` (mailtoUri déjà calculé et persisté dans le snapshot — il sera également relu par GET /relances/:id pour stabilité).
        - Si canal === 'pdf' :
          - Lookup bien (pour adresseBien dans PDF). encaissementsLies = encaissementRepo.listerParEcheance(echeance.id) (filtré actifs).
          - docDef = construireMiseEnDemeure(echeance, encaissementsLies, bailleur, locataire, bien, bien.adresse, bail, resteDu, clock.aujourdhui()).
          - pdfBuffer = await pdfRenderer.genererBuffer(docDef).
          - Retourner `{ relanceId: relance.id, canal: 'pdf', pdfBuffer }`.

    13. `src/application/encaissements/lister-relances.ts` (NOUVEAU) : wrapper `listerRelances({ inclureAnnulees }, relanceRepo): Promise<Relance[]>` pour la page liste.

    Vérifs : Tests unit + integration Wave 0 VERTS (sauf BDD qui dépendent du web Task 3). `pnpm tsc --noEmit` 0 erreur. `pnpm lint:deps` 0 violation (templates EJS dans `templates/` hors `src/` — domain pur).

    Commit : `feat(02-06): relance domain + use cases + buildMailto + templates EJS + mise-en-demeure PDF`.
  </action>
  <verify>
    <automated>cd /Users/valentinshodo/Projects/toolbox/gestion-locative &amp;&amp; pnpm tsc --noEmit &amp;&amp; pnpm lint:deps &amp;&amp; pnpm test:unit run tests/unit/encaissements/relance.test.ts tests/unit/encaissements/calculer-relance-disponible.test.ts tests/unit/encaissements/enregistrer-relance.test.ts tests/unit/helpers/build-mailto.test.ts &amp;&amp; pnpm test:integration run tests/integration/repositories/relance-repository-sqlite.test.ts tests/integration/pdf/mise-en-demeure.test.ts</automated>
  </verify>
  <done>
    - Relance agrégat + repository + 3 use cases (calculer/enregistrer/lister).
    - buildMailto helper + port `TemplateRenderer` (domain) + adapter `TemplateRendererEjs` (infrastructure) — architecture hexagonale stricte (M4 plan-checker).
    - Templates EJS 01/02/03 conformes UI-SPEC.
    - mise-en-demeure-doc-def.ts conforme art. 1344 Code civil.
    - Migration 0006 + Kysely types étendus.
    - Tests unit + integration VERTS.
    - Commit créé.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Routes /relances + view liste + relance-action partial + extension page Impayés (suggestion contextuelle) + BDD verts</name>
  <read_first>
    - src/web/routes/quittances.ts (analog routes avec PDF download response)
    - src/web/routes/encaissements.ts (analog plugin)
    - src/web/views/pages/quittances/liste.ejs (analog data-table)
    - src/web/views/pages/impayes/liste.ejs (créé 02-05 — à étendre)
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-UI-SPEC.md (§Bouton conditionnel relance + §Téléchargement PDF mise en demeure + §Copywriting "Lancer la relance amiable/ferme" + "Télécharger la mise en demeure PDF" + §Empty States "Aucune relance envoyée" + §Layout Shell sidebar)
    - .planning/phases/02-quittancement-ch-ances-encaissements-relances/02-PATTERNS.md (§routes/relances + §relance-schemas + §relance-action.ejs)
    - Tests rouges BDD Task 1
  </read_first>
  <action>
    Créer/modifier :

    1. `src/web/schemas/relance-schemas.ts` (NOUVEAU) :
       - `enregistrerRelanceFormSchema = z.object({ echeanceId: z.string().uuid('Échéance invalide'), niveau: z.coerce.number().int().min(1).max(3) })`.

    2. `src/web/routes/relances.ts` (NOUVEAU) :
       - `export async function plugin(app, opts: { relanceRepo, echeanceLoyerRepo, encaissementRepo, bailRepo, locataireRepo, bienRepo, bailleurRepo, pdfRenderer, clock }): Promise<void>`.
       - `GET /relances` : `const relances = await opts.relanceRepo.listerToutes({inclureAnnulees: true})`. Pour chaque relance, enrichir avec échéance + locataire (lookups). reply.view('pages/relances/liste.ejs', { relances, navActive: 'relances', banniereSuccess, banniereWarning }).
       - `POST /relances` : safeParse(enregistrerRelanceFormSchema). Si !success → redirect /impayes avec banniereWarning. Si success :
         - try `const result = await enregistrerRelance({echeanceId, niveau}, { relanceRepo, echeanceLoyerRepo, encaissementRepo, bailRepo, locataireRepo, bienRepo, bailleurRepo }, pdfRenderer, clock)`.
         - Si result.canal === 'email' : set session banniereSuccess = `Relance niveau ${niveau} enregistrée.` + session.mailtoUri = result.mailtoUri (pour affichage post-redirect ; alternative : passer en query param). DÉCISION PLANNER : redirect /relances/:id avec banniereSuccess et la mailtoUri rendue dans la fiche (page dédiée pour cliquer "Envoyer la relance" qui ouvre le client mail).
         - Si result.canal === 'pdf' : reply.header Content-Type application/pdf + Content-Disposition attachment filename `mise-en-demeure-${formatPeriode}.pdf` + send(result.pdfBuffer). Pas de redirect — réponse PDF directe.
         - catch RelanceNiveauNonDisponible → set session banniereWarning + redirect /impayes.
         - catch BailleurAbsent → redirect /bailleur avec banniereWarning.
       - `GET /relances/:id` : lookup relance + echeance + locataire. Si canal === 'email', extraire mailtoUri du contenuSnapshot OU regénérer à la volée via buildMailto (le contenuSnapshot contient le contenuRendu — peut être re-extracté). DÉCISION PLANNER : stocker mailtoUri dans contenuSnapshot lors de l'enregistrement pour stabilité. reply.view('pages/relances/fiche.ejs', { relance, echeance, locataire, mailtoUri }).

    3. `src/web/views/pages/relances/liste.ejs` (NOUVEAU) :
       - layout-debut + bannières + data-table colonnes [Date envoyée, Locataire, Période, Niveau, Canal, Statut].
       - Niveau : badge "Amiable" (vert clair) / "Ferme" (ambre) / "Mise en demeure" (rouge).
       - Canal : icône email ou PDF + texte.
       - Statut : actif / annulé.
       - Empty state si aucune relance (UI-SPEC §Empty States "Aucune relance envoyée").
       - Action par ligne : "Voir" → /relances/:id.
       - layout-fin.

    4. `src/web/views/pages/relances/fiche.ejs` (NOUVEAU) :
       - layout-debut.
       - Détails relance (niveau, canal, date envoyée, locataire, échéance).
       - Si canal email : `<a class="button-primary" href="<%= mailtoUri %>">Ouvrir le brouillon de mail</a>` (UI-SPEC §Téléchargement PDF — pour mailto, `<a>` semantic). `mailtoUri` est lu directement depuis `contenuSnapshot.mailtoUri` (stocké à l'enregistrement). Affiche aussi le contenu textuel rendu pour information.
       - Si canal pdf (niveau 3 uniquement) : `<a class="button-primary" href="/relances/<%= relance.id %>/pdf" download>Télécharger la mise en demeure (PDF)</a>`. Lien vers la route GET /relances/:id/pdf qui regénère le buffer à la volée depuis `contenuSnapshot` (cohérent D-66 esprit on-the-fly + audit : `contenuSnapshot.variables` reproduit le contenu à l'identique).
       - **DÉCISION PLANNER V1 RÉSOLUE (H4 plan-checker) :** PDF mise en demeure regénéré ON-THE-FLY depuis `contenuSnapshot.variables` à chaque GET /relances/:id/pdf. Pas de persistance binaire (cohérent D-66 avis d'échéance). Si une entité référencée (locataire, bien, bailleur) est supprimée entre l'envoi et la regénération → fallback 404 "Document indisponible — une entité référencée a été supprimée. Consulte le contenu textuel snapshot dans la fiche.". Robustesse documentée + acceptée (cas rare en V1 mono-user).
       - layout-fin.

    5. `src/web/routes/relances.ts` (compléter) — `GET /relances/:id/pdf` (niveau 3 uniquement) :
       - Lookup `relance = await opts.relanceRepo.trouverParId(id)`. 404 sinon.
       - Si `relance.canal !== 'pdf'` → `reply.code(400).send('Cette relance n\'est pas un PDF (niveau ' + relance.niveau + ', canal ' + relance.canal + ').')`.
       - Parser `JSON.parse(relance.contenuSnapshot) as { variables: VariablesRelance, contenuRendu: string, mailtoUri: string | null, version: 'v1' }`.
       - Lookup minimum : `echeance = echeanceLoyerRepo.trouverParId(relance.echeanceId)`, `bail = bailRepo.trouverParId(echeance.bailId)`, `bailleur = bailleurRepo.trouver()`, `locataire = locataireRepo.trouverParId(bail.locataireId)`, `bien = bienRepo.trouverParId(bail.bienId)`.
       - **Robustesse** : si l'un de ces lookups retourne null (entité supprimée entre l'envoi et la regénération) → `reply.code(404).send('Document indisponible — une entité référencée a été supprimée. Consulte le contenu textuel snapshot dans la fiche (/relances/' + id + ').')`. Logguer un warning serveur (`req.log.warn`).
       - Si tous les lookups OK : `encaissementsLies = await encaissementRepo.listerParEcheance(echeance.id)`, `sommePaiee = encaissementRepo.sommePaieeParEcheance(echeance.id)`, `resteDu = echeance.total.soustraire(sommePaiee)`.
       - `docDef = construireMiseEnDemeure(echeance, encaissementsLies, bailleur, locataire, bien, bien.adresse, bail, resteDu, clock.aujourdhui())`.
       - `buffer = await opts.pdfRenderer.genererBuffer(docDef)`.
       - `reply.header('Content-Type', 'application/pdf').header('Content-Disposition', \`attachment; filename="mise-en-demeure-${formatPeriode(echeance.periodeDebut).replace(/\\s/g,'-').toLowerCase()}.pdf"\`).send(buffer)`.
       - **IMPORTANT** : aucune nouvelle Relance n'est créée (relance.envoyeeLe inchangée, pas de compteur incrémenté). Cohérent T-D69-1 (D-69 — l'utilisateur peut imprimer plusieurs fois sans biaiser l'historique des envois).

    6. `src/web/views/partials/relance-action.ejs` (NOUVEAU) :
       - Pattern UI-SPEC §Component Patterns "Bouton conditionnel relance" :
         - `<% if (locals.niveauDisponible) { %><form method="POST" action="/relances"><input type="hidden" name="echeanceId" value="<%= echeanceId %>" /><input type="hidden" name="niveau" value="<%= niveauDisponible %>" /><button type="submit" class="relance-btn relance-btn--niveau<%= niveauDisponible %>" aria-describedby="relance-hint-<%= echeanceId %>"><%= niveauDisponible === 1 ? "Lancer la relance amiable" : niveauDisponible === 2 ? "Lancer la relance ferme" : "Télécharger la mise en demeure PDF" %></button><span id="relance-hint-<%= echeanceId %>" class="sr-only"><%= niveauDisponible === 1 ? "Ouvre un brouillon email pour relance amiable" : niveauDisponible === 2 ? "Ouvre un brouillon email pour relance ferme" : "Télécharge un PDF formel à envoyer par lettre recommandée AR" %></span></form><% } %>`.

    7. ÉTENDRE `src/web/views/pages/impayes/liste.ejs` (créé 02-05) :
       - Avant le rendu de la table, pour chaque impayé, calculer niveauDisponible. Approche : enrichir le viewModel côté route en faisant `await Promise.all(impayes.map(async (i) => { const relances = await relanceRepo.listerParEcheance(i.echeanceId); return { ...i, niveauRelanceDisponible: calculerRelanceDisponible(echeanceComplete, relances, clock.aujourdhui()) }; }))`. Ajouter une colonne "Relance" dans la table contenant `<%- include('../../partials/relance-action', { echeanceId: i.echeanceId, niveauDisponible: i.niveauRelanceDisponible }) %>`.
       - Pour récupérer `echeanceComplete` (Impaye DTO n'a pas le statut complet ni le jour_echeance_attendue dans la forme initiale 02-05 — vérifier le DTO : il les a tous, donc on peut le passer directement à calculerRelanceDisponible si on adapte la signature pour accepter `{ statut, jourEcheanceAttendue }` au lieu d'un EcheanceLoyer complet). DÉCISION PLANNER : refactorer calculerRelanceDisponible pour accepter `{ statut: StatutEcheanceLoyer; jourEcheanceAttendue: Temporal.PlainDate }` au lieu de l'agrégat EcheanceLoyer complet — fonction reste pure, signature plus flexible.

    8. ÉTENDRE `src/web/routes/impayes.ts` (créé 02-05) : injection des dépendances supplémentaires (relanceRepo). Calcul enrichi niveauDisponible pour chaque ligne. Passer dans le viewModel.

    9. ÉTENDRE `src/web/views/partials/sidebar-nav.ejs` : ajouter sous-lien "Relances" → /relances dans `<details>` Encaissements.

    10. CSS `public/app.css` (extension) : classes `.relance-btn` + `.relance-btn--niveau1/2/3` selon UI-SPEC §Spacing Scale (cibles tactiles 44px min via padding) + couleurs UI-SPEC §Color.

    11. ÉTENDRE `src/main.ts` :
        - Imports : `RelanceRepositorySqlite`, `TemplateRendererEjs`, `plugin as relancesPlugin`.
        - Instancier `const relanceRepo = new RelanceRepositorySqlite(db)`.
        - Instancier `const templateRenderer = new TemplateRendererEjs(path.join(import.meta.dirname, '..', 'templates', 'relances'))` (résolu absolu — `import.meta.dirname` ESM Node 22 LTS).
        - Registrer `relancesPlugin` avec opts complets (incluant `templateRenderer`).
        - Passer `relanceRepo` aux opts du `impayesPlugin` (extension 02-05 → 02-06).

    Sécurité (cf. <threat_model>) :
    - buildMailto : encodeURIComponent strict (prévient injection headers email).
    - Pas de log mailto URI (info disclosure si copié dans shell).
    - Templates EJS chargés depuis path FIXE (templates/relances/*.ejs) — pas d'input user.
    - PDF mise en demeure : pdfmake setUrlAccessPolicy(false) (hérité 02-02).
    - Zod strict niveau ∈ [1,3].

    Vérifs : `pnpm test:bdd -- --tags @enc-05` 6 scenarios VERTS. `pnpm test` tout VERT (incluant non-régression 02-01..02-05). `pnpm tsc --noEmit` 0 erreur. `pnpm lint` 0 warning.

    Commit : `feat(02-06): relances 3 niveaux + mailto + mise en demeure PDF + suggestion contextuelle (ENC-05)`.
  </action>
  <verify>
    <automated>cd /Users/valentinshodo/Projects/toolbox/gestion-locative &amp;&amp; pnpm tsc --noEmit &amp;&amp; pnpm lint &amp;&amp; pnpm lint:deps &amp;&amp; pnpm test &amp;&amp; pnpm test:bdd</automated>
  </verify>
  <done>
    - Routes /relances + /relances/:id + /relances/:id/pdf + POST /relances (canal email mailto + canal pdf buffer).
    - Views liste + fiche.
    - Partial relance-action.ejs.
    - Extension page Impayés avec colonne Relance contextuelle.
    - Sidebar étendue.
    - 6 scenarios BDD `@enc-05` VERTS.
    - TOUS scenarios BDD Phase 2 (@bailleur + @enc-01 + @enc-02 + @enc-03 + @enc-04 + @enc-05) VERTS.
    - Tous tests Phase 1 toujours verts (non-régression).
    - Commit créé.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigateur user → Fastify POST /relances | echeanceId + niveau — validés Zod + use case |
| Fastify → templates/relances/*.ejs | Lecture fichier statique — path FIXE, pas d'input user |
| Fastify → URI mailto (response HTML) | URI rendu via `<a href>` — encodeURIComponent strict |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06-01 | Spoofing — injection headers email via body | utilisateur saisit `nomLocataire = "x@y.com\nBCC: attacker@z.com"` qui finit dans le body mailto | MED | mitigate | `encodeURIComponent` prévient l'injection — les caractères `\n`, `\r`, `:`, `,` sont tous encodés. Body mailto ne peut pas injecter de headers (la frontière est strictement `?subject=&body=`). |
| T-02-06-02 | Information disclosure — mailto URI loggué | logs Fastify peuvent inclure les URL avec query params (mailto contient toutes les infos du locataire) | MED | mitigate | Configurer Fastify logger pour exclure les query strings sensibles (déjà silent par défaut Phase 1 D-26). NE PAS logger le mailto URI dans le `Set-Cookie` ni dans le redirect (utiliser session.banniereSuccess générique + fiche relance pour le mailto). |
| T-02-06-03 | Tampering — niveau hors [1,3] / saut chaînage | POST /relances niveau=5 ou tentative niveau=3 sans 2 | LOW | mitigate | Zod `z.coerce.number().int().min(1).max(3)` + use case `enregistrerRelance` vérifie via `calculerRelanceDisponible` + CHECK constraint SQLite niveau IN (1,2,3) (triple barrière). |
| T-02-06-04 | DoS — body mailto > 2046 chars (Windows truncate silently) | locataire avec nom très long + adresse → URI > 2000 chars | LOW | mitigate | `buildMailto` tronque body à 1900 chars encodés + mention `[Message tronqué...]` (RESEARCH §Topic 2). Tests T20 valide. |
| T-02-06-05 | Repudiation — relance falsifiée a posteriori | utilisateur modifie Relance.contenuSnapshot manuellement | LOW | accept | Mono-user local. Le contenuSnapshot est en TEXT en DB — l'utilisateur a accès via SQL. La rétention 10 ans (Phase 4) + backup (BAK-01 V2) sont les garde-fous futurs. |
| T-02-06-06 | Integrity — template EJS mal formé | `templates/relances/01-amiable.ejs` corrompu / incomplet | MED | mitigate | Templates committées avec le code (CLAUDE.md "Docs commitées"). Tests intégration vérifient le rendu correct (T18-T20 + T22). Pas d'override utilisateur V1 (D-70 — reportés V1.1). |
| T-02-06-07 | Information disclosure — locataire reçoit son adresse personnelle dans le mailto | adresse_locataire visible dans body | LOW | accept | C'est le but : le locataire VOIT son propre adresse dans la relance (mentions juridiques pour identifier le destinataire). |
</threat_model>

<verification>
- `pnpm tsc --noEmit` exit 0
- `pnpm lint` 0 warning
- `pnpm lint:deps` 0 violation (domain pur ; templates et infrastructure isolés)
- `pnpm test` tout VERT (incluant 100 % couverture calculerRelanceDisponible + chaînage strict)
- `pnpm test:bdd` 6 scenarios @enc-05 + non-régression @bailleur + @enc-01 + @enc-02 + @enc-03 + @enc-04 = ALL PASSED
- Migration 0006 idempotente
- Templates EJS rendus correctement (test integration buildMailto + render-template)
- PDF mise en demeure contient mentions essentielles (test T22)
</verification>

<success_criteria>
- ENC-05 satisfait : 3 niveaux escaladés + chaînage strict + canal hybride + templates EJS.
- D-68 satisfait : seuils J+10/J+30/J+60 dans constante de domaine.
- D-69 satisfait : mailto niveaux 1-2 + PDF niveau 3.
- D-70 satisfait : templates EJS dans `templates/relances/` (hors `views/`).
- D-71 satisfait : suggestion contextuelle + chaînage strict (impossible de sauter au niveau 3).
- buildMailto conforme RFC 6068 (encodeURIComponent + %0D%0A + tronquage 1900).
- Mentions Code civil art. 1344 + pratique pré-judiciaire française présentes dans PDF mise en demeure (vérifié buffer assertion).
- Domain pur (Relance n'importe rien de technique ; ejs reste en helpers/).
- 100 % couverture calculerRelanceDisponible (règle métier critique → critère projet).
- Ubiquitous language français (Relance, NiveauRelance, CanalRelance, envoyeeLe, contenuSnapshot, miseEnDemeure, templateRenderer).
- M4 plan-checker : port `TemplateRenderer` extrait dans `src/domain/encaissements/` ; adapter `TemplateRendererEjs` isolé dans `src/infrastructure/templates/` ; application/ n'importe ni `fs` ni `ejs` (vérifié par `pnpm lint:deps`).
</success_criteria>

<output>
After completion, create `.planning/phases/02-quittancement-ch-ances-encaissements-relances/02-06-SUMMARY.md` listant :
- 3 commits (tests rouges / domain+app+templates / web+BDD)
- Patterns établis : fonction pure `calculerRelanceDisponible`, canal hybride mailto/PDF, templates EJS hors views/, buildMailto RFC 6068, snapshot contenu pour audit
- Surprises éventuelles
- Phase 2 COMPLETE : tous les ENC-01..ENC-05 livrés. Préparer la transition vers Phase 3.

Note finale : à la fin de ce plan, le checkpoint humain peut être inséré pour acceptation finale Phase 2 (sanity check manual via `pnpm dev` → wizard d'activation complet → liste impayés → relance amiable → mise en demeure PDF).
</output>
