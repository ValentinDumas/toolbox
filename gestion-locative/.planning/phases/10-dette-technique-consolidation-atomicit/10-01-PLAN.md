---
phase: 10-dette-technique-consolidation-atomicit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/web/views/partials/partial-bandeau-cfe-echeance.ejs
  - src/web/views/partials/partial-bandeau-alerte.ejs
  - src/web/views/partials/_bandeau-cfe-corps.ejs
  - src/web/views/pages/fiscalite/index.ejs
  - src/web/views/pages/biens/detail.ejs
  - tests/unit/views/bandeau-cfe-consolidation.test.ts
autonomous: true
requirements: [DET-01]
must_haves:
  truths:
    - "Le markup CFE partagé existe dans UN seul partiel paramétré, inclus par les deux partiels appelants."
    - "Le bandeau CFE sur /fiscalite et /biens/detail rend SANS icône (byte-identique à l'avant-refactor)."
    - "Le bandeau polymorphe sur /baux/indexations et /dashboard rend AVEC icône + ARIA (byte-identique à l'avant-refactor)."
    - "Aucune icône ne fuit sur le bandeau CFE."
  artifacts:
    - path: "src/web/views/partials/_bandeau-cfe-corps.ejs"
      provides: "Partiel CFE réutilisable paramétré (flag d'icône)"
    - path: "tests/unit/views/bandeau-cfe-consolidation.test.ts"
      provides: "Snapshot de rendu EJS avant/après (D-10-06) sur les 2 surfaces"
  key_links:
    - from: "src/web/views/partials/partial-bandeau-cfe-echeance.ejs"
      to: "src/web/views/partials/_bandeau-cfe-corps.ejs"
      via: "include EJS avec flag icône=false"
      pattern: "include.*_bandeau-cfe-corps"
---

<objective>
DET-01 — Consolider les 2 partiels CFE en double en UN seul partiel réutilisable paramétré, sans aucune régression d'affichage (critère #1 « affichage identique avant/après »).

Purpose: Éponger la duplication du markup CFE tout en préservant strictement les deux rendus distincts (bandeau CFE sans icône / bandeau polymorphe avec icône + ARIA).
Output: Un partiel partagé `_bandeau-cfe-corps.ejs`, les deux partiels appelants rebranchés dessus, et un test de snapshot de rendu prouvant l'égalité byte-à-byte avant/après sur les 3 surfaces impactées.
</objective>

<execution_context>
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/workflows/execute-plan.md
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dette-technique-consolidation-atomicit/10-CONTEXT.md

<interfaces>
<!-- Données réellement consommées par chaque partiel (contrats divergents) -->
partial-bandeau-cfe-echeance.ejs (SANS icône) lit :
  alerte.joursRestants, alerte.millesime, alerte.dateEcheancePaiement,
  alerte.bienId, alerte.declarationCfeId
  helper requis : formatDate

partial-bandeau-alerte.ejs (AVEC icône + ARIA) lit :
  alerte.joursRestants, alerte.type, alerte.dateEcheance, alerte.urlAction, locals.inline
  helpers requis : formatDate, formaterAlerteUrgence, iconeTypeAlerte, libelleTypeAlerte

Surfaces d'inclusion (à ne pas régresser) :
  - pages/fiscalite/index.ejs:48  → partial-bandeau-cfe-echeance (sans icône)
  - pages/biens/detail.ejs:288    → partial-bandeau-cfe-echeance (sans icône)
  - pages/baux/indexations.ejs:34 → partial-bandeau-alerte (inline:true, avec icône)
  - pages/dashboard/accueil.ejs:29 → partial-bandeau-alerte (avec icône)

Helper de rendu EJS existant à réutiliser (D-10-06) :
  tests/unit/views/empty-state.test.ts → ejs.renderFile(PATH, locals, { async: true })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capturer le snapshot de rendu EJS AVANT refactor (D-10-06)</name>
  <files>tests/unit/views/bandeau-cfe-consolidation.test.ts</files>
  <read_first>
    - src/web/views/partials/partial-bandeau-cfe-echeance.ejs (rendu CFE actuel, sans icône)
    - src/web/views/partials/partial-bandeau-alerte.ejs (rendu polymorphe actuel, avec icône)
    - tests/unit/views/empty-state.test.ts (pattern ejs.renderFile à réutiliser)
  </read_first>
  <action>
    Créer tests/unit/views/bandeau-cfe-consolidation.test.ts. Réutiliser le pattern
    ejs.renderFile(PARTIAL_PATH, locals, { async: true }) de empty-state.test.ts.
    Rendre les DEUX partiels actuels avec des locals représentatifs couvrant les 3 variantes
    joursRestants (j<0 destructive, j entre 1 et 7 warning-fort, j>=8 warning) :
    pour partial-bandeau-cfe-echeance fournir alerte={joursRestants, millesime, dateEcheancePaiement,
    bienId, declarationCfeId} + helper formatDate ; pour partial-bandeau-alerte fournir
    alerte={joursRestants, type:'irl', dateEcheance, urlAction} + locals.inline=true + helpers
    formatDate/formaterAlerteUrgence/iconeTypeAlerte/libelleTypeAlerte (stubs déterministes).
    Asserter le HTML rendu via toMatchInlineSnapshot (ou égalité de chaîne figée). Ce snapshot est
    le contrat de non-régression : il NE doit PAS changer après le refactor des tâches 2-3.
    Asserter explicitement que le rendu CFE NE contient PAS d'icône (pas de span aria-hidden),
    et que le rendu polymorphe CONTIENT bien span aria-hidden + l'icône.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/views/bandeau-cfe-consolidation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run tests/unit/views/bandeau-cfe-consolidation.test.ts` exits 0.
    - Le test rend les 2 partiels sur les 3 variantes joursRestants.
    - Une assertion vérifie que le rendu CFE ne contient ni `aria-hidden="true"` ni span d'icône.
    - Une assertion vérifie que le rendu polymorphe contient `<span aria-hidden="true">`.
    - Les snapshots inline sont figés dans le fichier (committés).
  </acceptance_criteria>
  <done>Snapshots avant-refactor capturés et verts, distinction icône/sans-icône assertée.</done>
</task>

<task type="auto">
  <name>Task 2: Extraire le partiel partagé paramétré _bandeau-cfe-corps.ejs</name>
  <files>src/web/views/partials/_bandeau-cfe-corps.ejs</files>
  <read_first>
    - src/web/views/partials/partial-bandeau-cfe-echeance.ejs (markup à factoriser, sans icône)
    - src/web/views/partials/partial-bandeau-alerte.ejs (markup à factoriser, avec icône)
  </read_first>
  <action>
    Identifier le markup réellement partagé entre les deux partiels : le calcul variant/role/
    aria-live à partir de joursRestants, le styleBase/styleVariant, et la structure aside>p.
    Créer src/web/views/partials/_bandeau-cfe-corps.ejs prenant en props : joursRestants,
    role/aria-live dérivés, ariaLabel, titre (strong), texteEcheance, blocActions (HTML déjà
    composé par l'appelant), et un flag booléen `avecIcone` + `icone` (string). Quand
    `avecIcone` est false (cas CFE), NE PAS émettre le span aria-hidden — préserver le critère #1
    (aucune fuite d'icône). Quand true (cas polymorphe), émettre `<span aria-hidden="true"><%= icone %></span>`.
    Le partiel reste neutre : il ne lit AUCUN champ spécifique CFE ou Alerte, seulement les props
    passées. Ne pas modifier encore les appelants dans cette tâche.
  </action>
  <verify>
    <automated>test -f src/web/views/partials/_bandeau-cfe-corps.ejs && grep -q "avecIcone" src/web/views/partials/_bandeau-cfe-corps.ejs</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier src/web/views/partials/_bandeau-cfe-corps.ejs existe.
    - Il accepte un flag `avecIcone` et n'émet le span `aria-hidden` que si `avecIcone` est vrai.
    - Il ne référence aucun champ propre à un type d'alerte (pas de `alerte.millesime`, pas de `alerte.type`).
  </acceptance_criteria>
  <done>Partiel partagé paramétré créé, neutre, icône conditionnelle au flag.</done>
</task>

<task type="auto">
  <name>Task 3: Rebrancher les 2 partiels appelants sur le partiel partagé et prouver l'égalité</name>
  <files>src/web/views/partials/partial-bandeau-cfe-echeance.ejs, src/web/views/partials/partial-bandeau-alerte.ejs</files>
  <read_first>
    - src/web/views/partials/_bandeau-cfe-corps.ejs (partiel partagé créé en tâche 2)
    - tests/unit/views/bandeau-cfe-consolidation.test.ts (snapshots avant-refactor figés en tâche 1)
  </read_first>
  <action>
    Réécrire partial-bandeau-cfe-echeance.ejs : calculer ses libellés CFE (libelleEtat,
    titre "CFE <millesime> — <libelleEtat>", texteEcheance avec formatDate(dateEcheancePaiement),
    blocActions = les 2 liens impots.gouv.fr + /biens/.../cfe/.../editer), puis
    `include('_bandeau-cfe-corps', { ..., avecIcone: false })`. Réécrire partial-bandeau-alerte.ejs :
    calculer libelleUrgence/libelleType/icone, blocActions selon alerte.type et locals.inline,
    puis `include('_bandeau-cfe-corps', { ..., avecIcone: true, icone })`. AUCUN changement DOM :
    le HTML émis doit rester byte-identique. Relancer le test de snapshot de la tâche 1 sans
    régénérer les snapshots (`--no-update` / pas de `-u`) : il doit rester vert, prouvant l'égalité.
    Vérifier aussi les inclusions inchangées dans fiscalite/index.ejs, biens/detail.ejs,
    baux/indexations.ejs, dashboard/accueil.ejs (mêmes chemins d'include).
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/views/bandeau-cfe-consolidation.test.ts && pnpm vitest run tests/integration</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run tests/unit/views/bandeau-cfe-consolidation.test.ts` exits 0 SANS mise à jour des snapshots (les snapshots figés en tâche 1 matchent toujours).
    - `pnpm vitest run tests/integration` exits 0 (vues CFE et baux non régressées).
    - `grep -c "aside" src/web/views/partials/partial-bandeau-cfe-echeance.ejs` retourne 0 (le `<aside>` vit désormais dans le partiel partagé, plus dans l'appelant).
    - Les deux partiels appelants contiennent `include('../../partials/_bandeau-cfe-corps'` ou chemin relatif équivalent.
  </acceptance_criteria>
  <done>Les 2 appelants délèguent au partiel partagé, snapshot avant/après identique, intégration verte.</done>
</task>

</tasks>

<verification>
- Le `<aside>` du bandeau n'existe plus qu'à un seul endroit (`_bandeau-cfe-corps.ejs`).
- Snapshot EJS identique avant/après sur les 3 surfaces (preuve D-10-06).
- Aucune icône sur le rendu CFE ; icône + ARIA préservés sur le rendu polymorphe.
- `pnpm test:integration` vert.
</verification>

<success_criteria>
Un seul partiel CFE réutilisable, affichage strictement identique avant/après, prouvé par snapshot. (Critère #1)
</success_criteria>

<output>
After completion, create `.planning/phases/10-dette-technique-consolidation-atomicit/10-01-SUMMARY.md`
</output>
