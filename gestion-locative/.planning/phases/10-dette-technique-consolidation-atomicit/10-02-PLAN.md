---
phase: 10-dette-technique-consolidation-atomicit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/web/routes/baux.ts
  - src/web/views/pages/baux/indexations.ejs
autonomous: true
requirements: [DET-02]
must_haves:
  truths:
    - "routes/baux.ts construit la Map<BailId,string> des noms de locataires et la passe en 5e argument à calculerAlertesIrl."
    - "La boucle d'enrichissement post-hoc et le dict locatairesParBail sont supprimés."
    - "La vue baux/indexations lit alerte.source.extra.nomLocataire (chemin unique, comme le dashboard)."
    - "Les noms affichés et le rendu de /baux/indexations restent observablement identiques."
  artifacts:
    - path: "src/web/routes/baux.ts"
      provides: "Appel unifié calculerAlertesIrl avec nomLocataireParBail en 5e arg"
      contains: "calculerAlertesIrl(baux, biens, indexationsParBail, maintenant, nomLocataireParBail)"
  key_links:
    - from: "src/web/routes/baux.ts"
      to: "src/domain/locatif/alerte-irl.ts"
      via: "5e argument Map<BailId,string>"
      pattern: "calculerAlertesIrl\\([^)]*nomLocataireParBail"
    - from: "src/web/views/pages/baux/indexations.ejs"
      to: "alerte.source.extra.nomLocataire"
      via: "lecture du champ enrichi par le domaine"
      pattern: "source\\.extra\\.nomLocataire"
---

<objective>
DET-02 — Unifier les 2 patterns d'enrichissement `calculerAlertesIrl` sur un chemin unique : `routes/baux.ts` passe la Map des noms en 5e argument (comme `calculer-toutes-alertes.ts`, l'appelant de référence), supprime la boucle post-hoc et le dict séparé, la vue lit `alerte.source.extra.nomLocataire`.

Purpose: Éliminer le second chemin d'enrichissement divergent ; un seul mécanisme (Map injectée dans la fonction pure), domaine pur préservé.
Output: routes/baux.ts aligné sur le pattern de référence, vue baux/indexations rebranchée, rendu inchangé sous la couverture existante.
</objective>

<execution_context>
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/workflows/execute-plan.md
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dette-technique-consolidation-atomicit/10-CONTEXT.md

<interfaces>
Signature cible (déjà implémentée, ne pas modifier le domaine) — src/domain/locatif/alerte-irl.ts §80 :
  calculerAlertesIrl(baux, biens, indexationsParBail, maintenant, nomLocataireParBail?: Map<BailId,string>): Alerte[]
  → écrit alerte.source.extra.nomLocataire = nomLocataireParBail?.get(bail.id) ?? ''

Pattern de référence à mirroir — src/application/dashboard/calculer-toutes-alertes.ts §66-74 :
  const locatairesParId = new Map(locataires.map((l) => [l.id, l]));
  const nomLocataireParBail = new Map<BailId,string>(
    baux.filter(b => locatairesParId.has(b.locataireId))
        .map(b => [b.id, `${loc.prenom} ${loc.nom}`]));
  calculerAlertesIrl(baux, biens, indexationsParBail, maintenant, nomLocataireParBail);

Code actuel à remplacer — src/web/routes/baux.ts §165-185 :
  appel sans 5e arg (§165), puis boucle post-hoc §167-178 construisant
  `locatairesParBail: Record<string,string>`, passé à la vue §184.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Construire nomLocataireParBail dans routes/baux.ts et le passer en 5e argument</name>
  <files>src/web/routes/baux.ts</files>
  <read_first>
    - src/web/routes/baux.ts §144-186 (handler /baux/indexations, boucle post-hoc à supprimer)
    - src/application/dashboard/calculer-toutes-alertes.ts §52-100 (pattern de référence à mirroir)
    - src/domain/locatif/alerte-irl.ts §80-118 (signature et écriture de source.extra.nomLocataire)
  </read_first>
  <action>
    Dans le handler app.get('/baux/indexations') de src/web/routes/baux.ts : avant l'appel à
    calculerAlertesIrl, construire `const locatairesParId = new Map(locataires.map((l) => [l.id, l]))`
    puis `const nomLocataireParBail = new Map<BailId,string>(...)` en mirroir exact de
    calculer-toutes-alertes.ts (filter sur locatairesParId.has(bail.locataireId), value
    `${loc.prenom} ${loc.nom}`). Passer `nomLocataireParBail` en 5e argument :
    `calculerAlertesIrl(baux, biens, indexationsParBail, maintenant, nomLocataireParBail)`.
    SUPPRIMER intégralement la boucle d'enrichissement post-hoc (§167-178) et le dict
    `locatairesParBail`. Retirer `locatairesParBail` du payload `reply.view(...)`. Conserver l'import
    BailId déjà présent. Aucun nom affiché ne doit changer (même source : prenom + nom du locataire
    rattaché au bail).
  </action>
  <verify>
    <automated>grep -q "nomLocataireParBail" src/web/routes/baux.ts && ! grep -q "locatairesParBail" src/web/routes/baux.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "calculerAlertesIrl(baux, biens, indexationsParBail, maintenant, nomLocataireParBail)" src/web/routes/baux.ts` retourne 1.
    - `grep -c "locatairesParBail" src/web/routes/baux.ts` retourne 0 (dict et boucle post-hoc supprimés).
    - La construction de `nomLocataireParBail` est byte-équivalente au pattern de calculer-toutes-alertes.ts (même filter, même value).
    - `pnpm exec tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>routes/baux.ts utilise le chemin unique ; boucle post-hoc et dict séparé supprimés.</done>
</task>

<task type="auto">
  <name>Task 2: Rebrancher la vue baux/indexations sur alerte.source.extra.nomLocataire et prouver l'iso-rendu</name>
  <files>src/web/views/pages/baux/indexations.ejs</files>
  <read_first>
    - src/web/views/pages/baux/indexations.ejs (lecture actuelle de locatairesParBail à rebrancher)
    - src/web/routes/baux.ts (payload view sans locatairesParBail désormais)
  </read_first>
  <action>
    Dans src/web/views/pages/baux/indexations.ejs, remplacer toute lecture de
    `locatairesParBail[...]` (ou `locatairesParBail[alerte.source.refId]`) par
    `alerte.source.extra.nomLocataire`. Le libellé affiché doit rester identique (même nom
    `prenom nom`). Ne pas toucher au reste de la vue ni au partiel inline (géré par DET-01).
    Vérifier que la suite d'intégration couvrant /baux/indexations reste verte — c'est la
    preuve de non-régression du rendu (critère #2 « couvert par les scénarios existants restés
    au vert »).
  </action>
  <verify>
    <automated>grep -q "source.extra.nomLocataire" src/web/views/pages/baux/indexations.ejs && pnpm vitest run tests/integration</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "locatairesParBail" src/web/views/pages/baux/indexations.ejs` retourne 0.
    - `grep -c "source.extra.nomLocataire" src/web/views/pages/baux/indexations.ejs` >= 1.
    - `pnpm vitest run tests/integration` exits 0 (rendu /baux/indexations non régressé).
    - `pnpm test:bdd` (ou les features ciblant les alertes IRL) reste vert si exécutable dans l'environnement.
  </acceptance_criteria>
  <done>Vue lit le champ enrichi par le domaine ; rendu observablement identique sous couverture existante.</done>
</task>

</tasks>

<verification>
- Un seul chemin d'enrichissement (Map en 5e arg) partagé entre dashboard et baux.
- Boucle post-hoc et dict `locatairesParBail` éliminés.
- Vue lit `alerte.source.extra.nomLocataire`.
- Intégration + BDD verts (rendu inchangé).
</verification>

<success_criteria>
Enrichissement des alertes IRL par chemin unique `calculerAlertesIrl`, couvert par les scénarios existants restés verts. (Critère #2)
</success_criteria>

<output>
After completion, create `.planning/phases/10-dette-technique-consolidation-atomicit/10-02-SUMMARY.md`
</output>
