---
phase: 10-dette-technique-consolidation-atomicit
plan: 04
type: tdd
wave: 2
depends_on: [03]
files_modified:
  - src/application/locatif/appliquer-indexation-irl.ts
  - src/application/locatif/modifier-bail-actif.ts
  - src/web/routes/baux.ts
  - src/main.ts
  - tests/unit/application/locatif/atomicite-appliquer-indexation-irl.test.ts
  - tests/unit/application/locatif/atomicite-modifier-bail-actif.test.ts
autonomous: true
requirements: [DET-03]
must_haves:
  truths:
    - "appliquerIndexationIRL enveloppe ses écritures DB (bail + suppression/batch echeance + bail_indexation) dans db.transaction().execute(trx => …)."
    - "modifierBailActif enveloppe ses écritures DB (bail + suppression/batch echeance) dans db.transaction().execute(trx => …)."
    - "La génération PDF + écriture fichier reste HORS transaction (un échec disque ne tient pas la transaction ouverte)."
    - "Un échec de la 2e écriture DB déclenche un rollback complet : bail inchangé, aucune echeance orpheline supprimée, aucune ligne bail_indexations partielle."
    - "La suite test:unit reste verte et < 30 s (baseline mesurée ~2.5 s, 835 tests)."
  artifacts:
    - path: "tests/unit/application/locatif/atomicite-appliquer-indexation-irl.test.ts"
      provides: "Test d'injection d'échec + assertion rollback (site 1, D-10-05)"
    - path: "tests/unit/application/locatif/atomicite-modifier-bail-actif.test.ts"
      provides: "Test d'injection d'échec + assertion rollback (site 2, D-10-05)"
  key_links:
    - from: "src/application/locatif/appliquer-indexation-irl.ts"
      to: "db.transaction().execute"
      via: "wrapper transactionnel autour des writes DB, trxArg threadé aux repos"
      pattern: "db\\.transaction\\(\\)\\.execute"
    - from: "src/application/locatif/modifier-bail-actif.ts"
      to: "db.transaction().execute"
      via: "wrapper transactionnel + db ajouté à la signature + 4 call sites routes/baux.ts + wiring main.ts"
      pattern: "db\\.transaction\\(\\)\\.execute"
---

<objective>
DET-03 (atomicité) — Envelopper les écritures multi-tables des deux sites jumeaux dans une transaction Kysely enveloppante, et prouver le rollback complet par un test d'injection d'échec par site (D-10-05). La génération PDF/fichier reste hors transaction (D-10-04).

Purpose: Corriger la faille d'atomicité réelle : aujourd'hui, si la suppression d'echeances réussit puis l'insertion échoue, on obtient un état incohérent (echeances orphelines supprimées, bail/indexation partiels). C'est la déviation D-94 acceptée à corriger ici.
Output: Les deux use cases atomiques sous transaction, 2 tests d'injection prouvant le rollback, suite test:unit verte < 30 s.
</objective>

<execution_context>
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/workflows/execute-plan.md
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dette-technique-consolidation-atomicit/10-CONTEXT.md
@.planning/phases/03-conformit-du-bail-diagnostics-edl-irl-mobilier/03-CONTEXT.md
@.planning/milestones/v1.0-MILESTONE-AUDIT.md

<interfaces>
Pattern transaction établi à suivre — src/infrastructure/repositories/bail-repository-sqlite.ts §22,
src/application/fiscalite/cloturer-exercice.ts §199-207, activer-fiscalite-bien.ts §161-168 :
  await db.transaction().execute(async (trx) => { /* writes via repos(trx) */ });

Fondation du plan 03 (déjà livrée) : bailRepo.enregistrer(bail, trxArg),
echeanceLoyerRepo.supprimerLot(ids, trxArg), echeanceLoyerRepo.enregistrerBatch(echeances, trxArg),
bailIndexationRepo.enregistrer(bi, trxArg) acceptent un trxArg opaque.

SITE 1 — src/application/locatif/appliquer-indexation-irl.ts :
  - db: Kysely<DB> DÉJÀ dans la signature §87 (actuellement `void db;` §205 réservé).
  - Call site déjà OK : src/web/routes/indexations.ts:288 passe `opts.db` (opts.db?: Kysely<DB> §68).
  - Writes DB : bailRepo.enregistrer §123, echeanceLoyerRepo.supprimerLot §143,
    echeanceLoyerRepo.enregistrerBatch §155, bailIndexationRepo.enregistrer §172.
  - PDF/fichier §174-202 : reste HORS transaction (D-10-04).

SITE 2 — src/application/locatif/modifier-bail-actif.ts :
  - ⚠ db N'EST PAS dans la signature actuelle §42-48 → DOIT être ajouté.
  - ⚠ Le plugin baux N'EXPOSE PAS db dans ses opts (src/web/routes/baux.ts §50-61) → l'ajouter.
  - ⚠ Le wiring d'enregistrement N'envoie PAS db — src/main.ts:258 register(bauxPlugin, {…})
    (db est disponible localement dans main.ts, cf. register(racinePlugin, { db, … }) §250).
  - Writes DB : bailRepo.enregistrer §98, echeanceLoyerRepo.supprimerLot §109,
    echeanceLoyerRepo.enregistrerBatch §133.
  - 4 call sites à mettre à jour — src/web/routes/baux.ts §530,595,636,690 (passer opts.db).
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED→GREEN): Atomicité appliquer-indexation-irl — injection d'échec + wrapping transaction</name>
  <files>src/application/locatif/appliquer-indexation-irl.ts, tests/unit/application/locatif/atomicite-appliquer-indexation-irl.test.ts</files>
  <read_first>
    - src/application/locatif/appliquer-indexation-irl.ts §83-213 (use case cible D-94)
    - src/application/fiscalite/cloturer-exercice.ts §199-207 (pattern db.transaction().execute)
    - src/infrastructure/repositories/bail-repository-sqlite.ts §22-74 (pattern transaction)
  </read_first>
  <behavior>
    - RED : forcer l'échec de la 2e écriture DB (ex. echeanceLoyerRepo.enregistrerBatch ou
      bailIndexationRepo.enregistrer qui throw) APRÈS un bailRepo.enregistrer réussi.
    - Assertion rollback : après l'échec, le bail en base est INCHANGÉ (loyer/IRL d'avant),
      AUCUNE echeance n'a été supprimée (les echeances pré-existantes sont toujours là),
      AUCUNE ligne bail_indexations partielle n'a été créée.
    - L'erreur d'origine est bien propagée (le use case re-throw).
  </behavior>
  <action>
    Écrire d'ABORD tests/unit/application/locatif/atomicite-appliquer-indexation-irl.test.ts (RED) :
    monter une vraie DB SQLite in-memory (réutiliser le helper de setup DB des tests existants),
    seeder un bail + des echeances futures non encaissées, puis injecter l'échec sur la 2e écriture.
    Mécanique d'injection laissée à ta discrétion (D-10-05) : ex. wrapper l'adaptateur réel et faire
    throw enregistrerBatch/bailIndexationRepo.enregistrer au 1er appel. Asserter l'état post-échec
    (bail.loyerHc == valeur initiale, count echeances == seed, count bail_indexations == 0). Lancer →
    RED (rollback non garanti aujourd'hui). Puis GREEN : dans appliquer-indexation-irl.ts, envelopper
    les writes DB des étapes 6-8 (bailRepo.enregistrer, supprimerLot, enregistrerBatch,
    bailIndexationRepo.enregistrer) dans `await db.transaction().execute(async (trx) => { … })`,
    en passant `trx` comme trxArg à chacun de ces appels repo. Remplacer `void db;` §205. La section
    PDF/fichier (étape 9, §174-202) RESTE après le bloc transactionnel, HORS transaction (D-10-04 :
    un rollback DB ne peut pas annuler un fichier disque ; le log de compensation CRITICAL existant
    reste valable). Relancer → GREEN.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/application/locatif/atomicite-appliquer-indexation-irl.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run tests/unit/application/locatif/atomicite-appliquer-indexation-irl.test.ts` exits 0.
    - Le test échoue si on retire le wrapper transactionnel (preuve qu'il teste bien le rollback) — vérifié manuellement durant RED.
    - Après l'échec injecté : bail row == pre-state, count echeances == seed (aucune suppression orpheline), count bail_indexations == 0.
    - `grep -c "db.transaction().execute" src/application/locatif/appliquer-indexation-irl.ts` >= 1.
    - `grep -c "void db" src/application/locatif/appliquer-indexation-irl.ts` retourne 0.
    - La génération PDF reste hors du bloc transactionnel (le `try` PDF est après la fermeture de `db.transaction().execute`).
  </acceptance_criteria>
  <done>Site 1 atomique, rollback prouvé par injection, PDF hors transaction.</done>
</task>

<task type="tdd">
  <name>Task 2 (RED→GREEN): Atomicité modifier-bail-actif — ajout db (signature + opts + wiring), injection + wrapping</name>
  <files>src/application/locatif/modifier-bail-actif.ts, src/web/routes/baux.ts, src/main.ts, tests/unit/application/locatif/atomicite-modifier-bail-actif.test.ts</files>
  <read_first>
    - src/application/locatif/modifier-bail-actif.ts §1-140 (signature SANS db, writes à envelopper)
    - src/web/routes/baux.ts §50-61 (opts du plugin baux, SANS db) et §530,595,636,690 (4 call sites)
    - src/main.ts §250,258 (wiring : racinePlugin reçoit db ; bauxPlugin ne le reçoit PAS encore)
    - src/application/locatif/appliquer-indexation-irl.ts (référence du wrapping fait en tâche 1)
  </read_first>
  <behavior>
    - RED : forcer l'échec de enregistrerBatch APRÈS supprimerLot réussi (confirmation 'oui').
    - Assertion rollback : bail inchangé, AUCUNE echeance supprimée (les echeances pré-existantes
      restent), pas d'état partiel.
    - Le mode 'previsualiser' (preview) ne déclenche aucune écriture et reste inchangé.
  </behavior>
  <action>
    Écrire d'ABORD tests/unit/application/locatif/atomicite-modifier-bail-actif.test.ts (RED) :
    DB SQLite in-memory, seed bail actif + echeances futures, commande confirmation 'oui', injecter
    l'échec sur enregistrerBatch. Asserter rollback complet. Lancer → RED. Puis GREEN, en 3 wirings :
    (1) ajouter `db: Kysely<DB>` à la signature de modifierBailActif (importer Kysely depuis 'kysely'
    et DB depuis infrastructure/db/kysely-types.js), envelopper les writes de l'étape 2 (§97-134 :
    bailRepo.enregistrer, supprimerLot, enregistrerBatch) dans
    `await db.transaction().execute(async (trx) => { … })` en threadant `trx` à chaque repo ;
    (2) ajouter `db?: Kysely<DB>` au type d'options du plugin baux (src/web/routes/baux.ts §50-61) et
    passer `opts.db` aux 4 call sites §530,595,636,690 ;
    (3) wirer db à l'enregistrement : src/main.ts:258 `register(bauxPlugin, { …, db })` (db est
    disponible localement, cf. ligne 250 pour racinePlugin). Le mode preview (confirmation
    'previsualiser') ne doit ouvrir AUCUNE transaction. Relancer → GREEN.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/application/locatif/atomicite-modifier-bail-actif.test.ts && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run tests/unit/application/locatif/atomicite-modifier-bail-actif.test.ts` exits 0.
    - Après l'échec injecté : bail row == pre-state, count echeances == seed (aucune suppression orpheline).
    - `grep -c "db.transaction().execute" src/application/locatif/modifier-bail-actif.ts` >= 1.
    - `modifierBailActif` a `db` dans sa signature ; le plugin baux expose `db` dans ses opts ; les 4 call sites passent `opts.db` ; `src/main.ts` ligne ~258 passe `db` à bauxPlugin.
    - Le mode preview n'ouvre pas de transaction (aucun write en mode 'previsualiser').
    - `pnpm exec tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Site 2 atomique, db threadé depuis le wiring main.ts → plugin baux → use case, rollback prouvé, preview inchangé.</done>
</task>

<task type="auto">
  <name>Task 3: Vérification suite complète + seuil de durée (critère #4)</name>
  <files>package.json</files>
  <read_first>
    - package.json §16-21 (scripts test, test:unit, test:integration, test:bdd)
    - 10-CONTEXT.md §40-41 (point ouvert : périmètre du « < 30 s »)
  </read_first>
  <action>
    Résolution du point ouvert (10-CONTEXT.md §41) : le critère #4 « suite verte < 30 s » vise les
    tests UNITAIRES (`pnpm test:unit`), PAS la suite complète (test + bdd) qui prend plusieurs minutes.
    Baseline mesurée durant la planification : `pnpm test:unit` = ~2.5 s, 835 tests, 100 fichiers.
    Lancer `pnpm test:unit` et confirmer : exit 0, durée < 30 s. Lancer aussi `pnpm test:integration`
    (non régressé par DET-01/02/03) et `pnpm test:bdd` si exécutable, pour confirmer la non-régression
    globale. Consigner dans le SUMMARY la commande exacte que le critère #4 asserte (`pnpm test:unit`)
    et la durée mesurée. Ne PAS asserter « < 30 s » contre `pnpm test` (suite complète) qui dépasse ce
    seuil par conception (200+ fichiers + 45 features BDD).
  </action>
  <verify>
    <automated>pnpm test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test:unit` exits 0 ET sa durée rapportée par Vitest est < 30 s.
    - Le SUMMARY consigne : critère #4 = `pnpm test:unit`, baseline ~2.5 s, mesure finale < 30 s.
    - `pnpm test:integration` exits 0.
    - La résolution du périmètre (unit vs complète) est documentée dans le SUMMARY.
  </acceptance_criteria>
  <done>Critère #4 résolu et asserté sur le bon périmètre mesuré ; suites verte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| use case → DB | Écritures multi-tables (bail, echeance_loyer, bail_indexations) ; échec partiel = état incohérent. |
| transaction → I/O disque | La génération PDF/fichier est append-only non transactionnelle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering / Integrity | appliquer-indexation-irl + modifier-bail-actif (writes multi-tables) | mitigate | Envelopper les writes DB dans `db.transaction().execute` ; rollback complet sur échec partiel, prouvé par 2 tests d'injection (D-10-05). |
| T-10-04 | Denial of Service | I/O fichier dans la transaction | mitigate | PDF/écriture fichier HORS transaction (D-10-04) : l'I/O lent ne tient pas la transaction ouverte, pas de write-lock prolongé. |
| T-10-05 | Repudiation | échec PDF post-commit DB | accept | Compensation déjà documentée : log CRITICAL + régénération via route GET /baux/:id/avenant/:annee. Un fichier disque ne peut être rollback. |
</threat_model>

<verification>
- Les deux sites enveloppés dans une transaction Kysely ; PDF hors transaction.
- 2 tests d'injection d'échec verts, rollback complet asserté par site (D-10-05).
- `pnpm test:unit` vert < 30 s (critère #4, périmètre résolu) ; intégration verte.
</verification>

<success_criteria>
Écritures multi-tables atomiques (rollback complet sur échec partiel, vérifié par scénario) ; suite verte sous le seuil. (Critères #3 et #4)
</success_criteria>

<output>
After completion, create `.planning/phases/10-dette-technique-consolidation-atomicit/10-04-SUMMARY.md`
</output>
