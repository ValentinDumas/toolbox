---
phase: 05.1-hardening-hexagonal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/encaissements/quittance-builder.ts
  - src/infrastructure/pdf/quittance-builder-pdfmake.ts
  - src/domain/locatif/avenant-irl-builder.ts
  - src/infrastructure/pdf/avenant-irl-builder-pdfmake.ts
  - src/domain/encaissements/mise-en-demeure-builder.ts
  - src/infrastructure/pdf/mise-en-demeure-builder-pdfmake.ts
  - src/application/encaissements/generer-quittance.ts
  - src/application/locatif/appliquer-indexation-irl.ts
  - src/application/encaissements/enregistrer-relance.ts
  - src/web/routes/quittances.ts
  - src/web/routes/indexations.ts
  - src/web/routes/relances.ts
  - src/main.ts
  - tests/unit/encaissements/generer-quittance.test.ts
  - tests/unit/encaissements/enregistrer-relance.test.ts
  - tests/unit/locatif/appliquer-indexation-irl.test.ts
  - tests/integration/web/relances-mailto.test.ts
  - tests/bdd/step_definitions/indexation-irl.steps.ts
  - .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/deferred-items.md
autonomous: true
requirements:
  - HEX-HARDENING
tags:
  - hexagonal
  - port-adapter
  - ddd
  - pdf
  - refactor
  - encaissements
  - locatif

must_haves:
  truths:
    - "Aucune ligne `from.*infrastructure` n'apparait dans src/application/ a l'exception des `type Kysely<DB>` annotes acceptable (05-VERIFICATION.md L32). La regle non-negociable hexagonale CLAUDE.md est respectee de bout en bout (les 3 violations residuelles de deferred-items.md sont fermees)."
    - "Le port QuittanceBuilder existe dans src/domain/encaissements/quittance-builder.ts et expose une seule methode `construire(...): unknown` — retour `unknown` pour preserver la purete du domaine (pattern miroir de PdfRenderer.genererBuffer et RecapFiscalBuilder.construire)."
    - "Le port AvenantIRLBuilder existe dans src/domain/locatif/avenant-irl-builder.ts et expose `construire(...): unknown`."
    - "Le port MiseEnDemeureBuilder existe dans src/domain/encaissements/mise-en-demeure-builder.ts et expose `construire(...): unknown`."
    - "L'import `StockageFichierLocal` (classe concrete infra) est supprime de src/application/encaissements/generer-quittance.ts : l'appel a slugify est remplace par la fonction pure `slugify` deja exportee depuis src/domain/_shared/slug.ts (decouvert lors de l'audit, Task 0)."
    - "src/main.ts instancie les 3 adapters QuittanceBuilderPdfmake, AvenantIRLBuilderPdfmake, MiseEnDemeureBuilderPdfmake et les propage dans les 3 routes Fastify (quittances, indexations, relances) qui les acheminent jusqu'aux use cases."
    - "Les 3 routes (quittances.ts, indexations.ts, relances.ts) ne contiennent plus aucun import direct depuis `infrastructure/pdf/*-doc-def.ts` — la dependance passe uniquement par le port injecte via opts."
    - "`pnpm typecheck` exit 0, `pnpm test -- --run` tous verts (888+ vitest), BDD reste >= 171/173 (les 2 echecs fiscalite-qualification.feature heritees sont independants et restent constants)."
    - "Le fichier deferred-items.md voit ses 4 lignes (L17-L20) marquees `closed by Phase 5.1, see 05.1-01-SUMMARY.md`."
  artifacts:
    - path: "src/domain/encaissements/quittance-builder.ts"
      provides: "Interface port QuittanceBuilder cote domaine (jamais d'import infra)"
      contains: "export interface QuittanceBuilder"
    - path: "src/infrastructure/pdf/quittance-builder-pdfmake.ts"
      provides: "Adapter pdfmake wrappant construireQuittance"
      contains: "implements QuittanceBuilder"
    - path: "src/domain/locatif/avenant-irl-builder.ts"
      provides: "Interface port AvenantIRLBuilder cote domaine"
      contains: "export interface AvenantIRLBuilder"
    - path: "src/infrastructure/pdf/avenant-irl-builder-pdfmake.ts"
      provides: "Adapter pdfmake wrappant construireAvenantIRL"
      contains: "implements AvenantIRLBuilder"
    - path: "src/domain/encaissements/mise-en-demeure-builder.ts"
      provides: "Interface port MiseEnDemeureBuilder cote domaine"
      contains: "export interface MiseEnDemeureBuilder"
    - path: "src/infrastructure/pdf/mise-en-demeure-builder-pdfmake.ts"
      provides: "Adapter pdfmake wrappant construireMiseEnDemeure"
      contains: "implements MiseEnDemeureBuilder"
    - path: "src/application/encaissements/generer-quittance.ts"
      provides: "Use case utilisant QuittanceBuilder via param + slugify domain au lieu de StockageFichierLocal.slugify"
      contains: "QuittanceBuilder"
      forbids: "from '../../infrastructure"
    - path: "src/application/locatif/appliquer-indexation-irl.ts"
      provides: "Use case utilisant AvenantIRLBuilder via infra.avenantIRLBuilder"
      contains: "AvenantIRLBuilder"
      forbids: "from '../../infrastructure/pdf"
    - path: "src/application/encaissements/enregistrer-relance.ts"
      provides: "Use case utilisant MiseEnDemeureBuilder via param"
      contains: "MiseEnDemeureBuilder"
      forbids: "from '../../infrastructure/pdf"
    - path: "src/main.ts"
      provides: "DI : instanciation des 3 adapters + propagation dans les 3 plugins routes"
      contains: "QuittanceBuilderPdfmake"
  key_links:
    - from: "src/application/encaissements/generer-quittance.ts"
      to: "src/domain/encaissements/quittance-builder.ts"
      via: "import type { QuittanceBuilder } from '../../domain/encaissements/quittance-builder.js'"
      pattern: "from '../../domain/encaissements/quittance-builder"
    - from: "src/application/locatif/appliquer-indexation-irl.ts"
      to: "src/domain/locatif/avenant-irl-builder.ts"
      via: "import type { AvenantIRLBuilder } from '../../domain/locatif/avenant-irl-builder.js'"
      pattern: "from '../../domain/locatif/avenant-irl-builder"
    - from: "src/application/encaissements/enregistrer-relance.ts"
      to: "src/domain/encaissements/mise-en-demeure-builder.ts"
      via: "import type { MiseEnDemeureBuilder } from '../../domain/encaissements/mise-en-demeure-builder.js'"
      pattern: "from '../../domain/encaissements/mise-en-demeure-builder"
    - from: "src/main.ts"
      to: "QuittanceBuilderPdfmake / AvenantIRLBuilderPdfmake / MiseEnDemeureBuilderPdfmake"
      via: "new QuittanceBuilderPdfmake() + new AvenantIRLBuilderPdfmake() + new MiseEnDemeureBuilderPdfmake() injectes dans plugins routes"
      pattern: "new (Quittance|AvenantIRL|MiseEnDemeure)BuilderPdfmake"
---

<objective>
Phase 5.1 (hardening hexagonal proactif — NON gap_closure car non drive par VERIFICATION.md). Fermer les 3 violations hexagonales pre-existantes loggees dans `.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/deferred-items.md` (heritees de Phases 2 et 3) avec la MEME recette que le plan 05-11 (RecapFiscalBuilder / RecapFiscalBuilderPdfmake) vient d'appliquer pour CR-06.

3 use cases application importent encore directement des modules `infrastructure/pdf/*-doc-def.ts` :

| Use case | Ligne | Violation |
|---|---|---|
| `src/application/encaissements/generer-quittance.ts` | L22 | `import { construireQuittance } from '../../infrastructure/pdf/quittance-doc-def.js'` |
| `src/application/encaissements/generer-quittance.ts` | L23 | `import { StockageFichierLocal } from '../../infrastructure/storage/stockage-fichier-local.js'` (classe concrete infra, utilisee pour `StockageFichierLocal.slugify` L113) |
| `src/application/locatif/appliquer-indexation-irl.ts` | L26 | `import { construireAvenantIRL } from '../../infrastructure/pdf/avenant-irl-doc-def.js'` |
| `src/application/encaissements/enregistrer-relance.ts` | L17 | `import { construireMiseEnDemeure } from '../../infrastructure/pdf/mise-en-demeure-doc-def.js'` |

Cette regle est NON-NEGOCIABLE par CLAUDE.md : "Domaine pur : aucun import technique (ORM, HTTP, fichier) dans le coeur du domaine — ports & adapters strict". L'application etant le client immediat du domaine, elle est soumise a la meme regle (cf. practices/DDD.md, hexagonal architecture).

Fix : pour chaque violation, extraire un port dans `src/domain/<contexte>/` (interface TypeScript retournant `unknown` pour eviter la fuite du type `TDocumentDefinitions` de pdfmake dans le domaine — pattern miroir de PdfRenderer.genererBuffer deja en place). Creer un adapter dans `src/infrastructure/pdf/` qui wrap la fonction `construire*` existante (delegation pure, zero copie). Injecter via DI le port dans le use case et la route. Mettre a jour main.ts (3 nouvelles instances) + 3 routes (interface plugin etendue) + les tests appelants.

Le `StockageFichierLocal` import L23 est une violation differente (classe concrete infra, pas un import doc-def) : audit (Task 0) confirme qu'il est utilise UNIQUEMENT pour `StockageFichierLocal.slugify(...)` L113. Fix simple : remplacer par la fonction pure `slugify` deja exportee dans `src/domain/_shared/slug.ts:8` (decouverte par grep, pas d'extraction supplementaire necessaire). Note : la classe `StockageFichierLocal` reste instanciee dans main.ts et passee au use case via le parametre `stockage: Stockage` (interface inline L26-28 de generer-quittance.ts) — c'est le PATTERN CORRECT, le bug etait uniquement l'import direct de la CLASSE pour appeler la methode statique slugify.

Note BDD outside-in : refactor structurel a comportement strictement identique. Les fonctions `construire*` infra sont conservees telles quelles (les adapters delegent purement). Les tests existants (vitest + BDD) servent de filet de securite — aucun nouveau scenario BDD requis (cf. practices/BDD_PRACTICES.md, qui mandate BDD sur logique fiscale et comportement metier, pas sur refactor de cablage).

Purpose : la regle hexagonale non-negociable CLAUDE.md doit etre integralement respectee (objectif "100% des regles non-negociables respectees" inatteignable tant que les 3 violations persistent). Sans ce fix, toute future modification de la couche PDF (ex. swap pdfmake -> puppeteer) casse la couche application via couplage direct.

Output :
- 6 nouveaux fichiers (3 ports domaine + 3 adapters infrastructure).
- 3 fichiers application modifies (signature deps + appel via le port + suppression imports infra).
- 3 fichiers route modifies (interface plugin etendue + propagation).
- 1 fichier main.ts modifie (3 imports + 3 instances + 3 propagations dans `app.register(...)`).
- 5+ fichiers de test modifies (audit Task 0 + auto-propagation typecheck).
- deferred-items.md mis a jour (les 4 lignes L17-L20 marquees `closed by Phase 5.1`).
- `pnpm typecheck` exit 0, `pnpm test -- --run` tous verts.
- AUCUN `from.*infrastructure` ne subsiste dans src/application/ hors `type Kysely<DB>` (deja documentes acceptables 05-VERIFICATION.md L32).
</objective>

<execution_context>
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/workflows/execute-plan.md
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/deferred-items.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-11-gap-recap-fiscal-port-hexa-PLAN.md
@.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-11-gap-recap-fiscal-port-hexa-SUMMARY.md
@CLAUDE.md
@practices/DDD.md
@practices/SOFTWARE_CRAFTSMANSHIP.md
@practices/BDD_PRACTICES.md

<interfaces>
Pattern de reference (port + adapter) deja en place dans le codebase — A REPLIQUER MOT POUR MOT pour chacun des 3 ports.

PORT DOMAINE (src/domain/fiscalite/recap-fiscal-builder.ts, deja existant) :
- JSDoc d'entete explique le port + miroir de PdfRenderer.genererBuffer + regle CLAUDE.md hexagonale.
- Imports : uniquement `import type {...}` depuis le meme contexte ou d'autres contextes du domaine. AUCUN import infra, AUCUN import pdfmake.
- `export interface RecapFiscalBuilder { construire(...): unknown; }`
- Le type de retour est `unknown` — JSDoc precise "Le type concret est TDocumentDefinitions (pdfmake), volontairement masque pour preserver la purete du domaine".

ADAPTER INFRA (src/infrastructure/pdf/recap-fiscal-builder-pdfmake.ts, deja existant) :
- JSDoc d'entete explique l'adapter (miroir de PdfRendererPdfmake).
- Imports : le port depuis le domaine (import type) + les VO/agregat du domaine (import type) + la fonction `construire*` existante depuis `./xxxx-doc-def.js` (import value).
- `export class XxxBuilderPdfmake implements XxxBuilder { construire(...): unknown { return construireXxx(...); } }`
- Delegation pure — zero logique metier, zero copie. L'adapter wrap juste pour le widening `unknown`.

PORT PDFRENDERER (src/domain/encaissements/pdf-renderer.ts, deja existant — DEJA UN PORT) :
- `export interface PdfRenderer { genererBuffer(docDef: unknown): Promise<Buffer>; }`
- 100% pur, zero import. Reste utilise tel quel par tous les use cases (param dedie).

POINT IMPORTANT — `interface PdfRendererLike` redondant dans appliquer-indexation-irl.ts L52-54 :
La duplication locale `interface PdfRendererLike { genererBuffer(docDef: unknown): Promise<Buffer>; }` est strictement equivalente au port domaine `PdfRenderer`. Decision : Task 2 supprime cette duplication et importe `PdfRenderer` depuis le domaine — bonus de coherence sans surcout (3 lignes a supprimer + 1 import a ajouter).

SIGNATURES ACTUELLES CIBLEES (lignes exactes verifiees) :

`genererQuittance` (src/application/encaissements/generer-quittance.ts) :
- Signature (L61-68) : `genererQuittance(commande, repos, pdfRenderer: PdfRenderer, stockage: Stockage, clock: Clock, db: Kysely<DB>): Promise<ResultatGenererQuittance>`
- Imports infra violants : L22 `construireQuittance` + L23 `StockageFichierLocal`.
- Usage de la violation : L113 `StockageFichierLocal.slugify(...)`, L143-151 `construireQuittance(...)`.

`appliquerIndexationIRL` (src/application/locatif/appliquer-indexation-irl.ts) :
- Signature (L85-90) : `appliquerIndexationIRL(commande, repos: Repos, infra: Infra, db: Kysely<DB>): Promise<AppliquerIndexationIRLResult>`
- `Infra` (L60-64) regroupe deja `{ pdfRenderer: PdfRendererLike; stockage: StockageLike; clock: Clock; }`.
- Imports infra violants : L26 `construireAvenantIRL`.
- Usage de la violation : L183-192 `construireAvenantIRL(...)`.

`enregistrerRelance` (src/application/encaissements/enregistrer-relance.ts) :
- Signature (L59-65) : `enregistrerRelance(commande, repos: Repos, templateRenderer: TemplateRenderer, pdfRenderer: PdfRenderer, clock: Clock): Promise<ResultatEnregistrerRelance>`
- Imports infra violants : L17 `construireMiseEnDemeure`.
- Usage de la violation : L163-173 `construireMiseEnDemeure(...)`.

STRATEGIE DE DI POUR CHAQUE USE CASE (DECISION POUR EVITER LA REINVENTION) :

- `genererQuittance` : ajouter un parametre `quittanceBuilder: QuittanceBuilder` en 4e position, JUSTE APRES `pdfRenderer`. Nouvelle signature : `genererQuittance(commande, repos, pdfRenderer, quittanceBuilder, stockage, clock, db)`. Impact : tests + route + main.ts.

- `appliquerIndexationIRL` : etendre l'interface `Infra` (L60-64) pour ajouter `avenantIRLBuilder: AvenantIRLBuilder`. Pas de breakage de positions de parametres — le 3e param reste un objet `infra`. Impact : interface + tests (4 callers) + route + main.ts. BONUS : remplacer `interface PdfRendererLike` (L52-54) par l'import du port `PdfRenderer`, supprimer aussi `StockageLike` L56-58 SI une interface `Stockage` est trouvee cote domaine (verifier par grep — sinon laisser tel quel, c'est hors scope).

- `enregistrerRelance` : ajouter un parametre `miseEnDemeureBuilder: MiseEnDemeureBuilder` en 5e position, JUSTE APRES `pdfRenderer`. Nouvelle signature : `enregistrerRelance(commande, repos, templateRenderer, pdfRenderer, miseEnDemeureBuilder, clock)`. Impact : tests (3 callers) + route + main.ts.

ROUTES `relances.ts` L19+L169 :
La route relances utilise `construireMiseEnDemeure` directement (L19 import + L169-179 call) pour generer un PDF a la volee dans GET /relances/:id/pdf (mise en demeure brute). Decision : aligner aussi cette utilisation via le port pour COHERENCE de Phase 5.1 (l'objectif est qu'aucune route OU use case n'importe directement un module `infrastructure/pdf/*-doc-def`). Le port `MiseEnDemeureBuilder` injecte dans `opts` de la route remplace l'import direct.

AUDIT TASK 0 (preview, a verifier par execute) :
- 4 callers test connus : `tests/unit/encaissements/generer-quittance.test.ts` (5 sites), `tests/unit/encaissements/enregistrer-relance.test.ts` (3 sites), `tests/unit/locatif/appliquer-indexation-irl.test.ts` (4 sites), `tests/integration/web/relances-mailto.test.ts` (1 site), `tests/bdd/step_definitions/indexation-irl.steps.ts` (lignes a verifier).
- Probabilite forte que le typecheck (Rule 3) decouvre des callers additionnels — comme le plan 05-11 a decouvert `parcours-complet-cloture.test.ts` + `fiscalite-multi-bien.steps.ts`. Anticiper : grep `tests/` apres modif use case pour propagation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0 : Audit pre-execution — verifier l'etat exact des violations + reperer tous les callers</name>
  <files>(read-only — pas de modification)</files>

  <read_first>
    <file>src/application/encaissements/generer-quittance.ts</file>
    <file>src/application/locatif/appliquer-indexation-irl.ts</file>
    <file>src/application/encaissements/enregistrer-relance.ts</file>
    <file>src/domain/_shared/slug.ts</file>
    <file>src/infrastructure/storage/stockage-fichier-local.ts</file>
    <file>.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/deferred-items.md</file>
    <file>.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/05-VERIFICATION.md</file>
  </read_first>

  <action>
    Executer 6 commandes de verification — capturer leur sortie dans `/tmp/05.1-task0-audit.log` pour reference dans les Tasks 1-4 :

    1. Verifier exactement le contenu des lignes citees dans deferred-items.md :
       `grep -n "from.*infrastructure" src/application/encaissements/generer-quittance.ts src/application/locatif/appliquer-indexation-irl.ts src/application/encaissements/enregistrer-relance.ts`

    2. Verifier les usages internes de `StockageFichierLocal` (doit retourner UNIQUEMENT le call `StockageFichierLocal.slugify` L113 et l'import L23) :
       `grep -n "StockageFichierLocal" src/application/encaissements/generer-quittance.ts`

    3. Confirmer que la fonction `slugify` du domaine est utilisable (signature compatible) :
       `cat src/domain/_shared/slug.ts`. Attendu : `export function slugify(input: string): string` — signature identique a `StockageFichierLocal.slugify`.

    4. Inventorier tous les callers tests (vitest + bdd) :
       `grep -rln "genererQuittance\\|appliquerIndexationIRL\\|enregistrerRelance" tests/ --include="*.ts"`. Attendre ~6 fichiers (les 6 deja connus a date).

    5. Verifier que les 3 routes utilisent encore l'ancien import direct :
       `grep -n "construireQuittance\\|construireAvenantIRL\\|construireMiseEnDemeure" src/web/routes/`. Attendu : 2+ lignes (relances.ts L19 + L169 au minimum).

    6. Confirmer la baseline tests verte AVANT toute modification :
       `pnpm typecheck` doit exit 0. `pnpm test -- --run 2>&1 | tail -10` doit montrer la suite verte (888+ tests passes selon le SUMMARY 05-11).

    Si l'une de ces verifications echoue (ex: la baseline tests n'est pas verte, ou un import n'est pas a la ligne attendue), STOP et signaler dans le SUMMARY — ne pas proceder.

    Aucun fichier n'est cree ou modifie dans cette tache. Aucun commit.
  </action>

  <verify>
    <automated>grep -c "from '../../infrastructure" src/application/encaissements/generer-quittance.ts ; grep -c "from '../../infrastructure" src/application/locatif/appliquer-indexation-irl.ts ; grep -c "from '../../infrastructure" src/application/encaissements/enregistrer-relance.ts ; test -f src/domain/_shared/slug.ts && echo "slugify domain OK" ; pnpm typecheck 2>&1 | tail -3 ; pnpm test -- --run 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "from '../../infrastructure" src/application/encaissements/generer-quittance.ts` retourne >= 2 (l'import construireQuittance + l'import StockageFichierLocal, possiblement un type-only Kysely DB).
    - `grep -c "from '../../infrastructure" src/application/locatif/appliquer-indexation-irl.ts` retourne >= 1 (l'import construireAvenantIRL, possiblement un type-only Kysely DB).
    - `grep -c "from '../../infrastructure" src/application/encaissements/enregistrer-relance.ts` retourne >= 1 (l'import construireMiseEnDemeure).
    - `test -f src/domain/_shared/slug.ts` exit 0 ET `grep -c "export function slugify" src/domain/_shared/slug.ts` retourne 1.
    - `pnpm typecheck` exit code 0 (baseline verte).
    - `pnpm test -- --run` exit code 0 (baseline verte). Le total doit etre >= 880 tests passes.
    - Le grep des routes (cmd 5) retourne >= 2 lignes (relances.ts L19 import + L169 call).
    - Audit log `/tmp/05.1-task0-audit.log` cree avec les 6 sorties.
  </acceptance_criteria>

  <done>
    Audit confirme les 4 violations exactement comme listees dans deferred-items.md L17-L20. La fonction domain `slugify` est utilisable. La baseline tests + typecheck est verte. Pas de fichier modifie, pas de commit. L'executor peut proceder aux Tasks 1-3 en sachant exactement quels fichiers modifier.
  </done>
</task>

<task type="auto">
  <name>Task 1 : Hardening QuittanceBuilder — port + adapter + use case + slugify domain + route + DI + tests</name>
  <files>src/domain/encaissements/quittance-builder.ts, src/infrastructure/pdf/quittance-builder-pdfmake.ts, src/application/encaissements/generer-quittance.ts, src/web/routes/quittances.ts, src/main.ts, tests/unit/encaissements/generer-quittance.test.ts</files>

  <read_first>
    <file>src/domain/encaissements/pdf-renderer.ts</file>
    <file>src/domain/fiscalite/recap-fiscal-builder.ts</file>
    <file>src/infrastructure/pdf/recap-fiscal-builder-pdfmake.ts</file>
    <file>src/infrastructure/pdf/quittance-doc-def.ts</file>
    <file>src/application/encaissements/generer-quittance.ts</file>
    <file>src/web/routes/quittances.ts</file>
    <file>src/main.ts</file>
    <file>tests/unit/encaissements/generer-quittance.test.ts</file>
    <file>src/domain/_shared/slug.ts</file>
    <file>CLAUDE.md</file>
    <file>practices/DDD.md</file>
  </read_first>

  <action>
    1. CREER `src/domain/encaissements/quittance-builder.ts` — JSDoc d'entete style 05-11 (regle CLAUDE.md hexagonale + miroir de PdfRenderer + miroir de RecapFiscalBuilder + reference du use case consommateur + reference de l'adapter). Imports : uniquement `import type` depuis le domaine pour les VO/agregats consommes par `construireQuittance` cote infra (lire `src/infrastructure/pdf/quittance-doc-def.ts` ligne 43 pour la signature exacte). Exporter `interface QuittanceBuilder` avec UNE seule methode `construire(echeance: EcheanceLoyer, bailleur: Bailleur, locataire: Locataire, adresseBien: Adresse, numero: string, emiseLe: Temporal.PlainDate, modeCharges: 'forfait' | 'provisions'): unknown` — adapter la signature au cas reel de `construireQuittance`, en se basant sur l'appel L143-151 de generer-quittance.ts. JSDoc sur `construire` precisant que le retour `unknown` masque `TDocumentDefinitions` pour preserver la purete du domaine.

    2. CREER `src/infrastructure/pdf/quittance-builder-pdfmake.ts` — JSDoc d'entete miroir de RecapFiscalBuilderPdfmake. Imports : `import type { QuittanceBuilder }` depuis le domaine + `import type` des VO/agregats utilises + `import { construireQuittance } from './quittance-doc-def.js'`. Exporter `class QuittanceBuilderPdfmake implements QuittanceBuilder` dont la methode `construire(...)` est UNE delegation pure : `return construireQuittance(echeance, bailleur, locataire, adresseBien, numero, emiseLe, modeCharges);` — meme ordre de parametres que `construireQuittance` actuel.

    3. MODIFIER `src/application/encaissements/generer-quittance.ts` :
       - L22 : SUPPRIMER `import { construireQuittance } from '../../infrastructure/pdf/quittance-doc-def.js';`
       - L23 : SUPPRIMER `import { StockageFichierLocal } from '../../infrastructure/storage/stockage-fichier-local.js';`
       - Apres l'import de `PdfRenderer` (L13) : AJOUTER `import type { QuittanceBuilder } from '../../domain/encaissements/quittance-builder.js';`
       - Apres les imports de domaine : AJOUTER `import { slugify } from '../../domain/_shared/slug.js';` (remplacement de StockageFichierLocal.slugify).
       - L61-68 (signature `genererQuittance`) : ajouter un parametre `quittanceBuilder: QuittanceBuilder` JUSTE APRES `pdfRenderer`. Nouvelle signature exacte :
         `export async function genererQuittance(commande: { echeanceId: EcheanceLoyerId | string }, repos: Repos, pdfRenderer: PdfRenderer, quittanceBuilder: QuittanceBuilder, stockage: Stockage, clock: Clock, db: Kysely<DB> | { transaction: () => { execute: (fn: (trx: unknown) => Promise<unknown>) => Promise<unknown> } }): Promise<ResultatGenererQuittance>`
       - L113 : remplacer `const locataireSlug = StockageFichierLocal.slugify(\`${locataire.nom}-${locataire.prenom}\`);` par `const locataireSlug = slugify(\`${locataire.nom}-${locataire.prenom}\`);`
       - L143-151 : remplacer `const docDef = construireQuittance(echeance, bailleur, locataire, adresseBien, quittance!.numero, clock.aujourdhui(), bail.modeCharges);` par `const docDef = quittanceBuilder.construire(echeance, bailleur, locataire, adresseBien, quittance!.numero, clock.aujourdhui(), bail.modeCharges);`

    4. MODIFIER `src/web/routes/quittances.ts` :
       - L15 : conserver `StockageFichierLocal` (utilise comme TYPE du param `stockage`, plus comme classe a appeler statiquement — c'est legitime cote route/web qui PEUT importer infra). Aucune modification.
       - Apres L12 (`import type { PdfRenderer }`) : AJOUTER `import type { QuittanceBuilder } from '../../domain/encaissements/quittance-builder.js';`
       - L36-37 (interface plugin opts) : juste apres `pdfRenderer: PdfRenderer;`, AJOUTER `quittanceBuilder: QuittanceBuilder;`.
       - L87-102 (call a `genererQuittance`) : INSERER `opts.quittanceBuilder,` JUSTE APRES `opts.pdfRenderer,` (L98) — respecter le nouvel ordre positionnel. Ne pas toucher aux autres lignes.

    5. MODIFIER `src/main.ts` :
       - Apres L48 (`import { PdfRendererPdfmake } from './infrastructure/pdf/pdf-renderer-pdfmake.js';`) : AJOUTER `import { QuittanceBuilderPdfmake } from './infrastructure/pdf/quittance-builder-pdfmake.js';`
       - Apres L163 (`const recapFiscalBuilder = new RecapFiscalBuilderPdfmake();`) : AJOUTER `const quittanceBuilder = new QuittanceBuilderPdfmake();`
       - Dans le call `app.register(quittancesPlugin, {...})` (L271-283) : INSERER `quittanceBuilder,` JUSTE APRES `pdfRenderer,` (autour de L279). Shorthand cle/valeur — la variable porte le meme nom que la cle de l'interface plugin opts.

    6. MODIFIER `tests/unit/encaissements/generer-quittance.test.ts` :
       - Ajouter `import { QuittanceBuilderPdfmake } from '../../../src/infrastructure/pdf/quittance-builder-pdfmake.js';` (a cote des autres imports application/infrastructure).
       - Apres `fakeStockage` declare en tete du fichier (chercher la declaration `fakeStockage` ou `fakePdfRenderer`) : DECLARER `const fakeQuittanceBuilder: QuittanceBuilder = { construire: vi.fn().mockReturnValue({}) };`. Ajouter `import type { QuittanceBuilder } from '../../../src/domain/encaissements/quittance-builder.js';`. Le stub renvoie `{}` (TDocumentDefinitions vide cast en unknown) — suffit pour le test, le PDF reel n'est pas verifie ici (fakePdfRenderer retourne deja un buffer mock).
       - Dans CHAQUE call a `genererQuittance(...)` (5 sites L129, L148, L169, L195, L244, L286, L329 — utiliser grep pour les retrouver exactement) : INSERER `fakeQuittanceBuilder,` JUSTE APRES `fakePdfRenderer,`. L'ordre exact apres modification : `genererQuittance(commande, repos, fakePdfRenderer, fakeQuittanceBuilder, fakeStockage, clock, db)`.
       - Mettre a jour les casts `as Parameters<typeof genererQuittance>[N]` si necessaire (les indices changent : pdfRenderer reste [2], quittanceBuilder devient [3], stockage devient [4], clock devient [5], db devient [6]).

    7. APRES TOUTES LES MODIFS : executer `pnpm typecheck` pour valider, puis `pnpm test tests/unit/encaissements/generer-quittance.test.ts -- --run` pour valider la suite locale, puis `pnpm test -- --run` pour valider la suite complete. Si le typecheck echoue sur des callers TESTS supplementaires non listes ci-dessus, propager le meme fix (ajout de l'argument quittanceBuilder dans le call) avant de commiter — c'est le pattern Rule 3 Blocking du plan 05-11.

    COMMIT ATOMIQUE : `feat(05.1): port QuittanceBuilder + adapter pdfmake + slugify domain (closes deferred 1+2/4)`.
  </action>

  <verify>
    <automated>pnpm typecheck 2>&1 | tail -5 ; test -f src/domain/encaissements/quittance-builder.ts && echo "port OK" ; test -f src/infrastructure/pdf/quittance-builder-pdfmake.ts && echo "adapter OK" ; grep -c "from '../../infrastructure" src/application/encaissements/generer-quittance.ts ; grep -c "StockageFichierLocal" src/application/encaissements/generer-quittance.ts ; grep -c "construireQuittance" src/application/encaissements/generer-quittance.ts ; grep -c "slugify" src/application/encaissements/generer-quittance.ts ; grep -c "QuittanceBuilderPdfmake" src/main.ts ; pnpm test tests/unit/encaissements/generer-quittance.test.ts -- --run 2>&1 | tail -5 ; pnpm test -- --run 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `test -f src/domain/encaissements/quittance-builder.ts` exit 0.
    - `test -f src/infrastructure/pdf/quittance-builder-pdfmake.ts` exit 0.
    - `grep -c "export interface QuittanceBuilder" src/domain/encaissements/quittance-builder.ts` retourne 1.
    - `grep -c "implements QuittanceBuilder" src/infrastructure/pdf/quittance-builder-pdfmake.ts` retourne 1.
    - `grep -c "from.*infrastructure" src/application/encaissements/generer-quittance.ts` retourne 1 — uniquement le `type { DB } from '../../infrastructure/db/kysely-types.js'` deja annote acceptable dans 05-VERIFICATION.md L32. Aucun import value depuis infrastructure.
    - `grep -c "StockageFichierLocal" src/application/encaissements/generer-quittance.ts` retourne 0.
    - `grep -c "construireQuittance" src/application/encaissements/generer-quittance.ts` retourne 0.
    - `grep -c "slugify" src/application/encaissements/generer-quittance.ts` retourne >= 2 (import + usage).
    - `grep -c "QuittanceBuilder" src/application/encaissements/generer-quittance.ts` retourne >= 2 (import type + param signature).
    - `grep -c "quittanceBuilder.construire" src/application/encaissements/generer-quittance.ts` retourne 1.
    - `grep -c "from.*infrastructure\\|from 'pdfmake" src/domain/encaissements/quittance-builder.ts` retourne 0 (le port reste pur).
    - `grep -c "QuittanceBuilderPdfmake" src/main.ts` retourne >= 2 (import + new).
    - `grep -c "quittanceBuilder" src/main.ts` retourne >= 2 (variable + propagation `app.register`).
    - `grep -c "quittanceBuilder" src/web/routes/quittances.ts` retourne >= 3 (import type + interface plugin opts + propagation au call).
    - `pnpm typecheck` exit code 0.
    - `pnpm test tests/unit/encaissements/generer-quittance.test.ts -- --run` exit code 0 (suite locale verte, 10+ tests).
    - `pnpm test -- --run` exit code 0 (suite complete verte, 888+ tests).
  </acceptance_criteria>

  <done>
    Le port QuittanceBuilder + adapter sont en place. generer-quittance.ts n'importe plus aucun module value depuis infrastructure (le seul reste est `type DB` Kysely, type-only acceptable). L'import parasite `StockageFichierLocal` est supprime — remplace par la fonction pure `slugify` du domaine. La route quittances.ts propage le port. main.ts instancie l'adapter. La suite complete (vitest + BDD) reste verte. Commit atomique pousse.
  </done>
</task>

<task type="auto">
  <name>Task 2 : Hardening AvenantIRLBuilder — port + adapter + use case (Infra etendue) + consolider PdfRendererLike + route + DI + tests</name>
  <files>src/domain/locatif/avenant-irl-builder.ts, src/infrastructure/pdf/avenant-irl-builder-pdfmake.ts, src/application/locatif/appliquer-indexation-irl.ts, src/web/routes/indexations.ts, src/main.ts, tests/unit/locatif/appliquer-indexation-irl.test.ts, tests/bdd/step_definitions/indexation-irl.steps.ts</files>

  <read_first>
    <file>src/domain/encaissements/pdf-renderer.ts</file>
    <file>src/domain/encaissements/quittance-builder.ts</file>
    <file>src/infrastructure/pdf/quittance-builder-pdfmake.ts</file>
    <file>src/infrastructure/pdf/avenant-irl-doc-def.ts</file>
    <file>src/application/locatif/appliquer-indexation-irl.ts</file>
    <file>src/web/routes/indexations.ts</file>
    <file>src/main.ts</file>
    <file>tests/unit/locatif/appliquer-indexation-irl.test.ts</file>
    <file>tests/bdd/step_definitions/indexation-irl.steps.ts</file>
  </read_first>

  <action>
    1. CREER `src/domain/locatif/avenant-irl-builder.ts` — meme structure JSDoc que QuittanceBuilder (Task 1) mais reference `construireAvenantIRL` cote infra. La signature de `construireAvenantIRL` est visible dans `src/infrastructure/pdf/avenant-irl-doc-def.ts` et utilisee L183-192 de `appliquer-indexation-irl.ts` : `construireAvenantIRL(bail: Bail, locataire: Locataire, bailleur: Bailleur, irlNouveau: IRL, irlAncien: IRL, loyerAvant: Money, loyerApres: Money, dateEffet: Temporal.PlainDate)`. Le port expose : `interface AvenantIRLBuilder { construire(bail: Bail, locataire: Locataire, bailleur: Bailleur, irlNouveau: IRL, irlAncien: IRL, loyerAvant: Money, loyerApres: Money, dateEffet: Temporal.PlainDate): unknown; }`. JSDoc precise le retour `unknown` (TDocumentDefinitions masque).

    2. CREER `src/infrastructure/pdf/avenant-irl-builder-pdfmake.ts` — classe `AvenantIRLBuilderPdfmake implements AvenantIRLBuilder` qui delegue : `return construireAvenantIRL(bail, locataire, bailleur, irlNouveau, irlAncien, loyerAvant, loyerApres, dateEffet);`. Memes patterns d'imports que pour Task 1 (port domaine + VO + import value de la fonction construire infra).

    3. MODIFIER `src/application/locatif/appliquer-indexation-irl.ts` :
       - L26 : SUPPRIMER `import { construireAvenantIRL } from '../../infrastructure/pdf/avenant-irl-doc-def.js';`
       - Apres les imports domaine (autour de L20) : AJOUTER `import type { AvenantIRLBuilder } from '../../domain/locatif/avenant-irl-builder.js';`
       - CONSOLIDATION (bonus de coherence) : L52-54 (`interface PdfRendererLike { genererBuffer(docDef: unknown): Promise<Buffer>; }`) est strictement equivalent au port `PdfRenderer` deja en place dans `src/domain/encaissements/pdf-renderer.ts`. SUPPRIMER cette interface locale + AJOUTER `import type { PdfRenderer } from '../../domain/encaissements/pdf-renderer.js';` en tete de fichier. L60-64 (interface `Infra`) : remplacer `pdfRenderer: PdfRendererLike;` par `pdfRenderer: PdfRenderer;`.
       - L60-64 (interface `Infra`) : ajouter le champ `avenantIRLBuilder: AvenantIRLBuilder;` apres `pdfRenderer: PdfRenderer;`. Nouvelle forme :
         `interface Infra { pdfRenderer: PdfRenderer; avenantIRLBuilder: AvenantIRLBuilder; stockage: StockageLike; clock: Clock; }`
       - L183-192 (call `construireAvenantIRL`) : remplacer par `const docDef = infra.avenantIRLBuilder.construire(bailModifie, locataire, bailleur, irlNouveau, bail.irlReference, bail.loyerHc, sim.nouveauLoyerHc, dateEffet);`. NE PAS toucher au reste du fichier — la signature publique reste identique (3e param `infra: Infra` toujours en place, juste enrichi).
       - NOTE : ne PAS toucher `interface StockageLike` L56-58 — pas dans le scope (verifier par grep dans le domaine, mais probablement aucune interface `Stockage` cote locatif. Hors scope).

    4. MODIFIER `src/web/routes/indexations.ts` :
       - Apres L15 (`import type { PdfRenderer }`) : AJOUTER `import type { AvenantIRLBuilder } from '../../domain/locatif/avenant-irl-builder.js';`
       - L52-66 (interface plugin opts) : apres `pdfRenderer?: PdfRenderer;` (L63), AJOUTER `avenantIRLBuilder?: AvenantIRLBuilder;` (optionnel comme pdfRenderer, meme convention).
       - L240-253 (guard pre-call) : ajouter `!opts.avenantIRLBuilder ||` dans la liste de garde (entre `!opts.pdfRenderer ||` L247 et `!opts.stockage ||` L248).
       - L263-281 (call `appliquerIndexationIRL`) : dans le 3e param objet `infra` (L279 `{ pdfRenderer: opts.pdfRenderer, stockage: opts.stockage, clock: opts.clock }`), AJOUTER `avenantIRLBuilder: opts.avenantIRLBuilder,` entre `pdfRenderer` et `stockage`. Forme finale : `{ pdfRenderer: opts.pdfRenderer, avenantIRLBuilder: opts.avenantIRLBuilder, stockage: opts.stockage, clock: opts.clock }`.

    5. MODIFIER `src/main.ts` :
       - Apres l'import `QuittanceBuilderPdfmake` (ajoute en Task 1) : AJOUTER `import { AvenantIRLBuilderPdfmake } from './infrastructure/pdf/avenant-irl-builder-pdfmake.js';`
       - Apres `const quittanceBuilder = new QuittanceBuilderPdfmake();` (ajoute en Task 1) : AJOUTER `const avenantIRLBuilder = new AvenantIRLBuilderPdfmake();`
       - Dans `app.register(indexationsPlugin, {...})` (L237-249) : INSERER `avenantIRLBuilder,` JUSTE APRES `pdfRenderer,` (autour de L245). Shorthand.

    6. MODIFIER `tests/unit/locatif/appliquer-indexation-irl.test.ts` :
       - Ajouter `import { AvenantIRLBuilderPdfmake } from '../../../src/infrastructure/pdf/avenant-irl-builder-pdfmake.js';` (a cote des autres imports infra).
       - Dans la fonction `setupCtx` (lire les lignes ~50-115 pour reperer le `ctx` builder) : ajouter `avenantIRLBuilder: new AvenantIRLBuilderPdfmake()` au type de retour `Ctx` + a l'objet retourne (a cote de `pdfRenderer` et `stockage` deja presents).
       - Dans CHAQUE call a `appliquerIndexationIRL(...)` (4 sites L129, L170, L193, L217 — utiliser grep pour confirmation) : modifier le 3e param objet `infra` pour ajouter `avenantIRLBuilder: ctx.avenantIRLBuilder` apres `pdfRenderer: ctx.pdfRenderer` (sauf au site L233 qui utilise un `pdfStub` local — la, ajouter `avenantIRLBuilder: ctx.avenantIRLBuilder` comme valeur partagee).

    7. MODIFIER `tests/bdd/step_definitions/indexation-irl.steps.ts` :
       - grep `appliquerIndexationIRL` dans ce fichier. Probabilite : 1 site d'appel. Propager le meme fix (ajout de `avenantIRLBuilder: new AvenantIRLBuilderPdfmake()` ou via la variable du ctx du step) dans le 3e param `infra`.
       - Si la suite utilise un setup partage (ex. `before/beforeEach`), instancier l'adapter UNE fois dans ce hook et le passer dans chaque call.

    8. APRES TOUTES LES MODIFS : `pnpm typecheck` puis `pnpm test tests/unit/locatif/appliquer-indexation-irl.test.ts -- --run` puis `pnpm test -- --run` (suite complete). Si le typecheck remonte des callers supplementaires non listes (probabilite > 0 — c'est le pattern Rule 3 du plan 05-11), propager dans les fichiers signales avant commit.

    COMMIT ATOMIQUE : `feat(05.1): port AvenantIRLBuilder + adapter pdfmake + consolide PdfRendererLike (closes deferred 3/4)`.
  </action>

  <verify>
    <automated>pnpm typecheck 2>&1 | tail -5 ; test -f src/domain/locatif/avenant-irl-builder.ts && echo "port OK" ; test -f src/infrastructure/pdf/avenant-irl-builder-pdfmake.ts && echo "adapter OK" ; grep -c "from '../../infrastructure" src/application/locatif/appliquer-indexation-irl.ts ; grep -c "construireAvenantIRL" src/application/locatif/appliquer-indexation-irl.ts ; grep -c "PdfRendererLike" src/application/locatif/appliquer-indexation-irl.ts ; grep -c "avenantIRLBuilder" src/main.ts ; pnpm test tests/unit/locatif/appliquer-indexation-irl.test.ts -- --run 2>&1 | tail -5 ; pnpm test -- --run 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `test -f src/domain/locatif/avenant-irl-builder.ts` exit 0.
    - `test -f src/infrastructure/pdf/avenant-irl-builder-pdfmake.ts` exit 0.
    - `grep -c "export interface AvenantIRLBuilder" src/domain/locatif/avenant-irl-builder.ts` retourne 1.
    - `grep -c "implements AvenantIRLBuilder" src/infrastructure/pdf/avenant-irl-builder-pdfmake.ts` retourne 1.
    - `grep -c "from.*infrastructure" src/application/locatif/appliquer-indexation-irl.ts` retourne 1 (uniquement `type DB` Kysely acceptable). Aucun import value depuis infrastructure.
    - `grep -c "construireAvenantIRL" src/application/locatif/appliquer-indexation-irl.ts` retourne 0.
    - `grep -c "PdfRendererLike" src/application/locatif/appliquer-indexation-irl.ts` retourne 0 (interface locale supprimee, consolidee avec le port domaine).
    - `grep -c "PdfRenderer" src/application/locatif/appliquer-indexation-irl.ts` retourne >= 2 (import type + usage dans Infra).
    - `grep -c "avenantIRLBuilder" src/application/locatif/appliquer-indexation-irl.ts` retourne >= 2 (import type + appel via infra.avenantIRLBuilder.construire).
    - `grep -c "from.*infrastructure\\|from 'pdfmake" src/domain/locatif/avenant-irl-builder.ts` retourne 0 (port pur).
    - `grep -c "AvenantIRLBuilderPdfmake" src/main.ts` retourne >= 2 (import + new).
    - `grep -c "avenantIRLBuilder" src/main.ts` retourne >= 2 (var + propagation).
    - `grep -c "avenantIRLBuilder" src/web/routes/indexations.ts` retourne >= 4 (import type + interface plugin opts + guard + propagation au call).
    - `pnpm typecheck` exit code 0.
    - `pnpm test tests/unit/locatif/appliquer-indexation-irl.test.ts -- --run` exit code 0.
    - `pnpm test -- --run` exit code 0 (suite complete verte).
  </acceptance_criteria>

  <done>
    Port AvenantIRLBuilder + adapter en place. appliquer-indexation-irl.ts n'importe plus aucun module value depuis infrastructure (seul reste type DB acceptable). L'interface dupliquee PdfRendererLike est consolidee avec le port domaine PdfRenderer (bonus coherence). La route indexations.ts propage le port. main.ts instancie l'adapter. Tests + BDD verts. Commit atomique pousse.
  </done>
</task>

<task type="auto">
  <name>Task 3 : Hardening MiseEnDemeureBuilder — port + adapter + use case + route (use case ET PDF inline) + DI + tests</name>
  <files>src/domain/encaissements/mise-en-demeure-builder.ts, src/infrastructure/pdf/mise-en-demeure-builder-pdfmake.ts, src/application/encaissements/enregistrer-relance.ts, src/web/routes/relances.ts, src/main.ts, tests/unit/encaissements/enregistrer-relance.test.ts, tests/integration/web/relances-mailto.test.ts</files>

  <read_first>
    <file>src/domain/encaissements/pdf-renderer.ts</file>
    <file>src/domain/encaissements/quittance-builder.ts</file>
    <file>src/infrastructure/pdf/mise-en-demeure-doc-def.ts</file>
    <file>src/application/encaissements/enregistrer-relance.ts</file>
    <file>src/web/routes/relances.ts</file>
    <file>src/main.ts</file>
    <file>tests/unit/encaissements/enregistrer-relance.test.ts</file>
    <file>tests/integration/web/relances-mailto.test.ts</file>
  </read_first>

  <action>
    1. CREER `src/domain/encaissements/mise-en-demeure-builder.ts` — meme structure JSDoc que les autres ports. La signature de `construireMiseEnDemeure` est visible L163-173 de `enregistrer-relance.ts` ET L169-179 de `relances.ts` (route) : `construireMiseEnDemeure(echeance: EcheanceLoyer, encaissementsLies: Encaissement[], bailleur: Bailleur, locataire: Locataire, bien: Bien, adresseBien: Adresse, bail: Bail, resteDu: Money, aujourdhui: Temporal.PlainDate)`. Port : `interface MiseEnDemeureBuilder { construire(echeance: EcheanceLoyer, encaissementsLies: Encaissement[], bailleur: Bailleur, locataire: Locataire, bien: Bien, adresseBien: Adresse, bail: Bail, resteDu: Money, aujourdhui: Temporal.PlainDate): unknown; }`. JSDoc : noter que ce port est consomme par DEUX endroits — le use case `enregistrerRelance` ET la route `GET /relances/:id/pdf` (regeneration on-the-fly).

    2. CREER `src/infrastructure/pdf/mise-en-demeure-builder-pdfmake.ts` — classe `MiseEnDemeureBuilderPdfmake implements MiseEnDemeureBuilder` qui delegue : `return construireMiseEnDemeure(echeance, encaissementsLies, bailleur, locataire, bien, adresseBien, bail, resteDu, aujourdhui);`.

    3. MODIFIER `src/application/encaissements/enregistrer-relance.ts` :
       - L17 : SUPPRIMER `import { construireMiseEnDemeure } from '../../infrastructure/pdf/mise-en-demeure-doc-def.js';`
       - Apres l'import `PdfRenderer` (L8) : AJOUTER `import type { MiseEnDemeureBuilder } from '../../domain/encaissements/mise-en-demeure-builder.js';`
       - L59-65 (signature `enregistrerRelance`) : ajouter un parametre `miseEnDemeureBuilder: MiseEnDemeureBuilder` JUSTE APRES `pdfRenderer`. Nouvelle signature exacte :
         `export async function enregistrerRelance(commande: { echeanceId: EcheanceLoyerId | string; niveau: NiveauRelance }, repos: Repos, templateRenderer: TemplateRenderer, pdfRenderer: PdfRenderer, miseEnDemeureBuilder: MiseEnDemeureBuilder, clock: Clock): Promise<ResultatEnregistrerRelance>`
       - L163-173 (call `construireMiseEnDemeure`) : remplacer par `const docDef = miseEnDemeureBuilder.construire(echeance, encaissementsLies, bailleur, locataire, bien, bien.adresse, bail, resteDu, clock.aujourdhui());`.

    4. MODIFIER `src/web/routes/relances.ts` :
       - L19 : SUPPRIMER `import { construireMiseEnDemeure } from '../../infrastructure/pdf/mise-en-demeure-doc-def.js';`
       - Apres L15 (`import type { PdfRenderer }`) : AJOUTER `import type { MiseEnDemeureBuilder } from '../../domain/encaissements/mise-en-demeure-builder.js';`
       - L27-39 (interface plugin opts) : apres `pdfRenderer: PdfRenderer;` (L37), AJOUTER `miseEnDemeureBuilder: MiseEnDemeureBuilder;`.
       - L169-179 (call `construireMiseEnDemeure` dans GET /relances/:id/pdf) : remplacer par `const docDef = opts.miseEnDemeureBuilder.construire(echeance, await opts.encaissementRepo.listerParEcheance(echeance.id), bailleur, locataire, bien, bien.adresse, bail, resteDu, opts.clock.aujourdhui());`.
       - Reperer dans relances.ts le call a `enregistrerRelance` (s'il existe — peut etre `POST /relances`). Lire les lignes autour pour reperer ce site. Si trouve : INSERER `opts.miseEnDemeureBuilder,` JUSTE APRES `pdfRenderer,` dans le call (nouvelle position 5e parametre). Si pas trouve dans relances.ts mais ailleurs (ex. echeances.ts ou impayes.ts), grep et propager.

    5. MODIFIER `src/main.ts` :
       - Apres l'import `AvenantIRLBuilderPdfmake` (ajoute en Task 2) : AJOUTER `import { MiseEnDemeureBuilderPdfmake } from './infrastructure/pdf/mise-en-demeure-builder-pdfmake.js';`
       - Apres `const avenantIRLBuilder = new AvenantIRLBuilderPdfmake();` (ajoute en Task 2) : AJOUTER `const miseEnDemeureBuilder = new MiseEnDemeureBuilderPdfmake();`
       - Dans `app.register(relancesPlugin, {...})` (L294-304) : INSERER `miseEnDemeureBuilder,` JUSTE APRES `pdfRenderer,` (autour de L302). Shorthand.

    6. MODIFIER `tests/unit/encaissements/enregistrer-relance.test.ts` :
       - Ajouter `import { MiseEnDemeureBuilderPdfmake } from '../../../src/infrastructure/pdf/mise-en-demeure-builder-pdfmake.js';` (ou un stub vi.fn() — au choix selon la convention deja en place pour `pdfRendererStub`).
       - Apres `pdfRendererStub` (L74) : DECLARER `const miseEnDemeureBuilderStub = { construire: vi.fn().mockReturnValue({}) };`.
       - Dans CHAQUE call a `enregistrerRelance(...)` (3 sites L117, L133, L151) : INSERER `miseEnDemeureBuilderStub,` JUSTE APRES `pdfRendererStub,` (nouvelle 5e position).

    7. MODIFIER `tests/integration/web/relances-mailto.test.ts` :
       - grep pour reperer les callers. Si la route relances.ts est testee directement, son interface plugin opts a change (ajout de `miseEnDemeureBuilder`). Mettre a jour le setup du test pour injecter `new MiseEnDemeureBuilderPdfmake()` (ou un stub) dans les opts du plugin.

    8. APRES TOUTES LES MODIFS : `pnpm typecheck` puis `pnpm test tests/unit/encaissements/enregistrer-relance.test.ts -- --run` puis `pnpm test tests/integration/web/relances-mailto.test.ts -- --run` puis `pnpm test -- --run` (suite complete). Propager Rule 3 si typecheck signale d'autres callers (probabilite moderee : la route est testee aussi par d'autres BDD steps).

    COMMIT ATOMIQUE : `feat(05.1): port MiseEnDemeureBuilder + adapter pdfmake + aligne route relances.ts (closes deferred 4/4)`.
  </action>

  <verify>
    <automated>pnpm typecheck 2>&1 | tail -5 ; test -f src/domain/encaissements/mise-en-demeure-builder.ts && echo "port OK" ; test -f src/infrastructure/pdf/mise-en-demeure-builder-pdfmake.ts && echo "adapter OK" ; grep -c "from '../../infrastructure" src/application/encaissements/enregistrer-relance.ts ; grep -c "construireMiseEnDemeure" src/application/encaissements/enregistrer-relance.ts ; grep -c "construireMiseEnDemeure" src/web/routes/relances.ts ; grep -c "MiseEnDemeureBuilderPdfmake" src/main.ts ; pnpm test tests/unit/encaissements/enregistrer-relance.test.ts -- --run 2>&1 | tail -5 ; pnpm test -- --run 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `test -f src/domain/encaissements/mise-en-demeure-builder.ts` exit 0.
    - `test -f src/infrastructure/pdf/mise-en-demeure-builder-pdfmake.ts` exit 0.
    - `grep -c "export interface MiseEnDemeureBuilder" src/domain/encaissements/mise-en-demeure-builder.ts` retourne 1.
    - `grep -c "implements MiseEnDemeureBuilder" src/infrastructure/pdf/mise-en-demeure-builder-pdfmake.ts` retourne 1.
    - `grep -c "from.*infrastructure" src/application/encaissements/enregistrer-relance.ts` retourne 0 (aucun reste — ce fichier n'avait pas non plus de Kysely type-only).
    - `grep -c "construireMiseEnDemeure" src/application/encaissements/enregistrer-relance.ts` retourne 0.
    - `grep -c "construireMiseEnDemeure" src/web/routes/relances.ts` retourne 0.
    - `grep -c "MiseEnDemeureBuilder" src/application/encaissements/enregistrer-relance.ts` retourne >= 2 (import + signature).
    - `grep -c "miseEnDemeureBuilder.construire" src/application/encaissements/enregistrer-relance.ts` retourne 1.
    - `grep -c "miseEnDemeureBuilder" src/web/routes/relances.ts` retourne >= 3 (import type + interface plugin opts + propagation).
    - `grep -c "from.*infrastructure\\|from 'pdfmake" src/domain/encaissements/mise-en-demeure-builder.ts` retourne 0 (port pur).
    - `grep -c "MiseEnDemeureBuilderPdfmake" src/main.ts` retourne >= 2 (import + new).
    - `grep -c "miseEnDemeureBuilder" src/main.ts` retourne >= 2 (var + propagation).
    - `pnpm typecheck` exit code 0.
    - `pnpm test tests/unit/encaissements/enregistrer-relance.test.ts -- --run` exit code 0.
    - `pnpm test -- --run` exit code 0 (suite complete verte).
  </acceptance_criteria>

  <done>
    Port MiseEnDemeureBuilder + adapter en place. enregistrer-relance.ts ET relances.ts (route) n'importent plus le module `mise-en-demeure-doc-def` directement. La DI est complete dans main.ts. Tests + BDD verts. Commit atomique pousse.
  </done>
</task>

<task type="auto">
  <name>Task 4 : Finaliser deferred-items.md + verification globale + commit final</name>
  <files>.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/deferred-items.md</files>

  <read_first>
    <file>.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/deferred-items.md</file>
    <file>src/application/encaissements/generer-quittance.ts</file>
    <file>src/application/locatif/appliquer-indexation-irl.ts</file>
    <file>src/application/encaissements/enregistrer-relance.ts</file>
  </read_first>

  <action>
    1. METTRE A JOUR `.planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/deferred-items.md` :
       Au-dessus du tableau des 4 violations (entre L13 "Same fix shape..." et L15 le tableau), AJOUTER une section de cloture :

       `> **STATUT 2026-05-22 — CLOSED by Phase 5.1.** Les 4 violations listees ci-dessous ont ete fermees par le plan 05.1-01-pdf-builder-ports-hardening (3 ports + 3 adapters + DI). Voir 05.1-01-pdf-builder-ports-hardening-SUMMARY.md pour les diffs et commits.`

       Optionnellement (preference de l'executor), ajouter une colonne "Status" au tableau avec la valeur `closed by Phase 5.1, see 05.1-01-SUMMARY.md` pour chacune des 4 lignes. Choisir UNE des deux approches (entete OU colonne) — pas les deux.

    2. EXECUTER LA VERIFICATION FINALE GLOBALE (capturer dans `/tmp/05.1-task4-final.log`) :

       a. Verification du grep gate global hexagonal — la cible cle de cette phase :
          `grep -rn "from.*infrastructure" src/application/ | grep -v "type.*Kysely\\|type { DB }"`
          Attendu : 0 ligne. La regle CLAUDE.md hexagonale est respectee de bout en bout. Tout reste devrait etre uniquement les `type { DB } from '../../infrastructure/db/kysely-types.js'` filtrables par le grep -v ci-dessus (deja documente acceptable dans 05-VERIFICATION.md L32).

       b. Verification specifique fichier-par-fichier (les 3 cibles primaires de la phase) :
          `grep -rn "from '../../infrastructure/pdf/quittance-doc-def\\|from '../../infrastructure/pdf/avenant-irl-doc-def\\|from '../../infrastructure/pdf/mise-en-demeure-doc-def\\|from '../../infrastructure/storage/stockage-fichier-local" src/application src/domain src/web/routes`
          Attendu : SEULEMENT les lignes legitimes cote infrastructure-meme et routes/quittances.ts (qui peut importer StockageFichierLocal comme TYPE, c'est legitime cote web/route, ce n'est pas le coeur application). En particulier, ZERO ligne dans `src/application/`.

       c. Tests + typecheck :
          `pnpm typecheck` exit 0
          `pnpm test -- --run` exit 0 (888+ vitest)
          `pnpm bdd 2>&1 | tail -10` doit montrer >= 171 passes / 173 (les 2 echecs fiscalite-qualification.feature heritees sont independants — meme baseline que SUMMARY 05-11).

       d. Verification que les 6 nouveaux fichiers existent et sont commitos :
          `for f in src/domain/encaissements/quittance-builder.ts src/infrastructure/pdf/quittance-builder-pdfmake.ts src/domain/locatif/avenant-irl-builder.ts src/infrastructure/pdf/avenant-irl-builder-pdfmake.ts src/domain/encaissements/mise-en-demeure-builder.ts src/infrastructure/pdf/mise-en-demeure-builder-pdfmake.ts; do test -f "$f" && echo "OK $f" || echo "MISSING $f"; done`
          Attendu : 6 lignes "OK".

       e. Verification que les ports restent purs (aucune fuite pdfmake / infra) :
          `grep -rEn "from 'pdfmake|from \"pdfmake|from.*infrastructure" src/domain/encaissements/quittance-builder.ts src/domain/locatif/avenant-irl-builder.ts src/domain/encaissements/mise-en-demeure-builder.ts`
          Attendu : 0 ligne.

       f. Verification que main.ts instancie bien les 3 nouveaux adapters :
          `grep -c "new \\(QuittanceBuilderPdfmake\\|AvenantIRLBuilderPdfmake\\|MiseEnDemeureBuilderPdfmake\\)" src/main.ts`
          Attendu : >= 3.

    3. Si TOUTES les verifications passent, COMMIT FINAL : `docs(05.1): close deferred-items + phase 5.1 hardening hexagonal complete`. Ce commit contient uniquement la modification de deferred-items.md.

    4. NE PAS modifier STATE.md ni ROADMAP.md — c'est l'orchestrateur (post-execution) qui les met a jour. Ne PAS marquer la phase comme complete dans la roadmap.
  </action>

  <verify>
    <automated>grep -rn "from.*infrastructure" src/application/ | grep -v "type.*Kysely\|type { DB }" | wc -l ; grep -c "closed by Phase 5.1\|CLOSED by Phase 5.1" .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/deferred-items.md ; for f in src/domain/encaissements/quittance-builder.ts src/infrastructure/pdf/quittance-builder-pdfmake.ts src/domain/locatif/avenant-irl-builder.ts src/infrastructure/pdf/avenant-irl-builder-pdfmake.ts src/domain/encaissements/mise-en-demeure-builder.ts src/infrastructure/pdf/mise-en-demeure-builder-pdfmake.ts; do test -f "$f" && echo "OK" || echo "MISS"; done ; pnpm typecheck 2>&1 | tail -3 ; pnpm test -- --run 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn "from.*infrastructure" src/application/ | grep -v "type.*Kysely\\|type { DB }" | wc -l` retourne 0 (la ligne wc-l doit etre exactement `0` apres trim — c'est le grep gate central de Phase 5.1).
    - `grep -c "closed by Phase 5.1\\|CLOSED by Phase 5.1" .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/deferred-items.md` retourne >= 1.
    - Les 6 nouveaux fichiers existent (test -f exit 0 pour chacun).
    - `grep -rEn "from 'pdfmake|from \"pdfmake|from.*infrastructure" src/domain/encaissements/quittance-builder.ts src/domain/locatif/avenant-irl-builder.ts src/domain/encaissements/mise-en-demeure-builder.ts` retourne 0 ligne (les 3 ports restent purs).
    - `grep -c "new \\(QuittanceBuilderPdfmake\\|AvenantIRLBuilderPdfmake\\|MiseEnDemeureBuilderPdfmake\\)" src/main.ts` retourne >= 3.
    - `pnpm typecheck` exit code 0.
    - `pnpm test -- --run` exit code 0. Le compteur final montre 888+ tests passes (parite ou mieux avec la baseline du SUMMARY 05-11).
    - Commit `docs(05.1): close deferred-items + phase 5.1 hardening hexagonal complete` cree dans le worktree.
    - Le SUMMARY (cree par execute-plan en post-task) liste les 4 commits atomiques (Task 1, Task 2, Task 3, Task 4) et confirme les 4 lignes ferms.
  </acceptance_criteria>

  <done>
    deferred-items.md note la cloture des 4 violations. Grep gate global retourne 0 violations residuelles dans src/application/ (hors type Kysely acceptables). Les 6 nouveaux fichiers existent. Les 3 ports sont purs. Typecheck + suite complete verts. Commit final pousse. La phase 5.1 est techniquement complete — l'orchestrateur peut maintenant la fermer dans STATE.md / ROADMAP.md.
  </done>
</task>

</tasks>

<verification>
1. Grep gate hexagonal global — le critere de succes central de Phase 5.1 :
   ```
   grep -rn "from.*infrastructure" src/application/ | grep -v "type.*Kysely\|type { DB }"
   # attendu : 0 resultat (la chaine de violations est entierement fermee)
   ```

2. Les 6 nouveaux fichiers (3 ports + 3 adapters) existent et sont commitos :
   ```
   test -f src/domain/encaissements/quittance-builder.ts
   test -f src/infrastructure/pdf/quittance-builder-pdfmake.ts
   test -f src/domain/locatif/avenant-irl-builder.ts
   test -f src/infrastructure/pdf/avenant-irl-builder-pdfmake.ts
   test -f src/domain/encaissements/mise-en-demeure-builder.ts
   test -f src/infrastructure/pdf/mise-en-demeure-builder-pdfmake.ts
   # attendu : exit 0 pour les 6
   ```

3. Les 3 ports sont strictement purs (aucune fuite pdfmake / infra dans le domaine) :
   ```
   grep -rEn "from 'pdfmake|from \"pdfmake|from.*infrastructure" \
     src/domain/encaissements/quittance-builder.ts \
     src/domain/locatif/avenant-irl-builder.ts \
     src/domain/encaissements/mise-en-demeure-builder.ts
   # attendu : 0 resultat
   ```

4. DI complete dans main.ts pour les 3 nouveaux adapters :
   ```
   grep -c "new \(QuittanceBuilderPdfmake\|AvenantIRLBuilderPdfmake\|MiseEnDemeureBuilderPdfmake\)" src/main.ts
   # attendu : >= 3
   ```

5. Suite complete + typecheck :
   ```
   pnpm typecheck
   pnpm test -- --run
   pnpm bdd 2>&1 | tail -5
   # attendu : exit 0 pour les deux premiers, >= 171/173 pour BDD (les 2 echecs fiscalite-qualification.feature sont hérités)
   ```

6. deferred-items.md marque les 4 violations comme closed :
   ```
   grep -c "closed by Phase 5.1\|CLOSED by Phase 5.1" \
     .planning/phases/05-fiscalit-lmnp-r-gimes-recettes-charges-amortissement/deferred-items.md
   # attendu : >= 1
   ```

7. Ni `StockageFichierLocal` ni les 3 fonctions `construire*` ne sont importees dans `src/application/` :
   ```
   grep -rn "StockageFichierLocal\|construireQuittance\|construireAvenantIRL\|construireMiseEnDemeure" src/application/
   # attendu : 0 resultat
   ```

8. Les 3 routes propagent les ports (aucun import direct des doc-def) :
   ```
   grep -n "construireQuittance\|construireAvenantIRL\|construireMiseEnDemeure" \
     src/web/routes/quittances.ts src/web/routes/indexations.ts src/web/routes/relances.ts
   # attendu : 0 resultat
   ```
</verification>

<success_criteria>
- Les 4 violations hexagonales pre-existantes loggees dans deferred-items.md L17-L20 sont fermees.
- Regle non-negociable CLAUDE.md (Domaine pur — Ports & Adapters strict) respectee de bout en bout : `grep -rn "from.*infrastructure" src/application/ | grep -v "type.*Kysely\|type { DB }"` retourne 0 ligne.
- 3 nouveaux ports dans `src/domain/` (QuittanceBuilder, AvenantIRLBuilder, MiseEnDemeureBuilder), 3 nouveaux adapters dans `src/infrastructure/pdf/`. Tous retournent `unknown` pour preserver la purete du domaine (miroir de PdfRenderer.genererBuffer et RecapFiscalBuilder.construire).
- DI complete dans main.ts : 3 instances cree + 3 propagations dans `app.register(...)` (quittancesPlugin, indexationsPlugin, relancesPlugin).
- Les 3 routes Fastify (quittances.ts, indexations.ts, relances.ts) ont leur interface plugin opts etendue avec le port + propagent le port au use case ou l'utilisent directement pour la PDF on-the-fly de mise en demeure (route GET /relances/:id/pdf).
- `StockageFichierLocal` n'est plus importe dans `src/application/` — l'appel a la methode statique slugify est remplace par la fonction pure `slugify` deja exportee depuis `src/domain/_shared/slug.ts`. Le pattern d'injection du STOCKAGE (interface `Stockage` inline) reste inchange.
- Bonus consolidation : l'interface `PdfRendererLike` dupliquee dans appliquer-indexation-irl.ts L52-54 est supprimee et remplacee par l'import du port domaine `PdfRenderer`.
- Tests + BDD verts : `pnpm typecheck` exit 0, `pnpm test -- --run` exit 0 (888+ tests vitest), `pnpm bdd` >= 171/173 (les 2 echecs heritages restent constants).
- deferred-items.md marque les 4 violations comme `closed by Phase 5.1`.
- 4 commits atomiques pousses dans le worktree (Task 1, Task 2, Task 3, Task 4).
- Aucune autre signature publique modifiee. Aucune regression comportementale (PDFs generes restent strictement identiques — les adapters wrappent les fonctions existantes en delegation pure).
</success_criteria>

<output>
Apres completion, creer `.planning/phases/05.1-hardening-hexagonal/05.1-01-pdf-builder-ports-hardening-SUMMARY.md` listant :
1. Diff des 3 nouveaux ports (signatures completes + JSDoc).
2. Diff des 3 nouveaux adapters.
3. Diff des 3 use cases application modifies (suppression imports infra + ajout signature port + appel via port).
4. Diff des 3 routes modifiees (interface plugin etendue + propagation).
5. Diff de main.ts (3 imports + 3 instances + 3 propagations).
6. Diff de chaque fichier de test modifie.
7. Diff de deferred-items.md (cloture).
8. Resultats de `pnpm typecheck`, `pnpm test -- --run`, `pnpm bdd`.
9. Liste des 4 commits atomiques.
10. Confirmer que `grep -rn "from.*infrastructure" src/application/ | grep -v "type.*Kysely\|type { DB }"` retourne 0 ligne.
11. Statut Phase 5.1 : les 4 violations deferred sont closed, la regle hexagonale CLAUDE.md est respectee de bout en bout.
</output>
