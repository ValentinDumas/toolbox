---
phase: 09-finition-qualit-cl-ture-uat-r-conciliation-des-statuts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-liasse-2031-cfe/06-UAT.md
autonomous: true
requirements: [QUA-01]
user_setup: []

must_haves:
  truths:
    - "Les 10 scénarios mécaniquement vérifiables de la liasse 2031/CFE (1-7, 10, 11, 12) sont exécutés via Playwright et leur résultat est consigné dans 06-UAT.md"
    - "Aucun de ces scénarios ne reste au statut pending — chacun est pass, pass-with-note ou issue"
    - "Tout écart bloquant découvert (chiffre fiscal faux, case manquante, crash) est corrigé en Phase 9 et le scénario repassé au vert"
    - "Tout écart cosmétique non-bloquant est consigné en backlog et le scénario marqué pass-with-note (jamais pending)"
  artifacts:
    - path: ".planning/phases/06-liasse-2031-cfe/06-UAT.md"
      provides: "Résultats d'exécution des scénarios automatisables 1-7, 10, 11, 12"
      contains: "result: pass"
  key_links:
    - from: "Playwright (qa-pass)"
      to: "app live (pnpm dev, /fiscalite + /fiscalite/declarations/:id/liasse + fiche bien CFE)"
      via: "navigation UI + assertions DOM/téléchargement"
      pattern: "/fiscalite"
---

<objective>
Exécuter la partie **mécaniquement vérifiable** des 12 scénarios d'UAT humaine de la liasse 2031/CFE (Phase 06) via Playwright (skill qa-pass), et consigner les résultats en place dans `06-UAT.md`. Couvre les scénarios 1-7, 10, 11, 12 (cold start, blocs et cases liasse, drill-down, bandeaux réconciliation/rectificatif, déclenchement des téléchargements, création/édition CFE, carte+badge CFE, banner CFE J-30). Les scénarios 8 et 9 (lisibilité PDF, accents CSV) sont **perceptuels** et traités au plan 09-03 (humain).

Purpose: Lever le statut `paused` de la Phase 06 sur tout ce qui est automatisable, sans faux-positif perceptuel. Premier des deux critères de succès ROADMAP §Phase 9 (#1 + #2).
Output: `06-UAT.md` mis à jour scénario par scénario (statut réel + note), aucun scénario automatisable laissé en `pending`.
</objective>

<execution_context>
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/workflows/execute-plan.md
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-finition-qualit-cl-ture-uat-r-conciliation-des-statuts/09-CONTEXT.md
@.planning/phases/06-liasse-2031-cfe/06-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cold start + exécution Playwright des scénarios liasse réel/micro-BIC (sc. 1-9 partie automatisable, 10-12)</name>
  <files>.planning/phases/06-liasse-2031-cfe/06-UAT.md</files>
  <read_first>
    - .planning/phases/06-liasse-2031-cfe/06-UAT.md (les 12 scénarios + leur `expected` — c'est la spec de test)
    - .planning/phases/09-finition-qualit-cl-ture-uat-r-conciliation-des-statuts/09-CONTEXT.md (D-01 périmètre hybride, D-04 tri de gravité)
    - package.json (scripts `dev`, `db:migrate`)
  </read_first>
  <action>
    Démarrer l'app proprement : DB recréée ou migrations rejouées (`pnpm db:migrate`), puis `pnpm dev` (Fastify, port défini par env PORT, défaut 7878 ; `SESSION_SECRET` requis au boot — utiliser `SESSION_SECRET=$(openssl rand -hex 32) PORT=7878 pnpm dev`). Si la DB est vide, créer le contexte minimal nécessaire (1 Bien, 1 déclaration annuelle régime réel + 1 micro-BIC, 1 déclaration CFE) via l'UI pour alimenter les brouillons de liasse.

    Via Playwright (skill qa-pass), exécuter et consigner le résultat de CHAQUE scénario automatisable de 06-UAT.md :
    - Sc.1 Cold start smoke : serveur démarre sans erreur, migration `0023_phase6_declaration_cfe.sql` appliquée, `GET /fiscalite` répond 200.
    - Sc.2 Bloc « Brouillons de liasse » sur /fiscalite : présence du bloc, lignes réel + micro-BIC avec suffixe `(réel)`/`(micro-BIC)`, chaque ligne liant vers `/fiscalite/declarations/:id/liasse`.
    - Sc.3 Brouillon réel : bandeau S1, H1 millésime+bailleur, 5 sections (2031-SD, 2033-A, 2033-B, 2033-C, 2033-D), chaque case affichant numéro + libellé officiel + valeur formatée €. Vérifier la présence des numéros de cases attendus (CB/CC, AN/AP/AQ/AT/AV, FC/FK/FX/FY/FZ/GA, KA-KF, WG/WH/WI).
    - Sc.4 Brouillon micro-BIC : unique section `2042-C-PRO`, case `5NI`, valeur = total recettes BRUTES (aucun abattement appliqué côté app).
    - Sc.5 Drill-down sources : `<details>` natif par case, « Voir N source(s) » déplie des liens internes ; cases sans source affichent `—`.
    - Sc.6 Bandeau réconciliation : modifier une recette/charge après clôture d'une déclaration (snapshot figé), recharger le brouillon, vérifier l'apparition d'un bandeau rouge « Réconciliation » + nombre de pièces modifiées, absence de bouton « Re-calculer ».
    - Sc.7 Liasse rectificative : depuis une DeclarationCorrigee, `/fiscalite/declarations-corrigees/:id/liasse` affiche bandeau jaune S6 + lien « Voir la déclaration originale » ; l'originale reste consultable sans ce bandeau.

    Pour les scénarios 8 (PDF) et 9 (CSV), exécuter UNIQUEMENT la partie automatisable et NE PAS clore le scénario ici (la lisibilité réelle est perceptuelle → plan 09-03) :
    - Sc.8 partie auto : cliquer « Télécharger PDF » déclenche bien un téléchargement nommé `brouillon-liasse-{exercice}.pdf` (ou `-rectificative-`). Marquer la partie auto OK mais laisser le scénario en attente de confirmation humaine (note explicite « partie download OK, lisibilité PDF → plan 09-03 »).
    - Sc.9 partie auto : cliquer « Télécharger CSV (expert-comptable) » déclenche `brouillon-liasse-{exercice}.csv`. Vérifier de manière programmatique sur le fichier téléchargé : présence du BOM UTF-8, séparateur `;`, colonnes `Annexe;Case;Libellé;Valeur;Sources`, et qu'aucune cellule ne commence par `=`/`+`/`-`/`@` sans préfixe `'` (assertion CSV injection — automatisable). Laisser la confirmation accents Excel/LibreOffice → plan 09-03.

    Consigner en place dans 06-UAT.md : pour chaque scénario, remplacer `result: [pending]` par le résultat réel. Appliquer D-04 : un écart **bloquant** (chiffre fiscal faux, case manquante, export cassé/corrompu, crash) → marquer `result: issue` + décrire l'écart dans la section Gaps (sera corrigé en Task 2) ; un écart **cosmétique non-bloquant** (CSS, libellé) → `result: pass-with-note` + note du backlog. Mettre à jour le bloc `## Summary` (passed / issues / pending / skipped) en conséquence. Les scénarios 8 et 9 restent `pending` à ce stade (clos au plan 09-03).
  </action>
  <verify>
    <automated>grep -c 'result: \[pending\]' .planning/phases/06-liasse-2031-cfe/06-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - L'app a démarré et `GET /fiscalite` a répondu 200 (sc.1 consigné pass ou issue, pas pending).
    - Les scénarios 1-7, 10, 11, 12 de 06-UAT.md ont un `result:` ≠ `[pending]` (pass, pass-with-note ou issue).
    - Les scénarios 8 et 9 portent une note « partie download/structure auto OK ; lisibilité/accents → plan 09-03 » et restent pending (seuls 2 pending résiduels admis après Task 1).
    - Sc.9 : l'assertion CSV injection (aucune cellule débutant par `=`/`+`/`-`/`@` sans préfixe `'`) a été exécutée programmatiquement et son résultat est consigné.
    - Le bloc `## Summary` reflète les compteurs réels.
    - `grep -c 'result: \[pending\]' 06-UAT.md` retourne au plus 2 (sc.8 et sc.9 uniquement).
  </acceptance_criteria>
  <done>Scénarios 1-7,10-12 exécutés et consignés ; 8-9 partie auto exécutée, perceptuel reporté plan 09-03 ; Summary à jour.</done>
</task>

<task type="auto">
  <name>Task 2: Exécution CFE (sc. 10-12) + correction des écarts bloquants découverts + re-green</name>
  <files>.planning/phases/06-liasse-2031-cfe/06-UAT.md</files>
  <read_first>
    - .planning/phases/06-liasse-2031-cfe/06-UAT.md (scénarios 10, 11, 12 + section Gaps remplie en Task 1)
    - .planning/phases/09-finition-qualit-cl-ture-uat-r-conciliation-des-statuts/09-CONTEXT.md (D-04 tri gravité, Integration Points : routes /fiscalite, fiche bien section CFE, migration 0023)
  </read_first>
  <action>
    Via Playwright, exécuter les 3 scénarios CFE et consigner le résultat dans 06-UAT.md :
    - Sc.10 Création + édition déclaration CFE : depuis la fiche d'un bien, « Nouvelle déclaration CFE », renseigner millésime (2020-2030), statut (`non_deposee`/`deposee`/`payee`/`exoneree_premiere_annee`/`exoneree_commune`), date de dépôt + montant si le statut le requiert ; soumettre ; vérifier l'apparition dans la liste CFE du bien ; éditer et vérifier la persistance.
    - Sc.11 Carte + badge statut CFE sur fiche bien : déclarations triées par millésime, badge coloré selon `StatutCfe` (libellé FR officiel), millésime formaté, montant le cas échéant.
    - Sc.12 Banner CFE J-30 (3 variantes) : avec une déclaration `non_deposee` (ou `deposee` non `payee`) dont l'échéance tombe dans [-60j, +30j], vérifier le banner sur la fiche bien ET sur /fiscalite (section « Échéances CFE ») ; 3 variantes selon jours restants (warning J-30→J-8, warning forte J-7→J-0, destructive J+1+) ; lien « Régler sur impots.gouv.fr » avec `target=_blank rel=noopener noreferrer` ; aucun banner si statut `payee`.

    Traiter les écarts BLOQUANTS recensés en Task 1 ET Task 2 selon D-04 : corriger le code (chiffre fiscal faux, case manquante, export cassé/corrompu, crash), respecter les règles projet (domaine pur, ubiquitous language français, pas de code métier sans test → ajouter le test de non-régression), puis **rejouer le scénario concerné via Playwright** et passer son `result:` à `pass`. Les écarts cosmétiques non-bloquants restent en `pass-with-note` + backlog (NE PAS les corriger ici — hors scope Phase 9 par D-04 et Deferred Ideas du CONTEXT). Si AUCUN écart bloquant n'a été découvert, le consigner explicitement (« 0 écart bloquant »). Mettre à jour `## Summary` et la section `## Gaps`.
  </action>
  <verify>
    <automated>grep -vE '^#|result: pass-with-note' .planning/phases/06-liasse-2031-cfe/06-UAT.md | grep -c 'result: issue'</automated>
  </verify>
  <acceptance_criteria>
    - Scénarios 10, 11, 12 ont un `result:` ≠ `[pending]`.
    - Tout scénario marqué `issue` en Task 1 pour cause d'écart BLOQUANT est soit repassé `pass` (fix livré + test de non-régression ajouté + scénario rejoué), soit le fix est hors scope documenté.
    - Si un fix a été livré : `pnpm test` reste vert (commande exécutée et résultat consigné dans le SUMMARY).
    - La section `## Gaps` de 06-UAT.md liste chaque écart cosmétique non-bloquant renvoyé au backlog (severity minor).
    - Après Task 2, aucun `result: issue` bloquant non résolu ne subsiste (grep retourne 0 hors lignes pass-with-note).
  </acceptance_criteria>
  <done>10 scénarios automatisables (1-7,10-12) au vert ou pass-with-note ; écarts bloquants corrigés + re-green + test ; 0 scénario en attente hors 8-9 perceptuels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase de clôture qualité (UAT + réconciliation de docs) — **aucune nouvelle surface d'attaque introduite**. L'app exercée existe déjà (livrée v1.0). Une seule frontière pertinente, déjà couverte par le périmètre des scénarios :

| Boundary | Description |
|----------|-------------|
| app → fichier CSV ouvert dans tableur tiers | Le CSV liasse est ouvert dans Excel/LibreOffice par le bailleur (formula injection) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering (CSV formula injection) | Export CSV brouillon liasse (sc.9) | mitigate | Sc.9 vérifie programmatiquement qu'aucune cellule ne débute par `=`/`+`/`-`/`@` sans préfixe `'` — c'est l'assertion de test, pas un nouveau code. Si l'assertion échoue → écart bloquant (export corrompu) → corrigé en Task 2. |

Aucune autre menace nouvelle : pas d'auth (single-user local-first), pas d'entrée réseau ajoutée, pas de nouvelle écriture DB hors contexte de test.
</threat_model>

<verification>
- `06-UAT.md` : scénarios 1-7,10,11,12 ont un `result:` non-pending ; 8-9 portent une note de report vers plan 09-03.
- Tout écart bloquant a un fix + test de non-régression + scénario rejoué au vert.
- `## Summary` cohérent avec les `result:` individuels.
- Si fix livré : `pnpm test` vert.
</verification>

<success_criteria>
- 10 des 12 scénarios liasse exécutés et clos (pass / pass-with-note) — les 2 restants (8,9) clos au plan 09-03.
- 0 écart bloquant non corrigé (critère ROADMAP #2 partiel).
- Aucun scénario laissé en `pending` hors 8 et 9.
</success_criteria>

<output>
After completion, create `.planning/phases/09-finition-qualit-cl-ture-uat-r-conciliation-des-statuts/09-01-SUMMARY.md`
</output>
