---
phase: 07-dashboard-notifications-d-ch-ances
plan: 07
type: tdd
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/web/helpers/alerte-helpers.ts
  - src/web/views/pages/dashboard/accueil.ejs
  - src/domain/locatif/alerte-irl.ts
  - src/domain/locatif/alerte-fin-bail.ts
  - src/application/dashboard/calculer-toutes-alertes.ts
  - src/web/routes/racine.ts
  - tests/unit/web/alerte-helpers.test.ts
  - tests/unit/locatif/alerte-irl.test.ts
  - tests/unit/locatif/alerte-fin-bail.test.ts
  - tests/bdd/features/alerte-fin-bail.feature
  - tests/bdd/features/alerte-irl.feature
  - tests/bdd/step_definitions/alerte-fin-bail.steps.ts
  - tests/bdd/step_definitions/alerte-irl.steps.ts
autonomous: false
requirements: [DAS-01, DAS-02]
user_setup: []

must_haves:
  truths:
    - "Une alerte diagnostic de type électricité affiche le libellé « Électricité » (et l'aria-label « Alerte Électricité »), pas le fallback générique « Diagnostic »."
    - "Une alerte fin de bail affiche « Fin de bail — {nom du locataire} » quand le locataire est résolu, et « Fin de bail » sinon."
    - "Une action IRL dans la section « Actions du jour » du dashboard identifie le locataire par son nom."
    - "Le lien « Voir tout » d'une section dont le total dépasse 5 mène vers une cible existante (jamais une ancre morte)."
  artifacts:
    - path: "src/web/helpers/alerte-helpers.ts"
      provides: "libelleTypeAlerte compare typeDiagnostic à 'elec'"
      contains: "=== 'elec'"
    - path: "src/domain/locatif/alerte-irl.ts"
      provides: "calculerAlertesIrl émet extra.nomLocataire + extra.adresseBien"
      contains: "nomLocataire"
    - path: "src/domain/locatif/alerte-fin-bail.ts"
      provides: "calculerAlertesFinBail émet extra.nomLocataire"
      contains: "nomLocataire"
    - path: "src/web/views/pages/dashboard/accueil.ejs"
      provides: "Aucun href vers une ancre inexistante"
    - path: "tests/unit/web/alerte-helpers.test.ts"
      provides: "Test libelleTypeAlerte('elec') → 'Électricité'"
      contains: "Électricité"
  key_links:
    - from: "src/web/helpers/alerte-helpers.ts"
      to: "src/domain/_shared/duree-validite-diagnostic.ts (TypeDiagnostic = 'elec')"
      via: "comparaison littérale === 'elec'"
      pattern: "=== 'elec'"
    - from: "src/application/dashboard/calculer-toutes-alertes.ts"
      to: "src/domain/locatif/alerte-irl.ts + alerte-fin-bail.ts"
      via: "Map<BailId, string> nomLocataireParBail résolue depuis locataireRepo"
      pattern: "nomLocataireParBail"
    - from: "src/web/routes/racine.ts:151"
      to: "alerte.source.extra.nomLocataire"
      via: "extra produit par calculerAlertesIrl"
      pattern: "extra\\?\\.\\['nomLocataire'\\]"
---

<objective>
Fermer les gaps documentés dans 07-VERIFICATION.md qui font échouer 2 des 4 critères
de succès de la Phase 7 (truths #3 et #4). Trois causes racines :

1. **WR-02 (BLOCKER)** — `alerte-helpers.ts:72` compare `typeDiagnostic === 'electricite'`
   alors que le domaine produit `'elec'` (`TypeDiagnostic`). Le libellé « Électricité »
   n'est jamais rendu → fallback générique « Diagnostic » + aria-label faux.

2. **WR-01 (BLOCKER)** — `accueil.ejs:22` et `:76` pointent vers des ancres
   `#toutes-alertes-critiques` / `#toutes-actions-jour` qui n'existent nulle part →
   les alertes/actions au-delà du top 5 sont inaccessibles.

3. **WR-03 (contrat rompu)** — `nomLocataire` est lu par `alerte-helpers.ts:76` (fin_bail)
   et `racine.ts:151` (IRL S4) mais n'est jamais produit par `calculerAlertesIrl` /
   `calculerAlertesFinBail`. Branches mortes → le locataire n'est jamais identifié.

Et deux WARNINGS juridiques à verrouiller par scénario (doc↔code ne doit plus diverger) :

4. **WR-04** — JSDoc `alerte-fin-bail.ts:43-45` dit `j <= 60 && j >= -30` mais le code
   (correct, D-SRC-05 `[-30,+60]`) exécute `j <= 30 && j >= -60`. Réconcilier la doc.

5. **WR-05** — Docstring `alerte-irl.ts:5,59` annonce `[-30,+30]` mais `dateAnniversaireProchaine`
   retourne toujours une date future (`j >= 1`) → la borne `-30` est du code mort.
   Réconcilier la doc sur la fenêtre réelle forward-only `[0,+30]` (D-SRC-02) et verrouiller.

Purpose: prouver TRUE les truths #3 (libellé élec) et #4 (navigation un-clic + identification
locataire) sans replanifier toute la phase.
Output: helper corrigé + testé, calculateurs domaine émettant `extra.nomLocataire`, ancres
mortes supprimées/redirigées, docs juridiques réconciliées et verrouillées par BDD/TDD.
</objective>

<execution_context>
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/workflows/execute-plan.md
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-dashboard-notifications-d-ch-ances/07-VERIFICATION.md
@.planning/phases/07-dashboard-notifications-d-ch-ances/07-UI-SPEC.md
@CLAUDE.md
@practices/BDD_PRACTICES.md
@practices/DDD.md
@practices/ACCESSIBILITY.md

<interfaces>
<!-- Contrats déjà en place. L'executor implémente CONTRE ces signatures — pas d'exploration. -->

From src/domain/_shared/duree-validite-diagnostic.ts:
  export type TypeDiagnostic = 'dpe' | 'gaz' | 'elec' | 'erp';   // 'elec' — JAMAIS 'electricite'

From src/domain/_shared/alerte.ts:
  export interface Alerte {
    readonly type: 'cfe' | 'irl' | 'diagnostic' | 'fin_bail';
    readonly joursRestants: number;
    readonly dateEcheance: Temporal.PlainDate;
    readonly libelle: string;
    readonly urlAction: string;
    readonly source: { readonly type; readonly refId: string; readonly bienId?: BienId;
                       readonly extra?: Record<string, unknown>; };
  }

From src/web/helpers/alerte-helpers.ts (signature à conserver) :
  export function libelleTypeAlerte(alerte: Alerte): string;   // lit alerte.source.extra.typeDiagnostic
  // appelé par le partial : const libelleType = libelleTypeAlerte(alerte);

From src/domain/locatif/alerte-irl.ts (signature ACTUELLE à étendre) :
  export function calculerAlertesIrl(
    baux: readonly Bail[], biens: readonly Bien[],
    indexationsParBail: Map<BailId, boolean>, maintenant: Temporal.PlainDate,
  ): Alerte[];
  // produit aujourd'hui : source.extra = { adresseBien: bien.adresse.rue }

From src/domain/locatif/alerte-fin-bail.ts (signature ACTUELLE à étendre) :
  export function calculerAlertesFinBail(
    baux: readonly Bail[], maintenant: Temporal.PlainDate,
  ): Alerte[];
  // produit aujourd'hui : source SANS extra

From src/domain/locatif/bail.ts :
  readonly locataireId: LocataireId;   // le Bail porte l'ID, PAS le nom

From src/web/routes/baux.ts (pattern de résolution nom déjà éprouvé pour /baux/indexations) :
  const locataires = await opts.locataireRepo.listerTous();
  const locatairesMap: Record<string, string> = {};
  for (const l of locataires) locatairesMap[l.id] = `${l.prenom} ${l.nom}`;

From src/web/routes/racine.ts:151 (consommateur S4) :
  nomLocataire: String(alerte.source.extra?.['nomLocataire'] ?? ''),
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (TDD) : corriger le littéral 'elec' dans libelleTypeAlerte + créer son test</name>
  <files>tests/unit/web/alerte-helpers.test.ts, src/web/helpers/alerte-helpers.ts</files>
  <read_first>
    - src/web/helpers/alerte-helpers.ts (la fonction libelleTypeAlerte, ligne 60-83 — le bug est ligne 72)
    - src/domain/_shared/duree-validite-diagnostic.ts (TypeDiagnostic = 'dpe' | 'gaz' | 'elec' | 'erp')
    - 07-VERIFICATION.md (WR-02 / anti-pattern BLOCKER ligne 126)
    - src/domain/patrimoine/alerte-diagnostic.ts:67 (extra: { typeDiagnostic: diag.type } — produit 'elec')
    - tests/unit/fiscalite/alerte-cfe-j30.test.ts (pattern de test vitest miroir)
  </read_first>
  <behavior>
    - libelleTypeAlerte(alerte diagnostic, extra.typeDiagnostic = 'elec') → "Électricité"
    - libelleTypeAlerte(alerte diagnostic, extra.typeDiagnostic = 'dpe') → "DPE"
    - libelleTypeAlerte(alerte diagnostic, extra.typeDiagnostic = 'gaz') → "Gaz"
    - libelleTypeAlerte(alerte diagnostic, extra.typeDiagnostic absent ou 'erp') → "Diagnostic"
    - libelleTypeAlerte(alerte fin_bail, extra.nomLocataire = 'Marie Curie') → "Fin de bail — Marie Curie"
    - libelleTypeAlerte(alerte fin_bail, sans nomLocataire) → "Fin de bail"
    - libelleTypeAlerte(alerte cfe, extra.millesime = 2026) → "CFE 2026"
    - libelleTypeAlerte(alerte irl) → "Révision IRL"
  </behavior>
  <action>
    RED — Créer `tests/unit/web/alerte-helpers.test.ts` (le helper n'a AUCUN test aujourd'hui —
    c'est le trou Nyquist). Couvrir les cas `<behavior>` ci-dessus pour `libelleTypeAlerte`.
    Construire les Alerte de test en littéral inline (forme Alerte de _shared/alerte.ts), pas besoin
    de repo. Le test `'elec' → 'Électricité'` DOIT échouer en RED (le code compare 'electricite').
    Lancer le test → vérifier l'échec sur le cas elec.

    GREEN — Dans `libelleTypeAlerte` (alerte-helpers.ts), remplacer la comparaison
    `typeDiag === 'electricite'` (ligne 72) par `typeDiag === 'elec'` — la SEULE valeur que
    `TypeDiagnostic` peut prendre pour l'électricité. Ne pas toucher les branches 'dpe'/'gaz'/'erp'.
    Relancer → tous verts. (Ferme WR-02 / BLOCKER truth #3 / DAS-02.)

    REFACTOR — aucun si le code reste lisible. Ne PAS modifier les autres helpers.
  </action>
  <acceptance_criteria>
    - `grep -c "=== 'electricite'" src/web/helpers/alerte-helpers.ts` retourne 0.
    - `grep -c "=== 'elec'" src/web/helpers/alerte-helpers.ts` retourne 1.
    - `tests/unit/web/alerte-helpers.test.ts` existe et contient une assertion `"Électricité"`.
    - `pnpm vitest run tests/unit/web/alerte-helpers.test.ts` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/unit/web/alerte-helpers.test.ts && grep -c "=== 'elec'" src/web/helpers/alerte-helpers.ts | grep -qx 1 && echo OK</automated>
  </verify>
  <done>Le libellé « Électricité » est rendu pour les diagnostics élec ; le helper est couvert par un test unitaire dédié.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (TDD) : produire extra.nomLocataire dans les calculateurs IRL et fin de bail + réconcilier les docs juridiques WR-04/WR-05</name>
  <files>tests/unit/locatif/alerte-irl.test.ts, tests/unit/locatif/alerte-fin-bail.test.ts, tests/bdd/features/alerte-irl.feature, tests/bdd/features/alerte-fin-bail.feature, tests/bdd/step_definitions/alerte-irl.steps.ts, tests/bdd/step_definitions/alerte-fin-bail.steps.ts, src/domain/locatif/alerte-irl.ts, src/domain/locatif/alerte-fin-bail.ts</files>
  <read_first>
    - src/domain/locatif/alerte-irl.ts (calculerAlertesIrl — extra ligne 103 ; doc fenêtre ligne 5 + ligne 59 ; WR-05)
    - src/domain/locatif/alerte-fin-bail.ts (calculerAlertesFinBail — source ligne 79-83 sans extra ; JSDoc fenêtre ligne 43-45 vs code ligne 54 ; WR-04)
    - src/domain/locatif/bail.ts (le Bail porte locataireId, pas le nom — ligne 74)
    - tests/unit/locatif/alerte-irl.test.ts (pattern de test + builders unBailIndexableValide/unBienValide)
    - tests/unit/locatif/alerte-fin-bail.test.ts (pattern + bailActifAvecFinDans)
    - tests/bdd/step_definitions/alerte-fin-bail.steps.ts (les steps appellent calculerAlertesFinBail(this.baux, this.maintenant) — signature à préserver via paramètre OPTIONNEL)
    - 07-VERIFICATION.md (WR-03 contrat nomLocataire, WR-04, WR-05) ; 07-CONTEXT.md D-SRC-02 / D-SRC-05
    - 07-UI-SPEC.md §S6 (la table /baux/indexations lit alerte.source.extra.nomLocataire + adresseBien — contrat de forme à respecter)
  </read_first>
  <behavior>
    IRL — calculerAlertesIrl(baux, biens, indexationsParBail, maintenant, nomLocataireParBail?) :
      - extra contient TOUJOURS { adresseBien } (régression Phase 7 préservée)
      - quand nomLocataireParBail a une entrée pour bail.id → extra.nomLocataire = ce nom
      - quand nomLocataireParBail absent / sans entrée → extra.nomLocataire absent ou '' (pas de crash)
    Fin de bail — calculerAlertesFinBail(baux, maintenant, nomLocataireParBail?) :
      - quand nomLocataireParBail a une entrée pour bail.id → source.extra.nomLocataire = ce nom
      - quand absent → source.extra absent (forme inchangée, rétrocompatible) OU extra: {} sans nomLocataire
    Fenêtres juridiques (verrouillage doc↔code) :
      - fin de bail : fenêtre RÉELLE [-30 j avant fin, +60 j après] → code j <= 30 && j >= -60 (inchangé, déjà correct)
      - IRL : fenêtre RÉELLE forward-only [0, +30] (dateAnniversaireProchaine toujours future) ; J-30 inclus, J-31 exclu
  </behavior>
  <action>
    RED —
    (a) Étendre `tests/unit/locatif/alerte-irl.test.ts` : ajouter un test asserant
        `alerte.source.extra.adresseBien` reste présent ET `alerte.source.extra.nomLocataire`
        vaut le nom passé via un `Map<BailId,string>` (ex. 'Marie Curie'). Ajouter un test
        vérifiant que sans la map, le calcul ne crashe pas et extra.adresseBien reste là.
    (b) Étendre `tests/unit/locatif/alerte-fin-bail.test.ts` : ajouter un test asserant
        `alerte.source.extra.nomLocataire === 'Jean Dupont'` quand la map est fournie.
    (c) Ajouter un scénario BDD dans `alerte-fin-bail.feature` (tag @phase7-alerte-fin-bail)
        verrouillant la fenêtre juridique : « fin J-30 incluse » (joursRestants=30 → 1 alerte)
        et « fin J+60 incluse / J+61 exclue » — refléter explicitement D-SRC-05 [-30,+60] dans
        le titre du scénario pour qu'un futur mainteneur ne « corrige » pas le code à l'envers (WR-04).
    (d) Ajouter un scénario BDD dans `alerte-irl.feature` (tag @phase7-alerte-irl) verrouillant la
        fenêtre RÉELLE forward-only : « anniversaire J-30 inclus → 1 alerte », « J-31 exclu → 0 alerte »,
        et un scénario nommant explicitement que la révision n'est PAS alertée le lendemain de
        l'anniversaire (comportement acté WR-05) — pour figer la sémantique.
        Mettre à jour les step_definitions si de nouveaux Given/Then sont introduits ; conserver
        l'appel existant `calculerAlertesFinBail(this.baux, this.maintenant)` (paramètre nom optionnel).
    Lancer vitest + cucumber → les nouveaux tests nomLocataire DOIVENT échouer (extra non produit).

    GREEN —
    - `alerte-irl.ts` : ajouter un 5e paramètre OPTIONNEL `nomLocataireParBail?: Map<BailId, string>`
      à `calculerAlertesIrl`. Dans la construction de l'Alerte, étendre `extra` en
      `{ adresseBien: bien.adresse.rue, nomLocataire: nomLocataireParBail?.get(bail.id) ?? '' }`
      (ne PAS retirer adresseBien — la table /baux/indexations le lit, UI-SPEC §S6).
    - `alerte-fin-bail.ts` : ajouter un 3e paramètre OPTIONNEL `nomLocataireParBail?: Map<BailId, string>`
      à `calculerAlertesFinBail`. Ajouter `extra: { nomLocataire: nomLocataireParBail?.get(bail.id) ?? '' }`
      sur le `source` produit (le helper libelleTypeAlerte lit ce champ).
    - WR-04 : corriger la JSDoc lignes 43-45 d'`alerte-fin-bail.ts` pour qu'elle décrive la
      fenêtre RÉELLE du code (`j <= 30 && j >= -60`, soit J-30 avant la fin à J+60 après, D-SRC-05).
      Ne PAS changer le code ligne 54 (il est correct).
    - WR-05 : corriger la docstring lignes 5 et 59 d'`alerte-irl.ts` pour décrire la fenêtre RÉELLE
      forward-only `[0, +30]` (dateAnniversaireProchaine est toujours future). Décision verrouillée :
      pas d'alerte « IRL dépassée » en V1 — la borne basse négative est retirée du commentaire (et
      le `j >= -30` peut rester comme garde défensive documentée « inatteignable, défensif » OU être
      remplacé par `j >= 0` — préférence executor, mais le commentaire DOIT cesser d'annoncer [-30,+30]).
    Relancer vitest + cucumber → tous verts.

    REFACTOR — Garder l'ubiquitous language français (`nomLocataireParBail`). Domaine pur : aucun
    import infra, aucun accès repo dans ces fonctions (la map est passée par le use case — Task 3).
  </action>
  <acceptance_criteria>
    - `grep -c "nomLocataire" src/domain/locatif/alerte-irl.ts` >= 1.
    - `grep -c "nomLocataire" src/domain/locatif/alerte-fin-bail.ts` >= 1.
    - `grep -c "adresseBien" src/domain/locatif/alerte-irl.ts` >= 1 (régression préservée).
    - La JSDoc d'alerte-fin-bail.ts ne contient plus `j <= 60 && j >= -30` (ancien texte WR-04) :
      `grep -F "j <= 60 && j >= -30" src/domain/locatif/alerte-fin-bail.ts` retourne 0 ligne.
    - Le commentaire d'alerte-irl.ts n'annonce plus une fenêtre « [-30, +30] » exploitable :
      vérifier que la mention `[-30, +30]` est soit supprimée soit annotée « borne basse inatteignable ».
    - `pnpm vitest run tests/unit/locatif/alerte-irl.test.ts tests/unit/locatif/alerte-fin-bail.test.ts` exit 0.
    - `pnpm exec cucumber-js --tags "@phase7-alerte-irl or @phase7-alerte-fin-bail"` exit 0.
    - Aucun import depuis infrastructure/ dans les 2 fichiers domaine (dependency-cruiser vert).
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/unit/locatif/alerte-irl.test.ts tests/unit/locatif/alerte-fin-bail.test.ts && pnpm exec cucumber-js --tags "@phase7-alerte-irl or @phase7-alerte-fin-bail" && grep -q "nomLocataire" src/domain/locatif/alerte-fin-bail.ts && grep -q "nomLocataire" src/domain/locatif/alerte-irl.ts && echo OK</automated>
  </verify>
  <done>Les calculateurs domaine émettent extra.nomLocataire (et adresseBien pour IRL) ; les fenêtres juridiques fin-bail et IRL sont verrouillées par scénario et la doc reflète le code.</done>
</task>

<task type="auto">
  <name>Task 3 : câbler nomLocataireParBail dans le use case + la route, et corriger les ancres mortes du dashboard</name>
  <files>src/application/dashboard/calculer-toutes-alertes.ts, src/web/routes/racine.ts, src/web/views/pages/dashboard/accueil.ejs</files>
  <read_first>
    - src/application/dashboard/calculer-toutes-alertes.ts (charge baux/biens lignes 57-60 ; appelle les 4 calculateurs lignes 77-80 ; deps interface lignes 36-42)
    - src/web/routes/racine.ts (deps lignes 39-52, opts.locataireRepo déjà injecté ligne 49 ; appel calculerToutesAlertes ligne 64 ; consommateur S4 nomLocataire ligne 151)
    - src/web/routes/baux.ts:67-77 (pattern de résolution locatairesMap[l.id] = `${l.prenom} ${l.nom}`)
    - src/web/views/pages/dashboard/accueil.ejs (ancre morte ligne 22 #toutes-alertes-critiques ; ancre morte ligne 76 #toutes-actions-jour)
    - 07-UI-SPEC.md §S2 (lien S2) + §S4 (lien S4 — option « split par type → IRL vers /baux/indexations ») + §Copywriting « Voir tout »
    - 07-VERIFICATION.md (WR-01 — missing : « créer la cible OU pointer vers une vraie page OU supprimer »)
  </read_first>
  <action>
    (1) `calculer-toutes-alertes.ts` : ajouter `locataireRepo: LocataireRepository` à
        `CalculerToutesAlertesDeps`. Après chargement des baux, construire
        `const locataires = await deps.locataireRepo.listerTous();` puis une
        `Map<BailId, string> nomLocataireParBail` où chaque bail mappe vers
        `${locataire.prenom} ${locataire.nom}` (via bail.locataireId → locataire.id).
        Passer cette map en dernier argument à `calculerAlertesIrl(...)` et
        `calculerAlertesFinBail(...)`. Ne pas toucher l'ordre de tri global.
        Import type : `import type { LocataireRepository } from '../../domain/locatif/locataire-repository.js';`
    (2) `racine.ts` : passer `locataireRepo: opts.locataireRepo` dans l'objet deps de l'appel
        `calculerToutesAlertes({ ... })` (l'objet opts.locataireRepo existe déjà ligne 49).
        Le code S4 ligne 151 lit déjà `alerte.source.extra?.['nomLocataire']` — il se mettra à
        produire la vraie valeur sans autre changement. Vérifier que rien d'autre n'est requis.
    (3) `accueil.ejs` — corriger les 2 ancres mortes (WR-01), conforme UI-SPEC :
        - S4 (ligne 76) : remplacer `href="#toutes-actions-jour"` par `href="/baux/indexations"`
          (page transversale RÉELLE créée en 07-06, S6 UI-SPEC) — c'est la cible « Voir tout » des
          actions IRL. Conserver l'aria-label « Voir toutes les actions du jour ».
        - S2 (ligne 22) : aucune page « toutes les alertes critiques » n'existe en V1. Supprimer le
          lien « Voir tout » mort de S2 (retirer le bloc `<% if (alertesCritiquesTotal > 5) { %>...<% } %>`
          contenant l'ancre `#toutes-alertes-critiques`). Les 5 alertes les plus urgentes restent
          affichées ; les alertes par domaine restent atteignables via leurs pages respectives
          (/baux/indexations, fiche Bien diagnostics, /fiscalite CFE). Ne créer AUCUNE nouvelle route.
        Ne PAS toucher les liens S3 (/impayes) et S5 (/echeances) qui sont déjà corrects.
  </action>
  <verify>
    <automated>grep -c "toutes-alertes-critiques\|toutes-actions-jour" src/web/views/pages/dashboard/accueil.ejs | grep -qx 0 && grep -q "/baux/indexations" src/web/views/pages/dashboard/accueil.ejs && grep -q "nomLocataireParBail" src/application/dashboard/calculer-toutes-alertes.ts && pnpm tsc --noEmit && echo OK</automated>
  </verify>
  <done>Le dashboard ne contient plus d'ancre morte ; le « Voir tout » des actions du jour mène à /baux/indexations ; les actions IRL S4 et les bandeaux fin de bail identifient le locataire par son nom via la map résolue dans le use case.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 (checkpoint) : vérification visuelle hiérarchie d'urgence + lien externe CFE</name>
  <what-built>
    Correction WR-01/WR-02/WR-03 + réconciliation docs WR-04/WR-05. Le dashboard rend
    désormais « Électricité » pour les diagnostics élec, identifie le locataire dans les
    actions IRL et fin de bail, et n'a plus d'ancre morte.
  </what-built>
  <how-to-verify>
    1. Lancer l'app (`pnpm dev` ou équivalent) et ouvrir `GET /` avec des données comportant
       une alerte diagnostic élec, une fin de bail proche et une révision IRL.
    2. **Hiérarchie d'urgence visuelle** (human_verification #1) : créer/avoir une alerte à J-3
       et une à J-15. Vérifier que le bandeau J-3 a un fond orange/bordeaux (variante `warning-fort`,
       `background: var(--couleur-warning-bg)`) visuellement distinct du bandeau J-15 (variante
       `warning`, simple bordure gauche sans fond plein), avec `role="alert"` et le texte
       « Échéance dans N jours ».
    3. **Lien externe CFE** (human_verification #2) : depuis une alerte CFE, cliquer sur
       « Régler la CFE sur impots.gouv.fr » → doit s'ouvrir dans un NOUVEL onglet
       (`target="_blank" rel="noopener noreferrer"`) vers
       `https://www.impots.gouv.fr/professionnel/cotisation-fonciere-des-entreprises-cfe`.
    4. Vérifier que le libellé d'une alerte diagnostic électricité affiche bien « Électricité »
       (et que l'aria-label du bandeau lit « Alerte Électricité »).
    5. Vérifier que « Voir tout » de la section Actions du jour (si > 5 actions) mène à
       /baux/indexations, et que la section Alertes critiques n'affiche plus de lien « Voir tout » mort.
  </how-to-verify>
  <action>Présenter à l'utilisateur les 5 points de vérification de how-to-verify (rendu élec, hiérarchie warning-fort vs warning, lien externe CFE nouvel onglet, identification locataire, ancres « Voir tout » corrigées), puis attendre son verdict. Aucune mutation : ce checkpoint valide visuellement le rendu produit par les tâches 1-3.</action>
  <verify>Confirmation utilisateur « approved » après inspection navigateur des 5 points (les deux items human_verification de 07-VERIFICATION.md inclus).</verify>
  <done>L'utilisateur confirme « approved » ou les écarts sont consignés pour une nouvelle itération.</done>
  <resume-signal>Tapez « approved » ou décrivez les écarts visuels constatés.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domaine (calculateurs) → vue EJS | Le nom du locataire et le type de diagnostic transitent du domaine vers le rendu HTML (labels + aria-labels). |
| SQLite local → use case | Données lues en local (mono-utilisateur, pas d'entrée externe non maîtrisée). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-01 | Tampering (XSS) | accueil.ejs / partial-bandeau-alerte.ejs — rendu de `nomLocataire` et `libelleTypeAlerte` | mitigate | Le nom du locataire et les libellés sont rendus via `<%= %>` (EJS échappe le HTML par défaut). Vérifier qu'aucun `<%- %>` (non échappé) n'est introduit pour ces champs. `aria-label` construit côté partial : s'assurer que `libelleType` reste une chaîne issue d'un set fini (CFE/IRL/DPE/Gaz/Électricité/Diagnostic/Fin de bail — pas de donnée libre sauf le nom, déjà échappé par EJS dans l'attribut). |
| T-07-07-02 | Information disclosure | extra.nomLocataire dans Alerte read-model | accept | Application mono-utilisateur local-first (CLAUDE.md principe local-first / single-user) ; le bailleur est le seul à voir ses propres locataires. Pas de PII exposée hors session locale. |
| T-07-07-03 | Tampering | href /baux/indexations (lien interne statique) | accept | URL statique en dur, aucune interpolation de donnée utilisateur dans le href corrigé. |
</threat_model>

<verification>
- `pnpm vitest run` : suite complète verte (régression Phase 7 préservée, ~1065 tests + nouveaux).
- `pnpm exec cucumber-js --tags "@phase7"` : tous les scénarios verts, dont les nouveaux scénarios
  de fenêtre juridique WR-04 (fin de bail) et WR-05 (IRL forward-only).
- `pnpm tsc --noEmit` : 0 erreur (signatures optionnelles rétrocompatibles).
- `grep` gates des `<acceptance_criteria>` de chaque tâche tous satisfaits.
- dependency-cruiser : aucun import infra dans `src/domain/locatif/alerte-*.ts`.
- Checkpoint human-verify : hiérarchie visuelle warning-fort vs warning + lien externe CFE confirmés
  (items human_verification de 07-VERIFICATION.md, non automatisables).
</verification>

<success_criteria>
Re-dérivation des 2 truths en échec (preuve TRUE) :

**Truth #3 (notifications J-30/J-7 sur chaque échéance critique, dont expiration élec)** :
- libelleTypeAlerte rend « Électricité » pour `typeDiagnostic === 'elec'` (testé unitairement).
- Le bandeau et son aria-label « Alerte Électricité » sont corrects (checkpoint visuel).

**Truth #4 (notification → écran d'action en un clic + identification de l'action)** :
- Aucune ancre morte dans accueil.ejs ; « Voir tout » des actions du jour mène à /baux/indexations.
- Les actions IRL (S4) et les bandeaux fin de bail identifient le locataire par son nom
  (extra.nomLocataire produit par le domaine, résolu via locataireRepo dans le use case).

Verrouillage juridique (anti-régression) :
- Fenêtre fin de bail [-30,+60] (D-SRC-05) : doc alignée sur le code + scénario BDD figeant J-30/J+60/J+61.
- Fenêtre IRL forward-only [0,+30] (D-SRC-02) : doc alignée + scénario BDD figeant le comportement réel.
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-notifications-d-ch-ances/07-07-SUMMARY.md`.
</output>
