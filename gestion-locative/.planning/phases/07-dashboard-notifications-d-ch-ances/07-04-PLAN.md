---
phase: 07-dashboard-notifications-d-ch-ances
plan: 04
type: tdd
wave: 3
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - src/application/dashboard/calculer-toutes-alertes.ts
  - tests/unit/dashboard/calculer-toutes-alertes.test.ts
  - tests/_builders/alertes.ts
  - tests/bdd/features/alerte-agregation.feature
  - tests/bdd/steps/alerte-agregation.steps.ts
autonomous: true
requirements: [DAS-02]

must_haves:
  truths:
    - "`calculerToutesAlertes(deps)` charge les 4 repos sources en lecture seule, appelle les 4 fonctions pures (CFE + IRL + diagnostic + fin de bail) et retourne un `Alerte[]` unifié fusionné"
    - "Le résultat est trié ASC par `joursRestants` GLOBALEMENT (toutes sources confondues), pas par source"
    - "La `maintenant` provient UNIQUEMENT de `deps.clock.aujourdhui()` — jamais de `new Date()` / `Temporal.Now` / cron / setInterval (pattern Clock-driven D-CFE6.5)"
    - "Le filtre IRL « exercice courant » est pré-calculé par le use case : `indexationsParBail: Map<BailId, boolean>` (true = une `BailIndexation` existe dont `dateEffet.year === maintenant.year`), puis injecté dans `calculerAlertesIrl` — le domaine ne touche aucun repository (contrat 07-02 respecté)"
    - "Aucune implémentation infra importée : seules les interfaces domaine (repos) + Clock sont injectées via `Deps` ; aucune mutation d'agrégat ; aucune migration SQLite"
    - "Entrée vide (aucun bien / bail / déclaration) → `[]` sans exception"
  artifacts:
    - path: "src/application/dashboard/calculer-toutes-alertes.ts"
      provides: "Use case transversal calculerToutesAlertes + interface CalculerToutesAlertesDeps"
      exports: ["CalculerToutesAlertesDeps", "calculerToutesAlertes"]
      min_lines: 40
    - path: "tests/unit/dashboard/calculer-toutes-alertes.test.ts"
      provides: "Couverture : fusion 4 sources, tri ASC global, Clock-driven, Map indexations exercice courant, vide"
    - path: "tests/bdd/features/alerte-agregation.feature"
      provides: "Scénarios : agrégation multi-source, tri ASC global déterministe, exclusion exercice courant"
    - path: "tests/_builders/alertes.ts"
      provides: "Builder uneAlerteDiagnostic (extension finale, complète cfe/irl/fin_bail/diagnostic)"
      exports: ["uneAlerteDiagnostic"]
  key_links:
    - from: "src/application/dashboard/calculer-toutes-alertes.ts"
      to: "src/domain/fiscalite/cfe/alerte-cfe-j30.ts"
      via: "import { calculerAlertesCfe }"
      pattern: "calculerAlertesCfe"
    - from: "src/application/dashboard/calculer-toutes-alertes.ts"
      to: "src/domain/locatif/alerte-irl.ts"
      via: "import { calculerAlertesIrl } avec indexationsParBail pré-calculé"
      pattern: "calculerAlertesIrl"
    - from: "src/application/dashboard/calculer-toutes-alertes.ts"
      to: "src/domain/locatif/bail-indexation-repository.ts"
      via: "deps.bailIndexationRepo.dernierePourBail(bail.id) → dateEffet.year === maintenant.year"
      pattern: "dernierePourBail"
    - from: "src/application/dashboard/calculer-toutes-alertes.ts"
      to: "src/domain/_shared/clock.ts"
      via: "deps.clock.aujourdhui()"
      pattern: "clock\\.aujourdhui\\(\\)"
---

<objective>
Créer le use case application transversal `calculerToutesAlertes(deps): Promise<Alerte[]>` dans `src/application/dashboard/calculer-toutes-alertes.ts` (D-AL-02). Il charge les repos sources en lecture seule (CFE, Bien, Bail, BailIndexation), appelle les 4 fonctions pures créées en 07-01/07-02/07-03 (`calculerAlertesCfe`, `calculerAlertesIrl`, `calculerAlertesDiagnostic`, `calculerAlertesFinBail`), fusionne leurs `Alerte[]` et trie ASC GLOBALEMENT par `joursRestants`. C'est la donnée unique que le dashboard 07-05 consommera.

Purpose : c'est l'agrégateur de DAS-02 — il transforme les 4 sources d'alerte en un seul tableau trié par urgence. Il porte LA responsabilité que les fonctions pures domaine refusent : accéder aux repositories et calculer le filtre IRL « exercice courant » (`indexationsParBail: Map<BailId, boolean>` injectée dans `calculerAlertesIrl` — contrat verrouillé par 07-02). Pattern Clock-driven strict : `maintenant = deps.clock.aujourdhui()`, jamais de cron.

Output : `src/application/dashboard/calculer-toutes-alertes.ts` (nouveau), tests unitaires, feature BDD `alerte-agregation.feature` + steps, extension finale du builder `tests/_builders/alertes.ts` (ajout `uneAlerteDiagnostic`). Aucune implémentation infra importée, aucune migration SQLite, aucune mutation d'agrégat.
</objective>

<execution_context>
@/Users/valentinshodo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/valentinshodo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dashboard-notifications-d-ch-ances/07-CONTEXT.md
@.planning/phases/07-dashboard-notifications-d-ch-ances/07-PATTERNS.md
@.planning/phases/07-dashboard-notifications-d-ch-ances/07-01-PLAN.md
@.planning/phases/07-dashboard-notifications-d-ch-ances/07-02-PLAN.md
@.planning/phases/07-dashboard-notifications-d-ch-ances/07-03-PLAN.md
@src/application/fiscalite/lister-alertes-cfe-actives.ts

<interfaces>
<!-- État actuel exact — extrait du codebase. L'exécuteur n'a pas besoin d'explorer. -->

Contrat `Alerte` D-AL-01 (créé par 07-01, `src/domain/_shared/alerte.ts`) — forme verrouillée :

```typescript
export type TypeAlerte = 'cfe' | 'irl' | 'diagnostic' | 'fin_bail';
export interface Alerte {
  readonly type: TypeAlerte;
  readonly joursRestants: number;       // peut être négatif
  readonly dateEcheance: Temporal.PlainDate;
  readonly libelle: string;
  readonly urlAction: string;
  readonly source: { readonly type: TypeAlerte; readonly refId: string; readonly bienId?: BienId; readonly extra?: Record<string, unknown> };
}
```

Les 4 fonctions pures domaine à composer (signatures EXACTES, créées par 07-01..03) :

```typescript
// src/domain/fiscalite/cfe/alerte-cfe-j30.ts (07-01)
function calculerAlertesCfe(declarations: readonly DeclarationCfe[], maintenant: Temporal.PlainDate): Alerte[];

// src/domain/locatif/alerte-irl.ts (07-02)
//   indexationsParBail: true = une indexation existe DÉJÀ sur l'exercice courant (→ ne pas alerter)
function calculerAlertesIrl(baux: readonly Bail[], biens: readonly Bien[], indexationsParBail: Map<BailId, boolean>, maintenant: Temporal.PlainDate): Alerte[];

// src/domain/patrimoine/alerte-diagnostic.ts (07-03)
function calculerAlertesDiagnostic(biens: readonly Bien[], maintenant: Temporal.PlainDate): Alerte[];

// src/domain/locatif/alerte-fin-bail.ts (07-02)
function calculerAlertesFinBail(baux: readonly Bail[], maintenant: Temporal.PlainDate): Alerte[];
```

Port `Clock` (`src/domain/_shared/clock.ts`) — `aujourdhui(): Temporal.PlainDate`. Test double `ClockFixe` (constructeur prend une `Temporal.PlainDate`) à réutiliser dans les tests.

Repos sources à injecter (interfaces domaine RÉELLES — NE PAS modifier, aucune nouvelle méthode requise) :

```typescript
// src/domain/patrimoine/bien-repository.ts
interface BienRepository { trouverParId(id: BienId): Promise<Bien | null>; listerTous(): Promise<Bien[]>; }

// src/domain/locatif/bail-repository.ts
interface BailRepository { trouverParId(id: BailId): Promise<Bail | null>; listerTous(): Promise<Bail[]>; listerParLocataire(locataireId: LocataireId): Promise<Bail[]>; }

// src/domain/fiscalite/cfe/declaration-cfe-repository.ts
interface DeclarationCfeRepository { listerParBien(bienId: BienId | string): Promise<DeclarationCfe[]>; /* + enregistrer, trouverParId, trouverParBienMillesime */ }

// src/domain/locatif/bail-indexation-repository.ts
interface BailIndexationRepository { dernierePourBail(bailId: BailId): Promise<BailIndexation | null>; listerParBail(bailId: BailId): Promise<BailIndexation[]>; /* + enregistrer, trouverParId */ }
```

`BailIndexation` (`src/domain/locatif/bail-indexation.ts`) — champ utilisé ici : `readonly dateEffet: Temporal.PlainDate` (→ `.year`).

Agrégats lus : `Bail` a `id: BailId`, `bienId: BienId` ; `Bien` a `id: BienId`. (Aucune mutation.)

Pattern à mirrorer EXACTEMENT — `src/application/fiscalite/lister-alertes-cfe-actives.ts` (interface `Deps` + `const maintenant = deps.clock.aujourdhui()` + `Promise.all` repos + appel fonction pure). `DeclarationCfeRepository` n'a PAS de `listerTous` : pour agréger toutes les CFE, faire comme `lister-alertes-cfe-actives.ts` lignes 38-40 — `biens.map((b) => deps.cfeRepo.listerParBien(b.id))` puis `.flat()`.

Builder existant (07-01/07-02) : `tests/_builders/alertes.ts` exporte déjà `uneAlerte`, `uneAlerteCfe`, `uneAlerteIrl`, `uneAlerteFinBail`. Ce plan AJOUTE `uneAlerteDiagnostic` (dernier manquant). Builders domaine réutilisables : `unBailValide` (`tests/_builders/locatif.ts`), `unBienValide`/`unDiagnosticDpeValide` (`tests/_builders/patrimoine.ts`), builder déclaration CFE (`tests/_builders/fiscalite.ts`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD `src/application/dashboard/calculer-toutes-alertes.ts` — fusion 4 sources + tri ASC global + Map indexations exercice courant (Clock-driven)</name>
  <files>src/application/dashboard/calculer-toutes-alertes.ts, tests/unit/dashboard/calculer-toutes-alertes.test.ts</files>
  <read_first>
    - src/application/fiscalite/lister-alertes-cfe-actives.ts (PATTERN EXACT à mirrorer : interface Deps + clock.aujourdhui() + Promise.all + agrégation CFE par bien via listerParBien + .flat())
    - src/domain/_shared/alerte.ts (contrat Alerte, créé 07-01)
    - src/domain/fiscalite/cfe/alerte-cfe-j30.ts (signature calculerAlertesCfe, 07-01)
    - src/domain/locatif/alerte-irl.ts (signature calculerAlertesIrl + contrat indexationsParBail: Map<BailId, boolean>, 07-02)
    - src/domain/locatif/alerte-fin-bail.ts (signature calculerAlertesFinBail, 07-02)
    - src/domain/patrimoine/alerte-diagnostic.ts (signature calculerAlertesDiagnostic, 07-03)
    - src/domain/locatif/bail-indexation-repository.ts (méthode dernierePourBail — pas de listerTous)
    - src/domain/locatif/bail-indexation.ts (champ dateEffet: Temporal.PlainDate → .year)
    - src/domain/fiscalite/cfe/declaration-cfe-repository.ts (listerParBien uniquement, PAS de listerTous)
    - src/domain/_shared/clock.ts (Clock + ClockFixe pour les tests)
    - tests/_builders/alertes.ts (uneAlerteCfe/Irl/FinBail déjà créés par 07-01/07-02)
    - tests/_builders/locatif.ts + tests/_builders/patrimoine.ts + tests/_builders/fiscalite.ts (builders domaine pour construire des doubles de repos)
  </read_first>
  <behavior>
    - Test 1 (Clock-driven): `calculerToutesAlertes(deps)` lit `deps.clock.aujourdhui()` UNE fois et le passe à toutes les fonctions pures — avec un `ClockFixe(2026-06-11)`, toutes les fenêtres sont calculées relativement à cette date. (Vérifier via un repo bien/bail/cfe stub qui produit des alertes positionnées par rapport à 2026-06-11.)
    - Test 2 (fusion 4 sources): repos peuplés produisant 1 alerte CFE (J-30), 1 IRL (J-10), 1 diagnostic (J-5), 1 fin de bail (J-20) → le résultat contient 4 alertes, une de chaque `type`.
    - Test 3 (tri ASC GLOBAL): les 4 alertes ci-dessus reviennent triées par `joursRestants` croissant TOUTES sources mélangées → ordre `[diagnostic(5), irl(10), finBail(20), cfe(30)]` (pas regroupées par source).
    - Test 4 (IRL exercice courant exclu): un bail dont `bailIndexationRepo.dernierePourBail(bail.id)` retourne une `BailIndexation` avec `dateEffet.year === 2026` (= année de `maintenant`) → `indexationsParBail.get(bail.id) === true` → AUCUNE alerte IRL pour ce bail. Un bail dont la dernière indexation est `dateEffet.year === 2025` (ou null) → `false` → alerte IRL possible.
    - Test 5 (CFE agrégée par bien): `cfeRepo.listerParBien` est appelée pour CHAQUE bien (pas de listerTous), les déclarations sont aplaties avant `calculerAlertesCfe`.
    - Test 6 (vide): repos retournant `[]` partout → `calculerToutesAlertes(deps)` retourne `[]`, sans exception, sans accès cron/date système.
  </behavior>
  <action>
    RED : créer `tests/unit/dashboard/calculer-toutes-alertes.test.ts`. Construire des doubles in-memory des 4 repos (objets littéraux implémentant les interfaces : `bienRepo`, `bailRepo`, `cfeRepo`, `bailIndexationRepo`) — les méthodes non utilisées peuvent throw `new Error('non utilisé')`. Utiliser `ClockFixe` (`src/domain/_shared/clock.ts`) avec `Temporal.PlainDate.from('2026-06-11')`. Peupler les doubles via les builders domaine (`unBienValide`, `unBailValide`, `unDiagnosticDpeValide`, builder déclaration CFE) en calant les dates pour positionner chaque alerte au `joursRestants` voulu par rapport au 2026-06-11. Importer `calculerToutesAlertes` depuis `../../../src/application/dashboard/calculer-toutes-alertes.js` (module absent → échec). Écrire les 6 tests `<behavior>`.

    GREEN : créer `src/application/dashboard/calculer-toutes-alertes.ts` en mirrorant `lister-alertes-cfe-actives.ts`.

    Imports (interfaces domaine + fonctions pures UNIQUEMENT — aucun import infra/`src/infrastructure/`, aucun Fastify) :
    - `import { Temporal } from '@js-temporal/polyfill';`
    - `import type { BailId } from '../../domain/_shared/identifiants.js';`
    - `import type { Clock } from '../../domain/_shared/clock.js';`
    - `import type { Alerte } from '../../domain/_shared/alerte.js';`
    - `import type { BienRepository } from '../../domain/patrimoine/bien-repository.js';`
    - `import type { BailRepository } from '../../domain/locatif/bail-repository.js';`
    - `import type { DeclarationCfeRepository } from '../../domain/fiscalite/cfe/declaration-cfe-repository.js';`
    - `import type { BailIndexationRepository } from '../../domain/locatif/bail-indexation-repository.js';`
    - `import { calculerAlertesCfe } from '../../domain/fiscalite/cfe/alerte-cfe-j30.js';`
    - `import { calculerAlertesIrl } from '../../domain/locatif/alerte-irl.js';`
    - `import { calculerAlertesFinBail } from '../../domain/locatif/alerte-fin-bail.js';`
    - `import { calculerAlertesDiagnostic } from '../../domain/patrimoine/alerte-diagnostic.js';`

    `export interface CalculerToutesAlertesDeps { cfeRepo: DeclarationCfeRepository; bienRepo: BienRepository; bailRepo: BailRepository; bailIndexationRepo: BailIndexationRepository; clock: Clock; }`

    `export async function calculerToutesAlertes(deps: CalculerToutesAlertesDeps): Promise<Alerte[]>` :
    - `const maintenant = deps.clock.aujourdhui();` (Clock-driven — UNIQUE source de la date).
    - `const [biens, baux] = await Promise.all([deps.bienRepo.listerTous(), deps.bailRepo.listerTous()]);`
    - CFE agrégée par bien (pas de `listerTous` sur cfeRepo) : `const listesCfe = await Promise.all(biens.map((b) => deps.cfeRepo.listerParBien(b.id))); const declarations = listesCfe.flat();`
    - Construire `indexationsParBail: Map<BailId, boolean>` : pour chaque bail, `const derniere = await deps.bailIndexationRepo.dernierePourBail(bail.id);` ; `const aDejaExerciceCourant = derniere !== null && derniere.dateEffet.year === maintenant.year;` ; `map.set(bail.id, aDejaExerciceCourant);`. Faire les appels en parallèle : `await Promise.all(baux.map(async (bail) => { ... map.set(...) }))` (la Map est remplie dans les callbacks). `true` = indexation déjà présente sur l'exercice courant (contrat 07-02 : `true` ⇒ ne pas alerter).
    - Appeler les 4 fonctions pures avec `maintenant` :
      - `const alertesCfe = calculerAlertesCfe(declarations, maintenant);`
      - `const alertesIrl = calculerAlertesIrl(baux, biens, indexationsParBail, maintenant);`
      - `const alertesDiag = calculerAlertesDiagnostic(biens, maintenant);`
      - `const alertesFinBail = calculerAlertesFinBail(baux, maintenant);`
    - Fusion + tri ASC global : `const toutes = [...alertesCfe, ...alertesIrl, ...alertesDiag, ...alertesFinBail]; toutes.sort((a, b) => a.joursRestants - b.joursRestants); return toutes;`

    JSDoc d'en-tête français : référencer Phase 7 / DAS-02 / D-AL-02 / D-AL-04 (pas de persistance d'état) / D-SRC-03 (filtre exercice courant pré-calculé ici, le domaine `alerte-irl.ts` ne touche aucun repo). Rappeler le pattern Clock-driven (D-CFE6.5) : calcul à la demande, `deps.clock.aujourdhui()`, INTERDICTION de `new Date()` / `Temporal.Now` / `setInterval` / cron.

    AUCUN import depuis `src/infrastructure/` ni Fastify dans ce fichier (seulement interfaces domaine + fonctions pures). Le wiring DI réel (instanciation des repos concrets) est la responsabilité du composition root, branché par 07-05 (route `GET /`).

    REFACTOR (si besoin) : extraire le calcul de la Map dans une fonction interne `construireMapIndexations(baux, repo, maintenant)` SEULEMENT si la lisibilité l'exige ; sinon inline. Garder la complexité cyclomatique < 10 (gate craftsmanship).

    Commits : `test(07-04): add failing tests for calculerToutesAlertes (fusion 4 sources, tri ASC global, Clock-driven)` puis `feat(07-04): use case agrégateur calculerToutesAlertes (D-AL-02, Map indexations exercice courant)`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/dashboard/calculer-toutes-alertes.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run tests/unit/dashboard/calculer-toutes-alertes.test.ts` → 6 tests passent, 0 échec
    - `grep -c "export async function calculerToutesAlertes" src/application/dashboard/calculer-toutes-alertes.ts` → 1
    - `grep -c "export interface CalculerToutesAlertesDeps" src/application/dashboard/calculer-toutes-alertes.ts` → 1
    - `grep -c "deps.clock.aujourdhui()" src/application/dashboard/calculer-toutes-alertes.ts` → 1 (date lue UNE seule fois)
    - `grep -cE "new Date\(|Temporal\.Now|setInterval|setTimeout|cron" src/application/dashboard/calculer-toutes-alertes.ts` → 0 (pattern Clock-driven, anti-cron)
    - `grep -c "dernierePourBail" src/application/dashboard/calculer-toutes-alertes.ts` → 1 (filtre exercice courant calculé ici)
    - `grep -cE "dateEffet\.year === maintenant\.year|maintenant\.year === .*dateEffet\.year" src/application/dashboard/calculer-toutes-alertes.ts` → au moins 1 (comparaison exercice courant)
    - les 4 appels présents : `grep -cE "calculerAlertesCfe|calculerAlertesIrl|calculerAlertesDiagnostic|calculerAlertesFinBail" src/application/dashboard/calculer-toutes-alertes.ts` → 4
    - `grep -cE "sort\(\(a, b\) => a\.joursRestants - b\.joursRestants\)" src/application/dashboard/calculer-toutes-alertes.ts` → 1 (tri ASC global)
    - `grep -E "from '(fastify|kysely|better-sqlite3|node:|.*/infrastructure/)" src/application/dashboard/calculer-toutes-alertes.ts` → aucune sortie (use case sans infra)
    - `pnpm tsc --noEmit` → 0 erreur
  </acceptance_criteria>
  <done>`calculerToutesAlertes` agrège les 4 sources en `Alerte[]` trié ASC global, calcule `indexationsParBail` (exercice courant) côté use case et l'injecte dans `calculerAlertesIrl`, lit la date uniquement via `clock.aujourdhui()`, zéro import infra ; 6 tests verts + tsc vert.</done>
</task>

<task type="auto">
  <name>Task 2: Builder `uneAlerteDiagnostic` + feature BDD `alerte-agregation.feature` + steps (agrégation déterministe)</name>
  <files>tests/_builders/alertes.ts, tests/bdd/features/alerte-agregation.feature, tests/bdd/steps/alerte-agregation.steps.ts</files>
  <read_first>
    - tests/_builders/alertes.ts (uneAlerte/uneAlerteCfe/uneAlerteIrl/uneAlerteFinBail déjà créés par 07-01/07-02 — pattern overrides + spread à imiter ; NE PAS recréer)
    - src/domain/patrimoine/alerte-diagnostic.ts (forme exacte de l'alerte diagnostic : source.extra = { typeDiagnostic } — pour caler les défauts du builder)
    - tests/bdd/features/cfe-suivi-declaratif.feature (structure feature : commentaire en-tête, tags @phaseN, Background, Scenario)
    - tests/bdd/steps/ (un fichier steps existant à imiter : World partagé, Given/When/Then, assertions)
    - src/application/dashboard/calculer-toutes-alertes.ts (use case appelé par les steps avec des repos in-memory + ClockFixe)
    - tests/_builders/locatif.ts + tests/_builders/patrimoine.ts + tests/_builders/fiscalite.ts (builders domaine pour peupler les repos in-memory des steps)
  </read_first>
  <action>
    Étendre `tests/_builders/alertes.ts` (NE PAS recréer — 07-01/07-02 l'ont écrit) en AJOUTANT le dernier builder manquant, selon le même pattern overrides + spread que `uneAlerteCfe`/`uneAlerteIrl` :
    - `export function uneAlerteDiagnostic(overrides: Partial<Alerte> = {}): Alerte` — défauts : `type: 'diagnostic'`, `joursRestants: 5`, `dateEcheance: Temporal.PlainDate.from('2026-06-16')`, `libelle: 'Diagnostic DPE'`, `urlAction: '/biens/11111111-1111-4111-8111-111111111111/diagnostics#diag-dpe'`, `source: { type: 'diagnostic', refId: '44444444-4444-4444-8444-444444444444', bienId: '11111111-1111-4111-8111-111111111111' as BienId, extra: { typeDiagnostic: 'dpe' } }`, puis `...overrides`. Après ce plan, le builder couvre les 4 types (cfe/irl/fin_bail/diagnostic) — complet pour 07-05.

    Créer `tests/bdd/features/alerte-agregation.feature` (tags `@phase7 @phase7-alerte-agregation`, en-tête commentaire référençant DAS-02 / D-AL-01 / D-AL-02 / D-AL-04), reproduisant la structure de `cfe-suivi-declaratif.feature`. Scénarios (read-model unifié + déterminisme) :
    - `@phase7-alerte-agregation-01` Fusion multi-source : Given des données produisant 1 alerte CFE, 1 IRL, 1 diagnostic et 1 fin de bail dans leurs fenêtres / When je calcule toutes les alertes / Then 4 alertes sont retournées, une de chaque type.
    - `@phase7-alerte-agregation-02` Tri ASC global : Given des alertes de sources différentes avec des joursRestants 5, 10, 20, 30 / When je calcule toutes les alertes / Then elles sont triées par joursRestants croissant toutes sources confondues (l'alerte la plus urgente — diagnostic J-5 — en premier, la CFE J-30 en dernier).
    - `@phase7-alerte-agregation-03` Exclusion exercice courant : Given un bail dont une indexation a déjà été enregistrée cette année / When je calcule toutes les alertes / Then aucune alerte IRL n'est retournée pour ce bail (D-SRC-03, filtre calculé par l'agrégateur).
    - `@phase7-alerte-agregation-04` Aucune donnée : Given aucun bien, bail ou déclaration / When je calcule toutes les alertes / Then aucune alerte n'est retournée (liste vide).

    Créer `tests/bdd/steps/alerte-agregation.steps.ts` selon le style des steps existants : le World stocke des repos in-memory (objets littéraux implémentant `BienRepository`/`BailRepository`/`DeclarationCfeRepository`/`BailIndexationRepository`, méthodes inutilisées throw) peuplés via les builders domaine, un `ClockFixe(Temporal.PlainDate.from('2026-06-11'))`, et le résultat de `await calculerToutesAlertes(deps)`. Les Given construisent les entités domaine en mémoire (caler les dates par rapport au 2026-06-11), le When appelle `calculerToutesAlertes`, les Then assertent le nombre d'alertes + leur `type` + l'ordre de `joursRestants`. AUCUN serveur Fastify, AUCUN supertest — le use case est testé directement avec des repos doubles (couverture application pure).

    Ne PAS créer de route, ne PAS toucher au serveur, ne PAS ajouter de migration (la route `GET /` dashboard est le plan 07-05).

    Commit : `test(07-04): builder uneAlerteDiagnostic + BDD alerte-agregation (fusion, tri ASC global, exercice courant)`.
  </action>
  <verify>
    <automated>pnpm vitest run && pnpm cucumber-js --tags "@phase7-alerte-agregation" 2>/dev/null || pnpm exec cucumber-js --tags "@phase7-alerte-agregation"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/_builders/alertes.ts` exporte désormais `uneAlerte`, `uneAlerteCfe`, `uneAlerteIrl`, `uneAlerteFinBail` (plans précédents) ET `uneAlerteDiagnostic`
    - `grep -c "export function uneAlerteDiagnostic" tests/_builders/alertes.ts` → 1
    - La feature contient exactement 4 scénarios taggés `@phase7-alerte-agregation-01..04`
    - L'exécution Cucumber `@phase7-alerte-agregation` → tous verts (4/4), 0 step `undefined`/`pending`
    - `grep -c "calculerToutesAlertes" tests/bdd/steps/alerte-agregation.steps.ts` → ≥ 1 (appel use case direct)
    - `grep -cE "(supertest|app\.inject|fastify)" tests/bdd/steps/alerte-agregation.steps.ts` → 0 (pas de couche HTTP)
    - Le scénario 02 asserte un ordre `joursRestants` strictement croissant ; le scénario 03 asserte 0 alerte IRL ; le scénario 04 asserte 0 alerte
  </acceptance_criteria>
  <done>Builder `uneAlerteDiagnostic` disponible (4 types complets pour 07-05) ; feature `alerte-agregation.feature` (4 scénarios) verte via Cucumber, steps appelant le use case avec repos in-memory + ClockFixe ; fusion + tri ASC global + exclusion exercice courant couverts.</done>
</task>

<task type="auto">
  <name>Task 3: Fermeture — suite complète + lint + dependency-cruiser (hexagonal vert sur la couche application)</name>
  <files>src/application/dashboard/calculer-toutes-alertes.ts</files>
  <read_first>
    - .dependency-cruiser.cjs (règles hexagonales — application/ peut importer domain/ mais PAS infrastructure/web ; domain/ n'importe rien hors domain)
    - src/application/dashboard/calculer-toutes-alertes.ts (fichier à valider)
  </read_first>
  <action>
    Aucune nouvelle écriture de logique. Valider la fermeture du plan : lancer la suite complète + le lint + dependency-cruiser pour garantir que le nouveau use case `application/dashboard/calculer-toutes-alertes.ts` respecte le hardening hexagonal (Phase 5.1) — la couche application n'importe que des interfaces domaine et des fonctions pures, jamais `src/infrastructure/` ni Fastify. Si une violation apparaît (import interdit, warning lint, complexité > 10, cycle), corriger UNIQUEMENT le fichier créé en Task 1 (ce plan est lecture seule sur le reste du codebase). Vérifier aussi qu'aucune régression Phase 6 / 07-01..03 n'est introduite (la suite complète reste verte).
  </action>
  <verify>
    <automated>pnpm vitest run && pnpm lint && (pnpm depcruise 2>/dev/null || pnpm exec depcruise --config .dependency-cruiser.cjs src)</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run` → suite complète verte (0 fail), incluant la régression Phase 6 + les unitaires 07-01..03 + `calculer-toutes-alertes.test.ts`
    - dependency-cruiser → 0 violation (aucune flèche `application/dashboard → infrastructure/web` ; aucune flèche `domain → application`)
    - `pnpm lint` → 0 warning sur `src/application/dashboard/calculer-toutes-alertes.ts` et les fichiers de test créés
    - `git diff --name-only` ne liste AUCUN fichier hors des 5 déclarés dans `files_modified`
  </acceptance_criteria>
  <done>Suite complète du projet verte, architecture hexagonale validée (use case sans infra), agrégateur prêt à être consommé par la route dashboard 07-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Aucune nouvelle | Ce plan ne crée ni ne modifie aucune surface HTTP. Use case application en LECTURE SEULE : il lit 4 repos (interfaces domaine) via les méthodes `listerTous`/`listerParBien`/`dernierePourBail` et compose des fonctions pures. Aucune entrée utilisateur non trustée n'entre dans ce plan (pas de path param, pas de body). La frontière de confiance HTTP `GET /` est introduite en 07-05. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | T (Tampering) | `Alerte.urlAction` agrégées (toutes sources) interpolées plus tard dans EJS | accept | Les `urlAction` sont construites côté domaine (07-01..03) à partir d'identifiants brand types UUID v4 ; l'agrégateur ne fait que les concaténer dans un tableau sans modification. Aucune entrée utilisateur libre. L'échappement de sortie EJS `<%= %>` est vérifié par 07-05 au rendu. |
| T-07-04-02 | I (Information Disclosure) | agrégation cross-Bien de toutes les alertes (CFE/IRL/diag/fin de bail) en un seul tableau | accept | Application single-user local (D-AL-03, VISION.md) — pas de séparation de tenants, l'utilisateur est propriétaire de toutes les données agrégées. `source.extra` ne contient que des champs non sensibles déjà fixés par 07-01..03 (millesime, typeDiagnostic, adresseBien). Aucune donnée locataire/montant ajoutée par ce plan. |
| T-07-04-03 | D (Denial of Service) | `Promise.all(biens.map(cfeRepo.listerParBien))` + `Promise.all(baux.map(dernierePourBail))` (N requêtes repo) | accept | Single-user local, volumétrie bornée (quelques biens/baux). Pattern identique à `lister-alertes-cfe-actives.ts` (déjà en prod Phase 6). Complexité O(n + m) requêtes + O(k log k) tri, k petit. Pas de récursion, pas d'entrée non bornée. |
| T-07-04-04 | T (Tampering) | filtre IRL « exercice courant » (`dateEffet.year === maintenant.year`) | mitigate | Le calcul `true/false` est entièrement piloté par `deps.clock.aujourdhui()` (source unique de la date, testée par ClockFixe) et par les `BailIndexation` persistées append-only (D-96, non modifiables). Couvert par le test unitaire 4 et le scénario BDD `@phase7-alerte-agregation-03` pour garantir qu'un bail déjà indexé cette année ne génère JAMAIS d'alerte. |
</threat_model>

<verification>
- `pnpm vitest run` — suite complète verte, incluant `tests/unit/dashboard/calculer-toutes-alertes.test.ts` (6 tests) et la régression des plans 07-01..03, sans casse ailleurs.
- `pnpm cucumber-js --tags "@phase7-alerte-agregation"` — 4 scénarios verts (fusion multi-source, tri ASC global, exclusion exercice courant, vide).
- `pnpm tsc --noEmit` — 0 erreur. `git diff --name-only` ne contient AUCUN fichier hors des 5 de `files_modified` (lecture seule sur le reste).
- dependency-cruiser — 0 violation : `src/application/dashboard/calculer-toutes-alertes.ts` n'importe que des interfaces domaine (`*-repository.ts`, `clock.ts`, `alerte.ts`, `identifiants.ts`) + les 4 fonctions pures `calculerAlertes*` + `@js-temporal/polyfill`. Aucun import `src/infrastructure/` ni Fastify.
- Pattern Clock-driven respecté : `deps.clock.aujourdhui()` est la SEULE source de date ; `grep -E "new Date\(|Temporal\.Now|setInterval|setTimeout|cron"` → vide. Aucune nouvelle migration SQLite (la dernière reste `0022_phase6_*`), aucune mutation d'agrégat.
- Multi-source coverage audit : GOAL (notifications J-30/J-7 agrégées par les phases précédentes) → l'agrégateur fusionne les 4 sources triées par urgence, c'est exactement la donnée du dashboard ✓ ; REQ DAS-02 (notifications J-30/J-7 sur 4 sources agrégées) → use case `calculerToutesAlertes` livré ✓ ; RESEARCH/PATTERNS (analog `lister-alertes-cfe-actives.ts` mirrorée, section `calculer-toutes-alertes.ts` du Pattern Map, agrégation CFE par bien via `listerParBien`) ✓ ; CONTEXT D-AL-01 (read-model Alerte unifié, tri ASC global) ✓, D-AL-02 (localisation `application/dashboard/`, agrégation = use case, pas de nouveau BC) ✓, D-AL-04 (aucune persistance d'état, calcul à la demande) ✓, D-SRC-03 IRL (filtre exercice courant pré-calculé par le use case via `dernierePourBail` + `dateEffet.year`, le domaine ne touche aucun repo) ✓, D-CFE6.5 (Clock-driven, anti-cron) ✓. Aucun item différé implémenté (pas de snooze, pas d'email, pas de cron, pas de workflow bail).
</verification>

<success_criteria>
- `src/application/dashboard/calculer-toutes-alertes.ts` expose `CalculerToutesAlertesDeps` + `calculerToutesAlertes(deps): Promise<Alerte[]>`, use case en lecture seule sans aucun import infra.
- Les 4 sources (CFE + IRL + diagnostic + fin de bail) sont fusionnées et triées ASC GLOBALEMENT par `joursRestants`.
- Le filtre IRL « exercice courant » est calculé par le use case (`indexationsParBail: Map<BailId, boolean>` via `dernierePourBail` + `dateEffet.year === maintenant.year`) puis injecté dans `calculerAlertesIrl` — le domaine ne touche aucun repository (contrat 07-02 honoré).
- Pattern Clock-driven strict : `deps.clock.aujourdhui()` est l'unique source de date, aucun `new Date()`/`Temporal.Now`/cron/setInterval.
- Builder `uneAlerteDiagnostic` ajouté — les 4 types d'alerte sont disponibles pour le dashboard 07-05.
- vitest, cucumber (`@phase7-alerte-agregation`), tsc, lint, dependency-cruiser : 0 fail / 0 warning / 0 violation. Aucune migration, aucune mutation d'agrégat.
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-notifications-d-ch-ances/07-04-SUMMARY.md` (template @/Users/valentinshodo/.claude/get-shit-done/templates/summary.md). Documenter la signature exacte `calculerToutesAlertes(deps): Promise<Alerte[]>`, l'interface `CalculerToutesAlertesDeps` (4 repos + clock) et la convention de calcul du filtre exercice courant (`dernierePourBail` + `dateEffet.year === maintenant.year` → `Map<BailId, boolean>`) pour le wiring DI de la route dashboard 07-05.
</output>
