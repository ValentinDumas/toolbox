---
phase: 07-dashboard-notifications-d-ch-ances
plan: 08
type: tdd
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - tests/unit/patrimoine/alerte-diagnostic.test.ts
  - src/domain/patrimoine/alerte-diagnostic.ts
  - tests/_builders/alertes.ts
autonomous: true
requirements: [DAS-02]
user_setup: []

must_haves:
  truths:
    - "Cliquer « Voir le diagnostic » sur une alerte diagnostic critique mène à une page existante (HTTP 200), pas un 404."
    - "L'urlAction d'une alerte diagnostic pointe vers GET /biens/:id (route existante) et cible l'ancre #diagnostics-heading (qui existe dans detail.ejs)."
  artifacts:
    - path: "src/domain/patrimoine/alerte-diagnostic.ts"
      provides: "urlAction = /biens/{bien.id}#diagnostics-heading"
      contains: "#diagnostics-heading"
    - path: "tests/unit/patrimoine/alerte-diagnostic.test.ts"
      provides: "Assertion urlAction sur la cible corrigée"
      contains: "#diagnostics-heading"
  key_links:
    - from: "src/domain/patrimoine/alerte-diagnostic.ts:62"
      to: "GET /biens/:id (src/web/routes/biens.ts:98) + ancre #diagnostics-heading (detail.ejs:119)"
      via: "urlAction interpolé `/biens/${bien.id}#diagnostics-heading`"
      pattern: "#diagnostics-heading"
---

<objective>
Fermer le gap UAT (test 4, sévérité major) : cliquer « Voir le diagnostic » sur une
alerte diagnostic critique renvoie un HTTP 404.

Cause racine confirmée empiriquement en UAT :
`src/domain/patrimoine/alerte-diagnostic.ts:62` produit
`urlAction: /biens/${bien.id}/diagnostics#diag-${diag.type}`. Or :
- `GET /biens/:id/diagnostics` N'EXISTE PAS (seuls `GET /biens/:id/diagnostics/nouveau`
  et `POST /biens/:id/diagnostics` sont déclarés dans `src/web/routes/diagnostics.ts`). → 404.
- L'ancre `#diag-${type}` n'existe NULLE PART dans les vues.
- La page qui liste réellement les diagnostics est `GET /biens/:id` (200, `src/web/routes/biens.ts:98`),
  rendue par `detail.ejs`, dont le bloc diagnostics est
  `<section aria-labelledby="diagnostics-heading">` avec `<h2 id="diagnostics-heading">` (ligne 119-120).

Fix vérifié : changer urlAction en `/biens/${bien.id}#diagnostics-heading`
(route existante → 200, ancre existante → scroll vers la section diagnostics).
Les ancres par type `#diag-${type}` n'existent pas — NE PAS les inventer (hors périmètre,
ajouterait des ids dans detail.ejs ; on reste chirurgical).

Note : ce plan est indexé 07-08 (et non 07-07) car 07-07-PLAN.md existe déjà et ferme
des gaps DIFFÉRENTS (WR-01..05 de 07-VERIFICATION.md). Aucun chevauchement de fichiers
avec 07-07 → les deux plans restent cohérents et exécutables indépendamment.

Note : l'urlAction de l'alerte IRL `/baux/${bail.id}/indexer` a été vérifiée à 200 en UAT
→ NE PAS y toucher. Seule l'alerte diagnostic est cassée.

Purpose: prouver TRUE le critère « notification → écran d'action en un clic » pour les
diagnostics (truth #4 de la Phase 7) sans replanifier.
Output: urlAction diagnostic corrigée dans le domaine + son test unitaire + le builder de
fixture, tous alignés sur la cible existante (RED→GREEN).
</objective>

<execution_context>
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/workflows/execute-plan.md
@/Users/valentinshodo/Projects/toolbox/gestion-locative/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-dashboard-notifications-d-ch-ances/07-UAT.md
@CLAUDE.md
@practices/BDD_PRACTICES.md
@practices/DDD.md

<interfaces>
<!-- Contrats vérifiés dans le codebase. L'executor implémente CONTRE eux — pas d'exploration. -->

From src/web/routes/biens.ts:98 :
  app.get('/biens/:id', ...)        // route EXISTANTE → 200, rend detail.ejs

From src/web/routes/diagnostics.ts :
  app.get('/biens/:id/diagnostics/nouveau', ...)   // existe
  app.post('/biens/:id/diagnostics', ...)          // existe
  // GET /biens/:id/diagnostics N'EXISTE PAS → 404 (cause du gap)

From src/web/views/pages/biens/detail.ejs:119-120 :
  <section aria-labelledby="diagnostics-heading">
    <h2 id="diagnostics-heading">Diagnostics ...</h2>   // ancre CIBLE — existe
  // aucun id="diag-dpe" / "diag-gaz" / "diag-elec" nulle part → ne PAS cibler par type

From src/domain/patrimoine/alerte-diagnostic.ts:62 (ligne buggée à corriger) :
  urlAction: `/biens/${bien.id}/diagnostics#diag-${diag.type}`,   // → 404

From tests/unit/patrimoine/alerte-diagnostic.test.ts:58 (UNIQUE assertion urlAction du fichier) :
  expect(alerte.urlAction).toBe(`/biens/${bien.id}/diagnostics#diag-dpe`);
  // La feature BDD tests/bdd/features/alerte-diagnostic.feature N'ASSERTE PAS urlAction
  // (vérifié par grep — aucune occurrence). Donc PAS de step BDD à modifier.

From tests/_builders/alertes.ts:93,101 (fixture par défaut — encode la MÊME URL buggée) :
  // JSDoc ligne 93 : "lien /biens/{bienId}/diagnostics#diag-dpe"
  urlAction: `/biens/${DEFAULT_BIEN_ID}/diagnostics#diag-dpe`,   // ligne 101 — à corriger
  // Aucun test n'asserte cette valeur via le builder (grep), donc pas de RED ici, mais on la
  // corrige (valeur + commentaire) pour ne pas propager le motif d'URL morte aux futurs tests
  // (cohérence fixture↔domaine).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (TDD) : corriger urlAction de l'alerte diagnostic vers la cible existante</name>
  <files>tests/unit/patrimoine/alerte-diagnostic.test.ts, src/domain/patrimoine/alerte-diagnostic.ts, tests/_builders/alertes.ts</files>
  <read_first>
    - src/domain/patrimoine/alerte-diagnostic.ts (ligne 62 — urlAction ; et la JSDoc en tête ; ne PAS toucher au reste)
    - tests/unit/patrimoine/alerte-diagnostic.test.ts:58 (l'unique assertion urlAction)
    - tests/_builders/alertes.ts:93,101 (JSDoc + default urlAction du builder uneAlerteDiagnostic)
    - src/web/views/pages/biens/detail.ejs:119-120 (l'ancre cible diagnostics-heading)
    - .planning/phases/07-dashboard-notifications-d-ch-ances/07-UAT.md (gap test 4)
  </read_first>
  <behavior>
    - calculerAlertesDiagnostic → chaque Alerte produit
      urlAction === `/biens/${bien.id}#diagnostics-heading`
    - L'urlAction ne contient plus le segment `/diagnostics` (route inexistante)
      ni l'ancre `#diag-${type}` (ancre inexistante).
    - Toutes les autres propriétés de l'Alerte (type, joursRestants, dateEcheance,
      libelle, source.extra.typeDiagnostic) restent INCHANGÉES.
    - Le builder uneAlerteDiagnostic produit par défaut la même cible
      `/biens/${DEFAULT_BIEN_ID}#diagnostics-heading`.
  </behavior>
  <action>
    RED — Dans `tests/unit/patrimoine/alerte-diagnostic.test.ts`, modifier l'assertion ligne 58
    de l'ancienne valeur `/biens/${bien.id}/diagnostics#diag-dpe` vers
    `/biens/${bien.id}#diagnostics-heading` (template literal, même forme).
    C'est la SEULE assertion urlAction du fichier (les autres `describe` n'asserttent pas urlAction).
    Lancer `pnpm vitest run tests/unit/patrimoine/alerte-diagnostic.test.ts` → l'assertion DOIT
    échouer en RED (le code produit encore l'ancienne URL).

    GREEN — Dans `src/domain/patrimoine/alerte-diagnostic.ts`, remplacer la valeur de `urlAction`
    (ligne 62) de `/biens/${bien.id}/diagnostics#diag-${diag.type}` vers
    `/biens/${bien.id}#diagnostics-heading`. Mettre à jour le commentaire JSDoc en tête de fonction
    s'il décrit l'ancienne URL, pour qu'aucune doc n'annonce plus une cible par type.
    `diag.type` n'est plus utilisé dans l'urlAction — mais il reste utilisé pour `libelle` (ligne 61)
    et `extra.typeDiagnostic` (ligne 67), donc AUCUN orphelin à retirer.
    Relancer → vert.

    ALIGNEMENT FIXTURE — Dans `tests/_builders/alertes.ts`, corriger le default du builder
    `uneAlerteDiagnostic` (ligne 101) vers `/biens/${DEFAULT_BIEN_ID}#diagnostics-heading` et
    mettre à jour la JSDoc ligne 93 en conséquence. Aucun test n'asserte cette valeur (donc pas
    de RED dédié), mais on évite que la fixture propage le motif d'URL morte.

    REFACTOR — aucun. Ne PAS toucher aux autres modules d'alerte (alerte-cfe-j30.ts, alerte-irl.ts
    `/baux/.../indexer` vérifié 200, alerte-fin-bail.ts). Domaine pur préservé : aucun import
    technique introduit dans alerte-diagnostic.ts.
  </action>
  <acceptance_criteria>
    - `grep -c "/diagnostics#diag-" src/domain/patrimoine/alerte-diagnostic.ts` retourne 0.
    - `grep -c "#diagnostics-heading" src/domain/patrimoine/alerte-diagnostic.ts` retourne 1.
    - `grep -c "#diagnostics-heading" tests/unit/patrimoine/alerte-diagnostic.test.ts` retourne 1.
    - `grep -c "/diagnostics#diag-" tests/_builders/alertes.ts` retourne 0 (fixture alignée).
    - `pnpm vitest run tests/unit/patrimoine/alerte-diagnostic.test.ts` exit 0.
    - Aucun import infrastructure/ ajouté dans alerte-diagnostic.ts (dependency-cruiser vert).
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/unit/patrimoine/alerte-diagnostic.test.ts && grep -c "#diagnostics-heading" src/domain/patrimoine/alerte-diagnostic.ts | grep -qx 1 && grep -c "/diagnostics#diag-" src/domain/patrimoine/alerte-diagnostic.ts | grep -qx 0 && grep -c "/diagnostics#diag-" tests/_builders/alertes.ts | grep -qx 0 && echo OK</automated>
  </verify>
  <done>L'urlAction de l'alerte diagnostic (domaine + builder de fixture) pointe vers /biens/:id#diagnostics-heading (route + ancre existantes) ; le test unitaire asserte la cible corrigée et passe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domaine (alerte-diagnostic) → vue EJS (href) | L'urlAction est rendue dans un `href` du dashboard. La valeur est entièrement construite à partir d'un littéral statique + `bien.id` interne (pas d'entrée utilisateur libre). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08-01 | Tampering (open redirect / injection href) | urlAction `/biens/${bien.id}#diagnostics-heading` rendu en href | accept | URL relative interne, préfixe statique en dur ; `bien.id` est un identifiant interne (ULID/UUID) non saisi par un tiers. Aucune interpolation de donnée externe non maîtrisée. Application local-first mono-utilisateur (CLAUDE.md). |
</threat_model>

<verification>
- `pnpm vitest run tests/unit/patrimoine/alerte-diagnostic.test.ts` : vert (assertion urlAction corrigée).
- `pnpm vitest run` : suite complète verte (aucune régression — seul le littéral urlAction change).
- `pnpm exec cucumber-js --tags "@phase7-alerte-diagnostic"` : vert (les scénarios diagnostic
  ne dépendent pas de urlAction, mais on confirme l'absence de régression).
- `pnpm tsc --noEmit` : 0 erreur.
- `pnpm lint:deps` (dependency-cruiser) : aucun import infra dans le domaine.
- Grep gates des `<acceptance_criteria>` satisfaits.
</verification>

<success_criteria>
Gap UAT (test 4) fermé :
- `urlAction` de chaque alerte diagnostic = `/biens/${bien.id}#diagnostics-heading`.
- Cette URL résout sur `GET /biens/:id` (route existante → 200) et l'ancre `#diagnostics-heading`
  existe dans detail.ejs → le clic « Voir le diagnostic » scrolle vers la section diagnostics
  au lieu de retourner 404.
- Périmètre chirurgical : un seul littéral changé dans le domaine + son unique assertion +
  l'alignement de la fixture ; aucun module sibling d'alerte touché ; pureté hexagonale préservée.
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-notifications-d-ch-ances/07-08-SUMMARY.md`.
</output>
</content>
