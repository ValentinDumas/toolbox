---
phase: 07-dashboard-notifications-d-ch-ances
plan: 05
type: execute
wave: 3
depends_on: ["07-04"]
files_modified:
  - src/web/routes/racine.ts
  - src/web/helpers/alerte-helpers.ts
  - src/web/views/pages/dashboard/accueil.ejs
  - src/web/views/partials/partial-bandeau-alerte.ejs
  - src/web/views/partials/sidebar-nav.ejs
  - src/main.ts
  - tests/bdd/features/dashboard-composition.feature
  - tests/bdd/features/dashboard-empty-state.feature
  - tests/bdd/features/dashboard-premier-lancement.feature
  - tests/bdd/steps/dashboard.steps.ts
autonomous: true
requirements: [DAS-01, DAS-02]

must_haves:
  truths:
    - "`GET /` rend le dashboard `pages/dashboard/accueil.ejs` avec 4 sections empilées (Alertes critiques, Impayés, Actions du jour, Échéances loyer à venir) quand `estPremierLancement(db) === false`"
    - "`GET /` redirige vers `/wizard/bien` quand `estPremierLancement(db) === true` (KPI Activation Phase 1 préservé)"
    - "La section Alertes critiques affiche `calculerToutesAlertes(deps).filter(a => a.joursRestants <= 7)`, top 5, triées ASC, rendues par le partial unifié `partial-bandeau-alerte.ejs`"
    - "La section Impayés affiche `listerImpayes()` top 5 ; la section Échéances loyer à venir affiche les échéances non payées dont `jourEcheanceAttendue ∈ [today, today.add({months:2}).with({day:1}).subtract({days:1})]` top 5"
    - "La section Actions du jour compose relances dues (`calculerRelanceDisponible !== null`) + alertes IRL J-30..J-0 (redondance S1/S3 intentionnelle, D-DASH-02 §3)"
    - "Si TOUTES les sections sont vides → un seul bandeau succès `role='status' aria-live='polite'` 'Vous êtes à jour' s'affiche, les 4 sections ne sont PAS rendues"
    - "`partial-bandeau-alerte.ejs` reproduit à l'identique les 3 variantes tri-état (destructive/warning-fort/warning) du `partial-bandeau-cfe-echeance.ejs` + switch polymorphe sur `alerte.type` pour libellé et lien"
    - "L'entrée sidebar 'Tableau de bord' est en 1ère position avec `aria-current='page'` quand `navActive==='dashboard'`"
  artifacts:
    - path: "src/web/routes/racine.ts"
      provides: "Route GET / : branche premier lancement + render dashboard avec 8 locals de composition"
      contains: "calculerToutesAlertes"
    - path: "src/web/helpers/alerte-helpers.ts"
      provides: "Helpers EJS purs formaterAlerteUrgence, iconeTypeAlerte, libelleTypeAlerte"
      exports: ["formaterAlerteUrgence", "iconeTypeAlerte", "libelleTypeAlerte"]
    - path: "src/web/views/pages/dashboard/accueil.ejs"
      provides: "Vue dashboard 4 sections inline + bandeau succès état vide"
      contains: "titre-alertes-critiques"
    - path: "src/web/views/partials/partial-bandeau-alerte.ejs"
      provides: "Bandeau alerte unifié polymorphe (3 variantes + inline)"
      contains: "alerte.type"
    - path: "src/web/views/partials/sidebar-nav.ejs"
      provides: "Entrée Tableau de bord en 1ère position"
      contains: "navActive === 'dashboard'"
  key_links:
    - from: "src/web/routes/racine.ts"
      to: "src/application/dashboard/calculer-toutes-alertes.ts"
      via: "import { calculerToutesAlertes } + appel avec deps"
      pattern: "calculerToutesAlertes"
    - from: "src/web/routes/racine.ts"
      to: "src/domain/encaissements/impaye.ts"
      via: "import { listerImpayes }"
      pattern: "listerImpayes"
    - from: "src/web/routes/racine.ts"
      to: "src/web/views/pages/dashboard/accueil.ejs"
      via: "reply.view('pages/dashboard/accueil.ejs', locals)"
      pattern: "pages/dashboard/accueil"
    - from: "src/web/views/pages/dashboard/accueil.ejs"
      to: "src/web/views/partials/partial-bandeau-alerte.ejs"
      via: "include partial-bandeau-alerte pour chaque alerte critique"
      pattern: "partial-bandeau-alerte"
    - from: "src/main.ts"
      to: "src/web/routes/racine.ts"
      via: "app.register(racinePlugin, { db, cfeRepo, bienRepo, bailRepo, bailIndexationRepo, echeanceLoyerRepo, encaissementRepo, locataireRepo, relanceRepo, clock })"
      pattern: "racinePlugin"
---

<objective>
Livrer la slice verticale du dashboard (DAS-01) : `GET /` rend `pages/dashboard/accueil.ejs` avec 4 sections empilées par urgence (Alertes critiques, Impayés, Actions du jour, Échéances loyer à venir), consommant l'agrégateur `calculerToutesAlertes` (07-04) + les sources Phase 2 existantes (`listerImpayes`, `calculerRelanceDisponible`, `EcheanceLoyerRepository`). Le partial unifié `partial-bandeau-alerte.ejs` (D-AL-05) rend les alertes polymorphes (cfe/irl/diagnostic/fin_bail) avec les 3 variantes tri-état WCAG. La branche `estPremierLancement → /wizard/bien` est préservée (KPI Activation Phase 1).

Purpose : c'est la slice utilisateur de DAS-01 — après ce plan, l'utilisateur ouvre `/` et voit immédiatement ce qu'il doit faire (impayés, échéances, alertes J-30/J-7 agrégées). C'est le point d'entrée HTTP qui consomme la donnée agrégée par 07-04 et la donne à voir.

Output : `racine.ts` réécrit (render dashboard + composition), `accueil.ejs` (4 sections inline), `partial-bandeau-alerte.ejs` (nouveau, polymorphe), `alerte-helpers.ts` (3 helpers purs), `sidebar-nav.ejs` étendu (entrée Tableau de bord), wiring DI dans `main.ts`, 3 features BDD (composition, état vide, premier lancement). Aucune migration SQLite, aucune mutation de domaine.
</objective>

<execution_context>
@/Users/valentinshodo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/valentinshodo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dashboard-notifications-d-ch-ances/07-CONTEXT.md
@.planning/phases/07-dashboard-notifications-d-ch-ances/07-UI-SPEC.md
@.planning/phases/07-dashboard-notifications-d-ch-ances/07-PATTERNS.md
@.planning/phases/07-dashboard-notifications-d-ch-ances/07-04-PLAN.md
@.planning/phases/07-dashboard-notifications-d-ch-ances/07-01-PLAN.md
@src/web/routes/racine.ts
@src/web/views/partials/partial-bandeau-cfe-echeance.ejs
@src/web/views/partials/sidebar-nav.ejs

<interfaces>
<!-- État actuel exact extrait du codebase. L'exécuteur n'a PAS besoin d'explorer. -->

Use case agrégateur (07-04, `src/application/dashboard/calculer-toutes-alertes.ts`) :
```typescript
export interface CalculerToutesAlertesDeps { cfeRepo: DeclarationCfeRepository; bienRepo: BienRepository; bailRepo: BailRepository; bailIndexationRepo: BailIndexationRepository; clock: Clock; }
export function calculerToutesAlertes(deps: CalculerToutesAlertesDeps): Promise<Alerte[]>;
```

Contrat `Alerte` (07-01, `src/domain/_shared/alerte.ts`) :
```typescript
export type TypeAlerte = 'cfe' | 'irl' | 'diagnostic' | 'fin_bail';
export interface Alerte {
  readonly type: TypeAlerte;
  readonly joursRestants: number;        // peut être négatif
  readonly dateEcheance: Temporal.PlainDate;
  readonly libelle: string;
  readonly urlAction: string;
  readonly source: { readonly type: TypeAlerte; readonly refId: string; readonly bienId?: BienId; readonly extra?: Record<string, unknown> };
}
```
`source.extra` connu par type : cfe → `{ millesime, statutCfe, dateEcheancePaiement }` ; irl → `{ nomLocataire, adresseBien }` ; diagnostic → `{ typeDiagnostic }` ; fin_bail → `{ nomLocataire }` (07-02/07-03).

Section Impayés — `listerImpayes` (`src/domain/encaissements/impaye.ts`), signature RÉELLE :
```typescript
export async function listerImpayes(
  filtres: { locataireId?: LocataireId },
  repos: { echeanceLoyerRepo: EcheanceLoyerRepository; encaissementRepo: EncaissementRepository; bailRepo: BailRepository; locataireRepo: LocataireRepository },
  clock: Clock,
): Promise<Impaye[]>;   // déjà trié par jourEcheanceAttendue ASC
```
DTO `Impaye` champs RÉELS (à utiliser dans l'EJS — NE PAS inventer `libelleEcheance`/`nomLocataire`/`montantDu`) :
`echeanceId, bailId, locataireId, locataireNomComplet, periodeDebut, periodeFin, jourEcheanceAttendue, total (Money), sommePaiee (Money), resteDu (Money), statut, joursDeRetard, estEnRetard`.
Filtrer côté route : `impayes.filter(i => i.estEnRetard)` ; trier déjà fait ASC mais l'UI-SPEC demande `joursDeRetard` DESC → re-trier `.sort((a,b) => b.joursDeRetard - a.joursDeRetard)`. `formatMoney(impaye.resteDu)` pour le montant dû.

Section Actions du jour — `calculerRelanceDisponible` (`src/application/encaissements/calculer-relance-disponible.ts`) :
```typescript
export function calculerRelanceDisponible(echeanceOuImpaye: { jourEcheanceAttendue: Temporal.PlainDate }, relances: { niveau: NiveauRelance; annuleLe: ... }[], today: Temporal.PlainDate): NiveauRelance | null;
```
`NiveauRelance` = 1 | 2 | 3. `null` = pas de relance disponible. `relanceRepo.listerParEcheance(echeanceId)` fournit les relances (vérifier la méthode exacte dans `src/domain/encaissements/relance-repository.ts` ; à défaut `listerParBail` puis filtrer).

Section Échéances loyer à venir — `EcheanceLoyerRepository` (`src/domain/encaissements/echeance-loyer-repository.ts`) — il N'Y A PAS de `listerAVenir`. Méthodes disponibles : `listerNonPayees(): Promise<EcheanceLoyer[]>` (statut IN en_attente/partiellement_payee), `listerTous(filtres?: { bailId?; statut? })`. UTILISER `listerNonPayees()` puis filtrer dans la route : `e.jourEcheanceAttendue` dans la fenêtre `[today, today.add({months:2}).with({day:1}).subtract({days:1})]` (mois courant + 1 mois, D-DASH-02 §4 + Claude's Discretion). `EcheanceLoyer` a `jourEcheanceAttendue: Temporal.PlainDate`, `total: Money`, `periodeDebut/periodeFin`, `bailId`. Pour le nom locataire : non requis V1 dans cette section (UI-SPEC S5 affiche libellé période + montant ; le nom est optionnel — si besoin, joindre via `bailRepo` + `locataireRepo`, sinon afficher la période seule).

Composition root `main.ts` ligne 246 — état ACTUEL : `await app.register(racinePlugin, { db });`. Repos déjà instanciés disponibles (lignes 160-190) : `repo` (=BienRepository), `bailRepo`, `locataireRepo`, `echeanceLoyerRepo`, `encaissementRepo`, `bailIndexationRepo`, `relanceRepo`, `cfeRepo`, `clock`. Tous à passer en opts au racinePlugin.

Hook `preHandler` global `main.ts` lignes 195-215 : injecte déjà `formatDate, formatMoney, formatPeriode, formaterClasseDpe, today, ...` dans `reply.locals`. C'est LÀ qu'ajouter `formaterAlerteUrgence, iconeTypeAlerte, libelleTypeAlerte` (importés de `src/web/helpers/alerte-helpers.ts`).

Route `racine.ts` ACTUEL (15 lignes) :
```typescript
export async function plugin(app: FastifyInstance, opts: { db: Kysely<DB> }): Promise<void> {
  app.get('/', async (req, reply) => {
    const premier = await estPremierLancement(opts.db);
    if (premier) return reply.redirect('/wizard/bien');
    return reply.redirect('/biens');   // ← remplacer par render dashboard
  });
}
```

`partial-bandeau-cfe-echeance.ejs` (RÉFÉRENCE VISUELLE EXACTE, 69 lignes) : calcul `j` → `variant`/`role`/`styleVariant` lignes 16-46, markup `<aside role aria-live aria-label style>` lignes 47-69. `partial-bandeau-alerte.ejs` reproduit ce calcul à l'identique + ajoute le switch sur `alerte.type` (markup EJS prescrit dans UI-SPEC §Partial unifié, lignes 449-530).

`sidebar-nav.ejs` ACTUEL : `<ul>` avec 1er `<li>` = lien `/biens`. Insérer AVANT, en 1ère position, le `<li>` lien `/` "Tableau de bord" (markup UI-SPEC §Sidebar lignes 599-631).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helpers EJS `alerte-helpers.ts` + partial unifié `partial-bandeau-alerte.ejs` + entrée sidebar</name>
  <files>src/web/helpers/alerte-helpers.ts, src/web/views/partials/partial-bandeau-alerte.ejs, src/web/views/partials/sidebar-nav.ejs</files>
  <read_first>
    - src/web/views/partials/partial-bandeau-cfe-echeance.ejs (RÉFÉRENCE VISUELLE EXACTE — lire les 69 lignes : calcul j→variant→role→styleVariant + markup aside)
    - .planning/phases/07-dashboard-notifications-d-ch-ances/07-UI-SPEC.md §Partial unifié (lignes 434-540 : markup EJS prescrit + mapping urlAction + tableau libellés/liens par type) et §Copywriting §Libellés d'urgence (lignes 681-712) et §Helpers EJS (lignes 579-589)
    - src/web/helpers/ (un helper existant, ex. format français, pour imiter le style de module + export nommé)
    - src/web/views/partials/sidebar-nav.ejs (markup actuel — insérer entrée Tableau de bord en 1ère position)
    - src/domain/_shared/alerte.ts (type Alerte + TypeAlerte — contrat 07-01)
  </read_first>
  <action>
    Créer `src/web/helpers/alerte-helpers.ts` (3 fonctions PURES, déterministes, aucun I/O, aucun import infra) :
    - `export function formaterAlerteUrgence(alerte: Alerte): string` — dérive le libellé WCAG depuis `alerte.joursRestants` : si `j < 0` → `` `Échéance dépassée depuis ${Math.abs(j)} jour${Math.abs(j) > 1 ? 's' : ''}` `` ; si `j === 0` → `"Échéance aujourd'hui"` ; sinon → `` `Échéance dans ${j} jour${j > 1 ? 's' : ''}` `` (libellés IDENTIQUES à `partial-bandeau-cfe-echeance.ejs` lignes 21-34).
    - `export function iconeTypeAlerte(type: TypeAlerte): string` — map `{ cfe: '€', irl: '%', diagnostic: '⚠', fin_bail: '⏰' }`, fallback `'ⓘ'`. Caractère destiné à être rendu `aria-hidden="true"`.
    - `export function libelleTypeAlerte(alerte: Alerte): string` — switch sur `alerte.type` : `cfe` → `` `CFE ${alerte.source.extra?.['millesime'] ?? ''}` ``.trim() ; `irl` → `"Révision IRL"` ; `diagnostic` → selon `alerte.source.extra?.['typeDiagnostic']` : `'dpe' → 'DPE'`, `'gaz' → 'Gaz'`, `'electricite' → 'Électricité'`, fallback `"Diagnostic"` ; `fin_bail` → `` `Fin de bail — ${alerte.source.extra?.['nomLocataire'] ?? ''}` ``.trim() ou `"Fin de bail"` si nom vide. Importer `type Alerte, type TypeAlerte` depuis `../../domain/_shared/alerte.js`.
    - Header JSDoc fr référençant Phase 7 / DAS-01 / D-DASH-03 / D-AL-05 + WCAG 1.4.1 (couleur jamais seule). AUCUN import Fastify/infra.

    Créer `src/web/views/partials/partial-bandeau-alerte.ejs` selon le MARKUP PRESCRIT UI-SPEC §Partial unifié (lignes 449-530) — reproduire À L'IDENTIQUE le calcul de `partial-bandeau-cfe-echeance.ejs` :
    - `const j = alerte.joursRestants;` puis : `j <= 0` → `variant='destructive'`, `role='alert'`, `ariaLive='assertive'` ; `j <= 7` → `variant='warning-fort'`, `role='alert'`, `ariaLive='assertive'` ; sinon → `variant='warning'`, `role='status'`, `ariaLive='polite'`.
    - `styleBase = "padding: 16px; margin-bottom: 16px;"` ; `styleVariant` : destructive → `"background: var(--couleur-destructive-bg); border-left: 4px solid var(--couleur-destructive);"` ; warning-fort → `"background: var(--couleur-warning-bg, #FFF4E6); border-left: 4px solid var(--couleur-warning, #C2410C);"` ; warning → `"border-left: 4px solid var(--couleur-warning, #C2410C); padding-left: 12px;"`.
    - Calculer `libelleUrgence = formaterAlerteUrgence(alerte)`, `libelleType = libelleTypeAlerte(alerte)`, `icone = iconeTypeAlerte(alerte.type)`.
    - `<aside role="<%= role %>" aria-live="<%= ariaLive %>" aria-label="Alerte <%= libelleType %>" style="<%= styleBase %> <%= styleVariant %>">` contenant `<p><span aria-hidden="true"><%= icone %></span> <strong><%= libelleType %> — <%= libelleUrgence %>.</strong> Échéance le <%= formatDate(alerte.dateEcheance) %>.</p>`.
    - Bloc d'action conditionné par `<% if (!locals.inline) { %>` (prop `inline` = mode compact pour la table `/baux/indexations` du plan 07-06) — switch sur `alerte.type` : `cfe` → lien externe `href="https://www.impots.gouv.fr/professionnel/cotisation-fonciere-des-entreprises-cfe" target="_blank" rel="noopener noreferrer" role="button"` "Régler la CFE sur impots.gouv.fr" + lien interne `<a href="<%= alerte.urlAction %>">Mettre à jour le statut</a>` ; `irl` → `<a href="<%= alerte.urlAction %>" role="button">Lancer la révision IRL</a>` ; `diagnostic` → `<a href="<%= alerte.urlAction %>" role="button">Voir le diagnostic</a>` ; `fin_bail` → `<a href="<%= alerte.urlAction %>" role="button">Voir le bail</a>`.
    - Header `<%# ... %>` documentant les props (`alerte: Alerte`, `inline?: boolean`) et les helpers attendus (`formatDate`, `formaterAlerteUrgence`, `iconeTypeAlerte`, `libelleTypeAlerte`).
    - NE PAS modifier `partial-bandeau-cfe-echeance.ejs` ni `partial-indexation-banner.ejs` (la migration Phase 6/3 est OPTIONNELLE et hors scope de ce plan — D-AL-05 ; conserver les deux partials, noter dans le SUMMARY).

    Modifier `src/web/views/partials/sidebar-nav.ejs` : insérer en PREMIÈRE position dans `<ul>`, avant le `<li>` du lien `/biens`, le bloc : `<li><a href="/"<% if (locals.navActive === 'dashboard') { %> aria-current="page"<% } %>>Tableau de bord</a></li>`. Ne toucher à AUCUNE autre entrée existante.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm lint src/web/helpers/alerte-helpers.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function formaterAlerteUrgence" src/web/helpers/alerte-helpers.ts` → 1
    - `grep -c "export function iconeTypeAlerte" src/web/helpers/alerte-helpers.ts` → 1
    - `grep -c "export function libelleTypeAlerte" src/web/helpers/alerte-helpers.ts` → 1
    - `grep -E "from '(fastify|kysely|better-sqlite3|node:|.*/infrastructure/)" src/web/helpers/alerte-helpers.ts` → aucune sortie (helpers purs)
    - `grep -c "alerte.type === 'cfe'" src/web/views/partials/partial-bandeau-alerte.ejs` → ≥ 1 (switch polymorphe)
    - `grep -cE "'cfe'|'irl'|'diagnostic'|'fin_bail'" src/web/views/partials/partial-bandeau-alerte.ejs` → ≥ 4 (les 4 types couverts)
    - `grep -c "rel=\"noopener noreferrer\"" src/web/views/partials/partial-bandeau-alerte.ejs` → 1 (lien externe CFE sécurisé)
    - `grep -cE "role=\"alert\"|role=\"status\"" src/web/views/partials/partial-bandeau-alerte.ejs` → ≥ 2 (variantes tri-état)
    - `grep -c "aria-hidden=\"true\"" src/web/views/partials/partial-bandeau-alerte.ejs` → ≥ 1 (icône non porteuse de sens seule, WCAG 1.4.1)
    - `grep -c "locals.inline" src/web/views/partials/partial-bandeau-alerte.ejs` → ≥ 1 (mode compact pour 07-06)
    - `git diff --name-only` ne liste PAS `partial-bandeau-cfe-echeance.ejs` (non modifié)
    - `grep -c "navActive === 'dashboard'" src/web/views/partials/sidebar-nav.ejs` → 1 ; l'entrée Tableau de bord apparaît AVANT l'entrée Biens dans le fichier
    - `pnpm tsc --noEmit` → 0 erreur
  </acceptance_criteria>
  <done>Helpers purs `formaterAlerteUrgence`/`iconeTypeAlerte`/`libelleTypeAlerte` créés ; partial `partial-bandeau-alerte.ejs` polymorphe (4 types, 3 variantes tri-état, lien externe sécurisé, mode inline) reproduisant le pattern Phase 6 ; sidebar augmentée (Tableau de bord 1ère position). tsc + lint verts. Aucune migration, aucune mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Route `GET /` dashboard — composition 4 sections + branche premier lancement + wiring DI main.ts + vue accueil.ejs</name>
  <files>src/web/routes/racine.ts, src/web/views/pages/dashboard/accueil.ejs, src/main.ts</files>
  <read_first>
    - src/web/routes/racine.ts (route actuelle à réécrire — branche estPremierLancement à PRÉSERVER)
    - src/web/routes/impayes.ts (PATTERN plugin Fastify : opts multi-repos + clock + reply.view, modèle d'écriture exact)
    - src/domain/encaissements/impaye.ts (signature listerImpayes(filtres, repos, clock) + DTO Impaye champs RÉELS — locataireNomComplet/resteDu/joursDeRetard/jourEcheanceAttendue/estEnRetard)
    - src/application/encaissements/calculer-relance-disponible.ts (signature + NiveauRelance + null)
    - src/domain/encaissements/echeance-loyer-repository.ts (PAS de listerAVenir — utiliser listerNonPayees + filtrer dans la route)
    - src/domain/encaissements/relance-repository.ts (méthode pour lister les relances d'une échéance — listerParEcheance ou équivalent)
    - src/application/dashboard/calculer-toutes-alertes.ts (use case 07-04 : CalculerToutesAlertesDeps + calculerToutesAlertes)
    - src/main.ts lignes 160-215 (repos instanciés + hook preHandler global où injecter les 3 helpers) et lignes 246-268 (registrations — modèle d'enregistrement)
    - .planning/phases/07-dashboard-notifications-d-ch-ances/07-UI-SPEC.md §S1-S5 (lignes 137-353 : structure des 4 sections + IDs ARIA + empty states) et §Composition use case dashboard (lignes 892-914 : liste des 8 locals)
    - src/web/views/pages/impayes/liste.ejs (PATTERN layout-debut + section conditionnelle EJS)
  </read_first>
  <action>
    Réécrire `src/web/routes/racine.ts`. Nouvelle signature opts :
    `export async function plugin(app: FastifyInstance, opts: { db: Kysely<DB>; cfeRepo: DeclarationCfeRepository; bienRepo: BienRepository; bailRepo: BailRepository; bailIndexationRepo: BailIndexationRepository; echeanceLoyerRepo: EcheanceLoyerRepository; encaissementRepo: EncaissementRepository; locataireRepo: LocataireRepository; relanceRepo: RelanceRepository; clock: Clock }): Promise<void>`.
    Imports types domaine seulement (pas d'implémentation infra ici sauf `DB`/`Kysely` déjà présents) + `import { calculerToutesAlertes } from '../../application/dashboard/calculer-toutes-alertes.js'` + `import { listerImpayes } from '../../domain/encaissements/impaye.js'` + `import { calculerRelanceDisponible } from '../../application/encaissements/calculer-relance-disponible.js'`.

    Dans `app.get('/', ...)` :
    1. `const premier = await estPremierLancement(opts.db); if (premier) return reply.redirect('/wizard/bien');` (BRANCHE PRÉSERVÉE — D-DASH-01).
    2. `const today = opts.clock.aujourdhui();`
    3. Alertes : `const toutesAlertes = await calculerToutesAlertes({ cfeRepo, bienRepo, bailRepo, bailIndexationRepo, clock });` (passer les repos depuis opts). `const alertesCritiquesAll = toutesAlertes.filter(a => a.joursRestants <= 7);` `const alertesCritiques = alertesCritiquesAll.slice(0, 5);` `const alertesCritiquesTotal = alertesCritiquesAll.length;`
    4. Impayés : `const tousImpayes = (await listerImpayes({}, { echeanceLoyerRepo, encaissementRepo, bailRepo, locataireRepo }, clock)).filter(i => i.estEnRetard).sort((a,b) => b.joursDeRetard - a.joursDeRetard);` `const impayes = tousImpayes.slice(0, 5); const impayesTotal = tousImpayes.length;`
    5. Échéances à venir : `const finFenetre = today.add({ months: 2 }).with({ day: 1 }).subtract({ days: 1 });` ; charger `const nonPayees = await echeanceLoyerRepo.listerNonPayees();` ; filtrer `const echeancesAVenirAll = nonPayees.filter(e => Temporal.PlainDate.compare(e.jourEcheanceAttendue, today) >= 0 && Temporal.PlainDate.compare(e.jourEcheanceAttendue, finFenetre) <= 0).sort((a,b) => Temporal.PlainDate.compare(a.jourEcheanceAttendue, b.jourEcheanceAttendue));` ; `const echeancesAVenir = echeancesAVenirAll.slice(0, 5); const echeancesAVenirTotal = echeancesAVenirAll.length;`
    6. Actions du jour (composition 2 sources, D-DASH-02 §3) : relances dues = pour chaque impayé ouvert, `const relances = await relanceRepo.listerParEcheance(impaye.echeanceId)` (utiliser la méthode réelle vue dans relance-repository.ts) puis `const niveau = calculerRelanceDisponible(impaye, relances, today)` ; garder ceux avec `niveau !== null` → construire un objet `{ type: 'relance' as const, niveau, echeanceId, libellePeriode, nomLocataire: impaye.locataireNomComplet }`. IRL J-30..J-0 = `toutesAlertes.filter(a => a.type === 'irl' && a.joursRestants >= 0 && a.joursRestants <= 30)` → mapper `{ type: 'irl' as const, bailId: a.source.refId, joursRestants: a.joursRestants, nomLocataire: a.source.extra?.nomLocataire ?? '' }`. Fusionner → `actionsJourAll`, trier par urgence (relances d'abord par niveau DESC, puis IRL par joursRestants ASC). `const actionsJour = actionsJourAll.slice(0, 5); const actionsJourTotal = actionsJourAll.length;`
    7. `const etatGlobal = (alertesCritiquesTotal === 0 && impayesTotal === 0 && actionsJourTotal === 0 && echeancesAVenirTotal === 0) ? 'a_jour' : 'avec_alertes';`
    8. `return reply.view('pages/dashboard/accueil.ejs', { titre: 'Tableau de bord', navActive: 'dashboard', alertesCritiques, alertesCritiquesTotal, impayes, impayesTotal, actionsJour, actionsJourTotal, echeancesAVenir, echeancesAVenirTotal, etatGlobal });`
    JSDoc fr : Phase 7 / DAS-01 / D-DASH-01..04 / calcul à la demande Clock-driven (D-AL-03/D-CFE6.5). Pattern erreur : route lecture pure, laisser remonter au handler global Fastify (main.ts setErrorHandler). NE PAS `new Date()`/`setInterval`.

    Créer `src/web/views/pages/dashboard/accueil.ejs` selon UI-SPEC §S1-S5 (markup prescrit). Structure :
    - `<%- include('../../partials/layout-debut', { titre: 'Tableau de bord', breadcrumbs: [], navActive: 'dashboard' }) %>` puis `<h1>Tableau de bord</h1>`.
    - Si `etatGlobal === 'a_jour'` → afficher UNIQUEMENT `<aside role="status" aria-live="polite" class="banniere-success"><span aria-hidden="true">✓</span> Vous êtes à jour. Aucune action critique en attente.</aside>` et NE PAS rendre les 4 sections (suppression du bruit, UI-SPEC S6).
    - Sinon, rendre les 4 sections empilées, chacune `<section aria-labelledby="{id}">` + `<h2 id="{id}">` + lien `Voir tout (N)` conditionnel (`> 5`) + empty-state inline (`<p>` neutre) si la section est vide. IDs EXACTS : `titre-alertes-critiques`, `titre-impayes`, `titre-actions-jour`, `titre-echeances-venir`.
      - S2 Alertes critiques : `<% alertesCritiques.forEach(alerte => { %><%- include('../../partials/partial-bandeau-alerte', { alerte }) %><% }) %>` ; lien `Voir tout` → ancre `#toutes-alertes-critiques` ; empty `"Aucune alerte critique. Toutes vos échéances proches sont sous contrôle."`
      - S3 Impayés : liste `<ul>` items utilisant les champs RÉELS : `impaye.locataireNomComplet`, `impaye.joursDeRetard` (badge `var(--couleur-destructive)` + texte `En retard de N jours`), `formatMoney(impaye.resteDu)`, lien `/echeances/<%= impaye.echeanceId %>` "Saisir l'encaissement" ; `Voir tout` → `/impayes` ; empty `"Aucun impayé en cours. Tous vos loyers sont à jour."`
      - S4 Actions du jour : `<ul>` switch `action.type === 'relance'` (lien `/echeances/<%= action.echeanceId %>` "Lancer la relance") vs `'irl'` (lien `/baux/<%= action.bailId %>/indexer` "Lancer la révision IRL") ; `Voir tout` → ancre `#toutes-actions-jour` ; empty `"Aucune action à mener aujourd'hui. Profitez-en."`
      - S5 Échéances loyer à venir : `<ul>` items `formatPeriode(echeance.periodeDebut, echeance.periodeFin)` (ou libellé période existant) + `formatMoney(echeance.total)` right-aligned ; `Voir tout` → `/echeances` ; empty `"Aucune échéance de loyer prévue sur les deux prochains mois."`
    - `<%- include('../../partials/layout-fin') %>`.
    - Espacement : `lg` (24px) entre sections, `xl` (32px) entre h1 et 1ère section (styles inline cohérents avec UI-SPEC). Copy STRICTEMENT français, aucun "Cliquez ici", aucun "Snooze"/"Ignorer" (D-AL-04).

    Modifier `src/main.ts` :
    - Ajouter en haut des helpers du hook `preHandler` (lignes 195-215) : `import { formaterAlerteUrgence, iconeTypeAlerte, libelleTypeAlerte } from './web/helpers/alerte-helpers.js'` et les ajouter dans l'objet `reply.locals = { ...formaterAlerteUrgence, iconeTypeAlerte, libelleTypeAlerte }`.
    - Remplacer ligne 246 `await app.register(racinePlugin, { db });` par `await app.register(racinePlugin, { db, cfeRepo, bienRepo: repo, bailRepo, bailIndexationRepo, echeanceLoyerRepo, encaissementRepo, locataireRepo, relanceRepo, clock });`. NE TOUCHER à aucune autre registration.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "calculerToutesAlertes" src/web/routes/racine.ts` → ≥ 1
    - `grep -c "listerImpayes" src/web/routes/racine.ts` → ≥ 1
    - `grep -c "estPremierLancement" src/web/routes/racine.ts` → 1 (branche activation préservée)
    - `grep -c "/wizard/bien" src/web/routes/racine.ts` → 1
    - `grep -c "pages/dashboard/accueil" src/web/routes/racine.ts` → 1
    - `grep -cE "new Date\(|Temporal\.Now|setInterval|setTimeout|cron" src/web/routes/racine.ts` → 0 (Clock-driven)
    - `grep -c "listerNonPayees" src/web/routes/racine.ts` → 1 (pas de listerAVenir inexistant)
    - `grep -c "navActive: 'dashboard'" src/web/routes/racine.ts` → 1
    - les 4 IDs ARIA présents : `grep -cE "titre-alertes-critiques|titre-impayes|titre-actions-jour|titre-echeances-venir" src/web/views/pages/dashboard/accueil.ejs` → 4
    - `grep -c "partial-bandeau-alerte" src/web/views/pages/dashboard/accueil.ejs` → ≥ 1
    - `grep -c "etatGlobal" src/web/views/pages/dashboard/accueil.ejs` → ≥ 1 (branche bandeau succès)
    - `grep -c "locataireNomComplet" src/web/views/pages/dashboard/accueil.ejs` → ≥ 1 (champ DTO RÉEL, pas inventé)
    - `grep -cE "libelleEcheance|montantDu|nomLocataire(?!Complet)" src/web/views/pages/dashboard/accueil.ejs` → 0 dans la section Impayés (champs inexistants du DTO Impaye) [vérification manuelle : la section Impayés utilise locataireNomComplet/resteDu, pas de champ fictif]
    - `grep -c "racinePlugin, { db, cfeRepo" src/main.ts` → 1 (wiring DI étendu)
    - `grep -c "formaterAlerteUrgence" src/main.ts` → ≥ 1 (helper injecté en preHandler)
    - `pnpm tsc --noEmit` → 0 erreur ; `pnpm vitest run` → suite verte (aucune régression Phase 1-6)
  </acceptance_criteria>
  <done>`GET /` rend le dashboard 4 sections (branche premier lancement préservée), composant `calculerToutesAlertes` + `listerImpayes` + relances + échéances à venir, avec état zen si tout vide ; helpers injectés en preHandler ; wiring DI étendu dans main.ts ; champs DTO réels utilisés ; tsc + suite verts. Aucune migration, aucune mutation, Clock-driven.</done>
</task>

<task type="auto">
  <name>Task 3: BDD outside-in dashboard (composition, état vide, premier lancement) + fermeture lint/depcruise</name>
  <files>tests/bdd/features/dashboard-composition.feature, tests/bdd/features/dashboard-empty-state.feature, tests/bdd/features/dashboard-premier-lancement.feature, tests/bdd/steps/dashboard.steps.ts</files>
  <read_first>
    - tests/bdd/features/activation.feature (PATTERN feature de niveau route HTTP — Background, tags, scénarios via app.inject/supertest)
    - tests/bdd/steps/ (un steps existant de niveau HTTP : montage de l'app de test, app.inject, assertions sur le HTML rendu)
    - src/web/routes/racine.ts (route testée — locals rendus)
    - .dependency-cruiser.cjs (règles hexagonales — web/ peut importer application/domain ; helpers EJS purs)
    - .planning/phases/07-dashboard-notifications-d-ch-ances/07-UI-SPEC.md §S1 (branche premier lancement) + §Accessibilité (sélecteurs ARIA imposés)
  </read_first>
  <action>
    Créer 3 features BDD (tags `@phase7 @phase7-dashboard`, en-tête commentaire référençant DAS-01 / D-DASH-01..04) au niveau ROUTE HTTP (utiliser le harnais de test app existant — `app.inject({ method: 'GET', url: '/' })` ou supertest selon le pattern des steps existants, avec un `ClockFixe` et une DB de test peuplée via les builders) :

    - `tests/bdd/features/dashboard-composition.feature` :
      - `@phase7-dashboard-01` 4 sections présentes : Given un compte avec ≥1 bien + bail + des impayés et des alertes / When je visite `/` / Then la réponse contient les 4 titres de section ("Alertes critiques", "Impayés", "Actions du jour", "Échéances loyer à venir") et les sélecteurs ARIA `aria-labelledby="titre-alertes-critiques"` etc.
      - `@phase7-dashboard-02` Top 5 + Voir tout : Given plus de 5 alertes critiques / Then seules 5 bannières sont rendues ET un lien "Voir tout" avec le total est présent.
      - `@phase7-dashboard-03` Tri ASC urgence : Given des alertes critiques de joursRestants variés / Then la 1ère bannière rendue est la plus urgente (joursRestants minimal).

    - `tests/bdd/features/dashboard-empty-state.feature` :
      - `@phase7-dashboard-empty-01` État zen : Given un compte avec ≥1 bien+bail mais aucun impayé/alerte/échéance à venir / When je visite `/` / Then la réponse contient le bandeau succès `role="status"` "Vous êtes à jour" ET NE contient PAS les 4 titres de section.

    - `tests/bdd/features/dashboard-premier-lancement.feature` :
      - `@phase7-dashboard-premier-01` Redirection activation : Given un état de premier lancement (aucun bien/locataire/bail) / When je visite `/` / Then la réponse est une redirection 302 vers `/wizard/bien` (KPI Activation préservé, D-DASH-01).

    Créer `tests/bdd/steps/dashboard.steps.ts` : monter l'app de test (réutiliser le builder d'app de test des steps existants — DB en mémoire/temp + `ClockFixe`), peupler via les builders domaine, `app.inject` sur `/`, asserter le statut HTTP + le HTML (présence/absence de chaînes, sélecteurs ARIA). Réutiliser les step definitions partagées si le harnais World existant le permet.

    Puis FERMETURE du plan : lancer la suite complète + lint + dependency-cruiser. Si une violation apparaît (import interdit web→infra hors composition root, helper EJS impur, complexité > 10), corriger UNIQUEMENT les fichiers de ce plan. Vérifier que `git diff --name-only` ne liste QUE les 10 fichiers de `files_modified`.
  </action>
  <verify>
    <automated>pnpm vitest run && pnpm exec cucumber-js --tags "@phase7-dashboard" && pnpm lint && (pnpm depcruise 2>/dev/null || pnpm exec depcruise --config .dependency-cruiser.cjs src)</automated>
  </verify>
  <acceptance_criteria>
    - les 3 features existent et sont taggées `@phase7-dashboard*`
    - `pnpm exec cucumber-js --tags "@phase7-dashboard"` → tous verts, 0 step `undefined`/`pending`
    - le scénario premier lancement asserte un statut 302 + `location: /wizard/bien`
    - le scénario état vide asserte la présence de "Vous êtes à jour" ET l'absence des 4 titres de section
    - `pnpm vitest run` → suite complète verte (0 fail), aucune régression Phase 1-6
    - dependency-cruiser → 0 violation
    - `pnpm lint` → 0 warning sur les fichiers créés/modifiés
    - `git diff --name-only` ne liste AUCUN fichier hors des 10 déclarés dans `files_modified`
  </acceptance_criteria>
  <done>3 features BDD dashboard (composition 4 sections + top5/tri, état zen, redirection premier lancement) vertes via Cucumber ; suite complète verte ; architecture hexagonale validée. Dashboard DAS-01 livré et navigable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `GET /` | Nouvelle surface HTTP en LECTURE SEULE. `GET /` ne reçoit AUCUN path param, AUCUN body, AUCUNE query non triviale — pas d'entrée utilisateur non trustée. La page agrège des données déjà en base (repos) et rend du HTML. Frontière de confiance principale = sortie HTML (échappement EJS). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | I (Information Disclosure) / XSS | `alerte.urlAction`, `alerte.libelle`, `source.extra.*` (nomLocataire, adresseBien) interpolés dans `partial-bandeau-alerte.ejs` et `accueil.ejs` | mitigate | Utiliser `<%= %>` (échappement HTML EJS par défaut) pour TOUTE valeur dynamique, jamais `<%- %>` sur des données — réservé aux includes de partials. `urlAction` est construit côté domaine (07-01..03) à partir d'UUID v4 brand-typed, pas d'entrée libre. CSP global `script-src 'self' 'unsafe-inline'` déjà en place (main.ts:224) en défense en profondeur. |
| T-07-05-02 | I (Information Disclosure) | lien externe CFE `impots.gouv.fr` ouvert dans nouvel onglet | mitigate | `target="_blank"` accompagné de `rel="noopener noreferrer"` (hérité Phase 6, vérifié par acceptance criteria) — empêche `window.opener` tabnabbing et fuite de referrer. |
| T-07-05-03 | I (Information Disclosure) | agrégation cross-Bien de toutes les alertes + impayés + échéances sur une seule page | accept | Application single-user local (D-AL-03, VISION.md) — l'utilisateur est propriétaire de toutes les données. Pas de séparation de tenants, pas de PII tierce exposée à un autre utilisateur. |
| T-07-05-04 | D (Denial of Service) | `GET /` agrège N repos (alertes + impayés + relances par échéance + échéances à venir) | accept | Single-user local, volumétrie bornée (< 20 baux typiques LMNP). Charge attendue < 100ms en SQLite local (UI-SPEC §Loading). Pas d'entrée non bornée, pas de récursion. Top 5 par section limite le rendu. |
| T-07-05-05 | T (Tampering) | branche `estPremierLancement` contournée → dashboard rendu sans bien | accept | `estPremierLancement(db)` est calculé server-side à chaque requête depuis l'état DB réel (pas de flag client). Si false, le dashboard rend des sections vides (état zen) — comportement correct, aucune élévation. |
</threat_model>

<verification>
- `pnpm tsc --noEmit` — 0 erreur. `pnpm vitest run` — suite complète verte, aucune régression Phase 1-6.
- `pnpm exec cucumber-js --tags "@phase7-dashboard"` — 3 features (composition, état vide, premier lancement) vertes.
- `pnpm lint` — 0 warning ; dependency-cruiser — 0 violation (web/ → application/domain OK, helpers EJS purs sans infra).
- `git diff --name-only` ne liste QUE les 10 fichiers de `files_modified`. `partial-bandeau-cfe-echeance.ejs` NON modifié (migration Phase 6 hors scope, D-AL-05). Aucune migration SQLite (dernière reste `0022_phase6_*`).
- Pattern Clock-driven : `opts.clock.aujourdhui()` unique source de date dans `racine.ts` ; `grep -E "new Date\(|Temporal\.Now|setInterval"` → vide. Aucune mutation de domaine.
- Multi-source coverage audit :
  - GOAL (vue synthétique impayés/échéances/action du jour + notifications J-30/J-7 agrégées) → les 4 sections rendent exactement ces données via `calculerToutesAlertes` (07-04) + sources Phase 2 ✓
  - REQ DAS-01 (récap synthétique impayés/échéances/actions) → dashboard 4 sections livré ✓ ; DAS-02 (notifications J-30/J-7 4 sources) → section Alertes critiques + bannières J-7/J-30 rendues, action en un clic via `urlAction` ✓
  - RESEARCH/PATTERNS → `racine.ts` étend l'analog, `partial-bandeau-alerte.ejs` reproduit `partial-bandeau-cfe-echeance.ejs`, `accueil.ejs` mirror `impayes/liste.ejs`, sidebar étendue ✓ (17 analogs mappés respectés)
  - CONTEXT → D-DASH-01 (/ = dashboard + branche premier lancement) ✓, D-DASH-02 (4 sections empilées + redondance S1/S3 IRL) ✓, D-DASH-03 (tri-état couleur+texte+icône WCAG) ✓, D-DASH-04 (top 5 + Voir tout) ✓, D-AL-03 (banner UI seul, pas de cron/email) ✓, D-AL-04 (pas de dismiss/snooze) ✓, D-AL-05 (partial unifié polymorphe) ✓, fenêtre échéances mois courant+1 (Claude's Discretion) ✓, helpers EJS (Claude's Discretion) ✓.
  - Aucun item différé implémenté (pas de snooze, pas d'email, pas de KPI globaux, pas de page /baux/indexations — celle-ci est le plan 07-06).
- Exclusions (non-gaps) : page transversale `/baux/indexations` + audit axe-core = plan 07-06 (Wave 4) ; migration partials Phase 6/3 vers le partial unifié = optionnelle D-AL-05, conservée pour 07-06/LEARNINGS.
</verification>

<success_criteria>
- `GET /` rend `pages/dashboard/accueil.ejs` avec 4 sections empilées par urgence quand un bien+bail existe ; redirige vers `/wizard/bien` au premier lancement.
- Les 4 sections consomment les bonnes sources : Alertes critiques = `calculerToutesAlertes().filter(j<=7)` top 5 ; Impayés = `listerImpayes()` en retard top 5 ; Actions du jour = relances dues + IRL J-30..J-0 ; Échéances à venir = échéances non payées dans la fenêtre mois courant + 1 mois top 5.
- `partial-bandeau-alerte.ejs` rend les 4 types d'alerte polymorphes avec les 3 variantes tri-état (role/aria-live corrects, couleur jamais seule : texte + icône aria-hidden + libellé type) reproduisant le pattern Phase 6.
- État zen : toutes sections vides → bandeau succès "Vous êtes à jour", sections non rendues.
- Sidebar : entrée "Tableau de bord" en 1ère position, `aria-current="page"` actif.
- Wiring DI étendu dans `main.ts` ; helpers injectés en preHandler global.
- tsc, vitest, cucumber (`@phase7-dashboard`), lint, dependency-cruiser : 0 fail / 0 warning / 0 violation. Aucune migration, aucune mutation, Clock-driven strict.
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-notifications-d-ch-ances/07-05-SUMMARY.md` (template @/Users/valentinshodo/.claude/get-shit-done/templates/summary.md). Documenter : les 8 locals exacts passés à `accueil.ejs`, le contrat de props de `partial-bandeau-alerte.ejs` (`alerte`, `inline`), les 3 helpers EJS et leur injection preHandler, la fenêtre "mois courant + 1 mois" retenue, et la décision de NE PAS migrer `partial-bandeau-cfe-echeance.ejs`/`partial-indexation-banner.ejs` (D-AL-05 reporté à 07-06/LEARNINGS) — pour que le plan 07-06 (page `/baux/indexations` + audit axe-core) consomme le partial unifié avec `inline=true`.
</output>
