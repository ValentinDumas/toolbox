---
phase: 07-dashboard-notifications-d-ch-ances
plan: 02
type: tdd
wave: 2
depends_on: [01]
files_modified:
  - src/domain/locatif/alerte-irl.ts
  - src/domain/locatif/alerte-fin-bail.ts
  - tests/unit/locatif/alerte-irl.test.ts
  - tests/unit/locatif/alerte-fin-bail.test.ts
  - tests/_builders/alertes.ts
  - tests/bdd/features/alerte-irl.feature
  - tests/bdd/features/alerte-fin-bail.feature
  - tests/bdd/steps/alerte-irl.steps.ts
  - tests/bdd/steps/alerte-fin-bail.steps.ts
autonomous: true
requirements: [DAS-02]

must_haves:
  truths:
    - "`calculerAlertesIrl(baux, biens, indexationsParBail, maintenant)` retourne des `Alerte[]` (type='irl') uniquement pour les baux actifs, non gelés DPE F/G, sans indexation sur l'exercice courant, dans la fenêtre [-30, +30]"
    - "`calculerAlertesFinBail(baux, maintenant)` retourne des `Alerte[]` (type='fin_bail') uniquement pour les baux `actifDepuis !== null`, dans la fenêtre [-30, +60], avec `dateEcheance = dateDebut + dureeMois mois`"
    - "Les deux fonctions trient leur résultat par `joursRestants` ASC et retournent `[]` sur entrée vide"
    - "Aucun import technique dans `src/domain/locatif/` — dependency-cruiser vert ; aucune mutation des agrégats `Bail`/`Bien`"
    - "Logique juridique IRL 100 % couverte BDD (gel Climat F/G, exercice courant, fenêtre J-30/J-7)"
  artifacts:
    - path: "src/domain/locatif/alerte-irl.ts"
      provides: "estAlerteIrlActive, calculerAlertesIrl produisant Alerte[]"
      exports: ["estAlerteIrlActive", "calculerAlertesIrl"]
      min_lines: 40
    - path: "src/domain/locatif/alerte-fin-bail.ts"
      provides: "dateFinBail, estAlerteFinBailActive, calculerAlertesFinBail produisant Alerte[]"
      exports: ["dateFinBail", "estAlerteFinBailActive", "calculerAlertesFinBail"]
      min_lines: 30
    - path: "tests/_builders/alertes.ts"
      provides: "Builders uneAlerteIrl + uneAlerteFinBail (extension du builder 07-01)"
      exports: ["uneAlerteIrl", "uneAlerteFinBail"]
  key_links:
    - from: "src/domain/locatif/alerte-irl.ts"
      to: "src/domain/_shared/alerte.ts"
      via: "import { joursAvantEcheance } + import type { Alerte }"
      pattern: "from '\\.\\./_shared/alerte\\.js'"
    - from: "src/domain/locatif/alerte-irl.ts"
      to: "src/domain/patrimoine/bien.ts"
      via: "bien.estGelLoyer() pour filtrer le gel Climat F/G (D-78/D-92)"
      pattern: "estGelLoyer\\(\\)"
    - from: "src/domain/locatif/alerte-fin-bail.ts"
      to: "src/domain/locatif/bail.ts"
      via: "bail.dateDebut.add({ months: bail.dureeMois })"
      pattern: "add\\(\\{ months: "
---

<objective>
Créer les deux fonctions pures du bounded context `Locatif` qui produisent des `Alerte[]` unifiés (contrat D-AL-01 créé en 07-01) : `calculerAlertesIrl` (révision IRL annuelle, fenêtre [-30, +30], filtres gel DPE F/G + exercice courant + bail actif) et `calculerAlertesFinBail` (fin de bail = `dateDebut + dureeMois`, fenêtre [-30, +60], filtre bail actif). Deux slices TDD indépendantes, 100 % couvertes BDD (la révision IRL est de la logique juridique = couverture obligatoire, CLAUDE.md / BDD_PRACTICES.md).

Purpose : ce sont 2 des 4 sources d'alerte de DAS-02. Une fois livrées, l'utilisateur sera alertable sur la révision IRL imminente et sur la fin de bail approchante. Elles consomment le contrat `Alerte` + `joursAvantEcheance` de 07-01 et seront agrégées par le use case transversal 07-04 (`calculerToutesAlertes`).

Output : `src/domain/locatif/alerte-irl.ts` (nouveau), `src/domain/locatif/alerte-fin-bail.ts` (nouveau), tests unitaires fast-check, features BDD + steps, extension du builder `tests/_builders/alertes.ts`. Aucun import infra, aucune migration SQLite, aucune mutation de `Bail`/`Bien`.
</objective>

<execution_context>
@/Users/valentinshodo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/valentinshodo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dashboard-notifications-d-ch-ances/07-CONTEXT.md
@.planning/phases/07-dashboard-notifications-d-ch-ances/07-PATTERNS.md
@.planning/phases/07-dashboard-notifications-d-ch-ances/07-01-PLAN.md
@src/domain/fiscalite/cfe/alerte-cfe-j30.ts

<interfaces>
<!-- État actuel exact — extrait du codebase. L'exécuteur n'a pas besoin d'explorer. -->

Contrat `Alerte` (créé par 07-01, dans `src/domain/_shared/alerte.ts`) — forme VERROUILLÉE D-AL-01, à remplir ici (ne pas modifier) :

```typescript
export type TypeAlerte = 'cfe' | 'irl' | 'diagnostic' | 'fin_bail';

export interface Alerte {
  readonly type: TypeAlerte;
  readonly joursRestants: number;       // peut être négatif
  readonly dateEcheance: Temporal.PlainDate;
  readonly libelle: string;             // pré-calculé côté domaine
  readonly urlAction: string;           // string brut V1
  readonly source: {
    readonly type: TypeAlerte;
    readonly refId: string;
    readonly bienId?: BienId;
    readonly extra?: Record<string, unknown>;
  };
}

export function joursAvantEcheance(
  dateEcheance: Temporal.PlainDate,
  maintenant: Temporal.PlainDate,
): number; // = maintenant.until(dateEcheance, { largestUnit: 'days' }).days
```

Agrégat `Bail` (`src/domain/locatif/bail.ts`) — propriétés et méthodes RÉELLES utilisées ici :

```typescript
class Bail {
  readonly id: BailId;
  readonly bienId: BienId;
  readonly dateDebut: Temporal.PlainDate;
  readonly dureeMois: number;                       // ≥ 12 (invariant meublé)
  readonly actifDepuis: Temporal.PlainDate | null;  // null = jamais activé (filtre)
  // today < dateDebut → dateDebut + 1 an ; sinon plus petit N tel que dateDebut + N ans > today
  dateAnniversaireProchaine(today: Temporal.PlainDate): Temporal.PlainDate;
}
```

Agrégat `Bien` (`src/domain/patrimoine/bien.ts`) — méthode RÉELLE utilisée ici :

```typescript
class Bien {
  readonly id: BienId;
  readonly classeDpe: ClasseDpe | null;
  readonly adresse: Adresse;            // adresse.rue: string, adresse.ville: string
  estGelLoyer(): boolean;               // true si classeDpe ∈ {F, G} (D-78/D-92)
}
```

Brand types (`src/domain/_shared/identifiants.ts`) : `BailId`, `BienId` — `string & { readonly __brand: '…' }`.

NOTE filtre exercice courant (D-SRC-03 IRL) : ce filtre se calcule à partir du repository `BailIndexationRepository` (méthode `dernierePourBail` / liste `dateEffet`), qui appartient à la couche **application**. Les fonctions pures de CE plan ne touchent JAMAIS un repository. Le statut « indexation déjà présente sur l'exercice courant » est passé en argument pré-calculé `indexationsParBail: Map<BailId, boolean>` (true = ne pas alerter). Le calcul réel de cette Map est wiré par le use case 07-04 — ne pas l'implémenter ici.

Pattern test fast-check existant à reproduire : `tests/unit/fiscalite/alerte-cfe-j30.test.ts` (imports `vitest` + `fast-check` + `@js-temporal/polyfill`, `describe`/`it`, helper local `declCfe(...)`).

Builders existants réutilisables : `unBailValide`, `unBailIndexableValide` (`tests/_builders/locatif.ts`), `unBienValide`, `unBienAvecDpeF`, `unBienAvecDpeD` (`tests/_builders/patrimoine.ts`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD `src/domain/locatif/alerte-irl.ts` (estAlerteIrlActive + calculerAlertesIrl, fenêtre [-30,+30], filtres gel DPE F/G + exercice courant + actif)</name>
  <files>src/domain/locatif/alerte-irl.ts, tests/unit/locatif/alerte-irl.test.ts</files>
  <read_first>
    - src/domain/fiscalite/cfe/alerte-cfe-j30.ts (MODÈLE EXACT à dupliquer : structure `FENETRE_ALERTE_JOURS` + `estAlerteActive` + `calculer*` + tri ASC)
    - src/domain/_shared/alerte.ts (contrat `Alerte` + `joursAvantEcheance` créés par 07-01)
    - src/domain/locatif/bail.ts (signatures réelles : `dateAnniversaireProchaine(today)`, `actifDepuis`, `id`, `bienId` — lignes 73-107, 274-296)
    - src/domain/patrimoine/bien.ts (méthode `estGelLoyer()` ligne 161, `classeDpe`, `adresse`)
    - tests/unit/fiscalite/alerte-cfe-j30.test.ts (pattern test fast-check + helper local + describe/it)
    - tests/_builders/locatif.ts (`unBailValide`, `unBailIndexableValide` — défauts dateDebut/dureeMois/actifDepuis)
    - tests/_builders/patrimoine.ts (`unBienValide`, `unBienAvecDpeF`)
  </read_first>
  <behavior>
    - Test 1 (actif requis): un bail avec `actifDepuis === null` → `estAlerteIrlActive` false, aucune alerte (filtre D-SRC-03)
    - Test 2 (gel DPE): bail actif sur un Bien dont `estGelLoyer() === true` (classeDpe F ou G) → false (gel Climat, D-78/D-92 — inutile de bruiter)
    - Test 3 (exercice courant): bail actif, Bien classe C, mais `indexationsParBail.get(bail.id) === true` → false (indexation déjà faite cette année, D-SRC-03)
    - Test 4 (fenêtre J-30 incluse): bail actif, Bien C, pas d'indexation, anniversaire dans exactement 30 jours → true, `joursRestants === 30`
    - Test 5 (fenêtre J-31 exclue): anniversaire dans 31 jours → false (hors fenêtre haute)
    - Test 6 (déjà dépassé borne basse): anniversaire il y a 30 jours (`joursRestants === -30`) → true ; il y a 31 jours (`-31`) → false
    - Test 7 (forme Alerte): `calculerAlertesIrl([bailActif], [bienC], mapVide, maintenant)` à J-15 → 1 `Alerte` avec `type === 'irl'`, `source.type === 'irl'`, `source.refId === bail.id`, `source.bienId === bien.id`, `dateEcheance === bail.dateAnniversaireProchaine(maintenant)`, `urlAction === '/baux/{bailId}/indexer'`, `libelle` contient `'IRL'`
    - Test 8 (tri ASC + vide): liste mixte → tri `joursRestants` ASC ; `calculerAlertesIrl([], [], mapVide, maintenant)` → `[]`
    - Test 9 (bail sans bien correspondant): bail dont `bienId` n'est dans aucun Bien fourni → ignoré silencieusement (pas d'exception)
  </behavior>
  <action>
    RED : créer `tests/unit/locatif/alerte-irl.test.ts` reproduisant le pattern de `tests/unit/fiscalite/alerte-cfe-j30.test.ts` (imports `vitest` + `fast-check` + `@js-temporal/polyfill`). Importer `estAlerteIrlActive` et `calculerAlertesIrl` depuis `../../../src/domain/locatif/alerte-irl.js`. Écrire les 9 tests `<behavior>` ci-dessus en utilisant `unBailValide`/`unBailIndexableValide` (`tests/_builders/locatif.ts`) et `unBienValide`/`unBienAvecDpeF` (`tests/_builders/patrimoine.ts`). Pour piloter l'anniversaire, ajuster `dateDebut` du bail de sorte que `dateAnniversaireProchaine(maintenant)` tombe au nombre de jours voulu (ex : `dateDebut` un an avant `maintenant` décalé de N jours), OU fixer `maintenant` relativement à l'anniversaire connu. Lancer → échec (module absent).

    GREEN : créer `src/domain/locatif/alerte-irl.ts`. Imports autorisés UNIQUEMENT : `import { Temporal } from '@js-temporal/polyfill'`, `import type { BailId } from '../_shared/identifiants.js'`, `import type { Alerte } from '../_shared/alerte.js'`, `import { joursAvantEcheance } from '../_shared/alerte.js'`, `import type { Bail } from './bail.js'`, `import type { Bien } from '../patrimoine/bien.js'`. AUCUN import technique (pas d'ORM, pas de repository, pas de Fastify) — dependency-cruiser doit rester vert.

    Constante : `const FENETRE_ALERTE_JOURS = 30;` (D-SRC-02 ; même valeur que CFE).

    `export function estAlerteIrlActive(bail: Bail, bien: Bien, indexationDejaPresenteExerciceCourant: boolean, maintenant: Temporal.PlainDate): boolean` :
    - `if (bail.actifDepuis === null) return false;` (D-SRC-03 — bail jamais activé)
    - `if (bien.estGelLoyer()) return false;` (gel Climat F/G, D-78/D-92)
    - `if (indexationDejaPresenteExerciceCourant) return false;` (D-SRC-03 IRL — déjà indexé cette année)
    - `const dateAnniversaire = bail.dateAnniversaireProchaine(maintenant);` (D-91 source canonique)
    - `const j = joursAvantEcheance(dateAnniversaire, maintenant);`
    - `return j <= FENETRE_ALERTE_JOURS && j >= -30;` (fenêtre [-30, +30])

    `export function calculerAlertesIrl(baux: readonly Bail[], biens: readonly Bien[], indexationsParBail: Map<BailId, boolean>, maintenant: Temporal.PlainDate): Alerte[]` :
    - construire `const biensParId = new Map(biens.map((b) => [b.id, b]));`
    - itérer sur `baux` ; récupérer `bien = biensParId.get(bail.bienId)` ; `if (!bien) continue;` (bail orphelin ignoré)
    - `const aDeja = indexationsParBail.get(bail.id) ?? false;`
    - `if (!estAlerteIrlActive(bail, bien, aDeja, maintenant)) continue;`
    - `const dateAnniversaire = bail.dateAnniversaireProchaine(maintenant);`
    - push d'une `Alerte` : `{ type: 'irl', joursRestants: joursAvantEcheance(dateAnniversaire, maintenant), dateEcheance: dateAnniversaire, libelle: 'Révision IRL', urlAction: \`/baux/${bail.id}/indexer\`, source: { type: 'irl', refId: bail.id, bienId: bien.id, extra: { adresseBien: bien.adresse.rue } } }`
    - trier `alertes.sort((a, b) => a.joursRestants - b.joursRestants);` puis `return alertes;`

    JSDoc en-tête français : référencer Phase 7 / DAS-02 / D-SRC-03 / D-91 / D-92, rappeler le pattern Clock-driven (`maintenant` est un argument, jamais `Temporal.Now`). Préciser que le filtre exercice courant arrive pré-calculé via `indexationsParBail` (responsabilité use case 07-04, le domaine ne touche pas de repository).

    REFACTOR (si besoin) : extraire un helper local de construction d'`Alerte` seulement si la duplication avec fin-bail le justifie ; sinon laisser inline (sobriété).

    Commits : `test(07-02): add failing tests for calculerAlertesIrl (RED)` puis `feat(07-02): calculerAlertesIrl produit Alerte[] (fenêtre [-30,+30], filtres gel/exercice/actif)`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/locatif/alerte-irl.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run tests/unit/locatif/alerte-irl.test.ts` → 9 tests passent, 0 échec
    - `grep -c "export function estAlerteIrlActive" src/domain/locatif/alerte-irl.ts` → 1
    - `grep -c "export function calculerAlertesIrl" src/domain/locatif/alerte-irl.ts` → 1
    - `grep -c "estGelLoyer()" src/domain/locatif/alerte-irl.ts` → 1 (filtre gel Climat présent)
    - `grep -c "dateAnniversaireProchaine" src/domain/locatif/alerte-irl.ts` → au moins 1 (source D-91)
    - `grep -cE "FENETRE_ALERTE_JOURS = 30" src/domain/locatif/alerte-irl.ts` → 1
    - `grep -cE "j >= -30" src/domain/locatif/alerte-irl.ts` → 1 (borne basse fenêtre)
    - `grep -E "from '(fastify|kysely|better-sqlite3|node:)" src/domain/locatif/alerte-irl.ts` → aucune sortie (domaine pur)
    - `grep -cE "repository|Repository" src/domain/locatif/alerte-irl.ts` → 0 (le domaine ne connaît aucun repo)
    - Le type de retour de `calculerAlertesIrl` dans la signature source est `Alerte[]`
  </acceptance_criteria>
  <done>`calculerAlertesIrl` retourne des `Alerte[]` (type='irl', urlAction `/baux/{id}/indexer`) filtrés par bail actif + gel DPE F/G + exercice courant, fenêtre [-30,+30], triés ASC ; fonction pure sans import technique ; 9 tests verts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD `src/domain/locatif/alerte-fin-bail.ts` (dateFinBail + estAlerteFinBailActive + calculerAlertesFinBail, fenêtre [-30,+60])</name>
  <files>src/domain/locatif/alerte-fin-bail.ts, tests/unit/locatif/alerte-fin-bail.test.ts</files>
  <read_first>
    - src/domain/locatif/alerte-irl.ts (modèle frère créé en Task 1 — même structure)
    - src/domain/fiscalite/cfe/alerte-cfe-j30.ts (MODÈLE : structure `estAlerteActive` + `calculer*` + tri ASC)
    - src/domain/_shared/alerte.ts (contrat `Alerte` + `joursAvantEcheance`)
    - src/domain/locatif/bail.ts (signatures réelles : `dateDebut`, `dureeMois`, `actifDepuis`, `id`, `bienId`)
    - tests/_builders/locatif.ts (`unBailValide` — défauts dateDebut/dureeMois ≥ 12/actifDepuis)
  </read_first>
  <behavior>
    - Test 1 (actif requis): bail `actifDepuis === null` → `estAlerteFinBailActive` false, aucune alerte (D-SRC-03 fin_bail)
    - Test 2 (dateFinBail): `dateFinBail(bail)` pour `dateDebut = 2025-01-15`, `dureeMois = 12` → `2026-01-15` (`dateDebut.add({ months: dureeMois })`)
    - Test 3 (fenêtre J-30 incluse): bail actif dont la fin est dans 30 jours → true, `joursRestants === 30`
    - Test 4 (fenêtre J+60 incluse): bail actif dont la fin est déjà passée de 60 jours (`joursRestants === -60`) → true ; passée de 61 jours (`-61`) → false (au-delà de J+60 l'alerte disparaît, D-SRC-05)
    - Test 5 (borne haute exclue): fin dans 31 jours → false (avant J-30 pas d'alerte)
    - Test 6 (forme Alerte): `calculerAlertesFinBail([bailActif], maintenant)` à 15 jours de la fin → 1 `Alerte` avec `type === 'fin_bail'`, `source.type === 'fin_bail'`, `source.refId === bail.id`, `source.bienId === bail.bienId`, `dateEcheance === dateFinBail(bail)`, `joursRestants === 15`, `urlAction === '/baux/{bailId}'`, `libelle` contient `'bail'` (insensible casse acceptable)
    - Test 7 (tri ASC + vide): liste mixte → tri `joursRestants` ASC ; `calculerAlertesFinBail([], maintenant)` → `[]`
  </behavior>
  <action>
    RED : créer `tests/unit/locatif/alerte-fin-bail.test.ts` selon le pattern de la Task 1. Importer `dateFinBail`, `estAlerteFinBailActive`, `calculerAlertesFinBail` depuis `../../../src/domain/locatif/alerte-fin-bail.js`. Écrire les 7 tests `<behavior>` ci-dessus avec `unBailValide` (`tests/_builders/locatif.ts`). Piloter la date de fin via `dateDebut` + `dureeMois` et le `maintenant` passé en argument. Lancer → échec (module absent).

    GREEN : créer `src/domain/locatif/alerte-fin-bail.ts`. Imports autorisés UNIQUEMENT : `import { Temporal } from '@js-temporal/polyfill'`, `import type { Alerte } from '../_shared/alerte.js'`, `import { joursAvantEcheance } from '../_shared/alerte.js'`, `import type { Bail } from './bail.js'`. AUCUN import technique, AUCUN repository.

    `export function dateFinBail(bail: Bail): Temporal.PlainDate { return bail.dateDebut.add({ months: bail.dureeMois }); }` (fin de bail = dateDebut + dureeMois, D-29 Phase 1).

    `export function estAlerteFinBailActive(bail: Bail, maintenant: Temporal.PlainDate): boolean` :
    - `if (bail.actifDepuis === null) return false;` (D-SRC-03 — exclut les baux jamais activés)
    - `const dateFin = dateFinBail(bail);`
    - `const j = joursAvantEcheance(dateFin, maintenant);`
    - `return j <= 60 && j >= -30;` (fenêtre [-30, +60], D-SRC-05/D-FB-03 — borne SUPÉRIEURE +60, borne basse -30)

    `export function calculerAlertesFinBail(baux: readonly Bail[], maintenant: Temporal.PlainDate): Alerte[]` :
    - itérer sur `baux` ; `if (!estAlerteFinBailActive(bail, maintenant)) continue;`
    - `const dateFin = dateFinBail(bail);`
    - push d'une `Alerte` : `{ type: 'fin_bail', joursRestants: joursAvantEcheance(dateFin, maintenant), dateEcheance: dateFin, libelle: 'Fin de bail', urlAction: \`/baux/${bail.id}\`, source: { type: 'fin_bail', refId: bail.id, bienId: bail.bienId } }`
    - trier `alertes.sort((a, b) => a.joursRestants - b.joursRestants);` puis `return alertes;`

    JSDoc en-tête français : Phase 7 / DAS-02 / D-SRC-05 / D-FB-01..03. Rappeler : V1 = alerte seule, AUCUNE mutation `Bail` (pas de flag `cloture`, pas de `successeur` — D-FB-01/D-FB-04), action target = fiche Bail `/baux/{id}` (D-FB-02). Pattern Clock-driven (`maintenant` argument).

    Commits : `test(07-02): add failing tests for calculerAlertesFinBail (RED)` puis `feat(07-02): calculerAlertesFinBail produit Alerte[] (fenêtre [-30,+60], dateDebut+dureeMois)`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/locatif/alerte-fin-bail.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run tests/unit/locatif/alerte-fin-bail.test.ts` → 7 tests passent, 0 échec
    - `grep -c "export function dateFinBail" src/domain/locatif/alerte-fin-bail.ts` → 1
    - `grep -c "export function estAlerteFinBailActive" src/domain/locatif/alerte-fin-bail.ts` → 1
    - `grep -c "export function calculerAlertesFinBail" src/domain/locatif/alerte-fin-bail.ts` → 1
    - `grep -cE "add\(\{ months: " src/domain/locatif/alerte-fin-bail.ts` → 1 (dateDebut + dureeMois)
    - `grep -cE "j <= 60 && j >= -30" src/domain/locatif/alerte-fin-bail.ts` → 1 (fenêtre [-30,+60] D-SRC-05)
    - `grep -cE "cloture|successeur|statut" src/domain/locatif/alerte-fin-bail.ts` → 0 (aucune notion de clôture V1, D-FB-04)
    - `grep -E "from '(fastify|kysely|better-sqlite3|node:)" src/domain/locatif/alerte-fin-bail.ts` → aucune sortie
    - Le type de retour de `calculerAlertesFinBail` dans la signature source est `Alerte[]`
  </acceptance_criteria>
  <done>`calculerAlertesFinBail` retourne des `Alerte[]` (type='fin_bail', urlAction `/baux/{id}`) pour les baux actifs dont la fin (`dateDebut+dureeMois`) tombe dans [-30,+60], triés ASC ; fonction pure, aucune mutation `Bail` ; 7 tests verts.</done>
</task>

<task type="auto">
  <name>Task 3: Extension builder `tests/_builders/alertes.ts` + features BDD `alerte-irl` + `alerte-fin-bail` + steps + suite/lint/depcruise</name>
  <files>tests/_builders/alertes.ts, tests/bdd/features/alerte-irl.feature, tests/bdd/features/alerte-fin-bail.feature, tests/bdd/steps/alerte-irl.steps.ts, tests/bdd/steps/alerte-fin-bail.steps.ts</files>
  <read_first>
    - tests/_builders/alertes.ts (créé par 07-01 : `uneAlerte`, `uneAlerteCfe` — pattern overrides + spread)
    - tests/bdd/features/cfe-suivi-declaratif.feature (analog EXACT : Feature + Background + Scenario + tags `@phase7`/`@phaseN`)
    - tests/bdd/steps/ (un fichier steps CFE existant à imiter : Given/When/Then, World, assertions)
    - src/domain/locatif/alerte-irl.ts + src/domain/locatif/alerte-fin-bail.ts (fonctions à piloter depuis les steps)
    - tests/_builders/locatif.ts + tests/_builders/patrimoine.ts (builders réutilisés dans les steps)
  </read_first>
  <action>
    Étendre `tests/_builders/alertes.ts` (NE PAS recréer le fichier — 07-01 l'a écrit) en AJOUTANT deux builders, selon le même pattern overrides + spread que `uneAlerteCfe` :
    - `export function uneAlerteIrl(overrides: Partial<Alerte> = {}): Alerte` — défauts : `type: 'irl'`, `joursRestants: 15`, `dateEcheance: Temporal.PlainDate.from('2026-07-01')`, `libelle: 'Révision IRL'`, `urlAction: '/baux/33333333-3333-4333-8333-333333333333/indexer'`, `source: { type: 'irl', refId: '33333333-3333-4333-8333-333333333333', bienId: '11111111-1111-4111-8111-111111111111' as BienId }`, puis `...overrides`.
    - `export function uneAlerteFinBail(overrides: Partial<Alerte> = {}): Alerte` — défauts : `type: 'fin_bail'`, `joursRestants: 30`, `dateEcheance: Temporal.PlainDate.from('2026-08-01')`, `libelle: 'Fin de bail'`, `urlAction: '/baux/33333333-3333-4333-8333-333333333333'`, `source: { type: 'fin_bail', refId: '33333333-3333-4333-8333-333333333333', bienId: '11111111-1111-4111-8111-111111111111' as BienId }`, puis `...overrides`.
    Les variantes `diagnostic` seront ajoutées par 07-03 — ne pas les créer ici (pas de spéculatif).

    Créer `tests/bdd/features/alerte-irl.feature` (tags `@phase7 @phase7-alerte-irl`), reproduisant la structure de `cfe-suivi-declaratif.feature`. Scénarios obligatoires (logique juridique IRL = 100 % couverture) :
    - Alerte IRL active J-15 — bail actif, Bien classe C, aucune indexation exercice courant → 1 alerte, joursRestants = 15.
    - Pas d'alerte — Bien classé F (gel Climat) → 0 alerte (D-92).
    - Pas d'alerte — indexation déjà enregistrée sur l'exercice courant → 0 alerte (D-SRC-03).
    - Pas d'alerte — bail non activé (`actifDepuis` null) → 0 alerte.
    - Limite fenêtre — anniversaire J-30 inclus / J-31 exclu.

    Créer `tests/bdd/features/alerte-fin-bail.feature` (tags `@phase7 @phase7-alerte-fin-bail`). Scénarios :
    - Alerte fin de bail active — bail actif dont la fin est dans 30 jours → 1 alerte, joursRestants = 30.
    - Pas d'alerte — bail non activé (`actifDepuis` null) → 0 alerte.
    - Limite fenêtre — fin J+60 incluse / J+61 exclue (D-SRC-05).

    Créer les steps `tests/bdd/steps/alerte-irl.steps.ts` et `tests/bdd/steps/alerte-fin-bail.steps.ts` selon le pattern des steps CFE existants (World partagé, Given construisent baux/biens via les builders + une `Map<BailId, boolean>` pour l'indexation, When appelle `calculerAlertesIrl`/`calculerAlertesFinBail` avec un `maintenant` fixe, Then assertent le nombre d'alertes + `joursRestants` + `type`). Aucun repository réel — les steps construisent les entrées en mémoire (fonctions pures domaine).

    Puis valider la fermeture du plan : suite complète + lint + dependency-cruiser (le hardening hexagonal Phase 5.1 doit rester vert avec les 2 nouveaux fichiers `domain/locatif/alerte-*.ts`).
  </action>
  <verify>
    <automated>pnpm vitest run && pnpm cucumber-js --tags "@phase7-alerte-irl or @phase7-alerte-fin-bail" && pnpm lint && (pnpm depcruise 2>/dev/null || pnpm exec depcruise --config .dependency-cruiser.cjs src)</automated>
  </verify>
  <acceptance_criteria>
    - `tests/_builders/alertes.ts` exporte désormais `uneAlerte`, `uneAlerteCfe` (07-01), `uneAlerteIrl`, `uneAlerteFinBail`
    - `grep -c "export function uneAlerteIrl" tests/_builders/alertes.ts` → 1 ; `grep -c "export function uneAlerteFinBail" tests/_builders/alertes.ts` → 1
    - `pnpm cucumber-js --tags "@phase7-alerte-irl or @phase7-alerte-fin-bail"` → tous les scénarios passent (0 fail, 0 undefined step)
    - les 2 features couvrent : IRL {actif, gel F/G, exercice courant, fenêtre J-30/J-31} ; fin-bail {actif, fenêtre J+60/J+61}
    - `pnpm vitest run` → suite unitaire complète verte (0 fail), incluant `alerte-irl.test.ts` et `alerte-fin-bail.test.ts`
    - dependency-cruiser → 0 violation (aucune flèche `domain/locatif → infra/web/application`)
    - `pnpm lint` → 0 warning sur les fichiers créés/modifiés
  </acceptance_criteria>
  <done>Builders `uneAlerteIrl`/`uneAlerteFinBail` disponibles pour 07-04 ; logique IRL + fin de bail couverte par features BDD vertes ; suite complète + lint + dependency-cruiser au vert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Aucune nouvelle | Ce plan ne crée ni ne modifie aucune surface HTTP. Fonctions pures domaine `Locatif` + tests. Pas d'entrée utilisateur, pas de repository, pas de mutation. Les surfaces HTTP consommatrices (`GET /`, `GET /baux/indexations`) sont planifiées en 07-05/07-06. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | T (Tampering) | `Alerte.urlAction` (`/baux/{bailId}/indexer`, `/baux/{bailId}`) interpolé plus tard dans les vues EJS | accept | `bailId` est un brand type UUID v4 issu du domaine (jamais d'entrée utilisateur dans la construction). L'échappement EJS `<%= %>` au rendu sera vérifié par 07-05/07-06. Aucune route nouvelle exposée ici. |
| T-07-05 | I (Information Disclosure) | `Alerte.source.extra.adresseBien` (rue du bien dans la source IRL) | mitigate | Ne placer dans `extra` que des données déjà affichées sur les écrans Phase 1/3 (adresse du bien). Aucune donnée locataire/montant/PII sensible. Single-user local — pas de séparation de tenants. |
| T-07-06 | D (Denial of Service) | `calculerAlertesIrl`/`calculerAlertesFinBail` (boucles sur baux/biens + tri) | accept | Single-user local, volumétrie bornée (quelques baux/biens). Complexité O(n + m + n log n). Aucune récursion, aucune entrée non bornée. |
</threat_model>

<verification>
- `pnpm vitest run` — suite unitaire complète verte, incluant `tests/unit/locatif/alerte-irl.test.ts` (9 tests) et `tests/unit/locatif/alerte-fin-bail.test.ts` (7 tests), sans régression sur le reste.
- `pnpm cucumber-js --tags "@phase7-alerte-irl or @phase7-alerte-fin-bail"` — scénarios BDD verts (logique juridique IRL couverte : gel F/G, exercice courant, fenêtre ; fin de bail : actif, fenêtre [-30,+60]).
- dependency-cruiser — 0 violation : `src/domain/locatif/alerte-irl.ts` et `alerte-fin-bail.ts` n'importent rien hors `@js-temporal/polyfill` + domaine (`_shared/alerte`, `_shared/identifiants`, `bail`, `patrimoine/bien`). Aucun repository, aucun import infra.
- Aucune mutation des agrégats : `Bail`/`Bien` sont lus en readonly ; aucune nouvelle migration SQLite (la dernière reste `0022_phase6_*`).
- Multi-source coverage audit : GOAL (notifications J-30/J-7 sur IRL + fin de bail) → 2 des 4 sources livrées ✓ ; REQ DAS-02 → sources IRL + fin_bail produisent des `Alerte[]` unifiés consommables par 07-04 ✓ ; RESEARCH/PATTERNS (modèle `alerte-cfe-j30` dupliqué, helper `joursAvantEcheance` réutilisé, sections `alerte-irl.ts`/`alerte-fin-bail.ts` du Pattern Map) ✓ ; CONTEXT D-AL-01 (forme Alerte), D-AL-02 (localisation BC Locatif), D-SRC-02 (fenêtre 30 / palier J-7 hérité du partial), D-SRC-03 (filtres gel F/G + exercice courant + actif), D-SRC-05/D-FB-03 (fenêtre fin de bail [-30,+60]), D-FB-01/D-FB-04 (aucune mutation Bail, pas de clôture), D-91 (`dateAnniversaireProchaine` source canonique IRL), D-92 (gel Climat) ✓. Aucun item différé implémenté (pas de bail successeur, pas de clôture, pas de snooze).
</verification>

<success_criteria>
- `src/domain/locatif/alerte-irl.ts` expose `estAlerteIrlActive` + `calculerAlertesIrl` retournant des `Alerte[]` (type='irl') filtrés par bail actif + gel DPE F/G + exercice courant, fenêtre [-30,+30], triés ASC — 100 % couverts (unit + BDD).
- `src/domain/locatif/alerte-fin-bail.ts` expose `dateFinBail` + `estAlerteFinBailActive` + `calculerAlertesFinBail` retournant des `Alerte[]` (type='fin_bail') pour les baux actifs dont la fin tombe dans [-30,+60], triés ASC.
- Builders `uneAlerteIrl`/`uneAlerteFinBail` disponibles pour le use case agrégateur 07-04.
- Fonctions pures : zéro import technique, zéro repository, zéro mutation de `Bail`/`Bien`, dependency-cruiser vert.
- vitest, cucumber (tags phase 7 IRL + fin-bail), lint, dependency-cruiser : 0 fail / 0 warning / 0 violation.
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-notifications-d-ch-ances/07-02-SUMMARY.md` (template @/Users/valentinshodo/.claude/get-shit-done/templates/summary.md). Documenter le contrat des deux fonctions pures et la convention « filtre exercice courant pré-calculé via `indexationsParBail: Map<BailId, boolean>` injectée par le use case 07-04 » (le domaine ne touche aucun repository).
</output>
