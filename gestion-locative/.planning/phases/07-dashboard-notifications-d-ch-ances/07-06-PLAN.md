---
phase: 07-dashboard-notifications-d-ch-ances
plan: 06
type: execute
wave: 4
depends_on: ["07-05"]
files_modified:
  - src/web/routes/baux.ts
  - src/web/views/pages/baux/indexations.ejs
  - tests/bdd/features/dashboard-baux-indexations.feature
  - tests/bdd/steps/baux-indexations.steps.ts
  - tests/integration/web/accessibility-phase7.test.ts
autonomous: true
requirements: [DAS-01, DAS-02]

must_haves:
  truths:
    - "`GET /baux/indexations` rend `pages/baux/indexations.ejs` avec une table des baux dont la révision IRL est due (calculerAlertesIrl en vue COMPLÈTE, pas de top 5)"
    - "Les baux dont `bien.classeDpe ∈ {F, G}` sont exclus de la table (gel loyer Climat, déjà appliqué par calculerAlertesIrl via estGelLoyer — D-92) ET un paragraphe pédagogique explique l'exclusion (R4.3)"
    - "Chaque ligne affiche : adresse du bien (lien vers `/baux/{refId}`), nom du locataire, date anniversaire (formatDate), colonne État rendue par `partial-bandeau-alerte` en mode `inline=true`, et un lien d'action `/baux/{refId}/indexer` 'Lancer la révision'"
    - "La table est triée ASC par `joursRestants` (héritée du tri interne de calculerAlertesIrl)"
    - "Si aucune alerte IRL active → `empty-state.ejs` avec heading 'Aucune révision IRL en attente' + CTA vers `/baux` (pas de table rendue)"
    - "La route est servie sous le plugin `baux.ts` ; le routage Fastify (find-my-way) résout `/baux/indexations` (statique) avant `/baux/:id` (paramétrique) — aucune collision"
    - "Audit a11y : `axe-core` (ou assertions ARIA equivalentes app.inject) sur `GET /` et `GET /baux/indexations` → zéro violation ; table avec `<th scope='col'>` sur 5 colonnes + `aria-label='Révisions IRL à venir'`"
    - "La sidebar de `/baux/indexations` a `navActive='baux'` (pas de nouvelle entrée sidebar — D Claude's Discretion)"
  artifacts:
    - path: "src/web/routes/baux.ts"
      provides: "Route GET /baux/indexations : charge baux+biens+locataires, calculerAlertesIrl vue complète, enrichit noms locataires, render table"
      contains: "calculerAlertesIrl"
    - path: "src/web/views/pages/baux/indexations.ejs"
      provides: "Vue table révisions IRL transversale + empty-state + paragraphe pédagogique gel F/G"
      contains: "Révisions IRL à venir"
    - path: "tests/integration/web/accessibility-phase7.test.ts"
      provides: "Audit a11y axe-core (ou assertions ARIA) sur / et /baux/indexations"
      contains: "/baux/indexations"
  key_links:
    - from: "src/web/routes/baux.ts"
      to: "src/domain/locatif/alerte-irl.ts"
      via: "import { calculerAlertesIrl } + appel avec baux, biens, indexationsParBail, maintenant"
      pattern: "calculerAlertesIrl"
    - from: "src/web/routes/baux.ts"
      to: "src/web/views/pages/baux/indexations.ejs"
      via: "reply.view('pages/baux/indexations.ejs', { alertesIrl, locatairesParBail, navActive })"
      pattern: "pages/baux/indexations"
    - from: "src/web/views/pages/baux/indexations.ejs"
      to: "src/web/views/partials/partial-bandeau-alerte.ejs"
      via: "include partial-bandeau-alerte avec inline=true dans la colonne État"
      pattern: "partial-bandeau-alerte"
---

<objective>
Livrer la dernière slice verticale de DAS-01/DAS-02 : la page transversale `GET /baux/indexations` (cible du lien `Voir tout` IRL du dashboard 07-05) qui liste TOUS les baux dont la révision IRL annuelle est due (vue complète, pas top 5), DPE F/G exclus avec explication pédagogique (R4.3). Plus l'audit a11y `axe-core` (ou assertions ARIA `app.inject`) sur `GET /` (dashboard 07-05) ET `GET /baux/indexations`, exigence héritée Phase 3 (zéro violation tolérée).

La migration optionnelle des partials Phase 6/3 vers le partial unifié (D-AL-05) — reportée par 07-05 — est traitée ici : on documente le report définitif en V1.1 (les deux partials coexistent), conformément à la note de planification 07-05 (budget de ce plan réservé à la page + l'audit a11y, qui sont la vraie clôture de phase).

Purpose : depuis le dashboard, `Voir tout` sur les révisions IRL ouvre une vraie page d'action (satisfait D-90 Phase 3 deferred + D-DASH-04). C'est la dernière surface HTTP de la phase ; l'audit a11y clôt la conformité WCAG 2.1 AA sur les deux nouvelles vues.

Output : route `GET /baux/indexations` ajoutée dans `src/web/routes/baux.ts` (lecture seule, Clock-driven), vue `pages/baux/indexations.ejs` (table + empty-state + paragraphe gel F/G, partial unifié `inline=true`), feature BDD `dashboard-baux-indexations.feature` + steps, test d'intégration a11y `accessibility-phase7.test.ts`. Aucune migration SQLite, aucune mutation de domaine, aucune nouvelle route au-delà de `/baux/indexations`.
</objective>

<execution_context>
@/Users/valentinshodo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/valentinshodo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dashboard-notifications-d-ch-ances/07-CONTEXT.md
@.planning/phases/07-dashboard-notifications-d-ch-ances/07-UI-SPEC.md
@.planning/phases/07-dashboard-notifications-d-ch-ances/07-PATTERNS.md
@.planning/phases/07-dashboard-notifications-d-ch-ances/07-05-PLAN.md
@.planning/phases/07-dashboard-notifications-d-ch-ances/07-02-PLAN.md
@src/web/routes/baux.ts
@src/web/views/pages/baux/liste.ejs

<interfaces>
<!-- État actuel exact extrait du codebase. L'exécuteur n'a PAS besoin d'explorer. -->

Source de données IRL — `calculerAlertesIrl` (07-02, `src/domain/locatif/alerte-irl.ts`) :
```typescript
export function calculerAlertesIrl(
  baux: readonly Bail[],
  biens: readonly Bien[],
  indexationsParBail: Map<BailId, boolean>,  // true = indexation déjà sur exercice courant → exclu
  maintenant: Temporal.PlainDate,
): Alerte[];  // déjà trié joursRestants ASC ; gel DPE F/G déjà filtré (estGelLoyer), baux non activés déjà exclus
```
Chaque `Alerte` IRL (07-02) : `type='irl'`, `joursRestants`, `dateEcheance = bail.dateAnniversaireProchaine`, `libelle='Révision IRL'`, `urlAction='/baux/{bailId}/indexer'`, `source: { type:'irl', refId: bail.id (BailId), bienId, extra: { nomLocataire: '', adresseBien: bien.adresse.rue } }`.
ATTENTION : 07-02 met `extra.nomLocataire: ''` (placeholder vide). La route DOIT enrichir le nom du locataire elle-même (le domaine n'accède pas au LocataireRepository). Construire `locatairesParBail: Map<string, string>` (clé = `bail.id`, valeur = `${locataire.prenom} ${locataire.nom}`) et la passer en local à la vue ; la vue lit le nom via `locatairesParBail[alerte.source.refId]`.

Contrat `Alerte` (07-01, `src/domain/_shared/alerte.ts`) :
```typescript
export type TypeAlerte = 'cfe' | 'irl' | 'diagnostic' | 'fin_bail';
export interface Alerte {
  readonly type: TypeAlerte; readonly joursRestants: number; readonly dateEcheance: Temporal.PlainDate;
  readonly libelle: string; readonly urlAction: string;
  readonly source: { readonly type: TypeAlerte; readonly refId: string; readonly bienId?: BienId; readonly extra?: Record<string, unknown> };
}
```

Plugin `baux.ts` — signature opts ACTUELLE (`src/web/routes/baux.ts` lignes 48-62) :
```typescript
export async function plugin(app: FastifyInstance, opts: {
  bailRepo: BailRepository; bienRepo: BienRepository; locataireRepo: LocataireRepository;
  activiteBailDetector: ActiviteBailDetector;
  echeanceLoyerRepo?: EcheanceLoyerRepository; encaissementRepo?: EncaissementRepository;
  edlRepo?: EtatDesLieuxRepository; bailIndexationRepo?: BailIndexationRepository; clock?: Clock;
}): Promise<void> {
  const clock = opts.clock ?? new ClockSysteme();
  // app.get('/baux', ...) ligne 65, app.get('/baux/nouveau', ...) ligne 91, app.get('/baux/:id', ...) ligne 241
```
Tous les repos nécessaires (`bailRepo`, `bienRepo`, `locataireRepo`, `bailIndexationRepo`, `clock`) sont DÉJÀ dans opts — AUCUNE modification de `main.ts` requise (la registration `bauxPlugin` ligne 254 passe déjà ces deps). Ordre de routage : Fastify (find-my-way) résout les segments statiques (`/baux/indexations`) avant les paramétriques (`/baux/:id`) indépendamment de l'ordre de déclaration. Par sécurité ET lisibilité, déclarer `app.get('/baux/indexations', ...)` AVANT `app.get('/baux/:id', ...)` (ligne 241) — l'insérer juste après `app.get('/baux/nouveau', ...)` (ligne ~130, avant le POST).

Repositories — méthodes RÉELLES :
- `bailRepo.listerTous(): Promise<Bail[]>` ; `bienRepo.listerTous(): Promise<Bien[]>` ; `locataireRepo.listerTous(): Promise<Locataire[]>` (vérifier le nom exact dans `src/domain/locatif/locataire-repository.ts` — à défaut `trouverParId` par `bail.locataireId`).
- `bailIndexationRepo.dernierePourBail(bailId): Promise<BailIndexation | null>` — `BailIndexation` a `dateEffet: Temporal.PlainDate`. Pour construire `indexationsParBail` (true = déjà indexé sur l'exercice courant) : pour chaque bail, `const d = await bailIndexationRepo.dernierePourBail(bail.id)` ; `const dejaIndexe = d !== null && d.dateEffet.year === maintenant.year` (exercice courant = année civile, D-SRC-03). Map `bail.id → dejaIndexe`.

Entités domaine :
- `Bail` (`src/domain/locatif/bail.ts`) : `id: BailId`, `bienId: BienId`, `locataireId: LocataireId`, `dateDebut`, `dureeMois`, `actifDepuis`, `dateAnniversaireProchaine(today)`.
- `Locataire` (`src/domain/locatif/locataire.ts`) : `id: LocataireId`, `nom: string`, `prenom: string` (PAS de getter `nomComplet` — composer `${prenom} ${nom}`).
- `Bien` (`src/domain/patrimoine/bien.ts`) : `id: BienId`, `classeDpe`, `adresse.rue`, `estGelLoyer()`.

Partial unifié `partial-bandeau-alerte.ejs` (créé par 07-05) — props : `alerte: Alerte`, `inline?: boolean`. En mode `inline=true` : rend l'aside tri-état (role/aria-live + libellé urgence + icône aria-hidden) SANS le bloc de liens d'action (la colonne Action de la table gère l'action). À inclure dans la cellule `<td>` colonne État.

Helpers EJS disponibles en `preHandler` global (`main.ts`, injectés par 07-05) : `formatDate`, `formaterAlerteUrgence`, `iconeTypeAlerte`, `libelleTypeAlerte`. La vue les utilise sans les re-passer.

Partials EJS réutilisés : `layout-debut.ejs` / `layout-fin.ejs`, `breadcrumbs.ejs`, `empty-state.ejs` (props : `heading`, `body`, `ctaUrl`, `ctaLabel`).

Pattern test a11y EXISTANT (PAS d'axe-core dans le repo — vérifier `grep axe package.json`) : `tests/integration/web/accessibility-phase3.test.ts` utilise Vitest + `creerApp` + `app.inject()` + assertions sur le HTML (présence d'attributs ARIA : `aria-label`, `role`, `aria-current`, `<th scope="col">`, `aria-labelledby`). Reproduire CE pattern. Si `axe-core` est réellement présent dans `package.json`/`node_modules`, l'utiliser via `@axe-core/playwright` ou parsing JSDOM ; SINON (cas attendu) auditer par assertions ARIA déterministes sur le HTML rendu — c'est le pattern Phase 3 et il satisfait l'exigence "zéro violation a11y".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route `GET /baux/indexations` dans baux.ts + vue table `indexations.ejs`</name>
  <files>src/web/routes/baux.ts, src/web/views/pages/baux/indexations.ejs</files>
  <read_first>
    - src/web/routes/baux.ts (plugin actuel : opts multi-repos déjà fournis ; lire app.get('/baux/nouveau') ligne 91-130 comme modèle d'insertion AVANT app.get('/baux/:id') ligne 241 ; lire la gestion d'erreur try/catch existante)
    - src/domain/locatif/alerte-irl.ts (07-02 : signature calculerAlertesIrl(baux, biens, indexationsParBail, maintenant) — gel F/G + actif déjà filtrés en interne)
    - src/domain/locatif/bail-indexation-repository.ts (méthode dernierePourBail + champ BailIndexation.dateEffet pour construire indexationsParBail année civile)
    - src/domain/locatif/locataire.ts (nom + prenom, pas de nomComplet — composer)
    - src/domain/locatif/locataire-repository.ts (méthode listerTous ou trouverParId pour enrichir les noms)
    - src/web/views/pages/baux/liste.ejs (PATTERN layout-debut + table + empty-state Phase 1)
    - .planning/phases/07-dashboard-notifications-d-ch-ances/07-UI-SPEC.md §S6 (lignes 356-431 : markup table prescrit + colonnes + paragraphe pédagogique) et §Copywriting §Texte explicatif (lignes 724-730) et §Accessibilité §Sélecteurs ARIA (lignes 759-782)
    - src/web/views/partials/empty-state.ejs (props heading/body/ctaUrl/ctaLabel)
  </read_first>
  <action>
    Ajouter dans `src/web/routes/baux.ts` la route `app.get('/baux/indexations', async (req, reply) => { ... })`, insérée JUSTE APRÈS `app.get('/baux/nouveau', ...)` et AVANT `app.post('/baux', ...)` (donc avant `app.get('/baux/:id')` ligne 241 — sécurité de routage + lisibilité). Imports à ajouter en tête de fichier : `import { calculerAlertesIrl } from '../../domain/locatif/alerte-irl.js'` et `import { Temporal } from '@js-temporal/polyfill'` (si pas déjà importé).

    Corps de la route (lecture seule, Clock-driven) :
    1. `const maintenant = clock.aujourdhui();` (réutiliser le `clock` local déjà résolu ligne ~63 : `opts.clock ?? new ClockSysteme()`).
    2. Charger en parallèle : `const [baux, biens, locataires] = await Promise.all([opts.bailRepo.listerTous(), opts.bienRepo.listerTous(), opts.locataireRepo.listerTous()]);` (si `listerTous` n'existe pas sur locataireRepo, construire les locataires via `Promise.all(baux.map(b => opts.locataireRepo.trouverParId(b.locataireId)))`).
    3. Construire `indexationsParBail: Map<string, boolean>` : pour chaque bail, si `opts.bailIndexationRepo` est défini, `const derniere = await opts.bailIndexationRepo.dernierePourBail(bail.id);` et `dejaIndexe = derniere !== null && derniere.dateEffet.year === maintenant.year` ; sinon `false`. (exercice courant = année civile, D-SRC-03). Itérer en série ou via `Promise.all` sur les baux actifs uniquement (`bail.actifDepuis !== null`) pour limiter les requêtes.
    4. `const alertesIrl = calculerAlertesIrl(baux, biens, indexationsParBail, maintenant);` (gel DPE F/G et baux non activés sont déjà exclus par la fonction pure — NE PAS re-filtrer).
    5. Enrichir les noms : construire `const locatairesParBail: Record<string, string> = {};` en mappant chaque `bail.id → \`${locataire.prenom} ${locataire.nom}\`` pour les baux présents dans `alertesIrl` (clé = `alerte.source.refId`). Utiliser une `Map<LocataireId, Locataire>` intermédiaire pour le lookup.
    6. `return reply.view('pages/baux/indexations.ejs', { titre: 'Révisions IRL à venir', navActive: 'baux', alertesIrl, locatairesParBail });`
    JSDoc fr : Phase 7 / DAS-01 / D-DASH-04 / D-90 (page transversale Phase 3 deferred) / D-SRC-03 (exercice courant = année civile) / Clock-driven. Erreur : route lecture pure → laisser remonter au handler global Fastify. NE PAS `new Date()` / `setInterval` / cron.

    Créer `src/web/views/pages/baux/indexations.ejs` selon UI-SPEC §S6 (markup prescrit lignes 366-421). Structure EXACTE :
    - `<%- include('../../partials/layout-debut', { titre: 'Révisions IRL à venir', breadcrumbs: [{ label: 'Tableau de bord', url: '/' }, { label: 'Révisions IRL à venir' }], navActive: 'baux' }) %>` (adapter le chemin d'include au pattern réel de `liste.ejs`).
    - `<h1>Révisions IRL à venir</h1>`.
    - Paragraphe pédagogique (R4.3, copy EXACT UI-SPEC ligne 728) : `<p style="color: var(--couleur-neutre); max-width: 75ch;">Liste des baux pour lesquels la révision IRL annuelle est due dans les 30 prochains jours ou déjà dépassée. Les baux dont le DPE est classé F ou G sont exclus (gel loyer Climat — décret n° 2022-1313).</p>`.
    - Si `alertesIrl.length === 0` → `<%- include('../../partials/empty-state', { heading: "Aucune révision IRL en attente", body: "Tous vos baux éligibles sont à jour ou en dehors de la fenêtre d'anticipation (J-30).", ctaUrl: '/baux', ctaLabel: "Voir tous les baux" }) %>`.
    - Sinon → `<table aria-label="Révisions IRL à venir">` avec `<thead><tr>` de 5 `<th scope="col">` : `Bail`, `Locataire`, `Date anniversaire`, `État`, `Action`. `<tbody>` : `<% alertesIrl.forEach(alerte => { %>` une `<tr>` :
      - `<td><a href="/baux/<%= alerte.source.refId %>"><%= alerte.source.extra.adresseBien %></a></td>`
      - `<td><%= locatairesParBail[alerte.source.refId] %></td>`
      - `<td><%= formatDate(alerte.dateEcheance) %></td>`
      - `<td><%- include('../../partials/partial-bandeau-alerte', { alerte: alerte, inline: true }) %></td>` (mode compact, sans lien d'action redondant)
      - `<td><a href="/baux/<%= alerte.source.refId %>/indexer" role="button">Lancer la révision</a></td>`
    - `<%- include('../../partials/layout-fin') %>`.
    - Touch target ≥ 44px sur les `<a role="button">` (padding hérité). Copy STRICTEMENT français. Aucune nouvelle entrée sidebar (navActive='baux').
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "app.get('/baux/indexations'" src/web/routes/baux.ts` → 1
    - `grep -c "calculerAlertesIrl" src/web/routes/baux.ts` → 1
    - la déclaration `/baux/indexations` apparaît AVANT `/baux/:id` dans le fichier : `awk '/baux.indexations/{a=NR} /baux\/:id/{b=NR} END{exit !(a && a<b)}' src/web/routes/baux.ts` → exit 0
    - `grep -cE "new Date\(|Temporal\.Now|setInterval|setTimeout|cron" src/web/routes/baux.ts` → 0 dans la nouvelle route (Clock-driven ; le fichier n'introduit aucun de ces tokens)
    - `grep -c "dernierePourBail" src/web/routes/baux.ts` → ≥ 1 (filtre exercice courant)
    - `grep -c "dateEffet.year === maintenant.year" src/web/routes/baux.ts` → 1 (exercice courant = année civile, D-SRC-03)
    - `grep -c "pages/baux/indexations" src/web/routes/baux.ts` → 1
    - `grep -c "navActive: 'baux'" src/web/routes/baux.ts` → ≥ 1 dans la nouvelle route
    - `grep -c "aria-label=\"Révisions IRL à venir\"" src/web/views/pages/baux/indexations.ejs` → 1
    - `grep -c "scope=\"col\"" src/web/views/pages/baux/indexations.ejs` → 5 (5 colonnes)
    - `grep -c "partial-bandeau-alerte" src/web/views/pages/baux/indexations.ejs` → 1 ; `grep -c "inline: true" src/web/views/pages/baux/indexations.ejs` → 1
    - `grep -c "décret n° 2022-1313" src/web/views/pages/baux/indexations.ejs` → 1 (paragraphe pédagogique gel F/G, R4.3)
    - `grep -c "empty-state" src/web/views/pages/baux/indexations.ejs` → 1 (branche vide)
    - `grep -c "locatairesParBail" src/web/views/pages/baux/indexations.ejs` → ≥ 1 (nom locataire enrichi)
    - `pnpm tsc --noEmit` → 0 erreur ; `pnpm vitest run` → suite verte (aucune régression)
  </acceptance_criteria>
  <done>`GET /baux/indexations` rend la table des révisions IRL dues (vue complète, gel F/G exclu + explication, partial unifié inline, action Lancer la révision), empty-state si vide, route Clock-driven sans collision de routage, noms locataires enrichis côté route. tsc + suite verts. Aucune migration, aucune mutation, aucune modif de main.ts.</done>
</task>

<task type="auto">
  <name>Task 2: BDD `dashboard-baux-indexations.feature` + steps + audit a11y axe-core sur `/` et `/baux/indexations`</name>
  <files>tests/bdd/features/dashboard-baux-indexations.feature, tests/bdd/steps/baux-indexations.steps.ts, tests/integration/web/accessibility-phase7.test.ts</files>
  <read_first>
    - tests/integration/web/accessibility-phase3.test.ts (PATTERN EXACT audit a11y : Vitest + creerApp + DB temp + migrations + ClockFixe + app.inject + assertions ARIA sur le HTML ; PAS d'axe-core dans le repo — reproduire ce pattern d'assertions)
    - tests/bdd/features/dashboard-composition.feature (créé par 07-05 : structure feature niveau route HTTP, tags @phase7-dashboard)
    - tests/bdd/steps/dashboard.steps.ts (créé par 07-05 : harnais app de test + app.inject + builders — réutiliser le World/montage)
    - tests/_builders/locatif.ts + tests/_builders/patrimoine.ts (unBailIndexableValide, unBienValide, unBienAvecDpeF, unLocataireValide)
    - src/web/routes/baux.ts (route /baux/indexations testée)
    - .planning/phases/07-dashboard-notifications-d-ch-ances/07-UI-SPEC.md §Accessibilité (lignes 757-808 : règles WCAG + sélecteurs ARIA imposés sur / et /baux/indexations)
    - package.json (vérifier la présence/absence de axe-core / @axe-core/* avant de choisir la stratégie d'audit)
  </read_first>
  <action>
    Créer `tests/bdd/features/dashboard-baux-indexations.feature` (tags `@phase7 @phase7-baux-indexations`, en-tête commentaire référençant DAS-01 / D-DASH-04 / D-90 / D-SRC-03) au niveau ROUTE HTTP (réutiliser le harnais `app.inject({ method: 'GET', url: '/baux/indexations' })` du pattern 07-05, ClockFixe + DB peuplée via builders). Scénarios :
    - `@phase7-baux-indexations-01` Table peuplée : Given un bail actif sur un bien DPE classé C dont la révision IRL est due (anniversaire J-15) et aucune indexation cette année / When je visite `/baux/indexations` / Then la réponse contient `<table aria-label="Révisions IRL à venir">`, l'adresse du bien, le nom du locataire, et un lien `/baux/{id}/indexer`.
    - `@phase7-baux-indexations-02` Exclusion gel DPE F/G : Given un bail actif IRL-dû sur un bien DPE classé F / When je visite `/baux/indexations` / Then ce bail N'apparaît PAS dans la table (gel Climat, D-92) ET la réponse contient le texte "décret n° 2022-1313" (explication pédagogique).
    - `@phase7-baux-indexations-03` Exclusion exercice courant : Given un bail actif IRL-dû mais déjà indexé cette année civile / When je visite `/baux/indexations` / Then ce bail N'apparaît PAS (D-SRC-03).
    - `@phase7-baux-indexations-04` Empty state : Given aucun bail dont la révision IRL est due (tous hors fenêtre J-30) / When je visite `/baux/indexations` / Then la réponse contient "Aucune révision IRL en attente" + un lien vers `/baux` ET NE contient PAS de `<table`.

    Créer `tests/bdd/steps/baux-indexations.steps.ts` : monter l'app de test (réutiliser le builder d'app des steps 07-05 — DB temp + migrations + ClockFixe), peupler bien/bail/locataire/indexation via les builders, `app.inject` sur `/baux/indexations`, asserter statut HTTP + présence/absence de chaînes et sélecteurs ARIA. Réutiliser les World/step definitions partagés si le harnais existant le permet (ne pas dupliquer le montage si déjà factorisé en 07-05).

    Créer `tests/integration/web/accessibility-phase7.test.ts` (Vitest, pattern EXACT de `accessibility-phase3.test.ts`) — audit a11y sur les DEUX nouvelles surfaces Phase 7 :
    - Setup : `creerApp` avec DB temp + `appliquerToutesMigrations` + `activerPragmas` + `ClockFixe`, peuplée pour produire au moins 1 alerte critique (dashboard) et 1 ligne IRL (page indexations).
    - Audit `GET /` (dashboard 07-05) — asserter zéro violation via les sélecteurs ARIA imposés : les 4 `section[aria-labelledby]` (titre-alertes-critiques/titre-impayes/titre-actions-jour/titre-echeances-venir) avec leurs `<h2 id>` correspondants ; au moins un `aside[role="alert"][aria-live="assertive"]` OU `aside[role="status"][aria-live="polite"]` (selon les données) ; `nav[aria-label="Navigation principale"]` avec `aria-current="page"` sur l'entrée Tableau de bord ; icône d'alerte `aria-hidden="true"` (couleur jamais seule, WCAG 1.4.1).
    - Audit `GET /baux/indexations` — asserter : `table[aria-label="Révisions IRL à venir"]`, 5 `<th scope="col">`, `aside` tri-état inline avec `role`/`aria-live` dans la colonne État, liens d'action descriptifs (pas "Cliquez ici"), `aria-current="page"` sur l'entrée Baux de la sidebar.
    - SI `package.json` contient réellement `axe-core`/`@axe-core/*` : exécuter en complément l'audit programmatique axe (JSDOM + `axe.run`) et asserter `results.violations.length === 0`. SINON (cas attendu, conforme Phase 3) : l'audit par assertions ARIA déterministes ci-dessus FAIT FOI ("zéro violation" = toutes les assertions ARIA imposées passent). Documenter ce choix dans l'en-tête du fichier de test.
    Header de test fr référençant Phase 7 / WCAG 2.1 AA / DAS-01 / héritage pattern accessibility-phase3.test.ts.

    FERMETURE du plan : lancer suite complète + cucumber tags + lint + dependency-cruiser. Corriger uniquement les fichiers de ce plan en cas de violation. Vérifier `git diff --name-only` ⊆ les 5 fichiers de `files_modified`.
  </action>
  <verify>
    <automated>pnpm vitest run && pnpm exec cucumber-js --tags "@phase7-baux-indexations" && pnpm lint && (pnpm depcruise 2>/dev/null || pnpm exec depcruise --config .dependency-cruiser.cjs src)</automated>
  </verify>
  <acceptance_criteria>
    - la feature `dashboard-baux-indexations.feature` existe, taggée `@phase7-baux-indexations`, 4 scénarios
    - `pnpm exec cucumber-js --tags "@phase7-baux-indexations"` → tous verts, 0 step `undefined`/`pending`
    - le scénario gel DPE F/G asserte l'ABSENCE du bail F ET la présence de "décret n° 2022-1313"
    - le scénario empty-state asserte "Aucune révision IRL en attente" ET l'absence de `<table`
    - `tests/integration/web/accessibility-phase7.test.ts` audite `/` ET `/baux/indexations` (les deux URL apparaissent dans le fichier)
    - `grep -c "/baux/indexations" tests/integration/web/accessibility-phase7.test.ts` → ≥ 1 ; `grep -cE "inject\(.*url: '/'|url: '/' " tests/integration/web/accessibility-phase7.test.ts` → ≥ 1 (les 2 routes auditées)
    - `grep -cE "scope=\\\"col\\\"|aria-label=\\\"Révisions IRL à venir\\\"|aria-labelledby" tests/integration/web/accessibility-phase7.test.ts` → ≥ 1 (assertions ARIA déterministes)
    - `pnpm vitest run` → suite complète verte (0 fail), incluant `accessibility-phase7.test.ts`, aucune régression Phase 1-6
    - dependency-cruiser → 0 violation ; `pnpm lint` → 0 warning sur les fichiers créés
    - `git diff --name-only` ne liste AUCUN fichier hors des 5 déclarés dans `files_modified`
  </acceptance_criteria>
  <done>Feature BDD `/baux/indexations` (table, gel F/G exclu + explication, exercice courant exclu, empty-state) verte via Cucumber ; audit a11y `/` + `/baux/indexations` zéro violation (assertions ARIA pattern Phase 3) ; suite + lint + dependency-cruiser verts. DAS-01/DAS-02 entièrement livrés et conformes WCAG 2.1 AA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `GET /baux/indexations` | Nouvelle surface HTTP en LECTURE SEULE. Aucun path param (route statique), aucun body, aucune query. Pas d'entrée utilisateur non trustée. Agrège des données déjà en base (repos) et rend du HTML. Frontière de confiance principale = sortie HTML (échappement EJS). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | I (Information Disclosure) / XSS | `alerte.source.extra.adresseBien`, `locatairesParBail[refId]` (nom locataire), `alerte.urlAction` interpolés dans `indexations.ejs` et le partial inline | mitigate | Utiliser `<%= %>` (échappement HTML EJS par défaut) pour TOUTE valeur dynamique (adresse, nom locataire, dates). `<%- %>` réservé EXCLUSIVEMENT aux `include` de partials (layout, empty-state, partial-bandeau-alerte), jamais à des données. `refId` (BailId) est un UUID v4 brand-typed issu du domaine, pas d'entrée libre. |
| T-07-06-02 | I (Information Disclosure) | nom du locataire affiché en clair dans la table transversale | accept | Application single-user local (D-AL-03, VISION.md) — l'utilisateur est propriétaire de toutes les données locataires. Pas de séparation de tenants, pas de PII tierce exposée à un autre utilisateur. Donnée déjà affichée sur les écrans Phase 1/3. |
| T-07-06-03 | T (Tampering) | collision de routage `/baux/indexations` capté par `/baux/:id` → mauvaise vue | mitigate | Route statique déclarée AVANT `/baux/:id` ; find-my-way priorise de toute façon les segments statiques. Vérifié par acceptance criteria (ordre + scénario BDD-01 qui charge la table, pas la fiche bail). |
| T-07-06-04 | D (Denial of Service) | `GET /baux/indexations` boucle sur baux × `dernierePourBail` (1 requête/bail actif) | accept | Single-user local, volumétrie bornée (< 20 baux typiques LMNP). Charge attendue < 100ms en SQLite local. Pas d'entrée non bornée, pas de pagination requise V1 (UI-SPEC §note 4). |
| T-07-06-05 | I (Information Disclosure) | aucun lien externe nouveau sur cette page (les liens externes CFE restent dans le dashboard 07-05) | accept | La page `/baux/indexations` ne contient que des liens internes (`/baux/{id}`, `/baux/{id}/indexer`, `/baux`). Pas de `target="_blank"`, pas de surface tabnabbing nouvelle. |
</threat_model>

<verification>
- `pnpm tsc --noEmit` — 0 erreur. `pnpm vitest run` — suite complète verte, aucune régression Phase 1-6.
- `pnpm exec cucumber-js --tags "@phase7-baux-indexations"` — 4 scénarios (table peuplée, gel F/G exclu, exercice courant exclu, empty-state) verts.
- Audit a11y `accessibility-phase7.test.ts` — zéro violation sur `GET /` ET `GET /baux/indexations` (assertions ARIA déterministes, pattern Phase 3 ; axe programmatique en complément SI axe-core présent).
- `pnpm lint` — 0 warning ; dependency-cruiser — 0 violation.
- `git diff --name-only` ⊆ les 5 fichiers de `files_modified`. AUCUNE modification de `main.ts` (les deps `bauxPlugin` sont déjà passées). Aucune migration SQLite (dernière reste `0022_phase6_*`).
- Pattern Clock-driven : `clock.aujourdhui()` unique source de date dans la route ; `grep -E "new Date\(|Temporal\.Now|setInterval"` sur la nouvelle route → vide. Aucune mutation de domaine, route lecture seule.
- **Migration partials Phase 6/3 (D-AL-05) — décision DÉFINITIVE** : reportée en V1.1. Les deux partials (`partial-bandeau-cfe-echeance.ejs`, `partial-indexation-banner.ejs`) coexistent avec `partial-bandeau-alerte.ejs`. Le budget de ce plan est consacré à la page transversale + l'audit a11y (la vraie clôture de phase). À documenter dans `07-LEARNINGS.md`. NON-gap : explicitement optionnel dans CONTEXT.md D-AL-05.
- Multi-source coverage audit :
  - GOAL (vue synthétique + action en un clic) → le lien `Voir tout` IRL du dashboard ouvre une page d'action complète ; chaque ligne offre "Lancer la révision" (action en un clic, success-criteria #4) ✓
  - REQ DAS-01 (récap + accès "Voir tout") → page transversale livrée ✓ ; DAS-02 (notifications IRL) → la page rend la source IRL en vue complète avec tri-état conforme ✓
  - RESEARCH/PATTERNS → `baux.ts` étend l'analog, `indexations.ejs` mirror `baux/liste.ejs` + `data-table`, partial unifié `inline=true` réutilisé, `accessibility-phase7.test.ts` mirror `accessibility-phase3.test.ts` ✓ (17 analogs respectés)
  - CONTEXT → D-DASH-04 (Voir tout IRL → /baux/indexations) ✓, D-90 (page transversale Phase 3 deferred satisfaite) ✓, D-SRC-03 (filtre exercice courant année civile + gel F/G hérité de calculerAlertesIrl) ✓, D-92 (gel Climat exclu + explication R4.3) ✓, D-AL-05 (partial unifié inline ; migration reportée documentée) ✓, sidebar sobre sans nouvelle entrée (Claude's Discretion) ✓, pas de pagination V1 (Claude's Discretion) ✓.
  - Aucun item différé implémenté (pas de snooze, pas d'email, pas de KPI globaux, pas de migration forcée des partials).
- Exclusions (non-gaps) : migration partials Phase 6/3 = optionnelle D-AL-05, reportée V1.1 (documentée) ; KPI globaux / widgets recettes = hors scope DAS-01 (UI-SPEC §note 11).
</verification>

<success_criteria>
- `GET /baux/indexations` rend `pages/baux/indexations.ejs` : table des baux dont la révision IRL est due (vue complète, tri ASC par joursRestants), DPE F/G exclus avec paragraphe pédagogique (décret 2022-1313), exercice courant exclu (D-SRC-03), empty-state si vide.
- Chaque ligne : adresse bien (lien `/baux/{id}`), nom locataire enrichi côté route, date anniversaire, colonne État (partial unifié `inline=true`), action `/baux/{id}/indexer`.
- Route servie sous `baux.ts` sans collision avec `/baux/:id` (statique avant paramétrique) ; sidebar `navActive='baux'` ; aucune modif `main.ts`, aucune migration.
- Audit a11y zéro violation sur `GET /` ET `GET /baux/indexations` (assertions ARIA déterministes, pattern Phase 3 ; sélecteurs imposés UI-SPEC respectés).
- Migration partials D-AL-05 reportée V1.1 et documentée (les deux partials coexistent).
- tsc, vitest, cucumber (`@phase7-baux-indexations`), lint, dependency-cruiser : 0 fail / 0 warning / 0 violation. Lecture seule, Clock-driven strict.
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-notifications-d-ch-ances/07-06-SUMMARY.md` (template @/Users/valentinshodo/.claude/get-shit-done/templates/summary.md). Documenter : la stratégie d'enrichissement des noms locataires côté route (`locatairesParBail`), le calcul `indexationsParBail` (exercice courant = année civile via `dernierePourBail().dateEffet.year`), la décision DÉFINITIVE de reporter la migration des partials Phase 6/3 vers `partial-bandeau-alerte.ejs` en V1.1 (à reporter aussi dans `07-LEARNINGS.md`), et la stratégie d'audit a11y retenue (assertions ARIA pattern Phase 3 vs axe-core programmatique selon présence dans package.json). Phase 7 (DAS-01 + DAS-02) entièrement livrée.
</output>
