---
phase: 07-dashboard-notifications-d-ch-ances
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/domain/_shared/alerte.ts
  - src/domain/fiscalite/cfe/alerte-cfe-j30.ts
  - src/application/fiscalite/lister-alertes-cfe-actives.ts
  - tests/unit/fiscalite/alerte-cfe-j30.test.ts
  - tests/_builders/alertes.ts
autonomous: true
requirements: [DAS-02]

must_haves:
  truths:
    - "Le type partagé `Alerte` (D-AL-01) existe dans `src/domain/_shared/alerte.ts` avec discriminant `type` et helper `joursAvantEcheance` exporté"
    - "`calculerAlertesCfe` retourne des `Alerte[]` unifiés (forme D-AL-01) consommables par le futur agrégateur 07-04"
    - "Toute la suite Phase 6 reste verte : les routes fiscalité/biens et le partial `partial-bandeau-cfe-echeance.ejs` continuent de recevoir des `AlerteCfe` plats inchangés"
    - "Aucun import technique dans `domain/` — dependency-cruiser vert"
  artifacts:
    - path: "src/domain/_shared/alerte.ts"
      provides: "TypeAlerte, interface Alerte, joursAvantEcheance"
      exports: ["TypeAlerte", "Alerte", "joursAvantEcheance"]
      min_lines: 30
    - path: "src/domain/fiscalite/cfe/alerte-cfe-j30.ts"
      provides: "calculerAlertesCfe produisant Alerte[], AlerteCfe (projection plate Phase 6)"
      exports: ["AlerteCfe", "calculerAlertesCfe", "estAlerteActive", "joursAvantEcheance"]
    - path: "src/application/fiscalite/lister-alertes-cfe-actives.ts"
      provides: "Projection Alerte → AlerteCfe pour les consommateurs Phase 6"
      contains: "AlerteCfe"
    - path: "tests/_builders/alertes.ts"
      provides: "Builder uneAlerteCfe avec overrides"
      exports: ["uneAlerteCfe"]
  key_links:
    - from: "src/domain/fiscalite/cfe/alerte-cfe-j30.ts"
      to: "src/domain/_shared/alerte.ts"
      via: "import { joursAvantEcheance } + import type { Alerte }"
      pattern: "from '\\.\\./\\.\\./_shared/alerte\\.js'"
    - from: "src/application/fiscalite/lister-alertes-cfe-actives.ts"
      to: "src/domain/fiscalite/cfe/alerte-cfe-j30.ts"
      via: "calculerAlertesCfe puis mapping vers AlerteCfe plat"
      pattern: "calculerAlertesCfe"
    - from: "tests/unit/fiscalite/alerte-cfe-j30.test.ts"
      to: "src/domain/fiscalite/cfe/alerte-cfe-j30.ts"
      via: "assertions sur la nouvelle forme Alerte (source.refId, source.extra)"
      pattern: "source\\.extra"
---

<objective>
Créer le read-model unifié `Alerte` (D-AL-01) dans `src/domain/_shared/alerte.ts` avec le helper partagé `joursAvantEcheance`, puis refactorer `calculerAlertesCfe` (Phase 6) pour produire des `Alerte[]` unifiés — tout en gardant la suite Phase 6 verte via une projection plate `AlerteCfe` au niveau du use case `listerAlertesCfeActives`.

Purpose : c'est le contrat commun aux 4 sources d'alerte de la Phase 7 (DAS-02). Les 3 nouvelles fonctions pures (07-02, 07-03) et l'agrégateur (07-04) consomment ce type. Sans lui, rien d'autre ne peut démarrer — c'est le Wave 1 enabler.

Output : `src/domain/_shared/alerte.ts` (nouveau), `alerte-cfe-j30.ts` refactoré, `lister-alertes-cfe-actives.ts` adapté (projection), tests de régression Phase 6 verts, builder `tests/_builders/alertes.ts`.
</objective>

<execution_context>
@/Users/valentinshodo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/valentinshodo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dashboard-notifications-d-ch-ances/07-CONTEXT.md
@.planning/phases/07-dashboard-notifications-d-ch-ances/07-PATTERNS.md
@src/domain/fiscalite/cfe/alerte-cfe-j30.ts
@src/application/fiscalite/lister-alertes-cfe-actives.ts
@src/domain/_shared/identifiants.ts

<interfaces>
<!-- État actuel exact — extrait du codebase. L'exécuteur n'a pas besoin d'explorer. -->

État actuel de `src/domain/fiscalite/cfe/alerte-cfe-j30.ts` :

```typescript
const FENETRE_ALERTE_JOURS = 30;
const STATUTS_ALERTABLES: ReadonlySet<StatutCfe> = new Set(['non_deposee', 'deposee']);

export interface AlerteCfe {
  readonly declarationCfeId: DeclarationCfeId;
  readonly bienId: BienId;
  readonly millesime: number;
  readonly joursRestants: number;
  readonly dateEcheancePaiement: Temporal.PlainDate;
  readonly statutCfe: StatutCfe;
}

export function joursAvantEcheance(
  dateEcheance: Temporal.PlainDate,
  maintenant: Temporal.PlainDate,
): number {
  return maintenant.until(dateEcheance, { largestUnit: 'days' }).days;
}

export function estAlerteActive(d: DeclarationCfe, maintenant: Temporal.PlainDate): boolean {
  if (!STATUTS_ALERTABLES.has(d.statut)) return false;
  const j = joursAvantEcheance(d.dateEcheancePaiement, maintenant);
  return j <= FENETRE_ALERTE_JOURS && j >= -60;
}

export function calculerAlertesCfe(
  declarations: readonly DeclarationCfe[],
  maintenant: Temporal.PlainDate,
): AlerteCfe[] // → tri joursRestants ASC
```

État actuel de `src/application/fiscalite/lister-alertes-cfe-actives.ts` :

```typescript
export interface ListerAlertesCfeActivesFiltre { bienId?: BienId; }
export interface ListerAlertesCfeActivesDeps {
  cfeRepo: DeclarationCfeRepository;
  bienRepo: BienRepository;
  clock: Clock;
}
export async function listerAlertesCfeActives(
  filtre: ListerAlertesCfeActivesFiltre,
  deps: ListerAlertesCfeActivesDeps,
): Promise<AlerteCfe[]>
```

Consommateurs Phase 6 (NE PAS CASSER — ils continuent de recevoir des `AlerteCfe[]` plats) :
- `src/web/routes/fiscalite/racine.ts:92-98` — `alertesCfe: AlerteCfe[]` via `listerAlertesCfeActives({}, …)`
- `src/web/routes/biens.ts:140-149` — `alertesCfeBien: AlerteCfe[]` via `listerAlertesCfeActives({ bienId }, …)`
- `src/web/views/partials/partial-bandeau-cfe-echeance.ejs` — lit les champs PLATS : `alerte.joursRestants`, `alerte.millesime`, `alerte.dateEcheancePaiement`, `alerte.bienId`, `alerte.declarationCfeId`

Cible — interface `Alerte` D-AL-01 (forme verrouillée CONTEXT.md, ne pas modifier) :

```typescript
export type TypeAlerte = 'cfe' | 'irl' | 'diagnostic' | 'fin_bail';

export interface Alerte {
  readonly type: TypeAlerte;
  readonly joursRestants: number;       // peut être négatif
  readonly dateEcheance: Temporal.PlainDate;
  readonly libelle: string;             // pré-calculé côté domaine
  readonly urlAction: string;           // string brut V1 (Claude's Discretion tranchée)
  readonly source: {
    readonly type: TypeAlerte;
    readonly refId: string;             // DeclarationCfeId / BailId / DiagnosticId
    readonly bienId?: BienId;
    readonly extra?: Record<string, unknown>; // millesime, statutCfe, classeDpe…
  };
}
```

Brand types existants (`src/domain/_shared/identifiants.ts`) : `BienId`, `BailId`, `DiagnosticId`, `DeclarationCfeId` — format `string & { readonly __brand: '…' }`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Créer le contrat partagé `src/domain/_shared/alerte.ts` (TypeAlerte, Alerte, joursAvantEcheance)</name>
  <files>src/domain/_shared/alerte.ts, tests/unit/_shared/alerte.test.ts</files>
  <read_first>
    - src/domain/fiscalite/cfe/alerte-cfe-j30.ts (source du helper `joursAvantEcheance` à extraire, lignes 41-46)
    - src/domain/_shared/identifiants.ts (import `BienId`)
    - src/domain/_shared/clock.ts (convention fichiers _shared : pur, JSDoc fr)
    - tests/unit/fiscalite/alerte-cfe-j30.test.ts (pattern test fast-check existant pour joursAvantEcheance — monotonie)
  </read_first>
  <behavior>
    - Test 1: `joursAvantEcheance(PlainDate '2026-12-15', PlainDate '2026-11-30')` retourne 15
    - Test 2: `joursAvantEcheance(d, d)` retourne 0 (jour J)
    - Test 3: `joursAvantEcheance(PlainDate '2026-11-01', PlainDate '2026-11-30')` retourne -29 (échéance dépassée → négatif)
    - Test 4 (fast-check): monotonie — pour toute paire de dates, avancer `maintenant` d'un jour décrémente le résultat de 1
  </behavior>
  <action>
    RED : créer `tests/unit/_shared/alerte.test.ts` avec les 4 tests ci-dessus important `joursAvantEcheance` depuis `../../../src/domain/_shared/alerte.js` — le module n'existe pas encore, le test échoue.

    GREEN : créer `src/domain/_shared/alerte.ts` contenant exactement (per D-AL-01, forme verrouillée dans `<interfaces>` ci-dessus) :
    - `export type TypeAlerte = 'cfe' | 'irl' | 'diagnostic' | 'fin_bail';`
    - `export interface Alerte { … }` — la forme D-AL-01 complète, avec `import type { BienId } from './identifiants.js'` et `import { Temporal } from '@js-temporal/polyfill'`. Champ `urlAction: string` brut (Claude's Discretion tranchée : pas de template literal type, pas d'URL builder).
    - `export function joursAvantEcheance(dateEcheance: Temporal.PlainDate, maintenant: Temporal.PlainDate): number` — implémentation IDENTIQUE à celle de alerte-cfe-j30.ts : `return maintenant.until(dateEcheance, { largestUnit: 'days' }).days;`
    - JSDoc en-tête français : référencer Phase 7 / DAS-02 / D-AL-01, rappel pattern Clock-driven (le Clock n'est JAMAIS accédé ici — `maintenant` est un argument).
    - AUCUN import technique (pas d'ORM, pas de Fastify, pas de fs) — seuls `@js-temporal/polyfill` et `./identifiants.js` sont autorisés.

    Commits : `test(07-01): add failing tests for joursAvantEcheance partagé` puis `feat(07-01): create read-model Alerte unifié + joursAvantEcheance (D-AL-01)`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/_shared/alerte.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run tests/unit/_shared/alerte.test.ts` → 4 tests passent, 0 échec
    - `grep -c "export type TypeAlerte" src/domain/_shared/alerte.ts` → 1
    - `grep -c "export interface Alerte" src/domain/_shared/alerte.ts` → 1
    - `grep -c "export function joursAvantEcheance" src/domain/_shared/alerte.ts` → 1
    - `grep -E "from '(fastify|kysely|better-sqlite3|node:)" src/domain/_shared/alerte.ts` → aucune sortie (domaine pur)
    - L'interface contient les 6 champs : `type`, `joursRestants`, `dateEcheance`, `libelle`, `urlAction`, `source` (avec `source.type`, `source.refId`, `source.bienId?`, `source.extra?`)
  </acceptance_criteria>
  <done>Le contrat `Alerte` D-AL-01 existe, `joursAvantEcheance` est exporté et testé (positif/zéro/négatif/monotonie), zéro import technique.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactorer `calculerAlertesCfe` → `Alerte[]` unifié + projection `AlerteCfe` au niveau use case (Phase 6 reste vert)</name>
  <files>src/domain/fiscalite/cfe/alerte-cfe-j30.ts, src/application/fiscalite/lister-alertes-cfe-actives.ts, tests/unit/fiscalite/alerte-cfe-j30.test.ts</files>
  <read_first>
    - src/domain/fiscalite/cfe/alerte-cfe-j30.ts (fichier à refactorer — fenêtre [-60,+30] et STATUTS_ALERTABLES à NE PAS modifier)
    - src/application/fiscalite/lister-alertes-cfe-actives.ts (use case à adapter — la projection plate vit ici)
    - tests/unit/fiscalite/alerte-cfe-j30.test.ts (tests de régression à adapter : lignes 152 `alertes[0]!.statutCfe` et 153/169 `joursRestants`)
    - src/domain/_shared/alerte.ts (contrat créé en Task 1)
    - src/web/routes/fiscalite/racine.ts lignes 92-98 et src/web/routes/biens.ts lignes 140-149 (consommateurs — vérifier qu'ils restent compilables SANS modification)
  </read_first>
  <behavior>
    - Test 1 (nouvelle forme): `calculerAlertesCfe([declNonDeposee J-15], maintenant)` retourne 1 `Alerte` avec `type === 'cfe'`, `joursRestants === 15`, `dateEcheance` égale à `dateEcheancePaiement`, `source.type === 'cfe'`, `source.refId === declarationCfeId`, `source.bienId === bienId`, `source.extra.millesime === 2026`, `source.extra.statutCfe === 'non_deposee'`
    - Test 2 (urlAction): `urlAction === '/biens/{bienId}/cfe/{declarationCfeId}/editer'` (route Phase 6 existante, mapping CONTEXT.md `<specifics>`)
    - Test 3 (libelle): `libelle` contient `'CFE'` et le millésime (ex. `'CFE 2026'`)
    - Tests régression Phase 6 (adaptés, mêmes scénarios): filtres statut `payee`/`exoneree_*` → exclus ; fenêtre J-30 incluse / J-31 exclue / J-60 dépassée incluse / J-61 dépassée exclue ; tri `joursRestants` ASC `[7, 14, 30]` ; liste vide → `[]`
  </behavior>
  <action>
    Décision tranchée (recommandation CONTEXT.md Claude's Discretion, confirmée ici) : **refactor au niveau domaine** — `calculerAlertesCfe` produit directement `Alerte[]` ; la compatibilité Phase 6 est assurée par une **projection plate dans le use case**, ce qui laisse les routes `fiscalite/racine.ts`, `biens.ts` et le partial `partial-bandeau-cfe-echeance.ejs` STRICTEMENT inchangés.

    RED : adapter `tests/unit/fiscalite/alerte-cfe-j30.test.ts` — conserver tous les scénarios existants (filtres statut, fenêtre, tri, vide) mais asserter la NOUVELLE forme : ligne 152 `alertes[0]!.statutCfe` devient `alertes[0]!.source.extra?.['statutCfe']`, ajouter les assertions Test 1/2/3 ci-dessus. Lancer → échec (le domaine retourne encore `AlerteCfe[]` plat).

    GREEN — `src/domain/fiscalite/cfe/alerte-cfe-j30.ts` :
    - Ajouter `import type { Alerte } from '../../_shared/alerte.js';` et `import { joursAvantEcheance } from '../../_shared/alerte.js';`
    - SUPPRIMER l'implémentation locale de `joursAvantEcheance` et la remplacer par un ré-export : `export { joursAvantEcheance } from '../../_shared/alerte.js';` (préserve les imports Phase 6 existants depuis ce module).
    - `estAlerteActive` : INCHANGÉ (fenêtre `j <= 30 && j >= -60`, `STATUTS_ALERTABLES = {'non_deposee','deposee'}`).
    - `calculerAlertesCfe(declarations, maintenant): Alerte[]` : pour chaque déclaration alertable, construire `{ type: 'cfe', joursRestants, dateEcheance: d.dateEcheancePaiement, libelle: \`CFE ${d.millesime}\`, urlAction: \`/biens/${d.bienId}/cfe/${d.id}/editer\`, source: { type: 'cfe', refId: d.id, bienId: d.bienId, extra: { millesime: d.millesime, statutCfe: d.statut, dateEcheancePaiement: d.dateEcheancePaiement } } }`. Tri `joursRestants` ASC conservé.
    - `AlerteCfe` : conserver l'interface PLATE existante telle quelle (`declarationCfeId`, `bienId`, `millesime`, `joursRestants`, `dateEcheancePaiement`, `statutCfe`) — c'est la projection Phase 6, plus le type de retour du domaine.

    GREEN — `src/application/fiscalite/lister-alertes-cfe-actives.ts` :
    - `listerAlertesCfeActives` garde sa signature `Promise<AlerteCfe[]>` (les consommateurs Phase 6 ne changent PAS).
    - Ajouter une fonction de projection interne (non exportée ou exportée `versAlerteCfe(alerte: Alerte): AlerteCfe`) qui reconstruit le DTO plat depuis la forme unifiée : `declarationCfeId: alerte.source.refId as DeclarationCfeId`, `bienId: alerte.source.bienId!`, `millesime: alerte.source.extra.millesime as number`, `statutCfe: alerte.source.extra.statutCfe as StatutCfe`, `dateEcheancePaiement: alerte.source.extra.dateEcheancePaiement as Temporal.PlainDate`, `joursRestants: alerte.joursRestants`.
    - JSDoc fr expliquant : projection de compatibilité Phase 6 — le dashboard 07-04/07-05 consommera `calculerAlertesCfe` directement en forme unifiée.

    Vérifier la non-régression web : `pnpm tsc --noEmit` doit passer SANS toucher `src/web/routes/fiscalite/racine.ts`, `src/web/routes/biens.ts`, ni `partial-bandeau-cfe-echeance.ejs`.

    REFACTOR (si besoin) : nettoyage imports orphelins (`Temporal` si plus utilisé directement, etc.).

    Commits : `test(07-01): adapt CFE alert tests to unified Alerte shape (RED)` puis `feat(07-01): calculerAlertesCfe produit Alerte[] unifié, projection AlerteCfe au use case` (+ refactor éventuel).
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/fiscalite/alerte-cfe-j30.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run tests/unit/fiscalite/alerte-cfe-j30.test.ts` → tous les scénarios Phase 6 (filtres statut, fenêtre [-60,+30], tri ASC, vide) + nouveaux tests forme unifiée passent
    - `pnpm tsc --noEmit` → 0 erreur, sans AUCUNE modification de `src/web/routes/fiscalite/racine.ts`, `src/web/routes/biens.ts`, `src/web/views/partials/partial-bandeau-cfe-echeance.ejs` (`git diff --name-only` ne liste pas ces 3 fichiers)
    - `grep -c "FENETRE_ALERTE_JOURS = 30" src/domain/fiscalite/cfe/alerte-cfe-j30.ts` → 1 (fenêtre inchangée)
    - `grep -c "j >= -60" src/domain/fiscalite/cfe/alerte-cfe-j30.ts` → 1 (borne inférieure CFE inchangée)
    - `grep -cE "^export (interface|type) AlerteCfe" src/domain/fiscalite/cfe/alerte-cfe-j30.ts` → 1 (projection plate préservée)
    - Le type de retour de `calculerAlertesCfe` dans la signature source est `Alerte[]`
    - `grep -E "from '(fastify|kysely|better-sqlite3|node:)" src/domain/fiscalite/cfe/alerte-cfe-j30.ts` → aucune sortie
  </acceptance_criteria>
  <done>`calculerAlertesCfe` retourne `Alerte[]` unifié (type='cfe', source.refId/bienId/extra peuplés, urlAction route Phase 6) ; `listerAlertesCfeActives` projette en `AlerteCfe[]` plat ; les 3 consommateurs web Phase 6 sont intacts ; tsc + suite CFE verts.</done>
</task>

<task type="auto">
  <name>Task 3: Builder `tests/_builders/alertes.ts` + suite complète + dependency-cruiser</name>
  <files>tests/_builders/alertes.ts</files>
  <read_first>
    - tests/_builders/fiscalite.ts (pattern builder existant : fonction `unX(overrides = {})` avec defaults + spread)
    - src/domain/_shared/alerte.ts (le type à construire)
    - tests/_builders/patrimoine.ts (convention UUID v4 de test, brand types castés)
  </read_first>
  <action>
    Créer `tests/_builders/alertes.ts` selon le pattern builder du projet (`tests/_builders/fiscalite.ts`) :
    - `export function uneAlerte(overrides: Partial<Alerte> = {}): Alerte` — defaults : `type: 'cfe'`, `joursRestants: 15`, `dateEcheance: Temporal.PlainDate.from('2026-12-15')`, `libelle: 'CFE 2026'`, `urlAction: '/biens/11111111-1111-4111-8111-111111111111/cfe/22222222-2222-4222-8222-222222222222/editer'`, `source: { type: 'cfe', refId: '22222222-2222-4222-8222-222222222222', bienId: '11111111-1111-4111-8111-111111111111' as BienId, extra: { millesime: 2026, statutCfe: 'non_deposee' } }`, puis `...overrides` (le spread écrase les defaults).
    - `export function uneAlerteCfe(overrides: Partial<Alerte> = {}): Alerte` — alias sémantique de `uneAlerte` (delegate). Les variantes `irl`/`diagnostic`/`fin_bail` seront AJOUTÉES par les plans 07-02/07-03/07-04 — ne pas les créer ici (pas de spéculatif).
    - Imports : `Alerte` depuis `../../src/domain/_shared/alerte.js`, `BienId` depuis `../../src/domain/_shared/identifiants.js`, `Temporal` depuis `@js-temporal/polyfill`.

    Puis valider la fermeture du plan : suite unitaire complète + lint + dependency-cruiser (le hardening hexagonal Phase 5.1 doit rester vert avec le nouveau fichier `_shared/alerte.ts`).
  </action>
  <verify>
    <automated>pnpm vitest run && pnpm lint && pnpm depcruise 2>/dev/null || pnpm exec depcruise --config .dependency-cruiser.cjs src</automated>
  </verify>
  <acceptance_criteria>
    - `tests/_builders/alertes.ts` existe et exporte `uneAlerte` et `uneAlerteCfe`
    - `pnpm vitest run` → suite complète verte (0 fail), incluant la régression Phase 6 (`tests/unit/fiscalite/alerte-cfe-j30.test.ts`) et les tests BDD CFE existants
    - dependency-cruiser → 0 violation (aucune flèche `domain/ → infra/web`)
    - `pnpm lint` → 0 warning sur les fichiers créés/modifiés
  </acceptance_criteria>
  <done>Builder disponible pour 07-02..07-04, suite complète du projet verte, architecture hexagonale validée par dependency-cruiser.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Aucune nouvelle | Ce plan ne crée ni ne modifie aucune surface HTTP. Fonctions pures domaine + use case lecture seule existant (signature publique inchangée). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | T (Tampering) | `Alerte.urlAction` (string brut interpolé plus tard dans les vues EJS) | accept | Les `urlAction` sont construites côté domaine à partir d'identifiants UUID v4 validés par brand types — aucune entrée utilisateur n'entre dans la construction. EJS `<%= %>` échappe par défaut (plan 07-05 vérifiera l'échappement au rendu). |
| T-07-02 | I (Information Disclosure) | `Alerte.source.extra: Record<string, unknown>` | mitigate | Ne placer dans `extra` que des données déjà affichées sur les écrans Phase 6 (millesime, statutCfe, dateEcheancePaiement). Aucune donnée sensible (montants, données locataire) dans ce plan. |
| T-07-03 | D (Denial of Service) | `calculerAlertesCfe` (boucle sur déclarations) | accept | Single-user local, volumétrie bornée (≈1 déclaration CFE/bien/an). Complexité O(n log n) (tri). |
</threat_model>

<verification>
- `pnpm vitest run` — suite complète verte, y compris régression Phase 6 (unitaires CFE + BDD `@phase6` CFE).
- `pnpm tsc --noEmit` — 0 erreur ; `git diff --name-only` ne contient AUCUN fichier de `src/web/` (les consommateurs Phase 6 sont intacts).
- dependency-cruiser — 0 violation : `src/domain/_shared/alerte.ts` et `alerte-cfe-j30.ts` n'importent rien hors `@js-temporal/polyfill` + domaine.
- Multi-source coverage audit : GOAL (notifications J-30/J-7) → enabler contrat ✓ ; REQ DAS-02 → source CFE produite en forme unifiée ✓ ; RESEARCH/PATTERNS (modèle alerte-cfe-j30 canonique, helper extrait) ✓ ; CONTEXT D-AL-01 (forme Alerte exacte) ✓, D-AL-02 (localisation `_shared/alerte.ts` + refactor domaine, recommandation Claude's Discretion appliquée) ✓, D-SRC-02 (FENETRE_ALERTE_JOURS=30 inchangée) ✓. Aucun item différé implémenté (pas de snooze, pas d'email, pas de workflow bail).
</verification>

<success_criteria>
- Le type partagé `Alerte` (D-AL-01, forme exacte verrouillée) et `joursAvantEcheance` existent dans `src/domain/_shared/alerte.ts`, testés (4 scénarios dont fast-check monotonie).
- `calculerAlertesCfe` retourne `Alerte[]` unifié — consommable directement par l'agrégateur 07-04 sans mapping.
- La suite Phase 6 est verte SANS modification d'aucun fichier `src/web/` : routes et partial CFE reçoivent toujours des `AlerteCfe[]` plats via la projection du use case.
- Builder `uneAlerte`/`uneAlerteCfe` disponible pour les plans 07-02..07-04.
- tsc, lint, dependency-cruiser : 0 erreur / 0 warning / 0 violation.
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-notifications-d-ch-ances/07-01-SUMMARY.md` (template @/Users/valentinshodo/.claude/get-shit-done/templates/summary.md). Documenter la décision « refactor domaine + projection use case » pour les plans 07-02..07-05.
</output>
