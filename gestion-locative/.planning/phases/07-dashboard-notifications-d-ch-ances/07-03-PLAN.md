---
phase: 07-dashboard-notifications-d-ch-ances
plan: 03
type: tdd
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/domain/patrimoine/alerte-diagnostic.ts
  - tests/unit/patrimoine/alerte-diagnostic.test.ts
  - tests/bdd/features/alerte-diagnostic.feature
  - tests/bdd/step_definitions/alerte-diagnostic.steps.ts
autonomous: true
requirements: [DAS-02]

must_haves:
  truths:
    - "`calculerAlertesDiagnostic(biens, maintenant)` retourne 1 `Alerte` par diagnostic actif expirant (DPE/gaz/élec), 0 pour les ERP"
    - "Un ERP (`dateExpiration === null`) ne produit JAMAIS d'alerte (D-77 / D-SRC-03 — validité illimitée)"
    - "Un Bien avec DPE + gaz + élec actifs dans la fenêtre produit jusqu'à 3 alertes distinctes (D-SRC-04 — granularité 1 alerte/diagnostic actif)"
    - "Fenêtre `[-30, +30]` : un diagnostic déjà expiré (jusqu'à J-30) reste visible (miroir D-80), au-delà de +30 ou en deçà de -30 = pas d'alerte"
    - "Seul le diagnostic actif par type est considéré (le plus récent par dateEmission — D-79), pas les versions périmées remplacées"
    - "Aucun import technique dans `src/domain/patrimoine/alerte-diagnostic.ts` — dependency-cruiser vert"
  artifacts:
    - path: "src/domain/patrimoine/alerte-diagnostic.ts"
      provides: "Fonction pure calculerAlertesDiagnostic (BC Patrimoine) produisant Alerte[]"
      exports: ["calculerAlertesDiagnostic"]
      min_lines: 30
    - path: "tests/unit/patrimoine/alerte-diagnostic.test.ts"
      provides: "Couverture unitaire : ERP exclu, fenêtre [-30,+30], granularité multi-diagnostic, tri ASC, actif uniquement"
    - path: "tests/bdd/features/alerte-diagnostic.feature"
      provides: "Scénarios juridiques : ERP exclu, granularité, fenêtre, déjà expiré visible"
    - path: "tests/bdd/step_definitions/alerte-diagnostic.steps.ts"
      provides: "Steps appelant directement calculerAlertesDiagnostic (domaine pur, pas de HTTP)"
  key_links:
    - from: "src/domain/patrimoine/alerte-diagnostic.ts"
      to: "src/domain/_shared/alerte.ts"
      via: "import type { Alerte } + import { joursAvantEcheance }"
      pattern: "from '\\.\\./_shared/alerte\\.js'"
    - from: "src/domain/patrimoine/alerte-diagnostic.ts"
      to: "src/domain/patrimoine/bien.ts"
      via: "lecture bien.diagnostics / bien.diagnosticActif(type)"
      pattern: "diagnosticActif|\\.diagnostics"
    - from: "tests/bdd/step_definitions/alerte-diagnostic.steps.ts"
      to: "src/domain/patrimoine/alerte-diagnostic.ts"
      via: "appel direct calculerAlertesDiagnostic(biens, maintenant)"
      pattern: "calculerAlertesDiagnostic"
---

<objective>
Créer la fonction pure du BC Patrimoine `calculerAlertesDiagnostic(biens, maintenant): Alerte[]` (D-AL-02) dans `src/domain/patrimoine/alerte-diagnostic.ts`. Elle produit une `Alerte` unifiée (forme D-AL-01 issue de 07-01) par diagnostic actif (DPE / gaz / élec) dont la `dateExpiration` tombe dans la fenêtre `[-30, +30]`. Les ERP sont exclus (validité illimitée — D-77).

Purpose : c'est l'une des 4 sources d'alerte de DAS-02. Parallèle au plan 07-02 (locatif), fichiers patrimoine disjoints. Consommée ensuite par l'agrégateur `calculerToutesAlertes` (07-04). 100 % de couverture (logique juridique de validité des diagnostics — chaque exception du droit a son scénario, cf. BDD_PRACTICES.md).

Output : `src/domain/patrimoine/alerte-diagnostic.ts` (nouveau), tests unitaires fast-check/Vitest, feature BDD `alerte-diagnostic.feature` + steps appelant le domaine directement.
</objective>

<execution_context>
@/Users/valentinshodo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/valentinshodo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dashboard-notifications-d-ch-ances/07-CONTEXT.md
@.planning/phases/07-dashboard-notifications-d-ch-ances/07-PATTERNS.md
@src/domain/patrimoine/diagnostic.ts
@src/domain/patrimoine/bien.ts

<interfaces>
<!-- État actuel exact — extrait du codebase. L'exécuteur n'a pas besoin d'explorer. -->

Contrat `Alerte` D-AL-01 créé par 07-01 dans `src/domain/_shared/alerte.ts` (forme verrouillée — NE PAS modifier) :

```typescript
export type TypeAlerte = 'cfe' | 'irl' | 'diagnostic' | 'fin_bail';

export interface Alerte {
  readonly type: TypeAlerte;
  readonly joursRestants: number;       // peut être négatif
  readonly dateEcheance: Temporal.PlainDate;
  readonly libelle: string;             // pré-calculé côté domaine
  readonly urlAction: string;           // string brut V1
  readonly source: {
    readonly type: TypeAlerte;
    readonly refId: string;             // ici : DiagnosticId
    readonly bienId?: BienId;
    readonly extra?: Record<string, unknown>; // ici : { typeDiagnostic }
  };
}

export function joursAvantEcheance(
  dateEcheance: Temporal.PlainDate,
  maintenant: Temporal.PlainDate,
): number; // = maintenant.until(dateEcheance, { largestUnit: 'days' }).days
```

`src/domain/patrimoine/diagnostic.ts` (entité existante, NE PAS modifier) :

```typescript
export class Diagnostic {
  readonly id: DiagnosticId;
  readonly type: TypeDiagnostic;                     // 'dpe' | 'gaz' | 'elec' | 'erp'
  readonly dateEmission: Temporal.PlainDate;
  readonly dateExpiration: Temporal.PlainDate | null; // null UNIQUEMENT pour ERP (D-77)
  readonly classeDpe: ClasseDpe | null;
  static creer(props): Diagnostic;
  estExpire(today: Temporal.PlainDate): boolean;     // false si dateExpiration === null
}
```

`src/domain/patrimoine/bien.ts` (agrégat existant, NE PAS modifier) :

```typescript
export class Bien {
  readonly id: BienId;
  readonly diagnostics: ReadonlyArray<Diagnostic>;
  // D-79 : diagnostic actif = le plus récent par dateEmission pour un type, ou null
  diagnosticActif(type: TypeDiagnostic): Diagnostic | null;
  estGelLoyer(): boolean;
}
```

`src/domain/_shared/duree-validite-diagnostic.ts` (existant) :

```typescript
export type TypeDiagnostic = 'dpe' | 'gaz' | 'elec' | 'erp';
export const TYPES_DIAGNOSTIC: TypeDiagnostic[] = ['dpe', 'gaz', 'elec', 'erp'];
// DUREES_VALIDITE : dpe 10 ans, gaz/élec 6 ans, erp null (illimité)
```

Builders existants `tests/_builders/patrimoine.ts` (à réutiliser dans les tests) :

```typescript
unDiagnosticDpeValide({ dateEmission?, classeDpe? }): Diagnostic   // expire = dateEmission + 10 ans
unDiagnosticGazValide({ dateEmission? }): Diagnostic               // expire = dateEmission + 6 ans
unDiagnosticElecValide({ dateEmission? }): Diagnostic              // expire = dateEmission + 6 ans
unDiagnosticErpValide({ dateEmission? }): Diagnostic               // dateExpiration = null
unBienValide({ id?, diagnostics?, classeDpe?, ... }): Bien
```

Mapping `urlAction` (CONTEXT.md `<specifics>`, route Phase 3 existante) :
- `type='diagnostic'` → `/biens/{bienId}/diagnostics` + ancre `#diag-{type}` (ancre HTML, zéro nouvelle route — Claude's Discretion tranchée CONTEXT.md). Forme retenue : `/biens/${bien.id}/diagnostics#diag-${diag.type}`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD `calculerAlertesDiagnostic` — RED tests unitaires puis GREEN fonction pure</name>
  <files>src/domain/patrimoine/alerte-diagnostic.ts, tests/unit/patrimoine/alerte-diagnostic.test.ts</files>
  <read_first>
    - src/domain/fiscalite/cfe/alerte-cfe-j30.ts (MODÈLE EXACT à dupliquer : structure boucle + filtre fenêtre + push Alerte + tri ASC)
    - src/domain/_shared/alerte.ts (contrat Alerte + joursAvantEcheance créés par 07-01 — import obligatoire, NE PAS redéfinir joursAvantEcheance localement)
    - src/domain/patrimoine/diagnostic.ts (dateExpiration null = ERP ; type ∈ dpe/gaz/elec/erp)
    - src/domain/patrimoine/bien.ts (lignes 145-163 : diagnosticActif(type) D-79 + diagnostics ReadonlyArray)
    - src/domain/_shared/duree-validite-diagnostic.ts (TYPES_DIAGNOSTIC, exclusion ERP via durée null)
    - tests/_builders/patrimoine.ts (builders unDiagnosticDpeValide/Gaz/Elec/Erp + unBienValide — à réutiliser, NE PAS recréer)
    - tests/unit/fiscalite/alerte-cfe-j30.test.ts (style test fast-check + Vitest : describe/it, Temporal.PlainDate.from, assertions sur joursRestants/tri)
  </read_first>
  <behavior>
    - Test ERP exclu : Bien avec un seul diagnostic ERP (`dateExpiration === null`), `maintenant` quelconque → `calculerAlertesDiagnostic([bien], maintenant)` retourne `[]` (0 alerte). C'est l'exception juridique D-77 / D-SRC-03.
    - Test DPE dans fenêtre J-15 : DPE émis le `'2025-12-15'` (expire `'2035-12-15'`), `maintenant = '2035-12-01'` → 1 alerte `type === 'diagnostic'`, `joursRestants === 14`, `dateEcheance` = `'2035-12-15'`, `source.type === 'diagnostic'`, `source.refId === diag.id`, `source.bienId === bien.id`, `source.extra.typeDiagnostic === 'dpe'`, `urlAction === '/biens/{bienId}/diagnostics#diag-dpe'`.
    - Test borne haute J-30 incluse / J-31 exclue : diagnostic expirant exactement à J+30 → 1 alerte ; à J+31 → `[]`.
    - Test borne basse déjà expiré J-30 inclus / J-31 exclu : diagnostic expiré depuis 30 jours (`joursRestants === -30`) → 1 alerte (miroir D-80) ; expiré depuis 31 jours → `[]`.
    - Test granularité D-SRC-04 : Bien avec DPE + gaz + élec tous actifs et dans la fenêtre → 3 alertes distinctes, chacune son `dateEcheance`/`joursRestants`/`source.refId` propres.
    - Test actif uniquement D-79 : Bien avec 2 DPE (un ancien remplacé + un récent), seul le DPE actif (le plus récent par dateEmission) dans la fenêtre → 1 alerte (pas l'ancien, même s'il tomberait aussi dans la fenêtre).
    - Test tri ASC : plusieurs alertes multi-Bien → tableau trié par `joursRestants` croissant.
    - Test liste vide : `calculerAlertesDiagnostic([], maintenant)` → `[]`.
  </behavior>
  <action>
    RED : créer `tests/unit/patrimoine/alerte-diagnostic.test.ts` couvrant les 8 comportements ci-dessus. Importer `calculerAlertesDiagnostic` depuis `../../../src/domain/patrimoine/alerte-diagnostic.js` (module pas encore créé → échec). Réutiliser les builders `unBienValide`, `unDiagnosticDpeValide`, `unDiagnosticGazValide`, `unDiagnosticElecValide`, `unDiagnosticErpValide` de `tests/_builders/patrimoine.js`. Construire les diagnostics avec une `dateEmission` choisie pour que `dateExpiration` (= emission + 10/6/6 ans) tombe à la position voulue par rapport à `maintenant`. Ajouter au moins un invariant fast-check : pour tout diagnostic actif non-ERP dont `dateExpiration ∈ [maintenant-30, maintenant+30]`, exactement 1 alerte est produite avec `joursRestants === joursAvantEcheance(dateExpiration, maintenant)`. Lancer → rouge.

    GREEN : créer `src/domain/patrimoine/alerte-diagnostic.ts` en dupliquant EXACTEMENT la structure de `alerte-cfe-j30.ts` :
    - Imports autorisés UNIQUEMENT : `import { Temporal } from '@js-temporal/polyfill';`, `import type { Bien } from './bien.js';`, `import type { Alerte } from '../_shared/alerte.js';`, `import { joursAvantEcheance } from '../_shared/alerte.js';`, `import { TYPES_DIAGNOSTIC, type TypeDiagnostic } from '../_shared/duree-validite-diagnostic.js';`. AUCUN import technique (fastify, kysely, better-sqlite3, node:).
    - Constante `const FENETRE_ALERTE_JOURS = 30;` (D-SRC-02, alignée CFE). Borne basse `-30` (D-SRC-03 diagnostic).
    - `export function calculerAlertesDiagnostic(biens: readonly Bien[], maintenant: Temporal.PlainDate): Alerte[]`.
    - Pour chaque `bien`, pour chaque `type` de `TYPES_DIAGNOSTIC`, récupérer `bien.diagnosticActif(type)` (garantit le diagnostic ACTIF par type — D-79, évite les versions remplacées). Si `null` → continuer. Si `diag.dateExpiration === null` (ERP) → continuer (exclusion D-77 / D-SRC-03). Calculer `j = joursAvantEcheance(diag.dateExpiration, maintenant)`. Si `j > FENETRE_ALERTE_JOURS || j < -30` → continuer (hors fenêtre `[-30, +30]`). Sinon construire l'`Alerte` : `{ type: 'diagnostic', joursRestants: j, dateEcheance: diag.dateExpiration, libelle: \`Diagnostic ${diag.type.toUpperCase()}\`, urlAction: \`/biens/${bien.id}/diagnostics#diag-${diag.type}\`, source: { type: 'diagnostic', refId: diag.id, bienId: bien.id, extra: { typeDiagnostic: diag.type } } }`.
    - Itérer sur `TYPES_DIAGNOSTIC` plutôt que sur `bien.diagnostics` brut garantit la granularité D-SRC-04 (1 alerte par diagnostic actif par type) ET le filtre actif D-79 en une seule passe ; ne PAS itérer sur `bien.diagnostics` directement (sinon les versions remplacées produiraient des doublons).
    - Après la double boucle : `alertes.sort((a, b) => a.joursRestants - b.joursRestants);` puis `return alertes;`.
    - JSDoc d'en-tête français : référencer Phase 7 / DAS-02 / D-AL-02 / D-SRC-03 / D-SRC-04 / D-77 (ERP exclu) / D-79 (diagnostic actif) / D-80 (déjà expiré reste visible). Rappel : fonction pure, `maintenant` passé en argument, jamais de `Temporal.Now` ni `Clock` ici (pattern Clock-driven, le Clock vit dans le use case 07-04).

    REFACTOR (si besoin) : nettoyer imports orphelins. Garder la fonction sous complexité cyclomatique 10 (gate craftsmanship).

    Commits : `test(07-03): add failing tests for calculerAlertesDiagnostic (ERP exclu, fenêtre, granularité)` puis `feat(07-03): calculerAlertesDiagnostic — alertes DPE/gaz/élec J-30/J-7 (BC Patrimoine, D-AL-02)`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/patrimoine/alerte-diagnostic.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run tests/unit/patrimoine/alerte-diagnostic.test.ts` → tous les tests passent (ERP exclu, fenêtre [-30,+30], bornes J-30/J-31 et J+30/J+31, granularité 3 alertes, actif uniquement, tri ASC, vide), 0 échec
    - `grep -c "export function calculerAlertesDiagnostic" src/domain/patrimoine/alerte-diagnostic.ts` → 1
    - `grep -c "dateExpiration === null" src/domain/patrimoine/alerte-diagnostic.ts` → 1 (exclusion ERP explicite)
    - `grep -c "FENETRE_ALERTE_JOURS = 30" src/domain/patrimoine/alerte-diagnostic.ts` → 1
    - `grep -c "j < -30" src/domain/patrimoine/alerte-diagnostic.ts` → 1 (borne basse fenêtre)
    - `grep -c "from '../_shared/alerte.js'" src/domain/patrimoine/alerte-diagnostic.ts` → 2 (import type Alerte + import joursAvantEcheance — pas de redéfinition locale)
    - `grep -E "from '(fastify|kysely|better-sqlite3|node:)" src/domain/patrimoine/alerte-diagnostic.ts` → aucune sortie (domaine pur)
    - `pnpm tsc --noEmit` → 0 erreur
  </acceptance_criteria>
  <done>`calculerAlertesDiagnostic` retourne `Alerte[]` unifié (type='diagnostic'), ERP exclu, fenêtre `[-30,+30]`, granularité 1 alerte/diagnostic actif, tri ASC ; tests unitaires + fast-check verts ; zéro import technique.</done>
</task>

<task type="auto">
  <name>Task 2: Feature BDD `alerte-diagnostic.feature` + steps (couverture juridique outside-in)</name>
  <files>tests/bdd/features/alerte-diagnostic.feature, tests/bdd/step_definitions/alerte-diagnostic.steps.ts</files>
  <read_first>
    - tests/bdd/features/cfe-suivi-declaratif.feature (structure feature : commentaire en-tête, tags @phaseN, Background, Scenario)
    - tests/bdd/features/diagnostics.feature (vocabulaire métier diagnostic existant : dpe/gaz/elec/erp, dateEmission, dateExpiration)
    - tests/bdd/step_definitions/cfe.steps.ts (style step_definitions du projet : Given/When/Then, World, assertions)
    - src/domain/patrimoine/alerte-diagnostic.ts (la fonction créée en Task 1 — les steps l'appellent directement, domaine pur, pas de HTTP)
    - tests/_builders/patrimoine.ts (builders à réutiliser dans les steps)
  </read_first>
  <action>
    Créer `tests/bdd/features/alerte-diagnostic.feature` avec tags `@phase7 @phase7-alerte-diagnostic` et en-tête commentaire référençant DAS-02 / D-77 / D-SRC-03 / D-SRC-04 / D-80. Scénarios (1 par règle juridique — couverture obligatoire) :
    - `@phase7-alerte-diagnostic-01` ERP exclu : Given un Bien avec un diagnostic ERP / When je calcule les alertes diagnostic à une date quelconque / Then aucune alerte n'est retournée (validité illimitée D-77).
    - `@phase7-alerte-diagnostic-02` DPE dans la fenêtre J-15 : Given un Bien avec un DPE expirant dans 15 jours / When je calcule les alertes diagnostic / Then une alerte diagnostic de type "dpe" est retournée avec joursRestants = 15.
    - `@phase7-alerte-diagnostic-03` granularité D-SRC-04 : Given un Bien avec DPE, gaz et élec actifs tous expirant dans la fenêtre / When je calcule les alertes diagnostic / Then 3 alertes diagnostic distinctes sont retournées.
    - `@phase7-alerte-diagnostic-04` déjà expiré reste visible (miroir D-80) : Given un Bien avec un gaz expiré depuis 10 jours / When je calcule les alertes diagnostic / Then une alerte diagnostic de type "gaz" est retournée avec joursRestants = -10.
    - `@phase7-alerte-diagnostic-05` hors fenêtre haute : Given un Bien avec un élec expirant dans 45 jours / When je calcule les alertes diagnostic / Then aucune alerte n'est retournée.

    Créer `tests/bdd/step_definitions/alerte-diagnostic.steps.ts` selon le style du projet (cf. cfe.steps.ts) : utiliser le World Cucumber pour stocker le `Bien` construit (builders `unBienValide` + `unDiagnosticDpeValide`/`Gaz`/`Elec`/`Erp` avec `dateEmission` calée pour atteindre la `dateExpiration` voulue par rapport à une date `maintenant` fixe) et le résultat de `calculerAlertesDiagnostic([bien], maintenant)`. Les steps appellent la fonction DOMAINE directement (pas de serveur Fastify — c'est une fonction pure, coverage logique). `maintenant` = `Temporal.PlainDate.from('2026-06-11')` fixe (déterminisme). Asserter avec `expect` (vitest/chai selon convention du projet — répliquer cfe.steps.ts).

    Ne PAS créer de step HTTP, ne PAS toucher au serveur, ne PAS ajouter de route (la route dashboard est plan 07-05).

    Commit : `test(07-03): BDD alerte-diagnostic — ERP exclu, granularité, fenêtre, déjà expiré`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/patrimoine/alerte-diagnostic.test.ts && pnpm test:bdd -- --tags '@phase7-alerte-diagnostic' 2>/dev/null || pnpm exec cucumber-js --tags '@phase7-alerte-diagnostic'</automated>
  </verify>
  <acceptance_criteria>
    - La feature contient exactement 5 scénarios taggés `@phase7-alerte-diagnostic-01..05`
    - L'exécution Cucumber des scénarios `@phase7-alerte-diagnostic` → tous verts (5/5), 0 step `undefined`/`pending`
    - `grep -c "calculerAlertesDiagnostic" tests/bdd/step_definitions/alerte-diagnostic.steps.ts` → ≥ 1 (appel domaine direct)
    - `grep -cE "(supertest|app\\.inject|fastify)" tests/bdd/step_definitions/alerte-diagnostic.steps.ts` → 0 (pas de couche HTTP)
    - Le scénario 01 (ERP) asserte 0 alerte ; le scénario 04 asserte `joursRestants = -10` (déjà expiré visible)
  </acceptance_criteria>
  <done>Feature `alerte-diagnostic.feature` (5 scénarios juridiques) verte via Cucumber, steps appelant le domaine pur, exclusion ERP + granularité + fenêtre + déjà-expiré couverts.</done>
</task>

<task type="auto">
  <name>Task 3: Fermeture — suite complète + lint + dependency-cruiser</name>
  <files>src/domain/patrimoine/alerte-diagnostic.ts</files>
  <read_first>
    - .dependency-cruiser.cjs (règles hexagonales — domain/ ne doit importer aucun infra/web)
    - src/domain/patrimoine/alerte-diagnostic.ts (fichier à valider)
  </read_first>
  <action>
    Aucune nouvelle écriture de logique. Valider la fermeture du plan : lancer la suite complète + le lint + dependency-cruiser pour garantir que le nouveau fichier `domain/patrimoine/alerte-diagnostic.ts` respecte le hardening hexagonal (Phase 5.1) et n'introduit aucune régression. Si une violation apparaît (import interdit, warning lint, complexité > 10), corriger UNIQUEMENT le fichier créé en Task 1 (pas de modification d'autres fichiers — ce plan est lecture seule sur le reste du codebase).
  </action>
  <verify>
    <automated>pnpm vitest run && pnpm lint && (pnpm depcruise 2>/dev/null || pnpm exec depcruise --config .dependency-cruiser.cjs src)</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run` → suite complète verte (0 fail), aucune régression sur les autres BC
    - dependency-cruiser → 0 violation (aucune flèche `domain/patrimoine → infrastructure/web`)
    - `pnpm lint` → 0 warning sur `src/domain/patrimoine/alerte-diagnostic.ts` et les fichiers de test créés
    - `git diff --name-only` ne liste AUCUN fichier hors des 4 déclarés dans `files_modified`
  </acceptance_criteria>
  <done>Suite complète du projet verte, architecture hexagonale validée, source diagnostic prête à être consommée par l'agrégateur 07-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Aucune nouvelle | Ce plan ne crée ni ne modifie aucune surface HTTP. Fonction pure du domaine `calculerAlertesDiagnostic` lue plus tard par l'agrégateur (07-04) puis rendue par le dashboard (07-05). Aucune entrée utilisateur non trustée n'entre dans ce plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | T (Tampering) | `Alerte.urlAction` (`/biens/${bien.id}/diagnostics#diag-${diag.type}`) interpolé plus tard dans EJS | accept | `bien.id` est un `BienId` (brand type UUID v4 validé à la création du Bien) ; `diag.type` ∈ ensemble fermé `TYPES_DIAGNOSTIC`. Aucune entrée utilisateur libre dans la construction. L'échappement de sortie EJS `<%= %>` est vérifié par le plan 07-05 au rendu. |
| T-07-03-02 | I (Information Disclosure) | `Alerte.source.extra: { typeDiagnostic }` | mitigate | `extra` ne contient que le type de diagnostic (donnée non sensible, déjà affichée sur la fiche Bien Phase 3). Aucune donnée locataire, aucun montant, aucune PII. |
| T-07-03-03 | D (Denial of Service) | double boucle `biens × TYPES_DIAGNOSTIC` | accept | Single-user local, volumétrie bornée (≈ quelques Biens × 4 types). Complexité O(n) + tri O(m log m), m petit. |
</threat_model>

<verification>
- `pnpm vitest run` — suite complète verte, y compris la nouvelle suite unitaire diagnostic et les BDD `@phase7-alerte-diagnostic`.
- `pnpm tsc --noEmit` — 0 erreur. `git diff --name-only` ne contient AUCUN fichier hors des 4 de `files_modified` (lecture seule sur le reste).
- dependency-cruiser — 0 violation : `src/domain/patrimoine/alerte-diagnostic.ts` n'importe que `@js-temporal/polyfill` + `_shared/alerte.ts` + `_shared/duree-validite-diagnostic.ts` + `./bien.ts`.
- Multi-source coverage audit : GOAL (notifications J-30/J-7 sur diagnostics) → source diagnostic produite ✓ ; REQ DAS-02 (notifications agrégées) → 1/4 sources livrée ✓ ; RESEARCH/PATTERNS (modèle alerte-cfe-j30 canonique dupliqué, fenêtre [-30,+30] PATTERNS §alerte-diagnostic) ✓ ; CONTEXT D-AL-02 (localisation BC Patrimoine) ✓, D-77 (ERP exclu) ✓, D-SRC-03 (filtre dateExpiration ≠ null + diagnostic actif) ✓, D-SRC-04 (granularité 1 alerte/diagnostic actif) ✓, D-79 (diagnostic actif le plus récent) ✓, D-80 (déjà expiré reste visible, miroir) ✓. Aucun item différé implémenté (pas de DOC-03 justificatifs 10 ans, pas de snooze, pas d'email).
</verification>

<success_criteria>
- `calculerAlertesDiagnostic(biens, maintenant)` existe dans `src/domain/patrimoine/alerte-diagnostic.ts`, fonction pure, et produit des `Alerte[]` unifiés (forme D-AL-01).
- Les ERP ne produisent jamais d'alerte (validité illimitée D-77) — couvert par test unitaire ET scénario BDD dédié.
- Granularité D-SRC-04 : 1 alerte par diagnostic actif (DPE/gaz/élec), jusqu'à 3 par Bien.
- Fenêtre `[-30, +30]` respectée (bornes incluses J-30/J+30, déjà expiré jusqu'à -30 visible miroir D-80).
- Seul le diagnostic actif par type (D-79) est considéré ; tri `joursRestants` ASC.
- tsc, lint, dependency-cruiser : 0 erreur / 0 warning / 0 violation. Source consommable directement par l'agrégateur 07-04.
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-notifications-d-ch-ances/07-03-SUMMARY.md` (template @/Users/valentinshodo/.claude/get-shit-done/templates/summary.md). Documenter la signature exacte `calculerAlertesDiagnostic(biens, maintenant): Alerte[]` et la forme `source.extra = { typeDiagnostic }` pour l'agrégateur 07-04.
</output>
