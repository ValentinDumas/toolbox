<!doctype html>
<html lang="fr">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= titre %> — Gestion locative</title>
    <link rel="stylesheet" href="/styles/pico.min.css" />
  </head>
  <body>
    <header>
      <nav>
        <ul>
          <li><strong>Gestion locative</strong></li>
        </ul>
        <ul>
          <li><a href="/biens"<% if (locals.navActive === 'biens') { %> aria-current="page"<% } %>>Biens</a></li>
          <li><a href="/locataires"<% if (locals.navActive === 'locataires') { %> aria-current="page"<% } %>>Locataires</a></li>
          <li><a href="/baux"<% if (locals.navActive === 'baux') { %> aria-current="page"<% } %>>Baux</a></li>
        </ul>
      </nav>
    </header>

    <main class="container">
      <% if (locals.banniereSuccess) { %>
        <aside role="status" aria-live="polite">
          <%= banniereSuccess %>
        </aside>
      <% } %>
