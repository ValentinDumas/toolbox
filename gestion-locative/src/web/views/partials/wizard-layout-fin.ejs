      <% if (locals.etapePrecedente) { %>
        <nav aria-label="Navigation du wizard">
          <a href="<%= etapePrecedente.url %>">← Étape précédente</a>
        </nav>
      <% } %>

    </main>
  </body>
</html>
