/**
 * Formate une année fiscale (D-107).
 *   formaterAnneeFiscale(2026) === "Année fiscale 2026"
 */
export function formaterAnneeFiscale(annee: number): string {
  return `Année fiscale ${annee}`;
}
