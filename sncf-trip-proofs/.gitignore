# Local config (personal paths — do not commit)
config.json

# Cache de texte extrait (dérivé, indexé par checksum — jamais versionné)
.sncf-text-cache.json

# Python virtualenv
.venv/
__pycache__/
*.pyc

# Corpus local, ou lien symbolique vers le corpus. Les justificatifs sont
# nominatifs (nom, trajets, dates) : ni eux ni le lien qui pointe dessus ne
# doivent partir sur un remote.
inbox
output
curated
bilans
archive
*.pdf
